# BUG-0033: practice session L2 reviews exposed first-question and idempotency drift

> 模块: practice | 严重度: high | 状态: resolved
> 发现日期: 2026-05-10 | 关联 Commit: `fix(backend-practice): remediate session orchestration L2 findings`
> 关联日志: `-`

## 1 现象

- L2 code review 发现 `startPracticeSession` 首题解析仍只接受 `questionText` / `questionIntent`，而 F3 `practice.session.first_question` prompt 要求模型返回 `question` / `intent` / `focus_dimension` / `expected_signals` / `time_budget_seconds`。
- 同一路径把非 JSON AI 输出当作成功首题返回，削弱了 `AI_OUTPUT_INVALID` gate。
- `startPracticeSession` success replay 读取当前 `practice_sessions` / `practice_turns` 状态，而不是读取首次成功时写入 `idempotency_records.response_body` 的 response snapshot。
- `000003_practice_idempotency_baseline.down.sql` 仍会 drop baseline-owned `idempotency_records` 表和 index。
- Follow-up review 继续发现 `ResolveActive` 在 reservation 已提交后失败会直接返回错误，留下 queued session 与 pending idempotency record，后续同 key / 新 key 都可能被卡住。
- shared idempotency middleware 对 reservation 后的非 2xx response 不收口，`createPracticePlan` validation/error 会把 key 留在 `pending`，同 key corrected body 又被 fingerprint mismatch 阻断。
- `startPracticeSession` 自定义 idempotency reservation 未读取 `expires_at`，过期 pending / succeeded record 会被永久 pending / replay / conflict。
- F3 `practice.session.first_question` template 中的 `{{language}}`、`{{role_title}}`、`{{top_skills}}`、`{{practice_goal}}` 等占位没有渲染，真实模型可能收到无上下文或错误语言的首问指令。

## 2 诊断过程

### 假设 1：首题解析只是 OpenAPI response 字段名与 domain 字段名不同

- 验证方法：读取 `backend/internal/practice/session_starter.go`、F3 prompt markdown 与现有 `session_starter` tests。
- 结果：排除。当前 prompt 明确要求 strict JSON keys `question` / `intent` 等；实现只解析 legacy/generated response keys，并对非 JSON 文本 fallback 为成功。

### 假设 2：success replay 已经由 `response_body` 覆盖

- 验证方法：读取 `backend/internal/store/practice/create_plan.go` 的 reserve/replay/commit 路径和 scenario in-memory store。
- 结果：排除。commit 会写 `response_body`，但 succeeded replay 分支调用 `selectSessionForUser`，scenario harness 也返回 mutable session record。

### 假设 3：000003 down migration 仍拥有 idempotency table

- 验证方法：对比 `migrations/000001_create_baseline.up.sql` 与 `migrations/000003_practice_idempotency_baseline.down.sql`。
- 结果：排除。pre-launch baseline rebase 后 `000001` 已创建 `idempotency_records`；`000003` down 不应删除 baseline-owned 表或 index。

### 假设 4：reservation 后失败路径都会统一调用 `FailSessionStart`

- 验证方法：读取 `backend/internal/practice/session_starter.go` 中 `ResolveActive` / `AIClient.Complete` / parser error 三段路径，并补 focused test。
- 结果：排除。`ResolveActive` 失败发生在 AI call 前，旧代码直接返回 registry error，没有推进 session / idempotency 到失败状态。

### 假设 5：shared idempotency middleware 已覆盖非 2xx completion

- 验证方法：读取 `backend/internal/middleware/idempotency/middleware.go` 与 SQL store 状态机，补同 key 422 → corrected body 测试。
- 结果：排除。旧 middleware 只在 2xx response 调用 `MarkSucceeded`；非 2xx response flush 后 reservation 仍为 `pending`。

### 假设 6：custom startSession idempotency 与 shared middleware 同样 honor TTL

- 验证方法：读取 `backend/internal/store/practice/create_plan.go` 的 `selectStartSessionIdempotency` 分支，并用 SQL mock 注入 expired pending / succeeded rows。
- 结果：排除。旧查询未读取 `expires_at`，因此无法在过期后 reset reservation。

## 3 根因分析

- F3 prompt schema 与 `session_starter` parser gate 没有同测，导致真实模型输出 shape 与 runtime parser 分叉。
- F3 prompt template render 与 runtime payload 没有同测，导致 registry 模板占位原样透传给 provider。
- Idempotency replay 的测试只覆盖“无重复副作用”，没有断言 replay response 必须来自持久化 snapshot。
- Idempotency error-path gate 只覆盖 success / retryable AI failure，没有覆盖 registry resolution failure、generic non-2xx completion、custom TTL expiry。
- Migration ownership rebase 后缺少 down-path zero-drop contract test，导致旧 down 文件保留了 pre-rebase table ownership。

## 4 修复方案

- `backend/internal/practice/session_starter.go`：`parseFirstQuestion` 改为必须解析 strict JSON object，兼容 `question` / `intent` 与 `questionText` / `questionIntent`，并拒绝空内容、非 JSON、缺少 question 的输出。
- `backend/internal/store/practice/create_plan.go`：succeeded replay 查询并解码 `idempotency_records.response_body`，返回首次成功 response snapshot，不再读取 mutable session state。
- `backend/internal/store/practice/create_plan.go`：`startPracticeSession` reservation 读取并 honor `expires_at`，expired pending / succeeded record reset 为 fresh pending；reservation 同时读取 target job title / seniority / requirement labels 作为首问模板上下文。
- `backend/internal/practice/session_starter.go`：`ResolveActive` 失败后调用 `FailSessionStart`；首问 payload 渲染 F3 template placeholders，缺失字段使用显式 fallback。
- `backend/internal/middleware/idempotency/`：middleware 对非 2xx response 调用 `MarkFailed`，SQL store 将 record 标记为 `failed_terminal` 并允许同 key corrected body 重新执行。
- `backend/cmd/api/practice_http_scenario_test.go`：scenario in-memory harness 改为从 stored response snapshot replay，并新增 replay drift 断言。
- `migrations/000003_practice_idempotency_baseline.down.sql`：移除对 baseline-owned `idempotency_records` table/index 的 drop。
- `docs/spec/backend-practice/plans/001-plan-and-session-orchestration/`：原地追加 1.1 / 1.2 remediation checklist，并在验证后恢复 `completed`。

## 5 验证

- `cd backend && go test ./cmd/api ./internal/api/practice ./internal/practice ./internal/store/practice ./internal/middleware/idempotency ./internal/ai/registry ./internal/migrations`：PASS。
- `make validate-fixtures`：PASS。
- `python3 scripts/lint/migrations_lint.py && python3 scripts/lint/backend_practice_preflight.py --repo-root . && python3 scripts/lint/backend_practice_legacy.py --repo-root . --phase all`：PASS。
- `make codegen-check`：PASS。
- `make lint-events`：PASS。
- `E2E.P0.022` ~ `E2E.P0.026` scenario setup / trigger / verify / cleanup：PASS。
- `cd backend && go test ./internal/practice -run 'TestStartPracticeSession(RunsThreeStepFlowWithAIOutsideTransactions|FailsReservationWhenPromptResolutionFails|RejectsMissingFirstQuestionText|RejectsNonJSONFirstQuestionResponse)' -count=1`：PASS。
- `cd backend && go test ./internal/middleware/idempotency -run 'TestMiddleware(FinalizesNon2xxAndAllowsCorrectedSameKey|ReplaysSucceededResponse|RejectsFingerprintMismatchWithoutSideEffect|ExpiresRecordsByTTL)' -count=1`：PASS。
- `cd backend && go test ./internal/store/practice -run 'TestSQLRepositoryReserveSessionStart(ResetsExpired|ReusesFailedRetryableRecord|ReplaysStoredResponseBody|ScopesIdempotencyByUser|RejectsFingerprintMismatch|RejectsConcurrentPendingRecord|MapsActivePlanUniqueViolationToConflict)' -count=1`：PASS。
- `cd backend && go test ./internal/practice/... ./internal/middleware/idempotency/... ./internal/store/practice/... ./internal/api/practice/... ./cmd/api`：PASS。
- `python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check`：zero drift。

## 6 经验教训

- Prompt truth source 的 JSON keys 必须有 runtime parser 测试直接覆盖；不能只测 generated API response shape。
- Prompt template truth source 的 placeholders 也必须有 runtime payload render 测试覆盖，特别是语言 fallback 与目标岗位上下文。
- Idempotency replay gate 必须断言 replay response 来自 stored snapshot，并主动制造 mutable state drift。
- Idempotency gate 必须覆盖每一种 reservation completion 结果：2xx success、非 2xx terminal failure、retryable failure、TTL expiry、fingerprint mismatch。
- Pre-launch baseline rebase 后，新增 migration down-path ownership test，防止后续 rollback 删除 baseline-owned schema。
