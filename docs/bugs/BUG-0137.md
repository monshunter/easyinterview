# BUG-0137: resume creation kept generic names and obsolete parse steps

> 模块: materials | 严重度: medium | 状态: resolved
> 发现日期: 2026-07-07 | 关联 Commit: `fix(resume-workshop): make resume creation direct and detail readonly (BUG-0137)`
> 关联日志: `-`

## 1 现象

- 用户上传或粘贴简历后，列表和详情仍显示通用名称“粘贴的简历”，无法区分真实简历内容。
- 创建流程仍进入解析动画页和预览确认页，和当前“上传后直接打开只读详情”的产品诉求冲突。
- 详情页使用 structured projection 渲染正文，导致原始简历信息大量丢失；用户看到的不是简历原文。

## 2 诊断过程

### 假设 1：前端列表和详情直接信任注册时的通用 title
- 验证方法：检查 `mapResumeToUiSource` 和创建流 title 生成逻辑。
- 结果：确认。`mapResumeToUiSource` 使用 `displayName || title`，`deriveDefaultTitle("paste")` 仍返回通用名称，旧数据和解析前状态都会暴露“粘贴的简历”。

### 假设 2：创建流仍保留旧 parse/preview 状态机
- 验证方法：搜索 `ResumeParseFlow`、`ParsingStage`、`PreviewStage`、`ResumePreviewConfirm` 和场景脚本。
- 结果：确认。`ResumeCreateFlow` 注册后进入 parsing/preview 分支，P0.081-084 场景也仍把旧 DOM 当成正向期望。

### 假设 3：详情页正文来自 structuredProfile 而非原文
- 验证方法：检查 `ResumePreviewTab` 和 detail 场景断言。
- 结果：确认。详情正文由 `buildResumePreview` 投影 structuredProfile，未优先渲染 `parsedTextSnapshot` / `originalText`。

## 3 根因分析

- `frontend-resume-workshop` 已切换到 flat/read-only 产品合同后，创建流和详情投影仍保留早期“解析草稿 -> 预览确认 -> 保存”的二步模型。
- 通用 title 没有被识别为非业务名称，adapter 缺少从 LLM-derived `displayName`、原文首行、文件名或 structured headline 派生可识别名称的兜底。
- 场景 gate 和 UI contract test 仍锁定旧解析动画、确认保存和 structured preview，导致旧行为没有被负向拦截。

## 4 修复方案

- 简化 `ResumeCreateFlow`：上传或粘贴注册成功后直接导航到 `resume_versions?resumeId=<id>`，删除解析动画、预览确认和 create-flow 内 `updateResume` 相关组件与测试。
- 更新 paste 标题生成：从原始文本首个有效行派生临时 title，避免提交通用“粘贴的简历”。
- 更新 resume adapter 和详情：过滤通用名称，优先渲染 `parsedTextSnapshot` / `originalText`，无原文时才降级 structured fields。
- 补强 backend parse displayName 派生：过滤更多通用名称，并支持 root-level headline/title/summary。
- 同步 docs/spec、docs/ui-design、ui-design contract test、P0.037/P0.081-P0.084 场景资产和 checklist。

## 5 验证

- `go test ./backend/internal/resume/jobs`
- `corepack pnpm --filter @easyinterview/frontend test src/app/screens/resume-workshop/create`
- `corepack pnpm --filter @easyinterview/frontend test src/app/screens/resume-workshop/create/ResumeCreateFlow.test.tsx src/app/screens/resume-workshop/create/UploadTab.test.tsx src/app/screens/resume-workshop/create/CreateFlowNonCurrentNegative.test.ts src/app/screens/resume-workshop/adapters/resume.test.ts src/app/screens/resume-workshop/components/ResumePreviewTab.test.tsx src/app/screens/resume-workshop/components/ResumeDetailView.test.tsx src/app/i18n/localeFiles.test.ts`
- `corepack pnpm --filter @easyinterview/frontend test src/app/screens/resume-workshop/ResumeWorkshopScreen.test.tsx src/app/screens/resume-workshop/fixture-parity.test.ts`
- `corepack pnpm --filter @easyinterview/frontend typecheck`
- `node --test ui-design/ui-design-contract.test.mjs`
- `bash test/scenarios/e2e/p0-037-resume-detail-preview-readonly/scripts/trigger.sh && bash test/scenarios/e2e/p0-037-resume-detail-preview-readonly/scripts/verify.sh`
- `bash test/scenarios/e2e/p0-081-resume-create-flow-upload-paste-guided-happy/scripts/trigger.sh && bash test/scenarios/e2e/p0-081-resume-create-flow-upload-paste-guided-happy/scripts/verify.sh`
- `bash test/scenarios/e2e/p0-082-resume-create-flow-parsing-failure-and-retry/scripts/trigger.sh && bash test/scenarios/e2e/p0-082-resume-create-flow-parsing-failure-and-retry/scripts/verify.sh`
- `bash test/scenarios/e2e/p0-083-resume-create-flow-preview-confirm-and-cta-handoff/scripts/trigger.sh && bash test/scenarios/e2e/p0-083-resume-create-flow-preview-confirm-and-cta-handoff/scripts/verify.sh`
- `bash test/scenarios/e2e/p0-084-resume-flat-ui-regression/scripts/trigger.sh && bash test/scenarios/e2e/p0-084-resume-flat-ui-regression/scripts/verify.sh`
- `python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check`
- `make docs-check`
- `git diff --check`

## 6 经验教训

- 当产品合同明确删除中间确认页时，必须同时删除 runtime 组件、locale 文案、CSS、UI contract、scenario wrapper 和 expected-outcome；只改入口跳转会留下 false-green gate。
- 简历名称不能把来源描述当业务名称；通用 title 必须被负向过滤，并由原文/LLM 结构化结果派生可识别名称。
