# BUG-0106: privacy delete cleanup completed without removing UAT account identity

> 模块: platform | 严重度: medium | 状态: resolved
> 发现日期: 2026-05-26 | 关联 Commit: `fix(backend-async-runner): close privacy account identity cleanup (BUG-0106)`
> 关联日志: [2026-05-26](../work-journal/2026-05-26.md)

## 1 现象

- `E2E.P0.100` cleanup 按材料走真实产品路径：`DELETE /api/v1/me` 返回 `202`，创建 `privacy_delete` job。
- backend runner 将 privacy request 标记为 `completed`，async job 标记为 `succeeded`。
- 但随后查询 `users`，synthetic UAT 邮箱 `manual-uat-full-funnel@example.test` 对应用户行仍存在，且 `deleted_at` 为空。

## 2 诊断过程

### 假设 1：privacy_delete runner 没有执行
- 验证方法：查询 `privacy_requests` 与 `async_jobs`。
- 结果：排除。privacy request `019e6435-0a9c-7e09-876c-8f3f2109b190` 为 `completed`，job `019e6435-0a9c-7e0d-b785-bd2e1677126c` 为 `succeeded`。

### 假设 2：cleanup 材料使用了错误入口
- 验证方法：检查 `test/scenarios/manual-uat/full-funnel/materials/account.md` 与浏览器执行路径。
- 结果：排除。入口是文档指定的 `DELETE /api/v1/me` 产品路径，没有使用 broad DB delete 或直接 session table writes。

### 假设 3：privacy_delete runner 当前只删除部分 domain 数据
- 验证方法：阅读 `backend/internal/privacy/runner/delete_handler.go`。
- 结果：确认。runner 当前调用 upload/profile/jd_match deleter，并标记 request completed；没有删除、匿名化或 tombstone `users.email` / `users.deleted_at` 的实现。

## 3 根因分析

- 初步根因是 `privacy_delete` job 的完成语义与 cleanup/account deletion 期望不一致：job success 只代表部分 domain deleter 成功，不代表用户身份行已清除或匿名化。
- 涉及路径：
  - `backend/internal/auth/service.go` 创建 privacy delete handoff 并撤销 session。
  - `backend/internal/privacy/runner/delete_handler.go` 执行 upload/profile/jd_match cleanup 后完成 request。
  - `users` 表身份数据未被 hard delete、soft delete 或 anonymize。
- 之前没有发现，是因为 manual UAT cleanup gate 只检查 job/request terminal status，没有反查 UAT 邮箱或用户身份数据残留。

## 4 修复方案

- 采用 ADR-Q5 / B4 matrix 已锁定的两段删除语义：
  - `DELETE /api/v1/me` 受理期在 auth store 事务中同步设置 `users.deleted_at` / `users.status='deleted'`，并撤销该用户所有 active session，同时创建 idempotent `privacy_delete` request/job。
  - `privacy_delete` runner 在 upload/profile/JD Match 等 domain cleanup 全部成功后，完成 `privacy_requests` tombstone，并 hard delete `users` 行；domain cleanup 失败时不删除账户身份，保留 retry 语义。
- 新增 `migrations/000011_privacy_requests_user_tombstone.{up,down}.sql`，把 `privacy_requests.user_id` 改为 nullable + `ON DELETE SET NULL`，避免最终 hard delete `users` 时级联删除 request tombstone。
- 补充 focused tests：
  - `TestDeleteMeSoftDeletesUserRevokesAllSessionsAndCreatesPrivacyHandoff`
  - `TestSQLStorePrivacyDeleteHandoffSoftDeletesUserAndRevokesSessions`
  - `TestSQLStoreMarkDeleteRequestCompletedDeletesAccountIdentityAndPreservesRequestTombstone`
  - `TestPrivacyDeleteHandlerHardDeletesAccountIdentityAfterDomainCleanup`
  - `TestPrivacyDeleteHandlerDomainFailureKeepsAccountIdentityForRetry`
  - `TestPrivacyDeleteRemovesAccountIdentityAfterJobCompletion`

## 5 验证

- 已通过：
  - `go test ./backend/internal/auth ./backend/internal/privacy/runner ./backend/cmd/api -run '^(TestDeleteMeSoftDeletesUserRevokesAllSessionsAndCreatesPrivacyHandoff|TestSQLStorePrivacyDeleteHandoffSoftDeletesUserAndRevokesSessions|TestSQLStoreMarkDeleteRequestCompletedDeletesAccountIdentityAndPreservesRequestTombstone|TestPrivacyDeleteHandlerHardDeletesAccountIdentityAfterDomainCleanup|TestPrivacyDeleteHandlerDomainFailureKeepsAccountIdentityForRetry|TestPrivacyDeleteRemovesAccountIdentityAfterJobCompletion)$' -count=1`
  - `go test ./backend/internal/auth ./backend/internal/privacy/runner -count=1`
  - `DATABASE_URL='postgres://easyinterview:***@localhost:5432/easyinterview?sslmode=disable' make migrate-check`
  - `make docs-check`
  - `make lint-runner-legacy`
  - `python3 .agent-skills/implement/shared/scripts/validate_context.py --context docs/spec/backend-async-runner/plans/001-internal-job-outbox-runner/context.yaml --docs-root docs --target backend`
  - `python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check`
  - `git diff --check`
- 已知非本 BUG 范围残余：`go test ./backend/cmd/api -run '^TestE2EP0050PracticeAssistantActionProvenanceAndTaskRuns$' -count=1` 当前失败在 practice task-run 数量 / feature key 断言；本次未触碰 practice 逻辑，未作为 BUG-0106 通过证据。

## 6 经验教训

- Cleanup gate 不能只看 async job/request terminal state；必须反查 cleanup 目标的关键残留字段。
- 隐私删除类 runner 的 success 语义需要覆盖身份数据、domain data、session/token、audit tombstone 的最小闭环，而不是只覆盖已接入的 domain deleter。
