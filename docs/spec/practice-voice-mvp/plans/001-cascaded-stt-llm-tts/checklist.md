# Cascaded STT LLM TTS Voice MVP Checklist

> **版本**: 1.14
> **状态**: active
> **更新日期**: 2026-07-11

**关联计划**: [plan](./plan.md)

## Phase 0: Current-state preflight and handoff lock

- [x] 0.1 验证 A3 004 handoff 已在代码中可消费：`tts` capability、`AIClient.Transcribe` / `AIClient.Synthesize`、`practice.voice.stt.default` / `practice.voice.tts.default`、speech adapters、profile coverage 与 privacy tests；验证: focused grep + `cd backend && go test ./internal/ai/aiclient/... -count=1`
- [x] 0.2 固化当前 owner handoff 与待反转负向实现：记录并更新 `backend/internal/api/practice/README.md` 的 voice/audio route owner、`VoiceSurfaceComingSoon` coming-soon 测试边界和 `voice` route fallback 负向测试；验证: grep 证明不再存在 out-of-scope voice-surface owner 口径，且独立 `voice` route 仍 fallback `home`

## 2026-05-17 Review-Fix Evidence

- [x] RF.1 BUG-0070 playback `audioRef` gate：`createPracticeVoiceTurn` HTTP response 的 `ttsChunks[].audioRef` 返回浏览器可播放 `data:audio/...;base64,...`；持久化 voice turn summary 改写为 opaque `voice-turn://...`，不保存 audio bytes；验证: `go test ./internal/practice -run TestCreatePracticeVoiceTurnReturnsPlayableAudioRefWithoutPersistingAudioData -count=1`。
- [x] RF.2 BUG-0070 committed context replay gate：production service 在请求未携带 context 时从 store 加载 latest `follow_up_generated` + 后续 playback events，并注入下一轮 chat payload；验证: `go test ./internal/practice ./internal/store/practice -run 'TestCreatePracticeVoiceTurnLoadsCommittedContext|TestSQLRepositoryLoadCommittedVoiceContext' -count=1`。
- [x] RF.3 BUG-0070 barge-in partial playback gate：frontend `bargeIn()` 先上报 partial `tts_chunk_played`（含 playedTextLength/hash/offset），再上报 `barge_in_detected`；验证: `pnpm --dir frontend test src/app/screens/practice/__tests__/practiceVoiceTurn.test.tsx --run`。
- [x] RF.4 BUG-0072 fixture playback ref gate：`createPracticeVoiceTurn` default fixture 的 `ttsChunks[].audioRef` 与真实 HTTP response 语义一致，必须是浏览器可播放 `data:audio/...;base64,...` 或同计划 resolver URL；验证: `pnpm --dir frontend test src/api/devMockClient.test.ts --run` + `python3 scripts/lint/validate_fixtures.py --repo-root .`。
- [x] RF.5 backend-practice out-of-scope lint precision gate：out-of-scope route 负向 lint 继续拦截独立 `/voice` route / alias，但不得误伤本计划拥有的 `createPracticeVoiceTurn` endpoint、`voice-turn://` persisted summary ref、`practice.voice.*` profile / feature key；验证: `python3 -m pytest scripts/lint/backend_practice_out_of_scope_test.py -q` PASS + `make lint-backend-practice-out-of-scope` PASS + `make lint` PASS。

## Phase 1: Contract and fixture

- [x] 1.1 新增 `createPracticeVoiceTurn` OpenAPI operation 与 schema，锁定 `POST /practice/sessions/{sessionId}/voice-turns`、`Idempotency-Key`、`clientVoiceTurnId`、small audio payload、`userTranscriptFinal`、`assistantTextDraft`、`ttsChunks[]`、`providerMetaSummary` 与 `ttsError`；验证: `make lint-openapi` + operation matrix 字段完整
- [x] 1.2 新增 / 扩展 PracticeSessions fixtures：`createPracticeVoiceTurn` scenarios `default` / `stt-config-missing` / `chat-failed` / `tts-failed`，`appendSessionEvent` scenarios `voice-tts-started` / `voice-tts-played` / `voice-barge-in` / `voice-context-committed`；验证: `make validate-fixtures && make codegen-check`
- [x] 1.3 扩展 generated client allowlist / mock transport 消费路径；验证: frontend contract tests 不允许 ad hoc fetch shape，且未知 `Prefer: example=` voice scenario fail loudly

## Phase 2: Backend orchestration

- [x] 2.1 实现 voice turn service 串联独立 `stt`、`chat`、`tts` profiles；验证: backend service tests 断言三类 profile 可指向不同 provider
- [x] 2.2 实现 STT / chat / TTS 独立失败路径；验证: backend tests 断言 STT 失败不调用 chat/TTS，TTS 失败不丢 transcript/chat text
- [x] 2.3 实现 session event / AI metadata privacy 边界；验证: privacy tests + grep gate 不含 raw audio、TTS audio、provider secret、AI metadata transcript 明文，session event 业务正文与 AI/audit metadata 摘要字段分离

## Phase 3: Playback progress and barge-in context

- [x] 3.1 扩展或复用 `appendSessionEvent` 记录 `tts_chunk_started` / `tts_chunk_played` / `barge_in_detected` / `assistant_context_committed`；验证: API/handler tests 覆盖 event ordering、body-level `clientEventId` replay、禁止 `Idempotency-Key`
- [x] 3.2 实现 committed context builder；验证: unit tests 覆盖完整 chunk、部分 chunk、无播放、重复事件、乱序事件
- [x] 3.3 下一轮 prompt 注入 interruption note；验证: backend tests 断言未播放 draft 不进入 prompt，已播放内容和用户插话进入 prompt

## Phase 4: Frontend voice controller

> Historical completed evidence. Phase 7 supersedes the former restart/call-ended UI anchors.

- [x] 4.1 在 `PracticeScreen` 内复刻 ui-design 电话 Surface，并删除 / 反转 `VoiceSurfaceComingSoon` coming-soon 语义；验证: source-structure parity tests + visual geometry / pixel parity gate 覆盖 `practice-phone-surface`、`practice-phone-waveform`、`practice-phone-captions-toggle`、`practice-phone-hangup`、`practice-phone-restart`，且不新增 `voice` route 或 out-of-scope `voice` query 入口；证据: `pnpm --dir frontend test src/app/screens/practice/PracticeScreen.test.tsx src/app/screens/practice/__tests__/practiceModeSwitch.test.tsx src/app/App.test.tsx src/app/screens/practice/__tests__/outOfScopeNegative.test.ts`、`pnpm --dir frontend typecheck`、`pnpm --dir frontend build`、`pnpm --dir frontend test:pixel-parity tests/pixel-parity/practice.spec.ts --grep "phone mode"`
- [x] 4.2 实现音频采集、voice turn 提交、transcript / 字幕展示和电话错误恢复；不提供手动转写替代字段或入口；验证: frontend component/controller tests 使用 fixtures/stub；证据: `pnpm --dir frontend test src/app/screens/practice/__tests__/practiceVoiceTurn.test.tsx src/app/screens/practice/__tests__/outOfScopeNegative.test.ts src/app/screens/practice/PracticeScreen.test.tsx src/app/screens/practice/__tests__/practiceModeSwitch.test.tsx src/app/App.test.tsx`、`pnpm --dir frontend typecheck`、`pnpm --dir frontend build`、`pnpm --dir frontend test:pixel-parity tests/pixel-parity/practice.spec.ts --grep "phone mode"`
- [x] 4.3 实现 TTS 播放、播放完成回报、barge-in 停止播放；验证: frontend tests 覆盖 played chunk 上报、用户插话、TTS error fallback；证据: `pnpm --dir frontend test src/app/screens/practice/__tests__/practiceVoiceTurn.test.tsx src/app/screens/practice/__tests__/outOfScopeNegative.test.ts src/app/screens/practice/PracticeScreen.test.tsx src/app/screens/practice/__tests__/practiceModeSwitch.test.tsx src/app/App.test.tsx`、`pnpm --dir frontend typecheck`、`pnpm --dir frontend build`、`pnpm --dir frontend test:pixel-parity tests/pixel-parity/practice.spec.ts --grep "phone mode"`

## Phase 5: Verification and negative gates

- [x] 5.1 BDD-Gate: 创建并执行 `E2E.P0.007` 完整语音 turn 场景；证据: `test/scenarios/e2e/p0-007-cascaded-voice-turn/scripts/setup.sh && test/scenarios/e2e/p0-007-cascaded-voice-turn/scripts/trigger.sh && test/scenarios/e2e/p0-007-cascaded-voice-turn/scripts/verify.sh && test/scenarios/e2e/p0-007-cascaded-voice-turn/scripts/cleanup.sh`
- [x] 5.2 BDD-Gate: 创建并执行 `E2E.P0.008` 插话 / 打断 committed context 场景；证据: `test/scenarios/e2e/p0-008-voice-barge-in-committed-context/scripts/setup.sh && test/scenarios/e2e/p0-008-voice-barge-in-committed-context/scripts/trigger.sh && test/scenarios/e2e/p0-008-voice-barge-in-committed-context/scripts/verify.sh && test/scenarios/e2e/p0-008-voice-barge-in-committed-context/scripts/cleanup.sh`
- [x] 5.3 BDD-Gate: 创建并执行 `E2E.P0.009` provider failure / fallback 场景；证据: `test/scenarios/e2e/p0-009-voice-provider-failure-fallback/scripts/setup.sh && test/scenarios/e2e/p0-009-voice-provider-failure-fallback/scripts/trigger.sh && test/scenarios/e2e/p0-009-voice-provider-failure-fallback/scripts/verify.sh && test/scenarios/e2e/p0-009-voice-provider-failure-fallback/scripts/cleanup.sh`
- [x] 5.4 重跑 regression gates：OpenAPI fixture validation、codegen drift、frontend tests、backend tests、A3 profile coverage、privacy grep、out-of-scope route negative search、scenario wrapper contract；证据: `make codegen-check`、`make validate-fixtures`、`pnpm --filter @easyinterview/frontend test src/app/screens/practice/__tests__/practiceVoiceTurn.test.tsx src/api/devMockClient.test.ts src/app/screens/practice/PracticeScreen.test.tsx src/app/screens/practice/__tests__/practiceModeSwitch.test.tsx src/app/App.test.tsx`、`cd backend && go test ./internal/practice ./internal/api/practice ./internal/store/practice ./cmd/api ./internal/ai/aiclient ./internal/ai/aiclient/profile -count=1`、`python3 scripts/lint/ai_profile_coverage.py --repo-root .`、runtime privacy grep 零命中、out-of-scope voice route / coming-soon 负向搜索零命中、`test/scenarios/e2e/p0-007-cascaded-voice-turn/scripts/verify.sh`、`test/scenarios/e2e/p0-008-voice-barge-in-committed-context/scripts/verify.sh`、`test/scenarios/e2e/p0-009-voice-provider-failure-fallback/scripts/verify.sh`、`make docs-check`、`git diff --check`

## Phase 6: Real-interview phone-mode simplification

> Historical completed evidence. Phase 7 supersedes the former restart/call-ended control contract and is the only current acceptance source.

- [x] 6.1 用户可见 phone-mode copy：UI / docs / scenarios 使用 `电话模式 / Phone`，底层 `voice` 仅保留为工程能力名；验证: frontend/i18n/source grep + scenario expected-outcome grep。
  - 2026-07-10 evidence: updated `docs/ui-design/module-map.md` / `docs/ui-design/INDEX.md` and P0.007-P0.009 scenario README/seed/expected-outcome wording from user-facing voice/fallback phrasing to phone-mode wording while preserving backend `voice turn` engineering names. Gates: `pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/practice/__tests__/practiceVoiceTurn.test.tsx src/app/screens/practice/__tests__/practiceModeSwitch.test.tsx src/app/screens/practice/PracticeScreen.test.tsx`, `pnpm --filter @easyinterview/frontend lint`, focused user-visible copy grep over `frontend/src/app/screens/practice`, i18n locales, P0.007-P0.009 scenario docs, and UI design module map/index returned zero matches for old phone-mode copy markers.
- [x] 6.2 电话 surface 只保留当前电话控件：无语速/停顿/口头禅/音量 UI，无手动转写或 speech-to-text 替代入口；验证: focused phone Vitest + current-surface zero-reference search。
  - 2026-07-10 evidence: removed the old manual transcription request field from OpenAPI/baseline/generated Go/TS artifacts, fixture request body, backend handler/service, and frontend phone hook; added OpenAPI inventory negative assertion. Gates: `python3 -m pytest scripts/lint/openapi_inventory_test.py -q`, `make lint-openapi`, `make validate-fixtures`, `go test ./internal/practice ./internal/api/practice ./internal/store/practice ./cmd/api ./internal/ai/aiclient ./internal/ai/aiclient/profile -count=1`, `pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/practice/__tests__/practiceVoiceTurn.test.tsx src/api/frontendOwners.realApiMode.test.ts`, `pnpm --filter @easyinterview/frontend lint`; zero-reference search leaves only the negative assertion in `scripts/lint/openapi_inventory_test.py`.
- [x] 6.3 电话控制：phone surface 提供字幕、切断和重新开始，不以 `开始录音` / `提交本轮` 作为主流程；验证: focused phone controller tests + pixel parity。
  - 2026-07-10 evidence: phone controller tests assert `practice-phone-captions-toggle`, `practice-phone-hangup`, and `practice-phone-restart`, and assert old record/submit controls are absent. Pixel parity now checks current `mode=phone&modality=phone` phone surface anchors, restart control, old record/submit absence, and viewport bounds across desktop/mobile. Gates: `pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/practice/__tests__/practiceVoiceTurn.test.tsx src/app/screens/practice/__tests__/practiceModeSwitch.test.tsx src/app/screens/practice/PracticeScreen.test.tsx`; `pnpm --dir frontend test:pixel-parity tests/pixel-parity/practice.spec.ts --grep "phone mode"`.
- [x] 6.4 真实环境截图验收：本地真实前后端环境中打开 phone practice 页面并保存截图证据，截图证明 phone surface 只呈现字幕、切断、重新开始等当前电话控件，不包含语音分析、手动转写替代入口或开始录音/提交本轮主按钮；验证: scenario-env verify + browser screenshot artifacts。
  - 2026-07-10 evidence: refreshed the host-run stack with `test/scenarios/env-redeploy.sh all`, verified dependencies/endpoints with `test/scenarios/env-verify.sh`, seeded a synthetic local account plus minimal real `practice_plans` / `practice_sessions` / `practice_turns` rows, and opened the canonical `/practice?...mode=phone&modality=phone` URL against `VITE_EI_API_MODE=real`. Browser DOM gate confirmed `practice-phone-surface`, waveform, call-state, captions, hang-up, and restart controls are present while out-of-scope voice coming-soon, record/submit, manual/analysis anchors and copy are absent. Screenshot evidence: `.test-output/practice-voice-mvp/phone-surface-acceptance/phone-surface-real-env-playwright.png`; DOM evidence: `.test-output/practice-voice-mvp/phone-surface-acceptance/phone-surface-real-env-playwright-dom.json`.
- [x] 6.5 Regression gates：OpenAPI/codegen/fixtures/backend voice turn tests、committed-context/privacy gates、BDD P0.007-P0.009、docs/index/diff gates 通过。
  - 2026-07-10 evidence: `make codegen-openapi`, `make render-openapi-fixture-examples`, `python3 -m pytest scripts/lint/openapi_inventory_test.py -q`, `make lint-openapi`, `make validate-fixtures`, backend voice/A3 focused Go tests, `pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/practice/__tests__/practiceVoiceTurn.test.tsx src/api/frontendOwners.realApiMode.test.ts`, `pnpm --filter @easyinterview/frontend lint`, `test/scenarios/env-setup.sh`, `test/scenarios/env-verify.sh`, `E2E.P0.007` setup/trigger/verify/cleanup PASS, `E2E.P0.008` setup/trigger/verify/cleanup PASS, `E2E.P0.009` setup/trigger/verify/cleanup PASS, `make docs-check`, `git diff --check`.
- [x] 6.6 Frontend route params 不再保留 out-of-scope `mode=voice` / `modality=voice` query：正向 practice phone deep-link 只使用 `mode=phone&modality=phone`，out-of-scope voice mode query 在 parse / serialize / hash bootstrap 中被过滤；验证: routeUrl/bootstrap/App/P0.088/P0.090 focused tests + scenario docs grep。
  - 2026-07-10 evidence: removed `voice` route-param acceptance from `routeUrl` parse/serialize and `PracticeScreen` / `ReportContextStrip` phone detection; updated P0.088/P0.090 scenario tests and docs so positive phone deep-links use `mode=phone&modality=phone`, while out-of-scope `mode=voice&modality=voice` hash/query values are filtered and do not mount phone UI. Gates: `pnpm --filter @easyinterview/frontend exec vitest run src/app/bootstrapRoute.test.ts src/app/routeUrl.test.ts src/app/App.test.tsx src/app/AppRoutingHistory.test.tsx src/app/scenarios/p0-088-url-addressable-routing-canonical.test.tsx src/app/scenarios/p0-090-url-routing-hash-out-of-scope-negative.test.tsx src/app/screens/practice/__tests__/practiceModeSwitch.test.tsx src/app/screens/practice/__tests__/practiceVoiceTurn.test.tsx src/app/screens/report/__tests__/ReportContextStrip.test.tsx`, `pnpm --filter @easyinterview/frontend lint`, P0.088 setup/trigger/verify/cleanup PASS, P0.090 setup/trigger/verify/cleanup PASS, and focused out-of-scope-positive route-param grep returned zero matches.
- [x] 6.7 Current specs / UI truth sources 不再声明 out-of-scope `voice` query 可进入电话模式：product-scope、engineering-roadmap、frontend-workspace-and-practice、practice-voice-mvp、docs/ui-design 与 ui-design contract 均以 `mode=phone&modality=phone` 作为唯一正向电话入口；验证: ui-design contract + docs/index/diff gates + current-spec grep。
  - 2026-07-10 evidence: updated current specs, spec histories, `docs/ui-design/module-practice-review.md`, `docs/ui-design/INDEX.md`, `ui-design/src/screen-report.jsx`, and `ui-design/ui-design-contract.test.mjs` so `voice` remains only a backend/API/profile engineering name or negative route input. Gates: `node ui-design/ui-design-contract.test.mjs`, `pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/report/__tests__/ReportContextStrip.test.tsx src/app/screens/practice/__tests__/practiceModeSwitch.test.tsx src/app/scenarios/p0-090-url-routing-hash-out-of-scope-negative.test.tsx`, `python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check`, `make docs-check`, `git diff --check`, and focused current-truth-source grep.

## Phase 7: Phone lifecycle and conversational integrity

- [x] 7.1 UI / frontend/backend RED-GREEN: replace segmented mode controls and live chip with one accessible handset icon; remove visible “切断”, restart and `callEnded`, including any positive frontend/backend restart action/state/event/handler residue; text click enters phone, while phone icon and center red hang-up share `exitPhoneMode` and return to text for the same session.
  <!-- verified: 2026-07-11 method=single-handset-phone-ui evidence="UI contract 45/45 and formal Practice/ModeSwitch 20 tests pass with one shared PhoneIcon, no segmented/live/restart/callEnded/cut-off text, and both handset exits preserve sessionId." -->
- [x] 7.2 Exit settlement RED-GREEN: hang-up immediately stops microphone/TTS, may settle non-empty capture and commit heard playback through existing events, never emits barge-in, and suppresses any phone TTS completion after exit.
  <!-- verified: 2026-07-11 method=phone-exit-settlement evidence="Playback lifecycle 6 tests and phone integration 8 tests prove synchronous pause/release, optional heard-prefix played+committed events, no hang-up barge-in, and no late completion after exit." -->
- [x] 7.3 Turn timing RED-GREEN: VAD silence auto-submits non-empty capture, TTS-ended re-arms listening, and only real speech-start sends partial `tts_chunk_played` then `barge_in_detected`.
  <!-- verified: 2026-07-11 method=vad-auto-turn evidence="VAD kernel/monitor 6 tests, call-scoped microphone lifecycle 5, controller 2 and phone integration 8 tests pass; one getUserMedia stream serves utterance recorders, silence submits and TTS completion rearms." -->
- [x] 7.4 Backend voice follow-up RED-GREEN: use canonical current-turn/transcript/committed context + persisted session language; only structured/parser/language invalidity receives exactly one repair; provider/config/timeout errors do not repair; a second invalid result returns the existing top-level `AI_OUTPUT_INVALID` envelope before result/TTS persistence and leaves the session unchanged for text-mode exit.
  <!-- verified: 2026-07-11 method=voice-question-generation evidence="RED exposed request-language override/no repair; GREEN shares generateQuestion, session language wins, wrong-language repairs once before one TTS, provider failure does not repair, double invalid returns AI_OUTPUT_INVALID with no TTS or voice persistence; internal/practice full package PASS." -->
- [x] 7.5 BDD-Gate: revise and execute `E2E.P0.007` / `E2E.P0.008` / `E2E.P0.009` for automatic turn timing, hang-up-versus-speech-start truth, and repair failure recovery without adding new scenario IDs or HTTP schema.
  <!-- verified: 2026-07-11 method=phase-7-voice-bdd evidence="P0.007/P0.008/P0.009 setup-trigger-verify-cleanup wrappers PASS; focused frontend voice lifecycle/VAD/controller tests and backend canonical context/language/repair/persistence tests PASS without HTTP schema expansion." -->
- [x] 7.6 Parity / real-mode closeout: source DOM/computed-style/bounding-box/viewport/screenshot gates prove the single-handset UI and removed controls; focused/full frontend/backend, context, docs/index, diff and privacy gates pass.
  <!-- verified: 2026-07-11 method=real-mode-phone-closeout evidence="UI contract 45/45 and Practice pixel parity 11 pass/1 conditional skip; real API browser at 1440x900 captured text, phone and post-hang-up text. The same sessionId remains running/asked; one handset and center red hang-up are present, while segmented/live/cut-off/restart/callEnded controls are absent. Full frontend 144 files/889 tests, full backend, owner contexts, privacy, docs/index and diff gates PASS. STT/TTS runtime profiles were disabled and the automation browser had no microphone, so screenshots validate the real shell/session transition rather than provider audio." -->
