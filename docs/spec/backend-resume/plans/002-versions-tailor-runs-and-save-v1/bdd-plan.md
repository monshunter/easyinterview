# 002 BDD Plan

> **版本**: 1.0
> **状态**: active
> **更新日期**: 2026-06-13

**关联 Plan**: [plan](./plan.md)

## 1 场景矩阵

| 场景 ID | 类别 | 关联 Phase | 关联 Spec C-* | 关联 BDD-Gate（主 checklist） |
|---------|------|-----------|--------------|----------------------------|
| E2E.P0.074 | primary + boundary + auth · confirmResumeStructuredMaster 保存 v1 + 唯一性 409 + getResumeVersion + listResumeVersions cursor + cross-user 404 | Phase 2 + 3 | C-6, C-14, C-15 | Phase 3.8 + 9.5 |
| E2E.P0.075 | primary + failure · updateResumeVersion IK 主路径 + structured_profile partial merge + 不可编辑字段 422 + cross-user 404 | Phase 4 | C-14（follow-up: 用户编辑 v1） | Phase 4.8 + 9.6 |
| E2E.P0.076 | primary + boundary · branchResumeVersion `copy_master` / `blank` 同步 201 + IK replay + parent 404 + targetJob 404 | Phase 5 | C-10 (sync 分支) | Phase 5.8 + 9.7 |
| E2E.P0.077 | primary · branchResumeVersion `ai_select` 异步 + requestResumeTailor 202 + getResumeTailorRun queued→ready + resume.tailor.completed outbox event | Phase 5 + 6 + 7 | C-10 (async 分支), C-16 | Phase 7.10 + 9.8 |
| E2E.P0.078 | failure/recovery · resume.tailor AI timeout retryable + output_invalid permanent + retry-back-to-generating + ready-only outbox（failure 不发 completed event）+ resume_tailor_runs.status='failed' + ai_task_runs 多行 | Phase 7 | C-16 (failure 分支) | Phase 7.11 + 9.9 |
| E2E.P0.079 | primary + boundary + auth · accept/reject suggestion 终态状态机 + IK replay + already-decided 409 + accept-no-structured-profile-mutation（D-12）+ cross-user 404 | Phase 8 | C-11 | Phase 8.8 + 9.10 |
| E2E.P0.080 | regression + privacy · `inline\|rewrite\|mirror` zero-hit + `mistakes\|growth\|drill` zero-hit + outbox / audit / ai_task_runs 不含 prompt 文本 / suggested bullet / match_summary 文本 / raw resume content | Phase 9 | C-13 + spec §4.3 PII 边界 | Phase 9.11 |

---

## Phase 2 + 3: confirm structured master + getResumeVersion + listResumeVersions

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.074 | resume confirm master + version reads + uniqueness + cross-user | A2 dev stack 拉起；migration 000007 已 apply；`cmd/api` 使用真实 session middleware、IK middleware 与新增 confirm/get/list route；用户 A 已登录；用户 A 完成 backend-resume/001 `registerResume` + `resume.parse` 后拥有一个 `resume_asset` 行 `parse_status='ready'` 且 `parsed_summary` 非空；继续创建第二个 resume_asset 同样 ready，用于 listResumeVersions 0 行场景；为 listResumeVersions pagination 通过 store integration seed 25 行版本（1 master + 24 targeted；Phase 5 branch endpoint 不作为本场景前置）；用户 B 已登录但无 resume_asset；缺少 live env、integration-tag 测试 skip 或 focused gate no-op 时本场景必须 fail | 用户 A 通过 `cmd/api` route：（A1）`POST /api/v1/resumes/{firstAsset}/structured-master` body `{structuredProfile, displayName, language}` + IK1；（A2）同 IK1 + 同 body 再次 POST；（A3）新 IK2 + 同 firstAsset 再次 POST；（A4）新 IK3 + schema-valid blank displayName 调 POST；（A5）`GET /api/v1/resume-versions/{A1.id}`；（A6）`GET /api/v1/resumes/{firstAsset}/versions?pageSize=20`；（A7）`GET /api/v1/resumes/{firstAsset}/versions?pageSize=20&cursor={A6.nextCursor}`；（A8）`GET /api/v1/resumes/{secondAsset}/versions`；（A9）`GET /api/v1/resumes/{firstAsset}/versions?cursor=garbage`；（B1）用户 B `GET /api/v1/resume-versions/{A1.id}`；（B2）用户 B `POST /api/v1/resumes/{firstAsset}/structured-master`；（C1）准备 parse_status='processing' 的第三个 resume_asset，用户 A POST `/structured-master` | （A1）201 + `ResumeVersion`（`versionType='structured_master'`, `parentVersionId=null`, `targetJobId=null`, `seedStrategy=null`）；DB 新增 1 行 `resume_versions(version_type='structured_master', deleted_at IS NULL)`；asset.parse_status 不变；（A2）IK1 replay 返回首次 `ResumeVersion`，不创建新行；（A3）`409 + error.code='RESUME_STRUCTURED_MASTER_ALREADY_EXISTS'`，不创建第二行 master（DB partial UNIQUE INDEX 兜底）；（A4）`422 + error.code='VALIDATION_FAILED'`；不创建行；（A5）200 + ResumeVersion 字段完整；（A6）20 行 + `pageInfo.nextCursor` 非空 + 按 `updated_at DESC, id DESC` 唯一稳定序；（A7）5 行 + `hasMore=false`（store integration seed 25 行，首页 20 行，后续 5 行）；（A8）0 行 + `hasMore=false` + `nextCursor=null`；（A9）`422 + VALIDATION_FAILED`；（B1）404 不暴露存在；audit_events 不写敏感字段；（B2）404 不暴露 asset 存在；不创建任何 resume_versions 行；（C1）`422 + VALIDATION_FAILED` + `detail.reason='PARSE_NOT_READY'`；（D 字节比对）`confirmResumeStructuredMaster` 对齐 B2 fixture `default` / `idempotency-replay` / `already-exists-409` / `validation-422`；`getResumeVersion` 对齐 `default` / `not-found-404`；`listResumeVersions` 对齐 `default` / `empty` / `paginated`；（E 隐私）resume.parse 阶段 PII 边界仍维持；本 plan 不再保存 raw text 到事件 / audit / log；（F 旧口径）grep `mistakes|growth|drill` 在 `backend/internal/resume/` 0 命中；（G）`method=cmd-api-http` 或等价 live runtime evidence；no-op / skip 不可 PASS | `test/scenarios/e2e/p0-074-resume-confirm-master-and-version-reads/`；trigger 必须包含 `cd backend && go test ./cmd/api -run 'TestResumeConfirmStructuredMasterHTTPScenario\|TestResumeVersionReadHTTPScenario\|TestBuildAPIHandlerMountsResumeRoutesBehindSessionMiddleware' -count=1 -v` 与 `DATABASE_URL=... go test ./internal/resume/store -tags=integration -run 'TestStructuredMasterUnique\|TestResumeVersionListPagination' -count=1 -v` |

---

## Phase 4: updateResumeVersion merge + IK

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.075 | update resume version partial merge + IK + cross-user | A2 dev stack 拉起；用户 A 拥有 ready resume_asset + 1 行 structured_master `resume_versions` 行（含初始 structured_profile = `{headline, summary, skills:[A,B,C], sections:[...]}`）；用户 B 已登录但无版本；`cmd/api` PATCH route 已挂在新 buildResumeRuntime；缺少 live env / no-op 时本场景必须 fail | 用户 A：（A1）`PATCH /api/v1/resume-versions/{vid}` body `{structuredProfile:{summary:'new summary'}, displayName:'My Master v1'}` + IK1（partial structuredProfile 仅含 summary）；（A2）同 IK1 + 同 body 再次 PATCH；（A3）同 IK1 + 不同 body 再次 PATCH；（A4）新 IK4 + body `{versionType:'targeted'}`（不可编辑字段）；（A5）新 IK5 + body `{focusAngle:null}` + `{matchScore:0.82}`；（A6）用户 B `PATCH /api/v1/resume-versions/{vid}`；（A7）软删除 vid 后用户 A `PATCH` | （A1）200 + ResumeVersion；`structured_profile.summary` 更新为 'new summary'，`headline / skills / sections` 保留原值；`displayName` 更新；`structured_profile.provenance` 由 server 重置（不接受 client provenance 注入）；DB 新 `updated_at`；（A2）IK replay 返回首次结果；不更新 DB；（A3）`409 + IK conflict`；不更新 DB；（A4）`422 + VALIDATION_FAILED` + detail.fields 含 `versionType`；不更新 DB；（A5）200 + focusAngle=null + matchScore=0.82；（A6）`404`，不暴露存在；不更新；（A7）`404`，deleted_at 行不可编辑；（B 字节比对）`updateResumeVersion.json` `default` / `validation-error-422` + 本 plan 新增 `idempotency-replay` 字节比对 PASS；（C 隐私）log / audit 不含 structured_profile 文本；（D）`method=cmd-api-http`；no-op / skip 不可 PASS | `test/scenarios/e2e/p0-075-resume-update-version-merge-and-ik/`；trigger 必须包含 `cd backend && go test ./cmd/api -run TestResumeUpdateVersionHTTPScenario -count=1 -v` |

---

## Phase 5: branchResumeVersion 同步路径

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.076 | branch resume version sync (copy_master / blank) + IK + parent / targetJob 404 | A2 dev stack 拉起；用户 A 拥有 ready resume_asset + 1 行 structured_master version `vmaster`；用户 A 拥有 ready targetJob `tj1`；用户 B 已登录；`cmd/api` POST `/resume-versions` 已挂在新 runtime；缺少 live env / no-op 时本场景必须 fail | 用户 A：（A1）POST `/api/v1/resume-versions` `{parentVersionId:vmaster, targetJobId:tj1, seedStrategy:'copy_master', displayName:'JD A target', focusAngle:'platform leadership'}` + IK1；（A2）同 IK1 同 body replay；（A3）新 IK2 + `seedStrategy:'blank'`；（A4）新 IK3 + parentVersionId 不存在；（A5）新 IK4 + targetJobId 不存在；（A6）新 IK5 + `seedStrategy:'invalid'`；（B1）用户 B 用 A 的 parentVersionId 调 POST；（B2）用户 B 用自己创建的 vmaster + 用户 A 的 targetJobId | （A1）201 + `ResumeVersion`（`versionType='targeted'`, `parentVersionId=vmaster`, `targetJobId=tj1`, `seedStrategy='copy_master'`, `structured_profile` = vmaster.structured_profile copy + 新 provenance）；DB 新 resume_versions 行；不入队 async_jobs；（A2）IK replay 返回首次；（A3）201 + `seedStrategy='blank'` + `structured_profile.sections=[]`；（A4）404 parent；（A5）404 targetJob；（A6）422；（B1）404；（B2）404 targetJob 不属本人；（C 字节比对）`branchResumeVersion.json` `copy-master-sync` / `blank-sync` PASS；（D）`method=cmd-api-http`；no-op / skip 不可 PASS | `test/scenarios/e2e/p0-076-resume-branch-version-sync-paths/`；trigger 必须包含 `cd backend && go test ./cmd/api -run TestResumeBranchVersionHTTPScenario -count=1 -v` |

---

## Phase 5 + 6 + 7: branchResumeVersion ai_select + requestTailor + getTailorRun + resume.tailor async happy

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.077 | branch ai_select + tailor dispatch + resume.tailor async happy + outbox completed event | A2 dev stack 拉起；用户 A 拥有 ready resume_asset + structured_master `vmaster` + ready targetJob `tj1`；`cmd/api` in-process resume_tailor drainer 启动并注册 resume.tailor handler；[A3 AIClient](../../../ai-provider-and-model-routing/spec.md) 注入 stub provider 返回 success JSON（含 matchSummary + suggestions N=3）；F3 `resume.tailor.gap_review` + `resume.tailor.bullet_suggestions` feature_key prompt / rubric / profile ready；缺少 live env、drainer no-op 或 focused gate skip 时本场景必须 fail | 用户 A：（A1）POST `/api/v1/resume-versions` `{parentVersionId:vmaster, targetJobId:tj1, seedStrategy:'ai_select'}` + IK；（A2）POST `/api/v1/resume/tailor` `{resumeAssetId, targetJobId:tj1, mode:'gap_review'}` + IK；（A3）drainer `RunOnce(resume_tailor)`；（A4）`GET /api/v1/resume/tailor-runs/{A2.tailorRunId}` 在 ready 后；（A5）`GET /api/v1/resume/tailor-runs/{A2.tailorRunId}` 在 ready 前（应见 queued / generating）；（A6）IK replay A2 | （A1）202 + `BranchResumeVersionAccepted{resumeVersionId, version, job(jobType=resume_tailor, status=queued)}`；DB 新增 resume_versions + resume_tailor_runs(queued) + async_jobs(resume_tailor) 三行原子；（A2）202 + `{tailorRunId, job(jobType=resume_tailor, status=queued)}`；DB 新增 resume_tailor_runs + async_jobs；（A3）drainer claim 队列；调 AIClient profile=`resume.tailor.gap_review`；解析 JSON → resume_tailor_runs.status='ready' + match_summary + provenance；resume_version_suggestions 3 行 status='pending'；`ai_task_runs` 新行 typed columns（model_profile_name='resume.tailor.gap_review' / feature_key='resume.tailor.gap_review' / validation_status='ok'）；`outbox_events` 新行 `event_name='resume.tailor.completed' + aggregate_type='resume_tailor_run' + aggregate_id=tailorRunId + payload={tailorRunId, resumeAssetId, targetJobId, mode:'gap_review', status:'ready'}`；不含 suggested bullet / match_summary 文本；（A4）200 + ResumeTailorRun{status:'ready', matchSummary, suggestions[3], provenance}；（A5）200 + status='queued' 或 'generating' + suggestions=[]（视 RunOnce 进度）；（A6）IK replay；不创建第二个 tailor_run；（B 字节比对）`requestResumeTailor.json` `default`（含 `Idempotency-Key` request header）/ `idempotency-replay`、`getResumeTailorRun.json` `default` PASS；（C 隐私）`outbox_events` payload 不含 `suggested_bullet` / `match_summary.strengths / .gaps` 文本 / prompt input / model raw output；（D）`method=cmd-api-http`；no-op / skip 不可 PASS | `test/scenarios/e2e/p0-077-resume-tailor-async-dispatch-and-ready/`；trigger 必须包含 `cd backend && go test ./cmd/api -run 'TestResumeBranchVersionHTTPScenario\|TestResumeTailorEndpointsHTTPScenario\|TestResumeTailorDrainerHTTPScenario' -count=1 -v` |

---

## Phase 7: resume.tailor failure / retryable / non-retryable

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.078 | resume.tailor failure ready-only outbox + retry-back-to-generating | A2 dev stack 拉起；`cmd/api` in-process drainer 启动；A3 AIClient stub 三 response variant：（M1）provider timeout（retryable）/（M2）output_invalid（non-retryable）/（M3）retry 成功 JSON；用户 A 在 first asset 通过 `requestResumeTailor` 创建 3 个 queued `resume_tailor_runs` 行对应三 variant；F3 feature_key ready；缺少 live env / drainer no-op / focused gate skip 时本场景必须 fail | （A）`RunOnce(resume_tailor)` 处理 M1（timeout）；（B）`RunOnce(resume_tailor)` 处理 M2（output_invalid）；（C1）`RunOnce(resume_tailor)` 处理 M3 首次（注入 timeout）；（C2）retry 后再 `RunOnce(resume_tailor)` 处理 M3（注入 success）；（D）`cmd/api` shutdown | （A1）resume_tailor_runs.status='failed' + error_code='AI_PROVIDER_TIMEOUT'；retryable=true 写 `async_jobs` attempt metadata；（A2）outbox 不发 `resume.tailor.completed`；ai_task_runs 写 1 行 status='failed'；（A3）audit_events 写 ai_call_failed tombstone（无 prompt 内容）；（B1）resume_tailor_runs.status='failed' + error_code='AI_OUTPUT_INVALID'；retryable=false；（B2）outbox 不发 completed；ai_task_runs 写 1 行；（C1）首次 status='failed' + 'AI_PROVIDER_TIMEOUT' + retryable=true；async_jobs attempt+1；（C2）retry：先 status='generating'，最后 status='ready' + match_summary 写入；resume_version_suggestions 写入；ai_task_runs 写 2 行（首次 fail / 第二次 ok）；outbox 仅在最终 ready 发一次 `resume.tailor.completed`；（D）`cmd/api` shutdown 取消 drainer context 且不泄漏 goroutine；（E 旧口径负向）grep `inline|rewrite|mirror` in events / outbox payload 0 命中（B3 D-14 同步）；（F 隐私）prompt input / model raw output 不出现在 log / outbox payload / audit_events.last_error_message | `test/scenarios/e2e/p0-078-resume-tailor-failure-and-retry/`；trigger 必须包含 `cd backend && go test ./cmd/api -run TestResumeTailorDrainerFailureScenario -count=1 -v` |

---

## Phase 8: accept / reject 终态状态机

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.079 | accept / reject suggestion terminal state machine + IK + cross-user + accept-no-structured-profile-mutation | A2 dev stack 拉起；用户 A 拥有 ready `resume_versions(version_type='targeted', vid)` + 5 个 `resume_version_suggestions(status='pending')`；vid 关联的 `resume_versions.structured_profile` 已有初始内容；用户 B 已登录但无 suggestion；`cmd/api` POST accept / reject route 已挂在新 runtime；缺少 live env / no-op 时本场景必须 fail | 用户 A：（A1）POST `/api/v1/resume-versions/{vid}/suggestions/{s1}/accept` + IK1；（A2）同 IK1 同 path replay；（A3）新 IK2 + 同 s1 再次 accept；（A4）新 IK3 + s2 reject；（A5）新 IK4 + s2 再次 accept；（A6）新 IK5 + 不存在的 suggestion_id；（B1）用户 B accept s1；（B2）用户 B 用自己 suggestion 上调 A 的 vid 路径 | （A1）200 + ResumeVersion；`resume_version_suggestions[s1].status='accepted' + decided_at` 已写；其他 4 个 suggestion status 不变；**`resume_versions[vid].structured_profile` 不被 patch**（D-12 关键断言）；（A2）IK replay 返回首次；不写第二次 decided_at；（A3）409 already-decided；不重置 decided_at；（A4）200 + suggestion[s2].status='rejected' + decided_at；（A5）409 already-decided；（A6）404 不暴露 suggestion 不存在 vs cross-user；（B1）404；（B2）404；（C 字节比对）`acceptResumeTailorSuggestion.json` `default` / `idempotency-replay` / `already-decided-409`、`rejectResumeTailorSuggestion.json` 同样三 scenario PASS；（D 隐私）log / audit 不含 suggested_bullet / structured_profile 文本；（E）`method=cmd-api-http`；no-op / skip 不可 PASS | `test/scenarios/e2e/p0-079-resume-suggestion-accept-reject-terminal/`；trigger 必须包含 `cd backend && go test ./cmd/api -run TestResumeSuggestionAcceptRejectHTTPScenario -count=1 -v` |

---

## Phase 9: 旧口径与隐私 negative

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.080 | resume versions privacy red-line + legacy / events drift negative + retired module negative | 本 plan 全部 phase 已通过；`backend/internal/resume/` 已经过 Phase 0 baseline；outbox / audit / ai_task_runs 已落地（来自 E2E.P0.077 / P0.078 / P0.079）；当前 plan / BDD gate prose 与历史 out-of-scope 文档不纳入 raw zero-hit 扫描，避免自匹配；缺少 live env / no-op 时本场景必须 fail | （A）grep `inline\|rewrite\|mirror` in `backend/internal/resume/`（B）grep `mistakes\|growth\|drill\|inline-debrief-record` in `backend/internal/resume/`（C）outbox_events 表 `event_name='resume.tailor.completed'` 行 payload 字段集合断言；（D）audit_events 写入路径在本 plan 涉及的 handler / service / drainer 中 PII 字符串字典 grep；（E）ai_task_runs typed columns 不含 prompt_text / response_text / suggested_bullet 内容；（F）resume_tailor_runs 表 `match_summary` 列允许保存 JSON，但通过 outbox/audit/log sink 输出时不得 leak | （A）0 命中；（B）0 命中；（C）payload 仅含 `tailorRunId / resumeAssetId / targetJobId / mode / status`；不含其他字段；（D）prompt body / model raw response / suggested bullet text / match_summary 内容字符串 0 命中；（E）ai_task_runs 列断言通过；（F）log / outbox / audit 写入路径专项 unit test PASS；（G）`method=cmd-api-http` 或等价 live runtime evidence + no-op / skip 不可 PASS | `test/scenarios/e2e/p0-080-resume-versions-privacy-legacy/`；trigger 必须包含 `git grep -nE 'inline\|rewrite\|mirror' backend/internal/resume/` + `git grep -nE 'mistakes\|growth\|drill\|inline-debrief-record' backend/internal/resume/` + outbox payload privacy 断言 unit test 重跑 |
