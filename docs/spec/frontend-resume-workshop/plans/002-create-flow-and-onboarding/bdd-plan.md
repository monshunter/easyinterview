# 002 BDD Plan

> **版本**: 1.0
> **状态**: active
> **更新日期**: 2026-05-17

**关联 Plan**: [plan](./plan.md)

## 1 场景矩阵

| 场景 ID | 类别 | 关联 Phase | 关联 Spec C-* | 关联 BDD-Gate（主 checklist） |
|---------|------|-----------|--------------|----------------------------|
| E2E.P0.081 | primary + auth-boundary · CreateFlow 三 sourceType happy path + presign + register + parse polling 主路径 + IK 三链 + 隐私 + UI parity | Phase 1 + 2 + 3 + 4 + 6 | C-10（CreateFlow 主路径）, C-7 (i18n), C-8 (隐私), C-9 (旧入口) | Phase 6.8 |
| E2E.P0.082 | failure / recovery · Agent Parsing 失败态 (`parseStatus='failed'` / timeout) + cancel-and-return + retry-from-input | Phase 4 + 6 | C-10（CreateFlow 失败回退）, C-8 (隐私) | Phase 6.9 |
| E2E.P0.083 | primary + boundary + handoff · Preview Confirm 保存 v1 happy + 409 already-exists fallback + 422 validation + Home / Workspace CTA 串通 + auth pending action + UI parity | Phase 5 + 6 | C-10（Preview Confirm + CTA 串通）, C-8 (隐私), C-9 (旧入口) | Phase 6.10 |

---

## Phase 1 + 2 + 3 + 4 + 6: CreateFlow 三 sourceType happy + parse polling 主路径

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.081 | CreateFlow upload / paste / guided 三路径 happy + presign + register + parse polling + IK + 隐私 + UI parity + retired 负向 | A2 dev stack 拉起；fixture-backed mock-first 启用；`Uploads/createUploadPresign.json` `default`、`Resumes/registerResume.json` `default / paste-text / guided-answers`、`Resumes/getResume.json` `default` 可用；用户初始未登录，登录后 lang=zh-CN；上游 backend-resume/002 Phase 1 `confirmResumeStructuredMaster` fixture 已就位（虽然本场景不消费 confirm，但 plan 总 gate 要求） | （A）未登录加载 `/resume_versions?flow=create`；（B）完成 mock 登录并恢复 `/resume_versions?flow=create`；（C）选择 Upload tab → 选择有效 PDF (1.5MB) → 触发 createUploadPresign + 浏览器 PUT + registerResume；（D）切换 Paste tab → 输入 ≥ 30 字 rawText → 提交 registerResume；（E）切换 Guided tab → 5 step 各填非空回答 → 提交 registerResume；（F）三条路径都进入 ParseFlow → 轮询 getResume → parseStatus=ready；（G）lang 切换 zh → en | （A1）未登录态渲染 auth gate / 登录 CTA，不触发 createUploadPresign / registerResume / getResume；pendingAction 仅携带 `{ flow: 'create', createMode? }`，不含 raw text / file binary / guidedAnswers；（B1）路由：`ResumeWorkshopScreen` 渲染 `ResumeCreateFlow`，testid `resume-create-flow` 命中；TopBar `topbar-nav-resume_versions` 高亮；（B2）三 tab 渲染：`resume-create-tab-upload` (active 默认) / `-paste` / `-guided` 与 UI 真理源 1:1 复刻；（C1）UploadTab：`resume-create-upload-dropzone` testid 命中；触发选文件后 hook `useResumePresignUpload` 调 createUploadPresign（request spy 断言 `Idempotency-Key` header + `purpose='resume'` + 真实 fileName/contentType/byteSize）；presign 成功后 `fetch(uploadUrl, { method, headers, body: file })` 触发（spy 断言）；PUT 成功后 hook 返回 fileObjectId → 触发 registerResume `{ sourceType: 'upload', fileObjectId, title, language }` + 新 IK；（D1）PasteTab：textarea testid `resume-create-paste-textarea`；非空时 submit 启用；submit 触发 registerResume `{ sourceType: 'paste', rawText, title, language }` + IK；不上传 file；（E1）GuidedTab：左栏 5 step nav 命中 testid `resume-create-guided-step-{1..5}`；右栏 textarea；最后一步 "Generate v1" 触发 registerResume `{ sourceType: 'guided', guidedAnswers: { recentRole, direction, proofProject, metrics, target }, title, language }` + IK；（F1）三条路径完成 register 后 stage='parsing'：渲染 `ResumeParseFlow`，7 step ticker 显示（testid `resume-parse-step-{0..6}`）+ "取消并返回修改" button；hook `useResumeParsingPolling` 调 getResume 至少 1 次（mock client deterministic step → ready）；（F2）parseStatus=ready 后 transition 到 `ResumePreviewConfirm` stage='preview'；本场景不实施 confirm 提交（归 P0.083）；（G1）lang 切换：CreateFlow / ParseFlow / PreviewConfirm 三屏关键文案 zh→en；Accept-Language 在 4 个 op 的请求 header 中携带；（H IK）createUploadPresign / registerResume 两个 op 必含 `Idempotency-Key` header；getResume 不含 IK header；（I UI parity）desktop 1440px / mobile 390x844 DOM anchor + computed style + bounding box + screenshot smoke PASS；clean checkout 不依赖未跟踪 baseline；（J 旧入口）grep `welcome|mistake|growth|drill|followup|STAR|experiences|voice|OnboardingScreen|onboarding=true` in `frontend/src/app/screens/resume-workshop/create/` 0 命中；grep `ui-design/src/(data|screen-resume-workshop)` 0 命中；（K 隐私）rawText / guidedAnswers / file binary / parsedTextSnapshot / parsedSummary 0 出现在 console / URL / pendingAction / localStorage / mock transport log；（L mock-first）4 个 op response 与 generated client 期望字节一致；上游 fixture 状态满足 plan §3.1 operation matrix | `test/scenarios/e2e/p0-081-resume-create-flow-upload-paste-guided-happy/` |

## Phase 4 + 6: Agent Parsing 失败 + cancel-and-return

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.082 | Parse failed / timeout / cancel-and-return + retry-from-input + 隐私 | A2 dev stack 拉起；fixture-backed mock-first 启用；`Resumes/registerResume.json default` 与 `Resumes/getResume.json default` 可用；mock client harness 支持 attempt-aware `parseStatus` stepping（在 fixture 未提供 `queued / generating / failed` scenario 时使用 deterministic stepping），并显式标注；用户已登录 lang=zh-CN | （A）选择 Paste tab → 输入 → 提交 → 进入 ParseFlow；mock harness 模拟 `queued → generating → failed (errorCode='AI_TIMEOUT_RETRYABLE')`；（B）失败态后点击 "重试解析"；mock harness 第二次模拟 `queued → ready`；（C）选择 Guided tab → 提交 → ParseFlow；mock harness 模拟 `queued → generating` 共 8 attempt 后保持 generating；（D）超过 attempt 上限触发 `parseTimeout`；（E）任一 ParseFlow 阶段点击 "取消并返回修改" | （A1）ParseFlow 渲染 7 step ticker + polling 进度；parseStatus 终态 'failed' 后渲染 `resume-parse-failed-state` testid + 错误文案 enum 映射（不回显原 envelope）+ "重试解析" / "返回输入" 两 CTA；（A2）DOM 不渲染 parsedTextSnapshot / parsedSummary（这些字段在 failed 路径本就 null，但仍需断言 grep）；（B1）"重试解析" 点击触发 hook 重启 polling（不重新 createUploadPresign / registerResume），getResume 二次 attempt 序列 → ready → stage='preview'；（C1）模拟 generating 持续 8 attempt 后超过 ~30s 上限；（D1）parseTimeout 渲染同样 failed-state 模式但 errorCode='PARSE_TIMEOUT'；（E1）"取消并返回修改" 点击：stage='input' 且原 createMode + rawText / guidedAnswers / pickedFile 保留；不触发新的 registerResume；URL 不堆叠 stage 历史；（F 隐私）失败 toast / error state 文案不含 raw text / parsedSummary；mock transport log 不含 raw text；（G）`method=mock-fixture-client` 显式标注；测试本身不声称 backend real polling 闭环 | `test/scenarios/e2e/p0-082-resume-create-flow-parsing-failure-and-retry/` |

## Phase 5 + 6: Preview Confirm 保存 v1 + 409 fallback + 422 + CTA 串通

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.083 | PreviewConfirm 保存 v1 happy + idempotency replay + 409 already-exists fallback + 422 validation inline + Home / Workspace CTA 串通 + auth pending action + UI parity | A2 dev stack 拉起；fixture-backed mock-first 启用；`Uploads/createUploadPresign.json default`、`Resumes/registerResume.json default`、`Resumes/getResume.json default`、`Resumes/confirmResumeStructuredMaster.json default / idempotency-replay / already-exists-409 / validation-422`、`Resumes/listResumeVersions.json master-only` 与 plan 001 `Resumes/listResumes.json default` 可用；用户初始未登录，登录后 lang=zh-CN | （A）未登录态访问 `/home` → 点击 `还没有简历？1 分钟创建` CTA；（B）登录恢复后到 `/resume_versions?flow=create` 渲染 `ResumeCreateFlow`；（C）选 Upload tab → 完成 presign + register + parse ready → 进入 PreviewConfirm；（D）点击 "确认并保存 v1" → 触发 confirmResumeStructuredMaster + IK；（E）同 IK + 同 body 再次点击 confirm（mock harness 选 `idempotency-replay`）；（F）新 IK + 同 asset 再次 confirm（mock harness 选 `already-exists-409`）；（G）模拟 422 路径：confirm 前修改 mock harness 选 `validation-422`；（H）未登录态访问 `/workspace` 模拟无简历 → 点击 `WorkspaceMissingResumeState` 中 "创建简历" CTA | （A1）未登录态：home CTA click 触发 nav `resume_versions?flow=create` + 设置 pendingAction `{ route: 'resume_versions', params: { flow: 'create' } }`；不触发 createUploadPresign / registerResume / confirmResumeStructuredMaster；（B1）登录恢复后渲染 `ResumeCreateFlow`，testid `resume-create-flow` 命中；TopBar 高亮；（C1）presign + register + parse polling 与 P0.081 相同主路径，最终 stage='preview'；PreviewConfirm 渲染：testid `resume-preview-confirm-content` / `-save-button` / `-back-button` / `-sidebar-what-saved` / `-sidebar-parse-notes`；草稿主体渲染 identity / summary / experience / projects / skills / education，由 adapter `mapParsedSummaryToStructuredProfileDraft` 投影；（D1）confirm 触发 confirmResumeStructuredMaster `(resumeAssetId, { structuredProfile, displayName, language }, { idempotencyKey })`；request spy 断言 `Idempotency-Key` header；fixture `default` 返回 201 + ResumeVersion；toast `已保存 v1 主版本 · 进入简历工坊` 显示；nav `resume_versions` (list)；plan 001 list 中 testid `resume-tree-row-{newAssetId}` 命中（mock-first 模拟）；（E1）`idempotency-replay`：同 IK 二次返回首次 ResumeVersion；不重复 toast / nav；（F1）`already-exists-409`：toast `已存在主版本 · 跳转查看` 显示；hook 调 listResumeVersions → 找到 versionType='structured_master' 行 → nav `resume_versions?versionId={masterId}&tab=preview`（mock harness fixture `master-only` 命中）；（G1）`validation-422`：inline error 显示在 PreviewConfirm 内（不 nav），保留草稿；error 文案仅含 enum / generic，不回显 envelope；（H1）Workspace CTA：missing-resume 空态 click → pendingAction `{ flow: 'create' }`；登录恢复后渲染 `ResumeCreateFlow`；（I IK）confirm 三态都包含 `Idempotency-Key` header；同一用户操作内 happy / replay 复用同 IK；409 / 422 视为重新提交触发新 IK；（J UI parity）PreviewConfirm desktop / mobile DOM + computed style + bounding box + screenshot smoke PASS；（K 旧入口）grep `OnboardingScreen|onboarding=true` 与 retired 字面量在 `frontend/src/app/screens/resume-workshop/create/` 与 `frontend/src/app/screens/home/` / `frontend/src/app/screens/workspace/` 中本场景关联文件 0 命中（不破坏既有 plan 001 / home / workspace 范围）；（L 隐私）structuredProfile 内容渲染在 DOM 但 URL / pendingAction / localStorage / mock transport log / toast 文案不含；（M）`method=mock-fixture-client` 显式标注；上游 backend-resume/002 Phase 1 fixture 已就位（如缺失则本场景 fail 在 setup 阶段） | `test/scenarios/e2e/p0-083-resume-create-flow-preview-confirm-and-cta-handoff/` |
