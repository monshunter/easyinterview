apiVersion: plancontext.agent.dev/v1alpha1
kind: PlanContext
metadata:
  subspec: frontend-resume-workshop
  name: 001-listing-routing-and-detail-readonly
  sequence: 1
  specVersion:
    from: null
    to: 2.15
spec:
  defaultTarget: frontend
  discovery:
    aliases:
      - frontend-resume-workshop
      - 001-listing-routing-and-detail-readonly
      - ResumeWorkshopScreen
      - ResumeListView
      - ResumeDetailView
      - ResumeSummary
      - PaginatedResume items ResumeSummary
      - resume-workshop-flat-list
      - resume-detail-readonly
    keywords:
      - resume workshop route shell
      - flat resume list table
      - resumeId detail readonly
      - resumeId detail read-only resume body
      - out-of-scope tab tailorRunId ignored
      - resume auth gate no fetch before login
      - listResumes fixture backed rows
      - listResumes closed ResumeSummary items
      - resume list detail responsibility separation
      - strictmode request dedup single actual transport
      - rejected in-flight request evicted retry succeeds
      - getResume read-only detail body
      - getResumeSource PDF page stack source
      - markdown body displayName injection forbidden
      - markdown pdf shared reading surface
      - detail export copy original rewrites edit absent
      - resume workshop i18n en zh
      - resume workshop a11y read-only detail
      - resume workshop privacy route params
      - ui parity desktop mobile
      - out-of-scope route testid negative
      - ui-design data screen runtime import forbidden
  targets:
    frontend:
      plan: ./plan.md
      checklist: ./checklist.md
      spec: ../../spec.md
      bddPlan: ./bdd-plan.md
      bddChecklist: ./bdd-checklist.md
      discovery:
        packages:
          - frontend/src/app
          - frontend/src/app/screens/resume-workshop
          - frontend/src/app/screens/resume-workshop/components
          - frontend/src/app/screens/resume-workshop/adapters
          - frontend/src/app/screens/resume-workshop/hooks
          - frontend/src/app/i18n
          - frontend/src/api/generated
          - frontend/src/lib/conventions
          - frontend/tests/pixel-parity
          - ui-design/src
          - docs/ui-design
        uiRoutes:
          - resume_versions
        apiNames:
          - listResumes
          - getResume
          - getResumeSource
