# 001 BDD Plan

> **版本**: 1.2
> **状态**: completed
> **更新日期**: 2026-05-21

**关联 Plan**: [plan](./plan.md)

## 1 场景矩阵

| 场景 ID | 类别 | 关联 Phase | 关联 Spec C-* | 关联 BDD-Gate（主 checklist） |
|---------|------|-----------|--------------|----------------------------|
| E2E.P0.036 | primary + alternate + auth-boundary · resume list 默认渲染 + tree/flat 切换 + StatsStrip + fixture-derived counts + 旧入口负向 + UI parity | Phase 1 + 2 + 4 + 5 | C-1, C-2, C-3, C-5, C-6, C-7, C-8, C-9 | Phase 5.5 |
| E2E.P0.037 | primary + boundary + failure · resume detail tab routing + Preview Tab + 原件弹层 + export 501 fallback + 404 fallback + UI parity | Phase 1 + 3 + 4 + 5 | C-4, C-5, C-6, C-7, C-8 | Phase 5.6 |

---

## Phase 1 + 2 + 4 + 5: resume list 默认渲染 + tree/flat 切换

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.036 | resume list 默认渲染 + tree/flat ViewSwitcher + StatsStrip + i18n + auth boundary + UI parity + retired 负向 | A2 dev stack 拉起；fixture-backed mock-first 启用；`listResumes.json` `default` / `empty` / `paginated` 与 scenario-scoped `listResumeVersions.json` `default` / `master-only` / `with-targeted-branches` 可用；用户初始未登录，登录后 lang=zh-CN | （A）未登录加载 `/resume_versions`；（B）完成 mock 登录并恢复 `/resume_versions`；（C）点击 ViewSwitcher 切换 flat；（D）点击某 ResumeVersionRow 进入详情（导航行为）；（E）lang 切换 zh → en；（F）切换 empty / paginated / partial version fixture 状态 | （A1）未登录态渲染 auth gate / 登录 CTA，不触发 `listResumes` / `listResumeVersions`；pendingAction 只携带 `flow/versionId/tab/branchOriginalId` 等 route params，不携带 raw resume 内容；（B1）路由替换：`ResumeWorkshopScreen` 渲染而非 `PlaceholderScreen`；TopBar `topbar-nav-resume_versions` 高亮；（B2）StatsStrip 4 项 testid 命中：`resume-workshop-stats-originals/versions/top-match/recent`，数字从当前 fixture `items.length` / aggregated versions 派生，不写死静态原型规模；（B3）ViewSwitcher testid `resume-workshop-view-switcher-tree` (active) / `-flat` 渲染；（B4）默认 tree 视图按 `resumeAssetId` 分组渲染 `resume-tree-row-{resumeAssetId}` + `resume-version-row-{versionId}`，折叠可展开；当前 fixture 只给 `01918fa0-0000-7000-8000-000000001000` 返回 versions，`listResumes.default` 第二个 asset 必须显示 no-versions/partial indicator；（B5）"选为底稿" / "基于这棵树新建版本" 按钮渲染但 click 显示 toast "即将开放"；（C1）点击 `view-switcher-flat`：渲染 `ResumeFlatView`，`resume-flat-row-{versionId}` 按 `matchScore DESC nullsLast / updatedAt DESC` 排序；（D1）点击 version row → `nav("resume_versions", { versionId, tab: resumeDefaultTab(version) })`；TARGETED version 使用 `tab=rewrites`，MASTER 使用 `tab=preview`；（E1）lang 切换：StatsStrip 文案 zh→en；generated client 请求 `Accept-Language: en` 携带；（F1）empty list 显示空态；paginated `hasMore=true` 显示继续加载 / pagination 提示；version.resumeAssetId 不匹配时显示 partial/error state 且不伪造版本；（G UI parity）desktop 1440px / mobile 390x844 DOM anchor + computed style + bounding box + screenshot smoke PASS；只有稳定 baseline 存在时才使用 screenshot diff；（H 旧入口）grep `welcome|mistake|growth|plan|drill|followup|onboarding|STAR|experiences|voice` in `frontend/src/app/screens/resume-workshop/` 0 命中；grep `ui-design/src/(data|screen-resume-workshop)` 0 命中；（I 隐私）raw text / parsedTextSnapshot / parsedSummary / structuredProfile / suggestion text 0 出现在 console.log / URL / pendingAction / localStorage / mock transport log；（J mock-first）字节比对 generated client 期望与 fixture scenario shape 一致，且测试不声称 mock transport 已支持 path-param-specific fixture selection | `test/scenarios/e2e/p0-036-resume-list-tree-flat-toggle/` |

## Phase 1 + 3 + 4 + 5: resume detail tabs + Preview Tab + 原件弹层

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.037 | resume detail tab routing + Preview Tab 主路径 + 原件弹层 a11y + export 501 fallback + 404 fallback + UI parity | A2 dev stack 拉起；fixture `getResumeVersion.json` `default` / `master-default` / `targeted-with-suggestions` / `not-found-404`、`getResume.json` `default` / `master-default` / `not-found` 与 `exportResumeVersion.json` `p0-501-not-available` 可用；用户初始未登录，登录后 lang=zh-CN | （A）未登录直接访问 `/resume_versions?versionId={id}`；（B）登录恢复后从 list view 点击 MASTER version → 详情；（C）从 list view 点击 TARGETED version → 详情；（D）在 Preview Tab 点击 "查看原件" → 原件弹层；（E）原件弹层中 ESC / 外层遮罩 / X 按钮；（F）keyboard Tab 在 modal 内 focus trap；（G）点击导出 PDF；（H）访问不存在的 versionId | （A1）未登录态不触发 `getResumeVersion` / `getResume`，登录后恢复相同 `versionId/tab` route params；（B1）MASTER 渲染 `ResumeDetailView`：testid `resume-detail-breadcrumb` / `resume-detail-branch-graph` / `resume-detail-tab-{preview,rewrites,edit}` / `resume-detail-preview-content` 全部命中；默认 active tab=preview；MASTER 的 rewrites tab disabled 或展示主版本保持干净提示；（B2）Preview 内容来自 adapter 投影，不运行时 import `ui-design/src/screen-resume-workshop.jsx`；（C1）TARGETED 进入：默认 active tab=rewrites；001 阶段 rewrites 内容可为 `<ComingSoonTab>`，但 plan 003 完成后当前真理源为 `ResumeRewritesTab`（testid `resume-rewrites-tab`），旧 `resume-detail-tab-content-coming-soon-rewrites` 不应再作为场景 PASS 依据；active tab / URL `tab` 不改写为 preview；用户点击 preview 后渲染 `resume-detail-preview-content`；（D1）点击 `resume-detail-view-original` 按钮 → 通过 `getResume(version.resumeAssetId)` 读取 source asset，原件弹层 modal 渲染 `original-modal-content` + `parsedTextSnapshot` / `originalText` 原件 text；DOM `role="dialog"` + `aria-labelledby` 完整；（E1）ESC 关闭 / 外层遮罩点击关闭 / X 按钮关闭 → focus 回到 view-original 触发按钮；（F1）modal 内 Tab key 在按钮 / 关闭按钮间循环，不漏出 modal；（G1）`exportResumeVersion` 调用使用 `generateIdempotencyKey()`，request spy 断言 `Idempotency-Key` header；返回 501 / `RESUME_EXPORT_NOT_AVAILABLE` 时显示 "PDF 导出能力即将开放" toast，不生成 blob、不写 localStorage、不把错误 envelope 暴露给用户；（H1）访问 `getResumeVersion(non-existent)` 返回 404/default error envelope → 渲染 `<NotFoundEmptyState>` + "返回列表" CTA，UI copy 不依赖 fixture `error.code` 具体值；（I UI parity）detail view + modal desktop / mobile DOM/computed/bounding box + screenshot smoke PASS；focus ring computed style 与 ui-design 一致；（J 隐私）原件 text 在 DOM 中渲染但不出现在 console / URL / pendingAction / localStorage；modal 关闭后 DOM unmount（不持久）；（K a11y）axe-core check pass：role / aria / contrast / focus visible | `test/scenarios/e2e/p0-037-resume-detail-preview-readonly/` |
