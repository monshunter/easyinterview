# 003 BDD Plan

> **版本**: 1.0
> **状态**: completed
> **更新日期**: 2026-05-18

**关联 Plan**: [plan](./plan.md)

## 1 场景矩阵

| 场景 ID | 类别 | 关联 Phase | 关联 Spec C-* | 关联 BDD-Gate（主 checklist） |
|---------|------|-----------|--------------|----------------------------|
| E2E.P0.084 | primary + boundary + auth · BranchFlow 三 seedStrategy 主路径 + IK + 422 + 404 parent / targetJob + cross-user + UI parity | Phase 1 + 2 + 7 | C-11 (BranchFlow 三 seed), C-8 (隐私), C-9 (旧入口) | Phase 7.6 |
| E2E.P0.085 | primary + failure + auth · Rewrites Tab + ai_select 后 tailor run polling + 重新运行改写 + ready/failed/timeout + 隐私 + UI parity | Phase 2 + 3 + 5 + 7 | C-11 (Rewrites Tab + tailor polling), C-8 (隐私) | Phase 7.7 |
| E2E.P0.086 | primary + boundary · accept / reject / manual edit 终态 + updateResumeVersion 保存 + IK + 409 already-decided + 422 + cross-user + accept-no-structured-profile-mutation (D-12) | Phase 4 + 6 + 7 | C-11 (终态状态机 + Edit Tab), C-8 (隐私) | Phase 7.8 |
| E2E.P0.087 | regression + boundary · exportPDF / copyText 一致性 + Rewrites / Edit Tab desktop+mobile UI parity + retired tailor mode 与旧入口负向 grep | Phase 7 | C-11 (一致性 + parity), C-9 (旧入口), C-8 (隐私) | Phase 7.9 |

---

## Phase 1 + 2 + 7: BranchFlow 三 seedStrategy + IK + 422 + cross-user + UI parity

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.084 | BranchFlow 主路径 + 三 seedStrategy + IK 行为 + 422 + 404 parent / targetJob + cross-user + UI parity + 旧入口负向 | A2 dev stack 拉起；fixture-backed frontend path 启用，并记录 Phase 0 real-backend preflight；`Resumes/listResumes.json default` + `Resumes/listResumeVersions.json with-targeted-branches` + `Resumes/branchResumeVersion.json default / copy-master-sync / blank-sync / ai-select-202-with-job / idempotent-replay / validation-error-422` 可用；用户 A 初始未登录，登录后 lang=zh-CN；用户 A 拥有 MASTER 版本 `vmaster`；用户 B 登录但不拥有该 MASTER | （A）未登录加载 `/resume_versions?flow=branch&branchOriginalId={origId}`；（B）登录恢复；（C）填写 name + target + focus + seed='copy_master' → "创建版本"；（D）同 IK 再次提交（replay）；（E）切到 seed='blank' 新 IK + name + target → 提交；（F）切到 seed='ai_select' 新 IK + name + target → 提交；（G）提交缺 target 字段 → 触发 422；（H）切换 branchOriginalId 到不存在 ID；（I）用户 B 用 A 的 `parentVersionId` + 自己的 IK 提交；（J）lang 切换 zh→en | （A1）未登录态：渲染 auth gate / 登录 CTA；不触发 branchResumeVersion；pendingAction 仅携带 `{ flow: 'branch', branchOriginalId }`，不含 form draft；（B1）路由替换：`ResumeWorkshopScreen` 渲染 `ResumeBranchFlow`；testid `resume-branch-flow` 命中；BRANCHING FROM 卡显示 original + master；TopBar `topbar-nav-resume_versions` 高亮；（B2）表单 fields 渲染：`resume-branch-field-name / -target / -focus-chip-{platform,collaboration,fullstack,leadership,custom} / -seed-card-{copy_master,blank,ai_select}` 与 UI 真理源 1:1 复刻；submit disabled when name/target empty；（C1）copy_master 提交：request spy 断言 `Idempotency-Key` header + payload `{ parentVersionId, targetJobId, seedStrategy: 'copy_master', displayName, focusAngle }`；fixture `copy-master-sync` 返回 201 + ResumeVersion；toast `已创建定制版本` 显示；nav `resume_versions?versionId={newId}&tab=rewrites`；（D1）IK replay：同 IK 二次返回首次 ResumeVersion；不重复 nav；不创建额外行；（E1）blank seed：201 + ResumeVersion；nav `resume_versions?versionId={newId}&tab=edit`（空白起步默认进入 Edit）；（F1）ai_select：202 + `BranchResumeVersionAccepted{resumeVersionId, version, job(jobType=resume_tailor, status=queued)}`；nav `resume_versions?versionId={newId}&tab=rewrites`；Rewrites Tab 渲染 polling banner（tailorRunId 已注入）；（G1）422 `VALIDATION_FAILED`：inline error 显示在 target field；不创建行；（H1）branchOriginalId 不存在：NotFound CTA 渲染；不触发 branchResumeVersion；（I1）用户 B：404 parent 不属本人；不创建行；toast generic；（J1）lang 切换：BranchFlow 关键文案 zh→en；`Accept-Language` header 携带 branchResumeVersion / listResumes / listResumeVersions 请求；（K UI parity）BranchFlow desktop 1440px / mobile 390x844 DOM anchor + computed style + bounding box + screenshot smoke PASS；（L 旧入口）grep `OnboardingScreen|inline|rewrite|mirror` in `frontend/src/app/screens/resume-workshop/branch/` 0 命中；prototype import grep 0；（M 隐私）form draft / parentVersion / focusAngle 0 出现在 console / URL / pendingAction / localStorage / mock transport log；（N fixture parity）字节比对 generated client 期望与 fixture 六 scenario shape 一致，并附 backend real route preflight evidence | `test/scenarios/e2e/p0-084-resume-branch-flow-three-seed-strategies/` |

## Phase 2 + 3 + 5 + 7: Rewrites Tab + ai_select polling + 重新运行改写

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.085 | Rewrites Tab 主路径 + ai_select branch handoff 后 tailor run polling + 重新运行改写 + ready/failed/timeout + UI parity | A2 dev stack 拉起；fixture-backed frontend path 启用，并记录 Phase 0 real-backend preflight；`Resumes/branchResumeVersion.json ai-select-202-with-job` + `ResumeTailor/requestResumeTailor.json default / idempotency-replay` + `ResumeTailor/getResumeTailorRun.json queued / generating / default(ready) / failed` + `Resumes/getResumeVersion.json targeted-with-suggestions` 可用；fixture-backed harness 支持 deterministic tailor run status sequence；用户已登录 lang=zh-CN；已有 MASTER 版本 + targetJobId `tj1` | （A）BranchFlow 提交 ai_select → 自动 nav 到 Rewrites Tab；fixture sequence 模拟 tailor run `queued → generating → ready`；（B）ready 后 Rewrites Tab 渲染 suggestions[]；（C）点击 "重新运行改写" 选 mode='gap_review'；fixture sequence 模拟新 tailor run `queued → generating → failed`；（D）失败 banner 点击 "重试" → 新 tailor run `queued → generating × 12 → timeout`；（E）超过 attempt 上限触发 polling timeout；（F）切到 Edit Tab 再切回 Rewrites Tab；（G）组件 unmount（用户返回 list）| （A1）ai_select 提交后 nav `resume_versions?versionId={newId}&tab=rewrites`；Rewrites Tab 渲染 polling banner testid `resume-rewrites-polling-banner` + 进度文案；hook `useResumeTailorRunPolling` 调 getResumeTailorRun 至少 2 次（fixture-backed sequence）；ready 后 banner 消失 + 触发 `getResumeVersion` refetch；列表渲染 `resume-rewrites-bullet-row-{id}`；（A2）`getResumeTailorRun` 请求不含 `Idempotency-Key` header；`requestResumeTailor` 请求含 IK header；（B1）suggestions[] 渲染：每行 section / status 圆点 / 截断 rewritten；计数 chip `accepted=X / pending=Y / rejected=Z` 从 fixture 派生；scope banner `resume-rewrites-scope-banner` 含 `role="status"` + `aria-live="polite"`；（C1）"重新运行改写" click → 请求 `requestResumeTailor({ resumeAssetId, targetJobId, mode: 'gap_review' })` + IK；返回 tailorRunId；hook 启动 polling；（C2）failed 终态：失败 banner testid `resume-rewrites-failed-banner`，按当前 OpenAPI / fixture 无 `errorCode` 的事实渲染通用失败文案，不回显 raw envelope；列表保持上一次 ready 的 suggestions；（D1）"重试" click → 新 IK + requestResumeTailor；polling 12 attempt 后超出上限；（E1）polling timeout 渲染 timeout banner + 重试 CTA；（F1）切 tab：Edit Tab 渲染但 polling 状态保留在父 detail container；切回 Rewrites Tab 进度可见；（G1）unmount：polling cleanup，network sniff 不再出现 getResumeTailorRun 请求；（H UI parity）Rewrites Tab desktop / mobile DOM + computed style + bounding box + screenshot smoke PASS；（I 隐私）originalBullet / suggestedBullet / matchSummary 文本仅渲染在 DOM；console / URL / pendingAction / localStorage / mock transport log 0 命中；（J）`method=fixture-backed-frontend` 显式标注，并附 backend real route preflight evidence；（K 旧入口）`(^|[^A-Za-z0-9_])(inline|rewrite|mirror)([^A-Za-z0-9_]|$)` 在 tabs/ 内 0 命中；retired modules grep 0 | `test/scenarios/e2e/p0-085-resume-rewrites-tab-tailor-run-polling/` |

## Phase 4 + 6 + 7: accept / reject / manual edit + updateResumeVersion 保存

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.086 | 单条 suggestion 终态状态机 + manual edit explicit patch + Edit Tab updateResumeVersion 主路径 + IK + 409 already-decided + 422 + cross-user + accept 不自动改 structured_profile | A2 dev stack 拉起；fixture-backed frontend path 启用，并记录 Phase 0 real-backend preflight；`Resumes/getResumeVersion.json targeted-with-suggestions` + `Resumes/acceptResumeTailorSuggestion.json default / idempotency-replay / already-decided-409` + `Resumes/rejectResumeTailorSuggestion.json default / idempotency-replay / already-decided-409` + `Resumes/updateResumeVersion.json default / idempotency-replay / validation-error-422` 可用；用户 A 已登录 lang=zh-CN；用户 A 拥有 TARGETED 版本 `v1` + 5 pending suggestions；用户 B 已登录无版本 | （A）打开 `v1` Rewrites Tab；点击 bullet b1 → Accept；（B）同 IK + 同 b1 再次 Accept（replay）；（C）新 IK + 同 b1 再次 Accept；（D）选 b2 → 点击 Edit → 修改 textarea → Save manual edit；（E）选 b3 → Reject；（F）同 IK + 同 b3 再次 Reject；（G）新 IK + 同 b3 再次 Reject；（H）切到 Edit Tab；修改 headline + summary → 保存改动；（I）同 IK + 同 body 再次保存（replay）；（J）尝试 hook 调用传入 `versionType` 字段（应被 mapper 过滤）；（K）模拟 422 路径：updateResumeVersion 缺字段；（L）用户 B 调 acceptResumeTailorSuggestion(v1.id, b1.id) | （A1）Accept request spy 断言 `Idempotency-Key` header 且 request body 为 `undefined`；fixture `default` 返回 200 + version；UI 行 b1 status 变 `accepted`；列表行 line-through 不出现（仅 reject 时出现）；计数 chip accepted+1 / pending-1；DOM 断言 `version.structured_profile` 不被 patch（D-12 关键断言）；（B1）replay：返回首次结果；UI 状态不变；不调用 listResumeVersions；（C1）新 IK + 同 b1：返回 409 `VALIDATION_FAILED` + `error.details.reason='SUGGESTION_ALREADY_DECIDED'`；toast `该 bullet 已决定 · 如需重做请新建 suggestion 或 branch`；UI 状态保持 accepted；（D1）b2 inline edit：textarea 渲染当前 rewritten；Save manual edit 先触发 `updateResumeVersion` patch `structuredProfile.manualEdits[]`（含 suggestionId + edited text）+ IK，成功后再触发 bodyless `acceptResumeTailorSuggestion` + IK；若 update 成功但 accept 失败，渲染 saved-manual-pending retry，不重复写 manual edit；UI 行 b2 status 变 accepted (manual)；（E1）b3 Reject：return 200 + version；request body 为 `undefined`；UI 行 status 变 rejected + line-through；（F1）reject replay：返回首次；UI 状态不变；（G1）reject 新 IK 二次：409 already-decided；toast；UI 状态保持 rejected；（H1）Edit Tab：testid `resume-edit-tab` + `-headline-input` / `-summary-textarea` / `-save-button`；保存触发 updateResumeVersion `{ structuredProfile: { headline, summary } }` + IK；fixture `default` 返回 200 + version；toast `已保存改动`；getResumeVersion refetch 触发；（I1）replay：同 IK 返回首次；不重复 toast；（J1）mapper 过滤 `versionType` 字段，hook 不发送到 generated client；console error / lint warning 显示开发期警告（dev mode）；（K1）422：fixture `validation-error-422` 返回；inline error；不刷新 DOM 字段；（L1）用户 B：404；toast generic；（M）IK 行为：accept/reject/update 三类 op 都包含 `Idempotency-Key` header；replay / 409 / 422 三态映射正确；（N 隐私）structured_profile / originalBullet / suggestedBullet / manual edit text 0 出现在 console / URL / pendingAction / localStorage / mock transport log / toast 内容；（O）`method=fixture-backed-frontend` 显式标注，并附 backend real route preflight evidence | `test/scenarios/e2e/p0-086-resume-suggestion-accept-reject-edit-and-update-version/` |

## Phase 7: exportPDF / copyText 一致性 + 三屏 UI parity + 旧入口负向

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.087 | BranchFlow + Rewrites Tab + Edit Tab desktop+mobile UI parity + exportPDF P0 stub / copyText 真实可用一致性 + 旧入口与 retired tailor mode 负向 grep + 隐私 | A2 dev stack 拉起；fixture-backed frontend path 启用，并记录 Phase 0 real-backend preflight；plan 001 P0.037 中 `exportResumeVersion.json p0-501-not-available` 与 plan 003 BranchFlow / RewritesTab / EditTab 主路径 fixture 都可用；用户已登录 lang=zh-CN；用户 A 已有 TARGETED 版本 `v1` | （A）desktop 1440px viewport：进入 BranchFlow / `v1` Rewrites Tab / `v1` Edit Tab 三屏；（B）mobile 390x844 viewport：同上；（C）`v1` Rewrites Tab 顶 header 点击 Export PDF 按钮；（D）`v1` Edit Tab 顶 header 点击 Export PDF 按钮；（E）`v1` Rewrites Tab 顶 header 点击 复制纯文本 按钮；（F）`v1` Edit Tab 顶 header 点击 复制纯文本 按钮；（G）页面源码与 DOM 检视 retired tailor mode + 旧入口字面量 | （A1/A2/A3）三屏 desktop DOM anchor + computed style + bounding box + screenshot smoke PASS；clean checkout 不依赖未跟踪 baseline；（B1/B2/B3）三屏 mobile 同上；（C1/D1）Export PDF click：generated client `exportResumeVersion(v1.id, { idempotencyKey })` 调用；request spy 断言 `Idempotency-Key` header；fixture `p0-501-not-available` 返回 501 + `RESUME_EXPORT_NOT_AVAILABLE`；toast `PDF 导出能力即将开放`；不生成 blob / 不写 localStorage / 不刷新 detail；plan 001 P0.037 中 export 行为不退化；（E1/F1）复制纯文本 click：`buildResumePlainText` 投影通过 navigator.clipboard.writeText 写入；clipboard 可用环境下 toast `已复制 ${version.name} 的纯文本`；不可用环境下 toast `当前环境不支持剪贴板写入`（与 plan 001 实现一致）；（G1）retired tailor mode grep：`(^|[^A-Za-z0-9_])(inline|rewrite|mirror)([^A-Za-z0-9_]|$)` 在 `frontend/src/app/screens/resume-workshop/branch/` 与 `tabs/` 内 0 命中（与 [B3 D-14](../../../event-and-outbox-contract/spec.md#31-已锁定决策含-jobtype-映射表) 同步）；（G2）旧入口 grep：`welcome|mistake|growth|drill|followup|STAR|experiences|voice|OnboardingScreen|onboarding=true` 在上面两个目录内 0 命中；（G3）prototype import grep：`ui-design/src/(data|screen-resume-workshop)` 在两个目录内 0 命中；（H 隐私）matchSummary / originalBullet / suggestedBullet / structuredProfile / manual edit text 字段在 console / URL / pendingAction / localStorage / mock transport log / toast 内容中 0 命中；（I a11y）三屏 axe-core check PASS；（J）`method=fixture-backed-frontend` 显式标注，并附 backend real route preflight evidence（export PDF 仍按 plan 001 的 P0 501 stub） | `test/scenarios/e2e/p0-087-resume-detail-export-copy-consistency-and-parity/` |
