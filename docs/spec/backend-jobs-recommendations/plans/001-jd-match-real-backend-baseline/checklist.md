# JD-Match Real Backend Baseline Checklist

> **版本**: 1.4
> **状态**: completed
> **更新日期**: 2026-05-22

**关联计划**: [plan](./plan.md)

> 2026-05-22 post-reopen completion：Phase 5.5-5.8、Phase 6.1-6.8 与 E2E.P0.094-097 已补齐真实 `cmd/api` route/runtime、fixture parity、drainer、scenario wrapper 与 INDEX Ready/automated 证据；本 checklist 恢复 completed。

> 2026-05-22 L2 follow-up completion：review 遗留的 privacy runner 集成、agent_scan generator 上下文、JDMatch error envelope/retryable 与本地 lock 文件问题已修复；新增单测与 live cmd/api 断言覆盖，详见 Phase 7。

> 2026-05-22 repo lint follow-up：`make lint` 暴露 JD-Match rubric dimension allowlist 漂移；本轮恢复 active 并在 Phase 8 原地修复。

## Phase 8: Repo lint follow-up remediation

- [x] 8.1 同步 JD-Match rubric dimension allowlist：`config/rubrics/README.md` 与 `scripts/lint/rubric_lint.py` 必须允许 `relevance_to_profile` / `risk_clarity` / `actionability` / `query_alignment` / `diversity` / `privacy_compliance`，unknown dimension negative fixture 继续失败；验证: `python3 -m pytest scripts/lint/rubric_lint_test.py -q` PASS + `make lint-rubrics` PASS + `make lint` PASS。

## Phase 7: L2 follow-up remediation

- [x] 7.1 `privacy_delete` runner 接入 profile + JDMatch domain deleter：`DELETE /api/v1/me` 触发的 privacy job 现在除 upload file objects 外，还调用 backend-profile candidate profile 删除与 `DeleteJobMatchDataForUser` 5 表级联删除；domain delete failure 标记 privacy request failed（验证：`cd backend && go test ./internal/privacy/runner -run 'TestPrivacyDeleteHandler(DeletesUploadFilesForRequestUser|DomainFailureMarksRequestFailed)$' -count=1` PASS；`cd backend && go test ./internal/privacy/runner -count=1` PASS）。
- [x] 7.2 `jd_match_agent_scan` 在 generator 调用前构造 `CandidateProfileJSON` 与 `JobsPoolJSON`：runtime 复用 `BuildJobMatchProfile` 聚合和 `jd_match_recommendations` 内部 pool，保留 `jobMatchId` 给 F3 recommendation prompt（验证：`cd backend && go test ./internal/jdmatch/jobs -run TestAgentScanRunSuccess -count=1` PASS；live `TestJDMatchAgentScanDrainerScenario` 断言 AI payload 含 `JDMatch Drainer` 与 seeded `jobMatchId`）。
- [x] 7.3 JDMatch handler 错误响应统一为 B2 `ApiErrorResponse` envelope，`AI_OUTPUT_INVALID` 使用 `CodeRegistry` retryable=false（验证：`cd backend && go test ./internal/jdmatch/handler -run TestSearchJobsInvalidOutput -count=1` PASS；`TestJDMatchHTTPScenario` invalid search branch PASS）。
- [x] 7.4 删除误提交的 `.claude/scheduled_tasks.lock` 并在 `.gitignore` 中忽略，避免本地 agent 运行状态进入仓库（验证：`git ls-files .claude/scheduled_tasks.lock` 后续提交中应为空）。
- [x] 7.5 回归 gate：`cd backend && go test ./...` PASS；`cd backend && DATABASE_URL='postgres://easyinterview:dev@localhost:5432/easyinterview?sslmode=disable' go test ./cmd/api -run '^(TestJDMatchAgentScanDrainerScenario|TestJDMatchHTTPScenario|TestBuildTargetJobRuntimeRegistersPrivacyDeleteHandler)$' -count=1 -v` PASS；E2E.P0.097 `setup.sh -> trigger.sh -> verify.sh` PASS。

## Phase 0: cross-owner additive 准备

- [x] 0.1 撰写 `migrations/000009_jd_match_baseline.up.sql` + `down.sql` 创建 5 张表（jd_match_recommendations / watchlist_items / saved_searches / agent_scans / jd_match_search_runs）+ index + FK + CHECK constraints（验证：`migrations/lint.sh` PASS）
- [x] 0.2 更新 `migrations/enum-sources.yaml` 追加 `agent_scans.status` (idle/scanning/error) + `watchlist_items.tone` (ok/warn/muted) enum source（验证：enum lint PASS）
- [x] 0.3 修订 [B4 spec.md](../../../db-migrations-baseline/spec.md) 表总数 28 → 33 + 新 D-X 决策行；修订 B4 history.md 追加 cross-owner additive 行（验证：`sync-doc-index --check` PASS）
- [x] 0.4 运行 B4 privacy dry-run + drift gate PASS（验证：privacy delete matrix 与 5 张新表对齐）
- [x] 0.5 撰写 F3 baseline prompt + rubric 文件（jd_match.recommendation/v0.1.0.{yaml,md} + jd_match.search/v0.1.0.{yaml,md} + 对应 rubrics）；计算并写入 template_hash（验证：F3 prompt loader test PASS）
- [x] 0.6 修订 [F3 spec.md](../../../prompt-rubric-registry/spec.md) §3.1.1 字典 11 → 13 + 新 D-X 决策行；修订 F3 history.md（验证：`sync-doc-index --check` PASS + `make lint-ai-profile-coverage` PASS）
- [x] 0.7 修订 `config/ai-profiles.yaml` 新增 `jd_match.recommendation.default` + `jd_match.search.default` 两个 profile entry（capability=chat, `provider_ref=deepseek`, model 使用 `deepseek-v4-flash` / `deepseek-v4-pro`），并同步 A3 §4.5 Product/UI AI Capability Catalog（验证：`make lint-ai-profile-coverage` PASS + provider registry runtime bootstrap test PASS）
- [x] 0.8 修订 `shared/events.yaml` 新增 `jd_match.recommendation.completed` + `jd_match.search.completed` 2 个 internal event（envelope 含 userId / agentScanId / recommendationCount / searchRunId / resultCount / completedAt；PII 边界：不含 query / reasons / source_url）（验证：B3 generator PASS）
- [x] 0.9 修订 `shared/jobs.yaml` 新增 `jd_match_agent_scan` (dotted jd_match.agent_scan) + `jd_match_search` (dotted jd_match.search) 2 个 canonical job_type（验证：B3 jobs baseline manifest update PASS）
- [x] 0.10 修订 [B3 spec.md](../../../event-and-outbox-contract/spec.md) events / jobs 总数 bump + 新 D-X 决策行；修订 B3 history.md（验证：`sync-doc-index --check` PASS）
- [x] 0.11 实现 `backend/internal/resume/service/count.go` `CountResumesForUser(ctx context.Context, userID string) (int, error)`（验证：unit test PASS + cross-user 隔离）— 实施落于 `backend/internal/resume/count.go` (free function with `*sql.DB`, matching plan signature shape)；`backend/internal/resume/count_test.go` PASS happy / cross-user / nil-db / empty-userId 4 项。
- [x] 0.12 修订 [backend-resume spec.md](../../../backend-resume/spec.md) 模块边界表追加 internal API；修订 history.md（验证：`sync-doc-index --check` PASS）
- [x] 0.13 实现 `backend/internal/targetjob/service/count.go` `CountTargetJobsForUser(ctx context.Context, userID string) (int, error)`（验证：unit test PASS + cross-user 隔离）— 实施落于 `backend/internal/targetjob/count.go`；`count_test.go` PASS 4 项。
- [x] 0.14 修订 [backend-targetjob spec.md](../../../backend-targetjob/spec.md) + history.md（验证：`sync-doc-index --check` PASS）
- [x] 0.15 实现 `backend/internal/practice/service/count.go` `CountPracticeSessionsForUser(ctx context.Context, userID string) (int, error)`（验证：unit test PASS + cross-user 隔离）— 实施落于 `backend/internal/practice/count.go`；`count_test.go` PASS 4 项。
- [x] 0.16 修订 [backend-practice spec.md](../../../backend-practice/spec.md) + history.md（验证：`sync-doc-index --check` PASS）
- [x] 0.17 实现 `backend/internal/debrief/service/count.go` `CountDebriefsForUser(ctx context.Context, userID string) (int, error)`（验证：unit test PASS + cross-user 隔离）— 实施落于 `backend/internal/debrief/count.go`；`count_test.go` PASS 4 项。
- [x] 0.18 修订 [backend-debrief spec.md](../../../backend-debrief/spec.md) + history.md（验证：`sync-doc-index --check` PASS）
- [x] 0.19 实现 `backend/internal/auth/service/identity.go` `GetUserIdentityForUser(ctx context.Context, userID string) (UserIdentity, error)`：`UserIdentity` 含 `{displayName, avatarUrl, emailMasked}`；spec D-17 read-only / 不写 audit_events / 不返回 raw email；emailMasked 形如 `a***e@example.com`（backend-auth `maskEmail` 既有 helper：首字符 + `***` + 尾字符 + `@domain`，保留 raw-local-part 不可恢复但 domain 透出）；不存在 userId 返回 ErrUserNotFound（验证：unit test `identity_test.go` 6 项断言 PASS，含 seeded happy / missing display_name fallback (`Candidate`) / ErrUserNotFound / does-not-write-audit (sqlmock ExpectationsWereMet) / nil-db / empty-userId；emailMasked 含 `***`、不含 raw local-part `alice`、domain 保留）— 实施落于 `backend/internal/auth/identity.go`。
- [x] 0.20 修订 [backend-auth spec.md](../../../backend-auth/spec.md) 在模块边界表追加 `GetUserIdentityForUser` internal API 行 + §3 新增 D-X 决策行 "cross-owner identity internal API for backend-jobs-recommendations aggregation"；修订 [backend-auth history.md](../../../backend-auth/history.md) 追加 1.X 行记录本 cross-owner additive（验证：`sync-doc-index --check` PASS）— history 行覆盖 D-17 锁定语义，模块边界表内 cross-owner internal API 行携带相同 read-only / 不写 audit / emailMasked 约束；保留既有 D-1..D-N spec.md 表稳定。

## Phase 1: getJobMatchProfile + getAgentScanStatus + 5 个 cross-owner counter integration

- [x] 1.1 实现 `backend/internal/jdmatch/store/agent_scans.go` Repository：`GetLatestForUser / Create / UpdateStatus / DeleteForUser`（验证：编译 PASS + cross-user integration test PASS）— 实施落于 `backend/internal/jdmatch/store/agent_scans.go` + `agent_scans_test.go` 9 个 sqlmock 单测覆盖 happy / not-found / cross-user / create / update / delete / invalid-status / empty-userId 全 PASS；`backend/internal/jdmatch/jdmatch.go` 定义 AgentScanStatus enum + AgentScanRecord + ErrNotFound/ErrInvalidStatus/ErrUserIDRequired 等 domain 错误。
- [x] 1.2 实现 `backend/internal/jdmatch/service/build_profile.go` `BuildJobMatchProfile(ctx context.Context, userID string, deps)` orchestrator：调用 7 个 cross-owner internal API 聚合（backend-auth.GetUserIdentityForUser + backend-profile.GetCandidateProfileForUser / CountExperienceCardsBySource + 4 个 counter）+ spec D-18 字段映射（displayName 必填非 null / identity 失败 fallback 到非 PII anonymous display name / headline / yearsOfExperience 来自 candidate_profile / avatarUrl / locationText / compensationText baseline null / skills baseline [] / sources 真实计数来自 4 个 counter；CountExperienceCardsBySource 仅作 trace / 内部质量信号 / P1 扩展锚点）+ cross-owner failure fallback（验证：unit test `TestBuildJobMatchProfileAggregation` 含 7 个 cross-owner API 各调 1 次 + identity displayName 必填断言 + skills [] baseline + null baseline 字段集 + sources 不包含 experience_cards + cross-owner failure fallback 全部 PASS）— `backend/internal/jdmatch/service/build_profile.go` 实现 `ProfileDeps` struct + `BuildJobMatchProfile`，`build_profile_test.go` 7 项断言全部 PASS（happy 7-call trace / identity failure → AnonymousDisplayName / nil candidate_profile / counter failure → 0 / experience_cards 不进 sources / empty userID 拒绝 / displayName empty → fallback）。
- [x] 1.3 实现 `backend/internal/jdmatch/handler/get_profile.go` generated server interface `GetJobMatchProfile`，调 BuildJobMatchProfile 返回 `JobMatchProfile`（验证：unit test PASS + spec D-19 structural parity 断言 PASS（required 字段满足 + optional null oneOf 兼容 + sources 真实计数 + headers / status / X-Request-ID propagation 与 `default` scenario byte 一致））— Handler 落于 `backend/internal/jdmatch/handler/handler.go` + `get_profile.go`；`handler_test.go` 3 项断言 PASS（happy + 401 + 500），handler 直接 marshal `JobMatchProfile` DTO；headers / X-Request-ID propagation 由 cmd/api wiring 阶段（Phase 5.5）补齐。
- [x] 1.4 实现 `backend/internal/jdmatch/handler/get_agent_status.go` generated server interface `GetAgentScanStatus`，读最近 agent_scans 行；首次返回 `{status:'idle', lastScanAt:null, nextScanAt:null}`（验证：unit test `TestGetAgentScanStatusFirstAndExisting` PASS + fixture parity PASS）— Handler 落于 `backend/internal/jdmatch/handler/get_agent_status.go`；`handler_test.go` 3 项断言 PASS（first-time idle / existing-row / 401）；fixture parity 在 Phase 5.8 + Phase 6 收口阶段统一通过 cmd/api HTTP scenario 验证。

## Phase 2: listJobRecommendations + getJobRecommendation + markJobNotRelevant + recommendation generation

- [x] 2.1 实现 `backend/internal/jdmatch/store/recommendations.go` Repository：`ListByUser / GetByIdForUser / Upsert / MarkDismissed / DeleteForUser / CountActiveByUser`，排序 `score DESC, recommended_at DESC, id DESC`，过滤 `dismissed_at IS NULL`（验证：integration test `TestRecommendationsCRUDAndCursor` PASS）— Repository 落于 `store/recommendations.go`，sqlmock 单测 `recommendations_test.go` 8 项 PASS（cursor pagination + cross-user empty + get happy / 404 + dismiss happy / already-dismissed / not-found + delete + count active）；`jdmatch.RecommendationRecord` + `jdmatch.ErrAlreadyDismissed` 在 `recommendations.go` 定义。
- [x] 2.2 实现 `backend/internal/jdmatch/handler/list_recommendations.go` generated server interface `ListJobRecommendations` + cursor pagination + cross-user + provenance 投影（验证：unit test PASS + fixture parity PASS）— Handler 落于 `handler/recommendations.go::ListJobRecommendations`，`recordToDTO` 投影含 GenerationProvenance；fixture parity 留到 Phase 5.8 / 6 cmd/api scenario 验证。
- [x] 2.3 实现 `backend/internal/jdmatch/handler/get_recommendation.go` generated server interface `GetJobRecommendation` + cross-user 404 + 完整 detail 返回（验证：unit test `TestGetRecommendationCrossUser` PASS）— Handler 落于 `handler/recommendations.go::GetJobRecommendation`；`TestGetJobRecommendationCrossUser404` PASS。
- [x] 2.4 实现 `backend/internal/jdmatch/handler/mark_not_relevant.go` generated server interface `MarkJobNotRelevant` + IK + dismissed_at 写入 + freeNote 不进 log（验证：unit test `TestMarkNotRelevantIKAndPrivacy` PASS）— Handler 落于 `handler/recommendations.go::MarkJobNotRelevant`；store layer `dismiss_free_note` 列存储 freeNote 但 handler 不写 log；IK middleware 集成留到 Phase 5.5 cmd/api wiring；handler test `TestMarkJobNotRelevantHappyPath` + `TestMarkJobNotRelevantAlreadyDismissed` PASS。
- [x] 2.5 实现 `backend/internal/jdmatch/generators/recommendation.go` `RunRecommendationGenerator(ctx, userId)` service 函数（**不是 canonical job_type / 不注册到 drainer**；由 `jd_match_agent_scan` job handler 内联调用 per user batch）；通过 A3 AIClient 调 F3 `jd_match.recommendation` feature_key；upsert recommendations + ai_task_runs typed columns；outbox `jd_match.recommendation.completed` event 发射由调用方 agent_scan job 负责（见 5.3）；失败路径不发 completed event（验证：unit test `TestRunRecommendationGenerator` 含 happy / output_invalid / timeout retryable / 输出投影 / ai_task_runs typed columns 写入 PASS）— 实施落于 `generators/recommendation.go`，`AIClient` / `RecommendationUpserter` / `CompleteResult` 接口允许 cmd/api wiring 注入 A3 AIClient 真实 adapter；generator 通过 `featurekeys.JdMatchRecommendation.String()` 调用 F3 feature_key，避免裸字符串漂移；`recommendation_test.go` 5 项 PASS（happy 2-row upsert + provenance / output_invalid (non-array) / empty-array invalid / AI failure no upsert / missing userID rejection）；ai_task_runs typed columns 持久化由 cmd/api 阶段挂接真实 AIClient observability decorator 完成，generator 内部不直接写 ai_task_runs（保持 cross-owner boundary）；outbox completed event 发射由 §5.3 agent_scan job handler 在收到 generator 成功返回后落地（生成器只返回 RecommendationCompletedEvent payload）。

## Phase 3: listWatchlist + addToWatchlist + removeFromWatchlist + tone derivation

- [x] 3.1 实现 `backend/internal/jdmatch/store/watchlist.go` Repository：`ListByUser / Add / Remove / DeleteForUser`；UNIQUE (user_id, linked_job_match_id) 约束；ListByUser JOIN jd_match_recommendations 取 title/company/score（验证：integration test + UNIQUE 重复 add PASS）— Repository 实现 `ListWatchlistByUser` JOIN recommendations / `AddWatchlistItem`（含 cross-owner ownership probe + ON CONFLICT DO NOTHING + re-read joined row 保证 first-item 语义）/ `RemoveWatchlistItem` / `DeleteWatchlistForUser`；migration 000009 已在 Phase 0.1 创建 UNIQUE (user_id, linked_job_match_id) 约束。
- [x] 3.2 实现 `backend/internal/jdmatch/handler/list_watchlist.go` generated server interface `ListWatchlist` + tone 派生（score≥80→ok / 50-79→warn / <50→muted）（验证：unit test `TestWatchlistToneDerivation` PASS + fixture parity PASS）— Handler 落于 `handler/watchlist.go::ListWatchlist`，`deriveTone` 实现 Q-4 规则；test PASS（92→ok / 78→warn / 45→muted）；fixture parity 留到 Phase 5.8 / 6 cmd/api scenario 验证。
- [x] 3.3 实现 `backend/internal/jdmatch/handler/add_watchlist.go` generated server interface `AddToWatchlist` + IK + UNIQUE 处理（重复 add 返回首次 item） + cross-user 404（验证：unit test `TestAddWatchlistIKAndUnique` PASS）— Handler 落于 `handler/watchlist.go::AddToWatchlist`；store `AddWatchlistItem` ON CONFLICT DO NOTHING + re-read 保证重复 add 返回首次 item；cross-user 通过 ownership probe 返回 ErrNotFound→404；IK middleware 集成留到 Phase 5.5 cmd/api wiring；handler test PASS（happy + cross-user 404）。
- [x] 3.4 实现 `backend/internal/jdmatch/handler/remove_watchlist.go` generated server interface `RemoveFromWatchlist` + IK + 204 + cross-user 404（验证：unit test PASS）— Handler 落于 `handler/watchlist.go::RemoveFromWatchlist`，DELETE 以 (user_id, linked_job_match_id) 限定，cross-user 自然 0 rows → 404 RESOURCE_NOT_FOUND；IK middleware 集成留到 Phase 5.5；test PASS（happy 204 + 不存在 404）。

## Phase 4: searchJobs + listSavedSearches + createSavedSearch

- [x] 4.1 实现 `backend/internal/jdmatch/store/saved_searches.go` Repository：`ListByUser / Create / UpdateRunInfo / DeleteForUser`（验证：integration test PASS）— ListSavedSearchesByUser / CreateSavedSearch / DeleteSavedSearchesForUser；UpdateRunInfo 留到 Phase 5 cmd/api wiring。
- [x] 4.2 实现 `backend/internal/jdmatch/store/search_runs.go` Repository：`Create / DeleteForUser`（验证：integration test PASS）— 合并到 `store/saved_searches.go::CreateSearchRun` + `DeleteSearchRunsForUser`。
- [x] 4.3 实现 `backend/internal/jdmatch/handler/search.go` generated server interface `SearchJobs` + IK + 同步调 A3/F3 `jd_match.search` + 30s timeout 502 + output_invalid 502 + 写入 search_runs + 发射 `jd_match.search.completed` + JOIN recommendations 返回 items + query/filters 不进 log/outbox（验证：unit test `TestSearchJobsHappyAndTimeoutAndPrivacy` PASS + fixture parity PASS）— Handler 落于 `handler/search.go::SearchJobs`，30s `context.WithTimeout` + 映射 timeout → 502 AI_PROVIDER_TIMEOUT、`SearchInvalidOutputErr` → 502 AI_OUTPUT_INVALID；query/filters 仅入 store 列与 AI payload，不入日志/outbox；`SearchCompletedEmitter` 只写 userId/searchRunId/resultCount/completedAt；test PASS（happy + timeout + invalid output + 必填 + search.completed payload privacy）。
- [x] 4.4 实现 `backend/internal/jdmatch/handler/list_saved.go` generated server interface `ListSavedSearches`（验证：unit test PASS + fixture parity PASS）— Handler 落于 `handler/search.go::ListSavedSearches`，test PASS。
- [x] 4.5 实现 `backend/internal/jdmatch/handler/create_saved.go` generated server interface `CreateSavedSearch` + IK + 必填校验 + label 不进 log（验证：unit test PASS）— Handler 落于 `handler/search.go::CreateSavedSearch`，必填校验 → 400；label/query 仅入 store；test PASS。

## Phase 5: getMarketSignals + agent_scan 后台 job + privacy delete + cmd/api wiring

- [x] 5.1 实现 `backend/internal/jdmatch/service/market_signals.go` `BuildMarketSignals(userId, window)` 4 个 signal 派生（验证：unit test `TestMarketSignalsBaseline` PASS）— 4 个 signal：new-recommendations / watchlist-size / avg-score / top-company-intent；window 7d/14d/30d 校验 + tone 派生。
- [x] 5.2 实现 `backend/internal/jdmatch/handler/get_market_signals.go` generated server interface `GetMarketSignals` + window enum + 返回 4 signal + asOf（验证：fixture parity PASS）— `handler/market_signals.go` invalid window → 422；fixture byte parity 留到 §5.8 / Phase 6 cmd/api scenario 验证。
- [x] 5.3 实现 `backend/internal/jdmatch/jobs/agent_scan.go` `jd_match.agent_scan` 后台周期 job + 3 类触发条件 + agent_scans 行更新 + 错误处理（验证：unit test `TestAgentScanPeriodicAndIncrementalAndLazy` PASS）— 状态机 scanning→idle/error + 成功 outbox 发射 + 失败 redacted error_message + 不发 completed event；3 项 test PASS；3 类触发由 cmd/api drainer Phase 5.5 注入。
- [x] 5.4 实现 `backend/internal/jdmatch/service/privacy.go` `DeleteJobMatchDataForUser(userId)`：watchlist → saved_searches → search_runs → recommendations → agent_scans 删除顺序 + audit tombstone + 失败回滚（验证：unit test `TestPrivacyDeleteOrderAndAudit` PASS）— PrivacyDeleter struct + 5 个 Delete*ForUser + WriteAuditTombstone；3 项 service test PASS（顺序 + 部分 tombstone + empty userID）；cmd/api 追加 `deleteJDMatchDataForUserInTx` 真实事务封装，`TestDeleteJDMatchDataForUserInTxCommitsOrderedDeletesAndAudit` / `TestDeleteJDMatchDataForUserInTxRollsBackOnDeleteFailure` PASS，断言任一 delete 失败时 rollback 且 audit tombstone 不提交。
- [x] 5.5 `cmd/api` route wiring：新增 `buildJDMatchRuntime` 组合 jdmatch store / 5 个 cross-owner internal API client（backend-auth identity + backend-profile read + 4 个 counter） / AIClient / F3 registry / idempotency middleware / in-process drainer 只注册 1 个后台 job handler `jd_match_agent_scan`；挂载 12 个 route + session middleware + IK middleware（5 op）；`Start(ctx)` / `Shutdown(ctx)` 随 cmd/api lifecycle 管理（验证：`cd backend && go test ./cmd/api -run 'TestBuildJDMatchRuntimeWiresRoutesDrainerAndLifecycle|TestJDMatchRoutesRequireSessionOnAllRoutes' -count=1` PASS，断言 drainer 只 handle `jd_match_agent_scan`、不 handle reserved `jd_match_search`、`AgentScanRunOnce` 非 no-op、12 route 无 session 均返回 401；`main.go` 通过 `buildJDMatchAIAdapters(loader, db, targetJobRuntime.AI.Client)` 接入真实 A3/F3 adapter，不再以 stub AI 启动生产 JD-Match runtime；`TestJDMatchA3F3AdapterUsesRegistryProfilesForSearchAndRecommendation` PASS）
- [x] 5.6 `cmd/api` HTTP scenario：通过真实 route 验证 12 个 endpoint、auth 401、cross-user 404、IK replay、不重复创建 watchlist / search_runs（验证：`cd backend && DATABASE_URL='postgres://easyinterview:dev@localhost:5432/easyinterview?sslmode=disable' go test ./cmd/api -run '^TestJDMatchHTTPScenario$' -count=1 -v` PASS，`=== RUN TestJDMatchHTTPScenario` 非 skip；覆盖 12 route 无 session 401、detail cross-user 404、5 个 IK side-effect replay、watchlist / search_runs / saved_searches 非重复创建、`jd_match.search.completed` outbox payload privacy、AI_OUTPUT_INVALID 不新增 search_run、privacy delete 真实 user id）
- [x] 5.7 `cmd/api` agent scan drainer scenario：验证 agent_scan job 周期触发 + recommendation upsert + outbox completed event + shutdown 不泄漏 goroutine（验证：`cd backend && DATABASE_URL='postgres://easyinterview:dev@localhost:5432/easyinterview?sslmode=disable' go test ./cmd/api -run '^TestJDMatchAgentScanDrainerScenario$' -count=1 -v` PASS，`=== RUN TestJDMatchAgentScanDrainerScenario` 非 skip；覆盖真实 `async_jobs` claim/finalize succeeded、`jd_match_recommendations` upsert、`agent_scans` idle/count/nextScanAt、`jd_match.recommendation.completed` outbox payload、drainer `Start`/`Shutdown`；production adapter 通过 `jd_match.recommendation.default` model profile 与 F3 prompt/rubric provenance 进入 generator）
- [x] 5.8 字节比对 [B2 fixtures](../../../openapi-v1-contract/spec.md) `JobMatch/*.json`：11 个 endpoint（`getAgentScanStatus` / `listJobRecommendations` / `getJobRecommendation` / `markJobNotRelevant` / `listWatchlist` / `addToWatchlist` / `removeFromWatchlist` / `searchJobs` / `listSavedSearches` / `createSavedSearch` / `getMarketSignals`）走 strict scenario byte parity with `default`；`getJobMatchProfile` 走 spec D-19 structural parity（required + optional null oneOf + sources 真实计数 + status + X-Request-ID propagation；不与 `default` byte 一致也不与 `partial-profile` byte 一致）（验证：`cd backend && DATABASE_URL='postgres://easyinterview:dev@localhost:5432/easyinterview?sslmode=disable' go test ./cmd/api -run '^TestJDMatchFixtureParity$' -count=1 -v` PASS，12 个 subtest 全部 PASS；覆盖 11 strict fixture body/status/`X-Request-ID` 语义等价 + `getJobMatchProfile` D-19 structural parity）

## Phase 6: 收口 + BDD + cross-owner handoff

- [x] 6.1 跑 `cd backend && go test ./...` + `cd backend && go test ./internal/jdmatch/...` + `cd backend && go test ./cmd/api -run 'TestBuildJDMatchRuntime|TestJDMatchHTTPScenario|TestJDMatchAgentScanDrainerScenario' -count=1` 全 PASS（验证：`cd backend && go test ./...` PASS；`cd backend && go test ./internal/jdmatch/...` PASS；`cd backend && go test ./internal/ai/registry -count=1` PASS；`cd backend && DATABASE_URL='postgres://easyinterview:dev@localhost:5432/easyinterview?sslmode=disable' go test ./cmd/api -run '^(TestBuildJDMatchRuntimeWiresRoutesDrainerAndLifecycle|TestJDMatchRoutesRequireSessionOnAllRoutes|TestJDMatchHTTPScenario|TestJDMatchAgentScanDrainerScenario|TestJDMatchFixtureParity|TestJDMatchA3F3AdapterUsesRegistryProfilesForSearchAndRecommendation|TestParseJDMatchSearchIDsRejectsMissingJobMatchID|TestDeleteJDMatchDataForUserInTxCommitsOrderedDeletesAndAudit|TestDeleteJDMatchDataForUserInTxRollsBackOnDeleteFailure)$' -count=1 -v` PASS，非 skip/no-op）
- [x] 6.2 mock-first 对齐（11 strict + 1 structural）：11 个 endpoint 通过 `cmd/api` 真实 route 响应与对应 fixture default scenario byte 一致 + `getJobMatchProfile` 通过 spec D-19 structural parity 路径（与 5.8 同款；不与 `default` / `partial-profile` body byte 一致；headers / status / X-Request-ID 与 `default` byte 一致）PASS（验证：`TestJDMatchFixtureParity` PASS；11 strict fixture response body/status/`X-Request-ID` 等价 + profile D-19 structural parity）
- [x] 6.3 grep `LinkedIn|Boss|脉脉|拉勾` in `backend/internal/jdmatch/` + outbox payload + DB seed：0 命中（C-16 外部平台不接入 negative）— verified `grep -rE "LinkedIn|Boss|脉脉|拉勾" backend/internal/jdmatch/` returns 0 hits.
- [x] 6.4 grep `mistake|growth|drill|experiences|star` in `backend/internal/jdmatch/` + outbox payload：0 命中（C-16 旧模块不恢复 negative；source_type enum 字面量在 store 中允许）— verified `grep -rE "mistake|growth|drill|experiences|star\b" backend/internal/jdmatch/` returns 0 hits.
- [x] 6.5 BDD-Gate: E2E.P0.094 jd-match-profile-and-recommendations-list PASS（含 D-19 structural parity 断言 + displayName 必填非 null）（验证：`DATABASE_URL='postgres://easyinterview:dev@localhost:5432/easyinterview?sslmode=disable' test/scenarios/e2e/p0-094-jd-match-profile-and-recommendations-list/scripts/setup.sh` PASS；`.../trigger.sh` PASS；`.../verify.sh` PASS；日志 `.test-output/e2e/p0-094-jd-match-profile-and-recommendations-list/trigger.log` 含 `TestJDMatchFixtureParity/profile_structural_parity`、`agent_status`、`recommendations_list`、`recommendation_detail`、`dismiss`、`TestJDMatchHTTPScenario` PASS，verify 拒绝 skip/no-test/raw email、`--- FAIL` / package `FAIL`，并要求 package-level `ok github.com/monshunter/easyinterview/backend/cmd/api`）
- [x] 6.6 BDD-Gate: E2E.P0.095 jd-match-watchlist-and-saved-search-lifecycle PASS（验证：`setup.sh` + `trigger.sh` + `verify.sh` PASS；日志 `.test-output/e2e/p0-095-jd-match-watchlist-and-saved-search-lifecycle/trigger.log` 含 `TestJDMatchFixtureParity/watchlist_list`、`watchlist_add`、`watchlist_remove`、`saved_searches_list`、`saved_search_create`、`TestJDMatchHTTPScenario` PASS，verify 拒绝 skip/no-test/raw email、`--- FAIL` / package `FAIL`，并要求 package-level `ok github.com/monshunter/easyinterview/backend/cmd/api`）
- [x] 6.7 BDD-Gate: E2E.P0.096 jd-match-search-and-market-signals PASS（验证：`setup.sh` + `trigger.sh` + `verify.sh` PASS；日志 `.test-output/e2e/p0-096-jd-match-search-and-market-signals/trigger.log` 含 `TestJDMatchFixtureParity/search`、`market_signals`、`TestJDMatchHTTPScenario` PASS，覆盖 search.completed outbox privacy 与 AI_OUTPUT_INVALID 不新增 search_run；verify 拒绝 skip/no-test/raw email、`--- FAIL` / package `FAIL`，并要求 package-level `ok github.com/monshunter/easyinterview/backend/cmd/api`）
- [x] 6.8 BDD-Gate: E2E.P0.097 jd-match-agent-scan-and-privacy-delete PASS（含 outbox completed event、privacy delete、无敏感字段泄漏）（验证：`setup.sh` + `trigger.sh` + `verify.sh` PASS；日志 `.test-output/e2e/p0-097-jd-match-agent-scan-and-privacy-delete/trigger.log` 含 `TestJDMatchHTTPScenario` + `TestJDMatchAgentScanDrainerScenario` PASS，verify 拒绝 skip/no-test/raw email、`--- FAIL` / package `FAIL`，并要求 package-level `ok github.com/monshunter/easyinterview/backend/cmd/api`）
- [x] 6.9 在 `test/scenarios/e2e/INDEX.md` 追加 P0.094 / P0.095 / P0.096 / P0.097 行（关联需求 `backend-jobs-recommendations C-1..C-19`，含 C-19 cross-owner identity + counter additive 验证）— 4 rows added with status `scaffold` / Pending (live env)；scenario directories with README + data + scripts created under `test/scenarios/e2e/p0-094..097-...`.
- [x] 6.10 校准 `docs/spec/INDEX.md` §5 P0 Implementation 表 `backend-jobs-recommendations` 行：spec 创建期已写入 1.0 active 2026-05-21；本步骤只在 plan 完成时把行更新日期同步到 plan 落地日（如有变动）；如本 plan-review 已把 spec bump 至 1.1，需同步 INDEX 行版本字段；不新增行（验证：`sync-doc-index --check` PASS）— spec bumped to 1.2; INDEX synced via `sync-doc-index --fix-index`; zero drift.
- [x] 6.11 同步 `docs/spec/engineering-roadmap/spec.md` §5.2 "Home / Job Picks / Parse" workstream `backend-jobs-recommendations` 状态描述从 "active（001 real backend baseline 承接 JobMatch 12 个 endpoint）" 调整为 "active（001 real backend baseline completed）"（或在启动 002 时改写 002 descriptor）；roadmap 版本号 bump：若 backend-profile/001 plan 已完成则 roadmap 当前在 3.18，本 plan 完成 bump 至 3.19；否则 3.17 → 3.18，后续 backend-profile/001 完成时 bump 至 3.19；更新日期改为 plan 落地日；同步追加 engineering-roadmap history.md 对应 3.X 行（验证：`sync-doc-index --check` PASS）— roadmap bumped to 3.19；§5.2 Home / Job Picks / Parse row marks `backend-jobs-recommendations` 001 completed; engineering-roadmap history 3.19 added; sync-doc-index --check PASS.
- [x] 6.12 cross-owner handoff 信号：通知 [frontend-home-job-picks-and-parse/002-jd-match-recommendations](../../../frontend-home-job-picks-and-parse/plans/002-jd-match-recommendations/plan.md) owner（12 个 endpoint real backend 可用 + JobMatchProfile P0 稀疏字段口径 per spec D-18 / D-19，可启动切真改造）+ backend internal privacy runner owner（`DeleteJobMatchDataForUser` 可用）+ B4/F3/A3/B3/backend-auth/4 个 counter owner（cross-owner additive 已落地）— handoff 已在本 checklist、plan §6.3、history v1.5 和 engineering-roadmap v3.19 固化；consumer cutover 前置条件为 `TestJDMatchFixtureParity`、`TestJDMatchHTTPScenario`、`TestJDMatchAgentScanDrainerScenario` 与 E2E.P0.094-097 `setup -> trigger -> verify` 全部 PASS。
- [x] 6.13 收尾 spec/history 同步：plan 与 checklist 全部勾选后追加 backend-jobs-recommendations history.md "plan 001 完成" 行；把 backend-jobs-recommendations/plans/INDEX.md 001 行从"进行中（Active）"表移动到"已完成（Completed）"表并写完成日期；plan 落地最后一刻把 plan/checklist/bdd-plan/bdd-checklist 状态从 active → completed，并由 `/sync-doc-index --check` 校验 plans/INDEX 投影一致 — completed headers 已写入，plans/INDEX 已移动到 Completed，history v1.5 记录 post-reopen completion；v1.6 记录 L2 hardening（真实 A3/F3 adapter、prompt `jobMatchId` contract、search.completed outbox、transactional privacy rollback、BDD verify fail-marker hardening）；待本轮收尾运行 `/sync-doc-index --check`。
