# 001 BDD Plan

> **版本**: 1.2
> **状态**: completed
> **更新日期**: 2026-05-22

**关联 Plan**: [plan](./plan.md)

> 2026-05-22 post-reopen completion：E2E.P0.094-097 已补齐 scenario assets、wrapper 断言、trigger logs 与 `test/scenarios/e2e/INDEX.md` Ready/automated 状态；当前验证覆盖 12-route、IK replay、fixture parity、search persistence、agent_scan drainer、outbox 与 privacy delete live path。

## 1 场景矩阵

| 场景 ID | 类别 | 关联 Phase | 关联 Spec C-* | 关联 BDD-Gate（主 checklist） |
|---------|------|-----------|--------------|----------------------------|
| E2E.P0.094 | primary · profile aggregation (7 cross-owner API + D-18 字段映射 + D-19 structural parity) + recommendation list + detail + dismiss + agent status | Phase 1 + 2 + 5 | C-1, C-2, C-3, C-4, C-5, C-13, C-14, C-17, C-19 | Phase 6.5 |
| E2E.P0.095 | primary + alternate · watchlist add/list/remove + UNIQUE + tone + saved searches | Phase 3 + 4 + 5 | C-6, C-7, C-8, C-10, C-13, C-14, C-17 | Phase 6.6 |
| E2E.P0.096 | primary + failure · search sync + market signals + timeout 502 + IK + privacy | Phase 4 + 5 | C-9, C-11, C-13, C-14, C-17 | Phase 6.7 |
| E2E.P0.097 | primary + failure / recovery · agent scan job + recommendation generation + outbox event + privacy delete cascade + 失败回滚 | Phase 2 + 5 | C-12, C-15, C-16, C-18 | Phase 6.8 |

---

## Phase 1 + 2 + 5: profile + recommendations + dismiss + agent status

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.094 | JD-Match 主路径：profile 聚合 (7 cross-owner API + D-18 / D-19) + recommendations 列表 / 详情 / dismiss + agent status | A2 dev stack 拉起；`cmd/api` 使用真实 session middleware、IK middleware（5 op）、12 个 JobMatch route；用户 A 已登录（有效 session cookie；users 表已有 user_A 行含 displayName / email）；用户 A 的 candidate_profile 已 seed + 含 headline / yearsOfExperience；用户 A 已写入 3 resumes（通过 backend-resume store）+ 5 target_jobs（通过 backend-targetjob store）+ 8 practice_sessions（通过 backend-practice store）+ 2 debriefs（通过 backend-debrief store）+ 25 个 active jd_match_recommendation 行（已通过 store 直接写入，模拟 agent_scan 完成）；agent_scans 表已有最近 idle 行；用户 B 已登录但无 jd_match 数据；缺少 live env、integration-tag 测试 skip 或 focused gate no-op 时本场景必须 fail | 用户 A 通过 `cmd/api` route 分别调：（A1）`GET /api/v1/jd-match/profile`；（A2）`GET /api/v1/jd-match/agent-status`；（A3）`GET /api/v1/jd-match/recommendations?pageSize=20`；（A4）`GET /api/v1/jd-match/recommendations?pageSize=20&cursor={A3.nextCursor}`；（A5）`GET /api/v1/jd-match/recommendations/{A3.items[0].id}`；（A6）`POST /api/v1/jd-match/recommendations/{A3.items[1].id}/dismiss` body `{reason:'wrong_level', freeNote:'too senior for now'}` + IK；（A7）`GET /api/v1/jd-match/recommendations?pageSize=20` (重新读列表)；（A8）内部调 `backend-auth.GetUserIdentityForUser(ctx,userA)` + `backend-profile.GetCandidateProfileForUser(ctx,userA)` + `backend-profile.CountExperienceCardsBySource(ctx,userA)` + 4 个 `Count*ForUser(ctx,userA)` counter API 各 1 次（验证 7 个 cross-owner API trace 完整且按 BuildJobMatchProfile orchestrator 的调用计数）；（B1）用户 B 调 `GET /api/v1/jd-match/recommendations`；（B2）用户 B 调 `GET /api/v1/jd-match/recommendations/{A3.items[0].id}`；（B3）用户 B 调 `POST /api/v1/jd-match/recommendations/{A3.items[2].id}/dismiss` body `{reason:'other'}` + IK | （A1）返回 200 + `JobMatchProfile`（spec D-18 字段来源）：`displayName` 非 null（来自 backend-auth.GetUserIdentityForUser，identity 失败时 fallback 到非 PII anonymous display name）；`avatarUrl=null`；`headline` 非 null（来自 backend-profile.GetCandidateProfileForUser）；`yearsOfExperience` 非 null；`locationText=null`；`compensationText=null`；`skills=[]`；`sources={resumes:3, jds:5, mocks:8, debriefs:2}` 真实计数；**走 spec D-19 structural parity**：响应满足 [B2 `JobMatchProfile` schema](../../../openapi-v1-contract/spec.md) required（displayName / skills / sources）+ optional null oneOf 兼容（avatarUrl / headline / yearsOfExperience / locationText / compensationText）+ headers `X-Request-ID` 与 `default` scenario byte 一致 + status 200；不要求与 `default` scenario body byte 一致（spec D-19 例外）；不要求与 `partial-profile` scenario byte 一致（sources 真实计数不为 0）；（A2）返回 200 + `AgentScanStatus{ status:'idle', lastScanAt, nextScanAt, message:null }`；fixture parity；（A3）返回 20 行 + `pageInfo.nextCursor` 非空 + 按 `score DESC, recommended_at DESC, id DESC` 排序；每行携带完整 `provenance` (promptVersion/rubricVersion/modelId/language/featureFlag/dataSourceVersion)；fixture parity `listJobRecommendations.json` `default`；（A4）返回 5 行 + `hasMore=false`；（A5）返回 200 + 完整 `JobMatchRecommendation` 含 networkNote / interviewHypotheses / similarInterviewers 详细字段；fixture parity `getJobRecommendation.json` `default`；（A6）返回 200 + `MarkNotRelevantResult{jobMatchId, dismissedAt}`；DB recommendation 行 `dismissed_at` 写入；fixture parity `markJobNotRelevant.json` `default`；（A7）返回 19 行 + 上一行 dismissed 不在列表；（A8）7 cross-owner API trace 完整；identity API 返回 `emailMasked` 含 `***` / 不含 raw local-part；调用不写 audit_events / 不 bump user 字段；backend-profile `GetCandidateProfileForUser` 不触发 seed 副作用；4 counter 返回 ≥0 整数；（B1）返回 200 + items=[]（empty）+ pageInfo；（B2）返回 404 + `RESOURCE_NOT_FOUND`；（B3）返回 404 + `RESOURCE_NOT_FOUND`；DB 用户 A 的 recommendation 不被 dismissed；（C 字节比对）4 个 endpoint default scenario 字节比对 PASS；getJobMatchProfile 走 structural parity 而非 byte parity；（F 隐私）freeNote / reasons / source_url / interview_hypotheses / raw email 不出现在 console / URL / localStorage / log；（G 旧口径）grep `LinkedIn|Boss|脉脉|拉勾` in `backend/internal/jdmatch/` + outbox payload + DB seed：0 命中；grep `mistake|growth|drill|experiences|star` in code：0 命中（不影响 source_type='practice_report' enum） | `test/scenarios/e2e/p0-094-jd-match-profile-and-recommendations-list/`；trigger 运行 `cd backend && DATABASE_URL=... go test ./cmd/api -run '^(TestJDMatchFixtureParity|TestJDMatchHTTPScenario)$' -count=1 -v`；verify 必须断言 profile structural parity、agent status、recommendations list/detail、dismiss、HTTP scenario PASS，并拒绝 skip / no-test / raw email |

## Phase 3 + 4 + 5: watchlist + saved searches

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.095 | watchlist add / list / remove + UNIQUE + tone derivation + saved searches CRUD | A2 dev stack 拉起；用户 A 已登录；用户 A 已有 3 个 jd_match_recommendation（score: 92 / 78 / 45）；用户 B 已登录 | 用户 A 通过 `cmd/api` route 分别调：（A1）`POST /api/v1/jd-match/watchlist` body `{jobMatchId:R1}` + IK；（A2）同 IK 重放；（A3）不同 IK 重复 add 同 jobMatchId；（A4）`POST /watchlist` body `{jobMatchId:R2}` + IK；（A5）`POST /watchlist` body `{jobMatchId:R3}` + IK；（A6）`GET /api/v1/jd-match/watchlist`；（A7）`DELETE /api/v1/jd-match/watchlist/{R3}` + IK；（A8）同 IK 重放 DELETE；（A9）`GET /api/v1/jd-match/saved-searches`（初次为空）；（A10）`POST /api/v1/jd-match/saved-searches` body `{label:'frontend remote', query, filters}` + IK；（A11）`GET /api/v1/jd-match/saved-searches`；（B1）用户 B 调 `POST /watchlist` body `{jobMatchId:R1}` + IK（cross-user recommendation） | （A1）返回 200 + WatchlistItem with linked_job_match_id=R1；DB watchlist_items 新增 1 行 (user_A, R1, tone='ok')；fixture parity `addToWatchlist.json` `default`；（A2）IK replay 返回首次 item；DB 仍 1 行；（A3）UNIQUE 约束触发 → 返回首次 item（不创建新行）；DB 仍 1 行；（A4-A5）成功；DB 3 行 (R1=ok, R2=warn, R3=muted)；（A6）返回 3 items；按 `added_at DESC` 排序；每个 item tone 派生正确（92→ok / 78→warn / 45→muted）；fixture parity `listWatchlist.json` `default`；（A7）返回 204；DB R3 行删除；fixture parity `removeFromWatchlist.json` `default`；（A8）IK replay 返回 204；DB 仍删除状态；（A9）返回 200 + items=[]；fixture parity `listSavedSearches.json` (empty variant or default with N items)；（A10）返回 200 + SavedSearch；DB saved_searches 新增；label / query 不进 log；fixture parity `createSavedSearch.json` `default`；（A11）返回 200 + 1 item；newJobsCount / lastRunAt 字段在用户尚未 run 时为 null；（B1）返回 404 + `RESOURCE_NOT_FOUND`（cross-user recommendation 不可加入 watchlist）；DB 用户 B 的 watchlist 仍 0 行；（F 隐私）watchlist label / saved-search label / query 不出现在 console / URL / localStorage / log；（G 旧口径）grep `LinkedIn|Boss|脉脉|拉勾` in store data：0 命中 | `test/scenarios/e2e/p0-095-jd-match-watchlist-and-saved-search-lifecycle/`；trigger 运行 `cd backend && DATABASE_URL=... go test ./cmd/api -run '^(TestJDMatchFixtureParity|TestJDMatchHTTPScenario)$' -count=1 -v`；verify 必须断言 watchlist list/add/remove、saved searches list/create、HTTP scenario PASS，并拒绝 skip / no-test / raw email |

## Phase 4 + 5: search + market signals

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.096 | searchJobs 同步 + market signals + AI timeout 502 + IK + 隐私 | A2 dev stack 拉起；用户 A 已登录；内部 jobs 池已 seed 50 个 mock JD；A3 AIClient 注入 stub provider 3 variant：（M1）success（返回 3 个 matched recommendation） / （M2）AI timeout 模拟 30s 超时 / （M3）output_invalid；用户 A 已有 5 个 jd_match_recommendation 用于 market_signals 派生 | 用户 A 通过 `cmd/api` route 分别调：（A1）`POST /api/v1/jd-match/search` body `SearchJobsRequest{ query:'frontend platform remote', filters:{remote:true} }` + IK + stub variant M1；（A2）同 IK 重放；（A3）不同 IK + stub variant M2；（A4）不同 IK + stub variant M3；（A5）`GET /api/v1/jd-match/market-signals?window=7d`；（A6）`GET /api/v1/jd-match/market-signals?window=30d`；（A7）`GET /api/v1/jd-match/market-signals?window=invalid` | （A1）返回 200 + `SearchJobsResponse{ searchRunId, items:[3个 JobMatchRecommendation 含 provenance] }`；同步返回；DB `jd_match_search_runs` 新增 1 行（query / filters / result_count=3 / provenance）；handler 通过 A3 AIClient 调 F3 `jd_match.search` feature_key；items 通过 JOIN 已有 `jd_match_recommendations`（不创建 ephemeral 行）；fixture parity `searchJobs.json` `default`；（A2）IK replay 返回首次 searchRunId + items；DB search_runs 仍 1 行；（A3）30s timeout → 返回 502 + `error.code='AI_PROVIDER_TIMEOUT'`；ai_task_runs 记 1 行 fail；search_runs 不新增；fixture parity `searchJobs.json` `failed` variant；（A4）output_invalid → 返回 502 + `error.code='AI_OUTPUT_INVALID'`（or B1 generic）；（A5）返回 200 + `MarketSignalsResponse{ signals:[4个 signal 含 k/v/d?/tone], asOf }`；4 个 signal 派生从 watchlist + recommendations + 内部池；fixture parity `getMarketSignals.json` `default`；（A6）30d window 返回不同 asOf；（A7）window invalid 返回 422 + VALIDATION_FAILED；（F 隐私）query / filters / freeNote / search_run query 不出现在 console / URL / localStorage / log / outbox payload；audit_events 只记 search_run_id / user_id / result_count；（G 旧口径）grep `LinkedIn|Boss|脉脉|拉勾` in search code + AI prompt 输入 + outbox payload：0 命中 | `test/scenarios/e2e/p0-096-jd-match-search-and-market-signals/`；trigger 运行 `cd backend && DATABASE_URL=... go test ./cmd/api -run '^(TestJDMatchFixtureParity|TestJDMatchHTTPScenario)$' -count=1 -v`；verify 必须断言 search、market signals、HTTP scenario PASS，并拒绝 skip / no-test / raw email |

## Phase 2 + 5: agent scan + privacy delete

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.097 | agent scan job lifecycle + recommendation generation + outbox event + privacy delete cascade + 失败回滚 | A2 dev stack 拉起；`cmd/api` in-process drainer 启动并注册 `jd_match_agent_scan` job handler；A3 AIClient 注入 stub provider 2 variant（success / output_invalid）；F3 `jd_match.recommendation` feature_key 已 ready；用户 A 已登录 + 有 candidate_profile + 3 resumes；用户 A 已有 10 jd_match_recommendation + 3 watchlist + 2 saved_searches + 5 search_runs + 4 agent_scans 行；用户 B / C 不受影响 | 分子场景：（A）`jd_match.agent_scan` job RunOnce / drainer 队列消费 success；（B）队列消费 output_invalid；（C）调 internal `DeleteJobMatchDataForUser(user_A)`；（D）模拟 `jd_match_recommendations.DeleteForUser` 抛错验证回滚；（E）`cmd/api` shutdown | （A1）drainer 拉取队列；调用 A3 AIClient profile=`jd_match.recommendation.default`；（A2）upsert 5 个新 / 更新 recommendation；DB `jd_match_recommendations` 新增 / 更新；`ai_task_runs` 新行 typed columns（model_profile_name=jd_match.recommendation.default + model_profile_version + prompt_version + rubric_version + validation_status='ok'）；（A3）agent_scans 行 status: scanning → idle；last_scan_at + next_scan_at 更新；（A4）outbox `jd_match.recommendation.completed` event 写入；envelope 字段 `{userId, agentScanId, recommendationCount=5, completedAt}`；**不含** query / reasons / risks / highlights / source_url；（B1）AI output_invalid → DB agent_scans 行 status='error' + error_message；recommendation 不变；outbox 不发 completed event；ai_task_runs 写 1 行 fail；audit_events 写 ai_call_failed tombstone（无 prompt 内容）；（C1）调 `DeleteJobMatchDataForUser(user_A)`；删除顺序：watchlist_items (3 删) → saved_searches (2 删) → jd_match_search_runs (5 删) → jd_match_recommendations (10 删) → agent_scans (4 删)；audit_events 新增 tombstone 行：`user_id=user_A` / `event_name='jd_match.privacy_delete'` / `metadata` 含 `{watchlistCount:3, savedSearchCount:2, searchRunCount:5, recommendationCount:10, agentScanCount:4, deletedAt}`；**不含** query / label / reasons / risks / source_url / freeNote 任何字段值；（C2）DB 状态：user_A 5 张表全部 0 行；用户 B / C 不受影响；（D1）模拟 `jd_match_recommendations.DeleteForUser` 抛错：事务回滚（前面已删的 watchlist / saved_search 也回滚到原状）；audit 写 failure record；（E1）`cmd/api` shutdown 取消所有 drainer context 且不泄漏 goroutine；（F 隐私）log / audit / outbox payload 写入路径不序列化 query / label / reasons / source_url / interview_hypotheses；允许 SQL/store 层出现必要列名，禁止把列值写入日志或事件；（G 旧口径）grep `LinkedIn|Boss|脉脉|拉勾|mistake|growth|drill` in events / outbox 内容 + privacy delete code：0 命中 | `test/scenarios/e2e/p0-097-jd-match-agent-scan-and-privacy-delete/`；trigger 运行 `cd backend && DATABASE_URL=... go test ./cmd/api -run '^(TestJDMatchHTTPScenario|TestJDMatchAgentScanDrainerScenario)$' -count=1 -v`；verify 必须断言 HTTP scenario、agent scan drainer scenario PASS，并拒绝 skip / no-test / raw email |
