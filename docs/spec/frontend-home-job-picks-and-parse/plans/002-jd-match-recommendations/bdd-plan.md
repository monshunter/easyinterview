# 002 BDD Plan

> **版本**: 1.4
> **状态**: completed
> **更新日期**: 2026-05-10

**关联 Plan**: [plan](./plan.md)

## 1 场景矩阵

| 场景 ID | 类别 | 关联 Phase | 关联 Spec C-* | 关联 BDD-Gate（主 checklist） |
|---------|------|-----------|--------------|----------------------------|
| E2E.P0.022 | primary path · Recommended tab 主路径 + alternate path（Save/Unsave、Mark not relevant、Open source）+ failure path（4xx revert）+ alternate（auth pending action） | Phase 2 + 3 | C-12, C-13, C-15 | Phase 2.6、Phase 3.10 |
| E2E.P0.023 | primary path · Search tab 自然语言搜索 + alternate（Saved searches create、4 chip filter）+ failure path（5xx、empty、slow-response 持续渲染 5 步 AGENT panel + i18n + opacity 渐变） | Phase 4 | C-14, C-15 | Phase 4.9 |
| E2E.P0.024 | primary path · Watchlist tab + Market signals + alternate（chevron handoff to Recommended）+ boundary（empty、partial-data、unknown linkedJobMatchId） | Phase 5 | C-16 | Phase 5.7 |
| E2E.P0.025 | primary path · Profile chip + AGENT scan status + alternate（Auth pending action 跨 Recommended/Search API side-effect 综合）+ regression（i18n 切换 + 三主题态切换不破坏数据驱动） | Phase 2 + 3 + 4 | C-12, C-15 | Phase 2.6 |
| E2E.P0.026 | primary path · Confirm interview from jd_match → parse 出口 params 完整性 + regression（parse 屏不破坏 + 不写额外 storage） | Phase 3 | C-13 | Phase 3.10 |

---

## Phase 2 + 3: Recommended tab 主路径 + 4 button 闭环 + auth gate

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.022 | jd_match Recommended tab 主路径 + Save/Unsave/Mark not relevant/Open source 闭环 + 4xx revert + auth pending action | 用户进入 frontend dev server，可登录或未登录；`getJobMatchProfile` fixture 配置 `default` variant；`getAgentScanStatus` 配置 `idle/scanning/error` 三 variant；`listJobRecommendations` 配置 `empty/one/many/failed` 四 variant；`getJobRecommendation` 配置 `default/network-intel-empty/failed`；`addToWatchlist` 配置 `default/4xx-validation/5xx`；`removeFromWatchlist` 配置 `default/4xx-not-found`；`markJobNotRelevant` 配置 `default/4xx`；signed-in / signed-out 切换入口 | 切换 fixture variant 与登录态，分别走以下子用例：（A）已登录 + many variant：默认 Recommended tab，点击列表第二张卡 → JDDetail；（B）已登录：在 JDDetail 点 Save → 再点 Unsave；（C）已登录：在 JDDetail 点 Mark not relevant；（D）已登录：在 JDDetail 点 Open source；（E）已登录：addToWatchlist 4xx variant 下点 Save 触发 revert；（F）已登录：markJobNotRelevant 4xx variant 下点 dismiss 触发 revert；（G）未登录：点 Save 触发 auth_login → 登录 → 自动重新触发 Save；（H）empty variant：列表空态 + CTA 渲染；（I）failed variant：错误占位 + retry button | （A）`getJobMatchProfile` + `getAgentScanStatus` + `listJobRecommendations` 各调 1 次；点击第二张卡后 `getJobRecommendation("jm-2")` 调 1 次，JDDetail 详情不得只复用 list 摘要伪装；JDDetail header / + WHY MATCHES / ⚠ WHERE STRETCH / ROLE SNAPSHOT / INTEL / Action bar 4 button 全部 testid 命中；（B）`addToWatchlist` 调 1 次 + body `{ jobMatchId }` + `Idempotency-Key` header 存在 + button toggle + window.eiToast；再调 `removeFromWatchlist` 同样契约；（C）`markJobNotRelevant` 调 1 次 + body `{ reason: enum_default, freeNote: undefined }` + `Idempotency-Key` + 卡片从列表隐藏 + 自动选下一张 + toast；（D）`window.open` 调 1 次 + url + `_blank` + `noopener,noreferrer` flags 全命中；Source 不触发 auth pending action；sourceUrl 不在 URL/localStorage/telemetry/console；（E）（F）button 状态 revert + 卡片重新出现 + error toast；列表与 selected 不破坏；（G）route 跳 `auth_login`；登录后 route 跳回 `jd_match` Recommended tab + selected = selectedJobMatchId + Save 自动重新触发；pendingAction params 仅 `{ tab: "recommended", selectedJobMatchId, action: "save" }`，不含其他字段；（H）empty state DOM 命中 + retry/CTA 命中；（I）失败 UI + retry 按钮 + 不展示伪造卡片；（通用）jobMatchId / sourceUrl / freeNote 在 console.log / URL / localStorage / telemetry 全部 0 命中；mockTransport spy 只记录 status code；前端 network spy 不出现 LLM/provider/外部招聘平台调用；i18n zh/en 切换 + warm/light → dark → customAccent 切换关键文本 / computed background 出现可见变化；plan 001 placeholder testid (`jdmatch-placeholder*`) DOM 0 命中 | `test/scenarios/e2e/p0-022-jd-match-recommended-and-confirm/` |

## Phase 4: Search tab 主路径 + Saved searches + filter + 5 步 AGENT panel + failure

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.023 | jd_match Search tab 自然语言搜索 + Saved searches + 4 chip filter + 5 步 AGENT panel + failure + auth gate + privacy | 用户已登录或未登录；`searchJobs` fixture 配置 `default/empty/failed/slow-response` 四 variant；`listSavedSearches` 配置 `default/empty/4xx`；`createSavedSearch` 配置 `default/4xx-validation`；signed-in / signed-out 切换入口 | （A）切 Search tab：listSavedSearches 调 1 次；（B）已登录 + default variant：输入 query "高级前端 上海 远程"，点 Run；（C）已登录 + slow-response variant：点 Run，验证 5 步 AGENT panel（accent label + 5 step + opacity 渐变前 3 步 active 1.0 / 后 2 步 dim 0.4 + accent 边框）in-flight 渲染持续到 settle，不超时；（D）已登录 + failed variant：点 Run，验证 inline error；（E）已登录 + empty variant：点 Run，验证 no-results 空态；（F）已登录：results 渲染后切 4 chip filter（all/strong/remote/unseen）；（G）已登录：点 "Save current as watch" → createSavedSearch；createSavedSearch 4xx variant 下点击触发 toast + 不写入；（H）未登录：点 Run 触发 auth_login → 登录 → 自动重新触发 Run；（I）切 tab 时 abort in-flight search | （A）`listSavedSearches` 调 1 次；（B）`searchJobs` 调 1 次 + body `{ query, filters?, profileSnapshot? }` + `Idempotency-Key` header 存在；in-flight 期间 `jdmatch-search-searching-panel` testid 渲染 + 5 个 `jdmatch-search-searching-step-${i}` testid 全命中 + `jdMatch.search.searchingStep1..5` i18n 文案命中；results 渲染后 `jdmatch-search-results` 命中、JobMatchCard 数 ≤6；（C）`jdmatch-search-searching-panel` + 5 step testid 持续渲染直到 settle，不超时；opacity 渐变 computed style（前 3 step `opacity: 1`，后 2 step `opacity: 0.4`）命中；`● AGENT SCANNING` / `● AGENT 扫描中` accent label + 1px accent 外边框 + 3px accent 左边框 命中；不调用 setInterval 或 setTimeout step-advance；负向断言：动态 JD 数字（`248` / `87` / `unique postings` / `唯一岗位` / `248 → 87`）在 SearchTab DOM 与前端 i18n 中 0 命中；（D）`jdmatch-search-error` 命中 + 输入保留 + 不展示伪造 results；（E）`jdmatch-search-empty` 命中；（F）filter 切换不发新 search request；results count 与 filter active 状态正确；filter 4 状态各自命中；（G）`createSavedSearch` 调 1 次 + body `{ label, query, filters? }` + `Idempotency-Key` + toast 命中；4xx variant 下 toast + saved searches list 不变；（H）route 跳 `auth_login`；登录后 route 跳回 `jd_match` Search tab + Run 自动重新触发；pendingAction params 仅 `{ tab: "search", action: "run_search", pendingJdMatchActionId }`，不含 query 文本或 label；query/label 只通过当前 SPA 会话内存消费；（I）切 tab 后 search request 不再 settle 到当前组件；（通用）query / saved-search label / filter state / sourceJobUrl 在 console.log / URL / localStorage / telemetry 全部 0 命中；mockTransport spy 只记录 status code | `test/scenarios/e2e/p0-023-jd-match-search-and-saved/` |

## Phase 5: Watchlist tab + Market signals + chevron handoff + boundary

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.024 | jd_match Watchlist tab + Market signals + chevron handoff + empty + partial-data + privacy | 用户已登录；`listWatchlist` fixture 配置 `default/empty/few/4xx` 四 variant；`getMarketSignals` 配置 `default/partial-data/failed`；`listJobRecommendations` 已加载有 / 缺失 linkedJobMatchId 两种状态 | （A）切 Watchlist tab + default variant：列表 + Market signals 4 卡渲染；（B）切 Watchlist tab + empty variant：empty state；（C）切 Watchlist tab + partial-data variant：部分卡片 + 缺失值 fallback；（D）已渲染列表后点击某 WatchlistItem chevron（其 linkedJobMatchId 在当前 Recommended 列表）：跳 Recommended + selected = linkedJobMatchId；（E）点击 chevron（其 linkedJobMatchId 不在 Recommended 列表）：fallback 第一张 + warning toast；（F）listWatchlist 4xx variant：错误占位 | （A）`listWatchlist` 调 1 次 + `getMarketSignals` 调 1 次；不重复调；`jdmatch-watchlist-item-${id}` 与 `jdmatch-market-signal-${k}` 全部 testid 命中；3 tone (ok/warn/muted) 各自渲染 + 3px 左边框颜色对应 D2 token；refresh footer 文案 i18n zh/en；（B）`jdmatch-watchlist-empty` 命中 + CTA「去 Recommended 添加」可见；（C）部分卡片渲染 + 缺失值 fallback 文案显示而非 0 或硬编码；（D）tab 切 Recommended + selected = linkedJobMatchId + JDDetail 渲染对应卡；（E）tab 切 Recommended + selected = 第一张 + warning toast；（F）错误占位 + retry button；（通用）linkedJobMatchId / watchlist label / sourceJobUrl 在 console.log / URL / localStorage / telemetry 全部 0 命中；mockTransport spy 只记 status code | `test/scenarios/e2e/p0-024-jd-match-watchlist-and-signals/` |

## Phase 2 + 5: Profile + AGENT scan status + Auth pending action 综合

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.025 | jd_match Profile chip + AGENT scan status + Auth pending action 综合 | 用户首次进入 jd_match；`getJobMatchProfile` 配置 `default/unauthenticated/partial-profile`；`getAgentScanStatus` 配置 `idle/scanning/error/next-scan-soon`；signed-in / signed-out 切换入口；i18n zh/en + warm/light + dark + customAccent 全状态切换入口 | （A）已登录 + default profile + idle agent：进入 jd_match；（B）切回 Recommended tab：getAgentScanStatus 再调 1 次；（C）已登录 + scanning agent：tone 与文案变化；（D）已登录 + error agent：tone 与文案变化；（E）已登录 + partial-profile：部分字段 fallback；（F）未登录 + unauthenticated profile：chip 显示降级状态；（G）未登录：在 Recommended / Search 的 API side-effect button（Save、Confirm interview、Run、Save current）触发 auth_login；登录后回到对应 tab 自动重新触发；（H）未登录点击 Source 与 Watchlist chevron 不触发 auth pending action，分别执行安全新开页 / 本地 tab handoff；（I）切 zh/en；（J）切 warm/light → dark → customAccent | （A）`getJobMatchProfile` + `getAgentScanStatus` 各调 1 次；进入 Recommended tab 时 `getAgentScanStatus` 再调 1 次（B 验证），profile 不重复调；（C）AGENT badge tone = accent + i18n "扫描中…" / "Scanning…" + 不引入 setInterval / SSE / WebSocket；（D）AGENT badge tone = warn + i18n error 文案；（E）chip 缺失字段 fallback；（F）chip unauthenticated 状态；（G）pendingAction params 各自仅 `{ tab, selectedJobMatchId?, action }`，不含 query / label / freeNote / sourceUrl 等隐私字段；登录后 selectedJobMatchId 自动恢复 + action 自动重新触发；（H）Source 无 pendingAction 且 `noopener,noreferrer` 命中，Watchlist chevron 无 pendingAction 且 linkedJobMatchId 不落 URL/localStorage；（I）i18n 切换关键文本立即重绘；（J）AGENT badge tone / Profile chip border / chip 颜色三态切换出现可见变化；（通用）所有 mockTransport call audit；plan 001 placeholder testid 0 命中 | `test/scenarios/e2e/p0-025-jd-match-profile-and-agent-status/` |

## Phase 3: Confirm interview from jd_match → parse 出口 params 完整性

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.026 | Confirm interview from jd_match → parse 出口 params 完整性 + parse 屏不破坏 + 不写额外 storage | 用户已登录；jd_match Recommended tab；`listJobRecommendations` `many` variant；plan 001 已实现的 parse 屏（`importTargetJob` + `getTargetJob` + `updateTargetJob` 等链路） | 用户在 Recommended tab 选中卡 jm-2，点击 JDDetail Action bar Confirm interview button | （1）`nav("parse", { source: "jd_match", sourceJobMatchId: "jm-2" })` 调 1 次；params 仅含 `source` 与 `sourceJobMatchId` 两个 jd_match 注入字段（不含 query / saved / hidden 等其他 jd_match 内部状态）；（2）route 进入 parse 屏后 plan 001 已有的 parse 行为不破坏（D-2 polling、D-7 隐私、D-4 Confirm 跳 workspace 仍 PASS）；（3）source / sourceJobMatchId 不写入 URL query / localStorage / sessionStorage / telemetry；通过 React state 与 nav params 内部传递；（4）parse 屏 D1 已有 testid 全部命中；plan 002 注入的 source 字段不修改 parse 屏 DOM 或现有 testid；（5）regression：E2E.P0.015 / E2E.P0.016 主路径 setup→trigger→verify→cleanup 仍 PASS | `test/scenarios/e2e/p0-026-jd-match-confirm-interview-handoff/` |
