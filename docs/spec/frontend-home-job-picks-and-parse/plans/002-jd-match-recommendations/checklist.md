# 002 JD Match Recommendations Checklist

> **版本**: 1.4
> **状态**: completed
> **更新日期**: 2026-05-10

**关联计划**: [plan](./plan.md)

## Phase 0: UI truth-source + B2 owner contract preflight

- [x] 0.1 读取最新 `ui-design/src/screen-jd-match.jsx` 与 `docs/ui-design/`，确认 `SearchTab` `searching=true` 区域当前形态为 5 步 AGENT panel + accent label + 1px accent 外边框 + 3px accent 左边框 + 5 行 step 文案 + `opacity: i <= 2 ? 1 : 0.4` 渐变；plan 002 不修改 ui-design 静态文件，前端按此形态源级复刻并通过 i18n 5 step key（典型 `jdMatch.search.searchingStep1..5`）提供 zh/en 文案；ui-design 第 3 步动态 JD 数字（`248`/`87`/`unique postings`/`唯一岗位`）在前端 i18n 替换为不含具体数字的静态语义文案 — evidence: plan.md §7.1
- [x] 0.2 复核 B2 owner truth source 与 validators：`docs/spec/openapi-v1-contract/spec.md`、`openapi/README.md`、`openapi/fixtures/README.md`、`scripts/lint/openapi_inventory.py`、`scripts/lint/validate_fixtures.py`、`docs/spec/mock-contract-suite/spec.md`、`docs/spec/engineering-roadmap/spec.md` 中 12 tag / 34 endpoint / 34-operation coverage 口径均列入 Phase 1 同步范围 — evidence: plan.md §7.2（7 文件 / 多处硬编码 count gate 已锁定）
- [x] 0.3 冻结 12 operation matrix、5 个 `Idempotency-Key` side-effect operation、`getJobRecommendation` 必调详情契约、pendingAction action enum（`save` / `unsave` / `dismiss` / `confirm_interview` / `run_search` / `create_saved_search`）与隐私字段负向清单；确认 Source button 与 Watchlist chevron 不进入 auth pending action — evidence: plan.md §7.3 / §7.4 / §7.5
- [x] 0.4 Preflight Gate: Phase 0 evidence log 记录 UI truth-source 当前形态（5 步 AGENT panel 保留 + i18n 5 step key 列表 + 动态 JD 数字静态化策略）、B2 owner inventory 待同步清单、12 operation matrix freeze、auth action enum freeze 与隐私字段负向清单 — evidence: plan.md §7.6（feature branch + 修订版本 + 5 项 freeze 全部就位）

## Phase 1: JobMatch OpenAPI 契约 + fixture + codegen

- [x] 1.1 在 `openapi/openapi.yaml` 新增 `JobMatch` tag 与 12 operationId（`getJobMatchProfile` / `getAgentScanStatus` / `listJobRecommendations` / `getJobRecommendation` / `addToWatchlist` / `removeFromWatchlist` / `markJobNotRelevant` / `searchJobs` / `listSavedSearches` / `createSavedSearch` / `listWatchlist` / `getMarketSignals`）path / method / requestBody / responses；schemas 含 `JobMatchProfile` / `AgentScanStatus` / `JobMatchRecommendation` / `WatchlistItem` / `SavedSearch` / `MarketSignal` / `MarkNotRelevantRequest` / `SearchJobsRequest` / `SearchJobsResponse` / `AddToWatchlistRequest` / `CreateSavedSearchRequest` / `MarketSignalsResponse`；AI-generated 字段必须引用 `GenerationProvenance`；side-effect 5 操作支持 `Idempotency-Key` parameter；同步 B2 owner inventory / README / validator / mock-contract coverage 到 13 tag / 46 endpoint（contract test 断言 schema 完整与 tag=`JobMatch`） <!-- evidence: 2026-05-10 inventory check 13 tags/46 ops PASS; swagger-cli validate PASS; B2 spec D-17 + 7 owner files synced -->

- [x] 1.2 新增 `openapi/fixtures/JobMatch/` 目录与 12 个 operation fixture 文件，按 plan §3.6 列出的 scenarios 配置（至少 default + 一个 edge variant，含 empty / failed / slow-response 等关键 variant）；通过 `make validate-fixtures` <!-- evidence: 2026-05-10 make validate-fixtures OK 46 fixtures; per §3.6 scenarios: getJobMatchProfile{default,unauthenticated,partial-profile} / getAgentScanStatus{default,idle,scanning,error,next-scan-soon} / listJobRecommendations{default,empty,one,many,failed} / getJobRecommendation{default,network-intel-empty,failed} / addToWatchlist{default,4xx-validation,5xx-server-error} / removeFromWatchlist{default,4xx-not-found} / markJobNotRelevant{default,4xx} / searchJobs{default,empty,failed,slow-response} / listSavedSearches{default,empty,4xx} / createSavedSearch{default,4xx-validation} / listWatchlist{default,empty,few,4xx} / getMarketSignals{default,partial-data,failed}; openapi_inventory.py PASS (13 tags / 46 operations) -->
- [x] 1.3 升级 `mock-contract-suite`（如需要）支持 per-operation per-scenario 切换组合；保留 plan 001 fixture variant 切换向后兼容；spec / plan 同步更新 <!-- evidence: 2026-05-10 mockTransport.ts 已通过 Prefer: example=<scenario> per-request header 原生支持 per-operation per-scenario switching；App.tsx requestOptions per-operationId 通道（如 getMe / runtimeConfig / listJobRecommendations）即用即配；mock-contract-suite spec v1.3 §2.1 / §6 C-1 已 additive 升级到 13 tag / 46 operation 含 JobMatch 12 op；无需新增组件升级，plan 001 fixture variant 切换向后兼容保留 -->
- [x] 1.4 运行 OpenAPI codegen 重建 `frontend/src/api/generated/client.ts` 与 schema typings；`pnpm --filter @easyinterview/frontend typecheck` PASS；codegen drift check 0 <!-- evidence: 2026-05-10 make codegen 生成 12 个 JobMatch 方法 (getJobMatchProfile/getAgentScanStatus/listJobRecommendations/getJobRecommendation/markJobNotRelevant/listWatchlist/addToWatchlist/removeFromWatchlist/searchJobs/listSavedSearches/createSavedSearch/getMarketSignals) + 完整 schema typings；pnpm --filter @easyinterview/frontend typecheck PASS (tsc --noEmit 0 errors)；二次 make codegen 不再产生新 diff (drift check = 0)；backend lint-events 失败属 baseline 预存问题，与本 plan 无关 -->
- [x] 1.5 新增 `frontend/src/api/generated/jobMatch.contract.test.ts`（或同等位置）：测 generated client 12 method 可调用、request body / response schema 与 fixture variant parity、Idempotency-Key parameter 出现在 5 个 side-effect operation；contract test 全 PASS <!-- evidence: 2026-05-10 frontend/src/api/jobMatch.contract.test.ts (next to mockTransport.test.ts; "或同等位置")；vitest run src/api/jobMatch.contract.test.ts → 1 file / 14 tests PASS：12 method 可调用 + getJobMatchProfile/getAgentScanStatus/listJobRecommendations/getJobRecommendation/listWatchlist/getMarketSignals fixture variant parity + addToWatchlist/removeFromWatchlist/markJobNotRelevant/searchJobs/createSavedSearch Idempotency-Key header 验证 + 5 个 side-effect fixture default scenario `request.headers.Idempotency-Key` 命中负向反查；evidence remediation 2026-05-10：放宽 `SIDE_EFFECT_FIXTURES` 类型签名为结构 `SideEffectFixtureShape`（matches actual fixture.scenarios.default.request.headers usage），消除 4 个 TS2322 跨 fixture scenarios 形态错误；`pnpm --filter @easyinterview/frontend typecheck` PASS (0 errors) 加入 evidence 闭环。 -->
- [x] 1.6 BDD-Gate: 验证 `E2E.P0.022 / E2E.P0.023 / E2E.P0.024 / E2E.P0.025 / E2E.P0.026` 资产构建到 ready 态前置（fixture 存在性 + generated client 可调用），具体 scenario 验证延后到 Phase 6 <!-- verified: 2026-05-10 method=manual bddChecklist=fixture-prep-only 范围 PASS：5 个场景 fixture variant 准备项已在 bdd-checklist 勾选；fixture 存在性 = make validate-fixtures OK 46 fixtures 含 12 JobMatch；generated client 可调用 = pnpm exec vitest run src/api/jobMatch.contract.test.ts 1/1 file × 14/14 tests PASS（12 method + Idempotency-Key × 5 ops + multi-variant parity）；scenario 脚本 / setup→trigger→verify→cleanup / INDEX 行均按 plan 设计延后到 Phase 6.2-6.5 -->

## Phase 2: JDMatchScreen 容器升级 + Hero / Profile chip / AGENT badge 数据驱动

- [x] 2.1 删除 `frontend/src/app/screens/jd_match/JDMatchScreen.tsx` 中所有 plan 001 placeholder testid（`jdmatch-placeholder` / `jdmatch-placeholder-cta`）与 fallback 文案；删除 `jdMatch.placeholderTitle` / `placeholderCopy` / `placeholderCta` 三个 i18n key（zh + en）；Vitest typed helper test 断言 key 删除后无任何 runtime 引用 <!-- evidence: 2026-05-10 placeholder DOM block (JDMatchScreen.tsx 旧 lines 222-263) 删除；zh.ts/en.ts 各删除 3 个 placeholder key；新增 `frontend/src/app/screens/jd_match/JDMatchScreen.placeholderRemoved.test.tsx` (3 tests: testid removal + zh keys removal + en keys removal)；JDMatchPlaceholder.test.tsx 移除「renders placeholder content in tab area」测试用例；`pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/jd_match/` PASS 2 files / 8 tests；`grep -rn "jdmatch-placeholder|placeholderTitle|placeholderCopy|placeholderCta" frontend/src/ frontend/tests/` 0 命中（pixel-parity Playwright 由 Phase 6.1 升级覆盖） -->
- [x] 2.2 新增 `frontend/src/app/screens/jd_match/useJobMatchProfile.ts` 与 `useAgentScanStatus.ts` hooks，通过 D1 generated client 调 `getJobMatchProfile` / `getAgentScanStatus`；React state loading / data / error 三态；Vitest 断言进入 jd_match 各调 1 次；切回 Recommended tab 时 `getAgentScanStatus` 再调 1 次（不重复调 profile）；切其他 tab 时不调；不引入 setInterval / SSE / WebSocket（D-10） <!-- evidence: 2026-05-10 新增 useJobMatchProfile.ts (request seq cancellation, 3 态返回) + useAgentScanStatus.ts (依赖 activeTab，仅在 "recommended" 触发 fetch；D-10 不引入 setInterval/SSE/WebSocket); JDMatchScreen.tsx tabs 改为 interactive (onClick=setTab) 并 wire 两 hook (void 占位，data 消费由 2.3 接力); Vitest jd_match 5 文件 19 tests PASS — useJobMatchProfile.test.tsx (4 tests: loading 三态 + 异常 + 无 runtime) + useAgentScanStatus.test.tsx (5 tests: mount 1 调 + search/watchlist 不调 + tab 回 recommended 再调 + 无 setInterval/EventSource/WebSocket + 异常态) + JDMatchScreen.fetchBehavior.test.tsx (2 tests: entry 各调 1 次 + tab 切换契约); pnpm typecheck PASS (0 errors); grep `setInterval|EventSource|WebSocket` 仅匹配 useAgentScanStatus.ts docstring 中的负向声明 -->
- [x] 2.3 重构 `JDMatchScreen.tsx` 按 `ui-design/src/screen-jd-match.jsx::JDMatchScreen` line 4-170 源级复刻 Hero（label / title / sub）+ Profile snapshot chip（avatar / SEARCHING AS / skills tags / PROFILE SOURCES）+ AGENT ACTIVE badge（status enum / lastScanAt / nextScanAt 文案）+ 三 tab 标签（带数量 badge）；Profile chip 字段映射严格按 §3.7；AGENT badge tone 与文案按 §3.7；testid `jdmatch-hero-{label,title,sub}` / `jdmatch-profile-chip-{avatar,searching-as,skills,sources}` / `jdmatch-agent-status-{badge,last-scan,next-scan}` / `jdmatch-tab-{recommended,search,watchlist}` 全部命中；Vitest 断言 <!-- evidence: 2026-05-10 JDMatchScreen.tsx 重构: Hero 沿用现有 testid; Profile chip 数据驱动 (avatar 优先 avatarUrl 否则 displayName initials; searching-as 由 displayName · yearsOfExperience · locationText 组合; skills 渲染 jdmatch-profile-chip-skill-${i} 序列; sources 由 sources.{resumes,jds,mocks,debriefs} 组合 + i18n 单位); AGENT badge 三态 data-tone (idle/scanning/error/loading) + tone color + lastScanAt/nextScanAt 通过新增 formatRelativeMinutes 帮助器渲染 + scanning 隐藏 next-scan; tabs 准备 count badge 槽位 (count != null 渲染) — Phase 3/4/5 接力填充计数; 新增 i18n 命名空间 jdMatch.profile.* (10 keys) 与 jdMatch.agent.* (6 keys); 删除旧 flat keys (profileInitials/searchingAs/profileSummary/profileSources/profileSourcesStats/agentStatus); Vitest jd_match 6 文件 29 tests PASS — 含新增 JDMatchScreen.dataDriven.test.tsx (10 tests: avatar img/initials, skills, sources counts, searching-as, agent idle/scanning/error tones, fallback, no placeholder); pnpm typecheck PASS (0 errors); 旧 flat key grep 0 hits -->
- [x] 2.4 扩展 `frontend/src/app/i18n/locales/zh.ts` 与 `en.ts`，删除 plan 001 三个 placeholder key 后新增 5 子命名空间：`jdMatch.profile.*`（≥8 key 覆盖 searching-as / skills heading / sources count 模板等）/ `jdMatch.agent.*`（≥6 key 覆盖 idle/scanning/error 状态文案 + last/next scan 文案模板）/ `jdMatch.recommended.*`（≥10 key 覆盖 STRONG/GOOD/STRETCH 标签 / Top reason 标签 / fit footer / next scan footer 等）/ `jdMatch.search.*`（≥16 key 含：5 步 AGENT panel `searchingStep1..5` 文案、AGENT label `searchingPanelLabel`、Run/Run-disabled 按钮文案、Saved searches 区文案、filter 4 标签、datasource tags、empty/error 文案；第 3 步文案不得包含 `248` / `87` / `unique postings` / `唯一岗位` 任意硬编码数字，必须为不含数字的静态语义文案如 `→ Dedup by JD hash · removing duplicate postings` / `→ 按 JD 哈希去重 · 移除重复岗位`）/ `jdMatch.watchlist.*`（≥10 key 覆盖 list heading / 3 tone change 文案 + Market signals 4 卡 k 文案 + refresh footer）；保留 plan 001 已有的 `jdMatch.hero.*` / `jdMatch.tab.*`；`messages.ts` typed helper 同步；Vitest `i18n` 套件断言 zh/en 同步无缺漏 + 5 step key 完整 + 动态 JD 数字 grep 0 命中 <!-- evidence: 2026-05-10 zh.ts/en.ts 新增 5 命名空间共 81 keys: profile.* (10), agent.* (6 — Phase 2.3 已完成), recommended.* (24 含 STRONG/GOOD/STRETCH + WHY/WHERE/SNAPSHOT/INTEL + 4 button + empty/error + 4 toast), search.* (24 含 5 step keys + searchingPanelLabel + Run/Running + dataSource × 4 + 4 filter + Saved searches + empty/error/retry/savedSearchCreate{,Error}), watchlist.* (12 含 heading + 3 tone + addedTimeLabel + empty/error + Market signals + chevron tooltip/fallback toast); 保留 jdMatch.heroLabel/heroTitle/heroSub + jdMatch.tabRecommended/Search/Watchlist; 无任何动态 JD 数字 (`248`/`87`/`unique postings`/`唯一岗位`/`248 → 87`) 出现; messages.ts 通过 zh as truth source 自动同步 typed helper; 新增 src/app/i18n/jdMatchLocaleNamespaces.test.ts (8 tests: zh/en parity, 5 namespaces 必填覆盖, retired placeholder keys 0 命中, 动态 JD 数字 grep 0 命中); pnpm typecheck PASS; 全 44 tests (jd_match 30 + i18n 14) PASS -->
- [x] 2.5 新增 `jd_match/JDMatchScreen.test.tsx`（≥10 测试）：测 Hero / Profile chip / AGENT badge / 三 tab 标签 testid 全部命中；测 `getJobMatchProfile` / `getAgentScanStatus` 调用次数与 fixture variant 三态渲染（idle / scanning / error；default / unauthenticated / partial-profile）；测 i18n zh/en 切换；负向断言 plan 001 placeholder testid（`jdmatch-placeholder*`）DOM 0 命中；测 `topbar-nav-jd_match` 高亮；测不调用 setInterval / SSE <!-- evidence: 2026-05-10 新增 frontend/src/app/screens/jd_match/JDMatchScreen.test.tsx (12 tests ≥10): 1) Hero 三 testid; 2) Profile chip + 4 sub-testids; 3) 3 tab testids; 4) shell data-attrs (route name + JSON params); 5) plan 001 placeholder testids 0 命中; 6) 旧 prototype 业务 testid (jdmatch-card/saved-search/watchlist/market-signal/search-bar) 0 命中; 7) Profile chip 数据驱动 (default fixture: avatar img + 6 skills + sources counts 2/5/4/1); 8) Profile chip avatar fallback initials (partial-profile fixture: avatarUrl=null → "AE"); 9) AGENT badge 三态渲染 (idle/scanning/error data-tone, scanning 隐藏 next-scan); 10) i18n zh/en 切换 Hero title 文案变化; 11) topbar-nav-jd_match aria-current=page (when activeRoute=jd_match); 12) 不注册 setInterval/EventSource/WebSocket 在 mount 后 50ms 内. 删除 obsolete JDMatchPlaceholder.test.tsx (5 tests subsumed by 新文件). pnpm vitest run jd_match/ → 6 文件 36 tests PASS (含 dataDriven 10 / placeholderRemoved 3 / fetchBehavior 2 / useJobMatchProfile 4 / useAgentScanStatus 5 / JDMatchScreen 12). pnpm typecheck PASS -->
- [x] 2.6 BDD-Gate: 验证 `E2E.P0.022` Hero/Profile/AGENT 阶段 + `E2E.P0.025` Profile/AGENT/auth 综合场景资产构建到 ready 态 <!-- verified: 2026-05-10 method=manual bddChecklist=ready-state-only 范围 PASS：E2E.P0.022 / E2E.P0.025 fixture variant 准备项已在 bdd-checklist 勾选；ready-state 资产 = useJobMatchProfile + useAgentScanStatus hooks (Phase 2.2) + JDMatchScreen 数据驱动 Hero/Profile chip/AGENT badge 三态 (Phase 2.3) + jdMatch.profile.* + jdMatch.agent.* 命名空间 (Phase 2.4) + JDMatchScreen.test.tsx 12 tests 含 fixture variant 三态渲染断言 (Phase 2.5)；运行时证据 = pnpm vitest jd_match/ + i18n/jdMatch* → 7 files / 44 tests PASS + make validate-fixtures OK 46 fixtures（含 JobMatch profile + agent fixtures）；scenario 脚本 / setup→trigger→verify→cleanup / INDEX 行 / E2E.P0.022 4 button 闭环验证均按 plan 设计延后到 Phase 6.2-6.10 -->

## Phase 3: Recommended tab — JobMatchCard + JDDetail + Save / Mark not relevant / Confirm interview

- [x] 3.1 新增 `frontend/src/app/screens/jd_match/RecommendedTab.tsx` + `JobMatchCard.tsx` + `JDDetail.tsx` 三个组件，按 `ui-design/src/screen-jd-match.jsx::JobMatchCard` line 335 + `JDDetail` line 377 源级复刻；JobMatchCard：左 3px 边框按 score 三档着色 / 标题 / 未看过点 / saved pin / company-companyTag / location-comp / 右上 score 32px + STRONG/GOOD/STRETCH label / Top reason / 底部 must X/Y · plus X/Y · posted · source；JDDetail：Header (company tag / title / level/location/comp tag / right 36px score) + + WHY MATCHES (绿 check) + ⚠ WHERE STRETCH (橙 info) + ROLE SNAPSHOT + INTEL（条件渲染 networkNote && similarInterviewers，合规措辞「公开面经 / JD / 公司资料信号」与「public interview-review, JD, and company-source signals」严格按 ui-design-contract.test.mjs 锁定）+ Action bar 4 button (Confirm interview / Save toggle / Source / Not relevant)；testid 命名按 plan §3.5；Vitest 断言 DOM 完整 + 视图模型严格按 §3.7 <!-- evidence: 2026-05-10 新增 JobMatchCard.tsx + JDDetail.tsx + RecommendedTab.tsx 三个 props-driven 组件 (源级复刻 ui-design line 335/377/302-318)；JobMatchCard 数据驱动 score band (strong/good/stretch via resolveScoreBand) + 3px 左边框着色 + STRONG/GOOD/STRETCH 标签 + unseen dot + saved pin + Top reason + must/plus/posted/sourceLabel 底部 + testid jdmatch-card-${id}/-score/-label/-top-reason/-fit-footer/-company/-meta/-unseen-dot/-saved-pin/-title/-active 命中；JDDetail Header (testid jdmatch-detail-header) + WHY (jdmatch-detail-why) + STRETCH (jdmatch-detail-risk) + ROLE SNAPSHOT (jdmatch-detail-snapshot ul/li) + INTEL 条件渲染 (networkNote || similarInterviewers > 0) + 合规措辞 "public interview-review, JD, and company-source signals" / "公开面经 / JD / 公司资料信号"；Action bar 4 button testid jdmatch-detail-action-{confirm,save,source,dismiss} + Source disabled when sourceUrl null + Save 按 saved 状态显示 actionSave/actionSaved；RecommendedTab 1.1fr/1.4fr 双列 grid + 选中态 fallback 第一张 + empty/loading/error 三态 (jdmatch-recommended-{empty,loading,error,error-retry})；新增 i18n actionSaved 键 (zh "已收藏" / en "Saved")；Vitest 3 文件 42 tests PASS：JobMatchCard.test.tsx 14 / JDDetail.test.tsx 16 / RecommendedTab.test.tsx 12；jd_match + i18n + contract regression 11 files / 100 tests PASS -->

- [x] 3.2 新增 `useJobMatchRecommendations.ts` hook，通过 generated `listJobRecommendations` 调；状态 loading / data / error；支持 cursor pagination（如 backend 返回 cursor）；Vitest 断言 fixture variant 四态（empty → empty state；one → 1 张卡；many → 全部渲染；failed → 错误占位 + retry button） <!-- evidence: 2026-05-10 新增 useJobMatchRecommendations.ts (request seq cancellation + retry refetch + pageInfo 透传 含 nextCursor/pageSize/hasMore + inert state when no runtime); 新增 useJobMatchRecommendations.test.tsx 10 tests PASS：loading 三态 + variant=default (3 items) + variant=empty (0 items) + variant=one (1 item) + variant=many (4 items) + variant=failed (error 设置 + items=[]) + retry 从 error 恢复 + 无 runtime inert + pageInfo.hasMore false 暴露给 cursor pagination -->

- [x] 3.3 新增 `useToggleWatchlist.ts` hook + Save / Unsave 闭环：optimistic toggle + `addToWatchlist({ jobMatchId }, { idempotencyKey })`；Unsave → `removeFromWatchlist(jobMatchId, { idempotencyKey })`；4xx → revert + error toast；window.eiToast 反馈；Vitest 断言 request body / Idempotency-Key header / 4xx revert / toast 文案 zh/en；并发多次点击按最后 server 响应为准（race 处理） <!-- evidence: 2026-05-10 新增 useToggleWatchlist.ts (per-id sequence ref + applyOptimistic 双向调用 + IK 每调用唯一 + window.eiToast 安全调用 + pendingIds 跟踪 + race 处理); 新增 i18n key toastSaveError (zh "更新关注列表失败，请重试" / en "Could not update watchlist — try again"); useToggleWatchlist.test.tsx 9 tests PASS：Save 路径 (applyOptimistic true + addToWatchlist body+IK + ok toast) + Unsave 路径 (applyOptimistic false + removeFromWatchlist id+IK + ok toast) + addToWatchlist 4xx revert + error toast + removeFromWatchlist 4xx revert + error toast + IK 每次唯一 + 无 eiToast 时不 throw + race 同 id 两次点击仅最后成功响应应用 + inert when no runtime + 隐私反查 jobMatchId/sourceUrl 不进 console -->

- [x] 3.4 新增 `useDismissRecommendation.ts` hook + Mark not relevant 闭环：optimistic 隐藏卡片 + 自动选下一张 + `markJobNotRelevant(jobMatchId, { reason: enum_default, freeNote: undefined }, { idempotencyKey })`；ui-design 不弹窗收集 reason，frontend 透传默认 enum 值；4xx → revert（卡片重新显示并恢复 selected）+ error toast；Vitest 断言行为完整 + Idempotency-Key <!-- evidence: 2026-05-10 新增 useDismissRecommendation.ts (default reason "not_relevant" 透传 + 不带 freeNote + applyOptimisticHide 返回 revert 闭包供 4xx 调用 + IK 唯一 + neutral/danger toast 双态); useDismissRecommendation.test.tsx 7 tests PASS：apply hide + markJobNotRelevant body { reason: "not_relevant" } + IK + 不带 freeNote 字段 + default scenario neutral toast (toastDismissed) + 4xx scenario revert + danger toast (toastDismissError) + IK 每次唯一 + inert when no runtime + 隐私反查 jobMatchId/freeNote 不进 console -->

- [x] 3.5 在 JDDetail Action bar Confirm interview button 接入 `nav("parse", { source: "jd_match", sourceJobMatchId: jobMatchId })`；不携带其他 jd_match 内部状态（如 query / saved / hidden）；Vitest 断言 nav 调用 + params 完整性 + 不含其他字段 <!-- evidence: 2026-05-10 JDMatchScreen.tsx 集成 RecommendedTab + useJobMatchRecommendations + useToggleWatchlist + useDismissRecommendation + 本地 hiddenIds/savedOverrides overlay 模型 + selectedId 自动选第一张可见卡 + handleConfirmInterview 调 navigate({name:"parse", params:{source:"jd_match", sourceJobMatchId: rec.id}}) 不携带 query/saved/hidden 等内部状态；新增 RecommendedConfirmInterview.test.tsx 2 tests PASS：confirm 按钮 → nav 1 次 + params Object.keys 严格等于 ["source","sourceJobMatchId"] (无其他字段) + 切换卡片后 sourceJobMatchId 更新到新选中 jobMatchId；jd_match suite 15 files / 128 tests PASS regression -->

- [x] 3.6 在 JDDetail Action bar Source button 接入 `window.open(sourceUrl, "_blank", "noopener,noreferrer")`；sourceUrl 缺失时 button disabled；Vitest spy 断言 window.open 调用 + 两个 flags 出现 + sourceUrl 不进 console / URL / localStorage / telemetry <!-- evidence: 2026-05-10 JDMatchScreen handleOpenSource 调 window.open(rec.sourceUrl, "_blank", "noopener,noreferrer") + sourceUrl 缺失短路；JDDetail.tsx Source button disabled when sourceUrl null；新增 RecommendedOpenSource.test.tsx 3 tests PASS：window.open 调 1 次 + url + _blank + "noopener,noreferrer" 全命中 + 第三张卡 (sourceUrl=null) Source button.disabled=true 且不调 window.open + 隐私反查 sourceUrl 不进 console.log/error/warn / localStorage.setItem -->

- [x] 3.7 接入 `requestAuth` pending action：未登录用户对 Save / Unsave / Mark not relevant / Confirm interview 触发 `requestAuth({ type: "jd_match_action", route: "jd_match", params: { tab: "recommended", selectedJobMatchId, action: enum }, label })`；登录后回到 jd_match Recommended tab + selected = selectedJobMatchId 自动重新触发 action（如 selectedJobMatchId 已被 dismiss 则 fallback 第一张 + 提示）；Source button 不进 auth pending action，只验证安全 `window.open` 与隐私红线；Vitest `jd_match/JDMatchAuthGate.test.tsx` 断言 query / label / freeNote / sourceUrl 不进 pendingAction params <!-- evidence: 2026-05-10 JDMatchScreen 集成 useRequestAuth + useAppRuntimeOptional + isUnauthenticated 守卫 + 4 个 handler (handleConfirmInterview/handleToggleSave/handleDismiss + Source 不 gate) 在 unauth 时调 requestAuth({type:"jd_match_action", route:"jd_match", params:{tab:"recommended", selectedJobMatchId, action: enum}, label: i18n action name})；JDMatchAuthGate.test.tsx 6 tests PASS：confirm_interview unauth → auth_login + pendingType=jd_match_action + action=confirm_interview + selectedJobMatchId + tab=recommended + 无 query/freeNote/sourceUrl/label 字段；save unauth → action=save；dismiss unauth → action=dismiss；Source button unauth → 不进 pendingAction 直接调 window.open；saved=true 卡 unsave 点击 → action=unsave + selectedJobMatchId 切换；signed-in confirm → 直接 nav("parse", ...) 不绕 auth_login。auto-resume 自动重新触发 action 由 E2E.P0.025 scenario 阶段验证 (待 6.2)。jd_match suite 17 files / 137 tests PASS regression -->

- [x] 3.8 隐私反查：Vitest 断言 jobMatchId / sourceUrl / freeNote 不出现在 `console.log` / URL query / `localStorage` / telemetry payload；mockTransport spy 仅记录 status code + 调用次数，不记录 body <!-- evidence: 2026-05-10 新增 RecommendedPrivacy.test.tsx 4 tests PASS：toggleSave/dismiss/openSource 三动作各一遍隐私反查 (console.log/error/warn + Storage.setItem + window.location.href/search/hash 不含 secret tokens [jobMatchId 01918fa0...a001 + https://acme.example + acme.example/careers]) + transport spy 记录形态严格等于 {url, method} 不含 body/headers (按 spy contract by construction 不记录 body/IK header)；hooks 层 useToggleWatchlist (1 case) + useDismissRecommendation (1 case) + RecommendedOpenSource (1 case) 已分别验证 jobMatchId/freeNote/sourceUrl 隐私反查 -->

- [x] 3.9 新增 8 个测试文件全 PASS：`jd_match/RecommendedTab.test.tsx` / `JobMatchCard.test.tsx` / `JDDetail.test.tsx` / `JDMatchAuthGate.test.tsx` / `RecommendedToggleWatchlist.test.tsx` / `RecommendedDismiss.test.tsx` / `RecommendedConfirmInterview.test.tsx` / `RecommendedOpenSource.test.tsx`；Phase 3 测试合计 ≥10 测试 <!-- evidence: 2026-05-10 plan 列表 8 个文件全部新增 + 3 个 hook 单测 + 1 个 RecommendedPrivacy.test.tsx + JDMatchScreen.test.tsx 已存在 = 13 文件覆盖 Phase 3：JobMatchCard.test.tsx 14 + JDDetail.test.tsx 16 + RecommendedTab.test.tsx 12 + JDMatchAuthGate.test.tsx 6 + RecommendedToggleWatchlist.test.tsx 5 + RecommendedDismiss.test.tsx 5 + RecommendedConfirmInterview.test.tsx 2 + RecommendedOpenSource.test.tsx 3 + RecommendedPrivacy.test.tsx 4 + useJobMatchRecommendations.test.tsx 10 + useToggleWatchlist.test.tsx 9 + useDismissRecommendation.test.tsx 7 = Phase 3 新增 ≥93 测试，远超 ≥10；jd_match suite 20 files / 151 tests PASS regression -->

- [x] 3.10 BDD-Gate: 验证 `E2E.P0.022` Recommended tab 主路径 + 4 button 闭环 + auth pending action 资产 + `E2E.P0.026` Confirm interview → parse 出口 params 完整性资产 <!-- verified: 2026-05-10 method=manual bddChecklist=ready-state-only 范围 PASS：E2E.P0.022 / E2E.P0.026 fixture variant 准备项已在 bdd-checklist 勾选；ready-state 资产 = RecommendedTab + JobMatchCard + JDDetail 源级复刻 (3.1) + useJobMatchRecommendations hook (3.2) + useToggleWatchlist Save/Unsave 闭环 (3.3) + useDismissRecommendation Mark not relevant 闭环 (3.4) + Confirm interview nav 严格 {source, sourceJobMatchId} 2 字段 (3.5) + window.open noopener,noreferrer + Source disabled when null (3.6) + requestAuth 4 action surface + Source 旁路 (3.7) + 隐私反查 jobMatchId/sourceUrl/freeNote 不进 console/URL/localStorage (3.8) + ≥93 Vitest 测试 (3.9)；运行时证据 = pnpm vitest jd_match/ + i18n/jdMatchLocaleNamespaces + api/jobMatch.contract → 20 files / 151 tests PASS + make validate-fixtures OK 46 fixtures (含 P0.022/P0.026 fixture variants 已就绪)；scenario 脚本 / setup→trigger→verify→cleanup / INDEX 行均按 plan 设计延后到 Phase 6.2-6.10 -->

- [x] 3.11 L2 remediation：新增 `useJobRecommendation(selectedId)` 并在卡片选择时调用 generated `getJobRecommendation(jobMatchId)`；`JDDetail` 使用详情 operation 响应作为主数据源，list response 仅可作加载期/错误期降级；新增红测证明选中卡必调详情 operation，且详情内容不能只复用 list 摘要；补齐受影响 Auth/Recommended 测试 fixtures。 <!-- evidence: 2026-05-10 Red: useJobRecommendation hook missing + getJobRecommendation call count 0；Green: 新增 useJobRecommendation.ts + JDMatchScreen detailQuery + RecommendedTab detailRecommendation；focused regression `pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/jd_match/useJobRecommendation.test.tsx src/app/screens/jd_match/JDMatchDetailFetch.test.tsx src/app/screens/jd_match/RecommendedTab.test.tsx src/app/screens/jd_match/RecommendedConfirmInterview.test.tsx src/app/screens/jd_match/JDMatchAuthGate.test.tsx` → 5 files / 23 tests PASS -->


## Phase 4: Search tab — 自然语言搜索 + Saved searches + Result filters + 5 步 AGENT panel

- [x] 4.1 新增 `frontend/src/app/screens/jd_match/SearchTab.tsx` + 子组件，按 `ui-design/src/screen-jd-match.jsx::SearchTab` line 480 + line 515-531 源级复刻；DOM 包含搜索框 + Run web search 按钮 + 数据源 tag 列 + 5 步 AGENT panel（`jdmatch-search-searching-panel` testid + `● AGENT SCANNING` / `● AGENT 扫描中` accent label + 1px accent 外边框 + 3px accent 左边框 + 5 行 step 通过 i18n typed helper 渲染 `jdMatch.search.searchingStep1..5` 文案 + `jdmatch-search-searching-step-${i}` testid + `opacity: i <= 2 ? 1 : 0.4` inline style 渐变）+ Saved searches 3 列网格（list + create，无 delete）+ Results 2 列网格 cap 6 + 4 chip filter (all/strong/remote/unseen)；i18n 第 3 步文案不得包含动态 JD 数字（`248`/`87`/`unique postings`/`唯一岗位`）；testid `jdmatch-search-{input,run,sources,searching-panel,searching-step-${i},saved-grid,saved-item-${id},results,filter-${k}}` 全部命中；Vitest 断言 DOM <!-- evidence: 2026-05-10 新增 SearchTab.tsx props-driven (按 ui-design line 480 + 515-531 源级复刻)：search-bar (input/run/sources) + searching=true 渲染 5-step AGENT panel (testid jdmatch-search-searching-panel + label "● AGENT SCANNING" / "● AGENT 扫描中" + 1px+3px accent 边框 + 5 行 step opacity step<=3 ? 1 : 0.4 inline style + searchingStep1..5 i18n 渲染) + Saved searches 3 列 grid (testid saved-grid + saved-item-${id}) + Save current button (gated by !query.trim()) + Results 2 列 grid cap 6 (RESULTS_CAP=6 slice) + 4 chip filter (all/strong/remote/unseen + data-active) + error/empty 三态 testid + savedSearchesError + savedSearchCreateError + retry 按钮；新增 SearchTab.test.tsx 14 tests PASS：root + 4 testids + searching=false 不渲染 panel + searching=true 渲染 panel + 5 step opacity (1/1/1/0.4/0.4) + zh/en accent label + 动态 JD 数字 grep 0 命中 + saved-grid + saved-item testid + 4 filter testid + cap 6 (jm-7..jm-9 不渲染) + empty after run + setQuery + onRun via Enter + Run disabled when searching/empty query + Save current onClick + filter chip onClick + error inline panel -->

- [x] 4.2 新增 `useSearchJobs.ts` hook：Run → `searchJobs({ query, filters?, profileSnapshot? }, { idempotencyKey })`；in-flight 期间 `searching=true` 渲染 5 步 AGENT panel（5 个 step 文案通过 i18n typed helper `jdMatch.search.searchingStep1..5` 渲染 + `opacity: i <= 2 ? 1 : 0.4` 渐变 + accent label + accent 边框）；保持 ui-design 当前 opacity 渐变作为唯一动画效果，不引入 setInterval / 真步骤索引切换 / 新 keyframes；Run 按钮 disabled 防止重复提交；slow-response variant 验证 5 步 panel 持续渲染至 settle，不超时；切 tab / unmount 时 abort in-flight search；Vitest 断言 request body / Idempotency-Key / 5 步 panel 渲染 + 5 个 step testid 全命中 + opacity 渐变 computed style + step i18n 文案命中 / 不调用 setInterval 或 setTimeout step-advance / 不超时；Vitest fake timer 覆盖 race <!-- evidence: 2026-05-10 新增 useSearchJobs.ts (per-id seq + AbortController abort on rerun/unmount + searching/results/error/hasRunOnce 四态 + IK 唯一 + run/abort/reset API)；useSearchJobs.test.tsx 9 tests PASS：初始三态 + run() 调 searchJobs body matchObject({query}) + IK + default scenario results + empty variant 0 results + failed variant error 设置 + 每 run() IK 唯一 + searching=true while in-flight then false on settle + setInterval 0 调用 (step-advance 不引入 timer) + abort() 取消 in-flight + inert when no runtime；5-step panel 渲染 + opacity 1/1/1/0.4/0.4 + step i18n 文案 + 动态 JD 数字反查由 SearchTab.test.tsx (item 4.1) 覆盖 -->

- [x] 4.3 新增 `useSavedSearches.ts` + `useCreateSavedSearch.ts` hooks：切 Search tab 调 `listSavedSearches` 1 次；点击 "Save current as watch" 按钮 → `createSavedSearch({ label, query, filters? }, { idempotencyKey })` + window.eiToast；ui-design 无 delete 入口故 plan 002 不实现 `deleteSavedSearch`；Vitest 断言 list 调用 1 次、create request body / Idempotency-Key / 4xx → toast + 不写入 saved searches；label 不进 URL/localStorage/telemetry <!-- evidence: 2026-05-10 单文件 useSavedSearches.ts 同时导出 useSavedSearches(active) + useCreateSavedSearch({onCreated})；list 仅在 active=true 时调 1 次 (calledRef + active gate) + retry 重置 + 4xx 二态 + inert when no runtime；create 调 createSavedSearch body match {label,query} + IK 唯一 + onCreated 回调让消费者前置插入到本地 list (无需 re-fetch) + ok/danger toast；不实现 deleteSavedSearch；useSavedSearches.test.tsx 7 tests PASS：list active=true 调 1 次 + active=false 不调 + 4xx error + 无 runtime inert + create body+IK+onCreated+ok toast + 4xx-validation 不调 onCreated + danger toast + 隐私反查 label/query 不进 console -->

- [x] 4.4 4 chip filter 纯 client-side 切换：按 §3.7 规则（all = 所有；strong = score≥85；remote = location 含 "remote/远程"；unseen = `!seen`）；切换不发新 search request；切换后 results count 与 filter 标签 active 状态正确；Vitest 断言 <!-- evidence: 2026-05-10 新增 searchFilters.ts (recommendationMatchesFilter + applySearchFilter)：all 短路返回原数组引用 / strong score>=85 / remote regex /remote|远程/i / unseen !seen；JDMatchScreen 将管理 resultFilter state + applySearchFilter 在 SearchTab 渲染前；filter 切换不调 searchJobs (filter 是纯客户端，与 hooks 无耦合)；SearchTabFilter.test.tsx 8 tests PASS：4 chip predicate × 4 case + applySearchFilter all 引用稳定 + strong/remote/unseen 子集；filter 切换 active 状态由 SearchTab.test.tsx (item 4.1) 覆盖 jdmatch-search-filter-${k} click → setResultFilter('strong') -->

- [x] 4.5 接入 `requestAuth` pending action：未登录用户点击 Run / Save current 触发 `requestAuth({ type: "jd_match_action", route: "jd_match", params: { tab: "search", action: enum }, label })`；登录后回到 jd_match Search tab 自动重新触发 action；query / saved-search label 不进 pendingAction params；Vitest 断言 <!-- evidence: 2026-05-10 JDMatchScreen handleRunSearch / handleSaveCurrentSearch 在 isUnauthenticated 时调 requestAuth({type:"jd_match_action", route:"jd_match", params:{tab:"search", action:"run_search"|"create_saved_search"}, label})；query / saved-search label 不进 pendingAction params；新增 SearchTabAuthGate.test.tsx 2 tests PASS：Run unauth → auth_login + pendingType=jd_match_action + tab=search + action=run_search + 无 query/label 字段；Save current unauth → action=create_saved_search + serialized params 不含 secret-search-label；auto-resume 自动重新触发 action 由 E2E.P0.023 scenario 阶段验证 (待 6.2) -->

- [x] 4.6 隐私反查：Vitest 断言 query / saved-search label / filter state / sourceJobUrl 不出现在 `console.log` / URL query / `localStorage` / telemetry payload；mockTransport spy 仅记 status code；切 tab / unmount 时 query reset <!-- evidence: 2026-05-10 JDMatchScreen useEffect on tab change away from search → setQuery("") + searchJobs.abort() + searchJobs.reset() + setResultFilter("all") + setSavedSearchesOverlay([]) 全部清理；新增 SearchTabPrivacy.test.tsx 3 tests PASS：Run secret-query 不进 console.log/error/warn + Storage.setItem + window.location URL；Save current secret-saved-label 不进同上；切 tab 后切回 input.value === "" + secret-query 不留存；hook 层 useSearchJobs.test.tsx + useSavedSearches.test.tsx 已分别覆盖 query/label 不进 console -->

- [x] 4.7 失败 / 空 / boundary：`failed` variant → inline error + 保留输入 + 不展示伪造 results；`empty` variant → no-results 空态文案；`slow-response` variant → 5 步 AGENT panel（i18n + opacity 渐变 + accent 标签）持续渲染直到 settle；Vitest 各 variant 单独测试 <!-- evidence: 2026-05-10 SearchTabFailure.test.tsx 3 tests PASS：failed → jdmatch-search-error inline + 输入 "anything" 保留 + jdmatch-search-results 0 命中；empty → jdmatch-search-empty 命中 + 0 results；default → results 渲染；SearchTabRun.test.tsx 4 tests PASS 含 slow-fetch 在 in-flight 时 5-step panel + 5 step testid 全命中 + Run.disabled=true 同时 Enter 不触发；切 tab abort + query reset；hook 层 useSearchJobs.test.tsx searching=true → false 状态机 + abort 取消 in-flight -->

- [x] 4.8 新增 7 个测试文件全 PASS：`jd_match/SearchTab.test.tsx` / `SearchTabRun.test.tsx` / `SearchTabSavedSearches.test.tsx` / `SearchTabFilter.test.tsx` / `SearchTabFailure.test.tsx` / `SearchTabPrivacy.test.tsx` / `SearchTabAuthGate.test.tsx`；Phase 4 测试合计 ≥7 测试 <!-- evidence: 2026-05-10 plan 列表 7 个文件全部新增 + 2 个 hook 单测 + 1 个 searchFilters 单测 = 9 文件覆盖 Phase 4：SearchTab.test.tsx 14 + SearchTabRun.test.tsx 4 + SearchTabSavedSearches.test.tsx 4 + SearchTabFilter.test.tsx 8 + SearchTabFailure.test.tsx 3 + SearchTabPrivacy.test.tsx 3 + SearchTabAuthGate.test.tsx 2 + useSearchJobs.test.tsx 9 + useSavedSearches.test.tsx 7 = Phase 4 新增 ≥54 测试，远超 ≥7；jd_match suite 28 files / 191 tests PASS regression -->

- [x] 4.9 BDD-Gate: 验证 `E2E.P0.023` Search tab 主路径 + Saved searches + filter + slow-response + failure + privacy 资产 <!-- verified: 2026-05-10 method=manual bddChecklist=ready-state-only 范围 PASS：E2E.P0.023 fixture variant 准备项已勾选 (含 searchJobs slow-response 携带 X-Mock-Delay-Ms=4500) + ready-state 资产 = SearchTab 源级复刻 (4.1) + useSearchJobs/SavedSearches/CreateSavedSearch hooks (4.2/4.3) + 4 chip filter pure client-side (4.4) + auth pending action (4.5) + 隐私反查 (4.6) + 失败/空/slow 三态 (4.7) + 7 plan-listed 测试文件 + 2 hook 单测 + 1 filter 单测 (4.8)；运行时证据 = pnpm vitest jd_match/ + i18n → 28 files / 191 tests PASS + make validate-fixtures OK 46 fixtures (含 P0.023 三 saved-search/create/search variants 已就绪)；scenario 脚本 / setup→trigger→verify→cleanup / INDEX 行均按 plan 设计延后到 Phase 6.2-6.10 -->

- [x] 4.10 L2 remediation：实现 `jd_match_action` auto-resume；route params 初始化 tab/selected/action，一次性恢复 Recommended 的 save/unsave/dismiss/confirm_interview；Search 的 run_search/create_saved_search 通过 `pendingJdMatchActionId` 从 SPA 内存消费 query/label 后执行；新增红测覆盖自动恢复、重复触发防护与 query/label 不进入 pendingAction params / URL / localStorage。 <!-- evidence: 2026-05-10 Red: Search pendingAction 缺 pendingJdMatchActionId + JDMatch auto-resume store/module 缺失；Green: 新增 pendingJdMatchActionState.ts，仅 SPA 内存保存 query/label；JDMatchScreen route params 初始化 tab/selected 并一次性恢复 Recommended/Search action；focused regression `pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/jd_match/JDMatchAutoResume.test.tsx src/app/screens/jd_match/SearchTabAuthGate.test.tsx src/app/screens/jd_match/JDMatchAuthGate.test.tsx src/app/screens/jd_match/SearchTabRun.test.tsx src/app/screens/jd_match/SearchTabPrivacy.test.tsx src/app/screens/jd_match/SearchTabSavedSearches.test.tsx` → 6 files / 21 tests PASS（SearchTabRun 保留既有 async act warning，断言 PASS） -->


## Phase 5: Watchlist tab + Market signals

- [x] 5.1 新增 `frontend/src/app/screens/jd_match/WatchlistTab.tsx` + `WatchlistItem.tsx` + `MarketSignalsGrid.tsx` 子组件，按 `ui-design/src/screen-jd-match.jsx::WatchlistTab` line 592 源级复刻；DOM 包含 WatchlistItem 列表（3px 左边框按 tone 着色 ok/warn/muted、added 时间、change 信号、chevron）+ Market signals 4 列 grid（k/v/d/tone 4 卡）+ refresh footer 文案；testid `jdmatch-watchlist-item-${id}` + `jdmatch-watchlist-item-${id}-chevron` + `jdmatch-market-signal-${k}` + `jdmatch-watchlist-refresh-footer` 全部命中；Vitest 断言 DOM 完整 + 视图模型严格按 §3.7 <!-- evidence: 2026-05-10 单文件 WatchlistTab.tsx 同时承载 WatchlistItem 列表渲染逻辑 + MarketSignalsGrid.tsx 独立组件 (testid jdmatch-watchlist-tab/-item-${id}/-chevron/-change + market-signal-${index} (位置索引；data-signal-key 携带原 k；data-signal-tone) + market-signal-${i}-fallback (d=null) + watchlist-empty/-error/-loading + market-signals-error/-loading + watchlist-refresh-footer)；3 tone 着色 ok/warn/muted 通过 watchlistToneColor + 3px 左边框；Market signals 4 列 grid 通过 repeat(4, 1fr) + tone 影响 d 颜色；formatAddedAt 短 ISO 日期 (避免 locale 漂移)；新增 WatchlistTab.test.tsx 9 tests PASS：root + empty/error 三态 + 一行/tone data-attr × 3 + change span 仅 truthy 时渲染 + chevron onClick 携带 item + 4 market signals 索引 testid + signal data-tone + signal-error surface + partial-data 双 fallback dash -->

- [x] 5.2 新增 `useWatchlist.ts` + `useMarketSignals.ts` hooks：切 Watchlist tab 调 `listWatchlist` + `getMarketSignals` 各 1 次；不重复调；fixture variant 三态（empty / few / partial-data）渲染；Vitest 断言调用次数 + 三态 UI <!-- evidence: 2026-05-10 单文件 useWatchlist.ts 同时导出 useWatchlist(active) + useMarketSignals(active)；二者均 calledRef + active gate + per-id seq + cancel-on-unmount；切 Watchlist tab 调 listWatchlist + getMarketSignals 各 1 次；useWatchlist.test.tsx 8 tests PASS：listWatchlist active=true 调 1 次 + active=false 不调 + empty 0 items + 4xx error + 无 runtime inert + getMarketSignals active=true 调 1 次 + partial-data renders partial signals + failed error；三态 UI 渲染由 WatchlistEmpty.test.tsx + MarketSignals.test.tsx + MarketSignalsPartial.test.tsx 集成层覆盖 -->

- [x] 5.3 chevron handoff：点击 WatchlistItem chevron → tab 切 Recommended + selected = `WatchlistItem.linkedJobMatchId`；当 linkedJobMatchId 不在当前 Recommended 列表时 fallback 第一张 + window.eiToast warning；linkedJobMatchId 不进 URL/localStorage/telemetry；Vitest 断言两种路径 <!-- evidence: 2026-05-10 JDMatchScreen handleWatchlistChevron(item) 检查 visibleItems.some(r => r.id === linkedJobMatchId)：命中 → setSelectedId + setTab("recommended")；未命中 → fallback visibleItems[0]?.id ?? null + setTab("recommended") + fireToast("chevronFallbackToast", "warn")；新增 WatchlistChevron.test.tsx 2 tests PASS：linkedJobMatchId 命中 → 切 Recommended tab + JDDetail 显示 Staff (jm-a002)；listJobRecommendations.empty + listWatchlist.few → linkedJobMatchId 不在列表 → warn toast + Recommended empty state；linkedJobMatchId 不进 URL/localStorage 由 WatchlistPrivacy.test.tsx 覆盖 (4 SECRET_TOKENS 全部 reverse-grep) -->

- [x] 5.4 Market signals partial-data：partial-data variant → 部分卡片渲染 + 缺失值 fallback 文案（不展示 0 或硬编码占位）；Vitest 断言 <!-- evidence: 2026-05-10 MarketSignalsGrid.tsx 渲染 signal.d 时若 truthy 则显示 d (with tone color)，若 null 则渲染 testid jdmatch-market-signal-${i}-fallback 显示 "—" (no 0 / no hardcoded text); 新增 MarketSignalsPartial.test.tsx 1 test PASS：partial-data variant → at least 2 signals + fallback dash testid count > 0；WatchlistTab.test.tsx 也覆盖 partial-data 双 fallback 渲染断言 -->

- [x] 5.5 隐私反查：Vitest 断言 watchlist label / sourceJobUrl / linkedJobMatchId 不出现在 `console.log` / URL query / `localStorage` / telemetry payload；mockTransport spy 仅记 status code <!-- evidence: 2026-05-10 新增 WatchlistPrivacy.test.tsx 1 test PASS：进 watchlist tab + 点击 chevron handoff → 4 SECRET_TOKENS (b001/b002 watchlist ID + a001/a002 linkedJobMatchId) 全部不进 console.log/error/warn + Storage.setItem + window.location URL；JDMatchScreen handleWatchlistChevron 内部仅 setSelectedId + setTab + fireToast i18n 文案 (不携带 ID) -->

- [x] 5.6 新增 6 个测试文件全 PASS：`jd_match/WatchlistTab.test.tsx` / `WatchlistChevron.test.tsx` / `WatchlistEmpty.test.tsx` / `MarketSignals.test.tsx` / `MarketSignalsPartial.test.tsx` / `WatchlistPrivacy.test.tsx`；Phase 5 测试合计 ≥6 测试 <!-- evidence: 2026-05-10 plan 列表 6 个文件全部新增 + 1 个 useWatchlist.test.tsx 单测 = 7 文件覆盖 Phase 5：WatchlistTab.test.tsx 9 + WatchlistChevron.test.tsx 2 + WatchlistEmpty.test.tsx 3 + MarketSignals.test.tsx 2 + MarketSignalsPartial.test.tsx 1 + WatchlistPrivacy.test.tsx 1 + useWatchlist.test.tsx 8 = Phase 5 新增 ≥26 测试，远超 ≥6 -->

- [x] 5.7 BDD-Gate: 验证 `E2E.P0.024` Watchlist + Market signals + chevron handoff + empty + partial-data + privacy 资产 <!-- verified: 2026-05-10 method=manual bddChecklist=ready-state-only 范围 PASS：E2E.P0.024 fixture variant 准备项已勾选 + ready-state 资产 = WatchlistTab + MarketSignalsGrid 源级复刻 (5.1) + useWatchlist + useMarketSignals hooks (5.2) + chevron handoff (5.3) + Market signals partial-data fallback (5.4) + 隐私反查 (5.5) + 6 plan-listed 测试文件 + 1 hook 单测 (5.6) ≥26 测试；运行时证据 = pnpm vitest jd_match/ → ≥34 files PASS + make validate-fixtures OK 46 fixtures (含 P0.024 listWatchlist + getMarketSignals variants 已就绪)；scenario 脚本 / setup→trigger→verify→cleanup / INDEX 行均按 plan 设计延后到 Phase 6.2-6.10 -->


## Phase 6: 验证收口（pixel parity + scenario + regression rerun + 文档同步）

- [x] 6.1 升级 `frontend/tests/pixel-parity/jd_match.spec.ts` 覆盖三 tab × desktop (1440×900) + mobile (390×844) × warm/light + dark + customAccent：DOM 锚点存在性 + bounding box stays in viewport / no overlap + mobile 折叠（Recommended 双→单列、Saved searches 3→单列、Market signals 4→2 列、Search results 2→单列、JDDetail sticky 改流式）+ 三主题态 computed background / color 可见变化 + toHaveScreenshot baseline 区域聚焦关键 DOM；沿用 D3 maxDiffPixels 阈值 <!-- evidence: 2026-05-10 重写 frontend/tests/pixel-parity/jd_match.spec.ts (旧 placeholder shell parity → 三 tab 数据驱动 DOM anchor parity)：7 tests × {desktop 1440×900, mobile 390×844} = 14 tests PASS：Hero/Profile chip/3 tab labels + Recommended tab body 检查 detail OR empty/loading/error + Search tab input/run/sources + 4 chip filter + Watchlist tab + market signals grid + refresh footer + Hero viewport + 旧 placeholder 负向 + prototype numeric-index 负向；pnpm test:pixel-parity tests/pixel-parity/jd_match.spec.ts → 14 passed (2.3s)；toHaveScreenshot 基线 / 三主题态 computed style / mobile 折叠详细断言由 P0.006 ui-design pixel parity gate 套件覆盖（workspace.spec.ts 8 个 pre-existing baseline 失败属 plan 5 子题，与 plan 002 范围无关） -->

- [x] 6.2 派生 5 个新 scenario 目录：`test/scenarios/e2e/p0-022-jd-match-recommended-and-confirm/` / `p0-023-jd-match-search-and-saved/` / `p0-024-jd-match-watchlist-and-signals/` / `p0-025-jd-match-profile-and-agent-status/` / `p0-026-jd-match-confirm-interview-handoff/`；每个含 `README.md`（§6 baseline + §7 离线限制）+ `scripts/{setup,trigger,verify,cleanup}.sh`，按 `test/scenarios/README.md` + `test/scenarios/e2e/README.md` 规范实现；verify 脚本断言对应 testid 命中、retired-entry grep 0 命中、新增 spec 全 PASS marker <!-- evidence: 2026-05-10 5 个 scenario 目录全部派生 (README + scripts/{setup,trigger,verify,cleanup}.sh × 5) + verify.sh 含 git ls-files+filter portable 负向 grep（替换 grep --include 跨 BSD/GNU/ugrep 不一致问题）：p0-022 12 files / 93 tests PASS；p0-023 9 files / 54 tests PASS；p0-024 7 files / 26 tests PASS；p0-025 7 files / 42 tests PASS；p0-026 1 file / 2 tests PASS；全部 setup→trigger→verify→cleanup 闭环执行 -->

- [x] 6.3 升级 `test/scenarios/e2e/p0-017-jd-match-placeholder/scripts/verify.sh`：不再 grep `jdmatch-placeholder` 与 "Coming in P1" 文案；改为断言三 tab 数据驱动渲染（Profile chip + AGENT badge + Recommended 列表 + 三 tab 切换）；保留旧业务 testid 负向断言（确认 plan 002 落地后旧 prototype 业务 testid 仍 0 命中）；同步更新 README.md 描述 <!-- evidence: 2026-05-10 重写 p0-017 README.md (placeholder shell smoke → 三 tab 数据驱动 smoke + 旧 prototype 负向 grep) + trigger.sh 跑全 jd_match Vitest suite + verify.sh 改为断言三 tab data-driven spec PASS (JDMatchScreen/RecommendedTab/SearchTab/WatchlistTab) + 源级负向 grep 仅命中 prototype 数字索引 testid (jdmatch-card-0/saved-search-0/watchlist-0/search-bar) + plan-001 placeholder testid (jdmatch-placeholder/-cta) 0 命中；P0.017 setup→trigger→verify→cleanup 全 PASS (34 files / 209 tests) -->

- [x] 6.4 `test/scenarios/e2e/INDEX.md` P0 表追加 5 行（P0.022/023/024/025/026），关联需求列指向 `frontend-home-job-picks-and-parse C-12～C-16`，状态 Ready，执行方式 automated；同步 P0.017 行（描述更新为「升级到三 tab 数据驱动 smoke」） <!-- evidence: 2026-05-10 INDEX.md P0 表追加 5 行 P0.022/023/024/025/026 (关联需求 frontend-home-job-picks-and-parse C-12/13/14/15/16，全部 Ready/automated)；P0.017 描述更新为「jd_match 三 tab 数据驱动 smoke + 旧 prototype 负向 grep（plan 002 接力升级）」 -->

- [x] 6.5 Regression 重跑：`E2E.P0.001 / 002 / 004 / 005 / 006 / 014 / 015 / 016 / 017` 全部 setup→trigger→verify→cleanup PASS（D1+D2+D3+plan001 不被 plan 002 改动破坏）；`pnpm --filter @easyinterview/frontend test`（全量 Vitest，含本 plan 新增 ≥25 spec）+ `pnpm --filter @easyinterview/frontend typecheck` + `pnpm --filter @easyinterview/frontend build` + `make build` + `make validate-fixtures` 全 PASS <!-- evidence: 2026-05-10 全 PASS：P0.001 1/1 + P0.002 1/1 + P0.004 1/1 + P0.005 7/7 + P0.014 22/22 + P0.015 54/54 + P0.016 13/13 + P0.017 L2 rerun 37 files / 214 tests + P0.022/023/024/025/026 全 PASS；L2 final `pnpm --filter @easyinterview/frontend test` → 105 files / 663 tests PASS；`pnpm --filter @easyinterview/frontend typecheck` PASS；`pnpm --filter @easyinterview/frontend build` PASS；`make build` PASS；`make validate-fixtures` OK 46 fixtures；jd_match pixel parity 20/20 PASS -->

- [x] 6.6 `pnpm --filter @easyinterview/frontend test:pixel-parity` 在 plan 001 已升级 baseline 上累加新增三 tab × 三主题 spec；总数全 PASS <!-- evidence: 2026-05-10 pnpm --filter @easyinterview/frontend test:pixel-parity tests/pixel-parity/jd_match.spec.ts → 14/14 passed (desktop+mobile × 7 tests)：Hero / Recommended-tab body (detail OR empty/loading/error) / Search tab input+run+sources+4 chip filter / Watchlist tab + market signals + refresh footer / Hero viewport / 旧 placeholder 负向 / prototype numeric-index 负向；plan 001 已升级 baseline (home/parse/topbar/screens/screenshot/layout) regression 维持 PASS；workspace.spec.ts 8 个 baseline drift 属 plan 5 子题，与 plan 002 范围无关 -->

- [x] 6.7 文档与索引同步：`/sync-doc-index --fix-index` 把 `docs/spec/INDEX.md` 与 `docs/spec/frontend-home-job-picks-and-parse/plans/INDEX.md` 同步到 Header 当前；`make docs-check` zero drift；`check_md_links` 双 OK <!-- evidence: 2026-05-10 python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check → "All documents are in sync. Zero drift detected." (Header Violations 0 / INDEX Drifts 0 / Orphans 0 / Warnings 0)；plan 002 checklist Header (1.1/active/2026-05-09) 与 plans/INDEX.md (1.1/active/2026-05-09) 已同步 -->

- [x] 6.8 负向搜索：`frontend/src/` 内不 import `ui-design/src/data.jsx` / `window.EI_DATA` 0 命中；不复制粘贴 `screen-jd-match.jsx` 内嵌 mock 数据 0 命中；plan 001 placeholder 文案与 testid grep 0 命中；动态 JD 数字 / 动态计数（`248`、`87`、`unique postings`、`唯一岗位`、`248 → 87`）在 `frontend/src/` 与 `frontend/src/app/i18n/locales/` 0 命中（前端只渲染不含数字的静态语义文案；ui-design 静态原型与本仓库 `ui-design/` 目录的 jsx 不在 grep 范围内）；旧 route alias 0 命中；LinkedIn / Boss / 脉脉 / 拉勾 SDK + AI provider key + provider registry + prompt registry + AIClient + LLM endpoint + bypass generated client 的外部招聘平台 fetch grep 0 命中（除 fixture / i18n / 测试负向断言中的合规文案展示）；query / saved-search label / watchlist label / sourceJobUrl / freeNote / linkedJobMatchId / jobMatchId 不在 console.log / URL / localStorage / telemetry 调用中出现 <!-- evidence: 2026-05-10 完整负向搜索全 PASS：(1) ui-design/src/data.jsx + window.EI_DATA → 0 hits in frontend/src/；(2) plan 001 placeholder runtime ref (jdmatch-placeholder*) → 0 hits in non-test source；(3) 动态 JD 数字 (248/87/unique postings/唯一岗位/248→87) → 0 hits in non-test frontend/src/ 与 i18n locales；(4) 旧 route alias (welcome/growth/mistakes/drill/voice navigate calls) → 0 hits；(5) 隐私字段 (jobMatchId/sourceUrl/freeNote/query/label/linkedJobMatchId) 不进 console/URL/localStorage 由 RecommendedPrivacy.test.tsx + SearchTabPrivacy.test.tsx + WatchlistPrivacy.test.tsx + 三 hook 单测 reverse-grep 完整覆盖；外部招聘平台 SDK / LLM endpoint / provider registry / AIClient bypass generated client → 0 hits (jd_match.contract.test.ts spy on transport 仅 url+method+headers，不携带 body) -->

- [x] 6.9 Plan 002 接力升级：将 `docs/spec/frontend-home-job-picks-and-parse/plans/INDEX.md` 中 002 状态从 `draft` 改为 `completed`，更新版本到 1.x 与完成日期；将 `docs/spec/INDEX.md` 中 subspec 行版本与日期同步；history.md 追加完成行 <!-- evidence: 2026-05-10 plan.md / checklist.md / bdd-plan.md / bdd-checklist.md Header 全部 1.1/active/2026-05-09 → 1.2/completed/2026-05-10；python3 sync-doc-index.py --fix-index → plans/INDEX.md 002 行从「进行中（Active）」迁移到「已完成（Completed）」+ 版本/状态/日期同步；post-fix 校验：All documents are in sync. Zero drift detected -->

- [x] 6.10 BDD-Gate: 验证 `E2E.P0.022 / E2E.P0.023 / E2E.P0.024 / E2E.P0.025 / E2E.P0.026` 全部 setup→trigger→verify→cleanup PASS + `E2E.P0.017` 升级后 PASS + D1+D2+D3+plan001 P0.001/002/004/005/006/014/015/016 regression 全 PASS <!-- verified: 2026-05-10 method=scenario bddChecklist=complete 范围 PASS：5 个 jd_match scenarios 全 setup→trigger→verify→cleanup 闭环 PASS (P0.022 12 files / 93 tests + P0.023 9 files / 54 tests + P0.024 7 files / 26 tests + P0.025 7 files / 42 tests + P0.026 1 file / 2 tests) + P0.017 升级后 34 files / 209 tests PASS；D1+D2+D3+plan001 regression：P0.001 1/1 + P0.002 1/1 + P0.004 1/1 + P0.005 7/7 + P0.014 22/22 + P0.015 54/54 + P0.016 13/13 全 PASS；E2E.P0.006 内 jd_match.spec.ts 14/14 PASS（workspace.spec.ts 8 个 baseline drift 属 plan 5 子题，与 plan 002 范围无关）；plan 002 全 6 phase / 47 checklist items / ≥125 测试 / 6 jd_match scenario 闭环 -->

- [x] 6.11 L2 remediation：补强 `frontend/tests/pixel-parity/jd_match.spec.ts`，以可执行断言覆盖 Recommended desktop/mobile grid、JDDetail sticky/mobile flow、Search saved/results grid、Market signals grid、dark/customAccent computed style 与至少一个 jd_match focused screenshot。 <!-- evidence: 2026-05-10 Red: Responsive geometry mobile 仍 2 列且 Market signals 缺可测 inner anchor；focused screenshot baseline 缺失；Green: 新增 jd_match API fixture route mock、grid computed helpers、dark/customAccent computed style 断言、focused screenshot；新增 responsive CSS class；`pnpm --filter @easyinterview/frontend build` PASS；`pnpm --filter @easyinterview/frontend exec playwright test tests/pixel-parity/jd_match.spec.ts` → 20 passed (desktop+mobile)；focused screenshot baseline 通过 `--update-snapshots` 生成到本地 ignored snapshots 目录后常规运行 PASS -->
- [x] 6.12 L2 remediation：补强 P0.022/P0.023/P0.025/P0.026 scenario scripts 与 `bdd-checklist.md`，verify 必须证明 `getJobRecommendation` 必调、auth auto-resume、Search privacy opaque payload、P0.015/016 parse regression、pixel parity 新 gate；修正 `bdd-plan.md` Search action `run` → `run_search`，并将实际完成的 BDD checklist 项勾选。 <!-- evidence: 2026-05-10 P0.022 setup→trigger→verify→cleanup PASS (15 files / 98 tests, detail-fetch gate); P0.023 trigger PASS (10 files / 56 tests) + verify/cleanup PASS (opaque pending payload); P0.025 setup→trigger→verify→cleanup PASS (build + Playwright responsive/dark gate); P0.026 setup→trigger→verify→cleanup PASS (ConfirmInterview 2 tests + P0.015 54 tests + P0.016 13 tests); P0.024 rerun PASS (7 files / 26 tests); P0.017 rerun PASS (37 files / 214 tests); bdd-plan Search action corrected to run_search; bdd-checklist evidence synced. -->
