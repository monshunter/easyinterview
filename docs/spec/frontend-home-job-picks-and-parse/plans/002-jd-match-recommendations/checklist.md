# 002 JD Match Recommendations Checklist

> **版本**: 1.1
> **状态**: active
> **更新日期**: 2026-05-09

**关联计划**: [plan](./plan.md)

## Phase 0: UI truth-source + B2 owner contract preflight

- [x] 0.1 读取最新 `ui-design/src/screen-jd-match.jsx` 与 `docs/ui-design/`，确认 `SearchTab` `searching=true` 区域当前形态为 5 步 AGENT panel + accent label + 1px accent 外边框 + 3px accent 左边框 + 5 行 step 文案 + `opacity: i <= 2 ? 1 : 0.4` 渐变；plan 002 不修改 ui-design 静态文件，前端按此形态源级复刻并通过 i18n 5 step key（典型 `jdMatch.search.searchingStep1..5`）提供 zh/en 文案；ui-design 第 3 步动态 JD 数字（`248`/`87`/`unique postings`/`唯一岗位`）在前端 i18n 替换为不含具体数字的静态语义文案 — evidence: plan.md §7.1
- [x] 0.2 复核 B2 owner truth source 与 validators：`docs/spec/openapi-v1-contract/spec.md`、`openapi/README.md`、`openapi/fixtures/README.md`、`scripts/lint/openapi_inventory.py`、`scripts/lint/validate_fixtures.py`、`docs/spec/mock-contract-suite/spec.md`、`docs/spec/engineering-roadmap/spec.md` 中 12 tag / 34 endpoint / 34-operation coverage 口径均列入 Phase 1 同步范围 — evidence: plan.md §7.2（7 文件 / 多处硬编码 count gate 已锁定）
- [x] 0.3 冻结 12 operation matrix、5 个 `Idempotency-Key` side-effect operation、`getJobRecommendation` 必调详情契约、pendingAction action enum（`save` / `unsave` / `dismiss` / `confirm_interview` / `run_search` / `create_saved_search`）与隐私字段负向清单；确认 Source button 与 Watchlist chevron 不进入 auth pending action — evidence: plan.md §7.3 / §7.4 / §7.5
- [x] 0.4 Preflight Gate: Phase 0 evidence log 记录 UI truth-source 当前形态（5 步 AGENT panel 保留 + i18n 5 step key 列表 + 动态 JD 数字静态化策略）、B2 owner inventory 待同步清单、12 operation matrix freeze、auth action enum freeze 与隐私字段负向清单 — evidence: plan.md §7.6（feature branch + 修订版本 + 5 项 freeze 全部就位）

## Phase 1: JobMatch OpenAPI 契约 + fixture + codegen

- [x] 1.1 在 `openapi/openapi.yaml` 新增 `JobMatch` tag 与 12 operationId（`getJobMatchProfile` / `getAgentScanStatus` / `listJobRecommendations` / `getJobRecommendation` / `addToWatchlist` / `removeFromWatchlist` / `markJobNotRelevant` / `searchJobs` / `listSavedSearches` / `createSavedSearch` / `listWatchlist` / `getMarketSignals`）path / method / requestBody / responses；schemas 含 `JobMatchProfile` / `AgentScanStatus` / `JobMatchRecommendation` / `WatchlistItem` / `SavedSearch` / `MarketSignal` / `MarkNotRelevantRequest` / `SearchJobsRequest` / `SearchJobsResponse` / `AddToWatchlistRequest` / `CreateSavedSearchRequest` / `MarketSignalsResponse`；AI-generated 字段必须引用 `GenerationProvenance`；side-effect 5 操作支持 `Idempotency-Key` parameter；同步 B2 owner inventory / README / validator / mock-contract coverage 到 13 tag / 46 endpoint（contract test 断言 schema 完整与 tag=`JobMatch`） <!-- evidence: 2026-05-10 inventory check 13 tags/46 ops PASS; swagger-cli validate PASS; B2 spec D-17 + 7 owner files synced -->

- [x] 1.2 新增 `openapi/fixtures/JobMatch/` 目录与 12 个 operation fixture 文件，按 plan §3.6 列出的 scenarios 配置（至少 default + 一个 edge variant，含 empty / failed / slow-response 等关键 variant）；通过 `make validate-fixtures` <!-- evidence: 2026-05-10 make validate-fixtures OK 46 fixtures; per §3.6 scenarios: getJobMatchProfile{default,unauthenticated,partial-profile} / getAgentScanStatus{default,idle,scanning,error,next-scan-soon} / listJobRecommendations{default,empty,one,many,failed} / getJobRecommendation{default,network-intel-empty,failed} / addToWatchlist{default,4xx-validation,5xx-server-error} / removeFromWatchlist{default,4xx-not-found} / markJobNotRelevant{default,4xx} / searchJobs{default,empty,failed,slow-response} / listSavedSearches{default,empty,4xx} / createSavedSearch{default,4xx-validation} / listWatchlist{default,empty,few,4xx} / getMarketSignals{default,partial-data,failed}; openapi_inventory.py PASS (13 tags / 46 operations) -->
- [x] 1.3 升级 `mock-contract-suite`（如需要）支持 per-operation per-scenario 切换组合；保留 plan 001 fixture variant 切换向后兼容；spec / plan 同步更新 <!-- evidence: 2026-05-10 mockTransport.ts 已通过 Prefer: example=<scenario> per-request header 原生支持 per-operation per-scenario switching；App.tsx requestOptions per-operationId 通道（如 getMe / runtimeConfig / listJobRecommendations）即用即配；mock-contract-suite spec v1.3 §2.1 / §6 C-1 已 additive 升级到 13 tag / 46 operation 含 JobMatch 12 op；无需新增组件升级，plan 001 fixture variant 切换向后兼容保留 -->
- [x] 1.4 运行 OpenAPI codegen 重建 `frontend/src/api/generated/client.ts` 与 schema typings；`pnpm --filter @easyinterview/frontend typecheck` PASS；codegen drift check 0 <!-- evidence: 2026-05-10 make codegen 生成 12 个 JobMatch 方法 (getJobMatchProfile/getAgentScanStatus/listJobRecommendations/getJobRecommendation/markJobNotRelevant/listWatchlist/addToWatchlist/removeFromWatchlist/searchJobs/listSavedSearches/createSavedSearch/getMarketSignals) + 完整 schema typings；pnpm --filter @easyinterview/frontend typecheck PASS (tsc --noEmit 0 errors)；二次 make codegen 不再产生新 diff (drift check = 0)；backend lint-events 失败属 baseline 预存问题，与本 plan 无关 -->
- [x] 1.5 新增 `frontend/src/api/generated/jobMatch.contract.test.ts`（或同等位置）：测 generated client 12 method 可调用、request body / response schema 与 fixture variant parity、Idempotency-Key parameter 出现在 5 个 side-effect operation；contract test 全 PASS <!-- evidence: 2026-05-10 frontend/src/api/jobMatch.contract.test.ts (next to mockTransport.test.ts; "或同等位置")；vitest run src/api/jobMatch.contract.test.ts → 1 file / 14 tests PASS：12 method 可调用 + getJobMatchProfile/getAgentScanStatus/listJobRecommendations/getJobRecommendation/listWatchlist/getMarketSignals fixture variant parity + addToWatchlist/removeFromWatchlist/markJobNotRelevant/searchJobs/createSavedSearch Idempotency-Key header 验证 + 5 个 side-effect fixture default scenario `request.headers.Idempotency-Key` 命中负向反查 -->
- [x] 1.6 BDD-Gate: 验证 `E2E.P0.022 / E2E.P0.023 / E2E.P0.024 / E2E.P0.025 / E2E.P0.026` 资产构建到 ready 态前置（fixture 存在性 + generated client 可调用），具体 scenario 验证延后到 Phase 6 <!-- verified: 2026-05-10 method=manual bddChecklist=fixture-prep-only 范围 PASS：5 个场景 fixture variant 准备项已在 bdd-checklist 勾选；fixture 存在性 = make validate-fixtures OK 46 fixtures 含 12 JobMatch；generated client 可调用 = pnpm exec vitest run src/api/jobMatch.contract.test.ts 1/1 file × 14/14 tests PASS（12 method + Idempotency-Key × 5 ops + multi-variant parity）；scenario 脚本 / setup→trigger→verify→cleanup / INDEX 行均按 plan 设计延后到 Phase 6.2-6.5 -->

## Phase 2: JDMatchScreen 容器升级 + Hero / Profile chip / AGENT badge 数据驱动

- [ ] 2.1 删除 `frontend/src/app/screens/jd_match/JDMatchScreen.tsx` 中所有 plan 001 placeholder testid（`jdmatch-placeholder` / `jdmatch-placeholder-cta`）与 fallback 文案；删除 `jdMatch.placeholderTitle` / `placeholderCopy` / `placeholderCta` 三个 i18n key（zh + en）；Vitest typed helper test 断言 key 删除后无任何 runtime 引用
- [ ] 2.2 新增 `frontend/src/app/screens/jd_match/useJobMatchProfile.ts` 与 `useAgentScanStatus.ts` hooks，通过 D1 generated client 调 `getJobMatchProfile` / `getAgentScanStatus`；React state loading / data / error 三态；Vitest 断言进入 jd_match 各调 1 次；切回 Recommended tab 时 `getAgentScanStatus` 再调 1 次（不重复调 profile）；切其他 tab 时不调；不引入 setInterval / SSE / WebSocket（D-10）
- [ ] 2.3 重构 `JDMatchScreen.tsx` 按 `ui-design/src/screen-jd-match.jsx::JDMatchScreen` line 4-170 源级复刻 Hero（label / title / sub）+ Profile snapshot chip（avatar / SEARCHING AS / skills tags / PROFILE SOURCES）+ AGENT ACTIVE badge（status enum / lastScanAt / nextScanAt 文案）+ 三 tab 标签（带数量 badge）；Profile chip 字段映射严格按 §3.7；AGENT badge tone 与文案按 §3.7；testid `jdmatch-hero-{label,title,sub}` / `jdmatch-profile-chip-{avatar,searching-as,skills,sources}` / `jdmatch-agent-status-{badge,last-scan,next-scan}` / `jdmatch-tab-{recommended,search,watchlist}` 全部命中；Vitest 断言
- [ ] 2.4 扩展 `frontend/src/app/i18n/locales/zh.ts` 与 `en.ts`，删除 plan 001 三个 placeholder key 后新增 5 子命名空间：`jdMatch.profile.*`（≥8 key 覆盖 searching-as / skills heading / sources count 模板等）/ `jdMatch.agent.*`（≥6 key 覆盖 idle/scanning/error 状态文案 + last/next scan 文案模板）/ `jdMatch.recommended.*`（≥10 key 覆盖 STRONG/GOOD/STRETCH 标签 / Top reason 标签 / fit footer / next scan footer 等）/ `jdMatch.search.*`（≥16 key 含：5 步 AGENT panel `searchingStep1..5` 文案、AGENT label `searchingPanelLabel`、Run/Run-disabled 按钮文案、Saved searches 区文案、filter 4 标签、datasource tags、empty/error 文案；第 3 步文案不得包含 `248` / `87` / `unique postings` / `唯一岗位` 任意硬编码数字，必须为不含数字的静态语义文案如 `→ Dedup by JD hash · removing duplicate postings` / `→ 按 JD 哈希去重 · 移除重复岗位`）/ `jdMatch.watchlist.*`（≥10 key 覆盖 list heading / 3 tone change 文案 + Market signals 4 卡 k 文案 + refresh footer）；保留 plan 001 已有的 `jdMatch.hero.*` / `jdMatch.tab.*`；`messages.ts` typed helper 同步；Vitest `i18n` 套件断言 zh/en 同步无缺漏 + 5 step key 完整 + 动态 JD 数字 grep 0 命中
- [ ] 2.5 新增 `jd_match/JDMatchScreen.test.tsx`（≥10 测试）：测 Hero / Profile chip / AGENT badge / 三 tab 标签 testid 全部命中；测 `getJobMatchProfile` / `getAgentScanStatus` 调用次数与 fixture variant 三态渲染（idle / scanning / error；default / unauthenticated / partial-profile）；测 i18n zh/en 切换；负向断言 plan 001 placeholder testid（`jdmatch-placeholder*`）DOM 0 命中；测 `topbar-nav-jd_match` 高亮；测不调用 setInterval / SSE
- [ ] 2.6 BDD-Gate: 验证 `E2E.P0.022` Hero/Profile/AGENT 阶段 + `E2E.P0.025` Profile/AGENT/auth 综合场景资产构建到 ready 态

## Phase 3: Recommended tab — JobMatchCard + JDDetail + Save / Mark not relevant / Confirm interview

- [ ] 3.1 新增 `frontend/src/app/screens/jd_match/RecommendedTab.tsx` + `JobMatchCard.tsx` + `JDDetail.tsx` 三个组件，按 `ui-design/src/screen-jd-match.jsx::JobMatchCard` line 335 + `JDDetail` line 377 源级复刻；JobMatchCard：左 3px 边框按 score 三档着色 / 标题 / 未看过点 / saved pin / company-companyTag / location-comp / 右上 score 32px + STRONG/GOOD/STRETCH label / Top reason / 底部 must X/Y · plus X/Y · posted · source；JDDetail：Header (company tag / title / level/location/comp tag / right 36px score) + + WHY MATCHES (绿 check) + ⚠ WHERE STRETCH (橙 info) + ROLE SNAPSHOT + INTEL（条件渲染 networkNote && similarInterviewers，合规措辞「公开面经 / JD / 公司资料信号」与「public interview-review, JD, and company-source signals」严格按 ui-design-contract.test.mjs 锁定）+ Action bar 4 button (Confirm interview / Save toggle / Source / Not relevant)；testid 命名按 plan §3.5；Vitest 断言 DOM 完整 + 视图模型严格按 §3.7
- [ ] 3.2 新增 `useJobMatchRecommendations.ts` hook，通过 generated `listJobRecommendations` 调；状态 loading / data / error；支持 cursor pagination（如 backend 返回 cursor）；Vitest 断言 fixture variant 四态（empty → empty state；one → 1 张卡；many → 全部渲染；failed → 错误占位 + retry button）
- [ ] 3.3 新增 `useToggleWatchlist.ts` hook + Save / Unsave 闭环：optimistic toggle + `addToWatchlist({ jobMatchId }, { idempotencyKey })`；Unsave → `removeFromWatchlist(jobMatchId, { idempotencyKey })`；4xx → revert + error toast；window.eiToast 反馈；Vitest 断言 request body / Idempotency-Key header / 4xx revert / toast 文案 zh/en；并发多次点击按最后 server 响应为准（race 处理）
- [ ] 3.4 新增 `useDismissRecommendation.ts` hook + Mark not relevant 闭环：optimistic 隐藏卡片 + 自动选下一张 + `markJobNotRelevant(jobMatchId, { reason: enum_default, freeNote: undefined }, { idempotencyKey })`；ui-design 不弹窗收集 reason，frontend 透传默认 enum 值；4xx → revert（卡片重新显示并恢复 selected）+ error toast；Vitest 断言行为完整 + Idempotency-Key
- [ ] 3.5 在 JDDetail Action bar Confirm interview button 接入 `nav("parse", { source: "jd_match", sourceJobMatchId: jobMatchId })`；不携带其他 jd_match 内部状态（如 query / saved / hidden）；Vitest 断言 nav 调用 + params 完整性 + 不含其他字段
- [ ] 3.6 在 JDDetail Action bar Source button 接入 `window.open(sourceUrl, "_blank", "noopener,noreferrer")`；sourceUrl 缺失时 button disabled；Vitest spy 断言 window.open 调用 + 两个 flags 出现 + sourceUrl 不进 console / URL / localStorage / telemetry
- [ ] 3.7 接入 `requestAuth` pending action：未登录用户对 Save / Unsave / Mark not relevant / Confirm interview 触发 `requestAuth({ type: "jd_match_action", route: "jd_match", params: { tab: "recommended", selectedJobMatchId, action: enum }, label })`；登录后回到 jd_match Recommended tab + selected = selectedJobMatchId 自动重新触发 action（如 selectedJobMatchId 已被 dismiss 则 fallback 第一张 + 提示）；Source button 不进 auth pending action，只验证安全 `window.open` 与隐私红线；Vitest `jd_match/JDMatchAuthGate.test.tsx` 断言 query / label / freeNote / sourceUrl 不进 pendingAction params
- [ ] 3.8 隐私反查：Vitest 断言 jobMatchId / sourceUrl / freeNote 不出现在 `console.log` / URL query / `localStorage` / telemetry payload；mockTransport spy 仅记录 status code + 调用次数，不记录 body
- [ ] 3.9 新增 8 个测试文件全 PASS：`jd_match/RecommendedTab.test.tsx` / `JobMatchCard.test.tsx` / `JDDetail.test.tsx` / `JDMatchAuthGate.test.tsx` / `RecommendedToggleWatchlist.test.tsx` / `RecommendedDismiss.test.tsx` / `RecommendedConfirmInterview.test.tsx` / `RecommendedOpenSource.test.tsx`；Phase 3 测试合计 ≥10 测试
- [ ] 3.10 BDD-Gate: 验证 `E2E.P0.022` Recommended tab 主路径 + 4 button 闭环 + auth pending action 资产 + `E2E.P0.026` Confirm interview → parse 出口 params 完整性资产

## Phase 4: Search tab — 自然语言搜索 + Saved searches + Result filters + 单一加载文案

- [ ] 4.1 新增 `frontend/src/app/screens/jd_match/SearchTab.tsx` + 子组件，按 `ui-design/src/screen-jd-match.jsx::SearchTab` line 480 + line 515-531 源级复刻；DOM 包含搜索框 + Run web search 按钮 + 数据源 tag 列 + 5 步 AGENT panel（`jdmatch-search-searching-panel` testid + `● AGENT SCANNING` / `● AGENT 扫描中` accent label + 1px accent 外边框 + 3px accent 左边框 + 5 行 step 通过 i18n typed helper 渲染 `jdMatch.search.searchingStep1..5` 文案 + `jdmatch-search-searching-step-${i}` testid + `opacity: i <= 2 ? 1 : 0.4` inline style 渐变）+ Saved searches 3 列网格（list + create，无 delete）+ Results 2 列网格 cap 6 + 4 chip filter (all/strong/remote/unseen)；i18n 第 3 步文案不得包含动态 JD 数字（`248`/`87`/`unique postings`/`唯一岗位`）；testid `jdmatch-search-{input,run,sources,searching-panel,searching-step-${i},saved-grid,saved-item-${id},results,filter-${k}}` 全部命中；Vitest 断言 DOM
- [ ] 4.2 新增 `useSearchJobs.ts` hook：Run → `searchJobs({ query, filters?, profileSnapshot? }, { idempotencyKey })`；in-flight 期间 `searching=true` 渲染 5 步 AGENT panel（5 个 step 文案通过 i18n typed helper `jdMatch.search.searchingStep1..5` 渲染 + `opacity: i <= 2 ? 1 : 0.4` 渐变 + accent label + accent 边框）；保持 ui-design 当前 opacity 渐变作为唯一动画效果，不引入 setInterval / 真步骤索引切换 / 新 keyframes；Run 按钮 disabled 防止重复提交；slow-response variant 验证 5 步 panel 持续渲染至 settle，不超时；切 tab / unmount 时 abort in-flight search；Vitest 断言 request body / Idempotency-Key / 5 步 panel 渲染 + 5 个 step testid 全命中 + opacity 渐变 computed style + step i18n 文案命中 / 不调用 setInterval 或 setTimeout step-advance / 不超时；Vitest fake timer 覆盖 race
- [ ] 4.3 新增 `useSavedSearches.ts` + `useCreateSavedSearch.ts` hooks：切 Search tab 调 `listSavedSearches` 1 次；点击 "Save current as watch" 按钮 → `createSavedSearch({ label, query, filters? }, { idempotencyKey })` + window.eiToast；ui-design 无 delete 入口故 plan 002 不实现 `deleteSavedSearch`；Vitest 断言 list 调用 1 次、create request body / Idempotency-Key / 4xx → toast + 不写入 saved searches；label 不进 URL/localStorage/telemetry
- [ ] 4.4 4 chip filter 纯 client-side 切换：按 §3.7 规则（all = 所有；strong = score≥85；remote = location 含 "remote/远程"；unseen = `!seen`）；切换不发新 search request；切换后 results count 与 filter 标签 active 状态正确；Vitest 断言
- [ ] 4.5 接入 `requestAuth` pending action：未登录用户点击 Run / Save current 触发 `requestAuth({ type: "jd_match_action", route: "jd_match", params: { tab: "search", action: enum }, label })`；登录后回到 jd_match Search tab 自动重新触发 action；query / saved-search label 不进 pendingAction params；Vitest 断言
- [ ] 4.6 隐私反查：Vitest 断言 query / saved-search label / filter state / sourceJobUrl 不出现在 `console.log` / URL query / `localStorage` / telemetry payload；mockTransport spy 仅记 status code；切 tab / unmount 时 query reset
- [ ] 4.7 失败 / 空 / boundary：`failed` variant → inline error + 保留输入 + 不展示伪造 results；`empty` variant → no-results 空态文案；`slow-response` variant → 5 步 AGENT panel（i18n + opacity 渐变 + accent 标签）持续渲染直到 settle；Vitest 各 variant 单独测试
- [ ] 4.8 新增 7 个测试文件全 PASS：`jd_match/SearchTab.test.tsx` / `SearchTabRun.test.tsx` / `SearchTabSavedSearches.test.tsx` / `SearchTabFilter.test.tsx` / `SearchTabFailure.test.tsx` / `SearchTabPrivacy.test.tsx` / `SearchTabAuthGate.test.tsx`；Phase 4 测试合计 ≥7 测试
- [ ] 4.9 BDD-Gate: 验证 `E2E.P0.023` Search tab 主路径 + Saved searches + filter + slow-response + failure + privacy 资产

## Phase 5: Watchlist tab + Market signals

- [ ] 5.1 新增 `frontend/src/app/screens/jd_match/WatchlistTab.tsx` + `WatchlistItem.tsx` + `MarketSignalsGrid.tsx` 子组件，按 `ui-design/src/screen-jd-match.jsx::WatchlistTab` line 592 源级复刻；DOM 包含 WatchlistItem 列表（3px 左边框按 tone 着色 ok/warn/muted、added 时间、change 信号、chevron）+ Market signals 4 列 grid（k/v/d/tone 4 卡）+ refresh footer 文案；testid `jdmatch-watchlist-item-${id}` + `jdmatch-watchlist-item-${id}-chevron` + `jdmatch-market-signal-${k}` + `jdmatch-watchlist-refresh-footer` 全部命中；Vitest 断言 DOM 完整 + 视图模型严格按 §3.7
- [ ] 5.2 新增 `useWatchlist.ts` + `useMarketSignals.ts` hooks：切 Watchlist tab 调 `listWatchlist` + `getMarketSignals` 各 1 次；不重复调；fixture variant 三态（empty / few / partial-data）渲染；Vitest 断言调用次数 + 三态 UI
- [ ] 5.3 chevron handoff：点击 WatchlistItem chevron → tab 切 Recommended + selected = `WatchlistItem.linkedJobMatchId`；当 linkedJobMatchId 不在当前 Recommended 列表时 fallback 第一张 + window.eiToast warning；linkedJobMatchId 不进 URL/localStorage/telemetry；Vitest 断言两种路径
- [ ] 5.4 Market signals partial-data：partial-data variant → 部分卡片渲染 + 缺失值 fallback 文案（不展示 0 或硬编码占位）；Vitest 断言
- [ ] 5.5 隐私反查：Vitest 断言 watchlist label / sourceJobUrl / linkedJobMatchId 不出现在 `console.log` / URL query / `localStorage` / telemetry payload；mockTransport spy 仅记 status code
- [ ] 5.6 新增 6 个测试文件全 PASS：`jd_match/WatchlistTab.test.tsx` / `WatchlistChevron.test.tsx` / `WatchlistEmpty.test.tsx` / `MarketSignals.test.tsx` / `MarketSignalsPartial.test.tsx` / `WatchlistPrivacy.test.tsx`；Phase 5 测试合计 ≥6 测试
- [ ] 5.7 BDD-Gate: 验证 `E2E.P0.024` Watchlist + Market signals + chevron handoff + empty + partial-data + privacy 资产

## Phase 6: 验证收口（pixel parity + scenario + regression rerun + 文档同步）

- [ ] 6.1 升级 `frontend/tests/pixel-parity/jd_match.spec.ts` 覆盖三 tab × desktop (1440×900) + mobile (390×844) × warm/light + dark + customAccent：DOM 锚点存在性 + bounding box stays in viewport / no overlap + mobile 折叠（Recommended 双→单列、Saved searches 3→单列、Market signals 4→2 列、Search results 2→单列、JDDetail sticky 改流式）+ 三主题态 computed background / color 可见变化 + toHaveScreenshot baseline 区域聚焦关键 DOM；沿用 D3 maxDiffPixels 阈值
- [ ] 6.2 派生 5 个新 scenario 目录：`test/scenarios/e2e/p0-022-jd-match-recommended-and-confirm/` / `p0-023-jd-match-search-and-saved/` / `p0-024-jd-match-watchlist-and-signals/` / `p0-025-jd-match-profile-and-agent-status/` / `p0-026-jd-match-confirm-interview-handoff/`；每个含 `README.md`（§6 baseline + §7 离线限制）+ `scripts/{setup,trigger,verify,cleanup}.sh`，按 `test/scenarios/README.md` + `test/scenarios/e2e/README.md` 规范实现；verify 脚本断言对应 testid 命中、retired-entry grep 0 命中、新增 spec 全 PASS marker
- [ ] 6.3 升级 `test/scenarios/e2e/p0-017-jd-match-placeholder/scripts/verify.sh`：不再 grep `jdmatch-placeholder` 与 "Coming in P1" 文案；改为断言三 tab 数据驱动渲染（Profile chip + AGENT badge + Recommended 列表 + 三 tab 切换）；保留旧业务 testid 负向断言（确认 plan 002 落地后旧 prototype 业务 testid 仍 0 命中）；同步更新 README.md 描述
- [ ] 6.4 `test/scenarios/e2e/INDEX.md` P0 表追加 5 行（P0.022/023/024/025/026），关联需求列指向 `frontend-home-job-picks-and-parse C-12～C-16`，状态 Ready，执行方式 automated；同步 P0.017 行（描述更新为「升级到三 tab 数据驱动 smoke」）
- [ ] 6.5 Regression 重跑：`E2E.P0.001 / 002 / 004 / 005 / 006 / 014 / 015 / 016 / 017` 全部 setup→trigger→verify→cleanup PASS（D1+D2+D3+plan001 不被 plan 002 改动破坏）；`pnpm --filter @easyinterview/frontend test`（全量 Vitest，含本 plan 新增 ≥25 spec）+ `pnpm --filter @easyinterview/frontend typecheck` + `pnpm --filter @easyinterview/frontend build` + `make build` + `make validate-fixtures` 全 PASS
- [ ] 6.6 `pnpm --filter @easyinterview/frontend test:pixel-parity` 在 plan 001 已升级 baseline 上累加新增三 tab × 三主题 spec；总数全 PASS
- [ ] 6.7 文档与索引同步：`/sync-doc-index --fix-index` 把 `docs/spec/INDEX.md` 与 `docs/spec/frontend-home-job-picks-and-parse/plans/INDEX.md` 同步到 Header 当前；`make docs-check` zero drift；`check_md_links` 双 OK
- [ ] 6.8 负向搜索：`frontend/src/` 内不 import `ui-design/src/data.jsx` / `window.EI_DATA` 0 命中；不复制粘贴 `screen-jd-match.jsx` 内嵌 mock 数据 0 命中；plan 001 placeholder 文案与 testid grep 0 命中；动态 JD 数字 / 动态计数（`248`、`87`、`unique postings`、`唯一岗位`、`248 → 87`）在 `frontend/src/` 与 `frontend/src/app/i18n/locales/` 0 命中（前端只渲染不含数字的静态语义文案；ui-design 静态原型与本仓库 `ui-design/` 目录的 jsx 不在 grep 范围内）；旧 route alias 0 命中；LinkedIn / Boss / 脉脉 / 拉勾 SDK + AI provider key + provider registry + prompt registry + AIClient + LLM endpoint + bypass generated client 的外部招聘平台 fetch grep 0 命中（除 fixture / i18n / 测试负向断言中的合规文案展示）；query / saved-search label / watchlist label / sourceJobUrl / freeNote / linkedJobMatchId / jobMatchId 不在 console.log / URL / localStorage / telemetry 调用中出现
- [ ] 6.9 Plan 002 接力升级：将 `docs/spec/frontend-home-job-picks-and-parse/plans/INDEX.md` 中 002 状态从 `draft` 改为 `completed`，更新版本到 1.x 与完成日期；将 `docs/spec/INDEX.md` 中 subspec 行版本与日期同步；history.md 追加完成行
- [ ] 6.10 BDD-Gate: 验证 `E2E.P0.022 / E2E.P0.023 / E2E.P0.024 / E2E.P0.025 / E2E.P0.026` 全部 setup→trigger→verify→cleanup PASS + `E2E.P0.017` 升级后 PASS + D1+D2+D3+plan001 P0.001/002/004/005/006/014/015/016 regression 全 PASS
