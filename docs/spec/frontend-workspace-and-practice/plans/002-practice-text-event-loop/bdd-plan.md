# 002 BDD Plan

> **版本**: 1.0
> **状态**: active
> **更新日期**: 2026-05-13

**关联 Plan**: [plan](./plan.md)

## 1 场景矩阵

| 场景 ID | 类别 | 关联 Phase | 关联 Spec C-* | 关联 BDD-Gate（主 checklist） |
|---------|------|-----------|--------------|----------------------------|
| E2E.P0.042 | primary path · 文本面试 assisted happy path（mount → 首题 → answer_submitted → ask_follow_up → ask_question → CTA 可点 + UI parity） | Phase 1 + 2 + 3 | C-4, C-8, C-9 | Phase 1.7、Phase 2.7、Phase 3.9 |
| E2E.P0.043 | alternate · strict / assisted × baseline / debrief 显隐 + hint / skip / pause-resume 副路径 + text UI source parity + scoped voice placeholder + 旧口径负向（`practiceMode='debrief'` / `切到语音` / voice surface DOM / Idempotency-Key.appendSessionEvent） | Phase 3 | C-4 部分, C-10, C-12 | Phase 3.9 |
| E2E.P0.044 | failure / recovery · AI 502 timeout + session 404 sessionLost + 409 mismatch refresh + 409 strict conflict 防御 + retry 复用 clientEventId | Phase 4 | C-4, C-12 | Phase 4.10 |
| E2E.P0.045 | primary · completePracticeSession 202 + handoff `generating?sessionId&reportId&...PracticeDisplayContext` + Idempotency-Key replay + 隐私红线（body 不含展示字段 + raw text 不泄漏） | Phase 4 | C-4, C-6（handoff 入口端）, C-12 | Phase 4.10 |

> C-5（voice surface 源级复刻 + STT/TTS）属于 plan 003；C-6 内 generating 屏的 4 步进度态 / `getFeedbackReport` 轮询 / report handoff 属于 plan 004。本 plan 只覆盖到 generating 入口跳转 + 参数完整性。

---

## Phase 1 + 2 + 3: 文本面试 assisted happy path

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.042 | 文本面试 assisted happy path | 用户已登录；workspace 已成功 `createPracticePlan` + `startPracticeSession` 跳转到 `practice?sessionId=...&planId=...&targetJobId=...&jdId=...&resumeVersionId=...&roundId=...&mode=text&modality=text&practiceMode=assisted&practiceGoal=baseline&hintUsed=false&hintCount=0`；fixture：`getPracticeSession=default` + `appendSessionEvent=follow-up`（answer_submitted → ask_follow_up）+ `appendSessionEvent=default`（answer_submitted → ask_question；若当前 default 仍为 ask_follow_up，Phase 2 先修订 fixture truth）；InterviewContext 完整 hydrate | （A）practice 路由命中 `PracticeScreen`（不再是 PlaceholderScreen）；（B）`usePracticeSessionLoader` 首次 mount 拉取 `getPracticeSession.default`，渲染 currentTurn.questionText 到 QuestionCard；（C）用户在 InputBar 输入 answer 文本，点击「提交」 → `appendSessionEvent({clientEventId:UUIDv7, kind:'answer_submitted', payload:{turnId, answerText}})`，请求头**不**含 `Idempotency-Key`；（D）`SessionEventResult.assistantAction.type='ask_follow_up'` → Transcript 增加 AI follow-up message（带 Follow-up Tag）；（E）用户再次输入回答并提交 → 收到 `ask_question` → 推进 qIdx + SessionMap turn 状态切换；（F）切换 zh ↔ en + warm/light → dark → customAccent 多主题；（G）mobile (390×844) viewport 折叠为单列 | （1）`practice` route 渲染 `PracticeScreen` 命中 ≥ 20 个 `practice-*` testid；（2）TopBar 显示题号 / 计时 / Pause / segmented mode (text/voice) / strict toggle；段控 voice 选项渲染但**不**触发 voice surface；（3）QuestionCard 显示 `currentTurn.questionText` + topic + tags（来自 fixture）；（4）Transcript 在 user → AI follow-up → user → AI ask_question 序列后渲染 4 个 TranscriptMsg + 1 个 Follow-up Tag；（5）`appendSessionEvent` 调用 request init 反向断言：`headers` 不含 `Idempotency-Key`，body 含 `clientEventId`（UUIDv7 格式）+ `kind` + `occurredAt` + `payload`；（6）`generated client` `appendSessionEvent` 调用次数 ≥ 2，每次新 `clientEventId`；同 retry 复用同 id；（7）`provenance` 渲染到 RightPanel.AI TRANSPARENCY 卡，**不**渲染到主对话流；（8）切 zh/en 关键文案重绘；切 dark / customAccent 关键元素 computed background / color 可见变化；（9）mobile viewport 中部 grid 折单列 + 输入 sheet sticky bottom + RoleDropdown drawer；（10）负向：`window.EI_DATA` / `D.questions` / `D.sessionTranscript` literal grep 0 命中；voice 组件 import 0 命中；`getFeedbackReport` / `createPracticeVoiceTurn` 调用 0 命中；旧 prototype testid（`practice-mode-card-*` / `growth-*` / `drill-builder-*` / `practice-voice-*` voice surface dom）0 命中 | `test/scenarios/e2e/p0-042-practice-text-loop-assisted-happy-path/` |

## Phase 3: strict / assisted × baseline / debrief 显隐 + hint / skip / pause-resume

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.043 | strict / assisted × baseline / debrief 显隐 + hint / skip / pause-resume + 旧口径负向 | 分四子场景：（A）`practiceMode=assisted` + `practiceGoal=baseline` + fixture `appendSessionEvent` 含 `default`（answer）+ `show-hint`（hint requested 200 OK，assistantAction.type=show_hint）+ `turn-skipped` + `pause-resume`；（B）`practiceMode=assisted` + `practiceGoal=debrief`（同样 fixture）；（C）`practiceMode=strict` + `practiceGoal=baseline` + fixture 额外含 `hint-strict-conflict`（409 PRACTICE_SESSION_CONFLICT）；（D）`practiceMode=strict` + `practiceGoal=debrief`（同 C） | 分别加载 4 路由组合并触发：assisted 子场景点击「提示」/「跳过」/「暂停」/「继续」；strict 子场景验证 hint button DOM 缺失 + assisted/strict 显隐切换 + LIVE NOTES / experience cards / strict-mode banner 切换；所有子场景验证 voice segmented control 点击 → ComingSoon 占位 + 「返回 text」回流；执行旧口径负向 grep | （A1 assisted + baseline）（1）LIVE NOTES 渲染（左栏底部 dotted border 块）；（2）hint button 渲染（输入区左侧）；（3）右栏渲染「可调用经历」3 张 ExpCard；（4）点 hint → `appendSessionEvent({kind:'hint_requested'})` → 200 → `assistantAction.type='show_hint'` → HintBanner 渲染 `assistantAction.hint`；`InterviewContext.hintCount` 自增到 1、`hintUsed='true'`；右栏底部 CTA 下方文案 `1 hint used · will appear in report`；（5）点 skip → `appendSessionEvent({kind:'turn_skipped'})` → 200 → SessionMap 当前 turn 标记 `skipped` + renderer 推进下一题；（6）点 pause → `appendSessionEvent({kind:'session_paused'})` → 200 → 本地 timer 停止 + submit/hint/skip 三按钮 disabled + TopBar Pause 按钮变 Resume + voice live indicator 切「已暂停」；（7）点 resume → `appendSessionEvent({kind:'session_resumed'})` → 200 → timer 恢复 + 按钮解禁；（A2 assisted + debrief）显隐与 A1 完全一致；hint button 仍渲染、ExpCard 仍渲染、LIVE NOTES 仍渲染；负向：grep `goal === 'debrief'.*hide` 在 practice 模块 0 命中（仅注释 / 测试断言文件除外）；（C1 strict + baseline）（1）LIVE NOTES DOM 不存在；（2）hint button DOM 不存在；（3）右栏渲染 strict-mode banner 替换 ExpCard；（4）即使 fixture 强制注入 `hint-strict-conflict` 场景，UI 因按钮不存在不会触发请求；如手动调用 hook（防御层），409 错误码触发 InlineWarning「严格模拟不允许提示」，不重试；（D1 strict + debrief）显隐与 C1 一致；（E mode 切换）所有子场景点击 voice segmented control → `nav("practice", {...ctx, mode:'voice', modality:'voice'})` → 中部主区域渲染 `VoiceSurfaceComingSoon` 占位卡（图标 + 文案 + 「返回 text」按钮）；点击「返回 text」回流；voice 组件 DOM 不渲染；（F 旧口径负向 grep）`practice` 模块中：旧 enum 值 `practiceMode='debrief'` / 旧文案 `切到语音` / `Switch to voice` / 旧 reportLayout / 旧 featureKey / 旧 prototype testid（`practice-mode-card-*` / `growth-*` / `drill-builder-*` / `mistakes-queue-*` / `practice-voice-*` voice surface dom）/ 独立 voice route / `VoiceSessionSurface` 等 voice 组件 import / `Idempotency-Key.*appendSessionEvent` 全部 0 命中（仅注释 / 测试断言文件除外）；（G strict toggle 锁定）顶部 strict toggle 视觉保留但点击触发 toast「严格模拟需在新建规划时设定，本场已锁定」 + 不调 backend | `test/scenarios/e2e/p0-043-practice-text-loop-strict-and-debrief-display/` |

## Phase 4: AI 502 timeout + sessionLost 404 + 409 mismatch + 409 strict conflict 防御

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.044 | 失败恢复 · AI 502 timeout + sessionLost 404 + 409 mismatch + 409 strict conflict 防御 + retry 复用 clientEventId | 分五子场景：（A）`getPracticeSession=default`（首屏渲染）+ `appendSessionEvent=ai-timeout`（answer submit 失败）；（B）`getPracticeSession=missing-session`（404）；（C）`appendSessionEvent=mismatch`（client_event_fingerprint_mismatch）；（D）strict + 手动调用 `appendSessionEvent=hint-strict-conflict`（409 PRACTICE_SESSION_CONFLICT）；（E）`appendSessionEvent=ai-timeout` → retry 复用同一 clientEventId | （A 加载并点 submit；A1：等待 502 → 点 retry；B 加载 practice route 触发 `getPracticeSession`；C submit answer → 同 clientEventId 改 payload 再 submit；D 手动调用 `usePracticeEvents.requestHint`（虽然按钮不渲染）；E 同 A + retry） | （A）502 触发 InlineError「AI 暂时不可达，请重试」+ retry 按钮 + 输入保留；retry 调用 `appendSessionEvent` 使用与首次相同的 `clientEventId`；3 次失败展示「返回 workspace」fallback CTA；（B）404 触发渲染 `PracticeSessionLostState`（testid `practice-session-lost-{title,desc,cta}`）；CTA「返回 workspace」点击调 `nav("workspace", {targetJobId, jdId, planId, resumeVersionId})`；InterviewContext workspace 上下文保留（roundId / planId / targetJobId 仍在）；（C）409 mismatch 触发 InlineError「同步异常，请刷新」+ 自动触发 `getPracticeSession` refresh；refresh 期间 UI 进入 `refreshing` 锁定（输入禁用 + spinner）；refresh 成功后 server wins 重置本地 transcript；（D）409 PRACTICE_SESSION_CONFLICT (`detail.policy='hint_disabled_in_mode'`) 触发 InlineWarning「严格模拟不允许提示」；不重试；按钮（已隐藏）保持不渲染；（E）retry 复用同 `clientEventId` 在第 2 / 3 次 retry；fresh action（用户重新输入或切换 turn）生成新 `clientEventId`；（F）所有错误流中 `answerText` / `questionText` / `hint` / `provenance.modelId` 不出现在 console.log / URL / localStorage / telemetry；（G）负向 grep：旧 prototype error testid（`error-toast-legacy-*` / `growth-error-*`）0 命中；AI provider key / prompt registry / AIClient / LLM endpoint 0 命中 | `test/scenarios/e2e/p0-044-practice-text-loop-failure-and-recovery/` |

## Phase 4: completePracticeSession 202 + generating 入口 + 隐私红线

| 场景 ID | 场景 | Given | When | Then | 验证入口 |
|---------|------|-------|------|------|----------|
| E2E.P0.045 | completePracticeSession 202 + handoff `generating` + Idempotency-Key replay + 隐私红线 | 分四子场景：（A）`completePracticeSession=default`（202 + ReportWithJob queued）；（B）`completePracticeSession=replay`（同 Idempotency-Key 二次返回首次 response）；（C）`completePracticeSession=mismatch`（同 key 不同 body fingerprint）；（D）`completePracticeSession=session-already-completed`（session.status='completed' 反向查既有 report）+ `getPracticeSession=completing` 用于 UI 状态过渡；fixture 配合 `appendSessionEvent=completed`（assistantAction.type='session_completed'）用于触发底部 CTA 高亮；practice 路由完整 InterviewContext + `hintCount='2'`（用户使用过 2 次 hint） | （A1）用户达成 session_completed 后点底部 CTA「结束并生成报告」；（A2）再次点击 CTA 模拟双击（StrictMode 双触发）；（B）同 Idempotency-Key 二次提交；（C）同 key 不同 clientCompletedAt；（D）session 已 completed 状态下 mount practice 路由 | （A1）（1）调用 `completePracticeSession(sessionId, {clientCompletedAt:ISO8601}, {idempotencyKey:newIdempotencyBatch.complete})`；request body 仅 `{clientCompletedAt}`；不含 `mode / modality / practiceMode / practiceGoal / hintUsed / hintCount` 任何展示字段；（2）`Idempotency-Key` header 必须存在且来自 `lib/conventions/idempotency.ts::newIdempotencyBatch()`；（3）202 返回 `ReportWithJob{reportId, job}` → `nav("generating", {planId, targetJobId, jdId, resumeVersionId, roundId, sessionId, reportId, mode:'text', modality:'text', practiceMode:'assisted', practiceGoal:'baseline', hintUsed:'true', hintCount:'2'})`；（4）`generating` route 仍渲染 PlaceholderScreen（由 plan 004 替换）；（5）StrictMode 双触发：generated client `completePracticeSession` 调用次数 = 1，`nav` 调用次数 = 1（防抖）；（A2 双击）（1）第二次点击在 in-flight 中被 inFlightRef 拦截；（B replay）（1）二次提交返回首次 response；（2）UI 不重复 nav，nav 调用次数仍 = 1；（C mismatch）（1）409 触发 InlineError「同步异常，请刷新」；（2）建议用户重新点 CTA 生成新 idempotency batch；（D already-completed）（1）`getPracticeSession=completing` 渲染「正在生成报告…」占位；（2）若 backend 返回 `completed` 状态触发自动 nav generating（防抖一次）；（E 隐私红线）（1）grep URL query string：允许稳定 `InterviewContext` ID（`planId / targetJobId / jdId / resumeVersionId / roundId / sessionId / reportId`）+ `PracticeDisplayContext`（`mode / modality / practiceMode / practiceGoal / hintUsed / hintCount`）；不含 `answerText / questionText / hint / promptHash / modelId`；（2）grep localStorage / sessionStorage：不含 transcript 明文 / answerText / questionText / hint / provenance modelId；（3）grep console.log：不含 raw answer / question / hint / provenance；（4）grep telemetry / fixture transport spy：仅 status / latency / 4xx code / scenario name；（F 旧口径负向）（1）`getFeedbackReport` 在 practice 模块调用次数 = 0；（2）`createPracticeVoiceTurn` 在 practice 模块调用次数 = 0；（3）旧 prototype testid / 旧 route alias / `Idempotency-Key.*appendSessionEvent` 0 命中 | `test/scenarios/e2e/p0-045-practice-text-loop-complete-and-generating-handoff/` |
