apiVersion: plancontext.agent.dev/v1alpha1
kind: PlanContext
metadata:
  subspec: backend-practice
  name: 003-mode-policies-and-provenance
  sequence: 3
  specVersion:
    from: 1.17
    to: 1.18
spec:
  defaultTarget: backend
  discovery:
    aliases:
      - backend-practice
      - backend-practice/003
      - 003-mode-policies-and-provenance
      - mode-policies-and-provenance
      - appendSessionEvent
      - hint_requested
      - show_hint
      - hint_generate
    keywords:
      - practice hint mode policy
      - assisted hint show_hint
      - strict-mode optional hint
      - AssistantAction provenance wire boundary
      - practice.turn.lightweight_observe
      - practice.turn_observe.default
      - ai_task_runs hint_generate
      - graceful degrade session_wait
      - practice_turns hint_text
      - backend practice out-of-scope lint
  targets:
    backend:
      plan: ./plan.md
      checklist: ./checklist.md
      spec: ../../spec.md
      testPlan: ./test-plan.md
      testChecklist: ./test-checklist.md
      bddPlan: ./bdd-plan.md
      bddChecklist: ./bdd-checklist.md
      discovery:
        packages:
          - backend/internal/api/practice
          - backend/internal/practice
          - backend/internal/store/practice
          - backend/internal/ai/aiclient
          - backend/internal/ai/registry
          - backend/cmd/api
          - backend/internal/migrations
          - migrations
          - openapi/fixtures/PracticeSessions
          - scripts/lint
        apiNames:
          - appendSessionEvent
