apiVersion: plancontext.agent.dev/v1alpha1
kind: PlanContext
metadata:
  subspec: backend-practice
  name: 002-event-loop-and-completion
  sequence: 2
  specVersion:
    from: 1.23
    to: 1.26
spec:
  baseBranch: main
  branch: practice-conversation-simplification
  defaultTarget: backend
  discovery:
    aliases: [practice conversation loop, send practice message]
    keywords: [sendPracticeMessage, clientMessageId, replyToMessageId, practice_messages, completePracticeSession, complete resume source snapshot, parsed text snapshot precedence, resume grounding tail marker, empty resume context fail closed, session completed ledger, round progress projection, backend business persistence, target job bound resume, system policy untrusted JSON context, persona style only]
  targets:
    backend:
      plan: ./plan.md
      checklist: ./checklist.md
      spec: ../../spec.md
      testPlan: ./test-plan.md
      testChecklist: ./test-checklist.md
      bddPlan: ./bdd-plan.md
      bddChecklist: ./bdd-checklist.md
      discovery:
        packages:
          - backend/internal/api/practice
          - backend/internal/practice
          - backend/internal/store/practice
          - frontend/src/app/screens/practice
          - openapi/fixtures/PracticeSessions
          - test/scenarios/e2e
        apiNames: [sendPracticeMessage, completePracticeSession]
