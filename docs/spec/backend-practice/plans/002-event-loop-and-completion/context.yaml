apiVersion: plancontext.agent.dev/v1alpha1
kind: PlanContext
metadata:
  subspec: backend-practice
  name: 002-event-loop-and-completion
  sequence: 2
  baseBranch: main
  branch: fix/interview-turn-ux-0713
  specVersion:
    from: 1.31
    to: 1.32
spec:
  defaultTarget: backend
  discovery:
    aliases: [practice conversation loop, send practice message]
    keywords: [sendPracticeMessage, getPracticeSession, clientMessageId, replyStatus, retryable failed, terminal failed, refresh recovery, ApiClientError, replyToMessageId, practice_messages, completePracticeSession, reportable completion, zero answer completion rejected, report-context.v1, REPORT_CONTEXT_SNAPSHOT_PASS, backend business persistence]
  targets:
    backend:
      plan: ./plan.md
      checklist: ./checklist.md
      spec: ../../spec.md
      testPlan: ./test-plan.md
      testChecklist: ./test-checklist.md
      bddPlan: ./bdd-plan.md
      bddChecklist: ./bdd-checklist.md
      references:
        - ../../../backend-review/spec.md
        - ../../../backend-review/plans/001-report-generation-baseline/plan.md
      discovery:
        packages:
          - backend/internal/api/practice
          - backend/internal/practice
          - backend/internal/store/practice
          - frontend/src/app/screens/practice
          - openapi/fixtures/PracticeSessions
          - openapi/templates/ts
          - test/scenarios/e2e
        apiNames: [getPracticeSession, sendPracticeMessage, completePracticeSession]
