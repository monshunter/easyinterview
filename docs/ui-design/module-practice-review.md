# Interview Session 与 Report Dashboard 目标模块

> **版本**: 1.12
> **状态**: active
> **更新日期**: 2026-05-03

## 1 文档目的

本文档定义当前静态 UI 中完整模拟面试与仪表盘报告的闭环。目标是保证用户从一场明确的面试规划进入完整面试，结束后看到一份隶属于该会话的报告，并能明确选择复练当前轮或进入下一轮。

## 2 模块职责

Interview Session 负责：

- 运行一场完整模拟面试。
- 允许用户选择 `文本面试` 或 `语音面试` 作为整场面试形式。
- 在文本面试中提供 `语音转文字`，把语音识别结果填入文本回答框。
- 在同一场面试内提供 `带提示练习` / `严格模拟` 辅助程度开关。
- 记录问题、回答、追问、提示、跳过和会话状态。
- 允许在面试页切换面试官角色：综合面试官、HR、用人经理。
- 通过右侧底部固定 CTA 结束并进入报告生成，同时传递 `practiceMode`、`hintUsed` 和 `hintCount`。

Report Dashboard 负责：

- 以仪表盘形式展示本场面试结果。
- 明确展示报告 `sessionId`、目标岗位、绑定简历、面试轮次、沟通形式、练习方式和提示使用记录。
- 展示准备度、能力维度、题目回顾、证据详情和复练计划。
- 区分 `复练当前轮` 与 `进入下一轮`。

本模块不负责：

- 选择热身、反问、单题深钻等练习模式。
- 在面试前展示练习模式卡片。
- 独立追问树。
- 独立错题本或单题复练队列。
- 报告时间线。
- 独立刊物式报告页。
- 复盘。

## 3 面试 session 生命周期

```text
MockInterviewPlan
  -> CreateInterviewSession
  -> ChooseInterviewForm
     ├─ TextInterview
     │  ├─ TypeAnswer
     │  └─ SpeechToText -> FillTextAnswer
     └─ VoiceInterview
        └─ LiveSpokenConversation
  -> ChooseAssistanceLevel
     ├─ AssistedPractice
     └─ StrictMock
  -> InProgress
     ├─ Question
     ├─ Answer
     ├─ FollowUp
     ├─ Hint / Skip
     └─ NextQuestion
  -> Finished
  -> GeneratingReport
  -> ReportDashboard(sessionId)
```

文本面试和语音面试共享同一个面试生命周期、同一套上下文、同一个 `PracticeScreen` 外层骨架和同一种报告形态。旧 `voice` hash / route 只作为兼容入口折回 `practice?mode=voice&modality=voice`，不再渲染独立的语音页面框架。

当前 `screen-report.jsx` 已删除 `ReportEditorial`、`ReportTimeline` 等历史组件定义，`ReportScreen` 固定返回 `ReportDashboard`。设计画板里的报告变体标签和 `reportLayout` hash 参数也已从静态 UI 清理，不改变当前目标。

## 4 面试页面框架

```text
[Interview Session(sessionId)]
├─ Top Bar
│  ├─ 公司 / 岗位
│  ├─ 面试官角色
│  ├─ 题号 / 总题数
│  ├─ 计时
│  ├─ 暂停
│  ├─ 文本 / 语音形式切换
│  ├─ 语音 live 状态
│  └─ 严格模拟开关
├─ Interview Form
│  ├─ 文本面试 · 打字回答
│  └─ 语音面试 · 实时语音对话
├─ Left Panel
│  ├─ 本轮题目
│  └─ 实时观察
├─ Main
│  ├─ 当前问题
│  ├─ 文本面试 Surface
│  │  ├─ 对话记录
│  │  └─ 输入区
│  │     ├─ 文本框
│  │     ├─ 提示
│  │     ├─ 语音转文字
│  │     ├─ 跳过
│  │     └─ 提交
│  └─ 语音面试 Surface
│     ├─ 正在说话波形
│     ├─ 本次回答标注波形
│     └─ 实时转写
└─ Right Panel
   ├─ 文本面试: 与 JD 的关联 / 可调用经历 / AI 透明度
   ├─ 语音面试: 表达层指标 / 现场提示 / AI 透明度 / 音频留存说明
   └─ 固定底部 CTA: 结束并生成报告
```

## 5 语音相关边界

```text
面试形式:
├─ 文本面试
│  └─ 用户主要通过文字提交回答
└─ 语音面试
   ├─ 用户与 AI 进行实时语音对话
   ├─ 保留波形、停顿、口头禅、语速标注和实时转写
   └─ 复用文本面试的顶部控制、题目地图和会话上下文

文本输入辅助:
└─ 语音转文字
   ├─ 只在文本面试输入框内出现
   ├─ 识别结果填入文本回答
   └─ 不改变当前面试形式
```

因此，按钮文案不能再写成 `切到语音`。如果在文本输入框中使用麦克风，应使用 `语音转文字`、`插入转写` 等表达。

## 6 练习方式与结束动作

```text
Practice Assistance Level
├─ 带提示练习
│  ├─ 左侧展示实时观察
│  ├─ 文本面试可请求提示
│  ├─ 文本面试右侧展示可调用经历
│  └─ 语音面试右侧展示现场提示
└─ 严格模拟
   ├─ 隐藏提示按钮
   ├─ 隐藏左侧实时观察
   ├─ 隐藏可调用经历卡片
   └─ 隐藏语音现场提示
```

`严格模拟` 不是新的面试类型，也不是入口前的模式卡片；它只控制会话内提示和辅助信息的显隐。用户仍在同一个 `PracticeScreen`、同一个 `sessionId` 和同一套报告闭环内完成面试。

结束动作固定在右侧面板底部，避免用户在滚动题目、对话或语音转写时丢失结束入口。点击 `结束并生成报告` 后进入 `generating`，并传递：

```text
ReportGenerationParams
├─ mode / modality: text | voice
├─ practiceMode: assisted | strict
├─ hintUsed: true | false
└─ hintCount: number as string
```

报告只使用这些字段展示沟通形式、练习方式和提示记录，不得把提示使用推导成精确通过率。

## 7 报告仪表盘框架

```text
[Report Dashboard(sessionId)]
├─ Back
│  └─ 返回面试前确认
├─ Header
│  ├─ 面包屑: 模拟面试 / 会话 # / 面试报告
│  ├─ 标题: 岗位 · 当前轮次模拟报告
│  ├─ 说明: 报告隶属于一次已完成模拟面试
│  ├─ 复练当前轮: 当前轮次
│  └─ 进入下一轮: 下一轮次
├─ Context Strip
│  ├─ 所属会话
│  ├─ 目标岗位
│  ├─ 面试轮次
│  ├─ 绑定简历
│  ├─ 完成时间
│  └─ 沟通形式
├─ Summary Cards
│  ├─ 准备度
│  ├─ 维度详情
│  ├─ 题目回顾
│  └─ 下一动作
└─ Detail Surface
   ├─ 准备度详情
   ├─ 维度详情
   ├─ 题目回顾页
   ├─ 证据详情
   └─ 复练计划
```

## 8 复练与下一轮

```text
Report Next Actions
├─ Path A: 复练当前轮
│  ├─ 直接进入同一轮次的面试 session
│  ├─ 不推进到下一轮
│  ├─ 重复同一面试轮次
│  ├─ 带入题目回顾里的错题
│  ├─ 带入证据缺口
│  └─ 带入追问风险
└─ Path B: 进入下一轮
   ├─ 直接进入下一轮面试 session
   ├─ 沿用同一目标岗位 / JD
   ├─ 沿用同一绑定简历
   ├─ 切换到下一轮面试官视角
   └─ 改变题目结构和考察重点
```

这两条路径不能混用，也都不是返回面试前规划页的动作。`返回面试前确认` 可以回到准备页，但报告页上的 `复练当前轮` 与 `进入下一轮` 都应立即发起对应面试 session。`确认下一场模拟面试` 这类模糊文案应避免，因为用户无法判断是重复当前轮还是进入下一轮。

## 9 题目回顾边界

```text
Question Review
├─ 题目列表
├─ 回答分析
├─ 回答有效点
├─ 缺口
├─ 建议框架
├─ 证据片段
├─ 下次面试追问
└─ 加入本轮复练
```

题目回顾承载“错题本”的用户价值，但仍属于报告内部二级详情。它不产生独立错题本模块，不自动进入复盘。

## 10 复盘边界

复盘属于一级 `Debrief` 模块，不属于模拟面试报告。

```text
Debrief
├─ 选择目标岗位 / JD
├─ 选择关联模拟面试
├─ 选择绑定简历
├─ 复盘记录
├─ 复盘分析
└─ 复盘面试
```

模拟报告里的题目回顾可以作为用户理解本场表现的依据，但不自动写入复盘记录。

## 11 后续实现输入

1. `practice` 是 `InterviewSession` 的目标运行 route；`mode/modality=text` 表示文本面试，`mode/modality=voice` 表示语音面试。
2. 旧 `voice` route 必须折回 `practice` 并补齐 `mode=voice` 与 `modality=voice`，不得重新进入独立语音页面骨架。
3. 文本面试输入框的麦克风必须是 `语音转文字`。
4. 面试官角色是同一场面试的上下文参数，不是练习模式入口。
5. `带提示练习` / `严格模拟` 是会话内辅助程度，不得恢复为面试前练习模式卡片。
6. 严格模拟必须隐藏提示、实时观察、可调用经历和语音现场提示；带提示练习可以展示这些辅助信息。
7. `结束并生成报告` 必须保持为右侧底部固定动作，并传递 `practiceMode`、`hintUsed` 和 `hintCount`。
8. 报告生成后只进入 `ReportDashboard(sessionId)`。
9. 报告不得作为顶部一级导航。
10. 报告必须显示完整会话上下文；无 `sessionId` 时必须回到当前面试规划或历史列表，不展示假报告。
11. 任何刊物式长内容都必须挂到仪表盘模块详情，不新增报告形态。
12. 报告不得提供时间线视图。
13. 工作台和报告页的“再练”必须说明是复练当前轮还是进入下一轮；报告页 CTA 直接进入对应面试 session，不先回工作台二次确认。
14. 报告生成页不得再出现“错题条目 / 入本”等旧文案；当前文案应指向报告内部题目回顾和本轮复练线索，不代表恢复独立错题本。
