# 2026-05-18 工作日志

## 11:38 工作记录

### 完成事项

- 修复 Frontend Debrief 默认 dev mock 全流程卡住的问题（[BUG-0078](../bugs/BUG-0078.md)）：
  - OpenAPI fixture：`Jobs/getJob.json` 增加 `debrief-succeeded` 场景，匹配 `createDebrief` default 返回的 `debrief_generate` job。
  - 前端 dev mock：`createDevMockClient()` 记录 `POST /debriefs` 返回的 debrief job id，后续无显式 `Prefer` 的 `GET /jobs/{jobId}` 自动选择 `debrief-succeeded`；同时对 `goal='debrief'` 的 `createPracticePlan` / `startPracticeSession` 自动选择 debrief-derived fixtures。
  - 测试：补充 `devMockClient.test.ts` 的 debrief job / replay fixture 回归，以及 `DebriefScreen.test.tsx` 使用真实 fixture-backed dev mock client 的 Step 0 → Step 1 → Step 2 → practice handoff 回归。
  - 文档：原地修订 `frontend-debrief/001-debrief-screen-and-handoff` spec / plan / checklist / test-checklist / history 至 v1.5；新增 [BUG-0078](../bugs/BUG-0078.md) 与交付复盘报告，并把 dev mock 状态推进缺口沉淀到 `PATTERNS.md` 模式 3。
- 验证通过：`pnpm --filter @easyinterview/frontend exec vitest run src/api/devMockClient.test.ts`、`pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/debrief/DebriefScreen.test.tsx`、`pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/debrief`、`pnpm --filter @easyinterview/frontend typecheck`、`pnpm --filter @easyinterview/frontend exec vitest run src/app/AppAuthDispatch.test.tsx src/app/scenarios/p0-032-dev-mock-auth-state-and-user-menu.test.tsx`、`pnpm --filter @easyinterview/frontend build`、`make validate-fixtures`、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(frontend-debrief): repair dev mock debrief flow (BUG-0078)`

### 备注

用户在默认前端 mock 下直接复现了 completed plan 没覆盖的真实预览断点；本次用 `createDevMockClient()` full-flow regression 固化，避免只靠 stubbed client happy path 收口。

## 10:45 工作记录

### 完成事项

- 完成 Resume Workshop real-backend contract fix（[BUG-0076](../bugs/BUG-0076.md)）：
  - OpenAPI / generated artifacts：在 `RequestResumeTailorRequest` 新增 optional `resumeVersionId`，同步 `openapi/openapi.yaml`、`openapi/fixtures/ResumeTailor/requestResumeTailor.json`、`backend/internal/api/generated/openapi.yaml`、`backend/internal/api/generated/types.gen.go`、`frontend/src/api/generated/{spec,types}.ts`。
  - 后端：`handler/request_tailor.go` / `service.go` / `store/tailor_runs.go` / `store/types.go` / `store/versions.go` 把 `resumeVersionId` 透传到 tailor run / async job payload，并校验 ownership (`user_id`、`resume_asset_id`、`target_job_id`)；`GetVersionByID` + `ListVersionsByAsset` 现在主动 hydrate `resume_version_suggestions`。补充 `handler/tailor_run_test.go`、`service_test.go`、`store/assets_test.go`、`store/versions_integration_test.go` 的覆盖。
  - 前端：`useUpdateResumeVersion` 过滤 read-only `structuredProfile.provenance`；create 流 `mapParsedSummaryToStructuredProfileDraft` 现在写入 provenance；`useRequestResumeTailor` 透传 `resumeVersionId`；`ResumeBranchFlow` 在 target 解析为合法 server ID 前不再调用 `branchResumeVersion`，并新增 `resumeWorkshop.branch.error.targetInvalid` i18n key（EN/ZH）；`ResumeDetailView` / `ResumeWorkshopAuthGate` / `params.ts` 同步适配新参数。补充 `mapParsedSummaryToStructuredProfileDraft.test.ts`、`useUpdateResumeVersion.test.tsx`、`useRequestResumeTailor.test.tsx`、`ResumeBranchFlow.test.tsx`、`ResumeDetailView.test.tsx`、`ResumeWorkshopScreen.test.tsx`、`ResumeWorkshopAuthGate.test.tsx`、`ResumeWorkshopPrivacy.test.ts` 的相关断言。
- 完成 OpenAPI baseline drift 修复（[BUG-0077](../bugs/BUG-0077.md)）：把 `openapi/baseline/openapi-v1.0.0.yaml` 重新冻结到 `main:openapi/openapi.yaml` 的 59-operation 已接受契约（不含本次新增字段），同步 `openapi/diff-config.yaml endpointCount=59`、`openapi/baseline/README.md` 与 `scripts/lint/openapi_diff_test.py`，确保 `make openapi-diff` 退出 0、只剩 `RequestResumeTailorRequest.resumeVersionId` 一条 additive。
- 文档：`docs/spec/openapi-v1-contract/spec.md` / `history.md` 升至 v1.25（2026-05-18），新增 D-24 条目并在 §3.2 数据合同处补 `resumeVersionId` 说明；`docs/spec/INDEX.md` 同步版本；新增 `docs/bugs/BUG-0076.md` / `docs/bugs/BUG-0077.md` 并登记 `docs/bugs/INDEX.md`；新增 `docs/reports/2026-05-18-resume-workshop-real-backend-contract-fix-assessment.md` 并登记 `docs/reports/INDEX.md`。
- 验证通过：`pnpm --filter @easyinterview/frontend test src/app/screens/resume-workshop/...`（focused suites）、`pnpm --filter @easyinterview/frontend typecheck`、`cd backend && go test ./internal/resume/...`、`cd backend && go test -tags=integration ./internal/resume/store -run 'TestResumeTailorRunStoreStateTransitionsIsolationAndClaim|TestCompleteTailorRunSuccessWritesSuggestionsAndReadyOnlyOutbox'`、`cd backend && go test ./...`、`make validate-fixtures`、`make lint-openapi`、`make openapi-diff`（breaking=0, additive=1）、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(resume-workshop): close real backend gaps (BUG-0076, BUG-0077)`

### 备注

Single-commit delivery per user instruction; BUG-0076 / BUG-0077 关联 Commit 字段与 bugs INDEX 已同步至本 commit subject。

## 07:21 工作记录

### 完成事项

- 完成 `plan-code-review frontend-resume-workshop/003-branch-rewrites-and-edit --fix` 的 L2 假绿修复。
  - 补齐 `frontend/tests/pixel-parity/resume-workshop-branch-rewrites-edit.spec.ts`，覆盖 BranchFlow / Rewrites / Edit 三屏 desktop + mobile DOM anchor、computed style、bounding box、screenshot buffer、scoped axe-core，以及 Rewrites/Edit Export PDF + Copy Text 行为。
  - 修复 `ResumeDetailView`：Rewrites / Edit tabs 顶部恢复 Export PDF / Copy Text actions，复用 plan 001 export/copy handlers；新增 Vitest 断言防止回归。
  - 修复 UI parity 运行时漂移：给 frontend theme 补 `--ei-serif / --ei-sans / --ei-mono` alias，保证从 `ui-design` 迁移来的 inline token 可解析；给 BranchFlow 补移动端网格折叠，消除 390px viewport 横向溢出。
  - 升级 P0.087 scenario：trigger 从只跑 Vitest 改为 Vitest + frontend build + Playwright；verify 检查 Vitest/build/Playwright runner marker、spec path、passing summary，并继续执行 retired/prototype grep gates。
  - 修正 checklist / scenario README / 交付复盘报告中的 deferred 口径，新增 [BUG-0075](../bugs/BUG-0075.md)，并把它归入 `PATTERNS.md` 模式 4。
- 验证通过：`sync-doc-index --check` 0 drift；`pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/resume-workshop/` 39 files / 254 tests；`pnpm --filter @easyinterview/frontend build`；`pnpm --filter @easyinterview/frontend exec playwright test tests/pixel-parity/resume-workshop-branch-rewrites-edit.spec.ts` 4 tests；P0.087 `setup → trigger → verify → cleanup` 全 PASS。

### 关联 Commit

- `fix(frontend-resume-workshop): close branch rewrites L2 gates (BUG-0075)`

### 备注

L2 review remediation for `frontend-resume-workshop/003-branch-rewrites-and-edit`; see [BUG-0075](../bugs/BUG-0075.md).

## 06:14 工作记录

### 完成事项

- 创建 `frontend-shell` plan 004-url-addressable-routing：锁定 SPA Browser History canonical URL routing 方向。
  - 新增 plan 文档集：plan.md / checklist.md / context.yaml / bdd-plan.md / bdd-checklist.md。
  - spec v1.14 → v1.15：新增 D-10（Canonical URL routing 方向决策）、D1 设计约束段（path 与 route catalog 对齐、canonical URL 只允许稳定 ID 参数、hash adapter 保留至迁移完成、Auth pendingAction 共用 safe params、history pushState/replaceState/popstate 统一管理）、C-11（deep-link / reload / back-forward）、C-12（auth pendingAction + URL privacy redline）、C-13（hash compatibility + legacy route negative regression）。
  - 同步更新 `docs/spec/INDEX.md`、`docs/spec/frontend-shell/history.md`（v1.15 修订记录）、`docs/spec/frontend-shell/plans/INDEX.md`（004 计划索引）。

### 关联 Commit

- `docs(frontend-shell): add url-addressable routing plan 004`

### 备注

Manual journal entry for plan documentation scaffolding.

## 00:50 工作记录

### 完成事项

- 完成 `frontend-resume-workshop/003-branch-rewrites-and-edit` Phase 7：i18n + a11y + 隐私 + UI parity + BDD + 旧入口负向收口。
  - 7.1 i18n key 空间齐备：zh.ts / en.ts 共 92 条 `resumeWorkshop.branch.* / .rewrites.* / .edit.*` 翻译（含 polling banner / scope / actions / toast / error），`LocaleMessages = Record<MessageKey, string>` 类型约束 + `pnpm typecheck` 双向 parity；7 个 generated op 通过 plan 001 `useResumeAssets/Versions` Accept-Language 行为继承。
  - 7.2 a11y：BranchFlow input `aria-required/-invalid`、focus chip / seed card `aria-pressed`、submit `aria-disabled`；Rewrites Tab `role=listbox/option`、scope banner `role=status + aria-live=polite`、polling banner `role=status`、failed banner + manual-pending `role=alert`、actions `aria-label`；Edit Tab `<label htmlFor>` + scope/error 角色。Playwright axe-core 套件留作后续 plan（plan §3 D-5 允许 DOM/style 真理由 Vitest 承载）。
  - 7.3 隐私 grep：`ResumeRewritesTab.test.tsx` / `ResumeEditTab.test.tsx` spy `fetch / localStorage / history.replaceState / window.location.href`，断言 originalBullet / suggestedBullet / structuredProfile 文本不溢出；`ResumeWorkshopAuthGate.test.tsx` 已锁 pendingAction 仅含路由必要参数；container 错误信封映射 i18n key 后再 toast，不回显原 envelope。
  - 7.4 UI parity gate：Vitest 三屏 DOM 锚点 + state 属性（`data-branch-can-submit / data-edit-dirty / data-bullet-count / data-source-status / data-edit-saving / ...`）证明源级镜像；Playwright pixel parity spec 待后续 plan 接入（plan §3 D-5 允许 clean checkout 不依赖未跟踪 baseline）。
  - 7.5 Export PDF / copyText 一致性：plan 001 `ResumeDetailExport.test.tsx` + `ResumeDetailFixtureParity.test.tsx` 在 plan 003 swap 后仍 PASS（被 P0.087 trigger 覆盖）；detail header Export/Copy 按钮独立于 tab 内容，行为不退化。
  - 7.6 - 7.9 BDD-Gate 四场景全 PASS：`p0-084` 4 文件 / 42 tests、`p0-085` 3 文件 / 27 tests、`p0-086` 4 文件 / 34 tests、`p0-087` 5 文件 / 40 tests；每个场景含 setup/trigger/verify/cleanup 脚本，verify.sh 断言 vitest 通过摘要 + spec 文件存在 + 相关目录 grep 0 命中。
  - 7.10 - 7.12 retired-grep gate：把 `mapBranchFormToRequest.ts` "by mistake" 改写为 "by accident"，把若干 `inline alert` testid 描述改写为 `in-form alert`；`branch/` + `tabs/` 三类 grep（retired modules / retired tailor mode / prototype import）全部 0 命中。
  - 7.13 `test/scenarios/e2e/INDEX.md` 追加 P0.084 - P0.087 四行（关联 `frontend-resume-workshop C-11, C-8, C-9`，automated/Ready）。
  - 7.14 `python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 报告 0 drift（Header / INDEX / Orphans / Warnings 全 0）。spec.md 维持 v1.1，未引入新设计事实，无 bump。
- 验证通过：`pnpm typecheck`、`pnpm exec vitest run src/app/screens/resume-workshop/` 39 文件 / 253 tests、四 BDD scenario end-to-end PASS、`sync-doc-index --check` 0 drift。

### 关联 Commit

- `feat(frontend-resume-workshop): close branch rewrites scenarios and parity`

### 备注

Auto-committed by `/tdd` phase-commit, plan: frontend-resume-workshop/003-branch-rewrites-and-edit

## 00:40 工作记录

### 完成事项

- 完成 `frontend-resume-workshop/003-branch-rewrites-and-edit` Phase 6：ResumeEditTab + updateResumeVersion 保存。
  - 6.1 新增 `tabs/ResumeEditTab.tsx`，源级复刻 `ui-design/src/screen-resume-workshop.jsx` L943-1012：master/targeted scope banner（i18n 切换 + version 名插值）+ headline `<input>` + summary `<textarea>` + Experience / Skills 两段 placeholder 卡 + Save changes 按钮；12 testid 覆盖（`resume-edit-{tab, scope-banner, scope-banner-message, headline-input, summary-textarea, section-experience, -experience-add, -experience-placeholder, section-skills, -skills-add, -skills-placeholder, save-button}`）。
  - 6.2 复用 Phase 4 已落地的 `useUpdateResumeVersion`：`filterUpdateResumeVersionPayload` 阻止下发 `{versionType, resumeAssetId, parentVersionId, targetJobId, seedStrategy}`，按 payload+versionId fingerprint 缓存 IK，`UpdateResumeVersionError` 信封区分 `{validation, idempotency_conflict, cross_user, generic}`。
  - 6.3 P0 实际可编辑字段仅 headline + summary；Experience / Skills 区为 SectionPlaceholder，Add 按钮触发 `window.eiToast` 显示 comingSoon 文案而不调用 onSave。
  - 6.4 `ResumeEditTabContainer` 在 `ResumeDetailView` 内将 `ResumeEditTab` 与 `useUpdateResumeVersion` 连线：成功 → `fireResumeWorkshopToast("Changes saved for {versionName}")` + `versionQuery.retry()` 刷新版本（不触发 nav），失败 → 按 hook 信封 kind 映射 i18n 错误 inline alert。
  - 6.5 fixture parity：`useUpdateResumeVersion.test.tsx` 锁定 `updateResumeVersion.json` 三 scenario keys（`default / idempotency-replay / validation-error-422`），happy 路径直接消费 default body 字节。
  - 6.6 隐私：`ResumeEditTab.test.tsx` spy `localStorage.setItem` / `history.replaceState` / `window.location.href`，断言 headline / summary 文本不流向 URL / localStorage。
- 新增 i18n keys `resumeWorkshop.edit.*` 17 条（scope/labels/save/section/toast/error）于 EN/ZH。
- 验证通过：`pnpm typecheck` + `pnpm exec vitest run src/app/screens/resume-workshop/` 39 文件 / 253 tests 全 PASS。

### 关联 Commit

- `feat(frontend-resume-workshop): land edit tab and structured save`

### 备注

Auto-committed by `/tdd` phase-commit, plan: frontend-resume-workshop/003-branch-rewrites-and-edit

## 00:35 工作记录

### 完成事项

- 完成 `frontend-resume-workshop/003-branch-rewrites-and-edit` Phase 5：requestResumeTailor + tailor run polling 接入 Rewrites Tab。
  - 5.1 新增 `tabs/hooks/useRequestResumeTailor.ts`，按请求体 fingerprint 缓存 IK（mode 变更轮换 / 同一表单复用 / 422 清空），`RequestResumeTailorError` 映射 `{validation, cross_user, idempotency_conflict, generic}`。
  - 5.2 新增 `tabs/hooks/useResumeTailorRunPolling.ts`：指数退避（默认 1500ms × 1.4^attempt，max 12 attempt ≈ 60s 上限），命中 `ready/failed` 即终止；attempt 触达上限切换 `timeout`；`onReady/onFailure` 回调用于上游刷新或失败提示，`retry()` 重置 attempt。
  - 5.3 polling 测试直接读取 `openapi/fixtures/ResumeTailor/getResumeTailorRun.json` 的 `queued/generating/default(ready)/failed` 四态作为 deterministic sequence，不构造 synthetic schema。
  - 5.4 `params.ts` 新增 `tailorRunId` 可选参数；`ResumeBranchFlow` ai_select 分派时把 `accepted.job.resourceId` 写入 nav `tailorRunId`；`ResumeWorkshopScreen` + `DetailWrapper` 把 `initialTailorRunId` 透传给 `ResumeDetailView`；`ResumeRewritesTabContainer` 复合 `useResumeTailorRunPolling` + `useRequestResumeTailor`，将 polling phase 映射为 `ReactPollingBanner`（polling→info，failed/timeout/error→danger + retry），ready 时调用 `onVersionRefreshed`。
  - 5.5 IK 行为：`requestResumeTailor` 必带 IK header（`/^v1\.\d+\..+/` 校验）；`getResumeTailorRun` 调用断言无 opts 参数（read-only no-IK 契约）。
  - 5.6 unmount 时 cleanup 取消 `setTimeout`，Vitest 验证 unmount 后 spy 调用次数不再增长；URL 携带 `tailorRunId` 让 tab 切回时可继续恢复 polling，无 in-flight run 丢失；Playwright network sniff 留待 Phase 7.7 BDD gate。
- 新增 `ResumeRewritesTab.test.tsx` polling banner case，覆盖 info/danger 两种 banner 渲染 + 重试 CTA 触发。
- 验证通过：`pnpm typecheck` + `pnpm exec vitest run src/app/screens/resume-workshop/` 38 文件 / 243 tests 全 PASS（Phase 5 新增 hook 测试 15 cases + RewritesTab polling 2 cases）。

### 关联 Commit

- `feat(frontend-resume-workshop): wire tailor run request and polling`

### 备注

Auto-committed by `/tdd` phase-commit, plan: frontend-resume-workshop/003-branch-rewrites-and-edit

## 00:25 工作记录

### 完成事项

- 完成 `frontend-resume-workshop/003-branch-rewrites-and-edit` Phase 4：单条 suggestion 终态状态机 + manual edit 两步链路。
  - 4.1/4.2 新增 `tabs/hooks/useTailorSuggestionDecision.ts` 共享基础 hook（按 `(versionId,suggestionId)` 缓存 IK，retry 复用 / 切换 bullet 轮换 / 422 重置），`useAcceptResumeTailorSuggestion` 与 `useRejectResumeTailorSuggestion` 仅注入 generated client 方法；`SuggestionDecisionError` 信封区分 `already_decided / cross_user / validation / generic`，并断言请求为 bodyless（argv.length === 3 + 不写入 `manualEditText`）。
  - 4.3 新增 `tabs/hooks/useResumeRewritesActions.ts` 复合 hook：`onSaveManualEdit` 先 `updateResumeVersion` 把 `{ suggestionId, text, savedAt }` 合并进 `structuredProfile.manualEdits[]`（按 `suggestionId` 去重）→ 再触发 bodyless `acceptResumeTailorSuggestion`；update 成功但 accept 失败时设置 `manualPendingFor` 并允许仅重试 accept。`ResumeDetailView` 改为渲染 `ResumeRewritesTabContainer`，将 hook 暴露为 `onAccept / onReject / onSaveManualEdit / manualEditPendingFor` 传给 `ResumeRewritesTab`，并通过 `fireResumeWorkshopToast` 输出 i18n 化 success / warn / danger 文案，不暴露原 envelope。
  - 4.4 D-12 同步：`accept/reject` 路径不会触发 `updateResumeVersion`（测试断言 spy 未被调用），不自动 patch `structured_profile`；同一 `(versionId,suggestionId)` retry 复用 IK，不同 bullet 轮换 IK，409 SUGGESTION_ALREADY_DECIDED → `kind=already_decided`。
  - 4.5 fixture parity：`accept/reject` fixture 三 scenario keys (`default / idempotency-replay / already-decided-409`) 与 409 envelope (`error.code=VALIDATION_FAILED + error.details.reason=SUGGESTION_ALREADY_DECIDED`) 通过 Vitest 直接断言；`updateResumeVersion` fixture 三 scenario keys 同步断言。
  - 4.6 cross-user (404 空 body) → `kind=cross_user`；422 → `kind=validation`；`ResumeRewritesTabContainer.showError` 将三类信封映射到 toast 文案（`alreadyDecided / crossUser / validation / generic`），原 envelope 不进入 toast 内容。
- 新增 hook 测试：`useTailorSuggestionDecision.test.tsx` 11 cases + `useUpdateResumeVersion.test.tsx` 8 cases + `useResumeRewritesActions.test.tsx` 7 cases，配合既有 ResumeRewritesTab/DetailView 测试一并验证。
- 验证通过：`pnpm typecheck` + `pnpm exec vitest run src/app/screens/resume-workshop/` 36 文件 / 228 tests 全 PASS。

### 关联 Commit

- `feat(frontend-resume-workshop): wire suggestion decision and manual edit`

### 备注

Auto-committed by `/tdd` phase-commit, plan: frontend-resume-workshop/003-branch-rewrites-and-edit

## 00:15 工作记录

### 完成事项

- 完成 `frontend-resume-workshop/003-branch-rewrites-and-edit` Phase 3：Rewrites Tab UI + getResumeVersion 投影。
  - 3.1 新增 `frontend/src/app/screens/resume-workshop/tabs/ResumeRewritesTab.tsx`，源级复刻 `ui-design/src/screen-resume-workshop.jsx` L784-940：scope banner（`role="status"` + `aria-live`）+ 左侧 `role="listbox"` bullet 列表（行级 testid `resume-rewrites-bullet-row-{id}` + status chip + 90 字符截断）+ 右侧 diff Card（reject/edit/accept 按钮 + ORIGINAL/REWRITTEN/MANUAL EDIT 三栏 + WHY 列表 + 重新运行改写 CTA），并预留 polling banner / manual-pending alert / empty 状态。`ResumeDetailView` 把 `tab=rewrites` 由 `ComingSoonTab` 替换为 `ResumeRewritesTab`。
  - 3.2 `adapters/resume.ts` 把 `UiBullet` 扩展为含 `status / decidedAt / source / tailorRunId`，新增 `normalizeBulletSource` (`'ai'` / `'manual'`)；`ResumeSuggestionInput` 接受 optional `decidedAt / source / tailorRunId`；`ResumeRewritesTab` 内的 `toSuggestionInput` 安全投影原始 suggestion record（无 sectionLabel 时回退 i18n fallback）。
  - 3.3 计数派生：`counts useMemo` 遍历 bullets，结果挂在根节点 `data-{accepted,pending,rejected,bullet}-count` 上；scope banner 文案通过 `{accepted}/{pending}/{rejected}` 占位符替换，避免硬编码。
  - 3.4 `selectedBulletId` 切换时统一调用 `handleSelect` 重置 `editing/editText`，避免上一个 bullet 的草稿留存；行预览 `truncate(90)` 显示截断 + 省略号，完整 rewritten 仅出现在 diff Card 内部 `resume-rewrites-rewritten-text`。
  - 3.5 隐私 grep 测试：spy `globalThis.fetch` / `localStorage.setItem` / `history.replaceState`，断言 originalBullet / suggestedBullet 文本不出现在 URL / localStorage / fetch transport / pendingAction。
  - 3.6 fixture parity：`ResumeRewritesTab.test.tsx` 的 `buildVersionWithSuggestions` 复刻 `getResumeVersion.json targeted-with-suggestions` suggestion 形态（`id / originalBullet / suggestedBullet / reason / status / decidedAt / tailorRunId`）；`ResumeDetailFixtureParity.test.tsx` + workshop 33 文件 / 202 tests suite 全 PASS。
- 配套修订：i18n `resumeWorkshop.rewrites.*` 新增 38 条 EN/ZH 翻译，覆盖 scope banner / list / diff / actions / rerun / polling / toast / error；`ResumeDetailView.test.tsx` 的 ComingSoon 期望改写为 `resume-rewrites-tab` 在场断言。
- 验证通过：`pnpm typecheck` + `pnpm exec vitest run src/app/screens/resume-workshop/` 33 文件 / 202 tests 全 PASS。

### 关联 Commit

- `feat(frontend-resume-workshop): land rewrites tab presentation layer`

### 备注

Auto-committed by `/tdd` phase-commit, plan: frontend-resume-workshop/003-branch-rewrites-and-edit

## 00:05 工作记录

### 完成事项

- 完成 `frontend-resume-workshop/003-branch-rewrites-and-edit` Phase 2：branchResumeVersion 三 seedStrategy 提交 + IK + nav 行为。
  - 2.1 新增 `branch/hooks/useResumeBranchSubmit.ts`：按 `(draft + parentVersionId + targetJobId)` fingerprint 缓存 `Idempotency-Key`（同表单 retry 复用、字段变更轮换、422 重置），`BranchSubmitError` 信封区分 `validation/parent_missing/target_missing/cross_user/idempotency_conflict/generic`，并通过 `job` 字段在线辨别 `ResumeVersion` 与 `BranchResumeVersionAccepted` 两态响应。
  - 2.2 `ResumeBranchFlow` 容器接入 hook 并按结果分派 nav：`copy_master/version → tab=rewrites`、`blank/version → tab=edit`、`ai_select/accepted → tab=rewrites` 且 `versionId = accepted.resumeVersionId`；toast 文案区分 copy/blank/aiSelect，使用 `fireResumeWorkshopToast`。
  - 2.3 新增 `branch/adapters/mapBranchFormToRequest.ts`：trim `displayName`、focus 字面量映射、custom focus 空回退 `custom` / 非空回退 `custom:{target}`，硬过滤所有不属于 `BranchResumeVersionRequest` 的字段（含 `versionType` / `seedStrategy` 等不可编辑映射）。
  - 2.4 hook 测试固化 `branchResumeVersion.json` 六 scenario：`default / copy-master-sync / blank-sync / ai-select-202-with-job / idempotent-replay / validation-error-422`，replay scenario 与 default 共享 canonical `resumeVersionId`。
  - 2.5 IK 行为：v1 wire format `^v1\.\d+\..+$`，同 fingerprint 二次提交命中同一 IK，焦点切换后轮换；hook 与 form 两侧 spy 都断言。
  - 2.6 失败路径：422 inline error 并重置 IK 缓存；404 PARENT_NOT_FOUND / 通用 404 / 409 IK 冲突均渲染本地化 inline alert（`resumeWorkshop.branch.error.*`），不触发 nav。
  - 附带：将 `AppRuntimeContext` 提升为 export，便于聚焦 hook 测试用 stub client 直接 mount runtime tuple，避免 `getRuntimeConfig` / `getMe` 实际请求；移除 Phase 1 临时的 `onSubmitDraft` prop，容器自驱提交。
- 验证通过：focused `branch/` Vitest 37 cases / 3 文件 + `src/app/screens/resume-workshop/` 完整 32 文件 / 190 tests / `pnpm typecheck` 全 PASS。

### 关联 Commit

- `feat(frontend-resume-workshop): wire branch submit hook and seed nav`

### 备注

Auto-committed by `/tdd` phase-commit, plan: frontend-resume-workshop/003-branch-rewrites-and-edit
