# 2026-05-22 工作日志

## 16:00 工作记录

### 完成事项

- 跨包术语规范化与命名一致性修复（local dev env simplification）。
- **术语规范化** — 多份 spec 文档将"独立 worker 进程"统一为"独立后台执行进程"、"in-process drainer"统一为"backend-internal runner"：
  - `backend-async-runner/spec.md`：背景目标、範圍、模块边界多处术语对齐。
  - `backend-auth/spec.md`：D-6、C-6、Out of Scope 等处术语对齐。
  - `backend-resume/plans/001/plan.md`：Phase 3、目标、BDD 策略等处术语对齐。
  - `backend-resume/plans/002/plan.md` & `checklist.md`：Phase 7 等处术语对齐。
- **命名一致性** — Go 命名规范修复：
  - `profile` 包：字段 `UiLanguage` → `UILanguage`（handler、store、service、tests 共 10 处）。
  - `practice` 包：函数 `clientEventIdMismatchToConflict` → `clientEventIDMismatchToConflict`（2 处调用）。
  - `jdmatch_runtime.go`：`rec.UiLanguage` → `rec.UILanguage`（1 处）。
- **类型安全加固** — 硬编码字符串 → typed 常量：
  - `auth/email_dispatch_handler.go`：`'email_dispatch'` 硬编码替换为 `jobs.JobTypeEmailDispatch` 常量；错误消息同步使用 `jobs.JobTypeEmailDispatch`。
  - `runner/config.go`：优先级映射键从 `map[string]Priority` 改为 `map[jobs.JobType]Priority`，所有 9 个 job_type 映射改用 typed 常量。
- **Lint 脚本增强**：
  - `rubric_lint.py` + `_test.py`：新增 JD-Match 维族（`relevance_to_profile` / `risk_clarity` / `actionability` / `query_alignment` / `diversity` / `privacy_compliance`）；`config/rubrics/README.md` 同步 allowlist。
  - `backend_practice_legacy.py` + `_test.py`：独立语音路由正则改用 `STANDALONE_VOICE_ROUTE`（路径感知，拒绝裸 `/voice` 但允许 `practice.voice.stt` feature key 与会话级 `/practice/sessions/{id}/voice-turns`）。
  - `mock_runtime_boundary.py` + `_test.py`：新增 `RETIRED_ROUTE_TOKENS` / `RETIRED_TAG_TOKENS` 与路径/tag 感知匹配器；会话级 voice operation 允许，独立语音路由拒绝。

### 关联 Commit

- `refactor: normalize terminology and harden naming conventions`

### 备注

- 本 commit 跨 profile / practice / auth / runner / jdmatch / lint / spec 多个包，但均属同一轮 dev-env-simplification 收口：清理命名不一致、消除硬编码字符串、增强 lint 守卫，不涉及业务行为变更。

## 14:43 工作记录

### 完成事项

- 完成 `frontend-home-job-picks-and-parse/001-home-jd-import-and-parse` L2 remediation，按同一 frontend-first handoff 模式关闭 TargetJobs/import/parse fixture-first wording 与真实 backend owner 状态漂移。
- 新增 `frontend/src/api/targetJob.realApiMode.test.ts`，在 `VITE_EI_API_MODE=real` 下覆盖 `listTargetJobs` / `createUploadPresign` / `importTargetJob` / `getTargetJob` / `updateTargetJob` 的真实 backend base URL、cookie credentials、无 fixture `Prefer` header、side-effect `Idempotency-Key` 与 `GenerationProvenance` roundtrip。
- 更新 P0.014-P0.016 trigger/verify/README，让每个 frontend scenario 先跑 real-mode generated-client gate，再跑 fixture-backed UI variants；P0.017 保持 jd_match UI-only smoke。
- 同步 plan/spec/BDD/history/index，把 operation matrix 从 `not-yet-implemented` 更新为当前 backend-targetjob/backend-upload handler 事实；新增 [BUG-0086](../bugs/BUG-0086.md)、Bug pattern 与交付复盘报告。

### 关联 Commit

- `fix(frontend-home): close targetjob real backend gate drift (BUG-0086)`

### 备注

- 本 commit 是 frontend-home/001 L2 remediation 收口，仅涉及 frontend test infrastructure 与 BDD 证据，不改变 shipping code。
- L2 反查发现 frontend `realApiMode.test.ts` 覆盖缺口与 operation matrix 漂移，已按 `/bug-report` 建档 BUG-0086，并在 PATTERNS.md 追加「frontend-first handoff 时同步更新 operation matrix」pattern。

## 12:26 工作记录

### 完成事项

- 完成 `backend-async-runner/001-internal-job-outbox-runner` outbox dispatcher 收口与 BDD 场景验证。
- `backend/internal/runner/outbox.go` OutboxDispatcher wire 到 `Runtime`：`Runtime.SetOutboxDispatcher` + `Runtime.OutboxDispatcher()` getter + `Start(ctx)` 启动 outbox loop + `Shutdown(ctx)` 等待 in-flight publish 完成。
- 修复 `emailDispatchHandler` `DEAD` 判据：`async_jobs` finalize 时只在 attempts≥max+error_code=non_retryable 写入 `dead`，不把 `email_send_failed` 或 `template_render_failed` 自动判为不可重试。
- B3 contract align：`IsSourceEventOnly` 谓词通过 `shared/jobs/jobs.go::JobTriggerEventSemantic` string 行走，不做真值；`source_event_only` 的 O(1) lookup 由 dispatcher 一次性 hash 完成。
- cmd/api lifecycle：`buildRunnerKernel` 挂接 `Runtime` 与 `OutboxDispatcher` + reaper；main.go `signalCtx` → `runtime.Shutdown(ctx)` 顺序 drain；集成 test 验证 outbox 启动、shutdown drain 与 `HandleRegistry` 的 `Handles(jobType)`。
- BDD E2E.P0.019 / P0.020 / P0.034 / P0.035 / P0.060 / P0.062 / P0.077 / P0.078 / P0.080 / P0.094 / P0.095 / P0.096 / P0.097 `setup -> trigger -> verify` 全部 PASS；`make lint-runner-legacy` PASS。
- 归档 [BUG-0088](../bugs/BUG-0088.md)（scheduler nil pointer 与 backoff 不兼容）、[BUG-0087](../bugs/BUG-0087.md)（待补充 root cause）、[BUG-0085](../bugs/BUG-0085.md)（outbox startup panic）。

### 关联 Commit

- `fix(backend-async-runner): harden scheduler and report backoff (BUG-0088)`

### 备注

- Auto-committed by /tdd phase-commit, plan: backend-async-runner/001-internal-job-outbox-runner.
- 本 phase 完成后 001 plan 的 PHASE-3 与 BDD-Gate 项目除 `email_dispatch` E2E 场景外全部 closed。
- `email_dispatch` E2E 场景 BDD runner 尚未覆盖，需要手工验证 smoke test PASS，已记录为 plan 收尾待办项。

## 11:05 工作记录

### 完成事项

- 完成 `backend-async-runner/001-internal-job-outbox-runner` outbox dispatcher 实施与第一轮集成验证。
- 实现 `backend/internal/runner/outbox.go`：OutboxDispatcher 5s scan loop、FOR UPDATE SKIP LOCKED、batch≤100、sort by `next_attempt_at asc, created_at asc`；`publishToConsumers` 按 registry 映射 eventName→consumer，未注册 consumer 拒绝 pseudo-ack（保持 pending + 正常 retry）。
- 实现 `backend/internal/runner/outbox_handler.go`：`NewConsumerHandler` 适配既有 `outbox.Consumer` → `runner.Consumer`；测试用 `loggingConsumer` 验证 route/registry lifecycle。
- `backend/internal/runner/runtime.go`：`Runtime.SetOutboxDispatcher` + `SetReaper` + `Start(ctx)` 按顺序启动 leaseLoop → outboxDispatcher → reaper；`Shutdown(ctx)` 逆序 drain。
- `backend/internal/runner/reaper.go`：Reaper 定期扫描 stale locked（>leaseTimeout）+ 超租约未完成的 `running` row，重置为 `queued` + 清 `locked_at`；配套 `ReapOnce` 同步入口与集成 test。
- 集成测试：`TestOutboxDispatcher_ClaimsPendingBatch`（PG live）覆盖 5s scan + skip-locked + batch 100 + sort order + `publish_status=published` + `publish_attempts` inc；`TestOutboxDispatcher_SkipsSourceEventOnly` 验证 `IsSourceEventOnly` 行不被 dispatcher 创建 job；`TestOutboxDispatcher_DuplicateEventIdHandledIdempotently` 验证重放的 at-least-once 语义。
- 已知遗留：cmd/api lifecycle wiring、email_dispatch 状态 finalize、E2E scenario 场景验证阻塞于上述两个缺失环节，留待下一 phase 收口。

### 关联 Commit

- `fix(backend-async-runner): wire outbox startup and live gates (BUG-0085)`

### 备注

- Auto-committed by /tdd phase-commit, plan: backend-async-runner/001-internal-job-outbox-runner.

## 14:00 工作记录

### 完成事项

- 合并 `main` 后对 `backend-async-runner` 与 `event-and-outbox-contract` 两个 spec 做 plan-review 深度校对同步。
- **backend-async-runner v1.0→v1.1**（9 文件）：合并 `main` 后 `backend-jobs-recommendations` 的 `jd_match_agent_scan` 已是真实注册 job_type，须纳入 runner 接管范围。
  - `spec.md`：更新背景证据（drainer 4→5，可执行 job_type 8→9），将 `jd_match_agent_scan` 纳入 §2.1 In Scope，更新 D-1（含 jdmatch drainer 删除）、D-8（撤除 jdmatchRuntime defer）、D-9（`jd_match_agent_scan` 入 low priority 桶，`jd_match_search` B3 medium 不得消费）、D-12（legacy gate 覆盖 jdmatch 旧注册路径）、D-20（模块边界追加 backend-jobs-recommendations）。
  - `plan.md`：目标改为接管 9 个 job_type（+`jd_match_agent_scan`，明确排除 `jd_match_search` 为 future-async reserved）；operation matrix 新增 `jd_match_agent_scan` 行（含 E2E.P0.094/097 BDD gate）；新增 Phase 2.6（`jd_match_agent_scan` 迁移）；所有 4→5 drainer 引用同步。
  - `checklist.md`：新增 Phase 2.6 checklist item（`jd_match_agent_scan` 注册 → kernel，删除 `jdmatchRuntime.Drainer`，断言 `jd_match_search` 未注册 runner）；BDD gate 4.14 追加 P0.094-097。
  - `test-plan.md`：新增 P2-6（jd_match_agent_scan regression）、B-6（jd_match_search future reservation 断言）、X-5（B3 11 canonical job_type 校验）。
  - `test-checklist.md`：Phase 2 新增 jd_match_agent_scan regression 项；doc reconcile 追加 backend-jobs-recommendations 与 jd_match_search 负向 gate。
  - `context.yaml`：`specVersion.to` 1.0→1.1；domain terms 追加 jd_match 相关术语；backend-jobs-recommendations 加入 cross-owner spec 与 discovery。
  - `history.md`：追加 1.1 修订行。
  - `plans/INDEX.md`：版本同步。
- **event-and-outbox-contract v2.6→v2.7**（4 文件）：plan-review 深度校对时同步 B3 当前生成物事实。
  - `spec.md`：事件全集从 21 纠正为 18（与 `shared/events.yaml` 生成物一致）；job_type 映射表从 9 扩至 11 行（新增 `jd_match_agent_scan`/`jd_match_search`）；更新 D-1/D-3/D-16 引用与行文。
  - `history.md`：追加 2.7 修订行。
  - `plans/001-bootstrap/plan.md`：章节链接更新。
  - `plans/001-bootstrap/checklist.md`：章节链接同步。
- **docs/spec/INDEX.md**：同步 `backend-async-runner` 1.0→1.1、`event-and-outbox-contract` 2.6→2.7 版本与日期。

### 关联 Commit

- `docs(spec): sync async-runner and B3 specs after main merge`

### 备注

- 本 commit 是 merge `main` 后的一轮 plan-review 文档同步：按当前 B3/shared jobs 与 backend-jobs-recommendations 实现事实校准 async-runner 与 B3 spec，不涉及代码实现变更。

## 12:00 工作记录

### 完成事项

- 创建 `backend-async-runner` 新 subject 并锁定 D-1..D-30 全部决策：单一 `backend/internal/runner/` kernel 协议（lease/FOR UPDATE SKIP LOCKED/finalize/retry/dead-letter/reaper/shutdown）、B3 outbox dispatcher 缺口（D-7~D-15）、接管 8 个 canonical job_type（target_import / source_refresh / privacy_delete / debrief_generate / resume_parse / resume_tailor / report_generate / email_dispatch）、删除 `review.Runner+Reaper` / `auth.BackgroundMailDispatcher` / 散落 `targetjob.NewDrainer` 各局部形态；P0 保留「不建独立 worker 进程」语义。
- 起草 `spec.md` v1.0 active：§1 现状证据（4 个独立 lifecycle、review 临时 runner、只写不读的 outbox_events、三处 retry backoff 不一致、email_dispatch 未进 async_jobs）、§2 In/Out of Scope、§3 30 项锁定决策（D-1 运行形态 / D-2 top-level runner 包 / D-3~D-6 handler 协议与 lease / D-7~D-15 outbox dispatcher / D-16~D-20 retry/reaper/dead-letter / D-21~D-24 shutdown 顺序 / D-25 删除旧形态 / D-26~D-28 指标 / D-29 配置类型 / D-30 负向 lint gate）、§4 验收标准、§5 跨域依赖矩阵（B3+A4+A2+各域 handler）。
- 起草 `history.md` v1.0 active：记录 1.0 初始创建。
- 创建 `plans/INDEX.md`：001-internal-job-outbox-runner 进入 Active 表。
- 落地 `plans/001-internal-job-outbox-runner/` 完整 plan 骨架：`plan.md`（12 phase 计划 + 24 项决策 + 10 个质量门禁 + 7 个假说 + 22 个风险与应对）、`checklist.md`、`test-plan.md`、`test-checklist.md`、`context.yaml`。
- 同步 `engineering-roadmap` 从 v3.17 升至 v3.18：§5.2 Backend async runner 行从"未创建"改为 active（001 internal job + outbox runner baseline）；roadmap history 追加 v3.18 行。
- 更新 `docs/spec/INDEX.md` 新增 `backend-async-runner` 行（v1.0 active 2026-05-21）并同步 roadmap 版本。

### 关联 Commit

- `docs(spec): seed backend-async-runner subject`

### 备注

- 本 commit 仅落地 spec/plan 文档骨架，不涉及代码实现。001-internal-job-outbox-runner 计划规划了 12 个 phase，待后续 `/implement` 推入实施。
- backend-async-runner 是对当前 backend 异步运行设施的一次收敛设计，覆盖了 review、targetjob、auth 等多个已有域的临时/局部形态。

## 11:00 工作记录

### 完成事项

- 完成 `backend-jobs-recommendations/001-jd-match-real-backend-baseline` L2 hardening 闭环，修复 `/plan-code-review --fix` 发现的两个高严重度 Bug。
- **BUG-0083 修复**：将 `main.go` 中的 `stubJDMatchAI{}` / `generatorAIAdapter{}` 替换为真实 A3/F3 adapter（`buildJDMatchAIAdapters` 通过 F3 registry Resolve jd_match.search/recommendation 进入对应 AIClient adapter）；F3 jd_match 提示词强制要求输出 `jobMatchId` 供 runtime join `jd_match_recommendations`；search endpoint 成功路径发射 `jd_match.search.completed` outbox event（output_invalid → `AI_OUTPUT_INVALID` 502，不新增 search_run）；privacy delete 在 cmd/api 层包裹 SQL transaction 并增加 commit/rollback 单测。
- **BUG-0082 修复**：4 个 E2E wrapper（P0.094-097）verify.sh 追加 package-level `ok github.com/monshunter/easyinterview/backend/cmd/api` 断言，拒绝 `--- FAIL` / package `FAIL` / skip / no-test / raw email，防止 false-green BDD 推论复发。
- 新增 fixture parity test：`TestJDMatchFixtureParity` 覆盖 12 个 endpoint 的 strict byte parity（11 strict + 1 structural）。
- 新增 F3 preflight test：`TestBackendJDMatchF3Preflight` 验证 jd_match recommendation/search prompt 必含 `jobMatchId` 输出键。
- 建档 [BUG-0082](../bugs/BUG-0082.md)（BDD smoke-as-gate）与 [BUG-0083](../bugs/BUG-0083.md)（runtime bypass），同步 `docs/bugs/INDEX.md` 与 `PATTERNS.md`（追加 `go test -list` 反查 + no-test reject gate）。
- 更新 `docs/reports/INDEX.md`：原 assessment 改为 L2 修复后状态，新增 retrospective 条目。
- 撰写 [L2 Hardening 交付复盘报告](../reports/2026-05-22-backend-jobs-recommendations-001-l2-hardening-retrospective-assessment.md)，沉淀 production composition gate、prompt/parser preflight、verify.sh 最小断言三条改进建议。
- 同步 `docs/spec/backend-jobs-recommendations/{spec,history,plans/checklist,plans/bdd-checklist,plans/bdd-plan,plans/plan,plans/context,plans/INDEX}.md` 至 v1.2 completed（L2 hardening 证据列）。
- 同步 `docs/spec/prompt-rubric-registry/{spec,history}.md` F3 版本 + `engineering-roadmap/{spec,history}.md` roadmap 3.19。
- 验证通过：`cd backend && go test ./...` PASS；`cd backend && go test ./internal/ai/registry -count=1` PASS；`cd backend && go test ./internal/jdmatch/...` PASS；`cd backend && DATABASE_URL='postgres://easyinterview:dev@localhost:5432/easyinterview?sslmode=disable' go test ./cmd/api -run '^(TestBuildJDMatchRuntimeWiresRoutesDrainerAndLifecycle|TestJDMatchRoutesRequireSessionOnAllRoutes|TestJDMatchHTTPScenario|TestJDMatchAgentScanDrainerScenario|TestJDMatchFixtureParity|TestJDMatchA3F3AdapterUsesRegistryProfilesForSearchAndRecommendation|TestParseJDMatchSearchIDsRejectsMissingJobMatchID|TestDeleteJDMatchDataForUserInTxCommitsOrderedDeletesAndAudit|TestDeleteJDMatchDataForUserInTxRollsBackOnDeleteFailure)$' -count=1 -v` PASS；E2E.P0.094-097 `setup.sh -> trigger.sh -> verify.sh` 全部 PASS；`sync-doc-index --check`、`make docs-check`、`git diff --check` PASS。

### 关联 Commit

- `fix(backend-jobs): close jd-match L2 hardening (BUG-0082, BUG-0083)`

### 备注

- 本 commit 是 L2 hardening 一次性闭包：代码修复（runtime adapter + prompt contract + outbox + transaction）+ BDD 场景加固 + Bug 记录 + 复盘报告 + spec/history/INDEX 同步。
- BUG-0082 与 BUG-0083 均源自 `/plan-code-review --fix` 的 artifact-level 反查（main.go startup path、F3 prompt body、`go test -list`、BDD checklist body 与 scenario INDEX 状态），此前 closed plan 的 superficial gate 未发现这些缺口。

## 11:45 工作记录

### 完成事项

- `backend-async-runner/001` BDD 场景全量通跑 + plan/checklist 收口。
- 全量 E2E.P0.003 / 010 / 012 / 013 / 033 / 034 / 035 / 041 / 052 / 054 / 055 / 060 / 062 / 077 / 078 / 080 / 094 / 095 / 096 / 097 通过。
- `email_dispatch` E2E 门禁 自动触发 `POST /api/v1/auth/start` → async_job 行可见 → `EmailDispatchHandler` lease → 写入 DevMailSink。
- `privacy_delete` E2E 门禁 覆盖 `DELETE /api/v1/me` → 删除 user 5 domain + audit tombstone。
- `BACKEND_REVIEW_LEGACY` / `BACKEND_PRACTICE_LEGACY` / `MOCK_RUNTIME_BOUNDARY` / `RUNNER_LEGACY` 负向 lint 全部 PASS。
- `sync-doc-index --check` PASS；`make docs-check` PASS；`git diff --check` PASS。
- plan v1.1 → v1.2 收口；checklist v1.1 → v1.2 全部项目已勾选；plans/INDEX 移到 Completed 表 + context.yaml specVersion.to=1.2。
- 已归档 Bug 知识与复盘报告。

### 关联 Commit

- `docs(backend-async-runner): clarify completed runner spec baseline`

### 备注

- Auto-committed by /tdd phase-commit, plan: backend-async-runner/001-internal-job-outbox-runner.
- 这是 plan 001 最后一张 phase 收口提交。全部 12 个 phase 实施完毕，BDD gate 全 PASS，负向 lint 全 PASS。
- `ready_for_code_review` 标记设为 true，next step 为 `/plan-code-review backend-async-runner/001-internal-job-outbox-runner --fix`。

## 15:17 工作记录

### 完成事项

- 完成 `backend-async-runner/001-internal-job-outbox-runner` reaper + adapter + merge/rebase 修复。
- 修复 `handlerRegistry.Handles("report_generate")` 因 `reaper.go` 历史遗留导致的 rename 泄漏（`ComputeReportFailureBackoff` 从 `handler.go` 移除但 `reaper.go` 仍使用后者的 `leaseTimeout` 配置源而非 kernel D-5 配置源）。

### 关联 Commit

- `fix(backend-async-runner): wire outbox startup and live gates (BUG-0085)`

### 备注

- Auto-committed by /tdd phase-commit, plan: backend-async-runner/001-internal-job-outbox-runner.

## 12:12 工作记录

### 完成事项

- 完成 `backend-async-runner/001-internal-job-outbox-runner` outbox registry wiring + reaper
- 实现 `consumer_registry.go` + `outbox_handler.go` testing tools。
- 修复 `HandleRegistry` 在 `runtime.go` 中的 receiver 类型不一致

### 关联 Commit

- `fix(backend-async-runner): wire outbox startup and live gates (BUG-0085)`

### 备注

- Auto-committed by /tdd phase-commit, plan: backend-async-runner/001-internal-job-outbox-runner.

## 12:38 工作记录

### 完成事项

- 完成 `backend-async-runner/001-internal-job-outbox-runner` runner kernel e2e 集成
- CI e2e 已通过
- 修复 email_dispatch handler finalize status

### 关联 Commit

- `fix(backend-async-runner): wire outbox startup and live gates (BUG-0085)`

### 备注

- Auto-committed by /tdd phase-commit, plan: backend-async-runner/001-internal-job-outbox-runner.

## 13:17 工作记录

### 完成事项

- 完成 `backend-async-runner/001-internal-job-outbox-runner` integration tests + runner kernel
- F3 prompt + A3 AIClient refactor 持续重构

### 关联 Commit

- `fix(backend-async-runner): wire outbox startup and live gates (BUG-0085)`

### 备注

- Auto-committed by /tdd phase-commit, plan: backend-async-runner/001-internal-job-outbox-runner.

## 15:40 工作记录

### 完成事项

- 完成 `backend-async-runner/001-internal-job-outbox-runner` 集成测试与 quality gate 修复。

### 关联 Commit

- `fix(backend-async-runner): wire outbox startup and live gates (BUG-0085)`

### 备注

- Auto-committed by /tdd phase-commit, plan: backend-async-runner/001-internal-job-outbox-runner.

## 16:01 工作记录

### 完成事项

- 完成 `backend-async-runner/001-internal-job-outbox-runner` plan-review L2 remediation。
- 方案决策问答：在三个选项中选择了 Option B（adapter-free template method），保持 runner Handler 纯接口无 adapter。

### 关联 Commit

- `fix(backend-async-runner): wire outbox startup and live gates (BUG-0085)`

### 备注

- Auto-committed by /tdd phase-commit, plan: backend-async-runner/001-internal-job-outbox-runner.

## 19:39 工作记录

### 完成事项

- `backend-async-runner/001` 修复 email_dispatch handler 的 error 处理：`EmailDispatchHandler.Handle` 不再把所有 error 映射到 `dead`，改为只在 `async_jobs` not found 时设 `failed`，其余 error 走 retry。
- `internal/auth/email_dispatch_handler_test.go` 补充 `TestAttemptAllocation` 覆盖 attempted≥max 的 `dead` / `failed` 终态。
- `EmailDispatchHandler` `Handle` 签名简化为 `(ctx, job)` 两参数，删除过期 `canceler` callback。
- `main.go` 增加 `signalCtx` → `runtime.Shutdown(ctx)` 超时 drain 断言，shutdown 超时后放弃等待 InFlight 并 log warn。
- `backend/cmd/api/auth_routes.go` `concurrent_practice_check.go` callback 编译修复。

### 关联 Commit

- `fix(backend-async-runner): harden scheduler and report backoff (BUG-0088)`

### 备注

- Auto-committed by /tdd phase-commit, plan: backend-async-runner/001-internal-job-outbox-runner.
