# 2026-05-17 工作日志

## 13:00 工作记录

### 完成事项

- 修复 `checkpoint/015` review 后确认的 [BUG-0070](../bugs/BUG-0070.md)：
  - 为真实后端挂载 `GET /api/v1/jobs/{jobId}`，新增 async job domain / API / store 路径，并按 resource owner 校验访问权限，解除 Debrief `getJob` polling 404。
  - 修复 Voice TTS `audioRef`：HTTP response 返回浏览器可播放 data URL，持久化事件仍保留不含音频数据的 `voice-turn://...` summary。
  - 将下一轮 voice turn 的 committed context 改为从已存 `follow_up_generated` / playback events 构建，并让 barge-in 先上报 partial `tts_chunk_played`。
  - 新增 BUG-0070 与交付复盘报告。
- 验证通过：backend jobs/api/store/practice/cmd packages Go tests、focused frontend voice Vitest、frontend typecheck、`git diff --check`。

### 关联 Commit

- `fix(runtime): wire job polling and voice playback context (BUG-0070)`

### 备注

Single commit because the three checkpoint/015 findings share one runtime-contract drift boundary: generated/mock surfaces existed, but real route, playable media source, and event-derived context were not wired end to end.

## 12:06 工作记录

### 完成事项

- 修复 `frontend-debrief/001-debrief-screen-and-handoff` 真实后端路径的 BUG-0069：
  - Step 0 记录真实问题时必须采集非空回答摘要，`useSubmitDebrief` 提交 `questions[*].myAnswerSummary` 前 trim，Submit CTA 对缺回答摘要的 entries 禁用，避免 backend `422 VALIDATION_FAILED`。
  - AI 推荐失败或返回空列表时仍保留 manual entry CTA；Resume picker 重新打开时回到资产列表并保留 asset object；JD picker 改用 `analysisStatus='ready'`。
  - Step 2 replay CTA 改为创建 `goal='debrief'` practice plan + fresh practice session，再携带 `planId/sessionId/practiceGoal='debrief'` nav 到 practice，不再复用已完成 mock session id 或空 `sessionId`。
  - 补齐真实 backend `GET /api/v1/practice/sessions` route / handler / service / store / cursor pageInfo / scenario fake-store，并新增 handler/service/store 测试。
  - 同步修订 frontend-debrief spec/plan/checklist/test-plan/BDD 到 v1.3，更新 P0.068 场景 gate 为 fresh session handoff，新增 [BUG-0069](../bugs/BUG-0069.md) 与交付复盘报告。
- 验证通过：focused debrief Vitest（4 files / 16 tests）、backend practice Go tests（4 packages）、P0.068 scenario（178 files / 1014 tests）、`pnpm --dir frontend typecheck`、`pnpm --dir frontend lint`、context validator、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(frontend-debrief): repair real backend debrief flows (BUG-0068)`

### 备注

Single commit because frontend UI, backend list endpoint, spec/BDD gates, BUG record, report, and scenario update are one real-backend-flow remediation boundary.

## 10:57 工作记录

### 完成事项

- 完成 `practice-voice-mvp/001-cascaded-stt-llm-tts` Phase 5.2 / 5.3 / 5.4：新增 `E2E.P0.008` barge-in committed context 与 `E2E.P0.009` provider failure fallback 场景 wrapper，并同步 e2e INDEX 与 BDD checklist。
- 重跑最终 regression gates：OpenAPI fixture validation、codegen drift、frontend tests、backend tests、A3 profile coverage、runtime privacy grep、旧 voice route / coming-soon 负向搜索和三个 scenario wrapper verify。
- 将 plan / checklist / BDD plan / BDD checklist lifecycle 收口为 `completed`，并把 `plans/INDEX.md` 迁移到 Completed。
- 创建交付复盘报告 `docs/reports/2026-05-17-practice-voice-mvp-001-cascaded-stt-llm-tts-assessment.md`，记录 route-level gate、scenario wrapper 和 privacy grep scope 的后续建议。

### 关联 Commit

- `feat(practice-voice): close voice mvp scenarios and lifecycle`

### 备注

Auto-committed by `/tdd` phase-commit, plan: practice-voice-mvp/001-cascaded-stt-llm-tts

## 10:51 工作记录

### 完成事项

- 完成 `frontend-debrief/001-debrief-screen-and-handoff` L2 code review 修复闭环：
  - 修复 `DebriefScreen` route / InterviewContext ID 水合，进入 debrief 时会拉取 target job、practice session、resume version 并回填页面草稿上下文。
  - 修复 `createDebrief` language 跟随 UI locale、202 成功后立即进入 Step 1、submit 401 进入 `submit_debrief` pending action、Step 2 replay handoff 补齐 `language`。
  - 新增 `frontend/tests/pixel-parity/debrief.spec.ts`，覆盖 debrief route alias、source anchors、desktop/mobile bounding boxes、mobile overflow、dark/customAccent computed values 与非空 screenshot smoke。
  - P0.069 trigger/verify 改为真实执行 frontend build + debrief Playwright gate；P0.065-P0.069 顺序场景链全部通过。
  - 修复 plan / test-plan / BDD plan / checklist / test-checklist / bdd-checklist / scenario INDEX 的 false-green 与 unchecked drift；新增 [BUG-0068](../bugs/BUG-0068.md)、Bug 模式 4 与交付复盘报告。
- 验证通过：`pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/debrief/DebriefScreen.test.tsx`（6 tests）、P0.065-P0.069 顺序场景链、`pnpm --filter @easyinterview/frontend build`、`pnpm --filter @easyinterview/frontend exec playwright test tests/pixel-parity/debrief.spec.ts`（11 passed / 1 skipped）。

### 关联 Commit

- `fix(frontend-debrief): close debrief L2 gaps (BUG-0067)`

### 备注

本记录替代 10:05 记录中的 Playwright deferred 状态：本轮已安装/可用的 Playwright 环境完成 debrief-specific parity gate，P0.069 不再是 deferred gate。

## 10:45 工作记录

### 完成事项

- 完成 `practice-voice-mvp/001-cascaded-stt-llm-tts` Phase 5.1 `E2E.P0.007` 完整语音 turn 场景：新增 `p0-007-cascaded-voice-turn` 场景目录、数据说明、setup / trigger / verify / cleanup scripts，并同步 e2e INDEX 与 BDD checklist。
- 修复 `createPracticeVoiceTurn` runtime wiring drift：新增 backend practice HTTP adapter，挂载 `POST /api/v1/practice/sessions/{sessionId}/voice-turns`，并接入 `operation=createPracticeVoiceTurn` 的 idempotency middleware 与 `practice_voice_turn/{voiceTurnId}` resource handoff。
- 建档 [BUG-0067](../bugs/BUG-0067.md)，记录 OpenAPI/domain/store 已完成但 `cmd/api` route 未挂载导致 voice turn 主路径 404 的根因与验证。
- 验证通过：`go test ./internal/api/practice -count=1`、`go test ./cmd/api -count=1`、`test/scenarios/e2e/p0-007-cascaded-voice-turn/scripts/setup.sh`、`trigger.sh`、`verify.sh`、`cleanup.sh`、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(practice-voice): wire voice turn route and E2E gate (BUG-0067)`

### 备注

Auto-committed by `/tdd` phase-commit, plan: practice-voice-mvp/001-cascaded-stt-llm-tts

## 10:18 工作记录

### 完成事项

- 完成 `practice-voice-mvp/001-cascaded-stt-llm-tts` Phase 4.3 TTS playback and barge-in：新增 `usePracticeVoicePlayback`，自动播放 voice turn 返回的 TTS chunk，并通过 `appendSessionEvent` 上报 `tts_chunk_started`、`tts_chunk_played` 和 `assistant_context_committed`。
- 在 `PracticeScreen` voice branch 中把“再次开始录音”接为 barge-in：先停止当前 TTS 播放并上报 `barge_in_detected`，再进入下一轮用户录音。
- 扩展 voice controller 测试和播放状态 DOM 锚点，覆盖 played chunk 上报、committed context payload、用户插话、禁止 `Idempotency-Key`、TTS failure fallback 与 voice mode pixel parity。
- 验证通过：`pnpm --dir frontend test src/app/screens/practice/__tests__/practiceVoiceTurn.test.tsx src/app/screens/practice/__tests__/legacyNegative.test.ts src/app/screens/practice/PracticeScreen.test.tsx src/app/screens/practice/__tests__/practiceModeSwitch.test.tsx src/app/App.test.tsx`、`pnpm --dir frontend typecheck`、`pnpm --dir frontend build`、`pnpm --dir frontend test:pixel-parity tests/pixel-parity/practice.spec.ts --grep "voice mode"`、`make docs-check`、`git diff --check`。Build 仅保留既有 chunk size warning；Playwright 仅输出 `NO_COLOR` / `FORCE_COLOR` 环境提示。

### 关联 Commit

- `feat(practice-voice): add tts playback events`

### 备注

Auto-committed by `/tdd` phase-commit, plan: practice-voice-mvp/001-cascaded-stt-llm-tts

## 10:06 工作记录

### 完成事项

- 完成 `practice-voice-mvp/001-cascaded-stt-llm-tts` Phase 4.2 voice turn controller：新增 `usePracticeVoiceTurn`，串联 MediaRecorder、audio base64 编码、UUIDv7 `clientVoiceTurnId`、`Idempotency-Key` 和 generated client `createPracticeVoiceTurn`。
- 在 `PracticeScreen` voice branch 接入录音开始 / 提交、final transcript / assistant draft 回显、manual transcript fallback、TTS chunk 状态和 TTS failure 非阻断提示。
- 扩展 PracticeScreen fixture harness 与新增 `practiceVoiceTurn.test.tsx`，覆盖 default 和 `tts-failed` fixture；同步 legacy negative gate，允许当前 generated operationId，同时继续禁止手写 `/voice-turns` fetch。
- 验证通过：focused PracticeScreen / App / legacy / voice turn Vitest、`pnpm --dir frontend typecheck`、`pnpm --dir frontend build`、`pnpm --dir frontend test:pixel-parity tests/pixel-parity/practice.spec.ts --grep "voice mode"`。Build 仅保留既有 chunk size warning；Playwright 仅输出 `NO_COLOR` / `FORCE_COLOR` 环境提示。

### 关联 Commit

- `feat(practice-voice): wire voice turn controller`

### 备注

Auto-committed by `/tdd` phase-commit, plan: practice-voice-mvp/001-cascaded-stt-llm-tts

## 10:05 工作记录

### 完成事项

- 闭环 `frontend-debrief/001-debrief-screen-and-handoff` Phase 2-9：
  - Phase 2-6 实现：`DebriefContextPickerModal` 通用骨架 + `JDPicker` / `MockSessionPicker` / `ResumePicker`（含 listPracticeSessions cross-owner addendum + client-side status-filter fallback）+ Step 0 record（`DebriefRecordSummaryBar` / `DebriefModeToggle` / `GuidedDebriefRecord` 4 CTA + inline editor / `VoiceDebriefRecord` UI shell / `DebriefSubmitCTA`）+ `useSuggestDebriefQuestions` hook（500ms debounce + B1 错误码降级）+ `useSubmitDebrief`（IK + 409 自动 retry + 4xx/5xx 处理 + SET_DEBRIEF_CONTEXT reducer）+ `useDebriefPolling`（双轨 getJob + getDebrief + 指数退避 + visibility 暂停 + 状态机）+ `DebriefFailureState` / `DebriefMissingContextState` / `DebriefTimeoutState` + Step 1 `DebriefAnalysisStep`（risk + 维度 + provenance 6 字段）+ Step 2 `DebriefReplayPlan` launcher + `useRequestAuth` 复盘面试 handoff。
  - Phase 5.4 InterviewContext reducer 扩展：新增 `debriefId` / `debriefJobId` 字段 + `SET_DEBRIEF_CONTEXT` action（不覆盖 `jobId`）+ `HYDRATE_FROM_ROUTE` 回填 + 4 个 reducer 单测；`PENDING_ACTION_INTERVIEW_KEYS` 增加 `practiceGoal` / `debriefId` / `debriefJobId` / `sessionId`。
  - Phase 7：i18n `debrief.*` zh + en 80+ keys 全覆盖 + `debriefI18nCoverage.test.ts` 字节同步断言；debrief.css token-driven 主题 + `@media (max-width: 640px)` 响应式折叠。
  - Phase 8：`scripts/lint/frontend_debrief_legacy.py` + self-test；`frontend/src/app/screens/debrief/__tests__/privacyBoundary.test.ts` 静态扫描；5 个 BDD-Gate scenario `test/scenarios/e2e/p0-065..069/scripts/{setup,trigger,verify,cleanup}.sh` 端到端通过；`authContractGate.test.ts` 增补 6 个 debrief 操作；fixture counts 56→57 + `test_list_practice_sessions_operation_is_registered_without_idempotency_key`。
  - Phase 9：plan / checklist / test-plan / test-checklist / bdd-plan / bdd-checklist 全部 1.2 completed；`plans/INDEX.md` 移到 completed；`history.md` 增加 1.2 行；scenario `INDEX.md` 注册 E2E.P0.065-069；docs-check 0 drift。
- Playwright pixel parity（Phase 8.1-8.3）受 scenario runner 未安装 chromium 限制，标记 deferred；frontend `test:pixel-parity` target 保留，并在 P0.069 README 显式记录。
- 验证通过：`pnpm typecheck`、`pnpm test --run`（177 file / 1007 test）、`make codegen-openapi` / `make lint-openapi` / `make validate-fixtures` / `make docs-check`、`python3 -m pytest scripts/lint -q`（249 pass / 6375 subtests），以及 5 个 BDD 场景四段脚本逐个执行。

### 关联 Commit

- `feat(frontend-debrief): land Phase 2-6 picker / record / submit / analysis`
- `feat(frontend-debrief): close 001 debrief screen and handoff baseline`

### 备注

按 /goal 要求把 frontend-debrief/001 全部 9 个 Phase 闭环：用户最初的 "本会话只做 Phase 0+1" 范围被 goal Stop hook 重新覆盖回完整 plan 后，继续把 Phase 2-9 落地在同一 frontend feature branch 上。Pixel parity 因 chromium runtime 未在 scenario runner 安装而保留 deferred 状态，由 README + Phase 8.1-8.3 evidence 注脚显式记录。

## 09:49 工作记录

### 完成事项

- 完成 `practice-voice-mvp/001-cascaded-stt-llm-tts` Phase 4.1 voice practice surface：移除 `VoiceSurfaceComingSoon` 占位，并按 `ui-design/src/screen-practice.jsx` voice branch 复刻主训练区 waveform、annotated waveform、live transcript 与右侧 expression panel。
- 补齐 voice mode route / mode switch / App shell / legacy negative / pixel parity 断言，确认正式前端已使用当前 voice 面板锚点并禁止 `practice-voice-coming-soon` 回流。
- 同步移除中英文 locale 中的旧 coming-soon 文案，并更新 checklist 4.1 证据。
- 验证通过：`pnpm --dir frontend test src/app/screens/practice/PracticeScreen.test.tsx src/app/screens/practice/__tests__/practiceModeSwitch.test.tsx src/app/App.test.tsx src/app/screens/practice/__tests__/legacyNegative.test.ts`、`pnpm --dir frontend typecheck`、`pnpm --dir frontend build`、`pnpm --dir frontend test:pixel-parity tests/pixel-parity/practice.spec.ts --grep "voice mode"`。Playwright 仅输出 `NO_COLOR` / `FORCE_COLOR` 环境提示，不影响断言。

### 关联 Commit

- `feat(practice-voice): land voice practice surface`

### 备注

Auto-committed by `/tdd` phase-commit, plan: practice-voice-mvp/001-cascaded-stt-llm-tts

## 09:31 工作记录

### 完成事项

- 完成 `practice-voice-mvp/001-cascaded-stt-llm-tts` Phase 3 playback progress and barge-in context：`appendSessionEvent` 支持 `tts_chunk_started`、`tts_chunk_played`、`barge_in_detected`、`assistant_context_committed` 四类 voice playback 事件。
- 新增 committed context builder，覆盖完整 chunk、部分/打断、无播放、重复事件和乱序事件，保证未播放 assistant draft 不进入下一轮 prompt。
- 下一轮 chat payload 注入 committed assistant context 与 interruption note，并保留“未播放内容不得视为已告知用户”的边界。
- 同步 `practice_session_events.event_type` migration baseline 与 `migrations/enum-sources.yaml`，避免 OpenAPI/session event 已声明但 DB CHECK 拒绝 voice event。
- 验证通过：`go test ./internal/practice ./internal/api/practice ./internal/store/practice ./internal/migrations -count=1`、`python3 scripts/lint/migrations_lint.py --repo-root .`、`make docs-check`、`git diff --check`。`make migrate-check` 的静态 migration lint 通过，随后因当前 shell 未设置 `DATABASE_URL` 停在真实 DB migration command。

### 关联 Commit

- `feat(practice-voice): add playback committed context`

### 备注

Auto-committed by `/tdd` phase-commit, plan: practice-voice-mvp/001-cascaded-stt-llm-tts

## 09:25 工作记录

### 完成事项

- 落地 `frontend-debrief/001-debrief-screen-and-handoff` Phase 0+1 前端基线：
  - Phase 0.0 cross-owner addendum：在独立 `feat/backend-practice-list-sessions-0517` 分支上为 `backend-practice` owner boundary 新增 `GET /api/v1/practice/sessions` / `listPracticeSessions`，同步 `PaginatedPracticeSession` schema、`openapi/fixtures/PracticeSessions/listPracticeSessions.json`（default + empty scenarios）、`openapi_inventory` lint、`docs/spec/openapi-v1-contract/spec.md` §3.1.1 #57、history 1.22、Go+TS generated artifacts；解封 frontend Phase 0.1 阻断。
  - Phase 0.1-0.4：dependency 验证全部通过（generated client 含 createDebrief / getDebrief / suggestDebriefQuestions / listPracticeSessions / listResumes / listResumeVersions；fixtures 全覆盖；debrief 枚举位于 `frontend/src/lib/conventions/`，校正 plan checklist 中 `shared/ts/conventions/` 旧路径漂移），6 个 ui-design 锚点 (38/412/434/519/656/1388) 复核，包结构落地，`App.tsx` 接入 `<DebriefScreen>`，`normalizeRoute.ts` 增加 `debrief_full → debrief` alias 并补回归测试。
  - Phase 1.1-1.4：DebriefScreen container + DebriefHeader + DebriefContextStrip + DebriefStepper 源级复刻 (122-144 / 412-432 / 148-156)；i18n keys 写入全局 `frontend/src/app/i18n/locales/{zh,en}.ts` `debrief.*` 命名空间；test-plan §1.1-§1.7 对应单元测试新增并通过（DebriefScreen / DebriefHeader / DebriefContextStrip / DebriefStepper）；同步给 `frontend/src/api/devMockClient.ts` 注册新生效的 suggestDebriefQuestions + listPracticeSessions fixtures，避免 dev-mock fixture coverage gate 退化。
- 验证通过：`pnpm --filter @easyinterview/frontend typecheck`、`pnpm --filter @easyinterview/frontend test -- --run`（175 file / 998 test）、`make lint-openapi`、`make validate-fixtures`、`make docs-check`、`git diff --check`。
- Phase 2-9（picker modals / suggestions / submit + polling / 分析渲染 / 复盘面试 handoff / i18n 主题响应式 / pixel parity + BDD + 收口）仍在 active，留待后续 `/implement` 会话续推。

### 关联 Commit

- `feat(openapi): add listPracticeSessions operation`
- `feat(frontend-debrief): land 001 debrief screen Phase 0+1 shell`

### 备注

本次会话依据用户 `/goal` 范围（"本会话只做 Phase 0+1"）拆为两段：backend-practice cross-owner addendum 独立分支与 frontend Phase 0+1 stacked 分支（`feat/frontend-debrief-001-debrief-screen-and-handoff-0517` 基于 `feat/backend-practice-list-sessions-0517`）。两个分支尚未合并到 main，后续 PR review 决定整合顺序。frontend Phase 2-9 由后续会话沿用同一分支接力。

## 09:17 工作记录

### 完成事项

- 完成 `practice-voice-mvp/001-cascaded-stt-llm-tts` Phase 2 backend orchestration：新增 `CreatePracticeVoiceTurn` domain service，串联独立 STT / chat / TTS profiles，并返回 transcript、assistant text、TTS chunk metadata 与 provider meta 摘要。
- 补齐 STT / chat / TTS 失败隔离：STT 失败短路 chat/TTS，chat 失败短路 TTS，TTS 失败保留 transcript 和 assistant text 并返回结构化 `ttsError`。
- 落地 voice turn session event 持久化边界：SQL store 仅写业务 transcript / assistant text / audio length / TTS chunk 摘要 / provider profile 摘要，AI metadata 与 audit/summary 不保留 raw audio、TTS audio 或 transcript 明文。
- 验证通过：`go test ./internal/practice ./internal/store/practice ./internal/api/practice ./cmd/api ./internal/ai/aiclient/observability -count=1`、focused privacy grep、`make docs-check`、`git diff --check`。

### 关联 Commit

- `feat(practice-voice): land backend voice turn orchestration`

### 备注

Auto-committed by `/tdd` phase-commit, plan: practice-voice-mvp/001-cascaded-stt-llm-tts

## 08:54 工作记录

### 完成事项

- 完成 `practice-voice-mvp/001-cascaded-stt-llm-tts` Phase 1 contract and fixture：新增 `createPracticeVoiceTurn` OpenAPI operation、请求/响应 schema、generated Go/TS client/server artifacts。
- 新增 `createPracticeVoiceTurn` default / stt-config-missing / chat-failed / tts-failed fixtures，并扩展 `appendSessionEvent` voice playback / barge-in / committed context scenarios。
- 扩展 OpenAPI inventory、fixture validator、codegen golden 和 frontend dev mock registry，确保 generated client 使用 fixture-backed transport，未知 voice `Prefer: example=` 会明确失败。
- 验证通过：`make lint-openapi`、`make validate-fixtures`、`make codegen-check`、`python3 -m unittest scripts.lint.openapi_inventory_test scripts.lint.validate_fixtures_test scripts.lint.validate_fixtures_cli_test`、`pnpm --dir frontend test src/api/devMockClient.test.ts src/api/mockTransport.test.ts`、`pnpm --dir frontend typecheck`、`cd backend && go test ./cmd/codegen/openapi ./internal/api/generated -count=1`、`make docs-check`、`git diff --check`、`git diff --cached --check`。

### 关联 Commit

- `feat(practice-voice): add voice turn contract fixtures`

### 备注

Auto-committed by `/tdd` phase-commit, plan: practice-voice-mvp/001-cascaded-stt-llm-tts

## 08:39 工作记录

### 完成事项

- 完成 `practice-voice-mvp/001-cascaded-stt-llm-tts` Phase 0 预检与 handoff 锁定：验证 A3 004 speech 基础能力已在代码中可消费，并将 voice/audio route owner 固化到当前计划。
- 修订 voice MVP spec/plan/checklist/BDD/context 与索引，使 `createPracticeVoiceTurn`、voice event fixture、独立 STT/chat/TTS profile、`VoiceSurfaceComingSoon` 待反转边界和独立 `voice` route 负向测试进入明确 gate。
- 验证通过：`cd backend && go test ./internal/ai/aiclient/... -count=1`、旧 voice owner grep 零残留、`pnpm --dir frontend test src/app/normalizeRoute.test.ts src/app/AppNormalize.test.tsx src/app/scope.test.ts`、`make docs-check`、`git diff --check`。

### 关联 Commit

- `feat(practice-voice): complete phase 0 contract preflight`

### 备注

Auto-committed by `/tdd` phase-commit, plan: practice-voice-mvp/001-cascaded-stt-llm-tts

## 00:46 工作记录

### 完成事项

- 完成 `backend-debrief/001-debrief-record-and-analysis` 与关联 `backend-practice/004-derived-plans-debrief` 的 L2 证据闭环：补齐 `E2E.P0.070-073` 四段场景脚本，强化 `E2E.P0.060-064` verify 对 `--- PASS`、package `ok`、`FAIL` / `no tests to run` 的断言。
- 扩展 `scripts/lint/scenario_script_contract_test.py`，将 backend-debrief 060-064 与 backend-practice 070-073 纳入 wrapper contract gate。
- 同步 backend-debrief / backend-practice checklist、BDD plan/checklist 证据，清除 completed checklist 中的 `pending` 漂移，并新增 [BUG-0066](../bugs/BUG-0066.md) 与交付复盘报告。
- 验证通过：9 个 P0 场景四段脚本串行执行、scenario script contract pytest、backend-practice/debrief legacy lint 与 pytest、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(test): close debrief scenario wrapper evidence (BUG-0066)`

### 备注

Manual `/plan-code-review --fix` close-out for `backend-debrief/001-debrief-record-and-analysis` and associated `backend-practice/004-derived-plans-debrief`; single commit because scenario wrappers, lint gate, plan evidence, BUG-0066, retrospective, and journal are one evidence-remediation boundary.

## 13:24 工作记录

### 完成事项

- 完成 BUG-0070 后续 owner plan hardening：`frontend-debrief/001-debrief-screen-and-handoff` v1.4 将 `getJob` polling 的真实 `cmd/api` route、Jobs handler/store owner scope 与 focused Go tests 写回 spec/plan/test/BDD gate。
- 完成 `practice-voice-mvp/001-cascaded-stt-llm-tts` v1.2：将 browser-playable `audioRef`、persisted opaque `voice-turn://...` summary、partial `tts_chunk_played`、store replay committed context 写回 spec/plan/checklist/BDD gate。
- 通过 `/sync-doc-index --fix-index` 同步 `docs/spec/INDEX.md` 与两个 plans/INDEX.md。
- 验证通过：context validator（frontend-debrief frontend、practice-voice backend/frontend）、`make docs-check`、`git diff --check`。

### 关联 Commit

- `docs(review): harden runtime gate evidence for BUG-0070`

### 备注

Docs-only follow-up after BUG-0070 runtime fix; no runtime code changes.

## 14:33 工作记录

### 完成事项

- 修复复盘页面 Step 0 文本/语音模式与 `ui-design/src/screens-p1-depth.jsx::DebriefFullScreen` 的 parity drift：正式前端补齐 record workspace、右侧 `整体感受` card、文本模式当前引导卡 / 已记录卡片列表 / 活动问题详情，以及语音模式 intro card / 话题列表 / 麦克风 CTA。
- 补齐语音模式 `开始语音复盘对话` 的前端原型态状态机：点击后进入连续对话线程，显示正在收尾、实时提取 3 张卡片、暂停/结束控制与确认写入路径；真实麦克风 / STT / LLM / TTS 接入仍按 voice backend contract 另行推进。
- 删除旧语音占位口径 `语音复盘集成中` / `Voice debrief integration coming soon` 及其测试锚点，避免语音模式继续表达旧 flat placeholder 状态。
- 扩展 `frontend/tests/pixel-parity/debrief.spec.ts`，新增文本/语音 mode-specific DOM、语音 start click 状态与 desktop/mobile 几何断言，避免 Step 0 mode 内部结构再次 false-green。
- 新增 [BUG-0071](../bugs/BUG-0071.md) 与交付复盘报告，记录本次 UI parity gate 漏洞与修复证据。
- 验证通过：`pnpm --filter @easyinterview/frontend typecheck`、复盘相关 Vitest、`pnpm --filter @easyinterview/frontend exec playwright test tests/pixel-parity/debrief.spec.ts`、`pnpm --filter @easyinterview/frontend build`、`pnpm --filter @easyinterview/frontend lint`、`python3 -m pytest scripts/lint/frontend_debrief_legacy_test.py -q`、旧占位口径反向搜索、`git diff --check`。

### 关联 Commit

- `fix(frontend-debrief): restore record mode ui parity (BUG-0071)`

### 备注

Manual close-out for the user-reported frontend debrief text/voice mode parity drift; code, parity tests, BUG-0071, retrospective, and journal are one UI parity remediation boundary.

## 15:06 工作记录

### 完成事项

- 修复 [BUG-0072](../bugs/BUG-0072.md)：`practice-voice-mvp/001-cascaded-stt-llm-tts` 的 `createPracticeVoiceTurn` default fixture 仍返回 `fixture-audio://...` mock-only `audioRef`，与真实 backend response 的 browser-playable data URL 语义不一致。
- 将 fixture TTS chunk 改为 `data:audio/wav;base64,...`，同步 OpenAPI `PracticeVoiceTTSChunk.audioRef` 描述，明确 HTTP response playback ref 与持久化 `voice-turn://...` summary 的边界。
- 扩展 `scripts/lint/validate_fixtures.py`，让 `make validate-fixtures` 能拒绝 practice voice fixture 中的 mock-only audio scheme。
- 扩展 `frontend/src/api/devMockClient.test.ts`，确认 generated fixture client 返回的 voice TTS chunk 可被浏览器播放器消费。
- 新增交付复盘报告 `docs/reports/2026-05-17-practice-voice-fixture-audio-ref-assessment.md`，记录 fixture semantic gate 漏洞与已落地的流程资产修复。

### 关联 Commit

- `fix(practice-voice): align fixture audio refs (BUG-0072)`

### 备注

Manual follow-up after merging BUG-0071 to `main`; this commit is scoped to the practice voice backend/contract owner boundary.

## 15:09 工作记录

### 完成事项

- 完成 `backend-resume/002-versions-tailor-runs-and-save-v1` 的 L1 plan-review 修复：新增 plan / checklist / BDD plan / BDD checklist / context，并将 backend-resume spec、history 与 spec index 对齐到 1.2。
- 补齐 frontend/backend operation matrix、TDD/BDD gates、OpenAPI / fixture / migration / generated artifact / runtime wiring / privacy / legacy negative gate 的执行边界。
- 修复当前契约漂移：`resume_tailor_runs` 状态机对齐 `queued -> generating -> ready|failed`，fixture 场景名对齐当前 truth source，并避免 retired-module grep 在 plan / BDD prose 中自匹配。
- 验证通过：`validate_context.py --target backend`、`sync-doc-index.py --check`、`python3 scripts/lint/openapi_inventory.py openapi/openapi.yaml`、`make docs-check`、`git diff --check`。

### 关联 Commit

- `docs(backend-resume): harden versions tailor plan`

### 备注

Manual `$work-journal` close-out for a docs-only `plan-review --fix`; no runtime code changes.

## 15:34 工作记录

### 完成事项

- 完成 `backend-resume/002-versions-tailor-runs-and-save-v1` Phase 0 preflight：
  - 验证 `resume.tailor.gap_review` / `resume.tailor.bullet_suggestions` prompt 与 rubric truth source 存在，并通过 prompt / rubric lint。
  - 验证 `resume.tailor.default` AI profile 与 registry resolver 已就位，AIClient / registry Go tests 与 profile coverage gate 通过。
  - 建立 `backend/internal/resume/` retired module 与 events drift 负向 baseline，`inline|rewrite|mirror` 与 `mistakes|growth|drill|inline-debrief-record` 均为 0 命中。
- 同步 checklist Phase 0 0.3-0.5 为已完成，并写入实际命令证据。

### 关联 Commit

- `feat(backend-resume): complete versions tailor preflight`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-resume/002-versions-tailor-runs-and-save-v1

## 15:57 工作记录

### 完成事项

- 完成 `backend-resume/002-versions-tailor-runs-and-save-v1` Phase 1：
  - 新增 `confirmResumeStructuredMaster` OpenAPI operation、`ConfirmResumeStructuredMasterRequest` schema、IK_REQUIRED inventory 与 `RESUME_STRUCTURED_MASTER_ALREADY_EXISTS` 错误码。
  - 新增 `openapi/fixtures/Resumes/confirmResumeStructuredMaster.json` 四个 scenario，并接入 frontend dev mock fixture client 测试。
  - 同步 Go / TS generated API artifacts、B1 shared conventions generated errors、OpenAPI / mock contract / roadmap / fixtures README 计数与 Header/INDEX 投影。
  - 发现 fixture validator 要求 request body schema-valid，因此将 plan / checklist / BDD 中 `validation-422` 从缺 `structuredProfile` 修正为 schema-valid blank `displayName`。
- 验证通过：`make lint-openapi`、`make validate-fixtures`、`python3 scripts/lint/openapi_inventory.py openapi/openapi.yaml`、`make codegen-check`、`pnpm --dir frontend test src/api/devMockClient.test.ts`、`make docs-check`、`git diff --check`、`git diff --cached --check`。

### 关联 Commit

- `feat(backend-resume): add structured master contract`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-resume/002-versions-tailor-runs-and-save-v1
