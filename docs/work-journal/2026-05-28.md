# 2026-05-28 工作日志

## 10:00 工作记录

### 完成事项

- **后端认证重构**：简化 `StartEmailChallenge`，移除 `Purpose`（signup/login 分离）和 `DisplayName` 参数；新增 `CompleteProfileInput` 和 `CompleteProfile` 首次登录补全流程；适配 handler、service、store、验证测试
- **OpenAPI 契约更新**：新增 `completeMyProfile` PATCH 端点；`getMe` 增加 `profileCompletionRequired` 字段；更新 fixture 和 baseline
- **前端认证流重构**：统一邮箱验证码登录/注册流程，删除 `AuthRegisterScreen`，新增 `AuthProfileSetupScreen`；`App.tsx` 路由适配新流程（验证码登录后检查 profile 补全状态）；更新 `TopBar`、i18n、devMockClient 和测试
- **UI 原型更新**：`screen-auth.jsx` 统一登录/注册入口，移除 OAuth 存根，集成 profile setup 流程；`app.jsx`、`canvas.html` 同步更新
- **Spec 文档同步**：更新 `backend-auth` 和 `frontend-shell` spec、plan、BDD checklist、checklist 以匹配统一登录语义
- **E2E 场景**：扩展 auth email code 场景，新增 setup/verify 脚本
- **Lint 脚本**：更新 `openapi_inventory.py`

### 关联 Commit

- `feat(auth): unify email login flow with profile setup`

### 备注

本次变更是 auth unified email login 功能的跨层实施，涉及 backend、openapi、frontend、ui-design、docs、test、scripts 全部 7 个分组的同步更新，作为单个 commit 提交。

## 15:52 工作记录

### 完成事项

- **认证路由门禁修复**：Home 未登录态不再显示 Recent mock interviews，也不再触发 `listTargetJobs`；JD 导入、简历创建、最近面试卡片和直接业务路由访问在未登录时统一进入 `auth_login` 并保留 pendingAction。
- **后端保护证据补强**：补充 practice/profile route middleware 测试，确认业务 API 在无 session 时继续由 middleware 返回 `AUTH_UNAUTHORIZED`。
- **UI / Spec / BDD 同步**：更新 `frontend-shell` spec、plan、checklist、BDD、`docs/ui-design/auth-and-entry.md` 和 `ui-design/` 原型契约；新增 `E2E.P0.102` 场景资产。
- **回归与收尾**：修正旧测试对“业务页先挂载”的过期假设，完成全量前端测试、类型检查、UI 契约、后端关键测试、场景脚本、文档索引和 diff 检查；新增 [BUG-0115](../bugs/BUG-0115.md) 与 [交付复盘](../reports/2026-05-28-auth-gated-interview-routes-assessment.md)。

### 关联 Commit

- `fix(frontend-shell): gate protected routes before login (BUG-0115)`

### 备注

本次变更跨 frontend、backend 测试、ui-design、docs 和 test/scenarios，但主题均属于 `BUG-0115` 的同一认证边界修复闭环，作为单个 commit 提交。

## 16:44 工作记录

### 完成事项

- **L2 auth 语义修复**：`AuthLoginScreen` 发码请求体收紧为 email-only，legacy `returnTo` 不再进入 `startAuthEmailChallenge`，safe pendingAction 仅随 auth route params 传递。
- **资料补全恢复门禁**：`AuthProfileSetupScreen` 等待 `completeMyProfile` 刷新后的 `profileCompletionRequired=false` 才恢复 pendingAction；若后端仍要求补全，页面停留错误状态且不导航。
- **P0.102 wrapper 证据补强**：场景 trigger 输出 runner marker 和 Go `-v` named PASS；verify 检查目标 Vitest 文件、backend `--- PASS` test 名称、package `ok` 行和 no-test/skip/fail 负向 marker，并修复 `grep --` edge case。
- **文档与收尾**：`frontend-shell/001-app-shell-auth-settings` plan/checklist 升到 v1.15；新增 [BUG-0116](../bugs/BUG-0116.md) 与 [交付复盘](../reports/2026-05-28-frontend-shell-auth-profile-l2-gaps-assessment.md)，并完成文档索引同步。

### 关联 Commit

- `fix(frontend-shell): close auth profile L2 gaps (BUG-0116)`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/001-app-shell-auth-settings
