# 2026-05-21 工作日志

## 10:48 工作记录

### 完成事项

- 修复 2026-05-18 工作日志中遗留的两项前端基线失败：`p0-037` 断言改为当前 `ResumeRewritesTab` 真理源，`practiceVoiceTurn` 断言改为当前 data audio URL 行为。
- 同步修订 `p0-037` 场景 README 与 `frontend-resume-workshop/001` BDD 说明，明确旧 ComingSoon testid 不能再作为 plan 003 后的通过证据。
- 完成 roadmap/spec lifecycle rebaseline：`engineering-roadmap` 升至 v3.16，并将 5 个已全勾选但仍标记 active 的 plan/checklist/BDD 文档同步为 completed。
- 通过 `sync-doc-index` 修复 `docs/spec/INDEX.md` 与各 plans `INDEX.md` 投影漂移，并新增交付复盘报告记录验证证据与后续建议。
- 验证通过：focused frontend tests、`p0-037` scenario wrapper、`devMockClient` fixture gate、`validate_fixtures.py`、完整 `pnpm --filter @easyinterview/frontend test`、context validator、`sync-doc-index --check`、lifecycle scan、`make docs-check` 与 `git diff --check`。

### 关联 Commit

- `fix(frontend-baseline): repair stale assertions and spec lifecycle`

### 备注

- 本次按单一收口目标提交：前端基线修复与 roadmap/spec 生命周期重校对属于同一轮 baseline closure。

## 14:11 工作记录

### 完成事项

- 派生 `backend-profile` 新 subject（v1.1 active）：建立 spec.md / history.md / plans/INDEX.md，承接 Candidate Profile 业务域后端 owner，覆盖 5 个 Profile endpoint 真实 backend、experience cards CRUD、隐私删除链路与 source counts internal API 边界；锁定 D-1..D-13 决策（含 D-13 `GetCandidateProfileForUser` cross-owner read-only internal API）。
- 派生 `backend-jobs-recommendations` 新 subject（v1.1 active）：建立 spec.md / history.md / plans/INDEX.md，承接 JobMatch 业务域后端 owner，覆盖 12 个 JobMatch endpoint 真实 backend、5 张新表 B4 additive、2 个 F3 feature_key、2 个 event、2 个 B3 job_type 与 6 个 cross-owner counter integration；锁定 D-1..D-19 决策，含 `getJobMatchProfile` structural-parity 例外与 displayName 来源映射。
- 首批 plan 骨架落地：`backend-profile/plans/001-candidate-profile-and-experience-cards`、`backend-jobs-recommendations/plans/001-jd-match-real-backend-baseline`，各含 plan.md / checklist.md / context.yaml / bdd-plan.md / bdd-checklist.md。
- 同步 roadmap：`engineering-roadmap` 升级至 v3.17，§5.2 P0 workstream 候选表 "App shell + auth + settings" 行追加 `backend-profile`，"Home / Job Picks / Parse" 行追加 `backend-jobs-recommendations`，并移除 "JobMatch real backend subject not yet created" 旧口径；同步 `docs/spec/INDEX.md` 新增两个 subject 行。
- 验证：两份新 spec 的 Header / `docs/spec/INDEX.md` / 各自 `plans/INDEX.md` 三处 `版本 / 状态 / 更新日期` 字段一致；roadmap v3.17 history 与 spec.md 同步。

### 关联 Commit

- `docs(spec): seed backend-profile and jobs-recommendations subjects`

### 备注

- 两个 subject 同属一次 roadmap v3.17 派生：spec / plan / INDEX / roadmap 必须一并落地，否则旧口径与 INDEX 漂移立即触发。因此本次按单一 baseline commit 提交，未按 subject 拆分。

## 14:38 工作记录

### 完成事项

- 将当前本地分支从 `codex/backend-profile-and-jobs-recommendations-bootstrap` 重命名为 `spec-design/backend-profile-and-jobs-recommendations-bootstrap`，保留既有 spec bootstrap 提交。
- 修订 `AGENTS.md` Git 分支策略，明确分支第一段必须表达工作类型或业务域，禁止新建 `codex/`、`claude/`、`gemini/`、`agent/` 等工具名前缀分支。
- 同步加固 `/design`、`/change-intake`、`/plan-review --fix`、`/plan-code-review --fix` 的分支门禁，并为 `AGENTS.md`、`/design`、`/change-intake` 增加 contract test 覆盖。
- 按 `/bug-report` 建档 [BUG-0080](../bugs/BUG-0080.md)，并把分支门禁模式库扩展为“前置保护 + 语义命名约束”。
- 验证通过：skill / AGENTS contract tests 30 passed、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(governance): reject tool-name branch prefixes (BUG-0080)`

### 备注

- 诊断结论：`/implement` 的自动命名脚本没有生成 `codex/`；复发根因是入口 SOP / skill 未把工具名前缀列为显式禁止项，导致 Agent 执行时用工具身份代替了语义域。

## 14:40 工作记录

### 完成事项

- 按 `/retrospective` 对分支前缀治理修复做交付复盘，新增 [Branch Prefix Governance Fix 交付复盘](../reports/2026-05-21-branch-prefix-governance-fix-assessment.md)。
- 复盘确认本次根因属于 `AGENTS.md` + entry skill branch guard + contract test 覆盖缺口，而非 `/implement` 自动分支脚本。
- 报告沉淀下一步建议：继续在当前 `spec-design/backend-profile-and-jobs-recommendations-bootstrap` 分支上对 `backend-profile` 与 `backend-jobs-recommendations` 两个新 subject 做 L1 plan review；历史 `codex/*` 分支清理需要用户明确决策。
- 更新 `docs/reports/INDEX.md` 与工作日志索引。

### 关联 Commit

- `docs(retrospective): assess branch prefix governance fix`

### 备注

- 本提交仅补 post-pass retrospective 文档，不再改动已提交的治理修复内容。

## 16:32 工作记录

### 完成事项

- 落地 `backend-profile/001-candidate-profile-and-experience-cards` Phase 1-5 完整实施：5 个 Profile HTTP handler（`GetMyProfile` / `UpdateMyProfile` / `ListExperienceCards` / `CreateExperienceCard` / `UpdateExperienceCard`）、`backend/internal/profile/{handler,store,service}` 三层、`cmd/api` route wiring（含 idempotency middleware 仅作用于 experience card POST/PATCH）。
- 完成 B1 cross-owner additive：`shared/conventions.yaml` + B1 spec 1.20 / history 新增 `RESOURCE_NOT_FOUND` 错误码（cross-user 404 通用码）。
- 完成 B2 cross-owner additive D-24：`openapi/openapi.yaml` 在 `createExperienceCard` / `updateExperienceCard` 加 `IdempotencyKey` parameter $ref；`CandidateProfile` 的 `headline` / `yearsOfExperience` / `currentRole` / `region` 字段添加 `nullable: true`；fixture 同步追加 IK header 示例；B2 spec 1.26 / history 同步落地。
- 补齐 cross-owner internal API：`DeleteCandidateProfileForUser` / `CountExperienceCardsBySource` / `GetCandidateProfileForUser`（D-13 read-only / 不触发 seed 副作用）；audit tombstone 落到 `audit_events` 仅含 userId + experienceCardCount + deletedAt + jobId。
- 单元 + 集成 + cmd/api scenario 全 PASS：`go test ./...` 0 fail；`-tags=integration` 集成测试命中本地 dev-stack Postgres PASS；`TestProfileHTTPScenario` 在真实 `auth.SessionMiddleware` + IK middleware + Postgres 链路上覆盖 5 个 endpoint + IK replay + cross-user 404 + D-13 read-only。
- 3 个 BDD 场景全 PASS：`test/scenarios/e2e/p0-091-candidate-profile-seed-and-patch`、`p0-092-experience-cards-crud-with-ik`、`p0-093-profile-privacy-delete-lifecycle`（原 plan 文档草拟的 P0.081-083 与 frontend-resume-workshop 已占用 ID 冲突，已在 plan / bdd-plan / bdd-checklist / 场景目录中统一重命名为 P0.091-093）。
- 同步 `docs/spec/INDEX.md`、`docs/spec/backend-profile/{spec,history,plans/INDEX}.md`、`engineering-roadmap` 3.18、`shared-conventions-codified` 1.20、`openapi-v1-contract` 1.26、`shared/fixtures/conventions-parity.json`、`test/scenarios/e2e/INDEX.md`；plan / checklist / bdd-plan / bdd-checklist 状态 active → completed；`sync-doc-index --check` PASS。
- C-14 negative grep 在 `backend/internal/profile/` 0 命中；audit tombstone 不携带 PII（headline / currentRole / title / situation / task / action / result / metrics / skills 等均被 verify.sh 显式 grep 拒绝）。

### 关联 Commit

- `feat(backend-profile): land candidate profile and experience cards baseline`

### 备注

- Auto-committed by /tdd phase-commit, plan: backend-profile/001-candidate-profile-and-experience-cards.
- 实施期间发现 spec/plan 中 `RESOURCE_NOT_FOUND` 与 `CandidateProfile` nullable 字段两个 cross-owner additive 未在 plan §3.2 显式列出，本次直接在 implement 闭环阶段携带（B1 spec 1.20 + B2 spec 1.26）；用户在交互式 AskUserQuestion 中已确认 B1 / B2 双 additive 路径。
- 唯一未携带的原生 gate：`make codegen-events-check` 在 `frontend/src/api/devMockClient.ts` 有 `debrief_generate` naked literal pre-existing failure，与本 plan 无关，遗留给 frontend owner。

## 19:50 工作记录

### 完成事项

- 按 `/plan-code-review backend-profile/001-candidate-profile-and-experience-cards --fix` 完成 L2 修复闭环：统一清理 P0.091 / P0.092 / P0.093 场景资产中的旧 P0.081-083 编号与旧 `.test-output` 路径，并用负向搜索确认零残留。
- 修复 C-14 legacy negative gate：将 profile handler 测试 fixture 中的 `growth` 旧口径改为当前文案，`backend/internal/profile/` 对 `mistake|growth|drill|experiences|star` 0 命中。
- 加固隐私删除链路：新增事务性 `DeleteCandidateProfileForUserWithAudit`，失败时 rollback 并写入 redacted failure audit；P0.093 场景 trigger/verify 纳入 failure rollback integration gate。
- 修复 `make codegen-check` 闭环缺口：为 profile `resume_parse` source type 增加窄 allowlist 与 lint 单测，并将 frontend dev mock 的 `debrief_generate` 比较切到共享 job 常量。
- 将 backend-profile 001 plan/checklist/BDD 文档升至 v1.2 completed，新增 Phase 6 remediation gate；同步 [BUG-0081](../bugs/BUG-0081.md) 与 [Backend Profile 001 L2 Remediation 交付复盘](../reports/2026-05-21-backend-profile-001-l2-remediation-assessment.md)。
- 验证通过：P0.091 / P0.092 / P0.093 scenario wrappers、profile store integration、cmd/api HTTP scenario、`go test ./...`、frontend dev mock test/typecheck、event lint unit、`make lint-openapi`、`make validate-fixtures`、`make openapi-diff`、`make codegen-check`、`make docs-check`、context validator、`sync-doc-index --check`、`git diff --check`。

### 关联 Commit

- `fix(backend-profile): close profile L2 evidence drift (BUG-0081)`

### 备注

- 本提交是 backend-profile 001 completed plan 的原地 L2 remediation，代码、场景、plan v1.2、Bug 记录、复盘报告和 work journal 一次性提交，避免完成态证据再次分裂。

## 21:16 工作记录

### 完成事项

- 按 `/plan-review backend-jobs-recommendations/001-jd-match-real-backend-baseline --fix` 完成 docs-only L1 修复闭环，未改动代码实现。
- 将 JD-Match BDD 场景编号从已占用的 `E2E.P0.084-087` 改为当前未占用的 `E2E.P0.094-097`，并同步 plan / checklist / bdd-plan / bdd-checklist 中的目录名、BDD gate 与证据路径。
- 修正 operation matrix 中 `listSavedSearches` / `createSavedSearch` 的 scenario coverage，使 saved-search lifecycle 归入 `E2E.P0.095`。
- 将 `context.yaml` 的 `specVersion.to` 对齐当前 `backend-jobs-recommendations` spec v1.1。
- 统一 cross-owner internal API 签名口径为 `ctx context.Context, userID string` + error 返回，并把 identity fallback 从空 `displayName` 改为非 PII anonymous display name。
- 验证通过：context validator、`sync-doc-index --check`、`make docs-check`、`git diff --check`、旧编号 / 旧签名 / 空 displayName 负向搜索。

### 关联 Commit

- `docs(backend-jobs): align jd match plan review gates`

### 备注

- 本提交仅记录 plan-review 文档修复，不推进 `/implement` checklist，也不标记任何实现项完成。

## 22:30 工作记录

### 完成事项

- 落地 `backend-jobs-recommendations/001-jd-match-real-backend-baseline` Phase 0 全部 20 个 cross-owner additive items：B4 migration 000009 创建 5 张 JD-Match 表（`jd_match_recommendations` / `watchlist_items` / `saved_searches` / `agent_scans` / `jd_match_search_runs`）+ index + FK + CHECK constraints + ALTER `async_jobs.job_type` CHECK 扩展 2 个新 canonical；`migrations/enum-sources.yaml` 登记 `agent_scans.status` ∈ {idle, scanning, error} + `watchlist_items.tone` ∈ {ok, warn, muted} + `async_jobs.job_type` checksum 更新；`scripts/lint/migrations_lint.py` `EXPECTED_BASELINE_TABLES` 加 5 张表；`backend/internal/migrations/privacy.go` `PrivacyMatrix` cascade 组追加 5 张表 + `cli_test.go` baseline count 30 → 35。
- F3：落地 `config/prompts/jd_match.{recommendation,search}/v0.1.0.{yaml,md,en.yaml,en.md}` + `config/rubrics/jd_match.{recommendation,search}/v0.1.0.{yaml,en.yaml}` baseline prompt + rubric 文件，按 loader canonical algorithm 计算 4 份 template_hash；F3 spec 2.2 → 2.3、§3.1.1 字典 11 → 13、新增 D-12 决策行；F3 history 追加 2.3 行。Registry 测试 13 feature_keys × 2 languages = 26 coordinates、`backend_review_preflight_test.go` F3 spec 版本断言同步至 2.3。
- A3：`config/ai-profiles.yaml` 新增 `jd_match.recommendation.default`（chat / deepseek / deepseek-v4-pro / 20s timeout / temperature 0.3）+ `jd_match.search.default`（chat / deepseek / deepseek-v4-flash / 25s timeout / temperature 0.2）；A3 §4.5 Capability Catalog 新增 JD-Match 推荐解释 + 自然语言搜索两行，并把既有 "Job Picks 匹配解释" 改为 legacy frontend home picks 标识；A3 spec 2.10 → 2.11、history 追加 2.11 行；`make lint-ai-profile-coverage` 覆盖 13 个 F3 feature_key 默认 profile 通过。
- B3：`shared/events.yaml` 新增 `jd_match.recommendation.completed`（producer=backend_async, aggregateType=agent_scan, payload `userId / agentScanId / recommendationCount / completedAt`）+ `jd_match.search.completed`（producer=api, aggregateType=search_run, payload `userId / searchRunId / resultCount / completedAt`），PII 边界禁止 query / filters / reasons / risks / sourceUrl / interviewHypotheses；`shared/jobs.yaml` 新增 canonical `jd_match_agent_scan`（dotted `jd_match.agent_scan`, apiFacing=false）+ `jd_match_search`（dotted `jd_match.search`, apiFacing=false, future-async reserved）；`scripts/lint/events_inventory.py` 注册新 events/jobs + 新 event domain `jd_match` + EVENT_NAME_RE 允许下划线段；`make codegen-events` 重新渲染 Go / TS / JSON Schema / baseline manifest（events 全集 19 → 21；jobs 全集 9 → 11）；codegen events run_test schema count 16 → 18 + jobs.go align 对齐；B3 spec 2.5 → 2.6、新增 D-16 决策行、§3.1.1 generator 输入计数同步、§6 C-2 jobType 常量数同步；B3 history 追加 2.6 行。
- Cross-owner counter / identity APIs（5 个）：`backend/internal/{resume,targetjob,practice,debrief}/count.go` 各 `Count*ForUser(ctx, *sql.DB, userID) (int, error)`，read-only `SELECT COUNT(*)` cross-user 隔离 + 各 `count_test.go` 4 项断言（happy / cross-user / nil-db / empty-userId）；`backend/internal/auth/identity.go` `GetUserIdentityForUser(ctx, *sql.DB, userID) (UserIdentity{DisplayName, AvatarURL, EmailMasked}, error)`，复用既有 `maskEmail` helper（首字符 + `***` + 尾字符 + `@domain`），缺 user 返回 `ErrUserNotFound`，caller fallback 到 `AnonymousDisplayName="Candidate"`；`identity_test.go` 6 项断言（seeded happy / missing display_name fallback / ErrUserNotFound / does-not-write-audit (sqlmock ExpectationsWereMet) / nil-db / empty-userId），emailMasked 含 `***`、不含 raw local-part、保留 domain。
- 5 owner spec/history 同步：backend-resume 1.3、backend-targetjob 1.5、backend-practice 1.11、backend-debrief 1.3、backend-auth 1.3，模块边界表追加 cross-owner internal API 行；各 history.md 追加 1 行；`docs/spec/INDEX.md` 通过 `sync-doc-index --fix-index` 同步 6 个 owner spec 版本与日期。
- 验证通过：`migrations/lint.sh` PASS、`python3 scripts/lint/events_inventory.py` PASS、`make lint-ai-profile-coverage` PASS、`python3 scripts/lint/migrations_lint.py` PASS、`sync-doc-index --check` zero drift、`go build ./...` PASS、`go test ./...` 全 PASS、`pnpm --filter @easyinterview/frontend test` 1294 passed、registry 13 feature_keys × 2 languages = 26 coordinates、codegen-events 重新渲染 Go/TS/JSON Schema/baseline。本地缺 `DATABASE_URL`，`make migrate-check` 与 `make privacy-delete-dry-run` 中需 live DB 的子步骤不可冒充全绿，已记录为后续 phase / scenario 验证 blocker（`migrations/lint.sh` + `privacy.go` / `cli_test.go` PrivacyMatrix 35 张表覆盖已满足 plan §3 替代验证 gate 中的本地部分）。

### 关联 Commit

- `feat(backend-jobs): land jd-match cross-owner additive (b4 / f3 / a3 / b3 + counters + identity)`

### 备注

- Auto-committed by /tdd phase-commit, plan: backend-jobs-recommendations/001-jd-match-real-backend-baseline.
- Phase 0 完成；Phase 1-6（profile + agent status + recommendations + watchlist + search + market signals + agent scan job + privacy + cmd/api wiring + BDD E2E.P0.094-097）留给后续 `/implement` 会话推进。下游 frontend `frontend-home-job-picks-and-parse/002-jd-match-recommendations` 与 backend internal privacy runner 等 owner 在 Phase 6.3 handoff 信号阶段才会通知，本 phase commit 不主动改下游。
