# 2026-04-28 工作日志

## 06:12 工作记录

### 完成事项

- 原地修订 `local-dev-stack` spec 与 `001-bootstrap` plan：默认本地 docker compose 仅保留 Postgres+pgvector / Redis / MinIO + 当前项目可运行组件；默认不启动 OTel Collector / Grafana / Loki / Prometheus / AI gateway；本地观测改为应用 `/metrics` 与容器日志。
- 明确 AI provider 边界：单元测试 / 离线契约测试使用 deterministic stub；docker compose 与 Kind 本地部署使用真实 AI provider / OpenAI-compatible endpoint，缺少 `AI_GATEWAY_BASE_URL` / `AI_GATEWAY_API_KEY` 时 fail-fast，不静默回退 stub。
- 将 A5 `ci-pipeline-baseline` 修订为当前单人开发阶段的本地质量门禁与 deferred CI 边界：只要求本地 `make lint` / `make test` / `make build` / `make docs-check` / `make codegen-check` 等 gate；不创建 GitHub Actions / branch protection / required checks / artifact / nightly / CI secret scan。
- 同步父级 `engineering-roadmap`、ADR-Q3 / Q4 / Q6、A2/A3/A4/A5、B1/B2/B3/B4、F1/F3 等相关 spec / history / plan / checklist / context 与 INDEX，清理旧的“当前必须接入 CI / 生产观测组件 / 本地 AI gateway”口径。
- 验证：`sync-doc-index --check` zero drift；`validate_context.py` 对 roadmap / local-dev-stack / shared-conventions 三个 context 通过；CI 关键词回扫剩余命中均为 deferred / future 语义；`git diff --check` clean。

### 关联 Commit

- `docs(spec): align local dev and quality gates`

### 备注

本次为文档一致性修订，不包含运行时代码实现；`local-dev-stack/001-bootstrap` 仍处于 active，后续由 `/implement local-dev-stack/001-bootstrap` 执行代码落地。

## 06:45 工作记录

### 完成事项

- 落地 `deploy/dev-stack/docker-compose.yaml`：默认最小依赖 Postgres+pgvector / Redis / MinIO + 一次性 `minio-init` bucket 引导；按 D-2 锁镜像 tag、D-3 端口、D-4 bridge network `easyinterview-dev`、D-7 三个命名卷；不预留 OTel / Grafana / Loki / Prometheus / AI gateway。
- 落地 init 脚本：`init/postgres/01-pgvector.sql` 自动 `CREATE EXTENSION IF NOT EXISTS vector`；`init/minio/create-buckets.sh` 通过 `mc` 幂等创建默认 bucket `easyinterview-dev`。
- 落地 `deploy/dev-stack/.env.example`：连接串 / bucket 名 / 端口 / `AI_GATEWAY_BASE_URL` / `AI_GATEWAY_API_KEY` 占位；字段名与 `secrets-and-config` §3.1.1 P0 env 字典对齐；真实 AI key 留空，绝不入库。
- Phase 1 自检通过：`docker compose up -d` 后 `postgres-dev` / `redis-dev` / `minio-dev` 均 `Healthy`；`docker exec` Postgres 内 `select extname from pg_extension where extname='vector'` 返回 `vector`（关闭 C-6）；`mc ls local/` 返回 `easyinterview-dev/`；`docker compose down`（无 `--volumes`）后 3 个命名卷保留。
- 已知约束：compose `--wait` 在一次性 `minio-init` exit 0 时整体非 0，需要在 Phase 2 Makefile 里按 service 名分批 `--wait` 解决。

### 关联 Commit

- `feat(dev-stack): docker-compose 与 init 脚本骨架`

### 备注

Auto-committed by /tdd phase-commit, plan: local-dev-stack/001-bootstrap, phase: Phase 1: docker-compose 与 init 脚本。

## 06:53 工作记录

### 完成事项

- 落地 `deploy/dev-stack/Makefile`：`up` / `down` / `doctor` / `reset` / `logs` / `pull` 真实命令体；`up` 先按依赖服务名 `--wait` 再单独跑 `minio-init`，并轮询 init 退出码以避开 compose `--wait` 在 one-shot 容器上误判。
- 替换根 `Makefile` 中 repo-scaffold 占位 `dev-up` / `dev-down`：递归调用 `deploy/dev-stack/Makefile`；新增 `dev-doctor` / `dev-reset` / `dev-logs` / `dev-pull` 并入根 `make help`。
- 实现 dev-up 幂等（C-3）：`_all_healthy` 检查 3 个依赖 running+healthy 且 `minio-init` exited/0 时跳过启动；二次 `make dev-up` 1s 内输出 `already healthy`。
- 实现 dev-down 卷保留（C-4）：`docker compose down` 不带 `--volumes`；写入 `dev_probe` 表 → down → up → 数据完整可读。
- 实现 dev-reset（C-5）：交互式 `read -r` 确认，`DEV_RESET_FORCE=1` 跳过；输入 `no` 时 abort 不删卷且 exit 非 0；force 模式删除 3 个命名卷，下一次 `make dev-up` 从空卷重建（probe 表消失确认）。
- Phase 2 自检全部通过；后续 Phase 3 引入 `dev-doctor.sh` 后才会触发 `up` 中的 doctor gate。

### 关联 Commit

- `feat(dev-stack): 接入 make dev-* 生命周期`

### 备注

Auto-committed by /tdd phase-commit, plan: local-dev-stack/001-bootstrap, phase: Phase 2: Make targets 与生命周期语义。

## 07:03 工作记录

### 完成事项

- 落地 `deploy/dev-stack/scripts/dev-doctor.sh`（POSIX sh + jq，173 行）：输出 spec D-6 锁定的 JSON `services[]` + `summary` 结构；3 个固定依赖 (postgres-dev / redis-dev / minio-dev) 走容器健康 + e2e probe (PG `pg_isready` + `select 1` + vector 扩展、Redis set/get/del、MinIO `mc ls` 默认 bucket)；项目组件来自 compose label `easyinterview.dev-stack.role=app`，按 label 决定 healthz / metrics / aiclient probe；启用 AIClient 的组件缺 `AI_GATEWAY_BASE_URL` / `AI_GATEWAY_API_KEY` 时报 DOWN，`reason` 指向缺真实 AI provider 配置；不调用真实 LLM。
- 端口冲突识别（C-2）：`port_owner` 通过 `lsof +c 0` 取完整 cmd，`down_or_conflict` 过滤 docker / vpnkit / dockerd 持有的端口；非 docker 进程占端口时 reason 为 `port conflict: host port N held by pid=... cmd=...`；`make dev-up` 在 `compose up --wait` 失败时 stderr dump 冲突端口与占用进程后 exit 非 0。
- dev-up gate（C-1）：`up` 在 deps healthy + init 退出 0 后调用 `dev-doctor.sh`，仅 `summary.ok == total` 时退出 0；否则按 service 名 dump 最近 50 行 `docker logs` 后 exit 1。
- Phase 3 自检全部通过：全员 OK 时 `make dev-doctor` JSON 通过 jq schema 校验（3 个依赖名固定、`ok+degraded+down==total`），exit 0；`docker stop redis-dev` 后 doctor 报 redis-dev DOWN reason `container state=exited/`，exit 1；macOS Docker Desktop 下用双栈 IPv4+IPv6 Python 监听复现 5432 冲突，`make dev-up` 退出非 0 且 stderr 列出 Python 占用进程，`make dev-doctor` 对 postgres-dev 报 `port conflict: ...cmd=Python`，redis-dev / minio-dev 保持 OK。

### 关联 Commit

- `feat(dev-stack): 落地 dev-doctor 与端口冲突可观察`

### 备注

Auto-committed by /tdd phase-commit, plan: local-dev-stack/001-bootstrap, phase: Phase 3: dev-doctor 结构化健康检查。

## 07:09 工作记录

### 完成事项

- 落地 `deploy/dev-stack/README.md`：服务表（dependency 镜像 / 端口 / 凭据 / 命名卷）、项目组件接入约定（label 名 + healthz/metrics/aiclient probe 触发条件）、`make dev-*` 命令清单、AI provider 配置（本地真实 provider，stub 仅单测）、与 `test/scenarios/` Kind 路径双轨说明、资源占用与 < 1.5GB 镜像下载预算、故障排查、最低 docker engine 24+ / compose v2.20+ 版本声明、A5 远端 CI 边界声明（当前不触发）。
- A2 executable gate handoff（C-8）AC 重跑证据：
  - C-1 `make dev-up` → exit 0；`make dev-doctor` 输出 `summary.ok=3 degraded=0 down=0 total=3`。
  - C-2 macOS Docker Desktop 双栈 IPv6 squatter 占 5432 → `make dev-up` exit 1，stderr 列 Python 占用进程；doctor 报 postgres-dev `port conflict: ... cmd=Python`，redis-dev / minio-dev OK。
  - C-3 二次 `make dev-up` 0s 内输出 `already healthy`。
  - C-4 写入 `ac_probe` → `make dev-down`（卷保留）→ `make dev-up` → `select payload` 返回 `C4-evidence-2026-04-28`。
  - C-5 `DEV_RESET_FORCE=1 make dev-reset` → 3 个命名卷消失；下一次 `make dev-up` 后 `ac_probe` 表不存在。
  - C-6 `select extname from pg_extension where extname='vector'` 返回 `vector`。
  - C-7 `make dev-logs` / `make dev-logs SERVICE=postgres-dev` 均输出容器日志；当前无声明 `/metrics` 的项目组件，curl 校验为 vacuous（doctor 已按 label 实现，待后续组件接入触发）。
  - C-9 当前无启用 AIClient 的项目组件；`dev-doctor.sh` 的 AIClient env presence 校验逻辑已落地，对 `easyinterview.dev-stack.aiclient=true` 的容器自动检测 `AI_GATEWAY_BASE_URL` / `AI_GATEWAY_API_KEY`，缺一即 DOWN with reason；全程不启动 AI gateway 容器，不切 stub。
- 文档收口：plan / checklist Header `状态` active → completed（更新日期 2026-04-28）；`/sync-doc-index --fix-index` 把 plans/INDEX 从 §1 Active 迁到 §2 Completed；`--check` zero drift。
- engineering-roadmap/001-decompose-subspecs Phase 3.6 留下的「executable gate by A2 child」承诺由本 plan 4.4 关闭；不修改 parent roadmap checklist。

### 关联 Commit

- `feat(dev-stack): 收口 README 与 A2 executable gate`

### 备注

Auto-committed by /tdd phase-commit, plan: local-dev-stack/001-bootstrap, phase: Phase 4: 指标日志 + 文档 + AC 收口。本 plan 四个 phase 全部交付，状态切到 completed。

## 09:54 工作记录

### 完成事项

- 执行 `plan-code-review local-dev-stack/plans/001-bootstrap --fix` 后，修正 `deploy/dev-stack/README.md` 中 A3 spec 引用：从已不存在的 `ai-client-and-provider` 改为当前实际的 `ai-gateway-and-model-routing`。
- 复核本地 dev stack 运行面：`make dev-up`、`make dev-doctor`、二次 `make dev-up` 幂等、pgvector probe、MinIO bucket probe、`make lint`、`make test`、`make build`、`sync-doc-index --check` 均已执行；当前 backend/frontend 测试与构建仍为后续 child subspec 的 TODO 占位。

### 关联 Commit

- `docs(dev-stack): correct A3 spec link`

### 备注

本次为 L2 code review remediation 的文档引用修正；运行时实现无代码改动。

## 10:34 工作记录

### 完成事项

- 根据 L1 plan-review 结论原地修订 `openapi-v1-contract` spec：Auth tag 以 ADR-Q1 email magic link + first-party session cookie 为准，替换旧的 magic-link / session endpoint freeze。
- 将 privacy export 的 P0 `501 Not Implemented` 作为显式状态码例外写入状态码矩阵，并标注 P1 切回 `202 + PrivacyRequestWithJob` 的兼容判定。
- 补齐 schema inventory、Header / Idempotency 矩阵、AI 生成结果 `GenerationProvenance` 约束与对应验收项，保障后续 B2 `001-bootstrap` 可执行。
- 同步 `openapi-v1-contract/history.md` 1.3 修订记录与 `docs/spec/INDEX.md` 版本投影；验证 `sync-doc-index --check` zero drift、`git diff --check` clean。

### 关联 Commit

- `docs(openapi): reconcile v1 contract review findings`

### 备注

本次为文档一致性与完备性修订，不包含运行时代码实现。

## 11:50 工作记录

### 完成事项

- 根据 L1 plan-review 结论原地修订 `secrets-and-config` spec 至 1.5：移除 `JWT_SIGNING_KEY` 口径，改为 ADR-Q1 对齐的 `SESSION_COOKIE_SECRET`（first-party session cookie 签名）+ `AUTH_CHALLENGE_TOKEN_PEPPER`（magic link challenge token hash pepper），明确 server-side `sessions` 表仍是会话真理源、原始 token 不入库不进日志。
- 统一 PostHog env key 到 ADR-Q3：`POSTHOG_API_HOST` → `POSTHOG_HOST`，新增 `POSTHOG_SELF_HOSTED`（staging/prod 必须 `true`，防误接 PostHog Cloud），保留 `POSTHOG_PUBLIC_KEY` 仅前端 analytics 初始化使用。
- 收紧 `runtime-config` 边界：D-2 改为 allowlist-only public 字段（`defaultUiLanguage` / `featureFlags.public=true` / `appVersion` / `analyticsEnabled` / 可选 `postHogPublicKey`）；新增 `analytics_opt_in` 合并语义（opt-out 时 `analyticsEnabled=false` 且不返回 `postHogPublicKey`）；`operator-only` flag 一律不进 public 响应；用户分桶 flag 走 protected `/api/v1/me/runtime-config`。
- 新增 §3.1.2 canonical config schema 分类表（config path × env key × secret × required rule × runtime-config exposure × owner），让 validator / runtime-config / redaction 共用同一真理源；同步新增 C-11 验收项校验 secret/public 分类一致性。
- 收紧安全约束：redaction 必须覆盖结构化 JSON 日志、error wrapping 与 nested config dump；`RedactedString` 仅通过显式 `Reveal()` 透出明文，普通 `%s` / `%v` / JSON marshal 输出均为 `***`；并把 C-2 / C-4 / C-5 / C-6 / C-7 验收语义同步到新 env key 与新边界。
- 同步 `secrets-and-config/history.md` 1.5 修订记录与 `docs/spec/INDEX.md` A4 行版本投影；Header / INDEX 一致性检查通过，`git diff --check` clean。

### 关联 Commit

- `docs(secrets-and-config): reconcile A4 plan-review remediation`

### 备注

本次为文档一致性与完备性修订，不包含运行时代码实现；后续由 A4 child plan `001-bootstrap`（待 spawn）落地 validator / RedactedString / runtime-config endpoint 与 lint-config 红线。

## 11:51 工作记录

### 完成事项

- 通过 `/design` 物化 `openapi-v1-contract` spec §7 列出的 3 个 child plan，落地各自完整资产（plan.md / checklist.md / context.yaml）：
  - `001-bootstrap`（v1.0.0 骨架 + 双端 codegen + ADR-Q1 Auth + privacy export 501 + GenerationProvenance + 本地 drift gate）：4 phase，提供可消费的契约源给 002 / 003。
  - `002-fixtures-and-mock-source`（36 operationId default fixtures + prototype-baseline 同步工具 + 隐私脱敏校验 + Prism mock parity smoke）：依赖 001 的 codegen 完结。
  - `003-breaking-change-gate`（openapi-diff baseline + ruleset + privacy export 白名单 + ADR 模板 + B2 freeze handoff）：消费 001 的 v1.0.0 + 002 的 fixtures 验证 mock 兼容性。
- 同步 `openapi-v1-contract/spec.md` §0 引用：`B2 child 001 系列 plan` → 显式列出 `001-bootstrap` / `002-fixtures-and-mock-source` / `003-breaking-change-gate`，spec 文本不变保持 v1.3。
- 同步 `openapi-v1-contract/history.md` 1.3 修订记录补充 `/design` 物化条目；`openapi-v1-contract/plans/INDEX.md` §1 Active 表格新增 3 行（皆为 1.0 / active / 2026-04-28）。
- Header / INDEX 一致性自检通过；3 个 plan dir 的 plan.md / checklist.md / context.yaml 均在 ID 锁定（D-1..D-12 / 36 endpoint × 14 tag / ADR-Q1 / GenerationProvenance）边界内编排，未引入新 spec ID。

### 关联 Commit

- `docs(openapi): materialize B2 child plans 001/002/003`

### 备注

本次为 `/design` 结晶产出的 plan 资产，不包含运行时代码实现；后续由 `/implement openapi-v1-contract/001-bootstrap` 启动 v1.0.0 骨架与 codegen 的 TDD 落地。

## 12:37 工作记录

### 完成事项

- 落地 `openapi/openapi.yaml` v1.0.0 骨架：OpenAPI 3.1.0、`info.version: 1.0.0`、`servers: [{url: /api/v1}]`、按 spec §2.1 顺序的 14 个 tag、ADR-Q1 `sessionCookie` security scheme（apiKey/cookie/`ei_session`）、document-level `security: [{sessionCookie: []}]`；不引入 `Authorization: Bearer` 默认 scheme。
- 落地 components：`IdempotencyKey` / `XRequestID` / `Traceparent` / `AcceptLanguage` / `XClientVersion` 5 个可复用 parameters；`XRequestID` response header；`ApiErrorResponse` 共享错误响应体；schema 块以 `# === B1-AUTO-START === ... # === B1-AUTO-END ===` 标记同步 B1 真理源（`shared/conventions.yaml`）：14 个 enum、`ApiError`/`ApiErrorCode`/`PageInfo`/`JobStatus`，并定义 `GenerationProvenance`（6 字段，`rubricVersion` 描述含 `not_applicable` 字面量）、`ResourceType`/`JobType`/`PaginatedEnvelope`。
- 落地 spec §3.1.1 全部 36 个 operation：每条 op 含 `tags / summary / operationId / security（按 §4.1 public/protected 矩阵）/ 必要 parameters / request body（如有）/ success 或 P0 例外 response / default $ref ApiError`；副作用 endpoint 引用 `Idempotency-Key`，但 ADR-Q1 `POST /api/v1/auth/email/start` 与 `POST /practice/sessions/{sessionId}/events` 例外（后者改用 `clientEventId` 字段）；长耗时 op 走 `202 + *WithJob`；`POST /api/v1/privacy/exports` 唯一声明 `501 + example.error.code = "PRIVACY_EXPORT_NOT_AVAILABLE"`；`GET /api/v1/runtime-config` security `[]`；AI 生成 schema 通过 `provenance: $ref GenerationProvenance` 关联。
- 落地 `scripts/lint/openapi_inventory.py`（POSIX Python 3 + PyYAML，约 280 行）：断言 14 tag 顺序、36 operation 完整集合、每条 operation 都有 `default: $ref ApiErrorResponse`、ADR-Q1 sessionCookie 存在 + 无 http/bearer scheme、IK 必需 / 禁用集合互斥、public endpoint security `[]`、501 唯一性 + `PRIVACY_EXPORT_NOT_AVAILABLE` example、`GenerationProvenance` 6 字段 + `not_applicable` 描述、spec §4.6 7 个 AI schema 通过 $ref 拓扑可达 `GenerationProvenance`、与 `shared/conventions.yaml` enums/errors/jobStatuses 对账。
- 自检：`npx @apidevtools/swagger-cli validate openapi/openapi.yaml` exit 0；`python3 scripts/lint/openapi_inventory.py` 输出 `OK ... 14 tags, 36 operations, ...`；3 类负面注入（删 501 example / 给 events op 加 IK / 改 MistakeStatus enum）均被脚本拦截 exit 1。

### 关联 Commit

- `feat(openapi): v1.0.0 骨架与共享 components`

### 备注

Auto-committed by /tdd phase-commit, plan: openapi-v1-contract/001-bootstrap, phase: Phase 1: openapi.yaml v1.0.0 骨架与共享 components。

## 13:03 工作记录

### 完成事项

- 落地 `backend/cmd/codegen/openapi/` Go generator（`main.go` / `run.go` / `parse.go` / `b1_sync.go` / `render_go.go` / `render_ts.go` / `sha256.go`）：解析 openapi.yaml 与 shared/conventions.yaml，先把 `# === B1-AUTO-START ... # === B1-AUTO-END ===` 块从 conventions.yaml 重新渲染到 openapi.yaml；再用 `text/template` + `openapi/templates/{go,ts}/` 模板把 hand-authored 部分渲染为 Go 与 TS artefacts。Go 端通过 `type X = sharedtypes.X` alias 复用 B1 PageInfo / JobStatus / 14 enum，TS 端通过 `import type {X as XAlias} from "../../lib/conventions"` 复用 ApiError / PageInfo / 14 enum。
- 落地 `openapi/templates/{go,ts}/`：6 个 `*.tmpl`（types / server / spec for Go；types / client / spec for TS），与生成器一同进入仓库便于追溯。
- 生成 artefacts：
  - `backend/internal/api/generated/{types,server,spec}.gen.go`：B1 alias 块 + 全部 domain DTO（`oneOf [X, null]` → `*X`，`allOf` Paginated wrapper、struct/`omitempty`/pointer 处理、`integer/format` → `int32/int64`），36 operation `ServerInterface` + `AllRoutes` 路由表，`//go:embed openapi.yaml` 把 `openapi/openapi.yaml` 内嵌为 `SpecYAML`。
  - `frontend/src/api/generated/{types,client,spec}.ts`：B1 类型 re-export + domain interfaces + 字符串字面量联合 enum；`EasyInterviewClient` 提供 36 个 `operationId`-keyed fetch 方法（`credentials: include`、`Idempotency-Key` 选项、自动 path param 编码、JSON 请求/响应反序列化）；`SPEC_YAML` 内嵌 yaml 字符串。
- 落地 `backend/cmd/codegen/openapi/run_test.go`：`TestRun_Idempotent` 在临时目录连续两次运行 `Run` 后比对所有产物 SHA-256，证明字节级幂等；`TestRun_DriftPropagatesFromConventions` 突变 `MistakeStatus.values` 后断言 openapi.yaml 必须重写。`go test ./cmd/codegen/openapi/...` 通过。
- 根 `Makefile` 替换占位 `codegen` target：`codegen: codegen-conventions codegen-openapi`；新增 `make codegen-openapi`（idempotent 调用 generator）、`make lint-openapi`（swagger-cli + inventory 脚本）、`make codegen-check`（codegen-openapi + lint-openapi + `git diff --exit-code -- openapi/openapi.yaml backend/internal/api/generated frontend/src/api/generated`）；目标进入 `.PHONY` + `make help` 自动列出。
- `openapi/README.md` 改写为 codegen 入口文档：layout、$ref 拓扑（B1 → openapi.yaml AUTO 块 → Go/TS alias）、make 入口表、ADR-Q1 sessionCookie 唯一性、tooling 版本、A5 远端 CI required-check deferred 声明。
- 自检：`make codegen-openapi` 两次 stdout 无差；`make codegen-check` 在 clean tree 上 exit 0；故意添加 `x-test-drift` 字段后 exit 1 + diff 列出该字段；删除 `server.gen.go` 后 `make codegen-openapi` 还原；`go test ./cmd/codegen/openapi/...` PASS；`make codegen-check` 与 `make lint-conventions` 与 `cd frontend && npx tsc --noEmit` 全部 exit 0；`go mod tidy` 把 yaml/uuid 升为 direct dep。

### 关联 Commit

- `feat(openapi): 落地 codegen pipeline 与本地 drift gate`

### 备注

Auto-committed by /tdd phase-commit, plan: openapi-v1-contract/001-bootstrap, phase: Phase 2: Codegen pipeline。

## 13:08 工作记录

### 完成事项

- 落地 `make docs-openapi`：调用 `npx redoc-cli@0.13.21 redoc-cli bundle openapi/openapi.yaml -o openapi/dist/index.html --title "easyinterview API"`，输出单文件 1.6 MB self-contained HTML（包含 Redoc bundle 与 inlined spec），byte-stable 重复执行不变。
- `openapi/dist/` 走根 `.gitignore` 已存在的 `dist/` 规则；新增产物不入 git，不要求 A5 上传 CI artifact，符合 spec §2.1 / §5 关于 deferred CI 的边界。
- `openapi/README.md` 同步更新：layout 增加 `dist/` 行，`make` entry 表新增 `make docs-openapi`，并明确 deferred CI 边界既覆盖 codegen-check 也覆盖 docs-openapi。
- 自检：`make docs-openapi` 两次执行得到同一份 SHA-256；`make codegen-check` 仍 exit 0；`git check-ignore openapi/dist/index.html` 返回 0 确认产物 gitignored。

### 关联 Commit

- `feat(openapi): 接入 docs-openapi 本地 HTML 站点`

### 备注

Auto-committed by /tdd phase-commit, plan: openapi-v1-contract/001-bootstrap, phase: Phase 3: API 文档站点（本地）。

## 13:19 工作记录

### 完成事项

- 执行 spec §6 验收命令并采集证据：
  - **C-1**：`npx @apidevtools/swagger-cli validate openapi/openapi.yaml` exit 0；inventory 脚本输出 `14 tags, 36 operations, ApiError/IK/501/Provenance invariants enforced; B1 enums in sync.`。
  - **C-2 / C-3**：连续两次 `make codegen-openapi` 后 `git status -- openapi/openapi.yaml backend/internal/api/generated frontend/src/api/generated` 为空；删除 `backend/internal/api/generated/types.gen.go` + `frontend/src/api/generated/client.ts` 后 `make codegen-openapi` 还原为字节级一致；故意给 `PracticePlan` 加 `x-optional-metadata-test` 字段后 `make codegen-check` 失败并打印 diff，revert 后恢复 exit 0。
  - **C-7 partial**：脚本枚举 `endpoints declaring 501: [('post', '/privacy/exports')]`，example.error.code 为 `PRIVACY_EXPORT_NOT_AVAILABLE`。
  - **C-8**：在 `shared/conventions.yaml#enums[MistakeStatus].values` 注入 `test_drift_value`，运行 `make codegen-conventions && make codegen-openapi`，`git diff --stat` 显示 `openapi/openapi.yaml` / `backend/internal/api/generated/openapi.yaml` / `frontend/src/api/generated/spec.ts` 三处漂移；`make codegen-check` exit 1；revert 后恢复 exit 0。
  - **C-11 partial**：`GenerationProvenance` 在 schemas 中存在；6 字段 `[promptVersion, rubricVersion, modelId, language, featureFlag, dataSourceVersion]` 全部 `required`；`rubricVersion.description` 含 `not_applicable`；7 个 spec §4.6 AI schema (`TargetJobSummary` / `TargetJobFitSummary` / `AssistantAction` / `FeedbackReport` / `MistakeEntry` / `ResumeTailorRun` / `Debrief`) 全部通过 `$ref` 拓扑可达 `GenerationProvenance`。
- 文档与 INDEX 同步：plan/checklist Header `状态` `active` → `completed`（更新日期 2026-04-28）；`/sync-doc-index --fix-index` 把 `001-bootstrap` 从 `openapi-v1-contract/plans/INDEX.md` §1 Active 迁到 §2 Completed；`--check` zero drift；002 / 003 状态保持不变；未触动 `engineering-roadmap/001-decompose-subspecs/checklist.md` Phase 3.3。
- B2 child handoff 边界确认：本 plan 输出的 `openapi/openapi.yaml` v1.0.0 + 生成的 Go/TS artefacts 已经成为 `openapi-v1-contract/plans/002-fixtures-and-mock-source` (fixtures schema-validate / mock parity 真理源) 与 `openapi-v1-contract/plans/003-breaking-change-gate` (v1.0.0 freeze baseline) 的可消费输入；未直接登记 spec C-10（由 003 Phase 4 收口）。

### 关联 Commit

- `docs(openapi): 收口 001-bootstrap plan 与 plans/INDEX`

### 备注

Auto-committed by /tdd phase-commit, plan: openapi-v1-contract/001-bootstrap, phase: Phase 4: Verification + handoff。本 plan 四个 phase 全部交付，状态切到 completed；后续由 `/implement openapi-v1-contract/002-fixtures-and-mock-source` / `003-breaking-change-gate` 启动。

## 14:30 工作记录

### 完成事项

- 收口 `openapi-v1-contract/001-bootstrap` assessment R1-R6：
  - **R1**：ADR-Q1 v1.2 锁定 first-party session cookie 字面量 `ei_session`；A4 `secrets-and-config` v1.6 新增 D-8 与 `auth.sessionCookieName` 行，A4 不提供 cookie name env/config override。
  - **R2**：拆分 `ApiError` 与 `ApiErrorResponse` 形状——B1 `ApiError` 表示 inner error object，B2 OpenAPI 新增 `ApiErrorResponse` envelope；codegen 渲染 Go `type ApiError = sharederrors.APIError` + `ApiErrorResponse{Error ApiError}`，TS `ApiError = ApiErrorAlias` + `interface ApiErrorResponse { error: ApiError }`；`POST /privacy/exports` 501 与所有 default 错误响应改 `$ref ApiErrorResponse`。
  - **R3 / R6**：B2 spec v1.4 新增 D-13 锁定 `@apidevtools/swagger-cli@4.0.4`（deprecated-but-accepted validator）；同步 `openapi/README.md` tooling 章节增加版本与替换状态登记规约。
  - **R4**：B2 spec §3.1.2 / D-14 锁定 `ResourceType` 与 `JobType` API-facing 字面量；同步 `easyinterview-tech-docs/02-api-definition.md` / `03-db-definition.md` 标注 `ai_task_runs.resource_type` / `async_jobs.resource_type` 必须兼容这些 API-facing 名称。
  - **R5**：归类为低价值 no-op，不修改 `.agent-skills/tdd/SKILL.md`。
- Phase 6 docs renderer 迁移：根 `Makefile` 的 `docs-openapi` target 从 `redoc-cli@0.13.21 redoc-cli bundle` 迁移为 `@redocly/cli@2.30.1 redocly build-docs`，输入 / 输出 / 标题不变；`@apidevtools/swagger-cli@4.0.4` 继续作为 C-1 validator；B2 spec v1.5 / history 1.5 / plan v1.2 / checklist v1.2 同步登记。
- 同步 B1 `shared-conventions-codified` v1.3 新增 D-7 与 C-7：明确 `ApiError` 为 inner object，Go canonical 类型为 `backend/internal/shared/errors.APIError`，TS canonical 类型为 generated `conventions.ApiError`，B2 必须把 wire body 建模为 `ApiErrorResponse` envelope。
- 同步 plan / checklist 追加 Phase 5（assessment remediation）与 Phase 6（docs renderer）小节并恢复 `completed`；`003-breaking-change-gate` plan v1.1 把 privacy export P0 例外类型从 `ApiError` 修正为 `ApiErrorResponse`。
- `scripts/lint/openapi_inventory.py` 新增 `ApiError` inner object + `ApiErrorResponse` envelope 形状校验；`backend/cmd/codegen/openapi/run_test.go` 新增 `TestRun_ApiErrorInnerObjectAndResponseEnvelope` 回归测试；`backend/internal/api/generated/{openapi.yaml,types.gen.go}`、`frontend/src/api/generated/{client.ts,spec.ts,types.ts}` 重新生成。
- 验证：`go test ./backend/cmd/codegen/openapi/...` PASS；`make lint-openapi`、`make docs-openapi`（无 deprecated 横幅）、`cd backend && go build ./...`、`cd frontend && npx tsc --noEmit`、`/sync-doc-index --check` 全部 exit 0；`make codegen-check` 的 validate / inventory 通过，最终 `git diff --exit-code` 因本次未提交的预期 generated 变更返回非 0（已记录在 checklist Phase 5.4 的 dirty-tree 限制）；`git diff --check` clean。

### 关联 Commit

- `fix(openapi): 收口 001-bootstrap remediation R1-R6 与 docs renderer 迁移`

### 备注

Phase 5（R1-R6 assessment remediation）与 Phase 6（docs-openapi renderer 迁移）在 spec.md / history.md / plan.md / checklist.md / openapi/README.md / plans/INDEX.md 中 markdown 改动深度交叠，合并为单 commit；retrospective 报告独立 commit。

## 14:35 工作记录

### 完成事项

- 落地 `docs/reports/2026-04-28-openapi-v1-contract-001-bootstrap-remediation-assessment.md`：复盘对 `2026-04-28-openapi-v1-contract-001-bootstrap-assessment.md` 的核验与修复，覆盖 R1-R6 实际修复范围 + Phase 6 redocly 迁移、通过的验证命令清单、`make codegen-check` 在未提交 generated 变更时 dirty-tree 限制痛点，以及 `ApiError` 命名歧义 / `redoc-cli` 与 `swagger-cli` 双弃用面 / `codegen-check` dirty-tree 三类根因。
- 给出对流程资产的低优先级改进建议：未来需要更频繁 generated remediation 时，可补充只做重生成 + lint、不做 `git diff --exit-code` 的 `make codegen-openapi-verify` target，或文档化「dirty tree 验证组合」；后续主路径仍为 `openapi-v1-contract/002-fixtures-and-mock-source` 与 `003-breaking-change-gate`。
- 同步 `docs/reports/INDEX.md` 新增条目。

### 关联 Commit

- `docs(reports): land openapi-v1-contract/001-bootstrap remediation 复盘报告`

### 备注

本次为 `/retrospective` 产出的复盘报告，无运行时代码改动；短期不修改 skill / Makefile，等同类 generated remediation 增多后再触发流程优化。

## 15:15 工作记录

### 完成事项

- Phase 1 落地 `openapi-v1-contract/002-fixtures-and-mock-source`：14 tag 子目录 + 36 份 default fixture（spec §3.1.1 全覆盖），文件骨架 `{operationId, scenarios: {default: {request?, response: {status, headers?, body}}}}`，`scenarios.default` 强制为首键。
- 36 份 default fixture 内容贴合 spec §3.1 / §4.6 / §4.7：列表 endpoint 1–3 条 + `pageInfo.nextCursor: null`；长耗时 op 走 `202 + *WithJob` 且 `job.jobType` 与 endpoint 对齐；6 个 AI schema（`getTargetJob.summary` / `fitSummary` / `appendSessionEvent.assistantAction` / `getFeedbackReport` / `listMistakes` / `getResumeTailorRun` / `getDebrief`）携带完整 `GenerationProvenance`（6 字段非空，非评分场景填 `not_applicable`）；`POST /privacy/exports` 锁定 `501 + error.code = "PRIVACY_EXPORT_NOT_AVAILABLE"`，`POST /privacy/deletions` 保持 `202 + PrivacyRequestWithJob`；id 全部 UUIDv7 字面量（无 `tmp_`），datetime 全部 RFC3339 UTC，邮箱 / 电话 / 公司名走保留占位（`example.com` / `+1-555-01xx` / `Acme`）。
- 落地 `scripts/lint/validate_fixtures.py`：纯 Python 实现 OpenAPI 3.1 子集 schema 校验（type / required / oneOf / allOf / anyOf / $ref / enum / format=uuid+date-time+uri+email），叠加 6 大 AI schema provenance 强制校验、隐私 allowlist + 黑名单（仅锁雇主品牌；AI vendor 名例外）、UUIDv7 / `tmp_` 扫描、36 operationId 全覆盖；接入根 `Makefile` `make validate-fixtures` 与 `make help`。
- 单元测试：`scripts/lint/validate_fixtures_test.py`（16 用例，结构 + 内容契约直读 fixtures）+ `scripts/lint/validate_fixtures_cli_test.py`（7 用例，tempdir 故障注入：删 fixture / 删 provenance.modelId / `tmp_` id / privacy export 改 202 / 真实邮箱域名 / operationId 与文件名失配）。
- `_generate_default_fixtures.py` 作为 1.2 一次性 bootstrap 落入 `scripts/codegen/`，未挂 Makefile target；后续刷新由 Phase 2.2 的 `make sync-fixtures-from-prototype` 接管。
- 验证：`make validate-fixtures` exit 0；`python3 -m unittest scripts.lint.validate_fixtures_test scripts.lint.validate_fixtures_cli_test` 全部 23 用例通过；`make help` 列出 `validate-fixtures`。

### 关联 Commit

- `feat(openapi): 落地 36 default fixtures 与 validate-fixtures 门禁`

### 备注

Auto-committed by /tdd phase-commit, plan: openapi-v1-contract/002-fixtures-and-mock-source

## 15:45 工作记录

### 完成事项

- Phase 2 落地 `openapi-v1-contract/002-fixtures-and-mock-source` 的 prototype-baseline 同步工具：`openapi/fixtures/PROTOTYPE_MAPPING.md` 列出 8 个 P0 闭环关键 endpoint（`getMe` / `listExperienceCards` / `listTargetJobs` / `getTargetJob` / `getPracticeSession` / `getFeedbackReport` / `listMistakes` / `getGrowthOverview`）的 data.jsx 数据节映射、归一化规则（UUIDv7 派生、固定 RFC3339 锚点、enum 翻译表、雇主品牌 → `Acme` / `Lumen Labs` / `Helios Group` 重写）、prototype-baseline 单向同步契约（fail-fast on mapping gap，不静默兜底）。
- 落地 `scripts/codegen/sync_fixtures_from_prototype.py`：通过 `node` subprocess 解析 `easyinterview-ui/src/data.jsx`（`vm.runInContext` 安全 evaluate `window.EI_DATA`）→ 8 个 mapper 输出 OpenAPI schema-valid `prototype-baseline` body → 写入每个 fixture 的 `scenarios.prototype-baseline` 节（`default` 始终保持首键）→ 调用 `validate_fixtures.validate()` 作为最后一道 schema 门禁；接入根 `Makefile` `make sync-fixtures-from-prototype`（接 `make help`）。
- 单元测试：`scripts/codegen/sync_fixtures_from_prototype_test.py`（4 用例：sync 退出 0 + 8 P0 endpoint prototype-baseline 非空 / 再跑一次幂等（snapshot 对比）/ 后 `validate_fixtures.py` exit 0 / 故障注入 — drop `experiences` 节后 sync exit≠0 且日志含 `Mapping gap: listExperienceCards requires data.experiences`）。
- 在 `easyinterview-ui/src/data.jsx` 新增 `experiences` 数据节（5 张 STAR-style 经历卡片，从 `screens-p1-depth.jsx#ExperienceLibraryScreen` 上提到 data.jsx）以补齐 `listExperienceCards` 的 prototype 数据节，避免在 plan 2.4 自检的 P0 8 个 endpoint 出现映射缺口；UI 屏幕本身不调整。
- 落地 `openapi/fixtures/README.md`：scenario 命名规则（`default` 必填且首键、`prototype-baseline` 由 sync 工具单向产出、其它 `<purpose>-<variant>` 由 consumer 持有）、consumer 选择 scenario 的契约（默认 fallback `default`、未声明 scenario 必须 fail loudly 不静默兜底）、`make validate-fixtures` / `make sync-fixtures-from-prototype` 入口说明、隐私 placeholder 约束、E1 mock-server / msw 同源声明。
- 验证：`make sync-fixtures-from-prototype` exit 0（再跑一次幂等）；`make validate-fixtures` 同时通过 `default` 与 `prototype-baseline` exit 0；`python3 -m unittest scripts.lint.validate_fixtures_test scripts.lint.validate_fixtures_cli_test scripts.codegen.sync_fixtures_from_prototype_test` 27 用例全部通过；`make help` 同时列出 `validate-fixtures` 与 `sync-fixtures-from-prototype`。

### 关联 Commit

- `feat(openapi): 落地 prototype-baseline scenario 同步工具`

### 备注

Auto-committed by /tdd phase-commit, plan: openapi-v1-contract/002-fixtures-and-mock-source

## 18:02 工作记录

### 完成事项

- Phase 3 落地 fixtures → OpenAPI named examples 投影 + Prism 同源 smoke：`scripts/codegen/render_openapi_fixture_examples.py` 读取 `openapi/openapi.yaml` + `openapi/fixtures/`，把每个 fixture 的 `scenarios.default.response.body` 投射为 OpenAPI named example `default`，输出到 `openapi/.generated/openapi-with-fixtures.yaml`；36 个 default example 全覆盖（包括 logout 204 / startAuthEmailChallenge 202 等无 body 操作合成空 content slot）；重复运行幂等（sha256 一致）。
- 接入根 `Makefile` `make render-openapi-fixture-examples`（接 `make help`）；`scripts/codegen/render_openapi_fixture_examples_test.py` 用 4 用例验证 run 干净 / 36 example 全覆盖 / 5 个固定 op 的 example body 与 fixture body 字节级一致 / 二次运行 sha256 不变。
- 收口 spec C-9 partial：从 `openapi/openapi.yaml` 删除 `requestPrivacyExport` 501 的人写 inline `example` block（B2 002 §3.1「不得人工手写 OpenAPI examples」），改由 `openapi/fixtures/Privacy/requestPrivacyExport.json` 单源持有；`scripts/lint/openapi_inventory.py` 同步放弃旧的 example-content 校验，改为强制 `schema $ref ApiErrorResponse`；新增 `validate_fixtures.py#check_privacy_export_error_code` 强制 fixture default scenario `status=501 + error.code=PRIVACY_EXPORT_NOT_AVAILABLE`，spec D-12 / C-7 实质保护从 openapi.yaml 单点迁到「openapi.yaml structural + fixture content」双校验。
- 5 op Prism smoke 全部 byte-equal vs fixture：`getMe` / `listTargetJobs` / `getPracticeSession` / `getFeedbackReport` / `requestPrivacyExport`；落地 `scripts/codegen/prism_fixture_smoke.py` 作为可重复 smoke 入口（`make render-openapi-fixture-examples` → 启动 Prism → `python3 scripts/codegen/prism_fixture_smoke.py` → exit 0）。spec C-9 中「fixture 唯一真理源 + default scenario → OpenAPI example → Prism response 字节级一致」由本 plan 关闭；剩余「真实 msw / 后端 mock-server 同字节」由 W2 [E1 mock-contract-suite](../spec/engineering-roadmap/spec.md#55-layer-e--integration4-份) / D1 frontend-shell 在后续 plan 闭合。
- 更新 `openapi/README.md`：新增 `fixtures/` 与 `.generated/` 在 layout 中的位置、`make` entry-point 表追加 3 个 B2 002 owner 的 target、明确 `openapi.yaml` 不再持有 hand-written examples、链接到 `openapi/fixtures/README.md` 总入口；`openapi/fixtures/README.md` 收口 Prism smoke matrix（5 op 命令、`Prefer: example=default` / 501 需要 `code=501, example=default`）+ E1 handoff 出口。

### 关联 Commit

- `feat(openapi): 落地 fixtures→OpenAPI examples 投影与 Prism 同源 smoke`

### 备注

Auto-committed by /tdd phase-commit, plan: openapi-v1-contract/002-fixtures-and-mock-source

## 18:14 工作记录

### 完成事项

- Phase 4.1 收口 spec 验收：`make codegen-check` exit 0（lint-openapi + inventory + git diff 全部干净）；`make validate-fixtures` exit 0（spec C-6 / C-11 / 隐私 / UUIDv7 / 36 op 覆盖）；`scripts.lint.validate_fixtures_test` + `validate_fixtures_cli_test` + `sync_fixtures_from_prototype_test` + `render_openapi_fixture_examples_test` 共 31 用例通过；`make sync-fixtures-from-prototype` 与 `make render-openapi-fixture-examples` 二次幂等（`git diff --exit-code -- openapi/fixtures openapi/.generated` 干净）；Prism 5.14.2 + 5 op smoke 全部 byte-equal vs fixture（`getMe` / `listTargetJobs` / `getPracticeSession` / `getFeedbackReport` / `requestPrivacyExport`，含 spec C-7 P0 `501 + error.code = "PRIVACY_EXPORT_NOT_AVAILABLE"`）；至少 8 个 P0 闭环 endpoint 的 `prototype-baseline` 节非空且 schema-valid。
- Phase 4.2 文档与 INDEX 同步：把 `002-fixtures-and-mock-source` plan/checklist 状态从 `active` 切到 `completed`（更新日期保持 2026-04-28）；`/sync-doc-index --fix-index` 把 `docs/spec/openapi-v1-contract/plans/INDEX.md` 中本 plan 行迁到「已完成」组；最终 `python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 报告 `All documents are in sync. Zero drift detected.`；同 subspec 下 001 保持 completed，003 保持 active 等待自身 Phase 4 收口 B2 freeze handoff。
- Phase 4.3 E1 handoff 声明：W2 [E1 mock-contract-suite](../spec/engineering-roadmap/spec.md#55-layer-e--integration4-份) 启动时直接消费 `openapi/fixtures/<tag>/<operationId>.json`（fixture 真理源）+ `openapi/openapi.yaml`（schema 真理源）+ `openapi/.generated/openapi-with-fixtures.yaml`（Prism / docs-site 入口），不重建 fixture 或 example 真理源；frontend `msw` 与 backend `mock-server` 在 W2 起共享同一份 fixtures，前端禁止 hardcode mock（已写入 `openapi/fixtures/README.md`）；本 plan 不修改 E1 spec / plan，spawn 由 roadmap owner 触发；spec C-9 中「真实 msw / 后端 mock-server 同字节」由 E1 / D1 后续 plan 在 W2 闭合，不在本 plan 范围。

### 关联 Commit

- `docs(openapi): 收口 002-fixtures-and-mock-source plan 与 plans/INDEX`

### 备注

Auto-committed by /tdd phase-commit, plan: openapi-v1-contract/002-fixtures-and-mock-source

## 18:54 工作记录

### 完成事项

- 落地 `docs/reports/2026-04-28-openapi-v1-contract-002-fixtures-and-mock-source-assessment.md`：复盘 002 全 4 phase 的成功证据（make codegen-check / make validate-fixtures / 31 单元测试 / sync + render 二次幂等 / Prism 5 op byte-equal）与 4 类阻点（openapi.yaml inline example 与 fixture 真理源冲突 / Prism 非 2xx Prefer 写法 / data.jsx 与 ExperienceLibraryScreen 数据分裂 / `/tdd` Step 9.5 phase-commit chained 命令被 hook false-negative）+ 1 类已知项再现（`make codegen-check` dirty-tree 限制再次累一笔证据，未触发流程改造）。
- 给出 5 条改进建议，落点覆盖 spec-plan / skill / README，全部不直接修改资产，留给后续 `/implement` 或 `/plan-review` 触发；建议 1 标 high（在 002 plan §3.1 显式要求清理 openapi.yaml 残留 inline example），建议 2-4 标 medium（Prism 多状态码用法 / prototype 真理源边界 / Step 9.5 命令分步），建议 5 标 low 保持观察。
- 同步 `docs/reports/INDEX.md` 新增条目。

### 关联 Commit

- `docs(reports): land openapi-v1-contract/002-fixtures-and-mock-source 交付复盘报告`

### 备注

本次为 `/retrospective` 产出的复盘报告，无运行时代码改动；003-breaking-change-gate 仍 active，不在本次范围。
