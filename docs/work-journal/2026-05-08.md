# 2026-05-08 工作日志

## 09:00 工作记录

### 完成事项

- 派生 `frontend-shell/003-ui-design-pixel-parity-gate` plan 文档（plan v1.0 / checklist v1.0 / bdd-plan v1.0 / bdd-checklist v1.0 / context.yaml），把 D2 retrospective 列出的最高优先级 follow-up 落地为可执行 checklist：5 个 phase + 13 个 implementation checklist + 7 个 BDD checklist + 4 个 regression 重跑。
- 修订 [`docs/spec/frontend-shell/spec.md`](../spec/frontend-shell/spec.md) 至 v1.8：§6 新增 C-9 真实浏览器 pixel parity gate，要求 Playwright + chromium 在 desktop (1440×900) / mobile (390×844) 两个 viewport 下断言 `frontend/dist/index.html` 与 `ui-design/index.html` golden preview 的 DOM 锚点 / computed style / bounding box / 截图差异，E2E.P0.005（jsdom）保留作为 fast smoke gate；§7 关联计划同步追加 003 行。
- 同步 [`docs/spec/frontend-shell/history.md`](../spec/frontend-shell/history.md) 至 v1.8 记录派生 003 与 C-9 新增；[`docs/spec/frontend-shell/plans/INDEX.md`](../spec/frontend-shell/plans/INDEX.md) 在「进行中」段追加 003 行；通过 `/sync-doc-index --fix-index` 把 `docs/spec/INDEX.md` 同步到 spec v1.8 / 2026-05-08。
- 完成验证：context validator OK；`make docs-check` zero drift；`check_md_links` 双 OK。

### 关联 Commit

- `docs(frontend-shell): derive D2 follow-up pixel parity gate plan`

### 备注

派生只动文档，不动 `frontend/` / `ui-design/` 运行时代码。下一步 `/implement frontend-shell/003-ui-design-pixel-parity-gate frontend` → `/tdd` 按 5 个 phase 推进；首要风险是 chromium 二进制下载与 ui-design CDN 依赖。

## 03:55 工作记录

### 完成事项

- 落地 `frontend-shell/003-ui-design-pixel-parity-gate` Phase 1-6 全部 13 个 checklist 项 + 7 个 BDD checklist 项 + 4 个 regression 重跑：在 `frontend/` 接入 `@playwright/test ^1.59.1` + chromium-headless-shell 1217，配 desktop (1440×900) + mobile (390×844) 两个 chromium project；新增 `frontend/scripts/serve-pixel-parity.mjs`（Node 内置 http server，挂载 `frontend/dist` + `ui-design/`，暴露 `/health`），`frontend/playwright.config.ts`，4 个 spec（`tests/pixel-parity/{topbar,screens,layout,screenshot}.spec.ts`）共 21 个用例 × 2 viewport project = **42 项 Playwright PASS**。
- `auth.css` 增加 `@media (max-width: 768px)` 折叠 grid 为单列，让 mobile viewport 不再因 `0.88fr 1.12fr` 固定布局把卡片推到视口外；`layout.spec.ts` mobile 断言改为 vertical stacking + scrollHeight 包含。
- 派生 `test/scenarios/e2e/p0-006-ui-design-pixel-parity-gate/`：README §6 文档 baseline 维护、§7 离线运行限制；`scripts/{setup,trigger,verify,cleanup}.sh` 完整契约（setup 预检 chromium 缓存 + frontend dist；verify 断言 `42 passed` + 0 failed + 4 spec markers + desktop/mobile project markers + retired-entry grep 0 命中）；`test/scenarios/e2e/INDEX.md` 添加 P0.006 行（frontend-shell C-9, automated, Ready）。
- 扩写 `frontend/README.md` §2.7「Visual smoke 工具与 parity gate 重跑」段：把 D2 视觉系统改为「两层 gate 共同守住」结构 — jsdom fast smoke (E2E.P0.005) 用于日常开发循环，Playwright + chromium pixel parity gate (E2E.P0.006) 用于 CI / 主线合并。给出 chromium 安装、build、test:pixel-parity、scenario、baseline 维护、离线 vendor 五段命令。
- 新增 `frontend/.gitignore` 排除 baseline 目录（`tests/pixel-parity/__screenshots__/` + `tests/pixel-parity/*-snapshots/`）+ `.playwright-output/` + `playwright-report/`，避免截图入 git。
- 完成验证：`pnpm --filter @easyinterview/frontend test`（40 files / 237 tests）、`pnpm exec tsc -p . --noEmit` 0 错；`pnpm --filter @easyinterview/frontend build` + `make build` PASS；`E2E.P0.001 / 002 / 004 / 005 / 006` 五个 scenario `setup→trigger→verify→cleanup` 全 PASS；`make docs-check` zero drift；负向搜索：无 cypress / puppeteer / @emotion / styled-components / tailwind 依赖，无私有 brand 字体名，retired-module testid 仅在负向断言文件出现。

### 关联 Commit

- `feat(frontend-shell): land ui-design pixel parity gate (Playwright)`

### 备注

Phase 4.1 跨 ui-design golden preview 像素 diff 显式 deferred，原因：fontsource bundle 与 Google Fonts CDN 在 chromium 下渲染存在不可预测的字体子像素差异，强行接入会得到 false positive。本 plan 的 4.1 改为 frontend home baseline regression（toHaveScreenshot 自带 maxDiffPixels 阈值，捕获 frontend 内部回归），4.2 通过 token / inline style 断言守住 dark / customAccent 交互可见性，组合上仍闭环 spec.md C-9 的「DOM 锚点 + computed style + bounding box + 截图差异」四类。chromium 二进制约 92 MiB，离线 / 无外网时 ui-design 端 hash route 断言会失败，已在 scenario README §7 列出 vendor 步骤。

## 04:00 工作记录

### 完成事项

- 把 `frontend-shell/003-ui-design-pixel-parity-gate` plan / checklist / bdd-plan / bdd-checklist 四个 Header `状态` 切到 `completed`，版本升到 1.1；通过 `/sync-doc-index --fix-index` 把 `docs/spec/frontend-shell/plans/INDEX.md` 中 003 行从「进行中（Active）」迁移到「已完成（Completed）」，post-fix zero drift。

### 关联 Commit

- `docs(frontend-shell): close 003-ui-design-pixel-parity-gate plan lifecycle`

### 备注

收口 commit；下一步触发 `/retrospective --this`。

## 04:01 工作记录

### 完成事项

- 完成 `frontend-shell/003-ui-design-pixel-parity-gate` 交付复盘：写入 [`docs/reports/2026-05-08-frontend-shell-ui-design-pixel-parity-gate-assessment.md`](../reports/2026-05-08-frontend-shell-ui-design-pixel-parity-gate-assessment.md)，覆盖范围 / 成功证据 / 4 类阻点（跨字体源像素 diff 工程不可达、mobile project webkit 引擎冗余、AuthShell 在 mobile 溢出视口、`toBeVisible()` vs `toHaveCount(1)` 测试 API 选择）/ 根因分类 / 4 条改进建议（medium：`/design` Step 3.5 加 mobile breakpoint row；low：`/scenario-create` 视觉系统类 mobile chromium engine hint；done：跨字体源 diff 结论已固化进 README + plan + scenario；low：D1 plan 加 mobile breakpoint 责任声明）。
- 同步 `docs/reports/INDEX.md` 顶端追加 2026-05-08 条目；`make docs-check` zero drift。

### 关联 Commit

- `docs(reports): land ui-design pixel parity gate retrospective`

### 备注

D2 follow-up parity gate 到此正式收口；spec.md C-9 的真实浏览器 parity 验证全部可执行，retrospective 把 4 类工程现实约束 + AuthShell mobile 缺陷 + 测试 API 学习成本归档为可追溯证据。建议下一步：用户决定本分支 (D2 v2 + 003) 与 dev (D2 v1) 的整合策略；或者直接派生 `frontend-home-job-picks-and-parse` 进入 D2-D6 业务屏幕实现，已落地的视觉骨架 + 双层 parity gate 会持续守护。
