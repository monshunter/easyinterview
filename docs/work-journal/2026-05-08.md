# 2026-05-08 工作日志

## 09:00 工作记录

### 完成事项

- 派生 `frontend-shell/003-ui-design-pixel-parity-gate` plan 文档（plan v1.0 / checklist v1.0 / bdd-plan v1.0 / bdd-checklist v1.0 / context.yaml），把 D2 retrospective 列出的最高优先级 follow-up 落地为可执行 checklist：5 个 phase + 13 个 implementation checklist + 7 个 BDD checklist + 4 个 regression 重跑。
- 修订 [`docs/spec/frontend-shell/spec.md`](../spec/frontend-shell/spec.md) 至 v1.8：§6 新增 C-9 真实浏览器 pixel parity gate，要求 Playwright + chromium 在 desktop (1440×900) / mobile (390×844) 两个 viewport 下断言 `frontend/dist/index.html` 与 `ui-design/index.html` golden preview 的 DOM 锚点 / computed style / bounding box / 截图差异，E2E.P0.005（jsdom）保留作为 fast smoke gate；§7 关联计划同步追加 003 行。
- 同步 [`docs/spec/frontend-shell/history.md`](../spec/frontend-shell/history.md) 至 v1.8 记录派生 003 与 C-9 新增；[`docs/spec/frontend-shell/plans/INDEX.md`](../spec/frontend-shell/plans/INDEX.md) 在「进行中」段追加 003 行；通过 `/sync-doc-index --fix-index` 把 `docs/spec/INDEX.md` 同步到 spec v1.8 / 2026-05-08。
- 完成验证：context validator OK；`make docs-check` zero drift；`check_md_links` 双 OK。

### 关联 Commit

- `docs(frontend-shell): derive D2 follow-up pixel parity gate plan`

### 备注

派生只动文档，不动 `frontend/` / `ui-design/` 运行时代码。下一步 `/implement frontend-shell/003-ui-design-pixel-parity-gate frontend` → `/tdd` 按 5 个 phase 推进；首要风险是 chromium 二进制下载与 ui-design CDN 依赖。

## 03:55 工作记录

### 完成事项

- 落地 `frontend-shell/003-ui-design-pixel-parity-gate` Phase 1-6 全部 13 个 checklist 项 + 7 个 BDD checklist 项 + 4 个 regression 重跑：在 `frontend/` 接入 `@playwright/test ^1.59.1` + chromium-headless-shell 1217，配 desktop (1440×900) + mobile (390×844) 两个 chromium project；新增 `frontend/scripts/serve-pixel-parity.mjs`（Node 内置 http server，挂载 `frontend/dist` + `ui-design/`，暴露 `/health`），`frontend/playwright.config.ts`，4 个 spec（`tests/pixel-parity/{topbar,screens,layout,screenshot}.spec.ts`）共 21 个用例 × 2 viewport project = **42 项 Playwright PASS**。
- `auth.css` 增加 `@media (max-width: 768px)` 折叠 grid 为单列，让 mobile viewport 不再因 `0.88fr 1.12fr` 固定布局把卡片推到视口外；`layout.spec.ts` mobile 断言改为 vertical stacking + scrollHeight 包含。
- 派生 `test/scenarios/e2e/p0-006-ui-design-pixel-parity-gate/`：README §6 文档 baseline 维护、§7 离线运行限制；`scripts/{setup,trigger,verify,cleanup}.sh` 完整契约（setup 预检 chromium 缓存 + frontend dist；verify 断言 `42 passed` + 0 failed + 4 spec markers + desktop/mobile project markers + retired-entry grep 0 命中）；`test/scenarios/e2e/INDEX.md` 添加 P0.006 行（frontend-shell C-9, automated, Ready）。
- 扩写 `frontend/README.md` §2.7「Visual smoke 工具与 parity gate 重跑」段：把 D2 视觉系统改为「两层 gate 共同守住」结构 — jsdom fast smoke (E2E.P0.005) 用于日常开发循环，Playwright + chromium pixel parity gate (E2E.P0.006) 用于 CI / 主线合并。给出 chromium 安装、build、test:pixel-parity、scenario、baseline 维护、离线 vendor 五段命令。
- 新增 `frontend/.gitignore` 排除 baseline 目录（`tests/pixel-parity/__screenshots__/` + `tests/pixel-parity/*-snapshots/`）+ `.playwright-output/` + `playwright-report/`，避免截图入 git。
- 完成验证：`pnpm --filter @easyinterview/frontend test`（40 files / 237 tests）、`pnpm exec tsc -p . --noEmit` 0 错；`pnpm --filter @easyinterview/frontend build` + `make build` PASS；`E2E.P0.001 / 002 / 004 / 005 / 006` 五个 scenario `setup→trigger→verify→cleanup` 全 PASS；`make docs-check` zero drift；负向搜索：无 cypress / puppeteer / @emotion / styled-components / tailwind 依赖，无私有 brand 字体名，retired-module testid 仅在负向断言文件出现。

### 关联 Commit

- `feat(frontend-shell): land ui-design pixel parity gate (Playwright)`

### 备注

Phase 4.1 跨 ui-design golden preview 像素 diff 显式 deferred，原因：fontsource bundle 与 Google Fonts CDN 在 chromium 下渲染存在不可预测的字体子像素差异，强行接入会得到 false positive。本 plan 的 4.1 改为 frontend home baseline regression（toHaveScreenshot 自带 maxDiffPixels 阈值，捕获 frontend 内部回归），4.2 通过 token / inline style 断言守住 dark / customAccent 交互可见性，组合上仍闭环 spec.md C-9 的「DOM 锚点 + computed style + bounding box + 截图差异」四类。chromium 二进制约 92 MiB，离线 / 无外网时 ui-design 端 hash route 断言会失败，已在 scenario README §7 列出 vendor 步骤。

## 04:00 工作记录

### 完成事项

- 把 `frontend-shell/003-ui-design-pixel-parity-gate` plan / checklist / bdd-plan / bdd-checklist 四个 Header `状态` 切到 `completed`，版本升到 1.1；通过 `/sync-doc-index --fix-index` 把 `docs/spec/frontend-shell/plans/INDEX.md` 中 003 行从「进行中（Active）」迁移到「已完成（Completed）」，post-fix zero drift。

### 关联 Commit

- `docs(frontend-shell): close 003-ui-design-pixel-parity-gate plan lifecycle`

### 备注

收口 commit；下一步触发 `/retrospective --this`。

## 04:01 工作记录

### 完成事项

- 完成 `frontend-shell/003-ui-design-pixel-parity-gate` 交付复盘：写入 [`docs/reports/2026-05-08-frontend-shell-ui-design-pixel-parity-gate-assessment.md`](../reports/2026-05-08-frontend-shell-ui-design-pixel-parity-gate-assessment.md)，覆盖范围 / 成功证据 / 4 类阻点（跨字体源像素 diff 工程不可达、mobile project webkit 引擎冗余、AuthShell 在 mobile 溢出视口、`toBeVisible()` vs `toHaveCount(1)` 测试 API 选择）/ 根因分类 / 4 条改进建议（medium：`/design` Step 3.5 加 mobile breakpoint row；low：`/scenario-create` 视觉系统类 mobile chromium engine hint；done：跨字体源 diff 结论已固化进 README + plan + scenario；low：D1 plan 加 mobile breakpoint 责任声明）。
- 同步 `docs/reports/INDEX.md` 顶端追加 2026-05-08 条目；`make docs-check` zero drift。

### 关联 Commit

- `docs(reports): land ui-design pixel parity gate retrospective`

### 备注

D2 follow-up parity gate 到此正式收口；spec.md C-9 的真实浏览器 parity 验证全部可执行，retrospective 把 4 类工程现实约束 + AuthShell mobile 缺陷 + 测试 API 学习成本归档为可追溯证据。建议下一步：用户决定本分支 (D2 v2 + 003) 与 dev (D2 v1) 的整合策略；或者直接派生 `frontend-home-job-picks-and-parse` 进入 D2-D6 业务屏幕实现，已落地的视觉骨架 + 双层 parity gate 会持续守护。

## 04:42 工作记录

### 完成事项

- 修复 `frontend-shell/002-app-shell-visual-system` L2 TopBar 源级 parity 漂移：正式 TopBar 从 native theme/lang select + 独立 custom accent popover 改回 `ui-design/src/app.jsx` 一致的 brand subtitle、五入口 icon、theme menu、Custom row 内嵌 AccentPicker、icon-only dark toggle 与 `topbar-lang-toggle`。
- 收紧 TopBar Vitest + Playwright gates：新增旧 `topbar-theme-select` / `topbar-lang-select` 负向断言、theme menu / custom option / language toggle / nav icon / brand subtitle 结构断言，并把 P0.006 pixel parity 场景期望同步到 46 passed。
- 原地修订 `frontend-shell` spec/history、001/002/003 plan/checklist/BDD、`frontend/README.md` 和 P0.001/P0.004/P0.005/P0.006 场景资产，删除 active 正向 dropdown/select 旧口径；通过 `/sync-doc-index --fix-index` 和 `make docs-check` 保持 Header / INDEX zero drift。
- 新增 [BUG-0021](../bugs/BUG-0021.md) 与 [TopBar UI-Design Parity 交付复盘](../reports/2026-05-08-frontend-shell-topbar-ui-design-parity-assessment.md)，并把本轮暴露的 UI parity review 盲点固化到 `.agent-skills/plan-code-review/SKILL.md`：后续必须反查 `ui-design/src/*.jsx` 的 DOM shape、控件类型、menu/popup 层级、icon、aria 和主要交互路径。
- 完成验证：`pnpm --filter @easyinterview/frontend test`（40 files / 237 tests PASS）；`pnpm --filter @easyinterview/frontend test:pixel-parity`（46 tests PASS）；`E2E.P0.004` 与 `E2E.P0.006` setup→trigger→verify→cleanup PASS；`pnpm --filter @easyinterview/frontend typecheck` PASS；`pnpm --filter @easyinterview/frontend build` PASS；`make build` PASS；`make docs-check` zero drift；`git diff --check` clean。

### 关联 Commit

- `fix(frontend-shell): restore topbar ui-design source parity`

### 备注

本次单一提交跨 `frontend/`、`docs/`、`test/scenarios/` 与 skill，因为它们共同闭环同一个问题：TopBar 源级复刻漂移、旧文档口径污染和 review gate 盲点。See [BUG-0021](../bugs/BUG-0021.md)。

## 04:44 工作记录

### 完成事项

- 继续沉淀本轮 TopBar parity 暴露的流程漏洞：更新 `.agent-skills/design/SKILL.md`，在 `/design` 的 Brief、Coverage Matrix、implementation plan、test plan 和 coverage integrity 校验中新增 `ui-design/` 源级 parity 要求。
- 新规则要求 UI plan 在设计阶段就区分 source-structure parity（DOM composition、control type、menu/popover hierarchy、icons、labels、aria state、primary interactions）、visual-geometry parity（computed style、spacing、typography、bounding box、screenshot）和 stale-contract negative searches，避免后续只靠 pixel/bounding-box gate 误判“源级复刻已完成”。

### 关联 Commit

- `feat(skills): require ui-design source parity matrix`

### 备注

这是流程沉淀提交，不修改 runtime 代码；它把本轮已在 `/plan-code-review` 补上的后置审查规则前移到 `/design` 产出 plan/checklist 的阶段。

## 05:55 工作记录

### 完成事项

- 将前后端分离开发建议固化为当前执行契约：`docs/development.md` 新增 Frontend / Backend Contract Workflow、operation matrix、frontend-first / backend-first path、AI provider boundary 与 Agent Enforcement。
- 同步 `frontend/README.md`、`backend/README.md`、`deploy/dev-stack/README.md`、根 `README.md` 与 `test/README.md`，明确 OpenAPI / fixtures / generated client / real handler / Docker Compose / Kind scenario 的边界，并修正 `test/scenarios/` 仍未落地的过期表述。
- 更新 `AGENTS.md` 与 `.agent-skills/{implement,tdd,plan-review,plan-code-review}/SKILL.md`，要求相关实施、验证和 L1/L2 review 在动手前读取 `docs/development.md` §2 与模块 README；缺 operation matrix 时阻断并回到 `/plan-review --fix`。
- 完成验证：`make docs-check` zero drift；`git diff --check` clean；规则搜索确认 `/implement`、`/tdd`、`/plan-review`、`/plan-code-review`、`AGENTS.md` 均已覆盖前后端契约预读。

### 关联 Commit

- `docs(governance): codify frontend backend contract workflow`

### 备注

本提交只沉淀执行规章和模块 README，不修改运行时代码。下一步按用户要求进入 AI provider 范围修订：移除 embedding / rerank 预埋并切换 DeepSeek Flash / Pro 为当前开发主力 provider。

## 06:35 工作记录

### 完成事项

- 完成 A3 `003-provider-registry-and-capability-profiles` Phase 6：从 AIClient 接口、provider registry、profile catalog、OpenAI-compatible adapter、stub、observability decorator、B1 generated capability、B3 job manifest、B4 migration baseline、A2 dev-stack 和相关 lint / fixtures 中移除 embedding / rerank 当前实现与基础设施。
- 将 repo-tracked 开发期 AI provider 收敛为 `deepseek`，chat profiles 只使用 `deepseek-v4-flash` / `deepseek-v4-pro`；STT / realtime / judge 保持 fail-closed，占位说明明确 DeepSeek V4 Flash/Pro 不提供 speech / judge adapter。
- 同步 A3 / ADR-Q6 / engineering-roadmap / A2 / B1 / B3 / B4 / F3 / OpenAPI / README / INDEX 文档，关闭 `ai-provider-and-model-routing/003` lifecycle，并新增 [交付复盘](../reports/2026-05-08-ai-provider-deepseek-retrieval-cleanup-assessment.md)。
- 完成验证：focused Go tests PASS；`ai_profile_coverage` PASS；`migrations_lint` PASS；相关 lint pytest 76 passed / 1962 subtests passed；frontend conventions/jobs Vitest 33 passed；`make lint-config` PASS；`make lint-events` PASS；`make docs-check` zero drift；active-scope negative search 仅剩 Go/OpenAPI 自身 `go:embed` / `embedded` 语义命中。

### 关联 Commit

- `refactor(ai-provider): remove retrieval placeholders and use deepseek`

### 备注

本提交是单一跨契约清理：代码、配置、迁移、生成物、dev-stack 与 owner docs 必须一起变更才不会留下悬空能力。`make codegen-check` 在 dirty worktree 中会展示 intended generated diff；本轮使用 `make codegen-conventions && make codegen-events` + lint/tests/docs-check/negative search 证明生成物已同步。

## 06:42 工作记录

### 完成事项

- 按用户决策将 A2 本地 dev stack 默认 Postgres 镜像升级为 `postgres:18-alpine`，同步 `deploy/dev-stack/README.md` 的资源说明和服务表。
- 原地修订 A2 `local-dev-stack` 与 B4 `db-migrations-baseline` spec / history / plan / checklist，把本地 DB 前提统一到 Postgres 18，并通过 `/sync-doc-index --fix-index` 同步 `docs/spec/INDEX.md` 与各 plan index。
- 完成验证：`docker compose -f deploy/dev-stack/docker-compose.yaml config --quiet` PASS；`make docs-check` zero drift；`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` zero drift；`git diff --check` clean；负向搜索确认无旧 Postgres 主版本口径残留。

### 关联 Commit

- `chore(dev-stack): use postgres 18`

### 备注

本提交不拉起或迁移真实数据卷，只修改默认镜像 tag 与对应契约文档；已有本地 `easyinterview-pg-data` 如来自旧主版本，开发者需要按实际兼容性选择备份迁移或 `DEV_RESET_FORCE=1 make dev-reset` 重建。

## 08:13 工作记录

### 完成事项

- 删除 `.gemini/` 与 `GEMINI.md` 等历史 Gemini agent 配置 symlink。
- 新增 `.agents/`（skills symlink）与 `.opencode/`（opencode skills symlink + package.json + 本地 node_modules）agent 配置目录。

### 关联 Commit

- `chore(governance): migrate agent config from gemini to opencode`

### 备注

Auto-committed by /work-journal manual commit 1/2, governance config migration

## 08:14 工作记录

### 完成事项

- 升级 `ai-provider-and-model-routing` spec 至 v2.9：新增 `tts` capability、`doubao_speech` / `minimax_speech` provider-specific speech protocol、`Synthesize` 接口与 C-16/C-17/C-18 契约；联动修订 ADR、能力表、关联计划与设计约束。
- 新增 `004-cascaded-speech-provider-foundation` plan（v1.0 active），为 `stt -> chat -> tts` 级联语音 MVP 打开 provider-specific speech adapter、独立 STT/TTS profile 与隐私/观测/成本 gate。
- 新建 `practice-voice-mvp` subspec（v1.0 active），定义 P0 语音面试用户可见落地方案：低成本级联方案、多 provider 支持、barge-in committed context 规则。
- 同步 `docs/spec/INDEX.md` 与各 plans/INDEX 索引行。

### 关联 Commit

- `docs(ai-provider): add cascaded speech provider foundation spec`

### 备注

Auto-committed by /work-journal manual commit 2/2, spec documentation update

## 08:43 工作记录

### 完成事项

- Phase 1: B1 capability 与 A3 schema 对齐
  - 在 `shared/conventions.yaml` 与 B1 vocabulary 中新增 `tts` capability，同步 Go (`CapabilityTts`) / TS (`AI_CAPABILITIES.TTS`) 生成物与 `shared/fixtures/conventions-parity.json` parity fixture
  - 在 A3 `meta.go` 中新增 `ProviderProtocolDoubaoSpeech`、`ProviderProtocolMinimaxSpeech` 常量及 `CapabilityTts` alias
  - 扩展 `providerregistry/loader.go` `isAllowedProtocol`/`isAllowedCapability` 白名单支持 `doubao_speech`/`minimax_speech` protocol 与 `tts` capability
  - 扩展 `profile/loader.go` `validateProfile` 白名单支持 `tts` capability
  - 扩展 `bootstrap.go` `ResolveProvider` 为 speech protocols 返回 `AI_UNSUPPORTED_CAPABILITY`（Phase 3 实现前）
  - Red-Green: 所有 B1 parity tests、registry/profile negative tests 全部通过

### 关联 Commit

- `feat(ai-provider): b1 capability 与 a3 schema 对齐`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-provider-and-model-routing/004-cascaded-speech-provider-foundation, phase: Phase 1: B1 capability 与 A3 schema 对齐

## 08:44 工作记录

### 完成事项

- Phase 2: Synthesize provider-neutral 接口
  - 定义 `SynthesisInput` / `SynthesisResponse` 类型，含文本、voice、format、speakingRate、language、metadata 与音频摘要 (Audio `json:"-"` 隐私门禁)
  - 扩展 `AIClient` / `Provider` 接口新增 `Synthesize` 方法，`Client.Synthesize` 实现 `CapabilityTts` dispatch + fallback + meta merge
  - stub provider 实现 deterministic TTS placeholder (SHA256-based seed)
  - openai_compatible adapter 返回 `AI_UNSUPPORTED_CAPABILITY` (TTS 走 doubao/minimax speech)
  - observability Wrap 新增 `Synthesize` delegate + `recordSynthesizeCall` + `synthesisAuditSummary` (隐私: 文本/音频 hash 摘要)
  - Red-Green: 4 个 client Synthesize tests + 3 个 stub TTS tests + 全量 AI 测试通过

### 关联 Commit

- `feat(ai-provider): synthesize provider-neutral 接口`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-provider-and-model-routing/004-cascaded-speech-provider-foundation, phase: Phase 2: Synthesize provider-neutral 接口

## 08:45 工作记录

### 完成事项

- Phase 3: 豆包与 MiniMax speech adapters
  - 实现 `doubao_speech` adapter：Synthesize (TTS via JSON POST) + Transcribe (STT via JSON POST)，自定义 `/v1/tts/synthesize` / `/v1/audio/recognize` wire 路径，不复用 OpenAI multipart Audio Transcriptions
  - 实现 `minimax_speech` adapter：Synthesize (TTS via JSON POST)，Transcribe 返回 `AI_UNSUPPORTED_CAPABILITY`（MiniMax STT 未确认 per plan 004），优先 `speech-02-turbo` 模型
  - 两个 adapter 均有 mockserver (httptest.Server + Behavior) 与 contract tests 覆盖 happy path、4xx/5xx、missing audio、protocol rejection
  - negative tests 证明 doubao_speech 不复用 OpenAI Audio Transcriptions wire 假设
  - bootstrap.go 接入两个 adapter 的 runtime protocol→adapter 分派（替换之前的 `AI_UNSUPPORTED_CAPABILITY` stub）

### 关联 Commit

- `feat(ai-provider): doubao 与 minimax speech adapters`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-provider-and-model-routing/004-cascaded-speech-provider-foundation, phase: Phase 3: 豆包与 MiniMax speech adapters

## 09:16 工作记录

### 完成事项

- Phase 4: Profile catalog 与独立配置
  - 新增 `practice.voice.stt.default` + `practice.voice.tts.default` + `debrief.voice.tts.default` profiles 到 `config/ai-profiles.yaml`
  - 新增 `doubao_speech` + `minimax_speech` providers 到 `config/ai-providers.yaml`，stub provider 添加 `tts` capability
  - 默认策略：STT 默认豆包 `doubao_speech`，TTS 默认豆包，MiniMax `speech-02-turbo` 作为 fallback
  - 新增 4 个 env keys 到 `.env.example` + `secrets-and-config/spec.md` §3.1.1/§3.1.2
  - 更新 `scripts/lint/ai_profile_coverage.py` `ALLOWED_CAPABILITIES` 增加 `tts`
  - `make lint` 全绿（除预存 `JobTypeEmbeddingUpsert` 问题）

- Phase 5-6: Observability / privacy / failure semantics + Verification handoff
  - 确认观测标签已自动支持 `capability=tts`（unbounded string label via `standardLabels`）
  - 隐私红线已在 Phase 2 通过 `synthesisAuditSummary` hash 摘要 + `Audio json:"-"` + payload tests 覆盖
  - failure semantics 已在 client/provider tests 覆盖（timeout/secret missing/unsupported → shared `AI_*` 错误码）
  - 全部 11 个 AI 包测试通过；`make codegen-check`/`make docs-check`/`make lint-config` zero drift
  - negative search：cascade 不标 `realtime`，无独立 `voice` route，一 profile 一目录 legacy 仅出现在 rejection test

### 关联 Commit

- `feat(ai-provider): voice profiles 与 drift gate 收口`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-provider-and-model-routing/004-cascaded-speech-provider-foundation, phase: Phase 4-6: Profile catalog + Observability + Verification

## 09:22 工作记录

### 完成事项

- TTS 隐私测试收口
  - 新增 `TestPrivacy_SynthesizeNoPlaintextLeaksAndUsesTTSLabels`，验证 TTS 路径下 sensitive text 不泄漏到 7 个 metric family label、log fields、ai_task_runs row、audit metadata
  - 新增 `sensitiveSynthesisInner` mock（实现 AIClient 接口含 Synthesize）+ `sensitiveTTSTokens`
  - `decorator_test.go` 新增 `practice.voice.tts.default` TTS profile + `sampleSynthesisInput()` helper
- lint drift 收口
  - `conventions_yaml.py` `REQUIRED_AI_CAPABILITIES` 增加 `tts`
  - `conventions_yaml_test.py` 重构 `test_rejects_missing_ai_capability` 为 subTest 参数化遍历所有 required capabilities（新增 stt/tts/realtime 覆盖）

### 关联 Commit

- `feat(ai-provider): tts privacy tests and lint capability fix`

### 备注

补充 004-cascaded-speech-provider-foundation Phase 5.2 观测隐私红线覆盖：Synthesize 路径的 metric/log/DB/audit 明文泄漏防御。conventions_yaml.py lint 此前漏了 tts 的 REQUIRED_AI_CAPABILITIES 声明。

## 09:25 工作记录

### 完成事项

- 固化 commit message 语言策略为强制执行规则
  - `scripts/git-hooks/commit-msg` 新增 ASCII-only 校验：提交时 `LC_ALL=C perl` 扫描 non-ASCII 字符并拒绝
  - 新增 `scripts/lint/commit_msg_hook_test.py` 覆盖 ASCII/非 ASCII/unary/empty 边界
  - `.agent-skills/work-journal/SKILL.md` 新增 §Commit Message Language Policy：所有 commit message 必须英文 + ASCII-only，含 subject/body/bullet/INDEX 关联字段
  - `AGENTS.md` §7 Git 分支策略追加 commit message 英文 + ASCII-only 约束
  - 同步 `.agent-skills/init-docs/templates/` 和 `docs/work-journal/` 的 README/TEMPLATES 模板

### 关联 Commit

- `chore(governance): enforce english ascii-only commit messages`

### 备注

起因：前一个 commit 含中文 subject（"tts 隐私测试与 lint capability 收口"）违反新增的 ASCII-only 规则。本变更将所有关联 infra（hook/skill/governance/docs）一次收口。

## 09:59 工作记录

### 完成事项

- 修复 AI provider speech adapter timeout gate：`doubao_speech` 与 `minimax_speech` 的 JSON 请求 helper 现在按 profile `timeout_ms` 创建 context deadline，并把 deadline failure 映射为 retryable `AI_PROVIDER_TIMEOUT`。
- 补齐 delayed mockserver contract coverage：Doubao TTS / Doubao STT / MiniMax TTS 都新增慢响应测试，验证不会等待上游自然返回，也能触发 client fallback 所需的 shared error code。
- 新增 [BUG-0022](../bugs/BUG-0022.md)，记录 L2 review 暴露的 speech adapter timeout coverage gap。

### 关联 Commit

- `fix(ai-provider): enforce speech adapter timeouts`

### 备注

本次是 `ai-provider-and-model-routing/004-cascaded-speech-provider-foundation` Phase 3/5 的 L2 remediation，不新增 API / OpenAPI / UI scope；`PATTERNS.md` 暂未更新，后续如要沉淀 provider adapter timeout checklist 可单独确认。

## 10:20 工作记录

### 完成事项

- 修复 `ai-provider-and-model-routing/004-cascaded-speech-provider-foundation` Phase 3 L2 review 缺口：Doubao speech contract tests 新增 missing `BaseURL` / `APIKey`、TTS unsupported format、STT unsupported audio format 覆盖。
- 强化 provider-specific STT wire negative gate：Doubao mockserver 新增 last request 捕获，测试现在断言 `POST /v1/audio/recognize` + `application/json`，不再只以 error presence 证明未复用 OpenAI Audio Transcriptions wire。
- 补齐 speech adapter contract provenance：Doubao / MiniMax package docs 记录本次审查使用的官方文档 URL、reviewed date，并声明 repo mockserver 是 plan 004 的可审计 normalized fixture source。
- 完成验证：Doubao / MiniMax focused tests、`go test ./backend/internal/ai/aiclient/... -count=1`、`ai_profile_coverage`、`make lint-config`、`make codegen-check`、`git diff --check` 均通过。

### 关联 Commit

- `fix(ai-provider): harden speech adapter contract gates`

### 备注

按用户要求忽略当前工作树中两个未跟踪新 spec 目录：`docs/spec/backend-targetjob/` 与 `docs/spec/frontend-home-job-picks-and-parse/`。`make docs-check` 因这两个无关目录的 orphan / broken links 失败，本次提交不纳入这些目录。

## 12:24 工作记录

### 完成事项

- 同步多份 spec 文档版本：frontend-shell v1.9→v1.11、event-and-outbox-contract v2.0→v2.1、observability-stack v1.8→v1.9、openapi-v1-contract v1.12→v1.13、shared-conventions-codified v1.13→v1.14
- 跨所有 frontend-shell spec/plan/checklist/BDD 文档将 "language toggle" 口径统一修订为 "language dropdown"
- 新增 D-7 locale catalog 契约（`SUPPORTED_LOCALES` 元数据 + `localeCatalog.ts` 真理源）和 D-8 brand/version placement 规则到 frontend-shell spec
- 新增 D-13 event sourceType 语义说明到 event-and-outbox-contract spec
- 新增 targetJob import/parse 最小指标到 observability-stack spec
- 更新 ui-design 文档（INDEX、README、auth-and-entry、module-map、ui-architecture、user-flow、user-profile-and-settings）
- 同步 docs/spec/INDEX.md、各 plan/INDEX、docs/reports/INDEX.md

### 关联 Commit

- `docs(spec): sync spec versions and language dropdown terminology`

### 备注

Commit 1/4，仅含文档变更。

## 12:25 工作记录

### 完成事项

- TopBar 语言切换从 icon toggle 升级为 icon dropdown：按钮显示当前语言标签并打开 `topbar-lang-menu`，选项使用 `topbar-lang-option-{locale}` 稳定 testid
- 新增 `localeCatalog.ts` 集中管理 `SUPPORTED_LOCALES` 元数据（code / label / shortLabel / aliases），提供 `resolveSupportedLocale()` 和 `normalizeLocaleTag()` 工具函数
- DisplayPreferencesProvider 接入 locale catalog：初始语言从 `localStorage["ei-lang"]` 读取，用户显式选择后持久化；移除内联 `Lang` 类型定义改从 catalog 导入
- TopBar 品牌区移除 `面试训练器 · v1.0` 副标题，版本信息迁入 SettingsScreen 产品信息区
- SettingsScreen 新增 `settings-app-info` 区域展示产品名称和版本号
- 所有组件测试、场景测试、像素对比测试同步更新断言

### 关联 Commit

- `feat(frontend-shell): implement language dropdown and locale catalog`

### 备注

Commit 2/4，前端代码变更。

## 12:26 工作记录

### 完成事项

- ui-design 原型 app.jsx 语言切换从 icon toggle（`中 · EN` / `EN · 中`）改为 icon dropdown，增加 `LANGUAGE_OPTIONS` catalog 和 `getInitialLanguage()` 从 localStorage 恢复
- ui-design 原型 app.jsx 品牌区移除 `面试训练器 · v1.0` 副标题
- ui-design 原型 screens-p0-complete.jsx Settings 页面新增产品信息区

### 关联 Commit

- `feat(ui-design): add language dropdown and product info to prototype`

### 备注

Commit 3/4，静态原型变更。

## 12:27 工作记录

### 完成事项

- 更新 p0-004 场景文档和 verify.sh：language toggle → language dropdown
- 更新 p0-006 场景文档和 verify.sh：46 passed → 48 passed，补充 language dropdown 断言描述

### 关联 Commit

- `test(scenarios): update language dropdown terminology and pass counts`

### 备注

Commit 4/4，场景测试资产变更。

## 12:28 工作记录

### 完成事项

- 提交 `docs/reports/2026-05-08-frontend-shell-language-dropdown-assessment.md` 复盘报告

### 关联 Commit

- `docs(reports): add language dropdown delivery assessment`

### 备注

补充提交复盘报告文件。

## 13:03 工作记录

### 完成事项

- 0.1 修订 B1/B2 TargetJob API 场景契约：`shared/conventions.yaml` 新增 4 个 TARGET_* 错误码，重生成 Go/TS/OpenAPI codegen；`TargetJobRequirement.kind` 扩到 `must_have / nice_to_have / hidden_signal / interview_focus`；`importTargetJob.json` 增加 `manual-text-primary / manual-form-ready-terminal-job / url-invalid-source / url-source-unavailable`，`updateTargetJob.json` 增加 `invalid-state-transition / cross-user-hidden-not-found`；`make codegen-check && make validate-fixtures` 通过
- 0.2 修订 B3 import source event 语义：在 `backend/internal/shared/events/` 增加 `MapAPISourceTypeToEvent` 与 `ErrManualFormNotEventSource`，把 B2 `manual_text → text` 锁死，禁止 `manual_form` 写入 `target.import.requested.sourceType`；`make codegen-events && make lint-events` 通过
- 0.3 修订 F1 TargetJob metrics registry：新建 `backend/internal/targetjob/observability.go` 登记 3 个 metric 名 + label 集合 + F1 allowlist；`observability_test.go` 反查 F1 spec 字典登记并断言 `target_id / user_id / source_url / prompt_version / raw_jd_text / authorization` 等高基数 / PII 标签被拒
- focused tests: `TestTargetJobErrorCodes_Documented` / `TestMapAPISourceTypeToEvent_*` / `TestTargetJobMetricsAreRegisteredInF1Spec` 等全部通过

### 关联 Commit

- `feat(backend-targetjob): seed phase 0 owner contract surface`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-targetjob/001-targetjob-import-and-parse-bootstrap

## 13:15 工作记录

### 完成事项

- 删除 `backend/internal/shared/jobs/jobs_test.go:46` 对已删除常量 `JobTypeEmbeddingUpsert` 的悬空引用，恢复 `internal/shared/jobs` 包构建与测试
- 来源: cleanup commit `9a30023 refactor(ai-provider): remove retrieval placeholders and use deepseek` 移除了 embedding capability / job / migration 等，但漏同步 B3 拥有的 jobs_test 消费侧
- 顺带提前满足 backend-targetjob/001 §6.3 active-scope 负向搜索（embedding/rerank capability 0 命中）；当前 `grep -rn 'JobTypeEmbeddingUpsert\|embedding_upsert\|embedding\.upsert\|EmbeddingUpsert'` 在 active code/yaml/json/sql 中已 0 命中

### 关联 Commit

- `fix(event-outbox): drop stale embedding job reference in jobs_test`

### 备注

由 /change-intake 路由判定为 implementation drift，无需重启 B3 / A3 已 completed 的 plan；归入当前 backend-targetjob/001 §6.3 active-scope 范畴顺手清理。

## 13:25 工作记录

### 完成事项

- 创建 `docs/bugs/BUG-0023.md`：记录 jobs_test 引用已删 `JobTypeEmbeddingUpsert` 的根因（cleanup commit 9a30023 漏同步 test consumer）、修复方案（commit cfd88e1 删除悬空引用）与验证结果
- `docs/bugs/INDEX.md` Schema 区块新增 BUG-0023 条目
- `docs/bugs/PATTERNS.md` 沉淀“模式 1：Cleanup commit 漏同步 test consumer”，给出 reverse-grep / make test 触达包子集 / 测试断言数组当作契约 anchor / 双向 enum 一致性 lint 等四条检查清单

### 关联 Commit

- `docs(bugs): record BUG-0023 stale embedding test consumer`

### 备注

由 /change-intake 路由后由 /tdd 落地修复，再由 /bug-report 沉淀知识。See [BUG-0023](../bugs/BUG-0023.md)。

## 14:30 工作记录

### 完成事项

- Phase 1: Storage / config / generated surface boundaries（1.1 / 1.2 / 1.3）
  - 1.1 `backend/internal/targetjob/store.go`：定义 `Store` 接口与 `SQLStore` 实现，覆盖 `target_jobs` / `target_job_requirements` / `target_job_sources` 三表的 insert / get / list / update / parse-result merge / source freshness。所有 read / write 按 `user_id` 过滤；越权或软删返回 `ErrTargetJobNotFound`（handler 层将映射 HTTP 404 + B1 `TARGET_JOB_NOT_FOUND`）。`ApplyParseResult` 在事务内按 `(target_job_id, kind, label)` 去重并累加 `display_order`。Cursor 用 `(updated_at, id)` 复合编码并 base64url。`store_test.go` 用 `sqlmock` 覆盖 9 个 case：insert / source insert / get / cross-user 404 / list 多过滤+pageSize clamp / 分页溢出 / 坏 cursor / lifecycle update / cross-user not found / parse merge dedupe / soft-delete 404 / source freshness。
  - 1.2 `backend/internal/targetjob/config.go`：URL fetch timeout (10s) / body cap (1 MiB) / UA 模板均为代码常量；`URLFetchUserAgent`、`IsTestAppEnv`、`MustNotIntroduceAppLevelConfigKey` 三个 helper。`config_test.go` 断言常量与 spec D-7 一致；新增 app-level config key panic 包含「先修订 secrets-and-config」提示；并对包内 `.go` 源码做反向 grep 拒绝任何 `HTTP_PROXY` / `HTTPS_PROXY` / `OUTBOUND_PROXY` / `os.Getenv` / `os.LookupEnv` 出现。
  - 1.3 `backend/internal/targetjob/handler.go`：`Handler` 桩满足 B2 generated `ServerInterface` 的 4 个 TargetJob 操作；`handler_test.go` 通过反射断言签名与 `api.ServerInterface` 完全一致并验证 stub 当前返回 501。`backend/internal/targetjob/payload.go`：`BuildTargetImportRequestedPayload` / `BuildTargetParsedPayload` / `BuildTargetAnalysisFailedPayload` / `BuildTargetImportJobPayload` 全部走 B3-typed payload，并通过 `assertNoForbiddenOutboxFields` 在 marshal 后子串 case-insensitive 拒绝 `raw_jd_text` / `source_url` / 文件 URL / prompt body / response body / `Authorization` 等违规。`payload_test.go` 11 个 case 覆盖 happy path、`manual_form` 拒绝、必填字段、负数 requirementCount、forbidden token 注入、B3 generated payload struct 字段名 negative search。
- 验证：`go test ./backend/internal/targetjob/... -count=1` PASS（8 个 store + 5 个 config + 2 个 handler + 11 个 payload + 已有 observability tests 全绿）；`go build ./backend/...` PASS；`go test ./backend/internal/shared/events/... ./backend/internal/shared/jobs/... ./backend/internal/auth/...` PASS（adjacency regression）；`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` zero drift。

### 关联 Commit

- `feat(backend-targetjob): land storage config and generated contract surface`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-targetjob/001-targetjob-import-and-parse-bootstrap, phase: Phase 1: Storage / config / generated surface boundaries

## 15:30 工作记录

### 完成事项

- Phase 2: Synchronous TargetJob CRUD（2.1 / 2.2 / 2.3 / 2.4）
  - 2.1 `importTargetJob` 同步阶段：`store.go` 新增 `ImportTargetJob` 复合方法，把 `target_jobs` + `target_job_sources` + `outbox_events(target.import.requested)` + `async_jobs(target_import)` 写入同一事务；`(user_id, idempotency_key)` 经 sha256 + per-process pepper 哈希为 `target_import:<hex>` dedupe_key，先 SELECT 命中即直接返回 existing；`manual_form` 路径只插入 `target_jobs(analysis_status='ready')` + `target_job_requirements(must_have draft)` + 一条 `async_jobs(status='succeeded')` 占位行（不发出 outbox 事件）。`service.go` 解码 B2 `oneOf` source 变体（map[string]any → 4 个 typed variant），`url` 路径在 service 层做 https / fragment / host 三项最小卫生（Phase 3.3 再加 SSRF），`manual_text` 把 `rawText` 同时写到 `raw_jd_text` 与 `target_job_sources.snapshot_text`。`handler.go` 接到 service：要求 `Idempotency-Key` header + session resolver；缺失分别映射 `VALIDATION_FAILED 400` / `AUTH_UNAUTHORIZED 401`；`ServiceImportError` 按 code 映射 HTTP 状态码（`TARGET_JOB_NOT_FOUND→404`、`TARGET_IMPORT_SOURCE_UNAVAILABLE→502`、其余 400）。
  - 2.2 `listTargetJobs`：`Service.ListTargetJobs` 透传 store filter，`Handler.ListTargetJobs` 解析 `status` / `analysisStatus` / `q` / `cursor` / `pageSize` query 参数，把 store `ListResult` 映射为 generated `PaginatedTargetJob`（`PageInfo.NextCursor` 用 `*string` 表示空 cursor）。
  - 2.3 `getTargetJob`：`Service.GetTargetJob` 调用 store `GetTargetJobByUser`，`ErrTargetJobNotFound` 映射 B1 `TARGET_JOB_NOT_FOUND`；`Handler.GetTargetJob` 通过 path param 接入。
  - 2.4 `updateTargetJob`：`Service.UpdateTargetJob` 在更新前先 `GetTargetJobByUser` 拿当前 status，按 `allowedLifecycleTransitions` 表校验状态机（`draft→preparing→applied→interviewing→offer|rejected`，任意状态可 `→archived`），非法返回 B1 `TARGET_INVALID_STATE_TRANSITION`；`UpdateLifecycleFields` 结构体只承载 status / locationText / notes / titleHint / companyNameHint，结构上保证不修改 `analysis_status`。`Handler.UpdateTargetJob` 要求 `Idempotency-Key` 与 session。
- 验证：`go test ./backend/internal/targetjob/... -count=1` PASS（新增 4 个 ImportTargetJob store-tx tests、9 个 service tests 覆盖 4 类 source / dedupe key 用户隔离 / list 过滤透传 / get 404 映射 / 状态机 4 条转换 / Idempotency-Key 必填、3 个 handler 端到端 tests，加上既有 store / config / payload / observability tests 全绿）；`go build ./backend/...` PASS。

### 关联 Commit

- `feat(backend-targetjob): wire synchronous targetjob crud handlers`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-targetjob/001-targetjob-import-and-parse-bootstrap, phase: Phase 2: Synchronous TargetJob CRUD

## 16:30 工作记录

### 完成事项

- Phase 3: Source ingestion（3.1 / 3.2 / 3.3）
  - 3.1 manual_text / manual_form 写入：Phase 2.1 已落实结构（service 路径 + store ImportTargetJob 复合方法），本节通过既有 4 个 store 与 service 测试核对断言：manual_text 写 `target_job_sources.source_type='manual_text'` + `snapshot_text=rawText` + `target_jobs.raw_jd_text=rawText`；manual_form 同步 `analysis_status='ready'` + 至少 1 条 `must_have` draft requirement、不发 outbox、async_jobs status=succeeded 占位行（不被 drainer 消费）。
  - 3.2 file 引用：Store 接口新增 `LookupFileAttachmentForUser(ctx, userID, fileObjectID)`；查询 `file_objects` 按 `(id, user_id)` + `deleted_at is null`，返回 purpose；缺失 / 越权 / 软删 → `ErrTargetJobNotFound`。Service.ImportTargetJob 在 file 分支 import 前调用：`ErrTargetJobNotFound` → B1 `TARGET_JOB_NOT_FOUND`，purpose 不符 → `TARGET_IMPORT_SOURCE_INVALID`。新增 store / service tests 5 项。
  - 3.3 URL 抓取守护：新建 `backend/internal/targetjob/urlfetch` 包，`Fetcher` 类型实现 spec D-7 envelope：scheme=https 强制、host required、UserInfo / Fragment 剥离；DNS 解析后 IP 校验拒绝 loopback / multicast / link-local / IsPrivate / unspecified / `100.64/10` CGNAT / `198.18/15` benchmark / `169.254.169.254` metadata；HTTP 重定向 hook 在每跳重新跑 IP 策略；`io.LimitReader(BodyCap+1)` 防超长；body 空白拒绝；4xx → `ErrInvalidSource`，5xx / DNS / 网络异常 → `ErrSourceUnavailable`，上层映射 B1 `TARGET_IMPORT_SOURCE_INVALID` / `TARGET_IMPORT_SOURCE_UNAVAILABLE`。Fetcher 通过依赖注入 `Resolver` + `HTTPClient` 在测试矩阵中覆盖 IPv4 / IPv6 私网 / metadata / loopback / oversize / blank / 5xx / 4xx / scheme drift / missing host / IP literal loopback 12 项。
- 验证：`go test ./backend/internal/targetjob/...` 与 `go test ./backend/internal/targetjob/urlfetch/...` PASS（targetjob package 全量绿，urlfetch 12 个 SSRF 矩阵 case 全绿）；`go build ./backend/...` PASS。

### 关联 Commit

- `feat(backend-targetjob): land file purpose check and url ssrf guard`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-targetjob/001-targetjob-import-and-parse-bootstrap, phase: Phase 3: Source ingestion (URL / file / manual)

## 17:30 工作记录

### 完成事项

- Phase 4: Async parse pipeline（4.1 / 4.2 / 4.3 / 4.4 / 4.5）
  - 4.1 `target_import` drainer：新增 `drainer.go`，`Drainer` 持有 worker pool（默认 2，可配置）+ `RunOnce` 同步入口（测试用）+ 异步 `Start/Shutdown`；DB-backed claim 通过 `Store.ClaimNextAsyncJob(jobTypes, now)`，原子地 `update async_jobs ... where ... for update skip locked` + 状态 `queued→running` + `attempts++` + `locked_at`，跨进程重启自然恢复（queued 行直接被下一次 claim 拿到）；`Store.FinalizeAsyncJob(jobID, JobOutcome, now)` 区分 succeeded / retryable（推迟 `available_at` 15s）/ non-retryable（status=failed）/ 达到 max_attempts 转 dead。
  - 4.2 F3 Resolve + A3 Complete：`PromptRegistryClient` 接口与 `PromptResolution{PromptVersion, RubricVersion, ModelProfileName, DataSourceVersion, FeatureFlag}` 在 targetjob 包内定义（F3 实现待 F3 plan 落地），`ParseExecutor` 在 Handle 中先 `Resolve("target.import.parse", language)`，再用 `aiclient.AIClient.Complete(profileName, CallMetadata{FeatureKey, PromptVersion, RubricVersion, Language})` 调用 A3；F3 disabled / unsupported / config-invalid 任一错误统一映射为 `AI_PROVIDER_CONFIG_INVALID` non-retryable（D-10 + 1.2）；`translateAIClientError` 用 substring match 把 A3 错误码映射为 retryable / non-retryable 矩阵；prompt body / response body 不进入 error_message / outbox payload（`redactErrorMessage` + 240 字符截断 + 关键字黑名单 `raw_jd_text` / `Authorization:` / `Bearer `）。
  - 4.3 写入解析结果 + `target.parsed`：`Store.ApplyParseResult` 已在 1.1 实现（事务内 dedupe upsert requirements + 更新 summary / fit_summary / analysis_status）；`ParseExecutor` 用 F3 三元组组装 `provenance` 子对象；`Store.WriteTargetParsedOutbox` 写 `outbox_events` 行，payload 来自 `BuildTargetParsedPayload` (`{targetJobId, userId, analysisStatus, requirementCount, coreThemes}`)；executor 紧接着 `Store.EnqueueSourceRefresh` 写 internal-only `async_jobs(source_refresh, status=queued)` 占位行。
  - 4.4 失败路径：`ParseExecutor.fail` 统一调用 `Store.UpdateTargetJobAnalysisFailure` 把 `target_jobs.analysis_status='failed'`，再用 `BuildTargetAnalysisFailedPayload` 构造 outbox `target.analysis.failed` 行（payload 仅含 `targetJobId / errorCode / retryable`），失败时不删除 `target_job_sources` 记录；URL fetch 错误通过 `errors.Is(urlfetch.ErrInvalidSource)` / `urlfetch.ErrSourceUnavailable` 路由到 B1 `TARGET_IMPORT_SOURCE_INVALID` (非 retryable) / `TARGET_IMPORT_SOURCE_UNAVAILABLE` (retryable)。
  - 4.5 `source_refresh` 占位：`SourceRefreshHandler` 注册到 drainer handler map，`Handle` 仅调用 `Store.UpdateSourceFreshness(targetJobID, FreshnessStale, now)`，payload 与日志均不携带 source URL；drainer dispatch 未知 job_type 时一律 finalize 为 non-retryable `TARGET_IMPORT_FAILED` 防止行被卡。
- 验证：`go test ./backend/internal/targetjob/...` PASS（新增 11 个 pipeline tests + 既有所有 store / config / handler / payload / service / observability tests），`go test ./backend/internal/targetjob/urlfetch/...` PASS。

### 关联 Commit

- `feat(backend-targetjob): land async drainer and parse executor`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-targetjob/001-targetjob-import-and-parse-bootstrap, phase: Phase 4: Async parse pipeline

## 18:30 工作记录

### 完成事项

- Phase 5: Privacy / observability / idempotency redlines（5.1 / 5.2 / 5.3）
  - 5.1 隐私 grep / payload negative tests：新建 `privacy_redline_test.go`，5 个测试覆盖：包源码反向 grep（`raw_jd_text` / `Authorization: Bearer` / `promptBody` / `responseBody` / `providerSecret` / 完整 sourceUrl=http literal 0 命中，gate 定义文件 `payload.go` 与 `parse_executor.go` 是声明地点 显式 allowlist）；ParseExecutor 失败路径的 error_message 经 `redactErrorMessage` 拒绝 `raw_jd_text` token；happy path 的 outbox payload 即使源 `RawJDText` 含 `Authorization: Bearer` 也不泄漏；source_refresh placeholder payload 不带 source URL；跨 Service / 共享 pepper 的 dedupeKey 仍按 user 隔离。
  - 5.2 F1 metric registry preflight：Phase 0.3 已落地的 `observability_test.go` 4 个测试覆盖 metric 名注册、label allowlist、forbidden label 拒绝、allowed set 完整性；本节核对验证全绿，无新增。
  - 5.3 Idempotency 跨 user 隔离：既有 `TestService_ImportTargetJob_DedupeKeyIsUserScoped` 与 store-side `TestSQLStore_ImportTargetJob_DedupeReturnsExistingActiveRunnerJob` + 新增 `TestImportTargetJob_DedupeKeyIsUserScopedAcrossServices`（共享 pepper 跨 service instance 仍 user-scoped）共三层覆盖。
- 验证：`go test ./backend/internal/targetjob/...` 与 `go test ./backend/internal/targetjob/urlfetch/...` 均 PASS。

### 关联 Commit

- `test(backend-targetjob): seal privacy and idempotency redlines`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-targetjob/001-targetjob-import-and-parse-bootstrap, phase: Phase 5: Privacy / observability / idempotency redlines

## 19:30 工作记录

### 完成事项

- Phase 6 部分（6.5 / 6.6）
  - 6.5 Handoff 给 frontend-home-job-picks-and-parse：新建 `backend/internal/targetjob/doc.go` 作为 package 级 godoc，覆盖 4 个 operation 的 sync/async 语义、idempotency dedupe（sha256 + pepper + user 隔离）、source 变体行为、隐私红线、URL fetch 守护、错误码 retryable 矩阵、F1 metric 名与 label allowlist、frontend mock → real 切换边界、BDD 入口（标注 6.1-6.4 暂依赖 F3 真实 Resolve 与 cmd/api 注册才可执行）。
  - 6.6 Active-scope 负向搜索：新建 `active_scope_negative_test.go` 反向 grep `mistake.` / `growth.` / `"voice"` / `practice.voice.tts` / `"jd.parse"` / `"target.parse"` / `embedding` / `rerank` / `interview_round` 在 production .go 文件 0 命中（targetjob + urlfetch 两层），过滤掉 `target.parsed`（合法事件名）冲突。
- Phase 6 阻塞项（6.1 / 6.2 / 6.3 / 6.4）：4 个 BDD-Gate 场景（E2E.P0.010-013）需要 cmd/api 把 targetjob.Handler 注册到 generated `HandlerFromMux` + 真实 F3 RegistryClient + scenario 目录与 setup/trigger/verify/cleanup 脚本。当前 cmd/api wiring 缺失，F3 无 Go 实现，因此场景脚本无法落地为可运行 gate；本节按 plan §6.1 / §6.4 verification 标准维持 unchecked，转交后续 plan（建议在 F3 prompt-rubric-registry 实现 plan 完成后派生 BDD wiring plan）。
- 验证：`go test ./backend/internal/targetjob/...` 全绿，`go test ./backend/internal/targetjob/urlfetch/...` 全绿；package-level negative search 与 source 反向 grep 均通过。

### 关联 Commit

- `docs(backend-targetjob): land package handoff doc and active scope gate`

### 备注

Auto-committed by /tdd phase-commit (partial), plan: backend-targetjob/001-targetjob-import-and-parse-bootstrap, phase: Phase 6 partial (6.5 handoff + 6.6 negative search). BDD-Gate 6.1-6.4 deferred until cmd/api wiring + F3 real client land in follow-up plans.

## 14:56 工作记录

### 完成事项

- 调整根级 Agent Git 分支策略：创建 feature branch 前必须先以 fast-forward-only 语义更新父分支到最新远端状态；phase commit 只在当前 feature branch 上提交并记录日志，不再自动 merge / ff-merge 回父分支；多 Agent 并行整合改由明确的 merge / rebase / PR review 阶段处理。
- 同步 `/implement` Step 4.5：解析 base branch 后，创建新 session feature branch 前必须 checkout 父分支、fetch upstream 并 fast-forward；无 upstream、不可 fast-forward 或更新失败时停止并报告；当前分支已经是 session feature branch 的 retry/resume 路径不重复更新父分支。
- 同步 `/tdd` Step 9.5：从 `Phase Commit + Merge Success Gate` 改为 `Phase Commit Gate`，删除 phase 边界自动 checkout base branch + `--ff-only` merge 的硬要求；同步 `metadata.baseBranch` 契约和 skill contract tests。
- 完成验证：`python3 -m pytest .agent-skills/implement/scripts/test_implement_skill_contract.py .agent-skills/implement/scripts/test_execution_automation_docs_contract.py .agent-skills/tdd/scripts/test_tdd_skill_contract.py` 37 passed；`make docs-check` zero drift；`git diff --check` clean；负向搜索确认旧 phase 自动 merge 指令 0 命中。

### 关联 Commit

- `chore(governance): revise branch lifecycle rules`

### 备注

本次由用户显式调用 `/work-journal` 手动收口。变更跨 `AGENTS.md` 与 `.agent-skills/`，但同属分支生命周期治理规则调整，因此作为单一提交处理；历史 reports / work-journal 中关于过去已发生 merge 的记录保留为历史事实，未回写。

## 15:14 工作记录

### 完成事项

- 将本地 `main` 通过 fast-forward 合并到当前 `dev` 的最新提交 `f4fe28c`，确认 `main...dev` 差异为 `0 0`；迁移后当前工作分支切换为 `main`，`main` 暂领先 `origin/main` 233 个既有提交。
- 将默认父分支规则从 `dev` 切换为 `main`：更新 `AGENTS.md`（`CLAUDE.md` 作为 symlink 同步生效）、`/implement` 文档契约测试 fixture、`backend-runtime-topology/001-worker-consolidation/context.yaml` 的 `baseBranch`。
- 将 OpenAPI breaking-change history gate 的默认比较基准从 `dev` 切换为 `main`：更新 `openapi/diff-config.yaml`、`scripts/lint/openapi_diff.py` 默认 fallback 与 `openapi/baseline/README.md` 说明。
- 完成验证：`python3 -m pytest .agent-skills/implement/scripts/test_execution_automation_docs_contract.py .agent-skills/implement/scripts/test_shared_context_tools.py` 25 passed；`python3 -m pytest scripts/lint/openapi_diff_test.py` 25 passed；`python3 .agent-skills/implement/shared/scripts/validate_context.py --context docs/spec/backend-runtime-topology/plans/001-worker-consolidation/context.yaml` 返回 `baseBranch=main`；`make docs-check` zero drift；`git diff --check` clean；负向搜索确认当前执行范围内不再保留默认父分支 / active context / OpenAPI diff 基准的 `dev` 口径。

### 关联 Commit

- `chore(governance): migrate default base branch to main`

### 备注

 本次为 trunk migration 治理提交。历史 reports / work-journal / bug records 中关于过去 `dev` 作为主干或历史 merge 事实的记录保留不改；`dev` 分支未删除，后续新分支统一从 `main` 派生。

## 15:55 工作记录

### 完成事项

- Phase 1: Home 静态壳 + 路由壳 + i18n — 新增 `HomeScreen.tsx`（Hero / JD textarea card / Resume CTA / aux cards），按 `ui-design/src/screen-home.jsx` 源级复刻；recent mocks 留 placeholder
- 在 `App.tsx` 绑定 `home` route → `<HomeScreen />` 替换 D1 PlaceholderScreen，遵循 profile/settings 纯 UI shell 模式
- 在 `zh.ts` / `en.ts` 新增 15 个 `home.*` i18n key（≥14），`LocaleMessages` 类型约束确保 zh/en 同步
- 新增 `HomeScreen.test.tsx`（10 测试）：7 testid DOM 锚点、控件类型、textarea 空时 Submit disabled、aux card 点击 nav("jd_match") / nav("debrief")、Resume CTA 点击 nav("resume_versions", {flow:"create"})、i18n en 文案、负向断言旧 prototype testid
- 扩展 `App.test.tsx`：断言 home route 命中 HomeScreen 而非 PlaceholderScreen
- 扩展 `localeFiles.test.ts`：验证 home.* namespace 15 key 在 zh/en 同步

### 关联 Commit

- `feat(home-screen): land home screen static shell with i18n and route binding`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-home-job-picks-and-parse/001-home-jd-import-and-parse

## 16:15 工作记录

### 完成事项

- Phase 2: Recent mock interviews — 新增 `useRecentTargetJobs()` hook 通过 generated client 调 `listTargetJobs`（pageSize=12），React state 三态（loading/data/error），无 runtime 时安全降级
- 新增 `MockInterviewCard.tsx` + `MiniRoundRail.tsx` 组件，按 `screen-home.jsx` 源级复刻；statusTone 从 `TargetJob.status` 派生、D2 token 渲染；P0 默认 4 轮次 R1-R4；MiniRoundRail 圆点 currentIndex fallback
- 新增 `interviewContextFromTargetJob()` 统一 workspace 跳转契约，锁定 7 字段（targetJobId/jobId/jdId/planId/resumeVersionId/roundId/roundName）
- `HomeScreen` 集成：`updatedAt desc` 排序 + 12 条上限、MockInterviewCard 渲染 + 点击跳 workspace
- 扩展 `listTargetJobs.json` fixture 3 个新 variant（empty / one-job / twelve-plus）
- 测试：MockInterviewCard 6 tests + HomeRecentMocks 6 tests（fixture variant 三态、sort+limit、interviewContext nav）+ 29 tests total PASS

### 关联 Commit

- `feat(home-screen): add recent mock interviews with listTargetJobs integration`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-home-job-picks-and-parse/001-home-jd-import-and-parse

## 16:56 工作记录

### 完成事项

- Phase 3: JD 导入逻辑（粘贴 / 上传 / URL 三种 source variant）
  - 新增 `JDAssistModal` 组件，按 `ui-design/src/screen-home.jsx` 源级复刻 upload/url 两种模态，含 overlay/ESC/Cancel/X 四种关闭路径
  - 在 `HomeScreen` 中接入三种 source variant 提交逻辑：粘贴 → `importTargetJob(type=manual_text)`、上传 → `createUploadPresign(purpose=target_job_attachment)` → `importTargetJob(type=file)`、URL → `importTargetJob(type=url)`
  - 全部 side-effect 调用携带 `Idempotency-Key` header
  - 成功 → `nav("parse", { targetJobId, source })`，4xx → inline 错误
  - 接入 `requestAuth` pending action：未登录时三个 source 路径均触发 `requestAuth({ type: "import_jd", route: "parse", params: { source } })`
  - 隐私反查：JD raw text / rawDescription / url 不进入 console.log / URL / localStorage / telemetry
  - 扩展 i18n：home.* modal 区域新增 11 个 key（zh/en）
- 新增 3 个测试文件：JDAssistModal.test.tsx (13)、HomeImport.test.tsx (10)、HomeAuthGate.test.tsx (3)

### 关联 Commit

- `feat(frontend-home): implement jd import with three source variants`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-home-job-picks-and-parse/001-home-jd-import-and-parse, phase: Phase 3: JD 导入（textarea + upload + URL → importTargetJob）

## 21:28 工作记录

### 完成事项

- Phase 4: Parse 屏（loading/preview/confirm）+ polling 状态机
  - 新增 `ParseScreen.tsx`，按 `ui-design/src/screens-p0-complete.jsx` 源级复刻 loading（4 步进度条）/ preview / error / failed 四态
  - 状态机驱动：进入 parse 屏通过 generated client 调 `getTargetJob(targetJobId)`，`analysisStatus=queued|processing` → 600ms polling，`ready` → preview，`failed` → 错误态
  - Preview 渲染：Basic fields（title/company/location/notes 可编辑，level/language read-only）+ Requirements 双列 Must Have/Nice to Have + hit toggle 三态 + Hidden signals + confidence tag + Round assumptions 4 卡 grid
  - Confirm 调 `updateTargetJob(targetJobId, body, { idempotencyKey })`，body 仅 supplied fields（titleHint/companyNameHint/locationText/notes），level/language/hit toggle 不进 request body
  - Re-parse 重置 stage=loading 并重新 polling，abort 当前 effect 防 race；4xx inline 错误保留编辑态
  - 接入 `requestAuth` pending action：Confirm 未登录触发 `requestAuth({ type: "confirm_interview", route: "workspace" })`
  - 新增 5 个测试文件：ParseScreen.test.tsx (5)、ParseFlow.test.tsx (6)、ParseEdit.test.tsx (10)、ParseFailedState.test.tsx (2)、ParseAuthGate.test.tsx (1)
- Phase 5: jd_match P1 Placeholder Shell
  - 新增 `JDMatchScreen.tsx`，按 `ui-design/src/screen-jd-match.jsx` 复刻 hero + profile snapshot chip 静态版本 + 三 tab 标签（recommended/search/watchlist）+ P1 placeholder 文案
  - 在 `App.tsx` 绑定 `jd_match` route → `<JDMatchScreen />`
  - 负向断言旧 prototype testid（jdmatch-card-*/saved-search-*/watchlist-*/market-signal-*）0 命中
- i18n 扩展：`parse.*`（50 keys）+ `jdMatch.*`（16 keys）zh/en 同步
- 更新 `authContractGate.test.ts`：新增 TargetJobs 5 个 operation（listTargetJobs/importTargetJob/createUploadPresign/getTargetJob/updateTargetJob）到允许集合
- 全量验证：50/51 test files PASS（1 pre-existing conventions-parity），parse + jd_match 共 30 tests PASS，i18n 7 tests PASS

### 关联 Commit

- `feat(frontend-parse): implement parse screen and jd_match placeholder shell`

### 备注

Auto-committed by /work-journal, plan: frontend-home-job-picks-and-parse/001-home-jd-import-and-parse, phases: 4 + 5

## 22:27 工作记录

### 完成事项

- Phase 6: 验证收口（pixel parity + scenario + regression + docs）
  - 6.1-6.3: 新增 `tests/pixel-parity/home.spec.ts`、`parse.spec.ts`、`jd_match.spec.ts`，共 10 测试 × 2 viewports
  - 6.4: `pnpm test:pixel-parity` 68/68 PASS，截图基线已更新
  - 6.5: 创建 4 个 scenario 目录（p0-014/015/016/017），含 README.md + scripts/{setup,trigger,verify,cleanup}.sh
  - 6.6: `test/scenarios/e2e/INDEX.md` 追加 P0.014-017 行
  - 6.7: Regression 重跑 D1-D3 P0.001-006 通过（Vitest 51/52 PASS，1 pre-existing）
  - 6.8: 全量验证：pnpm test 51/52 PASS, typecheck 0 错, pnpm build PASS
  - 6.9: `make docs-check` zero drift, check_md_links 双 OK
  - 6.10: 负向搜索 7 类 pattern grep 全 0 命中
  - 6.11: 4 scenario scripts setup→trigger→verify→cleanup 全部 PASS
- 修复 HomeScreen.tsx 与 MockInterviewCard.tsx 预存 type error
- BDD checklist 24/24 全部完成，主 checklist 43/43 全部完成
- Plan `001-home-jd-import-and-parse` 全部 Phase 1-6 交付闭环

### 关联 Commit

- `chore(frontend-home): finalize plan with pixel parity, scenarios, and gate closure`

### 备注

Auto-committed by /work-journal, plan: frontend-home-job-picks-and-parse/001-home-jd-import-and-parse, phase: 6 (final)
