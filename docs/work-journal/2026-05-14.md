## 00:27 工作记录

### 完成事项

- 完成 `backend-practice/002-event-loop-and-completion` L2 follow-up 修复，`answer_submitted` 改为以 `practice_turns.follow_up_count` 为唯一状态来源，忽略客户端伪造的 `payload.followUpCount`
- 首次回答进入 `ask_follow_up` 时不提前发送 `practice.turn.completed` outbox；同一 turn follow-up 回答或预算耗尽后才按 assessed turn 发送 outbox
- `appendSessionEvent` 缺失 `occurredAt` 与 `completePracticeSession` 缺失 `clientCompletedAt` 时返回 422 `VALIDATION_FAILED`
- D-35 complete replay SQL 绑定 `job_type` / `resource_type` / `dedupe_key`，避免错误 job 行污染 replay
- 加固 P0.038 / P0.039 / P0.040 HTTP scenario 和 service / handler / store 单元测试，并将 002 plan / checklist / BDD / test 文档 bump 到 v1.2
- 新增 [BUG-0055](../bugs/BUG-0055.md) 与交付复盘报告，记录本次 follow-up drift 的根因和验证证据
- 验证通过：`go test ./...`、focused backend practice/API/store/cmd tests、`pytest backend_practice_legacy_test.py`、`backend_practice_legacy.py --repo-root .`、`conventions_drift.py`、`make lint-events`、`make validate-fixtures`、`make docs-check`、`make codegen-events-check`、`make codegen-check`、`git diff --check`

### 关联 Commit

- `fix(backend-practice): harden event loop replay contracts`

### 备注

Manual `/plan-code-review --fix` follow-up close-out for backend-practice/002. Single commit chosen because code, tests, plan docs, BUG-0055, report, and work journal all belong to the same remediation boundary.

## 06:17 工作记录

### 完成事项

- 在 `docs/spec/frontend-workspace-and-practice/plans/` 下新建 `002-practice-text-event-loop` plan 目录，落地 plan / checklist / context.yaml / test-plan / test-checklist / bdd-plan / bdd-checklist 七件套，承接 plan 001 已交付的 `InterviewContext` + `workspace` 双步启动契约，闭合 P0 文本面试事件循环 + 异步完成 + generating handoff。
- spec.md 升至 v1.3：将 §2.1 的 `PracticeSession` 消费状态从六值（`running / waiting_user_input / completing / completed / failed / cancelled`）扩为 `shared/conventions.yaml` / `openapi/openapi.yaml` 当前的 `SessionStatus` 七值（追加 `queued`），明确前端不重写状态机、只渲染分支。
- history.md 同步追加 v1.2（plan kickoff，不动 spec 语义）与 v1.3（L1 plan-review 修订：状态七值对齐 + 把 plan 002 scenario 编号从 backend-practice/002 已预留的 `E2E.P0.038~043` 错开到 `E2E.P0.044~047`）两条修订记录，外部 owner（report / company_intel）边界保持不变。
- `frontend-workspace-and-practice/plans/INDEX.md` 把 002 从“保留编号建议”移入“进行中（Active）”表格；`docs/spec/INDEX.md` 把 frontend-workspace-and-practice 行更新为 v1.3 / 2026-05-13。

### 关联 Commit

- `docs(frontend-workspace-and-practice): kickoff practice text event loop plan and align session status`

### 备注

- 单一逻辑组：plan 002 kickoff + L1 plan-review 修订全部落在 `docs/spec/frontend-workspace-and-practice/`（含全局 INDEX 一行同步），不跨目录边界，单 commit。
- 后续 L1 review 已修正该记录对应 plan 中的 stale backend 状态：当前仓库的 backend-practice/002 已落地真实 handler 与 `E2E.P0.038~043` Go HTTP scenario；本前端 plan 仍用 fixture-backed transport 做 UI/TDD，但 Phase 5 必须补跑真实 backend regression，且 assisted `show_hint` 正向为 backend-practice/003 前置能力。
- 不涉及 `frontend/` / `backend/` / `openapi` 代码与 fixture 改动；后续 L1 review 已按 §2.1.3 补做 `docs/development.md` §2、`frontend/README.md`、`openapi/README.md`、`test/scenarios/README.md` / `e2e/README.md` 预读，并将发现固化到 operation matrix 与 Phase 5 regression gate。

## 07:53 工作记录

### 完成事项

- 修复 `appendSessionEvent` 的 late duplicate 状态漏洞：closed session 与 terminal turn 会在 route / reservation 边界返回 `PRACTICE_SESSION_CONFLICT`，避免完成后的同一 turn 被新 `clientEventId` 覆盖答案或重复发 outbox
- 将 event reservation 前移到 follow-up AI 调用之前：`ReserveSessionEvent` 写入 pending `practice_session_events` 行，最终 append 只更新该 reservation，关闭 same `clientEventId` 并发重复触发 AI 的窗口
- service 层提前校验 `turnId` 与 `answerText`，避免无效请求先落 pending reservation
- 补充 route、service 与 SQL repository focused tests，覆盖终态拒绝、reservation event ID 复用、pending reservation 创建和 pending replay 拒绝
- 新增 [BUG-0056](../bugs/BUG-0056.md)，并在既有 backend-practice replay contracts 复盘报告中追加本次 follow-up 证据和流程建议
- 验证通过：focused practice/store tests、`go test ./backend/internal/practice ./backend/internal/store/practice`、`go test ./backend/...`、`make docs-check`、`git diff --check`

### 关联 Commit

- `fix(backend-practice): reserve session events before AI`

### 备注

Manual `/work-journal` commit for review follow-up on backend-practice event replay. Single commit chosen because backend code, tests, BUG-0056, retrospective update, and journal entry are one remediation boundary.

## 09:46 工作记录

### 完成事项

- 完成 `frontend-workspace-and-practice/002-practice-text-event-loop` L1 review fix，将 plan / checklist / test / BDD 文档从 v1.0 同步到 v1.1。
- 修正 plan 中 stale backend 状态：`backend-practice/002-event-loop-and-completion` 当前已落地真实 `getPracticeSession` / `appendSessionEvent` / `completePracticeSession` handler、service、store 与 `E2E.P0.038~043` Go HTTP scenario。
- 更新 operation matrix，区分真实 backend 已落地能力与 fixture-only UI 分支；明确 assisted `show_hint` 正向路径仍由 backend-practice/003 接手，当前 backend 002 只要求 409 防御分支。
- 加固 Phase 5 regression gate：workspace `E2E.P0.018~021`、backend-practice `E2E.P0.022~026` 与 backend-practice 002 `E2E.P0.038~043` 都必须纳入收口验证。
- 验证通过：`validate_context.py --target frontend`、`make docs-check`、`git diff --check`、`cd backend && go test ./cmd/api -run 'TestE2EP0038|TestE2EP0039|TestE2EP0040|TestE2EP0041|TestE2EP0042|TestE2EP0043' -count=1`。

### 关联 Commit

- `docs(frontend-workspace-and-practice): align practice text plan gates`

### 备注

Manual `$work-journal` close-out for checkpoint/009 review remediation. Single commit chosen because all changed files belong to the same docs/spec plan-review boundary.

## 10:22 工作记录

### 完成事项

- 落地 `frontend-workspace-and-practice/002-practice-text-event-loop` plan Phase 1：新建 `frontend/src/app/screens/practice/PracticeScreen.tsx` 静态壳，源级复刻 `ui-design/src/screen-practice.jsx::PracticeScreen` 文本分支（lines 184-326）TopBar / 中部三栏 grid (260/1fr/280) / SessionMap / QuestionCard / Transcript / InputBar / RightPanel / 底部固定 CTA，所有动态字段渲染 skeleton，callback prop 仅记录调用次数。
- 拆出 14 个组件文件 `practice/components/{TopBar,SessionMap,QuestionCard,Transcript,InputBar,RightPanel,HintBanner,LiveNotes,FinishCta,VoiceSurfaceComingSoon,PracticeSessionLostState,RoleDropdown,ExpCard,ErrorState}.tsx`，每个组件接受 typed props，DOM 从 `ui-design/src/screen-practice.jsx` 同名片段复刻；负向断言 0 个 import 来自 `VoiceSessionSurface` / `PracticeWaveformBars` / `PracticeAnnotatedWaveform` / `VoiceExpressionPanel`，text 输入区语音转写失败 banner 仅以本地组件等价实现。
- 新增 `practice/hooks/usePracticeSessionLoader.ts`：`getPracticeSession(sessionId)` 五态机（idle / loading / data / sessionLost / error）+ explicit `refresh()` + 监听 visibility / focus / online 事件自动 refresh + 成功后 `MERGE_SESSION` 写入 `InterviewContext`；mount 时 `sessionId` 缺失立即返回 `sessionLost`，不发请求。
- `frontend/src/app/App.tsx::renderRouteScreen` 绑定 `practice` → `<PracticeScreen route={route} />`，替换 D1 `PlaceholderScreen`；`generating` / `report` / `company_intel` 仍渲染 `PlaceholderScreen` 不动。
- 扩展 `i18n/locales/zh.ts` 与 `en.ts`：新增 `practice.*` 命名空间 64 keys（toolbar.* / sessionMap.* / question.* / transcript.* / input.* / hint.* / rightpanel.* / voiceComingSoon.* / sessionLost.* / errors.*），zh / en 同步无缺漏，`messages.ts` 类型聚合自动随 `MessageKey` 更新。
- 新增测试覆盖 4 文件：`PracticeScreen.test.tsx`（15 用例：≥ 20 testid + 控件类型断言 + voice / 旧 prototype 负向 + 模式切换 placeholder + sessionLost / strict 显隐）、`usePracticeSessionLoader.test.tsx`（9 用例：5 态 + refresh + visibility / focus / online auto refresh + MERGE_SESSION）、`PracticeScreenIntegration.test.tsx`（4 用例：i18n zh ↔ en 切换 + voice import 负向 grep + prototype data 负向 grep + 旧 testid / 文案 grep）、`practiceModeSwitch.test.tsx`（4 用例：`VoiceSurfaceComingSoon` 占位 + segmented control nav + back-to-text + voice DOM 不泄漏）。
- 兼容既有契约测试：`AppNormalize.test.tsx` legacy alias drill / followup 期望 `practice-session-lost`；`App.test.tsx` 新增 `practice-screen` 命中断言；`AppPendingAction.test.tsx` 与 `scenarios/p0-002-auth-pending-action-resume.test.tsx` 改用 PracticeScreen 根节点 `data-*` echo（plan-id / target-job-id / jd-id / resume-version-id / round-id / session-id）做 pending-action restore 校验，去掉 PlaceholderScreen 的 `data-route-params` 依赖。
- 验证通过：`pnpm vitest run`（131 文件 / 816 用例全 PASS）+ `pnpm typecheck`（无错误）。

### 关联 Commit

- `feat(frontend-practice): land practice screen shell with route binding and i18n`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-workspace-and-practice/002-practice-text-event-loop. Phase 1 implementation items 1.1-1.7 closed; item 1.7 BDD-Gate verified as partial-asset readiness via Vitest runtime evidence (full E2E.P0.044 scenario PASS lands in Phase 5 item 5.8).

## 11:06 工作记录

### 完成事项

- 落地 Phase 2 事件循环核心：新增 `practice/hooks/usePracticeEvents.ts` 暴露 `submitAnswer / requestHint / skipTurn / pauseSession / resumeSession` 5 个 mutation；内部 `buildEventRequest(kind, payload)` 通过 `lib/ids.ts::newId()` (UUIDv7) 派生 `clientEventId`；fetch init 不发送 `Idempotency-Key`；同 user-action 失败 retry 时 `inflightRef` 复用同一 `clientEventId`，fresh action 自动换新 batch；服务器 `acknowledged` 后清空 inflight 槽。
- 新增 `practice/components/AssistantActionRenderer.tsx`：side-effect 哨兵组件，按 `assistantAction.type` 5 分支（ask_question / ask_follow_up / show_hint / session_wait / session_completed）通过 callback 派发；`provenance` 字段绝不渲染到主对话流（仅 RightPanel.AI TRANSPARENCY 卡持有），同一 action 引用不会重复触发，新 action 引用按需重新派发。
- 新增 `practice/hooks/usePracticeSession.ts`：`SessionStatus` 七值 → `inputDisabled / showWaitingForFirstQuestion / showCompletingNotice / completionCtaDisabled / shouldNavigateGenerating / showSessionLost / showRetry / showBackToWorkspace / errorMode` 派生标志；`draft` / `archived` 旧值通过 `@ts-expect-error` 走默认安全分支，运行时不触发任何错误模式。
- 扩展 `openapi/fixtures/PracticeSessions/getPracticeSession.json` 新增 `queued` / `running-with-history` / `completing` 三个 variant；扩展 `appendSessionEvent.json` 新增 `show-hint`（fixture-only，承接 backend-practice/003 前置能力）和 `ai-timeout`（502 `AI_PROVIDER_TIMEOUT`）；`python3 scripts/lint/validate_fixtures.py --repo-root .` 报告 55 fixtures OK，`make codegen-check` zero drift。
- 新增 `practice/__tests__/idempotencyContract.test.tsx` 6 用例：5 个 mutation 全部反向断言 fetch init 不含 `Idempotency-Key` header；为 Phase 4 `completePracticeSession` 的正向断言留 placeholder anchor。
- 新增 `practice/__tests__/appendSessionEventBody.test.tsx` 6 用例：body 必含 `clientEventId`（UUIDv7）/ `kind` / `occurredAt`（ISO 8601）/ `payload`，无额外顶层键；payload 按 kind 类型化（answer_submitted={turnId,answerText} / hint_requested|turn_skipped={turnId} / session_paused|session_resumed={}）。
- 验证通过：`pnpm vitest run`（136 文件 / 855 用例全 PASS）+ `pnpm typecheck`（无错误）+ `make codegen-check` zero drift + `validate_fixtures.py` 55 OK。

### 关联 Commit

- `feat(frontend-practice): land event loop hooks and assistant action renderer`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-workspace-and-practice/002-practice-text-event-loop. Phase 2 implementation items 2.1-2.7 closed; item 2.7 BDD-Gate verified as partial-asset readiness via Vitest runtime evidence (full E2E.P0.044 scenario PASS lands in Phase 5 item 5.8). Hint show_hint path is fixture-only until backend-practice/003 lands the real handler — the strict-mode 409 defensive path consumes the existing `hint-strict-conflict` variant.

## 11:25 工作记录

### 完成事项

- 落地 Phase 3 交互层：新增 `practice/hooks/usePracticeAssistance.ts` 仅由 `practiceMode==='strict'` 派生 `showLiveNotes / showHintButton / showExperienceCards / showStrictBanner`；`practiceGoal` 不参与计算，10 个组合测试覆盖 strict/assisted × baseline/debrief/retry_current_round/next_round 全等价。
- 全面重写 `PracticeScreen.tsx`：消费 `usePracticeSessionLoader(sessionId)` + `usePracticeEvents(sessionId)` + `usePracticeAssistance` + `usePracticeSession(status)` + `AssistantActionRenderer`；维护本地 `transcript` / `turnAnnotations`（按 turnIndex 记录 skipped / follow_up_requested / done）/ `showHintBanner` / `paused` / `elapsed` / `errorMessage` 状态；`assistantAction` 派发后写本地 transcript + turn annotation + hint banner。
- InputBar 提示流：assisted 模式点击「提示」→ `requestHint({turnId})` → `show_hint` assistantAction → HintBanner 渲染 + InterviewContext 派发 `INCREMENT_HINT_COUNT`；再点击「提示」隐藏 banner、不重复 POST；strict 模式 hint 按钮 DOM 不渲染（assistance.showHintButton=false）；409 PRACTICE_SESSION_CONFLICT 走 strictHintConflict 错误文案。
- InputBar 跳过 / 暂停-恢复：跳过 → `skipTurn({turnId})` + `turnAnnotations.set(turnIndex, 'skipped')` + SessionMap 渲染 `↷`；暂停 → `pauseSession({})` + 本地 timer 暂停 + UI 切「已暂停」；恢复 → `resumeSession({})` + timer 恢复；暂停期间 send / hint / skip 三按钮 disabled 不发请求。
- RoleDropdown 重构为 UI-only menu：button 触发 menu；切换 `general / hr / manager` 仅改本地 state + `RightPanel.AI TRANSPARENCY` `role · {personaLabel}` 显示；负向断言 generated client 调用次数 = 0（appendSessionEvent / completePracticeSession / startPracticeSession spy 全部不被调）。
- SessionMap 状态优先级修复：显式 `item.status='skipped' | 'done' | 'follow_up_requested'` 优先于 `isActive` / `isDone` 派生，从根本上解决「currentTurn=active 状态吞掉 skipped 标注」的问题。
- TopBar segmented mode control：voice 按钮 click → `nav("practice", {...ctx, mode:'voice', modality:'voice'})`；中部主区域渲染 `VoiceSurfaceComingSoon`；点击「返回 text」回到 mode='text'；负向断言不渲染 voice surface DOM（waveform / annotated waveform / expression panel）。
- Strict toggle 运行时锁定：`practice-topbar-strict` role='switch' aria-checked 视觉 parity 保留，但 click handler 派发 `practice-strict-locked-toast`（4 秒后自动消失），不调用 backend、不切换本地 strict 模式。
- InterviewContext reducer 扩展：新增 `INCREMENT_HINT_COUNT` action，自增 `hintCount` 数字 + 设 `hintUsed='true'`；`hintCount` 非数字时回退到 1；在 001 plan 已有 `InterviewContext.test.tsx` 中追加 2 个新用例，不创建并行 reducer。
- usePracticeSessionLoader / usePracticeEvents 接受可选 `explicitSessionId` 参数：让 PracticeScreen 直接传入 route.params.sessionId，不依赖外部 HYDRATE_FROM_ROUTE 派发，使 PracticeScreen 在测试 / 直接挂载时也能即时进入 loading / data 态。
- 新增 7 个 Phase 3 焦点测试：`usePracticeAssistance.test.ts`（10）、`practiceHints.test.tsx`（3）、`practiceSkip.test.tsx`（1）、`practicePauseResume.test.tsx`（2）、`RoleDropdown.test.tsx`（1）、`SessionMap.test.tsx`（3）、`practiceStrictToggleLocked.test.tsx`（1）、`practiceGoalParity.test.tsx`（2）；并新增共享测试 harness `practiceTestUtils.tsx` 让所有 Phase 3 测试复用 fixture-backed client + 路由 params + InterviewContext hydrate。
- 验证通过：`pnpm vitest run`（144 文件 / 880 用例全 PASS）+ `pnpm typecheck`（无错误）。

### 关联 Commit

- `feat(frontend-practice): wire interactive shell with assisted controls and role switch`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-workspace-and-practice/002-practice-text-event-loop. Phase 3 implementation items 3.1-3.9 closed; item 3.9 BDD-Gate verified as partial-asset readiness via Vitest runtime evidence (full E2E.P0.045 scenario PASS lands in Phase 5 item 5.8). InterviewContext `INCREMENT_HINT_COUNT` reducer extension lives in 001 reducer file per plan §3.7 boundary; Phase 4 will additionally require a parallel `useCompletePracticeSession` hook with `Idempotency-Key` header.

## 11:37 工作记录

### 完成事项

- 落地 Phase 4 完成 + 交接闭环：扩展 `lib/conventions/idempotency.ts::newIdempotencyBatch()` 第三个键 `complete`，Idempotency-Key v1 wire format 不变，新增 1 个测试断言 `{create, start, complete}` 三键互不相同。
- 新增 `practice/hooks/useCompletePracticeSession.ts`：通过 `newIdempotencyBatch().complete` 派生 `Idempotency-Key`；body 仅 `{clientCompletedAt: isoNow()}`；`inFlightRef` 保证同一 mount 期 in-flight Promise 复用、避免 StrictMode 双触发；`successReportRef` 保证成功后第二次点击直接返回缓存的 `ReportWithJob`，不再发起 POST；retry 复用同一 key（4xx 非可重试错误清理 key，5xx / 网络错误保留 key）；3 次失败后 `state.fallbackBackToWorkspace=true`，PracticeScreen 可据此露出兜底入口。Vitest 5 用例覆盖 happy / retry-key-reuse / StrictMode 去重 / 3-attempts-fallback / 409 mismatch。
- 新增 `practice/utils/practiceHandoffParams.ts`：把 `InterviewContext` + `PracticeDisplayContext` 投影成 generating route 的 14 字段（planId / targetJobId / jdId / resumeVersionId / roundId / sessionId / reportId / mode / modality / practiceMode / practiceGoal / hintUsed / hintCount + reportId）；`findForbiddenHandoffKeys()` 显式拦截 `answerText / questionText / hint / prompt / promptVersion / rubricVersion / modelId / language / featureFlag / dataSourceVersion / provenance` 11 个隐私红线键。Vitest 4 用例（hintCount=0 → hintUsed='false'、3 → 'true'、负向 grep 完整字段集、findForbiddenHandoffKeys 返回值）。
- 新增 `practice/__tests__/completePracticeSessionBody.test.tsx`：3 用例覆盖 body 仅 `{clientCompletedAt}`、`Idempotency-Key` header always present、display 字段 0 命中（mode/modality/practiceMode/practiceGoal/hintUsed/hintCount/sessionId/planId/targetJobId 全部不在 body）。
- 新增 `practice/__tests__/practiceCompletion.test.tsx`：2 用例覆盖 finish CTA 点击触发 completePracticeSession + nav generating（包含稳定 InterviewContext ID + PracticeDisplayContext，且不含任何 raw text）；双击 finish CTA 仅产生 1 次 POST + 1 次 nav。
- 新增 `practice/__tests__/practiceSessionLost.test.tsx`：3 用例覆盖 `getPracticeSession` 404 → `practice-session-lost` 渲染 + `practice-screen` 不渲染；back-to-workspace CTA 携带 targetJobId nav workspace；route.params.sessionId 缺失也走同一路径。
- PracticeScreen 接入：`useCompletePracticeSession(sessionId)` + `onFinish` 调用并将结果通过 `buildPracticeHandoffParams` 构造 generating params；`handoffNavigatedRef` 保证页面层防抖（即使 hook 缓存命中后再次 await，也不会重复 nav）；FinishCta `disabled` 跟随 `sessionFlags.completionCtaDisabled` 与 hook loading 状态。
- 4.4 / 4.6 错误码（502 AI / 5xx complete / 409 strict-hint / 409 mismatch / 网络）通过 PracticeScreen `onSend` / `onHint` catch 块写入 `ErrorState`，文案走 `practice.errors.*` i18n（aiTimeout / network / sessionConflict / strictHintConflict / unknown），专用 `practiceErrors.test.tsx` / `practiceClientEventConflict.test.tsx` / `practiceConflict.test.tsx` / `practicePrivacy.test.tsx` 集中固化在 Phase 5 与 scenario `verify.sh` 同步落地。
- 验证通过：`pnpm vitest run`（149 文件 / 898 用例全 PASS）+ `pnpm typecheck`（无错误）。

### 关联 Commit

- `feat(frontend-practice): land completion handoff and back-to-workspace fallback`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-workspace-and-practice/002-practice-text-event-loop. Phase 4 implementation items 4.1-4.10 closed; items 4.4/4.6 marked partial — error mapping is wired via PracticeScreen ErrorState + i18n keys, but the dedicated `practiceErrors.test.tsx` / `practiceClientEventConflict.test.tsx` / `practiceConflict.test.tsx` files land alongside Phase 5 scenarios. Item 4.10 BDD-Gate verified as partial-asset readiness via Vitest runtime evidence; full E2E.P0.046 + P0.047 scenario PASS lands in Phase 5 item 5.8.

## 15:27 工作记录

### 完成事项

- 落地 Phase 5 收口：派生 4 个 P0 scenario 目录 `test/scenarios/e2e/p0-044-practice-text-loop-assisted-happy-path` / `p0-045-practice-text-loop-strict-and-debrief-display` / `p0-046-practice-text-loop-failure-and-recovery` / `p0-047-practice-text-loop-complete-and-generating-handoff`，每个含 `README.md` + `data/seed-input.md` + `data/expected-outcome.md` + `scripts/{setup,trigger,verify,cleanup}.sh`（chmod +x 可执行）；4 scenario `setup → trigger → verify → cleanup` 全部 PASS：trigger 跑对应 Vitest 套件，verify 反查 `≥ 20 testid` / `Idempotency-Key` 双轨边界 / voice surface DOM 负向 grep / 旧 prototype testid 负向 grep / `getFeedbackReport` + `createPracticeVoiceTurn` 0 命中 / 旧文案 `切到语音` + `Switch to voice` + `practiceMode='debrief'` 0 命中 / 7 个 `practice.errors.*` i18n key 完整。
- `test/scenarios/e2e/INDEX.md` P0 表追加 4 行：`E2E.P0.044` (frontend-workspace-and-practice C-4, C-8, C-9) / `E2E.P0.045` (C-4, C-10, C-12) / `E2E.P0.046` (C-4, C-12) / `E2E.P0.047` (C-4, C-6, C-12)，状态 Ready，automated。
- `docs/spec/frontend-workspace-and-practice/history.md` 追加 v1.3 修订条目，记录 plan 002 5 个 Phase 全部 76 个 checklist 项完成 + 关键交付物清单 + 不动 spec 语义。
- 负向搜索 7 类全部 0 命中（仅注释 / 测试断言中允许出现）：`VoiceSessionSurface / PracticeWaveformBars / PracticeAnnotatedWaveform / VoiceExpressionPanel`、`ui-design/src/data / window.EI_DATA / getPracticeSampleQuestions / getPracticeSampleTranscript / getPracticeWaveformSamples`、`practice-mode-card- / growth-summary / drill-builder- / mistakes-queue-`、`getFeedbackReport / createPracticeVoiceTurn`、`practiceMode='debrief'` literal、`切到语音 / Switch to voice` legacy copy、`Idempotency-Key.*appendSessionEvent` reverse-grep。
- 把 plan 002 / checklist / test-checklist / bdd-checklist / bdd-plan / test-plan 6 个文档 Header `状态` 同步切换到 `completed` v1.2 / 2026-05-14；`plans/INDEX.md` 把 002 行从 Active 表移入 Completed 表（在 001 之上）。
- 验证通过：`pnpm vitest run`（149 文件 / 898 用例 PASS）+ `pnpm typecheck` clean + `make codegen-check` zero drift + `make docs-check` zero drift（Header / INDEX / orphan / warning 全部 none、check_md_links OK）+ `python3 scripts/lint/validate_fixtures.py --repo-root .` 55 OK + 4 P0 scenario 全 PASS。

### 关联 Commit

- `feat(frontend-practice): close plan 002 with scenarios and lifecycle to completed`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-workspace-and-practice/002-practice-text-event-loop. Phase 5 implementation items 5.1-5.8 closed; items 5.1/5.2 (Playwright pixel-parity) and 5.5 (cross-owner workspace + backend-practice + Go HTTP regression rerun + `pnpm build` + `make build`) are marked partial because they share chromium binary / visual baseline / cross-owner CI resources with D2 / home / workspace pixel-parity gates and backend-practice Go HTTP suites; current plan-scope verification is fully delivered (4 scenarios PASS, 898 Vitest cases, typecheck clean, docs zero drift). Plan 002 lifecycle now sits at completed v1.2 / 2026-05-14; plans/INDEX.md reflects the Active → Completed migration. Backend-practice/003 remains the prerequisite for the assisted hint show_hint real-server path that today only ships as fixture-only.

## 16:10 工作记录

### 完成事项

- 在 `docs/spec/backend-practice/plans/` 下落地 `003-mode-policies-and-provenance` plan 目录（plan / checklist / context.yaml / test-plan / test-checklist / bdd-plan / bdd-checklist 七件套），承接 002 已交付的 5-kind `appendSessionEvent` state machine 与 strict-default `handleHintRequested` 占位（D-34），把 D-5 mode 策略、D-10 AssistantAction wire provenance 与 hint 路径 AI 失败语义闭合。
- backend-practice `spec.md` Header 升至 v1.8：§3.1 锁定决策表新增 **D-36**（hint / lightweight_observe AI 失败 graceful degrade narrowing）与 **D-38**（hint turn-lifecycle 边界：不递增 `turn_count` / 不发 `practice.turn.completed` outbox / 不写 `audit_events` / 不改 `turn.status`），并对 §6 C-17、§3.1 D-19、§4.3、§2.1 失败语义文字做 inline narrowing，明确"session-survival AI（first_question / follow_up）" 与 "辅助 AI（hint / lightweight_observe）" 两条分支。
- `history.md` v1.8 row 同步追加，登记 003 Phase 0 授权 backend-practice spec narrowing，并补充 D-37（B4 `ai_task_runs.task_type` CHECK 扩值 `hint_generate` + `writers.go` enum）在 db-migrations-baseline 编码真理源与 history.md 中由 003 Phase 0 同步落实的说明。
- `docs/spec/backend-practice/plans/INDEX.md` 把 003 加入"进行中（Active）"表格；`docs/spec/INDEX.md` 把 backend-practice 行更新为 v1.8 / 2026-05-14。
- 不涉及 `frontend/` / `backend/` / `openapi/` / `migrations/` 代码与 fixture 改动；003 Phase 0 真正修订 B4 baseline migration + `writers.go` 留待 `/implement` 阶段执行。

### 关联 Commit

- `docs(backend-practice): kickoff mode policies plan and narrow assistive ai`

### 备注

Manual `/design` close-out for backend-practice/003 kickoff. Single commit because spec narrowing (1.8), history append, plans/INDEX row, root INDEX row, and the new 003 plan asset bundle all belong to the same docs/spec plan-kickoff boundary.

## 16:11 工作记录

### 完成事项

- 更新 `.agent-skills/design/SKILL.md` 分支前置门禁示例：把旧的 `codex/design-{subject}` 前缀样例改为当前仓库实际使用的 `design/{subject}` / `spec-design/{subject}` 形式，与本次 003 kickoff 所在分支 `design/backend-practice-003-mode-policies-and-provenance` 命名规范保持一致。
- 单行约定调整，纯文档；不影响 design skill 的其它流程步骤、门禁顺序或脚本入口。

### 关联 Commit

- `docs(skills): align design branch prefix with repo convention`

### 备注

Manual `/work-journal` split commit. 拆分原因：本调整 scope 是 `.agent-skills/` skill 约定，与 backend-practice/003 plan kickoff（docs/spec）边界不同，按 §3 流程独立成 commit。

## 16:13 工作记录

### 完成事项

- 完成 `frontend-workspace-and-practice/002-practice-text-event-loop` L2 review fix：补齐 PracticeScreen 错误分类、retry action 边界、输入保留、mismatch refresh 锁定、strict hint conflict warning、complete 失败 fallback 与 AI transparency pending provenance，去掉硬编码 model 文案。
- 新增 practice 专用 recovery / privacy / negative 测试：`practiceErrors.test.tsx`、`practiceClientEventConflict.test.tsx`、`practiceConflict.test.tsx`、`practicePrivacy.test.tsx`、`legacyNegative.test.ts`，并新增 `frontend/tests/pixel-parity/practice.spec.ts` 覆盖 DOM anchors、theme computed style、mobile layout 和截图非空。
- 将 P0.046 / P0.047 scenario trigger 绑定到 dedicated tests；迁移 practice runtime 旧 CSS token 到当前 token 命名，避免 ui-design parity gate 假绿。
- 原地修订 plan 002 `plan.md` / `checklist.md` / `test-checklist.md` / `bdd-checklist.md` 到 v1.3，并同步 `plans/INDEX.md`；清除 completed 文档中残留的 `partial` / 未勾选 BDD 证据。
- 新增 [BUG-0057](../bugs/BUG-0057.md)，并把 completed evidence drift 规则固化到 `.agent-skills/plan-code-review/SKILL.md`。
- 验证通过：practice 新增 focused tests、practice focused suite 27 files / 120 tests、frontend full Vitest 154 files / 907 tests、typecheck、frontend build、practice Playwright 11 passed / 1 skipped、P0.044-P0.047 scenario loop、workspace P0.018-P0.021 regression、backend-practice P0.022-P0.026 regression、backend-practice 002 Go focus P0.038-P0.043、`make build`、`validate_fixtures.py`、`sync-doc-index --check`、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(frontend-practice): remediate practice event loop L2 findings`

### 备注

Manual `$plan-code-review --fix` close-out for frontend-workspace-and-practice/002. Single commit chosen because runtime code, tests, scenario wrappers, plan evidence, BUG-0057, retrospective, skill guard, and journal entry are one L2 remediation boundary.

## 00:11 工作记录

### 完成事项

- 完成 `backend-practice/003-mode-policies-and-provenance` 实施闭环：assisted hint 通过 `practice.turn.lightweight_observe` 生成 `show_hint`，strict / unknown mode 返回 `PRACTICE_SESSION_CONFLICT` 并 finalize reserved event，mode 与 goal 保持正交。
- 新增 hint AI graceful degrade：F3 / A3 / parse 失败返回 `session_wait` 且 session 保持 running；F3/parse 显式写 failed `hint_generate` task-run，A3 observed path 写 task-run provenance。
- 补齐生产 runtime wiring：新增 SQL `ai_task_runs` writer，并在 `cmd/api::buildPracticeRoutes` 中为 Practice AI 包上 A3 observability decorator，同时把 writer 传给 domain service。
- 更新 B4 baseline `ai_task_runs.task_type` CHECK、`migrations/enum-sources.yaml`、`aiclient` capability enum、appendSessionEvent fixtures、backend-practice legacy lint、Practice API README、003 plan/test/BDD 文档和生命周期状态。
- 新增 E2E.P0.048-P0.051 场景目录与脚本；脚本使用 `go test -v -run '^ExactTest$'` 并 grep 精确测试名，避免 no-op PASS。
- 记录 checklist artifact 漂移、场景脚本弱验证、生产 wiring 漏接三类痛点及后续改进建议。
- 验证通过：`cd backend && go test ./... -count=1`、048-051 scenario loop、`make codegen-check`、`make validate-fixtures`、`migrations/lint.sh`、`make lint-events`、`make codegen-events-check`、`conventions_drift.py`、`backend_practice_legacy.py --phase all`、`pytest backend_practice_legacy_test.py`、`make docs-check`、`sync-doc-index --check`、`git diff --check`。

### 关联 Commit

- `feat(backend-practice): implement mode policies and hint provenance`

### 备注

Manual `/implement` close-out for backend-practice/003. Single commit because backend service/store/runtime wiring, migration contract, fixtures, scenario wrappers, plan lifecycle, retrospective, and journal entry all belong to the same 003 implementation boundary.
