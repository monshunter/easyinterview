# 2026-05-15 工作日志

## 08:09 工作记录

### 完成事项

- 完成 `backend-practice/003-mode-policies-and-provenance` L2 review remediation，补强 P0.048-P0.051 Go HTTP scenario 对 BDD checklist 声称断言的真实覆盖。
- 修复 assisted hint replay 隐私问题：`show_hint` 的 `AssistantAction.Hint` 不再写入 `practice_session_events.payload`，replay 时从 `practice_turns.hint_text` 重建返回字段。
- 修复 A3 observability decorator 在 shared `APIError` callErr 时缺少 `error_code` / invalid validation status 的 task-run metadata 漏洞。
- 新增 [BUG-0058](../bugs/BUG-0058.md) 与 [L2 remediation 复盘报告](../reports/2026-05-15-backend-practice-003-l2-remediation-assessment.md)，并将 BDD test body assertion mapping 规则固化到 `plan-code-review` skill。
- 验证通过：`cd backend && go test ./... -count=1`、P0.048-P0.051 scenario loop、backend-practice legacy lint / pytest、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(backend-practice): remediate hint provenance L2 findings`

### 备注

Manual `$plan-code-review --fix` close-out for backend-practice/003. Single commit chosen because backend code, scenario tests, BUG-0058, retrospective report, skill guard, and work journal all belong to the same L2 remediation boundary.

## 09:28 工作记录

### 完成事项

- 完成 `backend-practice/003-mode-policies-and-provenance` L2 follow-up：strict / unknown hint 409 finalized replay payload 只保留 sanitized `requestFingerprint + error` envelope，不再保存 `requestPayload`、turn id、answer text 或空 `result`。
- 加固 P0.048-P0.051 scenario wrapper：`trigger.sh` 保留真实 `go test` exit code，`verify.sh` 必须断言目标测试 `--- PASS`、package `ok`，并拒绝 `FAIL` / `no tests to run`。
- 扩展 `backend_practice_legacy.py --phase all` 覆盖 003 scenario runtime assets，并新增 lint/pytest 合同测试避免 legacy-negative gate 回退。
- 将复盘优化落到 `plan-code-review` skill：后续 L2 evidence review 必须把 scenario wrapper 脚本作为独立 evidence artifact，不得只读取 Go test body。
- 同步更新 003 plan/checklist 到 1.1，补充 BUG-0058 和 003 L2 复盘 follow-up 证据。
- 验证通过：`cd backend && go test ./... -count=1`、P0.048-P0.051 wrapper loop、`python3 -m pytest scripts/lint -q`、`python3 -m pytest .agent-skills/plan-code-review/scripts/test_plan_code_review_skill.py -q`、`make codegen-check`、`make validate-fixtures`、`migrations/lint.sh`、`make lint-events`、`make codegen-events-check`、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(backend-practice): harden hint strict replay gates`

### 备注

Manual `$work-journal` close-out for the backend-practice/003 L2 follow-up. Single commit chosen because backend replay sanitization, scenario wrapper gates, legacy lint, plan evidence, BUG/report updates, and the plan-code-review evidence rule all close the same review finding family.

## 10:57 工作记录

### 完成事项

- 修复 `appendSessionEvent` SQL-backed replay 正确性：stored `ReplayError` 优先于零值 `ReplayResult` 返回，strict / unknown hint 409 重试会重放原始错误。
- 修复 assisted 多 hint 同 turn 的 per-event replay：`practice_session_events.payload` 继续保持 redacted，新增 `replay_payload` 保存 client-visible event snapshot，不再从可变的 `practice_turns.hint_text` 重建。
- 补充 service / store / cmd API 回归测试，并同步 backend-practice、db-migrations-baseline spec、003 plan/checklist/BDD/test 文档。
- 新增 [BUG-0059](../bugs/BUG-0059.md)，并更新 BUG-0058 与 003 L2 复盘报告中已过时的 replay source 说明。
- 验证通过：`cd backend && go test ./... -count=1`、focused replay tests、`cd backend && go test ./internal/migrations -count=1`、`migrations/lint.sh`、`make migrate-check`（dev-stack `.env`）、`make codegen-check`、`make validate-fixtures`、`make lint-events`、`make codegen-events-check`、backend-practice legacy lint / pytest、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(backend-practice): preserve append event replay snapshots`

### 备注

Manual review follow-up close-out for backend-practice/003 replay correctness. Single commit chosen because backend service/store changes, migration baseline, tests, BUG records, retrospective update, and plan/spec evidence all close the same appendSessionEvent replay contract regression.
