# 2026-07-19 工作日志

## 07:16 工作记录

### 完成事项

- 修复 active-session recovery 的两个 P1：恢复最终化先锁定 session row，queued recovery 使用 35 秒边界与 100ms 到 1 秒有界退避，超时原子写入 retryable failure。
- 为原启动 commit/failure 增加 queued status fencing 与 pending idempotency 条件，确保 timeout、completion 和迟到 worker 只有一个事务顺序胜出，失败事务不留下 opening message、event、outbox 或 audit 事实。
- 新增 deterministic service、SQL mock 与真实 PostgreSQL 相反顺序测试；根 `make test`、全部 practice integration、build、lint、codegen、OpenAPI、fixtures、docs/context/index 与 diff gates 通过。
- 原地完成 backend-practice/001 Phase 9 的 spec/plan/test/BDD 修订，并建立 [BUG-0190](../bugs/BUG-0190.md) 与[交付复盘](../reports/2026-07-19-active-session-recovery-hardening-assessment.md)。

### 关联 Commit

- `fix(practice): harden active session recovery (BUG-0190)`

### 备注

- OpenAPI 与 migration 均未变更；OpenAPI diff 为 0 breaking / 0 additive / 0 informational。
- 当前 Makefile 不存在历史文档引用的 `git-diff-check` target，本次直接执行 `git diff --check` 并通过。

## 08:14 工作记录

### 完成事项

- 完成 Harness Phase 0A 的 plan-context 合同硬收缩：49 份 `context.yaml` 删除 discovery、references 与冗余 metadata，只保留 target 和一等文档链接。
- 收缩 validator/generator/matcher/candidate 与分支推导，未知字段 fail closed；generator 保留 target identity/链接语义并可幂等重跑，change-intake 可发现无 context 的当前 plan，low 候选先用 live repo evidence 复核。
- 同步 AGENTS、相关 Skill、共享合同、两套模板和 docs 入口说明；完成交付复盘及 INDEX 更新。
- 最终验证为 155 个 Skill tests PASS、49/49 manifest validation PASS、generator 49/49 zero-diff、target/链接语义零漂移、docs/index/link 与 `git diff --check` PASS。

### 关联 Commit

- `refactor(harness): minimize plan context contract`

### 备注

Auto-committed by /tdd phase-commit, plan: harness-simplification/001-harness-v1-migration

## 09:25 工作记录

### 完成事项

- 按用户裁决把最小 `context.yaml` 固定为当前 Harness 合同，统一 generator、validator、candidate、change-intake 与 implement consumer 的读取、生成和 fail-closed 语义。
- 补齐 `scripts/harness_arch.py` 的 init/check/upgrade/repair 主路径、失败回滚与冲突保护，并新增根 `make harness-test` 聚合门禁；修复 INDEX 测试对历史标题的硬编码。
- 原地修订 Harness Spec 2.4、迁移 Plan 1.4、AGENTS、相关 Skill、模板和 docs 入口，建立 review remediation 交付复盘及 INDEX 投影。
- 当前验证为 `make harness-test` 181 passed、50/50 manifest validation、generator 50/50 zero-diff，以及 docs、Markdown links、Python compile 和 `git diff --check` 全部通过。

### 关联 Commit

- `fix(harness): close minimal context contract gaps`

### 备注

- 本次修订关闭无 manifest owner 旁路、一等链接补齐、Project Arch CLI/根 gate 缺失和 INDEX 旧标题硬编码四项 review gap；不创建独立 Bug 记录。

## 12:00 工作记录

### 完成事项

- 按方案 B 将 `PATCH /me` 泛化为 `updateMe`，以 PostgreSQL `user_settings` 原子持久化预定义/自定义账号主题；同步 OpenAPI、fixtures、generated Go/TypeScript、backend service/store/handler 与迁移约束。
- Settings 统一“设置 / Settings”和明确齿轮入口，主题草稿只在本地预览；保存仅发一次 PATCH，并以响应直接更新 runtime。登录 bootstrap / auth recovery 只获取一次 `/me`，Settings、普通页面和 Practice 切换均不重复读取远端主题。
- Practice 恢复共享全局 TopBar，同时保留独立 Session Header；完成响应式、可访问性、路由与 dev-mock 一致性测试。
- 根 `make test` 通过（Python 613 tests / 4615 subtests、Go 全包、frontend 127 files / 1037 tests），production build、OpenAPI/fixture/mock/migration/docs gates 通过；真实 `E2E.P0.101` run `e2e-p0-101-20260719034142-32741` 验证 route 往返 0 次额外 GET、保存 1 次 PATCH、重登恢复 plum 且浏览器错误为 0。
- 原地关闭相关 spec/plan/BDD lifecycle，并建立[交付复盘](../reports/2026-07-19-account-theme-runtime-persistence-assessment.md)；本次是功能交付，不新增 Bug 记录。

### 关联 Commit

- `feat(settings): persist account theme in runtime`

### 备注

- 共享场景数据库曾存在 migration metadata 与 v20 net-state 漂移；经只读确认后定向恢复 metadata，并通过正式 migrate-up 升至 v21 clean，未清理共享数据卷。
- `codegen-check` 的相对 HEAD drift 子门禁将在本提交完成后再次运行，以区分生成幂等与尚未提交的预期合同 diff。

## 16:49 工作记录

### 完成事项

- 修复 Settings 账号主题保存的异步 ownership 竞态：以 mounted state、request generation 和 current user ID 阻止卸载、退出或账号切换后的迟到响应回写 auth/runtime。
- 收敛账号主题的 closed runtime normalizer，非法枚举、额外字段、非有限或越界 custom accent 均整体回退 `ocean + null`；dev mock 同步区分 absent 与 present-invalid，并对 combined-invalid 请求保持原子拒绝。
- 原地重校对 frontend-shell、product-scope 与 OpenAPI owner，补齐 race、非法 projection、combined-invalid 和当前文档语义 gate；建立 [BUG-0191](../bugs/BUG-0191.md) 与[交付复盘](../reports/2026-07-19-account-theme-review-remediation-assessment.md)。
- 根 `make test`、typecheck、production build、OpenAPI/fixture/codegen/migration/docs gates 及真实 `E2E.P0.101` 已通过；提交前复跑前端 focused 45 tests、owner lint 31 tests 与 `git diff --check` 均通过。

### 关联 Commit

- `fix(settings): harden account theme review regressions (BUG-0191)`

### 备注

- frontend-shell/001 因既有 Phase 15.3 locale/Chrome 三项仍未完成而继续保持 active；本次账号主题修复本身已闭环。

## 17:55 工作记录

### 完成事项

- 按提供的 1916×821 参考稿原地修订首页与共享 TopBar：重建品牌导航、Hero、JD 输入卡、简历选择、主操作按钮和最近模拟面试横向记录，同时保留真实导入、幂等、运行时字节限制与继续面试行为。
- 补齐首页独立响应式样式、中文/英文文案、可访问键盘行为和路由稳定定位；桌面端与 390×844 移动端均无横向溢出，深浅色切换和真实表单状态通过 Chrome 验收。
- 原地修订 frontend-home-job-picks-and-parse 与 frontend-shell 的 spec/plan/checklist/BDD 及 `docs/ui-design/`，并建立[交付复盘](../reports/2026-07-19-home-screenshot-alignment-assessment.md)。
- 根 `make test` 通过（Python 615 tests / 4615 subtests、Go 全包、frontend 127 files / 1044 tests）；frontend typecheck、production build、docs/context/index 与 `git diff --check` 门禁均通过。

### 关联 Commit

- `feat(frontend): align home with reference UI`

### 备注

- 参考稿展示的 `5000` 未覆盖 owner runtime 合同；页面继续显示真实 `98304` 字节上限，避免以视觉静态值破坏业务一致性。
- 本次属于同一首页视觉交付，`frontend/` 实现、owner 文档、验收复盘与日志合并为一个 phase commit；不创建 Bug 记录。

## 19:07 工作记录

### 完成事项

- 按用户提供的 Workspace/Resume 列表参考稿重构正式前端：Workspace 使用全视口背景、1456px header/grid 共用右边界和 desktop 双列宽卡；Resume 使用 desktop 每行两张等宽卡、mobile 满宽单列及参考级 header/meta/footer 层级。
- 修复三项视觉验收漂移：Workspace 背景不再被 1508px 内容容器裁剪；“新建面试规划”右侧与第二列卡片右侧精确对齐；“新建简历”改用 Workspace 同款 22px 圆圈加号。
- 保持既有 generated client、详情 route、归档、启动、loading/empty/error、closed ResumeSummary 和 Home recent presentation 合同；补齐页面作用域 CSS、i18n、键盘与响应式测试。
- Chrome 在 1916×821、2048×917 和 390×844 下完成 bbox、截图、主题、键盘与 no-overflow 验收；Workspace 背景和 CTA 对齐差值均为 0，Resume/Workspace 创建 SVG 参数完全一致。
- 最终 owner scope 24 files / 151 tests、frontend typecheck/build、根 `make test`（615 tests / 4615 subtests）、local-dev 4/4 dependency、两份 context、docs/index/link 与 `git diff --check` 全部通过；原地关闭两组 plan/BDD lifecycle，并建立[交付复盘](../reports/2026-07-19-list-pages-screenshot-alignment-assessment.md)。

### 关联 Commit

- `feat(frontend): align interview and resume lists`

### 备注

- 本轮 Chrome 仅作为正式 UI 验收，不声明真实业务 E2E PASS；归档动作未在浏览器中执行，成功/失败由 owner tests 覆盖。
- 视觉反馈已由现有 spec/plan/UI owner 和复盘承接，未发现需要独立 Bug 知识库记录的新运行时缺陷或重复 Bug 模式。

## 20:18 工作记录

### 完成事项

- 按三张参考图统一优化面试进行、上传简历和面试报告页面：对齐全局导航下方起点、desktop 内容面、响应式网格、卡片、按钮、SVG icon、字体和视觉层级，同时保持原有 API、路由、幂等、消息、简历注册和报告事实合同。
- 根据追加反馈把 AI/用户角色改为方形标识，把说明改为带 sparkle icon 的浅色胶囊；进一步将 Transcript 固定为唯一滚动区、Composer 固定在会话卡底部，说明胶囊归属 InputBar 并始终贴在输入框上方 8px。
- 新增 [BUG-0192](../bugs/BUG-0192.md)，原地修订三组 owner spec/plan/checklist/BDD/test 与 UI design，并建立[交付复盘](../reports/2026-07-19-core-workflow-reference-ui-alignment-assessment.md)。
- Chrome 完成 1916×821 / 390×844 三页视图验收；Practice 在 desktop/mobile 实际滚动 Transcript 后，输入框坐标和 helper/input 间距保持不变，document overflow 为 0。正式环境健康与 fixture 视觉状态分开报告，未声明未运行的真实 active-session / ready-report E2E PASS。
- 最终 focused 为 Resume 21、Practice 56、Report 30 tests；根 `make test` 为 Python 615 tests / 4615 subtests、Go 全包、frontend 131 files / 1054 tests；typecheck/build、context、docs/index/link、Go/frontend lint 与 `git diff --check` 通过。

### 关联 Commit

- `fix(frontend): align core workflow pages with UI references (BUG-0192)`

### 备注

- 根 `make lint` 的唯一失败来自未改动的 `.agent-skills/change-intake/SKILL.md:120` 被 `generic-api-key` 规则误判；除该既有命中外的所有 lint owner 均已逐项通过，后续建议由 secrets/skills owner 单独修复。

## 20:45 工作记录

### 完成事项

- 根据用户标框重新实施报告页完整目标构图，替换上一轮仅调整宽度、背景与圆角的表层结果：Header 固定复练蓝色主按钮与下一轮描边次按钮，Context 改为一张四列内部竖线分隔的整卡。
- 将两张 Metrics、四张 Detail 与 Overall 统一为目标稿的左侧语义图标轨和紧凑正文层级；保留 Dimensions 的状态/置信度，删除 Highlights/Issues 的重复置信度，不改变 API、路由、报告事实与 CTA 行为。
- 以 RED/GREEN 补齐单体 Context、四种 Detail icon、1432px 网格、无 ready 行内视觉拼装和 fixed replay-primary 合同；focused 23 tests、根 `make test`（Python 615 / 4615 subtests、Go 全包、frontend 131 files / 1055 tests）、typecheck/build 全部通过。
- Chrome 在真实 backend ready report 上完成 desktop `1920×964` 与 exact mobile `390×844` 验收；desktop Overall 完整进入首屏，两档横向溢出为 0。原地更新 [BUG-0192](../bugs/BUG-0192.md)、owner spec/plan/BDD/test 与[交付复盘](../reports/2026-07-19-core-workflow-reference-ui-alignment-assessment.md)。

### 关联 Commit

- `fix(report): rebuild dashboard target composition (BUG-0192)`

### 备注

- 本轮 Chrome 是 scoped real UI acceptance；未执行并未声明完整 `E2E.P0.099` PASS。临时 390×844 viewport 验收后已恢复用户默认视口。
