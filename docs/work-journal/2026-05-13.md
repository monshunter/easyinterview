## 09:45 工作记录

### 完成事项

- L2 审查加固：register 时强制校验对象存储实际 size 与 byte_size 精确匹配，防止低报超限
- Upload presign idempotency cache TTL 对齐 presignTTLSeconds
- `cmd/api` 接入 privacy_delete job drainer，运行时链路打通
- 对象存储 delete + DB hard delete + audit tombstone 纳入同一 DB transaction
- ObjectStore interface 引入 `ObjectStat` (Exists+Size) 替换旧 bool exists checker
- store 新增 `HardDeleteWithAuditTombstone` 方法
- E2E.P0.033 场景脚本加固（trigger.sh / verify.sh）
- plan/checklist/spec/bdd 文档同步至 Phase 6
- INDEX.md（bugs/reports/spec/plans）更新

### 关联 Commit

- `fix(backend-upload): harden presign size enforcement, atomic privacy delete, and runtime wiring`

### 备注

L2 remediation hardening for backend-upload 001-file-objects-and-presign-baseline plan. Scope: Phase 6 - size enforcement, idempotency TTL alignment, privacy delete wiring, atomic DB+audit, scenario gate.

## 10:15 工作记录

### 完成事项

- MinIO client 并发安全修复：添加 `sync.Mutex` 保护 lazy client 初始化，消除 race condition
- 新增 `TestMinIOStoreClientForConcurrentFirstUse` 竞态回归测试（16 workers 并发首次调用）
- 新增 `TestUploadPresignRegisterPrivacyDeleteLiveRoundtrip` integration test：覆盖真实 HTTP presign → MinIO PUT → RegisterFileObject → DELETE /api/v1/me → privacy drainer 完整 roundtrip
- BUG-0049 记录：backend upload follow-up review exposed MinIO race and no-op live gate
- Plan/checklist/bdd 文档 v1.3 加固：trigger.sh 必须执行 `TestUploadPresignRegisterPrivacyDeleteLiveRoundtrip` live roundtrip gate
- verify.sh 拒绝 no-op gate（`no tests to run`）、skip 或缺少 live roundtrip 证据
- script_contract_test.py 扩展：新增 3 个 test case 覆盖 missing live roundtrip evidence、no tests to run、live roundtrip gate 存在性
- scenario README + expected-outcome 同步 live roundtrip 契约

### 关联 Commit

- `fix(backend-upload): close follow-up L2 gaps`

### 备注

BUG-0049 L2 跟进审查收口。修复了 MinIO 并发竞态，强化了 live roundtrip gate 以杜绝 no-op 证据通过场景验证。

## 11:43 工作记录

### 完成事项

- Resume Workshop 列表页重新按 `ui-design/src/screen-resume-workshop.jsx` 当前原型收口：TopBar 文案改为“简历”，补齐 header “新建简历”、StatsStrip 副文案、TreeView 底稿选择 helper、版本行缩进 / icon / tag / match badge 与上传新底稿入口
- 移除旧 per-row 分叉 toast 口径，删除 `resumeWorkshop.tree.toastSelect` / `resumeWorkshop.tree.toastBranch` locale key，并新增 locale 负向断言防止回流
- 加固 `TopBar`、`ResumeListView`、`ResumeTreeView`、`ResumeVersionRow`、P0.036 场景与 locale 文件测试，覆盖当前 UI parity 结构
- 新增并更新 BUG-0050、交付复盘报告与 docs index，记录本次 UI parity drift 的根因、修复和验证证据
- 验证通过：focused Vitest、Resume Workshop adjacent regression、full frontend Vitest、frontend build、pixel parity、`make docs-check`、`sync-doc-index --check`、`git diff --check`

### 关联 Commit

- `fix(frontend-resume-workshop): restore ui-design list parity`

### 备注

BUG-0050 L2 follow-up close-out. Scope: current `frontend-resume-workshop/001-listing-routing-and-detail-readonly` frontend target, UI parity drift, BDD/component test hardening, bug/report documentation.

## 14:16 工作记录

### 完成事项

- 完成 `backend-resume/001-asset-register-parse-and-listing` L1 plan-review 修订收口，保持 doc-only 范围
- 补齐 `registerResume` / `getResume` / `listResumes` operation matrix 的 `cmd/api` route、session / IK middleware、真实 runtime 与 fixture byte parity gate
- 明确 resume_parse in-process drainer、`RunOnce` deterministic stepping、ready-only outbox event、no-op / skip BDD 失败条件与 live runtime evidence
- 对齐 backend-upload 当前 `RegisterFileObject` object `Stat` + actual size mismatch rejection 契约，并固定 `resume_assets` + `async_jobs` 同事务创建要求
- 修正 listResumes cursor 唯一稳定序、export out-of-scope 口径与 history 收尾语句
- 验证通过：`validate_context.py --target backend`、`sync-doc-index --check`、`make docs-check`、`git diff --check`、`openapi_inventory.py openapi/openapi.yaml`

### 关联 Commit

- `docs(backend-resume): harden register parse plan gates`

### 备注

Manual `/work-journal` close-out for backend-resume plan-review `--fix`.

## 14:59 工作记录

### 完成事项

- 完成 `backend-resume/001-asset-register-parse-and-listing` Phase 1 register / get handler baseline
- 新增 register sourceType 三路校验、Idempotency-Key middleware replay/mismatch 测试、upload `RegisterFileObject` handoff service 测试
- 新增 `resume_assets` + `async_jobs(resume_parse)` 原子创建 store skeleton 与 rollback 测试
- 新增 getResume generated surface 与 not-found/cross-user 404 handler 映射测试
- 验证通过：`go test ./internal/resume/... -count=1`

### 关联 Commit

- `feat(backend-resume): add resume register and get baseline`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-resume/001-asset-register-parse-and-listing

## 20:41 工作记录

### 完成事项

- 完成 `backend-practice/002-event-loop-and-completion` Phase 0 跨 spec 前置修订与 preflight
- 为 `shared/jobs.yaml` 增加 `triggerEventSemantic`，由 B3 codegen 生成 Go/TS 语义常量与 `IsSourceEventOnly` 谓词，并让 `report_generate` 明确成为 `source_event_only`
- 同步 B3 `event-and-outbox-contract` v2.5 与 B2 `openapi-v1-contract` v1.19，原地 rebase `PracticeTurn.status` wire enum 到 5 值并刷新 OpenAPI baseline / generated Go+TS artifacts
- 扩展 PracticeSessions append / complete fixtures 的 replay、mismatch、strict hint conflict、completed、cross-user 等命名场景，并把场景集合写入 fixture validator 与 contract tests
- 补齐 F3 preflight focused test，断言 `practice.session.first_question` / `practice.session.follow_up` / `practice.turn.lightweight_observe` 可通过 RegistryClient Resolve
- 验证通过：`make lint-events`、`make validate-fixtures`、`make docs-check`、`make codegen-check`、OpenAPI / fixture / events unittest、`go test ./cmd/codegen/events ./internal/shared/jobs ./internal/ai/registry -run 'TestGenerate(Go|TS)Outputs|TestGeneratedJobMappings|TestResolvePracticeSessionBaselineFeatures' -count=1`

### 关联 Commit

- `feat(backend-practice): complete event loop phase 0 preflight`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-practice/002-event-loop-and-completion

## 15:14 工作记录

### 完成事项

- 完成 `backend-resume/001-asset-register-parse-and-listing` Phase 2 resume_assets store + state machine
- 新增 `CreateWithParseJob` / `Get` / `List` / `MarkParsing` / `MarkReady` / `MarkFailed` / `DeleteForUser` repository 方法
- 补齐 queued -> processing -> ready|failed 状态机、非法转换拒绝、cursor pagination 稳定排序测试
- 新增 live integration test 覆盖 CRUD、state transition、cross-user isolation、FK 约束、`resume_assets` + `async_jobs` 原子提交与 rollback
- 新增 `resume_assets.error_code` migration，支撑 parse failed error code 持久化
- 验证通过：`go test ./internal/resume/store -count=1`、`go test ./internal/resume/store -tags=integration -run TestResumeAssetsIntegrationCRUDStateIsolationPaginationAndRollback -count=1 -v`、`git diff --check`

### 关联 Commit

- `feat(backend-resume): add resume asset store state machine`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-resume/001-asset-register-parse-and-listing

## 15:33 工作记录

### 完成事项

- 完成 `backend-resume/001-asset-register-parse-and-listing` Phase 3 resume.parse async job + AIClient 集成
- 新增 `backend/internal/resume/jobs/parse.go`，覆盖 paste / guided / upload 三路 prompt input、F3 `resume.parse` registry adapter、A3 AIClient metadata、ready / failed 状态提交与 retryable async job 语义
- 收窄 `targetjob.Drainer` 的 store 依赖为 async job claim/finalize 子集，使 resume_parse 可复用现有 in-process drainer
- 补齐 `resume_assets` parse success / failure 原子提交、`resume.parse.completed` outbox、`ai_task_runs` observed wrapper、PII redline 与 Preview Confirm 前不创建 `resume_versions` 的测试
- 增加 object store `Read` 能力及 filesystem/minio 实现，支撑 upload 简历内容读取
- 验证通过：`go test ./internal/resume/... ./internal/upload/... ./internal/targetjob -count=1`、`go test ./internal/resume/store -tags=integration -run TestResumeAssetsIntegrationCRUDStateIsolationPaginationAndRollback -count=1 -v`、`sync-doc-index --check`、`git diff --check`

### 关联 Commit

- `feat(backend-resume): add resume parse job handler`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-resume/001-asset-register-parse-and-listing

## 15:54 工作记录

### 完成事项

- 完成 `backend-resume/001-asset-register-parse-and-listing` Phase 4 listResumes handler 与 cmd/api runtime wiring
- 新增 `ListResumes` handler、service read/list 映射、cursor `PageInfo` 传递、fixture parity 测试
- 将 `POST /api/v1/resumes`、`GET /api/v1/resumes`、`GET /api/v1/resumes/{resumeAssetId}` 挂载到 `cmd/api` session middleware，并对 register 接入 Idempotency-Key middleware
- 新增 `resumeRuntime`，把 resume_parse drainer 纳入 `Start(ctx)` / `Shutdown(ctx)` lifecycle，并为 APP_ENV=test 提供 deterministic `resume.parse` AI fixture
- 新增 `TestResumeRegisterListHTTPScenario` 覆盖 register/get/list、401、404、IK replay 与重复创建保护
- 验证通过：`go test ./cmd/api -count=1`、`go test ./internal/resume/... -count=1`、`git diff --check`

### 关联 Commit

- `feat(backend-resume): wire resume routes and listing`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-resume/001-asset-register-parse-and-listing

## 16:06 工作记录

### 完成事项

- 派生 backend-practice/002-event-loop-and-completion plan 包：plan.md / checklist.md / test-plan.md / test-checklist.md / bdd-plan.md / bdd-checklist.md / context.yaml
- 执行 `/plan-review --fix`，把 9 项 L1 发现固化到 002 plan 与 owner spec：
  - F1/F5/F6：dispatcher 集成 gate 改写为 forward-binding 元数据 + jobs.yaml lint + `IsSourceEventOnly` 谓词 + `async_jobs(job_type, dedupe_key)` UNIQUE + repo grep；删除 `backend/internal/runtime/outbox/` 不存在路径引用；风险表新增"无 runtime dispatcher"明确条目
  - F2：`outbox_events.dedupe_key` 误写改为 `aggregate_id=sessionId`，唯一性改为应用层 D-35 Replay 路径保证
  - F3/F9：spec §7 row 2 重写以反映 D-32 / D-33 / D-34 / D-35 plan-level 决策；D-25 末尾追加 002 落实备注（"handler 压缩映射" 分支淘汰）
  - F4：删除 `completing_at=now()` 虚构列，改为 `updated_at=now()`，002 不动 migration
  - F7：BDD E2E.P0.038 verify 限定 1:1 到 `event_name='practice.turn.completed'`
  - F8：精确化 idempotency middleware 调用顺序（Reserve → next.ServeHTTP buffered → MarkSucceeded/MarkFailed）
- spec.md / history.md Header `1.5 → 1.6` + 日期 `2026-05-10 → 2026-05-13`；history.md 追加 1.6 行
- 同步 `docs/spec/INDEX.md` 与 `docs/spec/backend-practice/plans/INDEX.md`；`sync-doc-index --check` zero drift

### 关联 Commit

- `docs(backend-practice): harden event loop plan gates`

### 备注

L1 plan-review --fix doc-only close-out for backend-practice/002-event-loop-and-completion. Scope: spec §7 row 2 reframing, D-25 footnote, plan-level D-32/D-33/D-34/D-35 forward-binding wording, outbox/dedupe_key/completing_at schema fixes, BDD 1:1 scoping, idempotency middleware ordering precision.

## 16:25 工作记录

### 完成事项

- 完成 `backend-resume/001-asset-register-parse-and-listing` Phase 5 收口与计划生命周期关闭
- 新增 E2E.P0.034 / E2E.P0.035 场景目录、脚本、数据与 INDEX 行，并实际执行 `setup -> trigger -> verify -> cleanup` 全 PASS
- 新增 `TestResumeParseDrainerHTTPScenario` 与 `TestGetResumeFixtureParity`，补齐 resume_parse drainer 和 getResume default/not-found fixture parity gate
- 修正 `getResume.not-found` fixture error code 漂移，创建 [BUG-0051](../bugs/BUG-0051.md) 记录 schema/fixture/handler parity 缺口
- 将 backend-resume plan / checklist / BDD 文档标记 completed，并补齐 frontend workspace 001 active-list handoff 与交付复盘报告
- 验证通过：`go test ./...`、`go test ./internal/resume/...`、`go test ./cmd/api -run 'TestBuildResumeRuntime|TestResumeRegisterListHTTPScenario|TestResumeParseDrainerHTTPScenario' -count=1`、E2E.P0.034/P0.035 scripts、`sync-doc-index --check`、`make docs-check`、`git diff --check`

### 关联 Commit

- `feat(backend-resume): close resume baseline verification`

### 备注

Final close-out for backend-resume/001. Includes BUG-0051 and `docs/reports/2026-05-13-backend-resume-001-assessment.md`.

## 18:07 工作记录

### 完成事项

- 按 `/plan-review backend-practice/002-event-loop-and-completion --fix` 二次审查结果继续原地修订，保持 doc-only 范围
- 将 B3 event/job gate 从无参 `events_inventory.py` 对齐为当前真实入口 `make lint-events` / `make codegen-events-check`，并明确 `triggerEventSemantic` 常量与谓词必须由 `backend/cmd/codegen/events` 生成
- 移除 002 对不存在 runtime dispatcher / drainer 包的实施依赖，改为 future `backend-async-runner` forward-binding
- 对齐 append audit 口径：`appendSessionEvent` 不写 `audit_events`，complete path 才验证 audit 红线
- 修正 BDD gate：`ReportWithJob` wire 不断言 `dedupeKey`，并发场景改为一个成功 + 一个 stale/conflict，N≥10 压力验证下放到 repository integration test
- 刷新 `context.yaml` discovery 路径，删除不存在的 `backend/internal/runtime/outbox`，补充 event codegen 与 shared jobs 路径
- 验证通过：`validate_context.py --target backend`、`sync-doc-index --check`、`make docs-check`、`git diff --check`

### 关联 Commit

- `docs(backend-practice): align event loop review gates`

### 备注

Manual `/work-journal` close-out for backend-practice/002 L1 plan-review follow-up. Scope: spec/plan/checklist/test/BDD/context wording only.

## 18:09 工作记录

### 完成事项

- 完成 `backend-resume/001-asset-register-parse-and-listing` Phase 6 L2 remediation
- 修复 `RegisterResume` / `ListResumes` typed validation error 到 `422 + VALIDATION_FAILED` 的 handler 映射
- 修复 `resume.parse` retryable timeout 状态落库：每次失败写 `failed + error_code`，retry 由 `async_jobs` 表达，并允许 failed asset 回到 processing 后 ready
- 加固 P0.034 / P0.035 trigger/verify，要求新增 validation / retry 测试名并拒绝 no-op / skip
- 创建 [BUG-0052](../bugs/BUG-0052.md) 与 `docs/reports/2026-05-13-backend-resume-asset-register-l2-assessment.md`
- 验证通过：`go test ./internal/resume/... ./cmd/api`、P0.034/P0.035 scripts、`make docs-check`、`sync-doc-index --check`、`git diff --check`

### 关联 Commit

- `fix(backend-resume): remediate asset registration L2 findings`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-resume/001-asset-register-parse-and-listing
