# 2026-04-26 工作日志

## 10:53 工作记录

### 完成事项

- 引入根级 Agent 治理文档：`AGENTS.md` 作为唯一真理源，`CLAUDE.md` / `GEMINI.md` 通过符号链接指向同一文件
- 落地 `.agent-skills/` 共享技能目录，沉淀 `change-intake`、`implement`、`tdd`、`plan-review`、`plan-code-review`、`work-journal`、`scenario-*` 等 skill 资产
- 通过 `.agent/`、`.claude/`、`.codex/`、`.gemini/` 下的 `skills` 符号链接，统一对接 Claude Code、Codex、Gemini CLI 等 Agent 客户端

### 关联 Commit

- `chore(governance): bootstrap agent governance and shared skills`

### 备注

首次提交治理文档与 Agent skill 集合，为后续 spec/plan 与功能实现提供执行入口与协作约束。

## 10:55 工作记录

### 完成事项

- 初始化 `docs/` 文档骨架：`README.md` 文档导航 + 七个子目录（`spec`、`plan`、`reports`、`discuss`、`apis`、`work-journal`、`bugs`）
- 每个子目录补齐规范三件套：`README.md`（命名/流程/检查清单）、`TEMPLATES.md`（可复制模板）、`INDEX.md`（索引导航）
- `docs/bugs/` 额外补充 `PATTERNS.md`，承载 Bug 知识库的模式总结

### 关联 Commit

- `docs(skeleton): initialize project documentation directories`

### 备注

为后续 spec/plan/report/journal/bug 写入提供统一的目录骨架与模板入口，对齐 §3 §4 文档规范。

## 10:55 工作记录（2）

### 完成事项

- 导入产品规格 `easyinterview-spec-v1-0.md`，作为 P0 MVP 闭环（JD 导入 → 工作台 → 模拟面试 → 证据化报告 → 错题本 → 复练）的真理源
- 导入技术实施文档包 `easyinterview-tech-docs/`：00 共享规范、01 技术架构、02 API 定义、03 DB 定义、04 指标观测、05 日志规范、06 事件契约
- 这两份资料保留在仓库根目录作为输入资产，后续 `/design` 与 `/implement` 流程会基于它们生成 `docs/spec/` 与 `docs/plan/` 文档

### 关联 Commit

- `docs(spec): import product spec and P0/P1 technical docs`

### 备注

仅做资料入仓，不修改既有内容；后续如需重组归位再走 `/create-doc` 与 INDEX 更新流程。

## 10:56 工作记录

### 完成事项

- 导入 UI 原型 `easyinterview-ui/`：包含 P0 / P1 主链路屏幕（home / workspace / jd-match / company-intel / practice / report / completion 等）的 jsx 实现
- 同时引入 `design-canvas.jsx` 与 `frames/design-canvas.jsx` 作为整体设计画布、`screenshots/` 主页截图、`uploads/` 历史 spec 资料以及三个独立的 standalone HTML 入口（Canvas / Standalone / 总入口）
- 这些原型作为 P0 模拟面试与证据化报告闭环的视觉与交互参考，后续将驱动 spec/plan 文档生成与正式前端工程化

### 关联 Commit

- `feat(ui): import easyinterview UI prototype`

### 备注

原型目录位于仓库根，尚未做工程化（构建、依赖、路由）；后续会基于 spec/plan 决定如何拆分为可运行的前端工程。

## 14:01 工作记录

### 完成事项

- 整理 `easyinterview-ui/` 原型文件名：`EasyInterview Canvas.html` → `easyinterview-canvas.html`（去除空格、统一小写连字符），便于本地静态服务器与脚本访问
- 移除冗余的 `EasyInterview · Standalone.html`（包含中点字符且与现有入口重复，不再保留）
- 新增 `easyinterview-ui/run.sh` 一键启动脚本：默认基于 `python3 -m http.server` 启动静态服务，支持 `-p` / `-f` / `--no-open` 参数，自动处理端口占用与入口文件 URL 编码

### 关联 Commit

- `chore(ui): tidy prototype filenames and add run script`

### 备注

仅做文件名规范化与本地预览脚本，未修改原型内部逻辑；后续工程化迁移仍以 spec/plan 为准。

## 19:20 工作记录

### 完成事项

- 调整文档结构为 spec-centric plan 布局：删除全局 `docs/plan/` 目录，改为 `docs/spec/<subspec>/plans/` 局部计划索引；同步更新 `docs/spec/{README,TEMPLATES,INDEX}.md` 与 `docs/reports/TEMPLATES.md`
- 重写 `init-docs` 模板资产：移除 `plan-{readme,index,templates}.md`，新增 `subspec-plans-{readme,index,templates}.md`；`docs-readme.md` 与 `init-docs` 契约测试同步对齐
- 重写 `implement` 共享脚本：`generate_context_yaml.py`、`list_context_candidates.py`、`validate_context.py` 全面适配 `docs/spec/<subspec>/plans/<NNN-plan>/context.yaml` 入口，`plan-context-contract.md` 同步更新
- 同步联动 skills：`change-intake`、`create-doc`、`design`、`implement`、`init-docs`、`plan-code-review`、`plan-review`、`scenario-create`、`sync-doc-index`、`tdd`、`work-journal` 的 SKILL.md 与契约测试统一引用 spec-centric 路径

### 关联 Commit

- `refactor(governance): adopt spec-centric plan layout under each subspec`

### 备注

无现存 subspec 受影响，本次仅迁移骨架与模板；后续新建 spec 时直接按新布局落地 plan 目录。

## 16:47 工作记录

### 完成事项

- 落地顶层规划 spec `docs/spec/engineering-roadmap/`：`spec.md`（§1–§7，含 6 层 38 child subspec 总表 + 依赖 DAG + 6 wave 实施顺序）、`history.md`（v1.0 初始修订）
- 创建 `engineering-roadmap` 唯一 plan `001-decompose-subspecs`：`plan.md`（7 phase 对应 W0–W5 + 收尾）、`checklist.md`（无 BDD-Gate 引用，纯 verification 项）、`context.yaml`（target=docs，不含 bddPlan/bddChecklist 字段）
- 初始化 `docs/spec/engineering-roadmap/plans/` 局部脚手架（README / TEMPLATES / INDEX，按 init-docs `subspec-plans-*` 模板）
- 同步 `docs/spec/INDEX.md`：新增 `engineering-roadmap` 主行 + 38 行 child subspec 占位（按 Layer A–F × Phase P0/P1/P2 两轴分组，状态 `_pending_`）
- 验证：`validate_context.py` 通过；`sync-doc-index --check` 报告 0 violations / 0 drifts / 0 orphans（38 warnings 全为预期占位行）

### 关联 Commit

- `docs(spec): scaffold engineering-roadmap top-level spec and 38 child placeholders`

### 备注

本会话仅产出顶层规划资产，不涉及业务代码与 child subspec 具体内容。锁定 4 项决策：spec 名 `engineering-roadmap`、6 层结构含 Quality 横切、UI 原型作为 mock fixtures + 视觉真理源、5 项历史悬而未决方案设为 W0 hard gate（每项产出 1 份 ADR）。后续按 plan checklist Phase 1 推进 → Phase 2 spawn `repo-scaffold` + `shared-conventions-codified`。

## 17:14 工作记录

### 完成事项

- 修复 `engineering-roadmap/001-decompose-subspecs` L1 plan-review 发现的问题：明确 W0 ADR 固定落点、将 `make dev-up` 从 W0 gate 调整到 A2/W1 gate、解除 E4 对 P1 F4 的静态依赖
- 对齐 roadmap spec 的依赖表与 DAG：补齐 C4 对 C2/C3 的依赖、E2 对 E1 mock suite 的依赖，并把 Q-5 P0 隐私范围改为 ADR 驱动的 gate
- 修订 Phase 7 的 P1/P2 child 生命周期：P0 收尾只创建 draft spec，进入 Wave 6+ 前再补齐 plan / checklist / context
- 勾选 checklist 1.3、1.4、1.5，记录占位行、L1 修复和 Header/INDEX 同步验证已经完成
- 验证：`validate_context.py --target docs` 通过；`sync-doc-index --check` 报告 0 violations / 0 drifts / 0 orphans（占位行 warnings 为预期）

### 关联 Commit

- `docs(spec): reconcile engineering-roadmap plan review findings`

### 备注

本次只修订已验证的 plan-review 文档问题，不创建新 child spec，不修改业务代码；ADR 决策项 1.1 / 1.2 仍保持未完成。

## 17:48 工作记录

### 完成事项

- 将 `engineering-roadmap` 顶层 spec 与唯一 plan 升级到 v1.1，新增 Q-6「AI 网关与模型路由」W0 hard gate
- 将 A3 从 `llm-gateway-bootstrap` 修订为 `ai-gateway-and-model-routing`，明确业务代码只依赖 provider-neutral `AIClient` 与 Model Profile
- 明确 Higress 等 AI Gateway 作为独立部署组件接入，不作为业务 SDK 绑定；生产通过 `AI_GATEWAY_BASE_URL` 指向 gateway route，由运维配置 provider / model / fallback / token rate limit
- 同步 `docs/spec/INDEX.md`、`engineering-roadmap` 局部 plans INDEX、history、context.yaml 与 checklist 中的版本和引用
- 验证：`validate_context.py --target docs` 通过；`sync-doc-index --check` 报告 0 violations / 0 drifts / 0 orphans（占位行 warnings 为预期）；`git diff --check` 通过

### 关联 Commit

- `docs(spec): add ai gateway model routing gate`

### 备注

本次只落地已确认的 AI Gateway / Model Profile 架构方向；RAG / embedding 策略仍保留为后续单独决策。

## 18:17 工作记录

### 完成事项

- 关闭 `engineering-roadmap/001-decompose-subspecs` Phase 1 W0 hard gate：6 项 ADR 全部 accepted
  - ADR-Q1 认证：自建 passwordless email magic link + first-party session cookie；OIDC / 托管 Auth 推迟到 P1 / 团队版触发条件
  - ADR-Q2 异步编排：Asynq + Redis 唯一 runtime；critical/default/low 三级队列；PG outbox + dispatcher；Temporal 推迟到跨日 SLA / 多步补偿
  - ADR-Q3 分析平台：PostHog Cloud（EU region）唯一后端；F2 adapter 抽象；feature flag 共用 PostHog console
  - ADR-Q4 云部署：Kubernetes（managed cluster；staging/dev = Kind）；helm chart 与 `test/scenarios/` 同源；CI 不直接 deploy
  - ADR-Q5 隐私节奏：P0 仅删除链路（24h SLA + 跨 17 表硬删 + audit）；导出延后到 P1；`POST /privacy/exports` 在 v1.0.0 freeze 中预留并返回 501
  - ADR-Q6 AI 网关：应用内 `AIClient` + Model Profile + stub provider；生产通过 `AI_GATEWAY_BASE_URL` 指向 OpenAI-compatible gateway route；零厂商 SDK 入侵；fallback / cost cap / rate limit 全部归运维 gateway 配置
- 新增 `docs/spec/engineering-roadmap/decisions/` 目录与 `README.md` 索引；6 份 ADR 统一采用「背景 / 选项与取舍 / 决策 / 影响范围 / 失效与修订条件 / 关联」结构
- 同步 `engineering-roadmap/spec.md` v1.1 → v1.2：§3.2 表替换为「锁定结论 + ADR 链接」六行；§4.4（F4 维持 P1 不升格）/ §5.5 E4 / §6 C-1 / C-6 引用具体 ADR
- 同步 `engineering-roadmap/history.md` v1.2 修订记录；plan / checklist Header 与 `context.yaml.specVersion.to` 同步至 1.2；checklist 1.1 / 1.2 勾选；`docs/spec/INDEX.md` 与本地 plans INDEX 投影同步
- 验证：`validate_context.py --target docs` 通过；`sync-doc-index --check` 报告 0 violations / 0 drifts / 0 orphans（38 placeholder warnings 为预期）；spec §3.2 anchor 与 plan.md 引用对齐

### 关联 Commit

- `docs(spec): land engineering-roadmap W0 ADRs and bump to v1.2`

### 备注

Phase 1 全部 5 项勾选完成（1.1 + 1.2 本次完成；1.3 / 1.4 / 1.5 此前已完成）。下一步进入 Phase 2 Wave 0：spawn A1 `repo-scaffold` + B1 `shared-conventions-codified` 两份 child subspec（每份 spec.md + history.md + plans 脚手架 + 至少 1 个 plan / checklist / context.yaml）。本次不涉及业务代码与 child subspec 内容。

## 18:59 工作记录

### 完成事项

- 修复 `engineering-roadmap/001-decompose-subspecs` L2 code review 发现的 async public `jobType` 命名漂移：公共 API / DB / event / metrics 继续使用 snake_case，内部 Asynq handler 可通过 C8 映射 dotted task name
- 按用户决策将 ADR-Q3 从 PostHog Cloud 切换为自托管 PostHog 优先，并明确不依赖第三方 Cloud / Segment / Warehouse；同时记录 PostHog K8s / Helm 支持不再作为默认假设
- 明确 ADR-Q5 中 P0 导出延后是对产品 P0 验收项的 W0 例外，release gate 必须记录该 tradeoff
- 同步 `engineering-roadmap` spec / history / plan / checklist / context.yaml / INDEX 到 v1.3，并勾选 checklist 1.6 remediation
- 验证：`validate_context.py --target docs` 通过；`sync-doc-index.py --check` 通过（38 个占位 warning 为预期）；`git diff --check` 通过

### 关联 Commit

- `docs(spec): reconcile roadmap ADR decisions`

### 备注

本次只修订 roadmap 与 ADR 文档，不创建新的 child subspec，不修改业务代码。

## 20:14 工作记录

### 完成事项

- 完成 `engineering-roadmap/001-decompose-subspecs` Phase 2 Wave 0 docs spawn：A1 `repo-scaffold`、B1 `shared-conventions-codified` 两份 child subspec
- A1 `repo-scaffold`：落地 spec.md（v1.0，锁定 7 个根容器目录 / 顶层 Makefile target / `.editorconfig` / `.tool-versions` / git hooks 落点）+ history.md + plans 脚手架（README/TEMPLATES/INDEX）+ `001-bootstrap` 三件套（plan/checklist/context.yaml）
- B1 `shared-conventions-codified`：落地 spec.md（v1.0，锁定跨语言真理源 `shared/conventions.yaml` / Go module `github.com/monshunter/easyinterview/backend` / pnpm workspace / UUIDv7 + tmp_ id 规则 / 错误码 `UPPER_SNAKE_CASE` lint）+ history.md + plans 脚手架 + `001-bootstrap` 三件套
- 同步 `docs/spec/INDEX.md`：A1 / B1 两行由 `_pending_` 占位切换为指向真实 `spec.md` 的链接，状态 `active`、版本 1.0、更新日期 2026-04-26
- 同步 `engineering-roadmap/001-decompose-subspecs/checklist.md` Phase 2.1–2.4 全部勾选（W0 收口边界明确：spec scaffold + context.yaml validator 通过；A1/B1 bootstrap 实现归各自 child 的 `/implement`，W1 末闭合 spec C-2）
- 验证：`validate_context.py --target docs` 对 A1 / B1 / engineering-roadmap 三个 context.yaml 全部通过；`sync-doc-index.py --check` 报告 0 violations / 0 drifts / 0 orphans（36 个 `_pending_` warning 为预期，比 Phase 1 时的 38 减少 2）

### 关联 Commit

- `docs(spec): spawn W0 child subspecs repo-scaffold and shared-conventions-codified`

### 备注

Phase 2 W0 docs scaffold 完成，engineering-roadmap/001 进度由 6/43（13%）提升至 10/43（23%）。本次仅产出文档资产，不修改业务代码；A1 与 B1 的 bootstrap 实现（root Makefile / Go module / TS lib / generator / 工具与测试）由各自 child 的 `/implement` 在 W0→W1 之间落地。下一步进入 Phase 3 Wave 1：并行 spawn 9 份 spec（A2 / A3 / A4 / A5 / B2 / B3 / B4 / F1 / F3）的 spec.md + history（仅 spec，不写 impl plan，等批量 `/plan-review` 后再决定是否提前进入 plan 阶段）。

## 20:33 工作记录

### 完成事项

- 收口 spec-centric plan scaffold：`docs/spec/<subspec>/plans/` 只保留 `INDEX.md`，具体 plan 目录只保留 `context.yaml` / `plan.md` / `checklist.md`（可选 BDD 文件）
- 删除已生成的 per-subspec `plans/README.md` 与 `plans/TEMPLATES.md`，删除 `/init-docs` 中对应的 `subspec-plans-readme.md` / `subspec-plans-templates.md` 模板资产
- 更新 `docs/spec/README.md`、`docs/spec/TEMPLATES.md`、`/init-docs`、`/create-doc`、`/design` 及契约测试，明确 plan 规则和模板统一由 `docs/spec/` 承载
- 同步 `engineering-roadmap/001-decompose-subspecs` plan/checklist 到 v1.5，记录 W0 脚手架结构修复
- 验证：相关契约测试 20 passed；`sync-doc-index.py --check` 0 drift / 0 orphan；A1 / B1 / engineering-roadmap 三个 context validator 通过；`git diff --check` 通过

### 关联 Commit

- `docs(spec): collapse per-subspec plan scaffold`

### 备注

本次只修订文档治理与 skill 脚手架规则，不修改业务代码；后续 child subspec spawn 不再生成 per-subspec plan README / TEMPLATES。
