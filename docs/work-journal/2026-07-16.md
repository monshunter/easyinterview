## 10:43 工作记录

### 完成事项

- 使用 humanizer 统一改写中英文前端文案，保留全部 i18n key、插值变量和业务语义，并将 Workspace 的“导入 JD”调整为更符合页面动作的“新建面试规划”。
- 将简历卡片“打开”按钮改为使用当前主题主色，保持深海、梅子和自定义主题下的主操作视觉一致性，并补充 CSS 合同测试。
- 同步受文案调整影响的前端精确断言，完成文档 Header/INDEX/链接检查、根 `make test`、production build 与补丁空白检查。

### 关联 Commit

- `fix(frontend): humanize product copy and align resume action`

### 备注

- 本次变更均属于正式前端文案与视觉一致性范围，按一个 owner scope 原子提交。

## 12:07 工作记录

### 完成事项

- 原地修订并完成 `local-dev-stack/001-bootstrap` Phase 12，为同一 Compose 增加显式 full-container profile、前后端多阶段镜像、migration 前置及 `dev-container-*` 生命周期入口，默认端口锁定为 10800/10801。
- 保持默认 host-run `dev-up` 语义不变，补齐 scenario environment 文档、Skill 与静态 contract，并完成 root test/build/docs/context/Compose 全量回归。
- 实际部署当前工作树，doctor 6/6 OK；Chrome 使用标准 E2E.P0.101 synthetic 邮箱完成验证码登录、资料补全、简历/JD 解析、真实 AI 面试问答与报告生成，保存四张脱敏截图并完成交付复盘。

### 关联 Commit

- `feat(local-dev): add full-container runtime`

### 备注

Auto-committed by /tdd phase-commit, plan: local-dev-stack/001-bootstrap

## 13:57 工作记录

### 完成事项

- 原地完成 `backend-auth/001` Phase 11、`secrets-and-config/001` Phase 14 与 `local-dev-stack/001` Phase 13：支持通过 `EMAIL_PROVIDER=mailpit|smtp` 选择本地 Mailpit 或标准 SMTP，并实现 STARTTLS/隐式 TLS、用户名密码认证、TLS 1.2 下限和脱敏失败路径。
- full-container 环境只切 provider 即可使用容器内 Mailpit，外部 SMTP 保留用户 `.env` endpoint；启动前停止仓库 PID 文件管理的 host-run app，修复两个 backend runner 竞争进程内 delivery secret 的 BUG-0178。
- 按 MVP 决策锁定单 active backend 实例，不引入 Redis client 或共享 secret store；Mailpit 登录收码闭环、外部 SMTP provider 接收和用户真实收件箱可见性均通过。
- 根 `make test`（Python 563 tests / 4481 subtests、Go 全包、frontend 126 files / 1004 tests）、`make build`、`make lint-config`、docs/context/index/diff、Compose、scenario contract 与 doctor 6/6 OK 全部通过，并完成交付复盘。

### 关联 Commit

- `feat(auth): add production smtp delivery (BUG-0178)`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-auth/001-email-code-session-bootstrap Phase 11

## 15:03 工作记录

### 完成事项

- 原地完成 `backend-auth/001` Phase 12 与 `local-dev-stack/001` Phase 14：复用既有 Redis 保存 AES-GCM 加密的邮箱投递 secret，使用 SHA-256 namespaced key 和 5 分钟 TTL，使 producer/consumer 跨 backend 实例共享验证码；成功投递删除，失败保留给异步重试。
- 补齐 runtime Redis ping/fail-fast/close 与 producer/writer 注入，完成两个真实 Redis client integration、full-container Mailpit Chrome 登录/资料补全、外部 SMTP 一次成功投递、Redis 成功后零残留和 doctor 6/6 验证。
- 原地修复 `prompt-rubric-registry/004` owner marker 漂移：当前 28-case offline eval 与 Promptfoo 28/28 PASS 后恢复 report rubric/context-aware marker，并收紧 preflight 只接受显式 `marker=<name>`，拒绝失败说明或普通文本假阳性，记录 BUG-0179。
- 根 `make test`（Python 564 tests / 4481 subtests、Go 全包、frontend 126 files / 1004 tests）、`make build`、`make lint-config`、owner contexts、docs/index/diff gates全部通过；三个 owner 生命周期恢复 `completed`，交付复盘已完成。

### 关联 Commit

- `feat(auth): use Redis for email delivery secrets (BUG-0179)`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-auth/001-email-code-session-bootstrap Phase 12
