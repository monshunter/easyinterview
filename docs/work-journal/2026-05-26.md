# 2026-05-26 工作日志

## 20:51 工作记录

### 完成事项

- 完成 `e2e-scenarios-p0/002-manual-uat-real-provider-full-funnel` 的真实 provider manual UAT review/fix 收口。
- 补齐 Mailpit synthetic account、dev-stack real mode、真实 provider evidence、prompt/profile/schema/report 语义链路和 cleanup BUG 记录。
- 修复 dev CORS、OpenAI-compatible JSON response format、AI profile timeout、practice answer summary 持久化、report assessment drift 与 follow-up action 重复触发问题。
- 完成真实 provider post-fix UAT 证据、BUG-0105/BUG-0106、交付复盘和 plan/BDD lifecycle 同步。

### 关联 Commit

- `fix(e2e-scenarios): close real provider full funnel UAT (BUG-0105)`

### 备注

Auto-committed by /tdd phase-commit, plan: 002-manual-uat-real-provider-full-funnel

## 21:19 工作记录

### 完成事项

- 修复 `BUG-0106`：`DELETE /api/v1/me` 受理期同步软删 `users` 并撤销该用户全部 active session，`privacy_delete` runner 在 domain cleanup 成功后 hard delete 账户身份。
- 新增 `privacy_requests.user_id` tombstone migration，避免删除用户时级联删除隐私请求审计记录。
- 补齐 auth store、privacy runner、`cmd/api` runtime wiring、upload roundtrip cleanup 与 migration 回归测试。
- 同步 `backend-auth`、`backend-async-runner`、`db-migrations-baseline` spec/plan/checklist、BUG 知识库、交付复盘与索引。

### 关联 Commit

- `fix(backend-async-runner): close privacy account identity cleanup (BUG-0106)`

### 备注

已知非本次范围残余：`TestE2EP0050PracticeAssistantActionProvenanceAndTaskRuns` 当前仍在 practice task-run 数量 / feature key 断言失败。

## 22:44 工作记录

### 完成事项

- 修复 `BUG-0107`：OpenAI-compatible adapter 仅对 object output schema 下发 `json_object`，避免 `jd_match.search` / `jd_match.recommendation` 顶层 array schema 被真实 provider object mode 冲突。
- 修复 report assessment fallback 聚合，让 mixed dimension status 中 `needs_work` 支配 `meets_bar`，消除 Go map iteration 顺序影响。
- 修复 privacy delete completed tombstone retry 幂等性，`privacy_requests.user_id=NULL,status=completed` 的重试直接标记 async job 成功，不重跑 domain cleanup。
- 补齐 focused regression tests、`cmd/api` runtime wiring regression、`BUG-0107` 记录与交付复盘报告。

### 关联 Commit

- `fix(backend): close review correctness follow-ups (BUG-0107)`

### 备注

验证覆盖：provider adapter、review aggregation、privacy runner focused tests，相关 `internal` 包测试，`cmd/api` report / JD-Match / privacy runtime focused tests，`git diff --check` 与 `make docs-check`。

## 21:30 工作记录

### 完成事项

- 修复 `TestE2EP0050PracticeAssistantActionProvenanceAndTaskRuns` 在 BUG-0105 引入 answer_summary observation 后 ai_task_runs 行数与固定下标断言不匹配的问题（BUG-0108）。
- 更新 `scenarioPracticeAIClient` 的 `practice.turn.lightweight_observe` 成功响应，同时返回 `hint` 与 `answerSummary`，消除 parse-after-success `AI_OUTPUT_INVALID` failed row。
- 将 P0.050 的 task-run 验证改为按每个 HTTP trigger 增量断言：start-session 写 1 行 `question_generate`；第一次 `answer_submitted` 写 1 行 `hint_generate` answer_summary observation + 1 行 `followup_generate`；`hint_requested` 写 1 行 `hint_generate`；`turn_skipped` / `session_paused` 写 0 行；最终 `answer_submitted` 写 1 行 `hint_generate` answer_summary observation。
- 新增 `assertScenarioTaskRunDelta` 辅助函数，断言 typed columns（capability/featureKey/modelProfileName/status/validationStatus/featureFlag/dataSourceVersion/userID/resourceType/resourceID）。
- 同步 `backend-practice/003-mode-policies-and-provenance` 的 `bdd-plan.md`（v1.1→v1.2）与 `bdd-checklist.md`（v1.1→v1.2）。
- 建档 [BUG-0108](../bugs/BUG-0108.md) 与交付复盘报告。
- 验证通过：`cd backend && go test ./cmd/api -run '^TestE2EP0050PracticeAssistantActionProvenanceAndTaskRuns$' -count=1 -v` PASS；`cd backend && go test ./cmd/api -run 'TestE2EP0048|TestE2EP0049|TestE2EP0050|TestE2EP0051' -count=1 -v` PASS；scenario wrapper PASS；`git diff --check` PASS；`make docs-check` PASS。

### 关联 Commit

- `fix(backend-practice): align p0050 task-run gate (BUG-0108)`

### 备注

- BUG-0108 根因是 BUG-0105 的 answer_summary observation 新增了 ai_task_runs 行，但 P0.050 测试仍用固定全局行数与下标断言。修复后按 trigger 增量断言，避免再次被后续语义变动破坏。
