# 2026-04-27 工作日志

## 00:04 工作记录

### 完成事项

- 收口 `shared-conventions-codified/001-bootstrap` Phase 4 全部 4 项 verification：
  - 4.1：`make codegen-conventions` 跑两次后 `git status --short` 无新增文件；删除 `backend/internal/shared/types/enums.go` 后再跑 generator 文件被完整还原，最终 `git status` clean，`writeIfChanged` 比较内容仅在差异时写盘的 idempotency 行为得到 e2e 验证
  - 4.2：`go test ./backend/internal/shared/... -count=1 -v` 18 用例全部通过（idx 6 + idempotency 7 + errors 5）；`go vet ./backend/...` 无输出，覆盖 spec C-3（UUIDv7 / RequireServerID） + 错误响应结构 §3.2
  - 4.3：补齐 frontend 测试基建（`tsconfig.json` strict + `noUncheckedIndexedAccess`、devDeps `typescript@^5.6.0` / `vitest@^2.1.0` / `@types/node@^22.10.0`）；`pnpm install` 创建 `pnpm-lock.yaml`；`pnpm --filter @easyinterview/frontend exec tsc --noEmit` exit 0；`pnpm --filter @easyinterview/frontend test`（vitest run）→ 3 test files、24 tests 全部通过（ids 9 + idempotency 11 + enums 4）
  - 4.4：`docs/spec/INDEX.md` 中 `shared-conventions-codified` 行为真实链接（spec.md），与 spec Header `1.0 / active / 2026-04-26` 一致；本地 plans `INDEX.md` 仍把 `001-bootstrap` 列在「进行中」，由 `/tdd` Step 10 lifecycle sync 之后切到「已完成」；`sync-doc-index --check` 报告 `All documents are in sync. Zero drift detected.`，21 个 W2/W4/P1/P2 placeholder warnings 与 W0 spawn 后预期一致；`engineering-roadmap/001-decompose-subspecs/checklist.md` 自 W0 spawn 提交以来未被修改

### 关键命令输出（节选）

```
$ make codegen-conventions && git status --short
(无输出，working tree clean)
$ rm backend/internal/shared/types/enums.go && make codegen-conventions
(无输出)
$ ls backend/internal/shared/types/ → doc.go enums.go http_dto.go
$ git status --short → (无输出，文件已还原)
```

```
$ go test ./backend/internal/shared/... -count=1 -v
... (idx 6 + idempotency 7 + errors 5 = 18 PASS)
ok  github.com/monshunter/easyinterview/backend/internal/shared/errors  0.709s
ok  github.com/monshunter/easyinterview/backend/internal/shared/idx     0.362s
$ go vet ./backend/...
(exit 0)
```

```
$ pnpm --filter @easyinterview/frontend exec tsc --noEmit
(exit 0)
$ pnpm --filter @easyinterview/frontend test
✓ src/lib/conventions/enums.test.ts        (4 tests)
✓ src/lib/ids/index.test.ts                 (9 tests)
✓ src/lib/conventions/idempotency.test.ts  (11 tests)
Test Files  3 passed (3)
Tests       24 passed (24)
```

### 关联 Commit

- `test(shared-conventions): wire ts test runner and verify cross-language helpers`

### 备注

Auto-committed by /tdd phase-commit, plan: shared-conventions-codified/001-bootstrap

执行偏差记录：
- `tsconfig.json` 启用 `noUncheckedIndexedAccess` 后，`parseIdempotencyKey` 中的 `const [version, unixStr, uuid] = parts` 触发 TS 推断为 `string | undefined`，本次在长度校验后显式 `as [string, string, string]` 规避；后续若引入运行时 schema 校验（如 zod），可移除该 cast。
- frontend `lint` / `build` script 当前仍是占位符：`lint` 由根 `make lint` 通过 `scripts/lint/error_codes.py` 实施，`build` 等 D1 `frontend-shell` 落地。`typecheck` 与 `test` 已可运行，是本次新增的实际入口。