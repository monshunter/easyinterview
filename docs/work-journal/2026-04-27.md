# 2026-04-27 工作日志

## 00:04 工作记录

### 完成事项

- 收口 `shared-conventions-codified/001-bootstrap` Phase 4 全部 4 项 verification：
  - 4.1：`make codegen-conventions` 跑两次后 `git status --short` 无新增文件；删除 `backend/internal/shared/types/enums.go` 后再跑 generator 文件被完整还原，最终 `git status` clean，`writeIfChanged` 比较内容仅在差异时写盘的 idempotency 行为得到 e2e 验证
  - 4.2：`go test ./backend/internal/shared/... -count=1 -v` 18 用例全部通过（idx 6 + idempotency 7 + errors 5）；`go vet ./backend/...` 无输出，覆盖 spec C-3（UUIDv7 / RequireServerID） + 错误响应结构 §3.2
  - 4.3：补齐 frontend 测试基建（`tsconfig.json` strict + `noUncheckedIndexedAccess`、devDeps `typescript@^5.6.0` / `vitest@^2.1.0` / `@types/node@^22.10.0`）；`pnpm install` 创建 `pnpm-lock.yaml`；`pnpm --filter @easyinterview/frontend exec tsc --noEmit` exit 0；`pnpm --filter @easyinterview/frontend test`（vitest run）→ 3 test files、24 tests 全部通过（ids 9 + idempotency 11 + enums 4）
  - 4.4：`docs/spec/INDEX.md` 中 `shared-conventions-codified` 行为真实链接（spec.md），与 spec Header `1.0 / active / 2026-04-26` 一致；本地 plans `INDEX.md` 仍把 `001-bootstrap` 列在「进行中」，由 `/tdd` Step 10 lifecycle sync 之后切到「已完成」；`sync-doc-index --check` 报告 `All documents are in sync. Zero drift detected.`，21 个 W2/W4/P1/P2 placeholder warnings 与 W0 spawn 后预期一致；`engineering-roadmap/001-decompose-subspecs/checklist.md` 自 W0 spawn 提交以来未被修改

### 关键命令输出（节选）

```
$ make codegen-conventions && git status --short
(无输出，working tree clean)
$ rm backend/internal/shared/types/enums.go && make codegen-conventions
(无输出)
$ ls backend/internal/shared/types/ → doc.go enums.go http_dto.go
$ git status --short → (无输出，文件已还原)
```

```
$ go test ./backend/internal/shared/... -count=1 -v
... (idx 6 + idempotency 7 + errors 5 = 18 PASS)
ok  github.com/monshunter/easyinterview/backend/internal/shared/errors  0.709s
ok  github.com/monshunter/easyinterview/backend/internal/shared/idx     0.362s
$ go vet ./backend/...
(exit 0)
```

```
$ pnpm --filter @easyinterview/frontend exec tsc --noEmit
(exit 0)
$ pnpm --filter @easyinterview/frontend test
✓ src/lib/conventions/enums.test.ts        (4 tests)
✓ src/lib/ids/index.test.ts                 (9 tests)
✓ src/lib/conventions/idempotency.test.ts  (11 tests)
Test Files  3 passed (3)
Tests       24 passed (24)
```

### 关联 Commit

- `test(shared-conventions): wire ts test runner and verify cross-language helpers`

### 备注

Auto-committed by /tdd phase-commit, plan: shared-conventions-codified/001-bootstrap

执行偏差记录：
- `tsconfig.json` 启用 `noUncheckedIndexedAccess` 后，`parseIdempotencyKey` 中的 `const [version, unixStr, uuid] = parts` 触发 TS 推断为 `string | undefined`，本次在长度校验后显式 `as [string, string, string]` 规避；后续若引入运行时 schema 校验（如 zod），可移除该 cast。
- frontend `lint` / `build` script 当前仍是占位符：`lint` 由根 `make lint` 通过 `scripts/lint/error_codes.py` 实施，`build` 等 D1 `frontend-shell` 落地。`typecheck` 与 `test` 已可运行，是本次新增的实际入口。

## 00:08 工作记录

### 完成事项

- 关闭 `shared-conventions-codified/001-bootstrap` 生命周期：plan.md 与 checklist.md Header `状态` 由 `active` 切到 `completed`、`更新日期` 由 `2026-04-26` 切到 `2026-04-27`（11/11 checklist 项全部勾选完成日同日）
- 调用 `sync-doc-index --fix-index` 自动修复本地 plans `INDEX.md`：001-bootstrap 行从「进行中（Active）」分组迁移到「已完成（Completed）」分组（这正是 23:17 commit `feat(skills): auto-migrate plans/INDEX rows between status groups` 提供的能力，本次首次在 child plan 收尾流程中端到端使用）；新建「## 2 已完成（Completed）」段，更新「完成日期」字段
- 验证：`sync-doc-index --check` 报告 `All documents are in sync. Zero drift detected.`（21 个 W2/W4/P1/P2 placeholder warnings 与现状一致）；spec 维持 `1.0 / active` 不动（后续如新增 plan 仍可在 spec 上扩展）

### 关联 Commit

- `docs(shared-conventions): close 001-bootstrap plan lifecycle to completed`

### 备注

完成 `/implement shared-conventions-codified/plans/001-bootstrap` 全流程：4 个 Phase 共 11/11 checklist 项全部勾选；feature branch `feat/shared-conventions-codified-001-bootstrap-0426` 通过 4 次 phase-commit 自动 ff-merge 回父分支 `dev`（`docs(governance)` + 4 × `feat/test(shared-conventions)`）。spec.md 维持 `active 1.0`；engineering-roadmap parent checklist 自 W0 spawn 提交以来保持未修改。

## 00:09 工作记录

### 完成事项

- 落地 `shared-conventions-codified/001-bootstrap` 交付复盘：[`docs/reports/2026-04-27-shared-conventions-codified-001-bootstrap-assessment.md`](../reports/2026-04-27-shared-conventions-codified-001-bootstrap-assessment.md)
- 复盘识别 5 条材料化阻点，全部归类为 spec-plan：
  - plan §3 步骤顺序（1.1→1.2→2.1）违反真实依赖（generator 需先 module init），会话内已 `1.1 → 2.1 → 1.2` 自然重排（落点：B1 plan §3，优先级 medium）
  - plan §1.2 与 §2.2 关于 `APIError` 归属口径不一（generator 输出 vs. 手写）（落点：B1 plan §1.2 / §2.2，优先级 medium）
  - 「13 类枚举」描述与 generator 实际 14 个生成类型（§5.13 拆 type+status）不一致（落点：B1 spec §3 / plan §1.1，优先级 low — 已用 YAML 注释 + lint 兜底）
  - spec §6 C-4 双语 idempotency 验收无 plan checklist 对偶项，会话内主动补 Go 侧 helper（落点：B1 plan §2.4 / checklist，优先级 medium）
  - TS toolchain（typescript / tsconfig / vitest）落点偏后到 §4.3，使 §2.4 缺真实可执行的 Red gate（落点：B1 plan §2.3，优先级 low — 不影响交付正确性）
- 同步 `docs/reports/INDEX.md`：在 2026-04 月份分组顶部加入本次复盘条目
- 一次性执行偏差（gofmt 对齐、`Allxxxs` 复数命名修正、`noUncheckedIndexedAccess` 类型窄化、shell session cwd 偶发漂移）归类为执行细节，不构成流程缺陷，不提改进建议

### 关联 Commit

- `docs(reports): land shared-conventions-codified/001-bootstrap delivery retrospective`

### 备注

`/retrospective --this` 收口；后续动作以 plan §3 步骤排序原则 + APIError 归属说明 + checklist 跨语言对偶项三项打包提交优先级最高，其余两项可延后或已用脚本兜底。

## 00:15 工作记录

### 完成事项

- 处理 L2 code review 在 `/retrospective` 完成后追加的 3 项 remediation（plan / checklist 同步从 completed 重新开放为 active，按顺序补完后再切回 completed）：
  - 2.5：`frontend/src/lib/conventions/idempotency.ts` 在 `parseIdempotencyKey` 中加入 `^\d+$` 严格十进制校验 + `Number.isSafeInteger` 上界保护，拒绝指数 / 十六进制 / 前后空白 / `+` 前缀 / 空字符串等 JS `Number()` 默认接受但 Go `strconv.ParseInt(s, 10, 64)` 会拒的形态；新增 5 条 vitest 用例（hex / exponent / spaced / leading-plus / empty-timestamp），TS 端 28 用例全绿
  - 3.3：`scripts/lint/error_codes.py` 用 `TS_OBJECT_RE`（`ERROR_CODES = { ... } as const`）抓出对象 body 后逐条扫描，键正则放宽到 `[A-Za-z_][A-Za-z0-9_]*`，再显式断言 key/value 均为 `UPPER_SNAKE_CASE` 且 key == value；同时落地 `scripts/lint/error_codes_test.py`（unittest）3 条契约用例：clean、lowercase key、valid key + lowercase value，全绿；端到端验证小写 key `auth_unauthorized: 'AUTH_UNAUTHORIZED'` 现在被精确报告为 `UPPER_SNAKE_CASE` 与 `key != value` 两条违规
  - 4.5：仓库根新增 `go.work`（`go 1.24.0` + `use ./backend`），让 `go test ./backend/internal/shared/...` 与 `go vet ./backend/...` 直接在仓库根可用，不再依赖会话内手动 `cd backend`；spec D-2 / repo-scaffold A1 §3.2 `go.work 决策` 由 B1 承接的明文授权得到落地
  - 同步 plan.md §3.2.2 / §3.3 / §4.2 各加一条 L2 remediation 说明；checklist 新增 2.5 / 3.3 / 4.5 三项并全部勾选；plan + checklist Header `状态` 切回 `completed`、`更新日期` 维持 2026-04-27；plans/INDEX 不再 active 占位
  - 验证：`make lint` exit 0；`go test ./backend/internal/shared/... -count=1` 18/18 PASS；`go vet ./backend/...` 0 warning；`pnpm --filter @easyinterview/frontend test` 28/28 PASS（含 5 条新的非十进制时间戳 case）；`python3 -m unittest scripts.lint.error_codes_test` 3/3 OK；black-box 注入小写 key 现报告 `ERROR_CODES key 'auth_unauthorized' is not UPPER_SNAKE_CASE` 与 `key != value`

### 关联 Commit

- `fix(shared-conventions): tighten ts decimal parse, error-code lint, and add go.work`

### 备注

由 L2 code review 串行触发的 reopen→remediate→reclose 循环（AGENTS.md §4.4.7 命中已完成主题原地修订路径）。主分支视图：feature branch 多出一条 `fix(shared-conventions): ...` commit，ff-merge 回 `dev` 后保持线性历史。后续若再次触发 L2 review 应继续走同一路径，不再新建 sibling plan。

## 10:30 工作记录

### 完成事项

- 客观复核 `docs/reports/2026-04-27-shared-conventions-codified-001-bootstrap-assessment.md` 中 5 条 spec-plan 问题，确认均为真实文档漂移而非运行时代码缺陷。
- 原地修订 `shared-conventions-codified` spec / history / `001-bootstrap` plan / checklist / context：
  - 将 B1 spec 与 plan/checklist 升至 `1.1`，回写 generator 依赖顺序、Go/TS `APIError` 归属、13 个上游枚举小节 / 14 个生成类型口径、Go/TS idempotency 双端 checklist 落点、TS toolchain 前置落点。
  - 同步 `docs/spec/INDEX.md` 与 `docs/spec/shared-conventions-codified/plans/INDEX.md`，保持 Header / INDEX 投影一致。
- 验证：`make codegen-conventions` 后生成物无漂移；`make lint`、`go test ./backend/internal/shared/... -count=1`、`go vet ./backend/...`、`pnpm --filter @easyinterview/frontend exec tsc --noEmit`、`pnpm --filter @easyinterview/frontend test`、`sync-doc-index --check` 均通过。

### 关联 Commit

- `docs(shared-conventions): reconcile bootstrap assessment findings`

### 备注

按 `/work-journal` 手动拆分提交：本 commit 只包含 B1 spec/plan 文档修订与工作日志；`sync-doc-index` 工具修复单独提交。
