# 2026-05-11

## 09:58 工作记录

### 完成事项

- 完成 `frontend-shell/001-app-shell-auth-settings` 用户菜单与 dev mock auth state 原地修订；已完成 plan 在 1.9 / 2026-05-10 恢复 completed，spec 升到 1.12。
- TopBar 已登录用户区源级复刻 `ui-design/src/app.jsx`：头像 chip 触发 dropdown，含姓名 + 脱敏 email + 用户画像 / 设置与隐私 / 退出登录 三个 action、backdrop 与 Escape 关闭行为；旧 inline 三按钮通过负向断言固化为不可回流。
- `createDevMockClient()` 维护本地 session：默认 `getMe` 为 unauthenticated，`verifyAuthEmailChallenge` 成功后切换为 authenticated，`logout` 成功后回到 unauthenticated；`Prefer` 命名场景仍保留 fixture override 通道。
- `App.tsx` 把 runtime user 注入 TopBar，移除硬编码用户展示；i18nShell / localeRuntime / `p0-004-app-shell-language-switch` / AppAuthDispatch / TopBar 测试改为通过头像 chip 打开 dropdown 后断言菜单，i18n 文案校验跟随调整。
- 新增 `frontend-shell` D-9 dev mock session state invariant 与 C-10 登录态菜单退出闭环 acceptance；history 追加 1.12 行记录修订动机。
- 派生 `E2E.P0.032` scenario：覆盖默认非登录、登录成功后 dropdown 分流、profile/settings 路由、登出后回到非登录态闭环；`test/scenarios/e2e/INDEX.md` 追加 Ready 行。
- 记录 [BUG-0039](../bugs/BUG-0039.md) 与 Frontend Shell Auth State Parity 交付复盘。
- 验证：聚焦红测 `TopBar.test.tsx` / `devMockClient.test.ts` PASS；focused regression（TopBar + i18nShell + devMockClient + AppAuthDispatch + AppRuntimeProvider + P0.004 + P0.032）7 files / 24 tests PASS；`pnpm --filter @easyinterview/frontend test` 108 files / 681 tests PASS；`typecheck` / `build` / `make docs-check` / context validator / `git diff --check` 通过；Playwright `topbar.spec.ts` / `layout.spec.ts` / `screens.spec.ts` 40 tests PASS；`E2E.P0.032` setup→trigger→verify→cleanup PASS。

### 关联 Commit

- `fix(frontend-shell): restore auth state and user menu parity`

### 备注

手动 `/work-journal` 拆分两个 commit 的第一个：仅承载 auth / 用户菜单源级复刻 + stateful dev mock + 新增 P0.032 + plan 001 原地修订 + BUG-0039 + 复盘。workspace pixel gate 漂移（plan 003）作为相邻测试债务，由下一个 commit 处理；本 commit 维持 `frontend-shell` spec / history / plans INDEX 仅含 1.12 一档。

## 10:01 工作记录

### 完成事项

- 完成 `frontend-shell/003-ui-design-pixel-parity-gate` 在原 owner plan 上的修订：常规 clean checkout pixel gate 不再依赖 `.gitignore` 排除的本地 screenshot baseline，workspace full-state 也不再依赖 Home recent card 的 `resume-unbound` synthetic path；plan 升级到 1.2 并恢复 completed，spec 升到 1.13。
- `frontend/tests/pixel-parity/screenshot.spec.ts` 改为 DOM anchor + computed style + bounding box + 非空 screenshot buffer smoke，避免缺失 `*-darwin.png` baseline 导致 clean checkout 首跑失败。
- `frontend/tests/pixel-parity/workspace.spec.ts` 通过测试注入的初始 route 携带 server-bound `targetJobId` / `resumeVersionId` / `planId` 进入 hydrated workspace；`frontend/src/main.tsx` 暴露受控 initial-route 注入入口，不修改 Home recent card 的 `resume-unbound` 业务语义。
- 更新 `frontend/README.md`：固化"ignored screenshot baseline 不得作为常规 clean checkout pixel gate""workspace full-state pixel tests 必须用 server-bound route params"的约束，并指明 `interviewContextFromTargetJob` 的 synthetic id 防线。
- 升级 `test/scenarios/e2e/p0-006-ui-design-pixel-parity-gate/`：README + expected-outcome + verify.sh 对齐当前 Playwright `--list` 实际口径 8 spec / 110 tests，旧 7 spec / 68 tests 文案不再回流。
- 修订 `frontend-shell` C-9 acceptance：覆盖范围扩展为 TopBar / auth / profile / settings / placeholder / home / parse / jd_match / workspace；新增 "workspace 必须 server-bound route 进入" 与 "常规 gate 不得依赖 ignored baseline" 两个不变量；history 追加 1.13 行记录修订动机。
- 记录 [BUG-0040](../bugs/BUG-0040.md) 与 Frontend Shell Workspace Pixel Gate 交付复盘。
- 验证（继承自 plan 003 修订证据）：聚焦 `screenshot.spec.ts + workspace.spec.ts` Red 12 failed / 24 passed → Green 36 passed；完整 `pnpm --filter @easyinterview/frontend test:pixel-parity` 110 passed；E2E.P0.006 setup→trigger→verify→cleanup PASS；`pnpm --filter @easyinterview/frontend test` 108 files / 681 tests PASS；`typecheck` / `build` / `make docs-check` / context validator / `git diff --check` 通过。

### 关联 Commit

- `fix(frontend-shell): harden workspace pixel parity gate`

### 备注

手动 `/work-journal` 拆分两个 commit 的第二个：仅承载 workspace / screenshot pixel gate 修复 + plan 003 原地修订 + BUG-0040 + 复盘 + P0.006 verify 升级。auth / 用户菜单源级复刻已在前一个 commit `fix(frontend-shell): restore auth state and user menu parity` 单独闭环；本 commit 把 `frontend-shell` spec / history / plans INDEX 升到 1.13。后续如需为 "常规 pixel gate 不允许 toHaveScreenshot" 或 "P0.006 suite manifest 自动校验" 增加静态 lint，可作为独立 follow-up 提案，不属于本次范围。

## 10:42 工作记录

### 完成事项

- 完成 `frontend-shell/001-app-shell-auth-settings` Phase 6 登录态与 `ui-design` parity 剩余空洞修复：authenticated user menu 纳入真实浏览器 Playwright gate，覆盖 login → avatar chip → dropdown → logout flow。
- 修复 mobile 已登录头像菜单从 viewport 左侧溢出的问题：移动端 `.ei-topbar-user-menu` 从 chip 左侧展开，desktop 仍保持右对齐。
- `frontend/tests/pixel-parity/topbar.spec.ts` 增加 fixture-backed mocked Auth API 状态流，断言 `ui-design/src/app.jsx` 源码字面量、dropdown header / profile / settings / logout、desktop/mobile bounding box、viewport containment、Escape 关闭和 logout 后非登录态。
- `E2E.P0.006` 从 110 tests 更新为 112 tests；同步 `frontend/README.md`、P0.006 README / expected outcome / verify.sh、frontend-shell spec/history、001/003 owner plan/checklist/bdd 文档。
- `001-app-shell-auth-settings` 补齐 Phase 6 operation matrix，覆盖 `getRuntimeConfig`、`getMe`、`startAuthEmailChallenge`、`verifyAuthEmailChallenge`、`logout` 的 fixture、frontend consumer、backend handler、persistence、AI dependency 与 scenario coverage。
- 记录 [BUG-0041](../bugs/BUG-0041.md) 与 Frontend Shell Auth Menu browser parity 修复事实。
- 验证：focused TopBar Playwright 22 tests PASS；完整 `test:pixel-parity` 112 passed；P0.006 setup→trigger→verify→cleanup PASS；P0.032 setup→trigger→verify→cleanup PASS；`pnpm --filter @easyinterview/frontend test` 108 files / 681 tests PASS；`pnpm --filter @easyinterview/frontend build`、两个 context validator、`make docs-check`、`git diff --check` 通过。

### 关联 Commit

- `fix(frontend-shell): close auth menu browser parity gap`

### 备注

See [BUG-0041](../bugs/BUG-0041.md). 本提交承接上一次 workspace pixel gate 修订后的下一步，专门闭环 Phase 6 登录态菜单在真实浏览器层的剩余 parity 空洞；没有修改 `ui-design/` 真理源。

## 11:32 工作记录

### 完成事项

- 完成 `/design` skill 分支门禁补强：新增 Step 2.5，要求用户确认 Brief 后、调用 `/create-doc` 与写入 spec / plan / checklist / BDD / context / INDEX 文件前先完成 branch guard。
- 明确 `/design` 不得在默认父分支上创建或修订 `docs/` 文件；父分支必须 fast-forward-only 更新成功后才能创建 `codex/design-{subject}` 类 feature branch，失败时停止并报告 stale parent branch blocker。
- 同步 `AGENTS.md` Git 分支策略，把 `/design` 生成 spec/plan 文档显式纳入"写文件入口 skill"的分支前置门禁范围。
- 新增 `/design` contract test，防止未来移除 branch guard、`/create-doc` 前置顺序、fast-forward-only 和 stale parent branch 禁止项。
- 验证：`python3 -m pytest .agent-skills/design/scripts/test_design_skill_contract.py .agent-skills/change-intake/scripts/test_change_intake_skill_contract.py` 15 passed；`python3 .agent-skills/skill-creator/scripts/quick_validate.py .agent-skills/design` 通过；`make docs-check` zero drift；`git diff --check` 通过。

### 关联 Commit

- `fix(design): require branch guard before document mutation`

### 备注

本次修复复用已有 BUG-0035 / Bug 模式 2"入口 Skill 在分支门禁前修改默认父分支"的治理口径，没有新增 Bug 记录。额外宽检查 `python3 -m pytest .agent-skills/design/scripts/test_design_skill_contract.py .agent-skills/change-intake/scripts/test_change_intake_skill_contract.py .agent-skills/init-docs/scripts/test_cross_reference_consistency.py` 中，`test/scenarios/e2e/README.md` 缺少"手动引导"小节导致 1 个既有断言失败，和本次 `/design` 改动无关。

## 18:13 工作记录

### 完成事项

- 通过 `/design` 结晶落地 Resume Workshop 3 个新 subspec：`backend-upload` v1.0、`backend-resume` v1.0、`frontend-resume-workshop` v1.0，含 spec.md / history.md / plans INDEX 骨架。
- 派生 3 个跨层 plan 并在对应 subspec 内就位：`db-migrations-baseline/002-flat-resume-migration`（DB 迁移承接 flat Resume schema）、`event-and-outbox-contract/002-resume-tailor-mode-drift-fix`（outbox 事件契约升级）、`openapi-v1-contract/004-resume-additive-coverage`（OpenAPI contract 新增 resume tag / schema / operations）。
- shared-conventions-codified 从 v1.15 → v1.16：新增 D-10 Resume Workshop additive vocabulary（`ResumeVersionType` / `ResumeSeedStrategy` / `ResumeTailorSuggestionStatus` 枚举 + `RESUME_EXPORT_NOT_AVAILABLE` 错误码），枚举类型 14 → 17（声明阶段）。
- db-migrations-baseline 从 v1.13 → v1.14：新增 D-17 Resume Workshop additive 表与字段声明（baseline 表 26 → 28，privacy deletion matrix 新增 2 行）。
- engineering-roadmap 从 v3.9 → v3.11：Resume Workshop 行状态从"未创建"更新为 active，并记录首批 plan 就位状态。
- openapi-v1-contract 从 v1.15 → v1.16：登记 004-resume-additive-coverage plan。
- event-and-outbox-contract 从 v2.2 → v2.3：登记 002-resume-tailor-mode-drift-fix plan。
- mock-contract-suite 从 v1.4 → v1.5：同步 spec 版本。
- 更新 `docs/spec/INDEX.md`：新增 backend-upload / backend-resume / frontend-resume-workshop 行，同步所有受影响的 spec 版本号与日期。

### 关联 Commit

- `docs(spec): add resume workstream subspecs and cross-layer additive plans`

### 备注

`/design` 在 feature branch `codex/design-resume-workstream` 上执行，branch guard 已前置通过。3 个新 spec 仅完成 plan 骨架声明阶段，具体实施由 `/implement` 按 plan checklist 顺序驱动。
