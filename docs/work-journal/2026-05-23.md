# 2026-05-23 工作日志

## 00:45 工作记录

### 完成事项

- 完成 real backend handoff gate 加固与 remaining frontend owner drift 扫描闭环。
- 加固 `.agent-skills/plan-code-review`：新增 frontend-first handoff 真实后端扫描、scheduler fairness / starvation 证据、migration-only escape hatch 反查规则，并补充 skill contract tests。
- 新增 `frontend/src/api/frontendOwners.realApiMode.test.ts`，在 `VITE_EI_API_MODE=real` 下覆盖 workspace/practice、report、resume、debrief 四类 frontend owner 的 generated-client base URL、cookie credentials、`Prefer` header 负向、side-effect idempotency key、provenance / response 字段与隐私 URL 断言。
- 新增 `test/scenarios/_shared/scripts/frontend-real-backend-gate.sh` 与 `frontend-real-backend-verify.sh`，并接入 P0.018-P0.021、P0.044-P0.047、P0.056-P0.059、P0.065-P0.069、P0.081-P0.087 的 trigger / verify wrapper。
- 修复 P0.044 / P0.047 旧 voice negative gate：不再 repo-wide 拒绝 `createPracticeVoiceTurn`，改为只允许 `usePracticeVoiceTurn.ts` owner，并继续拒绝 practice runtime 中的 report consumer 泄漏。
- 同步 frontend-workspace-and-practice、frontend-report-dashboard、frontend-resume-workshop、frontend-debrief 的 spec / plan / checklist / BDD / test-plan / history / INDEX，把 completed plan 的 operation matrix 与真实 backend owner gate 更新为当前事实。
- 归档 [BUG-0089](../bugs/BUG-0089.md)，同步 Bug INDEX / PATTERNS，并创建本次交付复盘报告。

### 关联 Commit

- `fix(frontend): close real backend handoff gate drift (BUG-0089)`

### 备注

- 验证通过：`sync-doc-index --check`、`plan-code-review` skill quick validation + pytest、frontend real-mode Vitest、scenario helper `bash -n`、代表性 P0.018 / P0.044 / P0.047 / P0.056 / P0.065 / P0.081 trigger+verify、`make docs-check`、`git diff --check`。
- P0.056 / P0.065 仍有既有 React `act(...)` warning；本次变更未放宽 gate，warning 不影响通过状态。

## 01:32 工作记录

### 完成事项

- 执行 `/scenario-run` 全量目标清单，覆盖 P0.018-P0.021、P0.044-P0.047、P0.056-P0.059、P0.065-P0.069、P0.081-P0.087。
- 首轮 run `scenario-run-20260522T172049Z` 暴露 P0.066 / P0.067 wrapper widened-scope 失败，以及 P0.087 hash route pixel harness 失败。
- 修复 P0.065-P0.069 debrief wrapper：统一改为 `pnpm --filter @easyinterview/frontend exec vitest run --reporter=verbose ...`，并在 P0.066 / P0.068 verify 中补关键 test file marker。
- 修复 P0.087 Playwright parity route：将 `/?pixelRoute=N#route=...` 改为 bare `/#route=...&pixelRoute=N`，避免 canonical search 抢先解析并吞掉 hash adapter。
- 归档 [BUG-0090](../bugs/BUG-0090.md)，更新 Bug pattern 与交付复盘报告。

### 关联 Commit

- `fix(test): close frontend owner scenario full-run gates (BUG-0090)`

### 备注

- 验证通过：`bash -n` 修改脚本，`scenario-run-fixcheck-20260522T172739Z` 受影响 6 场景 PASS，`scenario-run-full-20260522T172858Z` 全量 24 场景 PASS，`sync-doc-index --check`、`make docs-check`、`git diff --check` PASS。

## 14:23 工作记录

### 完成事项

- 落地「方案 A」本地开发与测试环境简化：把 Kubernetes / Kind / Helm 从当前 P0 默认测试与部署前提中下线，统一为「Docker Compose 外部依赖 + 宿主机 backend/frontend dev runtime + repo-tracked 本地 scenario runner」口径。
- 同步治理与根级契约：`AGENTS.md` §2.1.3/§5、`README.md`、根 `Makefile` `dev-up` 描述、`docs/development.md` §2 前后端契约工作流、`deploy/README.md`、`deploy/dev-stack/.env.example` AI provider 注释、`test/` 与 `test/scenarios/` 各级 README、`.agent-skills/init-docs/templates/scenarios-*` 模板。
- 同步受影响 spec/plan 措辞与版本：`engineering-roadmap`(3.21, ADR-Q4/ADR-Q6 部署目标重定)、`repo-scaffold`(1.4 `deploy/` 边界)、`ai-provider-and-model-routing`(2.12 fail-fast 边界)、`secrets-and-config`(2.6 K8s Secret 去默认)、以及 backend-practice/backend-review/backend-targetjob/backend-resume/frontend-report-dashboard 计划中的 Kind / provider 边界 BDD 措辞。
- 投影 `docs/spec/INDEX.md` 中 A 主题 subject 的 Header 版本。

### 关联 Commit

- `docs(local-dev-env): de-default kind/k8s/helm from p0 test and deploy targets`

### 备注

本批改动按主题拆分为 3 个 commit（方案 A 文档 / 原 `BUG-0087` Postgres 卷守卫 / 原 `BUG-0088` 仓库 lint 契约门禁）。合并 `main` 后 bug record 编号分别迁移为 `BUG-0091` / `BUG-0092`；少数被多主题协同编辑的共享 spec（如 `local-dev-stack/*`、`practice-voice-mvp/*`）整文件归并到其 bug owner commit，避免伪造 spec 中间版本。

## 14:24 工作记录

### 完成事项

- 修复原 `BUG-0087`（合并 `main` 后记录迁移为 [BUG-0091](../bugs/BUG-0091.md)）：Postgres 18 官方镜像把默认 PGDATA 调整为 major-version-specific 路径 `/var/lib/postgresql/18/docker`，本地 dev stack 仍沿用旧挂载点导致容器反复 unhealthy。
- `deploy/dev-stack/docker-compose.yaml` 将 `easyinterview-pg-data` 改挂到 `/var/lib/postgresql`，由镜像管理 `/18/docker` 子目录。
- 新增 `deploy/dev-stack/scripts/check-postgres-volume-layout.sh` 只读 preflight，并在 `deploy/dev-stack/Makefile` 的 `up` 前置 `_check_postgres_volume`：检测到旧 `/var/lib/postgresql/data` 或半初始化卷时给出明确 reset 指引而非自动删卷。
- 同步 `deploy/dev-stack/README.md` 卷布局与排障说明、`local-dev-stack` spec/plan/checklist/context（1.12）与 plans/INDEX 投影。
- 在 `.agent-skills/plan-code-review/SKILL.md` 固化 Docker Compose / dev-infra L2 runtime gate（镜像大版本、命名卷、entrypoint、UID、挂载路径、healthcheck、持久化布局变更必须跑干净卷 runtime 验证，且不得把自动删卷当作可接受的 L2 修复），并补 `test_plan_code_review_skill.py` 断言。
- 建档 `BUG-0091.md` 与交付复盘报告，更新 bugs/reports INDEX。

### 关联 Commit

- `fix(local-dev-stack): repair Postgres 18 dev volume guard (BUG-0087)`

### 备注

按用户确认「直接提交，信任前序会话验证」，本条未重跑 docker 冷启动验证，运行时证据见 BUG-0091 与对应交付复盘报告。

## 14:25 工作记录

### 完成事项

- 修复原 `BUG-0088`（合并 `main` 后记录迁移为 [BUG-0092](../bugs/BUG-0092.md)）：修复 Postgres 后 `make lint` 串行暴露多个与当前 owner 契约漂移的仓库级 lint 门禁。
- `scripts/lint/backend_practice_legacy.py`：收窄 non-current standalone voice route 正则，继续拦截独立 `/voice` route / alias，但放行 practice-voice-mvp 拥有的 `createPracticeVoiceTurn` / `/voice-turns` / `voice-turn://` / `practice.voice.*`；补 `backend_practice_legacy_test.py` 正向 fixture。
- `scripts/lint/rubric_lint.py` + `config/rubrics/README.md`：登记 JD Match 6 个业务维度（`relevance_to_profile` / `risk_clarity` / `actionability` / `query_alignment` / `diversity` / `privacy_compliance`），保留 unknown dimension 负向 fixture。
- `scripts/lint/mock_runtime_boundary.py`：把 non-current-token broad substring 改为锚定正则，避免误杀 practice voice 合法 fixture / generated artifact。
- 修复 Go revive 命名阻断：`backend/internal/profile/*` 的 `UiLanguage`→`UILanguage`、`backend/cmd/api/jdmatch_runtime.go`、`backend/internal/practice/append_session_event_service.go` 的 initialism 命名。
- 同步 owner 文档旧 runtime topology / voice token 措辞：`backend-async-runner`、`mock-contract-suite`、`practice-voice-mvp`、`backend-jobs-recommendations`、`backend-resume` 计划措辞。
- 建档 `BUG-0092.md` 与交付复盘报告，更新 bugs/reports/spec INDEX 投影。

### 关联 Commit

- `fix(repo-lint): align current contract gates (BUG-0088)`

### 备注

按用户确认「直接提交，信任前序会话验证」，本条未重跑 `make lint`；门禁通过证据见 BUG-0092 与对应交付复盘报告。

## 15:10 工作记录

### 完成事项

- Review follow-up：把 `/plan-code-review` 与 `/tdd` 的本地集成 / 场景 L2 runtime gate 措辞从历史的 “Docker Compose vs Kind / Kind deployment entrypoint / Kind scenario target” 收敛为当前真理源口径，与本分支 `docs(local-dev-env): de-default kind/k8s/helm` 主题对齐。
- `.agent-skills/plan-code-review/SKILL.md`：local integration/scenario review 改为检查 “Docker Compose external dependencies、host-run app commands、repo-tracked local scenario runner boundaries”；runtime lifecycle 反查入口改为 “Docker Compose service、host-run dev command、local scenario runner entrypoint”，不再默认 Kind deployment entrypoint。
- `.agent-skills/tdd/SKILL.md`：契约预读第 5 项改为区分 Docker Compose external dependencies、host-run app commands、repo-tracked local scenario runners，去掉 Kind scenario target 默认假设。
- `.agent-skills/plan-code-review/scripts/test_plan_code_review_skill.py`：新增 `test_local_integration_reviews_current_runner_boundaries`，正向断言新措辞存在，负向断言 “Kind scenario target”“Docker Compose vs Kind” 不再出现。

### 关联 Commit

- `docs(agent-skills): de-default kind from local integration review gates`

### 备注

skills 契约措辞调整，与场景测试当前真理源（`test/scenarios/README.md` 已去默认 Kind/K8s/Helm）保持一致。

## 15:11 工作记录

### 完成事项

- Review follow-up（[BUG-0091](../bugs/BUG-0091.md)）：Postgres 18 卷布局 preflight 守卫原本临时拉取 `alpine:3.20` 运行只读检查脚本，会在已有卷的机器上引入一个未在 compose 中声明、也未被 `make dev-pull` 覆盖的额外镜像拉取依赖。
- `deploy/dev-stack/scripts/check-postgres-volume-layout.sh`：guard 容器改为复用 `${POSTGRES_VOLUME_GUARD_IMAGE:-postgres:18-alpine}`，该镜像已在 compose 中锁定并由 `make dev-pull` 预拉；同时把 fail 提示中的硬编码卷名改为 `${VOLUME_NAME}`，与 `POSTGRES_VOLUME_NAME` 覆盖保持一致。
- 同步证据：`BUG-0091.md` 修复方案 / 验证记录与交付复盘报告补充 guard 镜像复用说明，记录 `bash -n` 正负例与 `make -C deploy/dev-stack -n pull` 仍只预拉锁定依赖镜像。

### 关联 Commit

- `fix(local-dev-stack): reuse pinned postgres image for volume guard (BUG-0091)`

### 备注

按用户确认信任前序会话验证，本条未重跑 docker 冷启动；guard 脚本语义与镜像复用证据见 BUG-0091 与对应交付复盘报告。

## 15:12 工作记录

### 完成事项

- Review correction（[BUG-0092](../bugs/BUG-0092.md)）：对照 merge base 后确认 JD-Match F3 六个 rubric 维度（`relevance_to_profile` / `risk_clarity` / `actionability` / `query_alignment` / `diversity` / `privacy_compliance`）原本已在 allowlist 中；上一轮 repo lint follow-up 重复添加了同一 stanza，并把它误记为 `lint-rubrics` 行为缺口。
- `scripts/lint/rubric_lint.py` + `config/rubrics/README.md`：移除重复 no-op 的 “JD Match family” allowlist / README 块，保留原有 “JD-Match family” 条目与 unknown dimension 负向 fixture；验证 `python3 -m pytest scripts/lint/rubric_lint_test.py -q` 5 passed、`make lint-rubrics` 26 files clean。
- 校正证据记录：`BUG-0092.md`（标题 / 假设 2 / 根因 / 修复方案 / 验证 / 经验教训）与 `docs/bugs/INDEX.md` 标题，把 “rubric allowlist 漂移修复” 改述为 “证据漂移校正 / 重复 stanza 移除”。
- 同步 owner 文档：`backend-jobs-recommendations/history.md`（新增 1.9 correction 记录并下修 1.8 表述）、`001-jd-match-real-backend-baseline` plan.md / checklist.md（Phase 8 改为 evidence correction，版本 1.5）、plans/INDEX.md 投影对齐。

### 关联 Commit

- `fix(repo-lint): drop duplicate rubric allowlist stanza and correct evidence (BUG-0092)`

### 备注

本条为 review 证据校正：实际 lint 行为未改变，仅移除重复 no-op 并校正 BUG / report / owner plan 记录。`rubric_lint_test.py`、`make lint-rubrics`、`sync-doc-index --check` 均通过。

## 15:40 工作记录

### 完成事项

- 修复 [BUG-0093](../bugs/BUG-0093.md)：补齐 9 个 Ready e2e 场景缺失的 `data/seed-input.md` 与 `data/expected-outcome.md`，覆盖 `E2E.P0.014`-`E2E.P0.017` 和 `E2E.P0.065`-`E2E.P0.069`。
- 新增每个场景的输入资产与期望结果资产，固定 fixture / real backend overlay / privacy negative / legacy negative / pixel parity 等关键验证边界。
- 串行执行 targeted `/scenario-run`：`scenario-run-20260523T073633Z` 中 9 个场景全部 PASS，`passed=9`、`failed=0`、`errored=0`。
- 建档 `BUG-0093.md` 与交付复盘报告，更新 Bug INDEX、reports INDEX，并把 Ready/Verified 场景 data 资产完整性检查补入 `docs/bugs/PATTERNS.md` 模式 4。

### 关联 Commit

- `fix(test): add missing scenario data assets (BUG-0093)`

### 备注

- 验证通过：场景结构检查 `scenario_dirs=87` / `missing_files=0`，新增资产空白检查 `new_asset_files=18` / `new_asset_whitespace=PASS`，350 个 scenario shell 脚本 `bash -n` PASS，`make docs-check` PASS，`make dev-doctor` PASS，`git diff --check` PASS。
- `bash` 输出仍有本机 `LC_ALL=C.UTF-8` warning；退出码为 0，未影响本次 scenario 结果。

## 18:43 工作记录

### 完成事项

- 执行 `plan-review 002-output-schema-contract --fix`，将 prompt-rubric-registry 002 计划从手写 prompt 字段表调整为 schema-first 输出契约方案。
- 同步 `prompt-rubric-registry` spec / history / plans INDEX：将 `output_schema` 升级为 v2.5 D-13 锁定契约，明确 `description` 仅作为 schema 注解，`response_format` 仍归 A3 后续。
- 修订 `002-output-schema-contract` plan / checklist / context：版本升至 1.1，新增 schema-rendered prompt contract、example JSON schema-valid、prompt contract drift negative fixture、alias / optional 字段治理与 Phase 7 收口门禁。

### 关联 Commit

- `docs(prompt-rubric): align output schema contract plan`

### 备注

验证通过：`validate_context.py --target backend`、`sync-doc-index --check`、`make docs-check`、`make lint-prompts`、`git diff --check`。

## 23:45 工作记录

### 完成事项

- 实施并闭环 `prompt-rubric-registry/002-output-schema-contract`：为 13 个 chat feature_key 落地语言无关 `v0.1.0.schema.json`，将 26 个 prompt body 输出段统一为 schema-rendered contract block，并同步 YAML `template_hash` 与 seed migration。
- 扩展 `prompt_lint` schema / renderer gate：覆盖 schema 子集、description、required path、feature contract、prompt contract drift、example JSON schema-valid 和 negative fixtures。
- 接通 registry loader/resolver `OutputSchema`，扩展 A3 `validateOutputSchema` enum fail-close，并把 targetjob、resume/jobs、debrief、review、practice chat、jdmatch 的 caller metadata 透传到 `CallMetadata.OutputSchema`；语音 STT/TTS 路径保持跳过。
- 修复本地 `gitleaks` 二层扫描误扫 `.gitignore` 下运行时 `.env` 的可重复性问题：扫描 git tracked + unignored candidate mirror，避免读取本地真实 provider secret。
- 将 002 plan / checklist Header 切到 `completed`，同步 `plans/INDEX.md`，并在 plan §8 写入 Phase 0-7/C-12 证据。
- 建档 [BUG-0094](../bugs/BUG-0094.md)，更新 Bug 模式库与交付复盘报告。

### 关联 Commit

- `feat(prompt-rubric): close output schema contract (BUG-0094)`

### 备注

- 验证通过：`python3 -m pytest scripts/lint/prompt_lint_test.py -q`、`make lint-prompts`、`make lint`、`go test ./backend/internal/ai/registry/... ./backend/internal/ai/aiclient/... ./backend/internal/targetjob/... ./backend/internal/resume/jobs/... ./backend/internal/review/... ./backend/internal/practice/... ./backend/internal/debrief/... ./backend/internal/jdmatch/... -race`、`validate_context.py --target backend`、`sync-doc-index --check`、`make docs-check`、`git diff --check`。
- 红线通过：语音 feature_key 无 schema；`backend/internal` 无 `response_format` / `json_schema` provider request 字段。
