# 2026-05-23 工作记录

## 14:23 工作记录

### 完成事项

- 落地「方案 A」本地开发与测试环境简化：把 Kubernetes / Kind / Helm 从当前 P0 默认测试与部署前提中下线，统一为「Docker Compose 外部依赖 + 宿主机 backend/frontend dev runtime + repo-tracked 本地 scenario runner」口径。
- 同步治理与根级契约：`AGENTS.md` §2.1.3/§5、`README.md`、根 `Makefile` `dev-up` 描述、`docs/development.md` §2 前后端契约工作流、`deploy/README.md`、`deploy/dev-stack/.env.example` AI provider 注释、`test/` 与 `test/scenarios/` 各级 README、`.agent-skills/init-docs/templates/scenarios-*` 模板。
- 同步受影响 spec/plan 措辞与版本：`engineering-roadmap`(3.21, ADR-Q4/ADR-Q6 部署目标重定)、`repo-scaffold`(1.4 `deploy/` 边界)、`ai-provider-and-model-routing`(2.12 fail-fast 边界)、`secrets-and-config`(2.6 K8s Secret 去默认)、以及 backend-practice/backend-review/backend-targetjob/backend-resume/frontend-report-dashboard 计划中的 Kind / provider 边界 BDD 措辞。
- 投影 `docs/spec/INDEX.md` 中 A 主题 subject 的 Header 版本。

### 关联 Commit

- `docs(local-dev-env): de-default kind/k8s/helm from p0 test and deploy targets`

### 备注

本批改动按主题拆分为 3 个 commit（方案 A 文档 / BUG-0087 Postgres 卷守卫 / BUG-0088 仓库 lint 契约门禁）。少数被多主题协同编辑的共享 spec（如 `local-dev-stack/*`、`practice-voice-mvp/*`）整文件归并到其 bug owner commit，避免伪造 spec 中间版本。
