# 2026-07-06 工作日志

## 11:34 工作记录

### 完成事项

- 修复 Home 新建模拟面试规划入口：删除冗余 hero sub，主按钮改为“立即面试”，并在 Home 选择已有 ready 简历后才允许提交 JD import。
- 将 Home import 成功路由扩展为携带真实 `resumeId`，并让 Parse 继承有效 route `resumeId`；缺失或无效时继续保持显式选择阻断。
- 同步 `ui-design/`、`docs/ui-design/`、owner spec / plan / checklist / BDD、P0.014/P0.015/P0.016 场景脚本、BUG-0131 和交付复盘报告。
- 验证通过：focused Home/Parse Vitest、P0.014/P0.015/P0.016 trigger + verify、UI contract、Home pixel parity、`sync-doc-index --check`、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(frontend-home): prebind resume for immediate interview (BUG-0131)`

### 备注

See [BUG-0131](../bugs/BUG-0131.md).

## 12:03 工作记录

### 完成事项

- 修复 Home “选择已有简历”控件形态：从平铺简历按钮列表改为 native dropdown / combobox，并保留 ready 简历显式选择后才能提交 JD import 的门禁。
- 修复 Home “最近模拟面试”信息架构：按 `updatedAt desc` 只展示最近 3 张卡片，超过 3 条时显示“更多”并跳转到 `workspace` 模拟面试列表页。
- 同步 `ui-design/`、`docs/ui-design/`、owner spec / plan / checklist / BDD、P0.014/P0.015 场景文档、BUG-0132 和交付复盘报告。
- 验证通过：focused Home Vitest、i18n、UI contract、frontend typecheck/build、Home pixel parity、P0.014/P0.015 setup/trigger/verify/cleanup、`sync-doc-index --check`、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(frontend-home): use resume dropdown and cap recent mocks (BUG-0132)`

### 备注

See [BUG-0132](../bugs/BUG-0132.md).

## 16:23 工作记录

### 完成事项

- 修复 Home 新建规划入口布局：将粘贴 JD 和上传文件拆成两个 source panel，上传/URL 不再作为 textarea 卡片底部辅助链接。
- 收敛简历选择行：`选择已有简历` 下拉框改为适度宽度，并让 `还没有简历？1 分钟创建 →` 位于下拉框右侧同一行。
- 调整 `立即面试` 主按钮归属：按钮移到简历选择状态下方，不再停留在 JD textarea 卡片右下角。
- 同步 `ui-design/`、`docs/ui-design/`、owner spec / plan / checklist / BDD、P0.014/P0.015 场景文档、BUG-0133 和交付复盘报告。
- 验证通过：focused Home Vitest、i18n、UI contract、frontend typecheck/build、Home pixel parity、P0.014/P0.015 setup/trigger/verify/cleanup、`sync-doc-index --check`、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(frontend-home): separate source and submit layout (BUG-0133)`

### 备注

See [BUG-0133](../bugs/BUG-0133.md).

## 16:51 工作记录

### 完成事项

- 修复 Home 新建规划入口的 JD source 布局回归：上传 JD 文件与 URL 导入回收到 `home-jd-input-card` 底部 `home-jd-source-controls`，删除独立上传 source panel。
- 同步 `ui-design/`、正式前端、owner spec / plan / checklist / BDD、`docs/ui-design/` 与 P0.014 / P0.015 场景文档。
- 补充并通过 Home layout 红绿测试、Home 邻近测试、ui-design contract、typecheck / build、Home pixel parity、P0.014 / P0.015 场景、docs-check 与 diff whitespace gate。
- See [BUG-0134](../bugs/BUG-0134.md).

### 关联 Commit

- `fix(frontend-home): integrate source controls into input card (BUG-0134)`

### 备注

Owner plan: `frontend-home-job-picks-and-parse/001-home-jd-import-and-parse` Phase 11.

## 21:37 工作记录

### 完成事项

- 继续收口 `product-scope/001-core-loop-module-pruning` Phase 6：删除无争议废弃的 Debrief / Profile / Jobs Recommendations subject 目录与旧 JD Match plan 包，并同步 `docs/spec` INDEX、context、引用和 owner evidence。
- 复查 active / completed 文档口径：将 B3 event/job、B4 migration baseline、backend-practice、backend-async-runner、prompt-rubric-registry、frontend-shell、workspace/practice 与 scenario 文档收敛到当前 core loop 合同。
- 修复 BUG-0135：恢复 `idempotency_records` privacy matrix hard-delete 覆盖，并补充 JD Match 历史迁移 final-state guard 与 frontend retired CSS scope guard。
- 验证通过：`sync-doc-index --check`、`make docs-check`、`git diff --check`、migration lint、`go test ./backend/internal/migrations -count=1`、focused frontend scope/workspace Vitest。

### 关联 Commit

- `fix(core-loop): close active doc pruning drift (BUG-0135)`

### 备注

See [BUG-0135](../bugs/BUG-0135.md) and [Core Loop Active Doc Pruning report](../reports/2026-07-06-core-loop-active-doc-pruning-assessment.md).
