# 2026-07-09 工作日志

## 07:55 工作记录

### 完成事项

- 收口 `frontend-workspace-and-practice/001` 面试规划列表入口：TopBar 文案收敛为 `面试` / `Interview`，无上下文 workspace 展示规划列表，卡片具备清晰视觉层级、简洁内容和主题色进入 CTA。
- 修复规划列表重进上下文：Home 导入 JD 时将选择的 `resumeId` 持久化到 `target_jobs.resume_id`，`listTargetJobs` / `getTargetJob` 返回 target job-level binding，Workspace 即使没有现成 `practice_plans` 行也能携带真实简历进入规划详情。
- 修复 JD identity 解析合同：`target.import.parse` prompt/schema 要求 `title` / `companyName`，TargetJob parse success 在同一事务写回岗位和公司身份字段，并同步 seed/eval prompt 资产。
- 同步 OpenAPI、generated Go/TS、fixtures、迁移、frontend/backend focused tests、ui-design 原型、E2E.P0.018 场景、owner spec/plan/checklist/BDD/context、BUG-0143/0144/0145 与交付复盘报告。
- 验证通过：`sync-doc-index --check`、`make docs-check`、`git diff --check`、`make lint-openapi`、`make validate-fixtures`、`make lint-prompts`、`make codegen-openapi`、`scripts/lint/migrations_lint.py`、focused backend Go tests、frontend typecheck/build/focused Vitest、workspace pixel parity、`E2E.P0.018` scenario wrapper。

### 关联 Commit

- `fix(workspace): close plan list UX and context regressions (BUG-0143, BUG-0144, BUG-0145)`

### 备注

Owner plans: `frontend-workspace-and-practice/001-workspace-and-interview-context`, `backend-targetjob/001-targetjob-import-and-parse-bootstrap`, `backend-practice/001-plan-and-session-orchestration`, `frontend-home-job-picks-and-parse/001-home-jd-import-and-parse`, `prompt-rubric-registry/002-output-schema-contract`.

## 09:34 工作记录

### 完成事项

- 将原 `JD 解析结果` 页收敛为统一的“面试规划详情 / 面试上下文确认”母版，首次导入后的 `parse` 和普通 `workspace?targetJobId=...` 回访共享同一套详情 DOM、文案、样式和简历绑定逻辑。
- 保留 `workspace` 无上下文规划列表和 `autoStartPractice=1` 启动面试合同，避免页面归一化破坏 create/start session、auth continuation 和 existing plan handoff。
- 同步 `ui-design` 静态原型、`docs/ui-design`、`frontend-home-job-picks-and-parse/001`、`frontend-workspace-and-practice/001`、BDD 场景、pixel parity、Vitest 覆盖和交付复盘报告。
- 验证通过：focused Vitest 113 tests、frontend build、Parse/Workspace pixel parity 44 tests、`E2E.P0.016` / `E2E.P0.018` / `E2E.P0.020` scenario wrappers、两个 `validate_context.py`、`sync-doc-index --check`、`make docs-check`、`git diff --check`。

### 关联 Commit

- `feat(frontend): unify interview plan detail`

### 备注

Owner plans: `frontend-home-job-picks-and-parse/001-home-jd-import-and-parse`, `frontend-workspace-and-practice/001-workspace-and-interview-context`.

## 11:10 工作记录

### 完成事项

- 收口 `backend-targetjob/001-targetjob-import-and-parse-bootstrap` Phase 8-11：
  - Phase 8: JD identity（`title`/`companyName`）解析成功必须写入且持久化
  - Phase 9: TargetJob 级别的 `resumeId` 绑定，import 时校验简历归属
  - Phase 10: 真实提供者解析健壮性（公司名校验降级、fenced JSON 包裹、A3 可观测性）
  - Phase 11: 解析失败 admission（`CompleteParseFailure` 事务删除 target + 级联 source/requirement）
- 同步 backend-targetjob spec/plan/checklist/BDD、E2E.P0.012 场景、BUG-0146 记录与交付复盘报告。

### 关联 Commit

- `feat(targetjob): close jd identity, resume binding, and parse robustness remediation`

### 备注

Auto-mode: work-journal multi-commit (1/3), plan: backend-targetjob/001-targetjob-import-and-parse-bootstrap.

## 11:25 工作记录

### 完成事项

- 收口 `frontend-workspace-and-practice/001-workspace-and-interview-context` Phase 12-14：
  - Phase 12: 统一详情路由（parse 母版页同时服务于导入后详情和 workspace re-entry）
  - Phase 13: 规划列表准入（排除 failed/queued/processing 记录，清除 stale InterviewContext）
  - Phase 14: Workspace 路由纯度（纯列表表面，移除旧 detail/start/modal hooks 和组件）
- 删除旧 workspace 运行时文件：AuthGate、Handoff、Header、InsightCard、StartPractice、modals、hooks 及相关测试。
- 前端核心路由、parse 页面、report/ReplayCta 同步适配。
- 同步 frontend-workspace-and-practice spec/plan/checklist/BDD、E2E.P0.018 场景、BUG-0147 记录与交付复盘报告。
- 同步 docs/ui-design 与 ui-design 静态原型。

### 关联 Commit

- `feat(workspace): close unified detail route, plan-list admission, and route purity remediation`

### 备注

Auto-mode: work-journal multi-commit (2/3), plan: frontend-workspace-and-practice/001-workspace-and-interview-context.

## 11:35 工作记录

### 完成事项

- 同步 docs/bugs/INDEX.md：添加 BUG-0146、BUG-0147 索引行。
- 同步 docs/reports/INDEX.md：添加两个交付复盘报告索引行。

### 关联 Commit

- `docs(reports): sync bugs and reports INDEX`

### 备注

Auto-mode: work-journal multi-commit (3/3), plan: downstream INDEX sync.

## 13:44 工作记录

### 完成事项

- 收口 `frontend-home-job-picks-and-parse/001-home-jd-import-and-parse` Phase 6：统一面试规划详情从可编辑确认页改为只读上下文收据，成功态只保留「立即面试」。
- 同步 UI 真理源、正式前端、pixel parity、P0.016 场景资产、owner spec/plan/checklist/BDD/context 和交付复盘报告，移除成功态 Save / Re-parse / Cancel、字段编辑、requirements toggle、hidden signal remove、resume picker / create fallback 等旧语义。
- 修复 code review 发现的 `evidenceLevel=inferred` 回归：`inferred` 与 `implicit` 一样展示为 PARTIAL，并补充 `explicit` / `implicit` / `inferred` badge 回归断言。
- 验证通过：focused Parse Vitest、Parse 相邻测试、frontend typecheck、`node --test ui-design/ui-design-contract.test.mjs`、`sync-doc-index --check`、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(frontend): close readonly plan detail review`

### 备注

Manual /work-journal commit for review remediation on `opt/interview-plan-readonly-0709`.

## 14:15 工作记录

### 完成事项

- 升级 `target.import.parse` prompt/schema：将 string-only `interviewHypotheses[]` 替换为结构化 `interviewRounds[]`（2-5 轮，含 type/name/focus/durationMinutes），并要求 `requirements[]` 至少包含一条 `hidden_signal`（evidenceLevel=inferred）。
- 后端 parser 新增结构化轮次校验（minItems=2/maxItems=5、合法 sequence/type、非空 name/focus、合理 durationMinutes）和 hidden signal 补偿逻辑：LLM 漏掉 hidden_signal 时从 `riskSignals[]` 派生；两者都缺失则返回 `AI_OUTPUT_INVALID`。
- 同步 OpenAPI `TargetJobSummary.interviewRounds`、generated Go/TS types、fixtures、seed migration、codegen 和 prompt lint。

### 关联 Commit

- `feat(targetjob): upgrade parse contract with structured interview rounds and hidden signal enforcement`

### 备注

Multi-commit (1/2), plan: frontend-home-job-picks-and-parse/001-home-jd-import-and-parse. Fixes BUG-0148 (round assumptions) and BUG-0149 (hidden signals).

## 14:25 工作记录

### 完成事项

- 新增共享 `buildTargetJobRoundAssumptions` mapper，从 `TargetJob.summary.interviewRounds[]` 派生 Parse 详情轮次卡片、Home 最近模拟面试卡片和导航上下文，不再使用 locale 静态 `parse.round*Focus` 或本地 `DEFAULT_ROUNDS` 补齐 4 轮。
- 删除 `frontend/src/app/i18n/locales/{en,zh}.ts` 中 `parse.round1Focus`-`round4Focus` 静态文案。
- 同步 owner spec/plan/checklist/BDD（Phase 7: 共享数据绑定，Phase 8: 结构化轮次合同），BDD scenario E2E.P0.016 增加结构化轮次断言与截图验收。
- 创建 BUG-0148（结构化轮次数据绑定）、BUG-0149（hidden signal 空白）与交付复盘报告。
- 同步 `ui-design/` 静态原型、`docs/ui-design/`、pixel parity、contract test 和 scenario 资产。

### 关联 Commit

- `feat(frontend): bind structured round assumptions across parse, home, and navigation`

### 备注

Multi-commit (2/2), plan: frontend-home-job-picks-and-parse/001-home-jd-import-and-parse. Closes BUG-0148, BUG-0149.

## 16:59 工作记录

### 完成事项

- 修复 `target.import.parse` deterministic fixture 合同漂移：默认测试 AI fixture 现在提供 2 条结构化 `interviewRounds[]`，并包含 `hidden_signal` / `riskSignals` 覆盖，满足当前 parser 对 2-5 轮和隐性信号的要求。
- 加固 `TestDeterministicParseAIClient_OnlyInterceptsTargetImportParse`，让 deterministic fixture 走完整 `ParseExecutor.Handle`，并断言成功落库、hidden signal 覆盖和 2-5 条结构化轮次，避免只验证 JSON 形状却绕过 parser 合同。
- 验证通过：`go test ./backend/internal/targetjob -run TestDeterministicParseAIClient_OnlyInterceptsTargetImportParse -count=1 -v`、`go test ./backend/cmd/api -run TestE2EP0010HTTPTextImportParseReady -count=1 -v`、`go test ./backend/internal/targetjob -count=1`、targetjob API 相邻 focused tests 和 `git diff --check`。

### 关联 Commit

- `fix(targetjob): align deterministic parse fixture with structured rounds`

### 备注

Review remediation for `frontend-home-job-picks-and-parse/001-home-jd-import-and-parse`; closes deterministic fixture coverage drift for BUG-0148 / BUG-0149 structured round and hidden signal contracts.
