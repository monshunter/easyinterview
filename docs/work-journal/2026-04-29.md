# 2026-04-29 工作日志

## 00:14 工作记录

### 完成事项

- 收口 `openapi-v1-contract/003-breaking-change-gate` L2 remediation：原地修订 003 plan/checklist，追加并完成 Phase 2.5，状态恢复 `completed`，同步 plans/INDEX。
- 修复 `scripts/lint/openapi_diff.py`：递归比较 `oneOf` / `allOf` / `anyOf` composition schema，composition 分支内的 type / `$ref` / required / enum 破坏性变更会被 gate 拦截。
- 修复 privacy export `501→202` 白名单 history gate：默认按 `openapi/diff-config.yaml` 的 `tooling.historyDiffBase` 与 `HEAD` 的 merge-base 比较 history 行数，并在 `make openapi-diff` 增加 `HISTORY_REF=` 覆盖入口。
- 补充 3 个回归测试：nullable `oneOf` type 变更、paginated `allOf` `$ref` 变更、feature branch 已提交 history 增量的正向白名单路径。
- 更新相邻文档与治理资产：`openapi/README.md`、`openapi/baseline/README.md`、`openapi/diff-config.yaml`、B2 history 1.7、`BUG-0001`、remediation 交付复盘报告。
- 验证通过：`python3 -m unittest scripts.lint.openapi_diff_test -v`（24/24）、`make codegen-check && make validate-fixtures && make openapi-diff`、`sync-doc-index --check`、`git diff --check`。

### 关联 Commit

- `fix(openapi): tighten breaking-change gate composition diff`

### 备注

本次为 `/plan-code-review --fix` 后的 L2 remediation；见 [BUG-0001](../bugs/BUG-0001.md) 与 [remediation 复盘](../reports/2026-04-29-openapi-v1-contract-003-breaking-change-gate-remediation-assessment.md)。

## 10:15 工作记录

### 完成事项

- 在 `shared/conventions.yaml` 授权追加 A3 AI gateway baseline 错误码：`AI_PROVIDER_TIMEOUT` / `AI_OUTPUT_INVALID` / `AI_FALLBACK_EXHAUSTED`。
- 同步 Go / TS 共享错误码 registry 与 OpenAPI `ApiErrorCode` 生成产物，保证后端、前端 SDK 与 API contract 使用同一错误码集合。
- 调整 `scripts/lint/conventions_yaml.py`，由固定 6 个错误码改为校验 6 个上游示例必须存在，并补充重复错误码与 `retryable` bool 类型检查。
- 验证通过：`make lint-conventions`、`make codegen-check`、`git diff --check`。

### 关联 Commit

- `feat(shared-conventions): add AI gateway error codes`

### 备注

本提交按 `/work-journal` 方案 A 拆分，仅包含共享约定真理源、lint 与生成产物；对应 spec remediation 文档单独提交。

## 10:17 工作记录

### 完成事项

- 收口 W1 contract review 文档修订：A3 `ai-gateway-and-model-routing` 明确 `AICallMeta` 归 A3 runtime 拥有、B1 只提供 `AI_*` 错误码，fallback 边界改为 AI routing layer，并区分 per-call metric 与 event-only counter。
- 修订 B3 `event-and-outbox-contract`：补齐 18 个事件 payload schema inventory、API-facing 与 internal job type 边界、outbox retry / dead-letter 字段和 dispatcher due-query 语义。
- 修订 B4 `db-migrations-baseline`：把 ADR-Q1 auth/session 支撑表、`schema_backfills` ledger、pgvector 生命周期、enum/check 来源矩阵与 B3 outbox retry 字段纳入 baseline。
- 同步 parent roadmap、ADR-Q5、shared-conventions spec/history 与 `docs/spec/INDEX.md` 版本/日期投影。
- 验证通过：`sync-doc-index --check`（zero drift；仅现有占位条目 warnings）、`git diff --check`。

### 关联 Commit

- `docs(spec): reconcile W1 contract review findings`

### 备注

本提交按 `/work-journal` 方案 A 拆分，仅包含 spec / history / INDEX 文档修订。

## 11:34 工作记录

### 完成事项

- 原地修订 A1-A5 与 B1-B4 相关 spec / ADR / plan 契约，收口 plan-review findings 的用户确认项。
- A1 `repo-scaffold` 根目录契约补齐 `shared/` 与 `config/`，并标注已完成 artifact 需要原地 remediation 的交接边界。
- B2/B3/B4 同步 `email_dispatch` internal-only jobType、`DELETE /api/v1/me` 删除入口、P0 deletion matrix、37 endpoint freeze、AI call meta typed columns 与 dotted event name 规范。
- 按 ADR-Q6 authoritative 口径修订 A3/B1 AI 边界：`AI_GATEWAY_*` 为 AIClient OpenAI-compatible 连接参数，fallback 由 endpoint / gateway route 承担，A3 client 不自行切换模型。
- 将 C9 真实面试复现 / 复盘文本流升格为 P0，并同步 roadmap、F3 feature_key、B2/B3 契约与 `docs/spec/INDEX.md`。
- 验证通过：`sync-doc-index --check`（zero drift，仅 pending child 占位 warnings）、`git diff --check`、OpenAPI endpoint 表计数 37、旧契约关键词扫描无 active spec 残留。

### 关联 Commit

- `docs(spec): reconcile A/B contract review findings`

### 备注

本提交为 `/plan-review` 后的文档 remediation；只修改 spec / plan / history / INDEX 文档，不更新生成物或 runtime artifact。B2 与 A1 后续 artifact remediation 已写入对应 spec 交接说明。

## 17:03 工作记录

### 完成事项

- 使用 `/design` + `/plan-review` 收口 [engineering-roadmap](../spec/engineering-roadmap/spec.md) 中 A1-A5、B1-B4 的 plan 设计；确认当前 Layer B 只有 4 份 contract spec，未新建 B5。
- 物化 A3 `001-aiclient-and-profile-bootstrap` / `002-tools-streaming-and-stt`、A5 `001-local-quality-gates`、B1 `002-codegen-pipeline`、B3 `001-bootstrap`、B4 `001-bootstrap` 等 plan/checklist/context。
- 原地 reopen B2 `001-bootstrap` / `002-fixtures-and-mock-source` / `003-breaking-change-gate` remediation phase，补齐 37 endpoint、`DELETE /api/v1/me`、`deleteMe` fixture 与 baseline freeze 修复入口。
- 补齐 A1/A4 artifact 交接：根 README 增加 `shared/` / `config/`，新增 `config/README.md`，A4 spec/plan 对齐 24 项 env key、`async.queueWeights`、PostHog last-known-good 与 secret 样本红线。
- 同步 A/B spec history、plans/INDEX、根 `docs/spec/INDEX.md` 与 ADR-Q1 `email_dispatch` internal-only 口径。
- 验证通过：`validate_context.py --plan-root docs --docs-root docs`（14/14）、`sync-doc-index.py --check`（zero drift，仅 pending 占位 warnings）、`git diff --check`、secret 样本与脚本参数扫描。

### 关联 Commit

- `docs(roadmap): materialize A/B plan designs`

### 备注

本提交为用户确认后的 A/B plan design 收口；只提交文档、plan/checklist/context 与 `config/README.md`，不实现 runtime code / OpenAPI artifacts。

## 22:34 工作记录

### 完成事项

- 收口 `openapi-v1-contract/001-bootstrap` Phase 7 v1.8 contract remediation，新增 `DELETE /api/v1/me` / `operationId=deleteMe` / `202 PrivacyRequestWithJob` 契约。
- 将 inventory lint 提升到 37 operation，补充 `deleteMe` 的 `Idempotency-Key` 与 active `privacy_delete` dedupe 契约测试。
- 更新 OpenAPI 生成物：Go server interface 与 TS client 均包含 `deleteMe`；generated embedded spec 同步。
- 明确 P0 `Debrief` 的感谢信草稿与完整跟进建议字段为 P1 optional/hidden，不进入 P0 required surface。
- 验证通过：`python3 -m unittest scripts.lint.openapi_inventory_test -v`、`python3 scripts/lint/openapi_inventory.py openapi/openapi.yaml`、`make lint-openapi`、`make codegen-openapi`、`go test ./cmd/codegen/openapi -count=1`、`sync-doc-index --fix-index`。

### 关联 Commit

- `feat(openapi): complete v1.8 contract remediation`

### 备注

Auto-committed by /tdd phase-commit, plan: openapi-v1-contract/001-bootstrap

## 22:40 工作记录

### 完成事项

- 收口 `openapi-v1-contract/002-fixtures-and-mock-source` Phase 5 v1.8 fixture remediation，新增 `openapi/fixtures/Auth/deleteMe.json` default fixture。
- 将 fixtures validator、CLI fault-injection 测试与 examples 投影工具提升到 37 operation coverage；缺 `deleteMe` fixture 或 projection source 会失败。
- 移除 `getDebrief` default fixture 中 P1 `thankYouDraft` / `nextRoundChecklist` 字段，保持 P0 Debrief fixture 不依赖感谢信草稿或完整跟进建议。
- 更新 `openapi/README.md` 与 `openapi/fixtures/README.md` 的 37 operation 与 header-only `deleteMe` request 说明。
- 验证通过：`python3 -m unittest scripts.lint.validate_fixtures_test -v`、`python3 -m unittest scripts.lint.validate_fixtures_cli_test -v`、`python3 -m unittest scripts.codegen.render_openapi_fixture_examples_test -v`、`make validate-fixtures`、`make render-openapi-fixture-examples`、`sync-doc-index --fix-index`。

### 关联 Commit

- `feat(openapi): add deleteMe fixture coverage`

### 备注

Auto-committed by /tdd phase-commit, plan: openapi-v1-contract/002-fixtures-and-mock-source

## 22:46 工作记录

### 完成事项

- 收口 `openapi-v1-contract/003-breaking-change-gate` Phase 5 v1.8 baseline remediation，原地重新冻结 `openapi/baseline/openapi-v1.0.0.yaml` 到 37 endpoint / `DELETE /api/v1/me`，未创建 `openapi-v1.0.1.yaml`。
- 更新 `openapi-diff` wrapper 输出 inventory 信息，`make openapi-diff` 现在报告 expected / baseline / current operations 均为 37。
- 同步 `openapi/diff-config.yaml` 与 `openapi/baseline/README.md`：明确 v1.0.0 freeze 包含 `deleteMe`，privacy export `501→202` 白名单仍仅作用于 `POST /api/v1/privacy/exports`。
- 更新 B2 history、003 plan/checklist lifecycle 与 plans/INDEX。
- 验证通过：`python3 -m unittest scripts.lint.openapi_diff_test -v`、`make openapi-diff`、`make codegen-check`、`make validate-fixtures`、`sync-doc-index --check`、`git diff --check`。

### 关联 Commit

- `feat(openapi): refresh v1 baseline freeze`

### 备注

Auto-committed by /tdd phase-commit, plan: openapi-v1-contract/003-breaking-change-gate

## 22:49 工作记录

### 完成事项

- 创建 `openapi-v1-contract v1.8 artifact remediation` 交付复盘报告，覆盖 001/002/003 三个原地 reopen plan 的执行证据与流程阻点。
- 在 `docs/reports/INDEX.md` 登记新复盘报告。
- 复盘结论：主要改进点是为同一 subspec 的多 active remediation plan 增加 ordered batch 约定，以及补充 dirty-tree codegen-check 证据组合说明；无关 dirty 文件按当前方式恢复到工作树即可。

### 关联 Commit

- `docs(reports): land openapi v18 remediation retrospective`

### 备注

本提交为 `/retrospective --this` 收尾报告，不修改 skill / README / AGENTS.md / spec-plan，只记录建议。

## 23:09 工作记录

### 完成事项

- 收紧 `config/README.md` 的 ownership 边界：明确 A1 只锁定 `config/` 顶层根容器，不在本目录写入 secret 示例值。
- 补全 `config/README.md` owner 说明，明确 A4 owns `config/config.yaml`、`config/{dev,staging,prod}.yaml`、`config/feature-flags.yaml` 与 `.env.example` 字典，A3/F3 后续消费 `config/ai-profiles/` 与 prompt/rubric 路径。
- 勾选 `docs/spec/repo-scaffold/plans/001-bootstrap/checklist.md` Phase 4.1-4.4，记录本轮 root-container remediation 的 README ownership、root entrypoint、context discovery 与校验已完成。
- 验证通过：`python3 .agent-skills/implement/shared/scripts/validate_context.py --context docs/spec/repo-scaffold/plans/001-bootstrap/context.yaml --docs-root docs --target repo`、`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check`、`git diff --check`。

### 关联 Commit

- `docs(repo-scaffold): mark root-container remediation complete`

### 备注

本提交按 `/work-journal` 手动记录；`README.md`、`context.yaml` 与 `docs/spec/repo-scaffold/plans/INDEX.md` 已在 `4dcadd7 docs(roadmap): materialize A/B plan designs` 落地，本次只补 Phase 4 收口勾选与 `config/README.md` ownership 文案收紧。

## 23:39 工作记录

### 完成事项

- 落地 `backend/internal/ai/aiclient/` Go 包骨架与 `AIClient` 三方法接口（`Complete` / `Embed` / `Stream`），冻结 `AIStreamEvent` (`delta` / `error` / `done`) 类型与 channel close 语义。
- 落地 `AICallMeta` 17 字段固定顺序 + `metaBuilder` helper（profile/CallMetadata/provider partial 三流合并 + 必填校验）。
- 落地 `providers/stub/` deterministic provider：`sha256(profile + canonical(payload))` 作 seed；factory 强制校验 `APP_ENV=test` 或显式 `WithAllowed(true)` 才能实例化；`Stream` 一次 `done` + close。
- 引用 B1 共享错误码 `CodeAiOutputInvalid`（`AI_PROVIDER_TIMEOUT` / `AI_OUTPUT_INVALID` / `AI_FALLBACK_EXHAUSTED` 三项已在 `backend/internal/shared/errors/codes.go` 落地）；本 plan 对 B1 002 未生成的 `task_type` enum 在包内维护私有 `TaskType` 别名，handoff 时切换。
- 单测覆盖：`Complete` 路由 stub 且 meta.Provider="stub"、deterministic 多次调用一致、空 messages 返回 `AI_OUTPUT_INVALID`、`Embed` 向量数量、`Stream` `done` + close、`stub.New` `APP_ENV` 拒绝/允许门、`aiclient.New` non-test 缺凭证返回 `ErrMissingGatewayConfig`。
- 验证：`go build ./internal/ai/aiclient/...` 通过；`go test ./internal/ai/aiclient/... -count=1` 全绿（`aiclient` + `providers/stub` 两个 package）。

### 关联 Commit

- `feat(aiclient): aiclient interface + stub provider 骨架`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-gateway-and-model-routing/001-aiclient-and-profile-bootstrap.

## 23:49 工作记录

### 完成事项

- 落地 `backend/internal/ai/aiclient/providers/openai_compatible/` adapter：纯 `net/http` + `encoding/json`，零厂商 SDK；`Complete` 走 `/v1/chat/completions`、`Embed` 走 `/v1/embeddings`；header 含 `Authorization: Bearer`、`Content-Type`、`X-Request-ID` 透传；解析 `usage.{prompt,completion}_tokens` / `model` / `X-Fallback-{From,To}` / `X-Route` 入 `AICallMeta`；`context.WithTimeout` + `profile.timeout_ms` → 超时和 5xx 统一返回 `AI_PROVIDER_TIMEOUT`，4xx 透传 body `error.code`，未识别落 `AI_OUTPUT_INVALID`。
- 落地 `backend/internal/ai/aiclient/profile/` Model Profile YAML loader：字段集严格对齐 spec §2.1（含 `task_type=stt` 预留），解析失败附 file path；polling reloader（5s cadence，plan §2.3 允许的 fsnotify fallback 路径，README 中后续可升级 fsnotify）+ `atomic.Pointer[snapshot]` + RW reload mutex 保证正在进行的调用持有捕获时的 profile pointer，新调用观察新 snapshot；暴露 `Reload(ctx) error`、`Names()`、`LoadedAt()`、`Close()`。
- 落地 fixture profile：`config/ai-profiles/practice.followup.default.yaml`、`config/ai-profiles/review.report.default.yaml`，仅供本 plan 测试 / 本地验证，真实内容由 F3 维护。
- 落地 `backend/internal/ai/aiclient/providers/openai_compatible/mockserver/` 可被 E1 复用的 helper：覆盖 chat / embeddings 正常响应、超时、5xx、4xx error envelope、X-Fallback-{From,To} + X-Route 注入、`MissingChoices` `validateOutput` 失败路径，captures Authorization / Content-Type / X-Request-ID 供断言。
- 单测覆盖 contract：normal chat / normal embed / 超时 → `AI_PROVIDER_TIMEOUT` / 5xx → `AI_PROVIDER_TIMEOUT` / 4xx envelope passthrough / fallback chain + route header / missing choices → `AI_OUTPUT_INVALID` / 缺 base URL 或 API key 报错。
- 单测覆盖 loader：parse + 必填校验 7 例（missing-name / -task / -provider / -model / bad task type / non-positive timeout / missing-version）/ Reload 用例对比新旧 profile / polling reloader 在 SLA 内自动收敛（50ms cadence ≪ 30s）/ 重名拒绝 / Names 排序。
- 单测覆盖 race：`loader_concurrency_test.go` 8 个并发 reader × 100 轮 reload，`go test -race` 全绿。
- 验证：`go test ./internal/ai/aiclient/... -count=1 -race` 全绿（4 packages：`aiclient`、`profile`、`providers/stub`、`providers/openai_compatible`）；`go build ./internal/ai/aiclient/...` 通过。

### 关联 Commit

- `feat(aiclient): openai_compatible adapter + profile loader + mockserver`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-gateway-and-model-routing/001-aiclient-and-profile-bootstrap. Phase 2.3 偏离 plan 默认 fsnotify 路径，使用 plan §2.3 允许的 polling fallback；选择原因：避免与 A4 secrets-and-config WIP 中 fsnotify 间接依赖纠缠，让本 plan 提交保持 self-contained。30 秒 SLA 由 5s cadence 覆盖。fsnotify 升级路径在 Phase 4 README 中记录。stub.go 与 stub_test.go 由 A4 boundary lint 调整为禁止读 `os.Getenv`，改用 `WithAppEnv(env)` 显式注入；本提交将该改动一并纳入。

## 23:55 工作记录

### 完成事项

- 落地 `backend/internal/ai/aiclient/observability/` 包：`Registerer` / `Counter` / `Histogram` 抽象避免 `backend/go.mod` 引入 prometheus 直依赖（F1 落地 plan 时切换实现），并提供 `InMemoryRegistry` 给单测；`Logger` + `MemoryLogger` + `LogFields` allowlist。
- 注册 7 个 metric family：`ai_task_runs_total`、`ai_task_latency_seconds`（histogram）、`ai_task_input_tokens_total`、`ai_task_output_tokens_total`、`ai_task_cost_usd_total`、`ai_output_validation_failures_total`、`ai_fallback_total`；标签集对齐 F1（`provider` / `model_family` / `model_profile_name` / `route` / `task_type` / `language` / `result`，fallback 额外 `from_model_family` / `to_model_family`）。
- 落地 `Wrap` middleware decorator：包裹 inner `aiclient.AIClient`，统一记录 4 类日志事件（`ai.task.completed` / `ai.task.failed` / `ai.task.fallback` / `ai.output.validation_failed`）、写 `ai_task_runs` 行、写 `audit_events.action='ai.call'` 行；audit metadata 严格只含 `prompt_hash` / `response_hash` / `prompt_char_length` / `response_char_length` / `profile_name`。
- 计数器语义：每次调用 run / latency / token / cost 增长；fallback 仅在 `meta.FallbackChain` > 1 时 +1；validation failure 仅在 `ValidationStatus=invalid` 且 `ErrorCode=AI_OUTPUT_INVALID` 时 +1。`Complete` 内置 `validateJSONShape`，调用方提供 `OutputSchema` 时 response 不是合法 JSON 直接返回 `AI_OUTPUT_INVALID`，更复杂的 JSON Schema 验证留给后续 plan。
- 单测：`decorator_test.go` 覆盖 7 metric family 注册、success path（runs/latency/tokens 增长 + ai.task.completed log + ai_task_runs 行 + audit row）、failure path（empty messages → AI_OUTPUT_INVALID）、fallback path（chain >1 → ai_fallback_total +1 + ai.task.fallback log）、output schema 拒绝、构造器必填 4 项 DI；`privacy_test.go` 白盒：注入 4 个 sensitive token，断言 metric labels / log fields / `ai_task_runs` row / audit metadata 全部不含明文，仅 hash + 长度 + profile + version 信息。
- 验证：`go test ./internal/ai/aiclient/... -count=1` 全绿（5 packages）。

### 关联 Commit

- `feat(aiclient): observability decorator 与 7 metric / 4 log / audit 红线`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-gateway-and-model-routing/001-aiclient-and-profile-bootstrap. Phase 3.1 决策：用本地 `Registerer` 抽象代替直接依赖 `prometheus.Registerer`；在 F1 observability-stack 落地 plan 时由 F1 实现适配器接入 `prometheus.DefaultRegisterer`。`backend/go.mod` 不增加 prometheus 直依赖，避免提前与 F1 主线绑定。
