# 2026-04-29 工作日志

## 00:14 工作记录

### 完成事项

- 收口 `openapi-v1-contract/003-breaking-change-gate` L2 remediation：原地修订 003 plan/checklist，追加并完成 Phase 2.5，状态恢复 `completed`，同步 plans/INDEX。
- 修复 `scripts/lint/openapi_diff.py`：递归比较 `oneOf` / `allOf` / `anyOf` composition schema，composition 分支内的 type / `$ref` / required / enum 破坏性变更会被 gate 拦截。
- 修复 privacy export `501→202` 白名单 history gate：默认按 `openapi/diff-config.yaml` 的 `tooling.historyDiffBase` 与 `HEAD` 的 merge-base 比较 history 行数，并在 `make openapi-diff` 增加 `HISTORY_REF=` 覆盖入口。
- 补充 3 个回归测试：nullable `oneOf` type 变更、paginated `allOf` `$ref` 变更、feature branch 已提交 history 增量的正向白名单路径。
- 更新相邻文档与治理资产：`openapi/README.md`、`openapi/baseline/README.md`、`openapi/diff-config.yaml`、B2 history 1.7、`BUG-0001`、remediation 交付复盘报告。
- 验证通过：`python3 -m unittest scripts.lint.openapi_diff_test -v`（24/24）、`make codegen-check && make validate-fixtures && make openapi-diff`、`sync-doc-index --check`、`git diff --check`。

### 关联 Commit

- `fix(openapi): tighten breaking-change gate composition diff`

### 备注

本次为 `/plan-code-review --fix` 后的 L2 remediation；见 [BUG-0001](../bugs/BUG-0001.md) 与 [remediation 复盘](../reports/2026-04-29-openapi-v1-contract-003-breaking-change-gate-remediation-assessment.md)。

## 10:15 工作记录

### 完成事项

- 在 `shared/conventions.yaml` 授权追加 A3 AI gateway baseline 错误码：`AI_PROVIDER_TIMEOUT` / `AI_OUTPUT_INVALID` / `AI_FALLBACK_EXHAUSTED`。
- 同步 Go / TS 共享错误码 registry 与 OpenAPI `ApiErrorCode` 生成产物，保证后端、前端 SDK 与 API contract 使用同一错误码集合。
- 调整 `scripts/lint/conventions_yaml.py`，由固定 6 个错误码改为校验 6 个上游示例必须存在，并补充重复错误码与 `retryable` bool 类型检查。
- 验证通过：`make lint-conventions`、`make codegen-check`、`git diff --check`。

### 关联 Commit

- `feat(shared-conventions): add AI gateway error codes`

### 备注

本提交按 `/work-journal` 方案 A 拆分，仅包含共享约定真理源、lint 与生成产物；对应 spec remediation 文档单独提交。

## 10:17 工作记录

### 完成事项

- 收口 W1 contract review 文档修订：A3 `ai-gateway-and-model-routing` 明确 `AICallMeta` 归 A3 runtime 拥有、B1 只提供 `AI_*` 错误码，fallback 边界改为 AI routing layer，并区分 per-call metric 与 event-only counter。
- 修订 B3 `event-and-outbox-contract`：补齐 18 个事件 payload schema inventory、API-facing 与 internal job type 边界、outbox retry / dead-letter 字段和 dispatcher due-query 语义。
- 修订 B4 `db-migrations-baseline`：把 ADR-Q1 auth/session 支撑表、`schema_backfills` ledger、pgvector 生命周期、enum/check 来源矩阵与 B3 outbox retry 字段纳入 baseline。
- 同步 parent roadmap、ADR-Q5、shared-conventions spec/history 与 `docs/spec/INDEX.md` 版本/日期投影。
- 验证通过：`sync-doc-index --check`（zero drift；仅现有占位条目 warnings）、`git diff --check`。

### 关联 Commit

- `docs(spec): reconcile W1 contract review findings`

### 备注

本提交按 `/work-journal` 方案 A 拆分，仅包含 spec / history / INDEX 文档修订。

## 11:34 工作记录

### 完成事项

- 原地修订 A1-A5 与 B1-B4 相关 spec / ADR / plan 契约，收口 plan-review findings 的用户确认项。
- A1 `repo-scaffold` 根目录契约补齐 `shared/` 与 `config/`，并标注已完成 artifact 需要原地 remediation 的交接边界。
- B2/B3/B4 同步 `email_dispatch` internal-only jobType、`DELETE /api/v1/me` 删除入口、P0 deletion matrix、37 endpoint freeze、AI call meta typed columns 与 dotted event name 规范。
- 按 ADR-Q6 authoritative 口径修订 A3/B1 AI 边界：`AI_GATEWAY_*` 为 AIClient OpenAI-compatible 连接参数，fallback 由 endpoint / gateway route 承担，A3 client 不自行切换模型。
- 将 C9 真实面试复现 / 复盘文本流升格为 P0，并同步 roadmap、F3 feature_key、B2/B3 契约与 `docs/spec/INDEX.md`。
- 验证通过：`sync-doc-index --check`（zero drift，仅 pending child 占位 warnings）、`git diff --check`、OpenAPI endpoint 表计数 37、旧契约关键词扫描无 active spec 残留。

### 关联 Commit

- `docs(spec): reconcile A/B contract review findings`

### 备注

本提交为 `/plan-review` 后的文档 remediation；只修改 spec / plan / history / INDEX 文档，不更新生成物或 runtime artifact。B2 与 A1 后续 artifact remediation 已写入对应 spec 交接说明。
