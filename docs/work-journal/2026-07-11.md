# 2026-07-11 工作日志

## 07:27 工作记录

### 完成事项

- 延续跨层技术债清理，将 backend、frontend、OpenAPI、UI 原型、场景资产、工程脚本与 active spec/plan/index 对齐到当前产品范围。
- 清理过时的生成产物、运行时辅助、模板、旧 UI surface 与范围外场景资产，并补充或同步契约测试、负向门禁和文档索引。
- 提交前执行 `git diff --check`，未发现空白错误。

### 关联 Commit

- `refactor(repo): close remaining scope and contract drift`

### 备注

本次按用户确认将当前全部变更合并为一个 commit。

## 18:38 工作记录

### 完成事项

- 原地修订四个 Practice owner 计划并闭环 [BUG-0158](../bugs/BUG-0158.md)：电话模式删除 restart、`callEnded`、分段/live/cut-off 旧契约，文本态使用单 handset 进入电话，顶部 handset 与中央红色挂断共用同一退出路径并返回同一个文本 session。
- 同步 `ui-design/` 与正式 frontend，补齐 call-scoped microphone/utterance recorder、VAD silence submit、TTS-ended re-arm、speech-start barge-in、late-callback suppression、session-keyed remount和 generated `getTargetJob` 真实身份显示；mock dialogue 与 raw `questionIntent` 不再进入用户界面。
- 统一 backend 文本/语音问题生成：使用 server-owned session/turn/plan/committed context 与 persisted session language，结构/parser/语言无效最多修复一次，provider/config/timeout 不修复，二次无效走 typed error / `session_wait` 且不生成 canned question 或额外 TTS/persistence。
- 收紧 voice committed-context 证据、answer summary 持久化、event metadata whitelist、fixture exact replay/hash 与 P0.007/P0.008/P0.009/P0.038/P0.045/P0.046/P0.051 场景门禁；backend-practice/002 与 /003 恢复 `completed`。
- 完整验证通过：frontend `144 files / 889 tests`、typecheck/build、UI contract `45/45`、Practice parity `11 passed / 1 conditional skip`；backend full tests、staticcheck、prompt/eval `36/36`、OpenAPI/fixture/codegen、migration、privacy/runtime-boundary、四个 owner context、docs/index/diff gates 通过。
- 本地真实前后端与真实 API 浏览器验收通过；`.test-output/practice-phone-session-flow-0711/` 保存三张 `1440x900` 文本/电话/挂断后截图，挂断前后 `sessionId` 不变且数据库 session 仍为 `running`。本地 STT/TTS profile 未启用、自动化浏览器无麦克风，因此截图结论限定为 UI、真实数据和同会话切换；provider 音频由 focused/BDD gates 覆盖。
- 创建 [BUG-0158](../bugs/BUG-0158.md) 与 [交付复盘](../reports/2026-07-11-practice-phone-session-language-assessment.md)。本地运行密钥曾出现在工具输出中，未写入仓库文档；继续真实 provider UAT 前需轮换 AI provider key、session cookie secret 与 auth challenge pepper。

### 关联 Commit

- `fix(practice): align phone flow and session language (BUG-0158)`

### 备注

backend、frontend、UI prototype、prompt/fixture、scenarios 与 owner docs 共同承接同一个 BUG-0158 会话契约，作为一个跨层原子提交收口；未合并或推送到父分支。
