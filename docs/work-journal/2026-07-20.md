# 2026-07-20 工作日志

## 00:48 工作记录

### 完成事项

- 按四张参考图统一改造 Practice 启动、Resume 解析、Report 生成和 JD 解析等待态：新增 shell-owned `AsyncTransitionScene` 与 brand/resume/report/job 四种 code-native SVG，共享 atmospheric canvas、标题层级、indeterminate progress、动作、步骤轴和 reduced-motion 合同。
- 修正上下文导航：Practice、Parse、Reports、Generating、Report 与 ReportConversation 保留全局 TopBar 并统一高亮“面试”；删除 Generating 的 no-chrome 例外，不改变各 owner 的 route locator、API、轮询、错误恢复或 ready handoff。
- 根据首轮 Chrome 对照继续收敛几何：Resume scene 实测全宽 `x=0/width=1920`；Report 中心卡实测 `x=415/width=1090`；JD 改为 1–4 编号、竖向连线和 done/current/pending 真实步骤轴。
- Chrome 在真实 frontend/backend 上多次触发四类异步流程并全部到达 ready 页面，browser error/warning 为 0；该证据为 scoped real UI acceptance，不声明新的 E2E ID。mobile containment 与 reduced-motion 由同一 shared component/CSS contract 覆盖。
- 最终 focused 9 files / 89 tests、frontend production build、local frontend redeploy、本地依赖 4/4、根 `make test` 615 tests / 4615 subtests、六份 owner context、Header/INDEX、docs links 与 `git diff --check` 全部通过。
- 原地更新 [BUG-0192](../bugs/BUG-0192.md)、既有[交付复盘](../reports/2026-07-19-core-workflow-reference-ui-alignment-assessment.md)、相关 UI design 与六组 owner spec/plan/checklist/BDD；除 shell/001 的既有 Auth Phase 15.3 外，其余本轮 owner lifecycle 均已恢复 `completed`。

### 关联 Commit

- `fix(frontend): align async transition states (BUG-0192)`

### 备注

- 本轮未用伪百分比或延时动画替代真实业务状态；Chrome 截图均来自真实 pending 请求，最终页只用于确认 handoff 成功。

## 01:08 工作记录

### 完成事项

- 按 Settings UI 参考稿重画 Header 右侧 code-native SVG：以半透明账号资料窗口、头像与信息行、柱状图、前景锁卡、盾牌对勾和双星芒取代山形人物稀疏线稿；所有视觉层从当前主题 token 派生，继续保持装饰性 `aria-hidden` 与窄屏隐藏。
- 原地修订 frontend-shell/001 Phase 21、UI design、spec、BDD、BUG-0192 与既有交付复盘；新增 DOM layer/CSS token 负向 gate，避免“SVG 容器存在”再次冒充参考稿内容已对齐。
- TDD RED 时 `ScreensVisual` 13 项仅新增分层合同失败、其余 12 项通过；GREEN 后 Settings visual/behavior 26/26 与 typecheck 通过。最终 production build、frontend redeploy、本地依赖 4/4、根 `make test`（Python 615 / 4615 subtests、Go 全包、frontend 134 files / 1082 tests）、owner context、Header/INDEX/docs links 与 diff gate 全部通过。
- Chrome 在真实 `1264×964` Settings 页面验收：插画 `360×200`、7 个目标层、Header/Appearance 同边界、`documentWidth=viewportWidth`；Ocean / Plum / Custom 临时预览均随主题着色，未保存切换在 reload 后恢复账号确认主题，无页面/浏览器错误或警告。

### 关联 Commit

- `fix(settings): redraw header security illustration (BUG-0192)`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/001-app-shell-auth-settings

## 02:35 工作记录

### 完成事项

- 修复 Settings Appearance 在 Custom 展开时“保存主题”按钮上下跳动：把 Ocean / Plum / Custom 一级选项与 Save 收入同一固定 primary row，Custom hue/chroma 与错误提示只在其后的正常文档流展开；移除独立第三列 action 与条件内容高度的纵向耦合。
- 原地修订 frontend-shell/001 Phase 23、Spec、UI architecture、认证/设置设计与 `BDD.SHELL.SETTINGS.THEME.001`，更新既有 [BUG-0193](../bugs/BUG-0193.md) 和[交付复盘](../reports/2026-07-19-settings-custom-theme-hierarchy-assessment.md)，没有新增并行 plan、Bug、report、API 或 E2E ID。
- TDD RED 为 2 failed / 24 passed，准确暴露缺少 primary row 与旧 action-column CSS；GREEN 为 26/26。主题相关 focused 回归 34/34、typecheck、production build、根 `make test`（Python 615 / 4615 subtests、Go 全包、frontend 135 files / 1091 tests）通过。
- frontend 重部署后环境 readiness 4/4。Chrome 在 `1440×900` 量得 preset/custom 两态 Save 均为 `top=341`、`bottom=385`，差值 `0px`；Custom panel 从 `top=399` 开始。`390×844` 下顺序、可逆切换、无横向溢出与 0 warning/error 通过，证据位于 `.test-output/settings-theme-primary-row/`。
- owner context、Header/INDEX、docs links、core-loop pruning 与 `git diff --check` 全部通过；frontend-shell/001 因既有 Auth Phase 15.3 仍未完成而继续保持 `active`。

### 关联 Commit

- `fix(settings): anchor theme save action (BUG-0193)`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/001-app-shell-auth-settings

## 02:15 工作记录

### 完成事项

- 为正式前端新增并导出 `--ei-radius-control: 8px`，将 TopBar/Auth/Home/Workspace/Parse/Practice/Reports/Report/Generating/Resume/Settings 的 28 个 CSS action selector 与 10 个内联恢复 action 显式迁移到统一语义圆角。
- 保留 circular 用户标记、theme/language pill、无边框 link/back、card/input/status surface 的原有几何；新增 source inventory/exception 测试，拒绝页面私有 `2px`、`6px`、`7px` action 和全局 `button` selector 回流。
- 原地修订 Shell Visual Phase 23、UI architecture、frontend-shell Spec 与 `BDD.SHELL.VISUAL.002`，同步更新 [BUG-0192](../bugs/BUG-0192.md) 与既有[交付复盘](../reports/2026-07-19-core-workflow-reference-ui-alignment-assessment.md)，owner lifecycle 已恢复 `completed`。
- TDD RED 产生 4 类目标失败；GREEN 后 token/source/TopBar/Settings/Screens/Practice 6 files / 62 tests PASS。根 `make test`（Python 615 / 4615 subtests、Go 全包、frontend 全量）、typecheck、production build、owner context、Header/INDEX 与 diff gate 通过。
- 正式 frontend 重建后，Chrome 在 Settings desktop `1264×964` / mobile `390×844` 验证保存、退出、注销和 dialog action 均为 `8px`；TopBar/Home/Workspace/Parse/Practice/Report/Resume 样本同样通过，两档无横向溢出且 warning/error 为 0。证据位于 `.test-output/ui-alignment/button-radius/`，不声明 E2E。

### 关联 Commit

- `fix(frontend): unify framed action button radius (BUG-0192)`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/002-app-shell-visual-system

## 01:46 工作记录

### 完成事项

- 将 Home 唯一 JD textarea 的默认可视高度从 `106px` 提升到 `212px`，保持 `width: 100%`；受控内容变化时先清除旧高度，再按最新 `scrollHeight` 自动增高或回缩，并通过 `overflow-y: hidden` / `resize: none` 完整展示当前粘贴内容。
- 原地修订 Home/JD UI design、spec、plan/checklist 与 BDD，新增 `BDD.HOME.JD.TEXTAREA.006`，明确空/短/长/删减四态、横向 containment、内部无滚动和不改变 byte limit、请求、route、storage、Resume/CTA 的合同；同步更新 [BUG-0192](../bugs/BUG-0192.md) 与既有[交付复盘](../reports/2026-07-19-core-workflow-reference-ui-alignment-assessment.md)。
- TDD RED 时 `HomeLayout` 5 项中 2 项按预期失败；GREEN 后 Home 9 files / 67 tests 与 typecheck 通过。production build、frontend redeploy、本地依赖 4/4、根 `make test`（Python 615 / 4615 subtests、Go 全包、frontend 134 files / 1088 tests）全部通过。
- Chrome 在真实 `1916×821` Home 验收：空值和删减后均为 `212px`；36 行 JD 自动增高到 `993px` 且 `clientHeight=scrollHeight`，textarea/外框为 `1346/1348px`，页面无横向溢出，计数器、Resume 与 CTA 顺序稳定，browser warning/error 为 0；证据位于 `.test-output/home-jd-textarea-acceptance/`。
- owner context、Header/INDEX、docs links、core-loop pruning 与 `git diff --check` 全部通过；Home/JD 文档生命周期已恢复 `completed`，没有新增重复 plan、Bug、report 或 E2E ID。

### 关联 Commit

- `fix(home): expand and auto-fit JD input (BUG-0192)`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-home-job-picks-and-parse/001-home-jd-import-and-parse

## 12:27 工作记录

### 完成事项

- 删除 Resume upload 主体中央 72px 上传图标，使虚线区域真实支持单文件 drag/drop；拖放与“选择文件”复用格式、大小、runtime、submitting、presign、browser PUT、register 和 direct-detail handoff，失败路径保持零副作用。
- 将独立 `fix/resume-a4-preview-0720` 快进合并到当前 create-flow 分支，解析 spec/INDEX 冲突为 v2.27：保留 `D-20` A4 preview geometry，并以 `D-21` 承接 drag/drop，避免重新部署上传改动时再次回退 PDF/Markdown 纸张。
- TDD 与组合回归通过：Resume Workshop 20 files / 124 tests、typecheck/build、根 `make test`（Python 621 tests / 4628 subtests、Go 全包、frontend 137 files / 1125 tests）、两个 owner context 和 docs/index/diff gates 均通过。
- Chrome 在真实 frontend/backend 上确认 desktop/mobile 上传区无 legacy icon、无横向溢出；PDF desktop `794×1123px` / mobile `358×506px` 且 canvas 无 inline presentation size；Markdown desktop 单页 `794×2003px` / mobile `358×3429px`，console warning/error 为 0。
- 新增 [BUG-0194](../bugs/BUG-0194.md) 记录 sibling branch 未集成导致 redeploy 恢复旧实现的根因。Chrome 扩展仍禁止本地 `setFiles`，因此 002 的 live valid-file handoff 与 lifecycle gate 保持未勾选，不虚报 PASS。

### 关联 Commit

- `feat(resume): add drag and drop uploads (BUG-0194)`

### 备注

- 本提交完成 Phase 15.1/15.2 和当前可执行的组合验收；Phase 15.3/15.4 因 Chrome 本地文件权限继续保持 active，用户已明确要求将当前分支合并回 `main`。

## 11:41 工作记录

### 完成事项

- 原地重开 Resume Workshop 001 Phase 27：PDF 页面采用 `794px` A4 宽度与 `210:297` 比例，canvas 去除 inline presentation width/height 后等比填满页框；Markdown 只采用相同纸宽，整份正文保持单个连续长页，不设置 A4 比例、固定高度或最小高度。
- TDD RED 分别捕获旧 `1150px` 纸宽、PDF canvas 内侧空白和 Markdown desktop/mobile 高度约束；最终 focused preview tests 10/10、Resume owner 20 files/118 tests、typecheck、production build 与根 `make test`（621 tests / 4628 subtests）通过。
- 真实 frontend/backend 重新部署且依赖 readiness 4/4；Chrome 验收 Markdown desktop `794 × 2003px`、mobile `358 × 3429px`，均只有一个 page node、无 A4 高度约束及横向溢出；PDF desktop/mobile 保持 A4 比例并由 canvas 填满可用页宽，console warning/error 为 0。
- 原地更新 Resume UI design、spec、plan/checklist 与 `BDD.RESUME.DETAIL.A4.007`，新增[交付复盘](../reports/2026-07-20-resume-a4-preview-assessment.md)，owner lifecycle 已恢复 `completed`；本次属于单一低严重度 UI 几何修订，不新增 Bug、API、fixture、persistence、route 或 E2E ID。

### 关联 Commit

- `fix(resume): align PDF and Markdown preview geometry`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-resume-workshop/001-listing-routing-and-detail-readonly

## 10:40 工作记录

### 完成事项

- 将账号级预设原地扩展为 Ocean / Plum / Forest，并保留 Custom；按确认的 light/dark OKLCH 矩阵更新三个预设的 accent/accent-soft，Forest 复用 Ocean 的中性与状态语义色。
- 同步 OpenAPI `AccountTheme`、generated Go/TS、fixture、backend service/store、`user_settings.theme` migration、frontend runtime/dev mock/Settings/i18n 与 `E2E.P0.101`；Forest 支持一次 `updateMe` 保存、零 follow-up GET、重登恢复，并继续拒绝 `warm` 和未知值。
- 原地更新 frontend-shell spec、001/002 plan/checklist/BDD、UI design、frontend README 与既有账号主题交付复盘；002 恢复 `completed`，001 仅因独立 Phase 15.3 保持 `active`，没有新增平行 plan、Bug 或 E2E ID。
- 当前收口验证通过：Header/INDEX 零漂移、docs/context/diff、fixture、migration lint、auth/migration focused Go、frontend production build，以及根 `make test`（Python 621 tests / 4628 subtests、Go 全包、frontend 137 files / 1119 tests）。一次性 PostgreSQL up/down/up 与真实 P0.101 证据已记录在 owner checklist/复盘；当前 shell 未配置 `DATABASE_URL`，未重复执行数据库命令。

### 关联 Commit

- `feat(settings): add forest account theme preset`

### 备注

- `make codegen-check` 提交前仅报告本次预期的 OpenAPI/generated diff；按收口合同在提交后重跑确认幂等。

## 09:37 工作记录

### 完成事项

- 为 Resume 与 Workspace 列表删除引入共享 `DestructiveActionDialog`：首次点击只打开确认框，覆盖取消、focus trap、Escape/遮罩关闭、焦点恢复、pending 锁定、失败保留与同一 idempotency key 重试；两个 consumer 继续使用既有软归档 API，并分别持有目标与成功后的本地投影。
- 修复列表页代码审查发现的三项合同漂移：Practice 启动过渡阻断完整 App root、Reports 将 `TargetJob.createdAt` 标注为规划创建日期、A5 `make docs-check` 新增 subject `spec.md` 内 `D-*` / `C-*` ID 唯一性 ratchet；范围外三组 legacy 重复保持显式 baseline，不掩盖新增重复。
- 原地修订 Resume、Workspace/Practice、Report 与 A5 owner 的 UI design、spec、plan/checklist、BDD/test 文档及 INDEX，并新增两份交付复盘；所有临时重开的 owner 文档已按当前验证事实恢复生命周期。
- 真实 Chrome 已在 Resume 与 Workspace 验收删除确认、取消保留、焦点恢复、桌面 containment 与截图 magic bytes；本次收口重新执行 `git diff --check`、`make docs-check` 与根 `make test`，通过 Python 620 tests / 4615 subtests、Go 全包及 frontend 137 files / 1117 tests。

### 关联 Commit

- `feat(frontend): harden list flows and review contracts`

### 备注

- 按用户选择将删除确认功能、列表页审查修复与 Spec ID gate 合并为一个原子提交；未纳入未被接受的卡片按钮嵌套修订，也未新增后端 API、持久化或 E2E 编号。

## 01:29 工作记录

### 完成事项

- 优化共享 TopBar 语言入口：用 20×20 可见底板内的 14×14 code-native SVG chevron 取代 9px 文本 `▾`，菜单展开时旋转 180°，保留本地化 accessible name、键盘交互和移动端 containment。
- 设置入口改为消费 `AppShell` 已持有的 authenticated `displayName`，trim 后取首个 Unicode 字符；拉丁字母按 locale 大写，空名称显示 `?`。没有新增 `/me` 请求、账号菜单或完整姓名暴露。
- 原地修订 frontend-shell/001 Phase 22、UI design、spec、BDD、[BUG-0192](../bugs/BUG-0192.md) 与既有[交付复盘](../reports/2026-07-19-core-workflow-reference-ui-alignment-assessment.md)，将品牌标识与账号身份、字体符号与交互 chevron 的语义边界固化为可执行合同。
- TDD RED 最终为 4 个文件 43 项中的 11 项预期失败；GREEN 后 focused 43/43、typecheck、production build、frontend redeploy、本地依赖 4/4 和根 `make test`（Python 615 / 4615 subtests、Go 全包、frontend 134 files / 1087 tests）通过。
- Chrome 在真实 `1920px` 页面确认 `displayName=星期无` 对应设置标记 `星`；chevron 尺寸、展开旋转、42×42 设置入口、`/settings` 直达、无横向溢出和 0 warning/error 全部通过。owner context、Header/INDEX、docs links 与 diff gate 同步通过。

### 关联 Commit

- `fix(topbar): clarify language and account controls (BUG-0192)`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/001-app-shell-auth-settings

## 03:19 工作记录

### 完成事项

- 按修订后的报告生成 UI 删除 `AsyncTransitionScene` 的单消费者 `card` 分支、Generating card testid 与 desktop/mobile 白色 surface CSS；报告等待内容直接落在共享氛围画布，真实 queued/generating、polling、ready handoff、失败恢复、TopBar 和 reduced-motion 语义保持不变。
- 新增共享 `common.back`，将 14 个 Auth/Parse/Resume/Practice/Reports/Generating/Report/Conversation 返回消费者统一为中文“返回”、英文“Back”，删除 20 个无消费者目标特定 locale key；原 target、push/replace、trusted-context 和 fail-closed 行为继续由各 owner 测试承接。
- TDD 分别完成 cardless RED/GREEN（2 项目标失败后 12/12 PASS）与 Back copy RED/GREEN（16/16 失败后 16/16 PASS）；受影响 owner 回归 65 files / 495 tests、typecheck、production build和根 `make test`（Python 615 / 4615 subtests、Go 全包、frontend 136 files / 1107 tests）通过。
- frontend 重新部署且环境 readiness 4/4。Chrome 通过两条最小真实面试完成链路捕获短暂 Generating：desktop `1512×777` 和 exact mobile `390×844` 均为透明内容层、0 边框/阴影/局部毛玻璃、无旧 card class/testid、无横向溢出与 0 warning/error；Report/Reports 的“返回 / Back”和可信目标回跳通过。
- 原地更新 [BUG-0192](../bugs/BUG-0192.md)、既有[交付复盘](../reports/2026-07-19-core-workflow-reference-ui-alignment-assessment.md)、UI design、Report/Shell spec/plan/checklist/BDD；Report owner 恢复 `completed`，Shell 因既有 Auth Phase 15.3 继续保持 `active`。两个 owner context、Header/INDEX、docs links 与 diff gate 全部通过。

### 关联 Commit

- `fix(frontend): align report generating and back copy (BUG-0192)`

### 备注

- Chrome 验收在本地开发数据中新增了两条最小回答的已完成面试及报告，分别用于捕获真实生成中窗口；截图位于 `.test-output/ui-alignment/report-generating-cardless/`，该 scoped 证据不声明完整 `E2E.P0.099`。

## 03:44 工作记录

### 完成事项

- 采用“发送动作位于内层输入表面内”的方案：textarea 与 send 共享一个有边界的 `practice-input-surface`，按钮在底部独立 action area 右对齐；输入正文保持全宽，按钮不再悬在内外边框之间，也不会覆盖文本。
- 首轮 TDD RED 暴露缺少统一 input surface；首轮 Chrome overlay 方案虽在桌面可用，但 `126px` 右内边距压缩了 `390px` 窄屏正文，因此立即将合同修订为非叠加 action area，并新增拒绝 absolute overlay、正文预留宽度与双重 focus ring 的负向断言。
- focused 回归 3 files / 55 tests、production build、frontend redeploy、本地依赖 4/4 和根 `make test`（Python 615 / 4615 subtests、Go 全包、frontend 136 files / 1109 tests）全部通过。
- Chrome 在真实 active Practice 会话完成 desktop `1512×829` 与 exact mobile `390×844` 验收：textarea/send 同属 surface、按钮完整内含、textarea/action 无重叠、正文使用完整可用宽度、helper 间距稳定、无横向溢出和 browser error；textarea 清除自身 outline，由 surface 提供唯一 focus ring。
- 原地更新 [BUG-0192](../bugs/BUG-0192.md)、既有[交付复盘](../reports/2026-07-19-core-workflow-reference-ui-alignment-assessment.md)、Practice UI design 与 frontend-workspace-and-practice/002 的 spec/plan/checklist/test/BDD；owner lifecycle 已恢复 `completed`，未新增重复 plan、Bug、report、API 或 E2E ID。
- owner context、post-pass spec/plan/code reconcile、Header/INDEX、docs links 与 `git diff --check` 全部通过。

### 关联 Commit

- `fix(practice): align send action surface (BUG-0192)`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-workspace-and-practice/002-practice-text-event-loop

## 07:57 工作记录

### 完成事项

- 恢复 Report Generating 的 target-aware 返回文案：trusted Reports destination 在正常等待态和 typed error state 显示“返回面试报告 / Back to interview reports”，Workspace fallback 与其它二三级页面继续使用共享“返回 / Back”；既有 target、push/replace、polling、retry、ready handoff 与 fail-closed 导航矩阵不变。
- 按最新简历详情设计移除 `ResumePreviewTab` 的空 `className` 和 `ei-resume-detail-preview-card` desktop/mobile/后代 CSS，删除旧背景、尺寸、阴影正向断言并增加 legacy selector 零残留 gate；Markdown page、PDF stack、正文与 source endpoint 行为保持不变。
- Chrome 首轮发现删除视觉 wrapper 后短内容会把透明 `article` 和 Markdown page 收缩到约 497px；补充 follow-up RED/GREEN，只用 `width: 100%` 接管透明布局，最终外层/纸张为 1443/1150px，且无 class、背景、边框、圆角、阴影或 padding，横向溢出与 browser warning/error 均为 0。
- 最终 Generating/i18n focused 6 files / 72 tests、Resume focused 14/14、Resume owner 20 files / 118 tests、typecheck、production build、两次根 `make test`（均为 Python 615 / 4615 subtests、Go 全包、frontend 全量）、frontend redeploy 与环境 readiness 4/4 通过。
- 原地更新 Report、Resume、Shell 的 UI design/spec/plan/checklist/BDD、[BUG-0192](../bugs/BUG-0192.md) 与既有[交付复盘](../reports/2026-07-19-core-workflow-reference-ui-alignment-assessment.md)；Report/Resume owner 恢复 `completed`，Shell 因既有 Auth Phase 15.3 继续保持 `active`。三个 context、Header/INDEX、docs links 与 diff gate 通过。

### 关联 Commit

- `fix(frontend): restore report back copy and remove resume backdrop (BUG-0192)`

### 备注

- 真实 Generating 约三秒后自动 ready handoff：Chrome 已捕获精确文案，但未把未可靠执行的 live click 宣称为通过，destination 由 component navigation tests 承接。`screens.css` 中既有 Practice 输入区尺寸改动不属于本次提交，已保留在工作区。

## 08:24 工作记录

### 完成事项

- 原地重开 Home/JD owner Phase 29：已认证用户成功加载且没有可展示面试规划时，不再渲染“最近模拟面试”整个区块；loading、用户安全的 error 与 1～3 条记录保持既有反馈、导航和 quick-start 行为。
- TDD RED 在旧实现上唯一失败并捕获 section、标题、说明和空卡片残留；GREEN 后 `HomeRecentMocks.test.tsx` 14/14、Home owner 9 files / 68 tests 通过，新增 deferred loading-to-empty 边界防止 loading 被一并隐藏。
- 根 `make test` 通过 Python 615 tests / 4615 subtests、Go 全包与 frontend 136 files / 1113 tests；frontend typecheck、production build、owner context、Header/INDEX、docs links 与 diff gate 全部通过。
- 原地更新 UI design、spec、plan/checklist 与 `BDD.HOME.RECENT.EMPTY.007`，owner lifecycle 已恢复 `completed`；本次属于用户确认的 feature revision，不新增 Bug、API、fixture、persistence、route 或 E2E ID。
- 新增[交付复盘](../reports/2026-07-20-home-empty-recent-section-assessment.md)：低置信度 matcher 误排序已由现有 live-search 规则正确兜底，不需要立即修改治理资产。

### 关联 Commit

- `feat(home): hide empty recent interview section`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-home-job-picks-and-parse/001-home-jd-import-and-parse

## 08:50 工作记录

### 完成事项

- 原地重开 Home/JD owner Phase 30：简历下拉业务选项只显示 `displayName || title`，移除语言、来源、更新时间和摘要拼接；selectable predicate、更新时间排序、option value、选择状态与 import request 保持不变。
- TDD RED 精确捕获旧文本 `Zhang San - Backend Engineer · zh-CN · upload · 2026-07-19`；GREEN 后 `HomeResumeSelection.test.tsx` 7/7，通过 displayName 优先、title fallback 与四类元信息负向断言。
- `BDD.HOME.RESUME.OPTION.008` 的 selection/import owner behavior tests 16/16 通过；根 `make test` 通过 Python 615 tests / 4615 subtests、Go 全包与 frontend 全量测试，typecheck、production build、owner context、Header/INDEX、docs links、pruning、scoped source search 与 diff gate 全部通过。
- 原地更新 UI design、spec、plan/checklist/BDD，并新增[交付复盘](../reports/2026-07-20-home-resume-option-labels-assessment.md)；owner lifecycle 已恢复 `completed`。本次属于用户确认的 feature revision，不新增 Bug、API、fixture、backend、persistence、route、E2E ID 或浏览器验收。

### 关联 Commit

- `feat(home): simplify resume option labels`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-home-job-picks-and-parse/001-home-jd-import-and-parse
