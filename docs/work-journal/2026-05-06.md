# 2026-05-06 工作日志

## 10:27 工作记录

### 完成事项

- 按 `/plan-code-review mock-contract-suite --fix` 的确认结果，完成 mock contract runtime gate remediation：`lint-mock-contract` 接入 operation registry metadata unittest，并用 Makefile dry-run test 锁住聚合门禁。
- 修复 Go OpenAPI server 生成模板的旧 `36-row` operation count，改为由 `len .Operations` 动态渲染并重新生成 `server.gen.go`。
- 补强前端 fixture-backed mock transport 回归测试，覆盖 `Prefer: example=<scenario>` named scenario 选择和 unknown scenario fail-loudly。
- 原地修订 `mock-contract-suite/001-fixture-backed-mock-runtime` plan / checklist / context，新增并完成 4.3 L2 remediation；创建 [BUG-0010](../bugs/BUG-0010.md) 与交付复盘报告。
- 完成验证：focused Python / Go / frontend tests、`make lint-mock-contract`、`make lint`、`make test`、`make docs-check`、临时 Git index `make codegen-check` 与 `git diff --check` 均通过。

### 关联 Commit

- `fix(mock-contract): harden runtime drift gates`

### 备注

See [BUG-0010](../bugs/BUG-0010.md). 裸 `make codegen-check` 在未提交 generated diff 下会按预期停在 `server.gen.go` diff；已用临时 Git index 将当前补丁作为基线验证 codegen 幂等。

## 11:47 工作记录

### 完成事项

- 按 `$plan-code-review mock-contract-suite --fix` 的复扫发现，修复 `lint-mock-contract` 未覆盖空旧 fixture tag 目录的问题。
- 在 `mock_runtime_boundary` lint 中增加 fixture tag directory set gate，从 `openapi/openapi.yaml` 的当前 12 tag 派生允许目录集合，拒绝额外或缺失目录。
- 删除工作区残留的空 `openapi/fixtures/Growth` 与 `openapi/fixtures/Mistakes` 目录，并新增 Red-Green 测试确保空旧目录也会失败。
- 原地修订 `mock-contract-suite` spec / plan / checklist，新增并完成 4.4 L2 remediation；创建 [BUG-0011](../bugs/BUG-0011.md) 与交付复盘报告。
- 完成验证：focused boundary tests、`make lint-mock-contract`、`make codegen-check`、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `fix(mock-contract): reject retired fixture tag dirs`

### 备注

See [BUG-0011](../bugs/BUG-0011.md). 这次修复把 Git 不跟踪的空目录也纳入 contract gate，避免 retired tag 目录仅靠人工 filesystem 检查暴露。

## 13:25 工作记录

### 完成事项

- 按 `$plan-review ai-provider-and-model-routing/plans/002-tools-streaming-and-stt --fix` 完成 A3 002 L1 文档修复。
- 将 002 的下游 owner 口径从旧 C/D shorthand 对齐到当前 roadmap subject / future candidate 命名。
- 在 A3 spec / plan / checklist 中明确 F1 `task_type` label 迁移必须由 F1 owner 先承接，避免 active-scope negative search 把其他 owner 未迁移文档误判为 A3 pass。
- 固化 draft plan 的 `/implement` 候选阻断规则，并把 context specVersion 调整为 v2.4 -> v2.5 激活窗口。
- 完成验证：context validator、`sync-doc-index --check`、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `docs(ai-provider): harden 002 activation gates`

### 备注

`/implement` no-arg 当前仍会列出 draft plan；本次在 002 文档中固化阻断规则，后续应修复 implement candidate filter / lifecycle hard-stop。

## 13:45 工作记录

### 完成事项

- 按用户明确确认的“方案 B”，归档 A3 002 提前激活触发证据：AI Tools / provider-side streaming / STT 底座提前落地，避免后续 backend-practice、backend-debrief、production voice 与 F3 接入返工。
- 原地修订 ADR-Q6 到 v2.0，保留 provider-neutral、零厂商 SDK、AIClient 唯一入口和隐私红线，并把 Complete tools payload、streaming SSE consumer 与 Audio Transcriptions 纳入当前 OpenAI-compatible 子集。
- 原地修订 A3 spec / history 到 v2.5，将 002 plan / checklist 切换为 active v1.0，并通过 context validator、`sync-doc-index --fix-index` 与 `make docs-check` 验证文档激活闭环。

### 关联 Commit

- `docs(ai-provider): activate tools streaming stt plan`

### 备注

Realtime multimodal、TTS、媒体留存、UI voice release gate、rerank 与 judge 仍保持 fail-closed 或归后续 owner plan。

## 14:00 工作记录

### 完成事项

- 完成 A3 002 Phase 2 Tools / function calling 底座：`CompletePayload` 增加 provider-neutral `tools[]` / `tool_choice`，`CompleteResponse` 增加 `tool_calls[]`。
- openai_compatible adapter 映射 OpenAI Chat Completions tool wire，并解析 provider `tool_calls`；stub provider deterministic replay 覆盖带 tool payload。
- `AICallMeta` 增加 tool invocation 摘要字段，只保存 tool name、arguments hash 与长度；B1 AI vocabulary 同步新增 `tool_invocations` / `partial_meta_reason` 并重新生成 Go / TS 共享常量。
- 完成验证：focused Red-Green tests、`cd backend && go test ./internal/ai/aiclient/... -count=1`、`python3 scripts/lint/conventions_drift.py --repo-root .` 均通过。

### 关联 Commit

- `feat(ai-provider): add provider-neutral tool calls`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-provider-and-model-routing/002-tools-streaming-and-stt

## 14:14 工作记录

### 完成事项

- 完成 A3 002 Phase 3 provider-side streaming consumer：openai_compatible adapter 直接消费 OpenAI-compatible SSE `data:` frames，并映射为 plan 001 锁定的 `delta` / `error` / `done` channel 事件。
- 补齐 streaming context cancellation partial meta：取消后产出带 `AI_PROVIDER_TIMEOUT`、`validation_status=invalid`、`partial_meta_reason=context_cancelled` 与已收到增量 token 估算的 terminal done event。
- mockserver 增加 streaming chunk 与分块延迟能力，契约测试覆盖多 chunk、malformed chunk、provider error event、done event 与 cancellation partial terminal。
- 原地修订 A3 spec / history 到 v2.6，锁定 provider-side SSE consumer 决策，并明确业务 HTTP wire 继续由 backend / frontend owner 在自身 API plan 决定；同步完成 checklist 3.1-3.3。
- 完成验证：focused streaming tests、`cd backend && go test ./internal/ai/aiclient/... -count=1`、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `feat(ai-provider): consume provider streaming events`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-provider-and-model-routing/002-tools-streaming-and-stt

## 14:45 工作记录

### 完成事项

- 完成 A3 002 Phase 4 STT 底座：`AIClient` / provider interface 新增 `Transcribe`，输入契约固定为 audio bytes + filename + content type + optional language/prompt，stub provider 提供 deterministic transcript。
- openai_compatible adapter 落地 `/v1/audio/transcriptions` multipart wire；mockserver 与 contract tests 覆盖 multipart happy path、provider error、missing text 输出非法、secret missing 与 realtime profile fail-closed。
- `config/ai-providers.yaml` 将 `default-openai-compatible` 扩展为 `chat/embed/stt`，`practice.dictation.stt.default` 与 `debrief.voice.extract.default` 升为 active；`practice.voice.realtime.default` 保持 unsupported。
- 完成 Phase 5 owner handoff：F1 AI metric labels 迁移到 `capability` / `model_profile_name` / `route`，F3 记录 provider-neutral `tools[]` / `output_schema` / `stream_wire` hints，B1 codegen/parity fixture 与前端 AI vocabulary tests 同步。
- Phase 6 可执行 gate 已收口：A3 AC C-13/C-14/C-15 覆盖 tools/stream/STT；`make test`、`make lint`、`make codegen-check`、`make docs-check`、`git diff --check` 均通过；active-scope negative search 无旧口径 active truth source 命中。

### 关联 Commit

- `feat(ai-provider): add stt transcription adapter`

### 备注

`6.3 本地部署 + Kind 场景 smoke` 未完成：当前 `test/scenarios/e2e/INDEX.md` 只有 Planned 场景，且没有 `env-setup.sh` / `env-verify.sh`、Kind 部署入口、Ready/Verified AI tool/stream/STT 场景或 consuming business flow。已在 checklist 中记录 blocker，未伪造通过。

## 15:30 工作记录

### 完成事项

- 按 `$plan-code-review ai-provider-and-model-routing/002-tools-streaming-and-stt --fix` 复扫，修复 L2 发现的工具调用、streaming 终态与 observability label 三个运行时语义缺口。
- Stub provider 把 tools / tool_choice 纳入 deterministic seed，带 tool payload 时回放结构化 `ToolCalls` 并写入 `AICallMeta.ToolInvocations`（仅保存 name + arguments hash + 长度，不落明文）。
- `Client.Stream` 包装 provider stream，对 terminal `done.Meta` 重新执行 canonical merge；merge 失败时返回 `AI_OUTPUT_INVALID` stream error，覆盖 normal done 与 cancellation partial done。
- Observability decorator 在 complete / embed / transcribe failure 与 stream done / error 路径上消费 `ProfileResolver`，从 call meta 补齐 provider/model/profile/capability/route 与 profile version label，可解析 profile 不再落到 `unknown`。
- 原地修订 002 plan/checklist 到 v1.1，新增并完成 2.4 / 3.4 / 5.4 三项 L2 remediation gate；同步更新 `plans/INDEX.md`、创建 [BUG-0012](../bugs/BUG-0012.md) 与交付复盘报告。
- 完成验证：focused stub / client / observability Red-Green tests、`go test ./internal/ai/aiclient/... -count=1`、`make lint`、`make test`、`make codegen-check`、`make docs-check`、`git diff --check`、`sync-doc-index --check` 均通过。

### 关联 Commit

- `fix(ai-provider): remediate tools streaming stt L2 findings`

### 备注

See [BUG-0012](../bugs/BUG-0012.md). plan 002 仍保持 active：`checklist.md` 6.3 本地部署 + Kind 场景 smoke 仍 blocked，待 scenario owner 补 Ready 资产后再收口。

## 16:47 工作记录

### 完成事项

- 按 `$plan-review spec/frontend-shell --fix` 完成 `frontend-shell/001-app-shell-auth-settings` L1 文档修复。
- 将 D1 shell 范围补齐到 `parse` route shell、runtime config / generated client bootstrap、fixture-backed mock transport 与 `/me` session bootstrap。
- 收紧 auth contract gate：正式前端只允许消费 `startAuthEmailChallenge`、`verifyAuthEmailChallenge`、`getMe`、`logout` 和 first-party session cookie；密码 / OAuth / reset 仅保留 UI shell 或 stub。
- 将 `DESIGN.md` 只读参考边界写入 plan / checklist，明确不机械同步 token、不引入私有授权字体，验收仍以 `docs/ui-design/` 与 `ui-design/` 为准。
- 同步 frontend-shell spec / plan / checklist / BDD / context 与 spec / plans INDEX 到 v1.1，并完成验证：context validator、`sync-doc-index --check`、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `docs(frontend-shell): remediate app shell plan review gates`

### 备注

本次为 docs-only L1 remediation，未修改前端代码；后续进入实现时应从 `/implement frontend-shell/001-app-shell-auth-settings frontend` 开始。

## 17:56 工作记录

### 完成事项

- 按 `$plan-review docs/spec/backend-auth --fix` 的确认结果，完成 `backend-auth/001-passwordless-session-bootstrap` L1 文档修复。
- 将 C1 passwordless session plan 对齐到 B2 generated Auth surface，补齐 `deleteMe` auth handoff、protected session middleware 与 public/protected endpoint gate。
- 接入 B3/C8 `email_dispatch` redaction 口径，明确 C1 必须使用 generated `BuildEmailDispatchPayload`，禁止 raw token、完整 URL、邮箱明文和邮件正文进入 outbox / async payload / log / audit。
- 固化 ADR-Q1 15 分钟 challenge TTL、30 天 session TTL、1 分钟第 3 次 rate-limit / dedupe、B4 `sessions.updated_at` renewal 边界，以及 F1 auth metrics / audit 红线。
- 扩展 `E2E.P0.003` BDD 文档，覆盖 runtime-config allowlist、重复 logout、无效 / 重复 token、logout 后 `/me` 与隐私证据断言。
- 完成验证：context validator、`sync-doc-index --check`、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `docs(backend-auth): remediate passwordless plan review gates`

### 备注

本次为 docs-only L1 remediation，未修改后端代码；后续进入实现时应从 `/implement backend-auth/001-passwordless-session-bootstrap backend` 开始。

## 18:09 工作记录

### 完成事项

- 完成 `backend-auth/001-passwordless-session-bootstrap` Phase 1 Storage and config boundaries。
- 新增 C1 `internal/auth` store surface 与 SQL boundary tests，锁定 `users` / `user_settings` / `auth_challenges` / `sessions` 表边界，确认 P0 不暴露 `external_identities` store 方法。
- 固化 auth 常量：`ei_session` cookie name、15 分钟 challenge TTL、30 天 session TTL、1 分钟第 3 次 rate-limit / dedupe 与 dev mail sink 默认值，未新增 A4 runtime config key。
- 使用 B2 generated `AllRoutes` 锁定 C1/A4 session policy：auth start / verify / runtime-config public，logout optional，其余 operation protected。
- 完成验证：`go test ./internal/auth -count=1`、focused config fail-fast tests 均通过。

### 关联 Commit

- `feat(backend-auth): storage and config boundaries`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-auth/001-passwordless-session-bootstrap

## 21:30 工作记录

### 完成事项

- 按 passwordless session bootstrap 的 L2 review findings，完成 `backend-auth/001-passwordless-session-bootstrap` Phase 6 remediation。
- 修复真实 `cmd/api` Auth runtime wiring：挂载 start / verify / logout / `/me` / `DELETE /me` routes，并让 `/runtime-config` 使用 session-aware resolver。
- 修复 `DELETE /me` idempotency 跨用户同 key 复用、session cookie Secure policy、challenge rate-limit SQL scope、logout revoke failure response、runtime auth secret fail-fast、optional logout resolver/store error 分类，以及 logout revoke race 的 `TouchSession` zero-row 归类。
- 新增 route / store / handler / middleware regression tests，覆盖空 auth secret、resolver store error、known auth-state optional logout、touch/revoke TOCTOU race、cross-principal dedupe 与 cookie policy。
- 原地修订 Phase 6 plan / checklist，创建 [BUG-0013](../bugs/BUG-0013.md) 与交付复盘报告，补齐同类 session contract drift 的后续检查建议。
- 完成验证：focused backend tests、`cd backend && go test ./cmd/api ./internal/auth -count=1`、`cd backend && go test ./... -count=1`、`make lint`、`make codegen-check`、`make docs-check`、`sync-doc-index --check`、`git diff --check` 均通过。

### 关联 Commit

- `fix(backend-auth): remediate passwordless L2 findings`

### 备注

See [BUG-0013](../bugs/BUG-0013.md). 本次提交合并 backend 代码与同一 owner plan 的 BUG / retrospective / checklist 记录，保持修复证据与代码同源提交。

## 18:31 工作记录

### 完成事项

- 完成 `backend-auth/001-passwordless-session-bootstrap` Phase 3 Verify, session, and current user。
- 实现 `verifyAuthEmailChallenge`：按 token hash 消费一次性 challenge，创建/读取用户，写入 server-side `session_hash`，通过 `Set-Cookie` 签发 opaque `ei_session`。
- 实现 session resolver / middleware：required operation 缺失、无效、revoked、expired session 返回 B1 `AUTH_UNAUTHORIZED` envelope，active session 续期 `sessions.updated_at` / `expires_at`。
- 实现 `/me` masked user context、idempotent logout 清 cookie与 session revoke，以及 `DELETE /me` C1 auth handoff。
- `DELETE /me` 返回 B2 `202 + PrivacyRequestWithJob` 兼容响应，按 `Idempotency-Key` 交给 store 复用 active `privacy_delete` handoff；实际 privacy_delete worker / 删除矩阵未进入 C1。
- 完成验证：focused verify / middleware / getMe / logout / deleteMe tests 与 `go test ./internal/auth -count=1` 通过。

### 关联 Commit

- `feat(backend-auth): verify session and current user`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-auth/001-passwordless-session-bootstrap

## 18:19 工作记录

### 完成事项

- 完成 `backend-auth/001-passwordless-session-bootstrap` Phase 2 Challenge issue and delivery。
- 实现 `startAuthEmailChallenge` handler/service：请求返回 B2 `202`，challenge token/IP/UA 只写入 hash，dev mail sink retrieval 可生成一次性 magic link。
- 落地同邮箱或同 IP 1 分钟第 3 次请求的 rate-limit / dedupe 基线，重复请求不创建 challenge、不派发邮件且不泄露账号存在性。
- 接入 B3 generated `BuildEmailDispatchPayload`，auth service 只产出 allowed payload 字段；redacted field negative tests 覆盖 raw token、magic link URL、recipient email 与 email body。
- 实现 C1 backend-internal goroutine dispatcher：handler 入队不等待 provider，shutdown drain 队列，失败摘要不包含 token / 邮箱 / URL。
- 完成验证：focused auth/service/dispatcher tests、`go test ./internal/auth -count=1`、`go test ./internal/shared/jobs -run TestBuildEmailDispatchPayload -count=1` 均通过。

### 关联 Commit

- `feat(backend-auth): challenge issue and delivery`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-auth/001-passwordless-session-bootstrap

## 19:03 工作记录

### 完成事项

- 完成 `backend-auth/001-passwordless-session-bootstrap` Phase 4 Runtime config resolver, privacy, and observability。
- 接入 A4 `/runtime-config` session-aware resolver：未登录保持 public response，有效 session 只影响 allowlist 内用户级偏好。
- 补齐 auth 隐私红线测试，确认 dev mail sink、dispatcher failure summary、verify response、metric / audit observable surface 不泄露 magic token、session cookie、完整邮箱、secret、URL 或 session id。
- 按方案 A 原地修订 F1 `observability-stack` spec / history 到 v1.7，登记 `auth_challenge_started_total`、`auth_session_minted_total`、`auth_logout_total`、`auth_delete_handoff_total`、`auth_failure_total` 及 allowed labels。
- 实现 C1 auth metrics registry 与 audit event 最小层，challenge started / session minted / logout / delete handoff / failure audit 只记录 ID / hash / status / trace。
- 完成验证：focused auth observability tests、`go test ./internal/auth -count=1`、`go test ./... -count=1`、F1 registry preflight、privacy grep、`make docs-check` 均通过。

### 关联 Commit

- `feat(backend-auth): runtime config resolver, privacy, and observability`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-auth/001-passwordless-session-bootstrap

## 19:15 工作记录

### 完成事项

- 完成 `backend-auth/001-passwordless-session-bootstrap` Phase 5 BDD and handoff。
- 为 `E2E.P0.003` 创建 scripted scenario 目录、数据、expected outcome 与 setup / trigger / verify / cleanup 脚本，并将 `test/scenarios/e2e/INDEX.md` 中该场景切为 Ready。
- 新增 focused BDD test，串起 start challenge、backend-internal dev mail sink retrieval、verify、`/me`、`/runtime-config`、logout / repeated logout、logout 后 `/me`、repeated `DELETE /me` idempotency 与隐私证据断言。
- 完成 scenario-run 串行验证：setup / trigger / verify / cleanup 均通过，并记录本地 run artifact 到 `.test-output/runs/20260506T1911-backend-auth-p0-003/e2e/E2E.P0.003/result.json`。
- 在 package docs 中完成 frontend-shell handoff，说明 generated Auth API、`ei_session` cookie、dev mail sink、B1 `AUTH_UNAUTHORIZED` 错误 envelope 与 pendingAction owner 边界。
- 完成 active-scope 负向搜索，确认 C1 auth / API wiring 未引入 Bearer token P0 主认证、OAuth / SSO P0、`external_identities` P0 store、log-only magic token delivery、独立 C8 worker 前置或旧 AI gateway / voice route 口径。
- 完成验证：scenario scripts、focused BDD test、`go test ./internal/auth -count=1`、`go test ./... -count=1`、context validator、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `feat(backend-auth): bdd and handoff`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-auth/001-passwordless-session-bootstrap

## 22:17 工作记录

### 完成事项

- 完成 `backend-runtime-topology/001-worker-consolidation`：新增 owner spec / plan / checklist / context，并将计划生命周期收口到 completed。
- 删除独立 `backend/cmd/worker` 入口和 `WORKER_LISTEN_ADDR` / `worker.listenAddr` 配置，更新 A4 env dictionary、validator、lint allowlist、dev-stack 与相关 handoff 文档。
- 将 B3 event producer 从 `worker` 改为 `backend_async`，同步 `shared/events.yaml`、Go/TS generated envelope、JSON Schema、baseline、fixture 和 event inventory tests。
- 固化开发期观测边界：A2/F1 默认 dev gate 只依赖应用 `/metrics`、日志和内存 registry，不要求 Prometheus/Grafana/OTel/Loki 实例。
- 完成验证：`make lint-config`、`make lint-events`、focused backend/frontend tests、context validator、`sync-doc-index --check`、`make lint`、`make test`、`make codegen-check`、`make docs-check`、`git diff --check` 与 `git diff --cached --check` 均通过。
- 创建交付复盘报告：[Backend Runtime Topology Worker Consolidation](../reports/2026-05-06-backend-runtime-topology-worker-consolidation-assessment.md)。

### 关联 Commit

- `refactor(runtime): consolidate backend worker topology`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-runtime-topology/001-worker-consolidation

## 23:11 工作记录

### 完成事项

- 完成 `backend-runtime-topology/001-worker-consolidation` L2 remediation，修复 retired standalone worker process 口径在 active code comments、completed owner plan/checklist 与质量门禁中的残留。
- 新增 `scripts/lint/runtime_topology.py` 与 `scripts/lint/runtime_topology_test.py`，将 `cmd/worker`、worker producer enum、worker 类组件 probe、privacy worker、Asynq worker 等旧口径固化为 executable negative lint。
- 在 `Makefile` 新增 `lint-runtime-topology` 并接入 `make lint`，确保后续聚合 lint 会扫描 active runtime/code/config/deploy/spec handoff surface。
- 修订 backend 注释及 event/outbox、secrets/config、local-dev-stack、db migrations、AI provider routing 等 owner plan/checklist 中的旧 worker handoff 语义。
- 将 backend-runtime-topology spec/plan/checklist/context 提升到 v1.1，新增 C-5 acceptance 与 Phase 4.3 evidence，并同步 spec / plans INDEX。
- 创建 [BUG-0014](../bugs/BUG-0014.md) 与交付复盘报告：[Backend Runtime Topology L2 Remediation](../reports/2026-05-06-backend-runtime-topology-l2-remediation-assessment.md)。
- 完成验证：`python3 -m unittest scripts.lint.runtime_topology_test`、`make lint-runtime-topology`、`make lint`、context validator、`sync-doc-index --check`、`make docs-check`、`make test`、`make codegen-check`、`git diff --check && git diff --cached --check` 均通过。

### 关联 Commit

- `fix(runtime): harden worker topology drift gate`

### 备注

See [BUG-0014](../bugs/BUG-0014.md). 本次提交合并 lint gate、active handoff cleanup、owner plan v1.1、Bug 记录与复盘报告，保持 L2 修复证据与门禁同源提交。

## 23:41 工作记录

### 完成事项

- 完成 `backend-runtime-topology/001-worker-consolidation` false-negative gate remediation，补强 `scripts/lint/runtime_topology.py` 对 `` `worker` producer``、`app/worker listen addr`、`worker bindings` 与 `backend-async-runtime` shorthand 的拦截。
- 为 runtime topology lint 新增 Red-Green 单测 fixture，确认旧口径同义语序和旧 future subject shorthand 不再能绕过 `make lint-runtime-topology`。
- 修订 B3 event/outbox、A4 secrets/config 与 ADR-Q3 active handoff 文案，将旧 `worker` / `backend-async-runtime` 口径对齐到 `backend_async`、`app listen addr` 与 `backend-async-runner`。
- 将 backend-runtime-topology spec / history / plan / checklist / context 提升到 v1.2，新增并完成 Phase 4.4 evidence，同步 spec / plans INDEX。
- 创建 [BUG-0015](../bugs/BUG-0015.md) 与交付复盘报告：[Backend Runtime Topology False-negative Gate](../reports/2026-05-06-backend-runtime-topology-false-negative-gate-assessment.md)。
- 完成验证：`python3 -m unittest scripts.lint.runtime_topology_test`、`make lint-runtime-topology`、`make lint`、`make test`、`make codegen-check`、`make docs-check`、context validator、`sync-doc-index --check`、`git diff --check` 均通过。

### 关联 Commit

- `fix(runtime): harden worker topology false-negative gate`

### 备注

See [BUG-0015](../bugs/BUG-0015.md). 本次提交补齐上一轮 runtime topology lint 的 false-negative 覆盖，并把 active handoff 文案与 executable gate 同源收口。
