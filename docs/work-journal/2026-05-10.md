# 2026-05-10 工作日志

## 00:55 工作记录

### 完成事项

- 完成 `backend-practice/001-plan-and-session-orchestration` L2 code review remediation。
- 修复 `startPracticeSession` 首题解析与 F3 `practice.session.first_question` JSON schema 的字段漂移，拒绝非 JSON 或缺少 question 的 AI 输出。
- 修复 `startPracticeSession` success replay，从 `idempotency_records.response_body` 返回首次成功 response snapshot，避免读取 mutable session 当前状态。
- 修复 `000003_practice_idempotency_baseline.down.sql`，避免 rollback 删除 baseline-owned `idempotency_records` table / index。
- 原地更新 plan/checklist v1.1，新增并完成 `0.11` / `1.10` / `2.10` remediation gate，创建 [BUG-0033](../bugs/BUG-0033.md) 和交付复盘报告。
- 验证通过：backend practice 相关 Go 包、fixture/codegen/event lint、migration/preflight/legacy lint、`E2E.P0.022` ~ `E2E.P0.026` scenario、文档索引和 markdown link 检查。

### 关联 Commit

- `fix(backend-practice): remediate session orchestration L2 findings`

### 备注

- 本次提交保留为单一逻辑边界：runtime bugfix、migration ownership fix、plan/bug/report/journal 证据同步。

## 08:20 工作记录

### 完成事项

- 修复 `/change-intake` 分支前置门禁缺失：入口 skill 在任何 spec / plan / checklist / docs / bug/report/journal 写入前必须确认当前分支与 dirty worktree 状态。
- 将当前会话已经落在 `main` 的未提交改动转移到 `fix/frontend-dev-mock-runtime`，并记录恢复规则：当前会话 dirty 可保留切分支，来源不明的 dirty 必须先询问用户。
- 在 `AGENTS.md` 补充全局 Git 分支策略，覆盖 `/change-intake`、`/plan-review --fix`、`/plan-code-review --fix`、`/bug-report`、`/retrospective`、`/create-doc` 等会写文件的入口。
- 新增 `/change-intake` contract test，防止未来移除 branch guard 文案。
- 记录 [BUG-0035](../bugs/BUG-0035.md) 与 Bug 模式库条目。

### 关联 Commit

- `fix(change-intake): require branch guard before mutation`

### 备注

本次修复先收口流程缺陷，再继续 frontend dev mock runtime 的功能提交，避免后续会话继续在默认父分支上写入文件。

## 08:21 工作记录

### 完成事项

- 完成 `backend-practice/001-plan-and-session-orchestration` follow-up review remediation。
- 修复 `startPracticeSession` reservation 后 F3 `ResolveActive` 失败不 cleanup 的路径，避免 queued session 和 pending idempotency record 卡住重试。
- 修复首问 prompt template 未渲染的问题，AI payload 现在包含语言、目标岗位、seniority、skills、rubric 和 goal 上下文。
- 修复 shared idempotency middleware 非 2xx response 不 finalize 的问题，允许同 key corrected body 重新执行。
- 修复 `startPracticeSession` 自定义 idempotency reservation 不 honor `expires_at` 的问题，过期 pending / succeeded record 会 reset。
- 原地更新 backend-practice plan/checklist/test-plan/test-checklist、BUG-0033 与 retrospective，并验证文档索引 zero drift。

### 关联 Commit

- `fix(backend-practice): harden session idempotency review paths`

### 备注

- 验证通过：focused practice / idempotency / store tests、相关后端包、`cmd/api`、`git diff --check`、`sync-doc-index --check`。

## 08:22 工作记录

### 完成事项

- 完成 `mock-contract-suite/001-fixture-backed-mock-runtime` named scenario drift 修复。
- 修复 `internal/api/mockruntime` 测试复制过期 fixture response expectation 的问题，named scenario 测试现在从 `openapi/fixtures` 读取 status/body 真理源。
- 保留 unknown scenario fail loudly 断言，避免 selector 静默回落到 default。
- 原地更新 mock-contract plan/checklist/spec v1.3，新增并完成 4.5 remediation gate。
- 创建 BUG-0034 与 mockruntime named scenario drift retrospective。

### 关联 Commit

- `fix(mock-contract): align mockruntime named scenarios`

### 备注

- 验证通过：`go test ./internal/api/mockruntime -run TestHandlerSelectsNamedSeedScenariosAndFailsUnknown -count=1`、`go test ./internal/api/mockruntime -count=1`、`go test ./...`、`make docs-check`、`git diff --check`。

## 08:22 工作记录

### 完成事项

- 修复 frontend Vite dev preview 默认打真实相对 API 的问题：开发模式默认使用 fixture-backed generated client，真实 backend 改为显式 `VITE_EI_API_MODE=real` opt-in。
- 新增 `createAppClient` 与 `createDevMockClient`，统一 `main.tsx` bootstrap；dev real backend 默认指向 `http://localhost:8080/api/v1`，不再隐式落到 `localhost:5173/api/v1`。
- 将 34 个 OpenAPI fixtures 接入 dev mock client registry，并用 generated `ALL_OPERATION_IDS` 做覆盖断言。
- 补充 frontend README / API README 说明 dev mock、real backend opt-out 和 `VITE_EI_API_BASE_URL`。
- 修复 stale frontend convention test 中 `feature_key` consumer 漂移，避免全量前端测试被历史断言阻断。
- 记录 [BUG-0036](../bugs/BUG-0036.md) 与交付复盘报告。

### 关联 Commit

- `fix(frontend-dev): default vite preview to fixture-backed API`

### 备注

验证覆盖 focused API client tests、frontend full test/typecheck/build、Vite browser smoke、docs drift check、docs-check、lint-mock-contract 和 diff check。
