# 2026-05-10

## 00:13 工作记录

### 完成事项

- 完成 plan `frontend-home-job-picks-and-parse/002-jd-match-recommendations` Phase 1 第 1 项：B2 OpenAPI 契约 D-17 additive 升级。
- `openapi/openapi.yaml` 新增 `JobMatch` tag 与 12 operationId（`getJobMatchProfile` / `getAgentScanStatus` / `listJobRecommendations` / `getJobRecommendation` / `markJobNotRelevant` / `addToWatchlist` / `removeFromWatchlist` / `listWatchlist` / `searchJobs` / `listSavedSearches` / `createSavedSearch` / `getMarketSignals`）；新增 `JobMatchProfile` / `JobMatchProfileSourceCounts` / `JobMatchAgentStatus` / `AgentScanStatus` / `JobMatchFitFooter` / `JobMatchRecommendation` / `PaginatedJobMatchRecommendation` / `WatchlistItem` / `WatchlistItemTone` / `WatchlistResponse` / `MarkNotRelevantReason` / `MarkNotRelevantRequest` / `MarkNotRelevantResult` / `SearchJobsFilters` / `SearchJobsRequest` / `SearchJobsResponse` / `SavedSearch` / `SavedSearchesResponse` / `CreateSavedSearchRequest` / `MarketSignalTone` / `MarketSignal` / `MarketSignalsResponse` schemas；`JobMatchRecommendation.provenance` 引用 `GenerationProvenance`；5 个 side-effect operation（`addToWatchlist` / `removeFromWatchlist` / `markJobNotRelevant` / `searchJobs` / `createSavedSearch`）携带 `Idempotency-Key` parameter。
- `scripts/lint/openapi_inventory.py`：`EXPECTED_TAGS` 13 项、`EXPECTED_OPERATIONS` 46 项、`IK_REQUIRED` 新增 5 条、`AI_PROVENANCE_SCHEMAS` 追加 `JobMatchRecommendation`、operation count gate `34 → 46`。
- `scripts/lint/validate_fixtures.py`：注释从 34 operationIds 同步到 46；不引入第二份 hardcoded operation list。
- `docs/spec/openapi-v1-contract/spec.md`：Header `1.13 → 1.14` / `2026-05-10`；§1 / §2.1 / §3.1.1（追加行 35-46 + 总计行）/ §5.1 plan 001-bootstrap 描述；新增 D-17 lock；C-1 acceptance 行同步到 13 tag / 46 endpoint。
- 同步 B2 owner 边界 6 处文案：`openapi/README.md`、`openapi/fixtures/README.md`、`docs/spec/mock-contract-suite/spec.md`、`docs/spec/engineering-roadmap/spec.md` 的 `12 tag / 34 operation` 表述全部 additive 升级到 `13 tag / 46 operation`，并标注 JobMatch tag 12 operation 由 `frontend-home-job-picks-and-parse/002-jd-match-recommendations` 通过 D-17 落地。
- `docs/spec/frontend-home-job-picks-and-parse/plans/002-jd-match-recommendations/checklist.md`：item 1.1 标记为 `[x]`，evidence 注释记录 inventory check / swagger-cli validate 结果与 D-17 同步覆盖。
- 验证：`python3 scripts/lint/openapi_inventory.py` → `13 tags, 46 operations, ApiErrorResponse/IK/501/Provenance invariants enforced`；`npx @apidevtools/swagger-cli@4.0.4 swagger-cli validate openapi/openapi.yaml` → `valid`。
- Phase 1 剩余项目（1.2 fixtures × 12 operation × 多 variant、1.3 mock-contract-suite 评估、1.4 codegen + typecheck、1.5 frontend contract test、1.6 BDD-Gate prep）按 plan 检查表顺序在后续会话续接。

### 关联 Commit

- `feat(openapi): additive upgrade jobmatch tag and 12 jd-match operations`

### 备注

D-17 是 B2 spec v1.0.0 freeze 的 additive 升级；真实 backend handler / store / agent scan pipeline / 真实联网搜索 / 候选池抓取由独立未来 subspec `backend-jobs-recommendations` 落地，本次仅冻结 OpenAPI 契约。Plan 002 仅完成 1.1，后续 1.2-1.6 在下一会话续接；`make validate-fixtures` 在 1.2 落地 12 个 fixture 文件之前会因为缺少 `openapi/fixtures/JobMatch/` 目录而失败，这是预期的中间状态，不阻塞 1.1 收口。

## 00:36 工作记录

### 完成事项

- 完成 plan `frontend-home-job-picks-and-parse/002-jd-match-recommendations` Phase 1 收口（1.2 → 1.6）。
- 1.2 fixtures：新增 `openapi/fixtures/JobMatch/` 12 个 operation fixture，按 plan §3.6 配置全部 scenario variants —— `getJobMatchProfile{default,unauthenticated,partial-profile}` / `getAgentScanStatus{default,idle,scanning,error,next-scan-soon}` / `listJobRecommendations{default,empty,one,many,failed}` / `getJobRecommendation{default,network-intel-empty,failed}` / `addToWatchlist{default,4xx-validation,5xx-server-error}` / `removeFromWatchlist{default,4xx-not-found}` / `markJobNotRelevant{default,4xx}` / `searchJobs{default,empty,failed,slow-response}` / `listSavedSearches{default,empty,4xx}` / `createSavedSearch{default,4xx-validation}` / `listWatchlist{default,empty,few,4xx}` / `getMarketSignals{default,partial-data,failed}`；fixture 间 jobMatchId / linkedJobMatchId 互相对齐（`a001..a004` recommendation × `b001..b003` watchlist），便于 P0.024 chevron handoff 与 P0.026 confirm-interview 复用同一卡 ID = `a002`。
- 1.3 mock-contract-suite：评估结论为 N/A；`mockTransport.ts` 已通过 `Prefer: example=<scenario>` per-request header 原生支持 per-operation per-scenario 切换；`AppRuntimeProvider` `requestOptions` 通道已具备逐 operationId 注入入口；`mock-contract-suite` spec v1.3 在 plan 1.1 同步阶段已 additive 升级到 13 tag / 46 operation 含 JobMatch 12 op；plan 001 fixture variant 切换向后兼容保留。
- 1.4 codegen：`make codegen` 重建 `frontend/src/api/generated/{client,types,spec}.ts` 与 `backend/internal/api/generated/{openapi,server.gen,types.gen}.{yaml,go}`；`pnpm --filter @easyinterview/frontend typecheck` 0 errors；二次 `make codegen` 不再产生新 diff，drift 校验通过。`backend/internal/ai/registry/resolver.go` lint-events 失败属 baseline 预存（与 plan 002 无关，stash 后失败可复现）。
- 1.5 contract test：新增 `frontend/src/api/jobMatch.contract.test.ts`（与 `mockTransport.test.ts` 同目录，符合 plan「或同等位置」表述）；`pnpm --filter @easyinterview/frontend exec vitest run src/api/jobMatch.contract.test.ts` → 1 file / 14 tests PASS：12 method 可调用断言 + getJobMatchProfile/getAgentScanStatus/listJobRecommendations/getJobRecommendation/listWatchlist/getMarketSignals fixture variant parity + addToWatchlist/removeFromWatchlist/markJobNotRelevant/searchJobs/createSavedSearch 5 个 side-effect operation Idempotency-Key request header 验证 + 5 fixture default scenario `request.headers.Idempotency-Key` 命中负向反查。
- 1.6 BDD-Gate：fixture 存在性 + generated client 可调用前置达成；同步勾选 `bdd-checklist.md` 中 P0.022 / P0.023 / P0.024 / P0.025 / P0.026 各自的 fixture-prep 子项；scenario 脚本与 INDEX 行按 plan 设计延后到 Phase 6.2-6.5。
- 验证：`make validate-fixtures` → `OK — 46 fixtures`；`python3 scripts/lint/openapi_inventory.py` → `13 tags, 46 operations, ApiErrorResponse/IK/501/Provenance invariants enforced`；`vitest run src/api/jobMatch.contract.test.ts` → 14/14 PASS；`pnpm --filter @easyinterview/frontend typecheck` PASS；`make codegen` 二次运行无 diff drift。

### 关联 Commit

- `feat(jobmatch): land fixtures and contract tests for jd-match operations`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-home-job-picks-and-parse/002-jd-match-recommendations。Phase 1 完成代表前后端 OpenAPI 契约 + fixture-backed mock + generated client + contract test 闭环已就绪，frontend 可在 Phase 2 起按 §3.6/§3.7 view-model 直接消费 `JobMatch` 12 个生成 method；真实 backend handler / agent scan pipeline / 真实联网搜索仍由未来独立 subspec `backend-jobs-recommendations` 落地（plan §3.6 backend handler 列保持 `not-yet-implemented` 状态）。`backend/internal/ai/registry/resolver.go` lint-events 三个 naked literal 失败属 baseline 预存问题，与 plan 002 范围无关。`src/lib/conventions/ai.test.ts` 失败（`feature_key` 21 vs 期望 20）同样属 baseline 预存。Phase 2 起进入 JDMatchScreen 容器升级 + Profile chip / AGENT badge 数据驱动 + 三 tab 源级复刻。

## 07:39 工作记录

### 完成事项

- 完成 plan `frontend-home-job-picks-and-parse/002-jd-match-recommendations` Phase 2 全部 6 项 (2.1 → 2.6) + 修复 Phase 1.5 evidence drift。
- 2.1 placeholder 删除：移除 `JDMatchScreen.tsx` 中 plan 001 placeholder DOM 块（`jdmatch-placeholder` / `jdmatch-placeholder-cta` testid + fallback 文案）；删除 zh.ts/en.ts 各 3 个 placeholder i18n key (`jdMatch.placeholderTitle/Copy/Cta`)；新增 `JDMatchScreen.placeholderRemoved.test.tsx` (3 tests) 断言 testid + key 在 DOM 与字典中 0 命中；JDMatchPlaceholder.test.tsx 移除 obsolete 「renders placeholder content in tab area」用例。
- 2.2 hooks：新增 `useJobMatchProfile.ts`（mount-only fetch，request seq cancellation，3 态返回）+ `useAgentScanStatus.ts`（依赖 activeTab，仅在 "recommended" 触发 fetch；D-10 不引入 setInterval / SSE / WebSocket）；`JDMatchScreen` tabs 改为 interactive (`onClick={() => setTab(...)}`) 并 wire 两 hook（void 占位，data 消费由 2.3 接力）；新增 `useJobMatchProfile.test.tsx`（4 tests：loading 三态 + 异常 + 无 runtime）+ `useAgentScanStatus.test.tsx`（5 tests：mount 1 调 + search/watchlist 不调 + tab 回 recommended 再调 + 无 setInterval/EventSource/WebSocket + 异常态）+ `JDMatchScreen.fetchBehavior.test.tsx`（2 tests：entry 各调 1 次 + tab 切换契约）。
- 2.3 数据驱动 Hero/Profile chip/AGENT badge：重构 `JDMatchScreen.tsx`，Profile chip avatar 优先 `avatarUrl` 否则 `displayName` initials；searching-as 由 `displayName · yearsOfExperience · locationText` 组合；skills 渲染 `jdmatch-profile-chip-skill-${i}` 序列；sources 由 `sources.{resumes,jds,mocks,debriefs}` 组合 + i18n 单位；AGENT badge 三态 `data-tone` (idle/scanning/error/loading) + tone color + lastScanAt/nextScanAt 通过新增 `formatRelativeMinutes` 帮助器渲染 + scanning 隐藏 next-scan；tabs 准备 count badge 槽位（`count != null` 渲染）；新增 `JDMatchScreen.dataDriven.test.tsx`（10 tests）覆盖 avatar img/initials、skills、sources、agent 三态 tone、fallback。
- 2.4 i18n 5 子命名空间扩展：`zh.ts` / `en.ts` 新增 5 命名空间共 81 keys —— `jdMatch.profile.*` (10), `jdMatch.agent.*` (6 — 2.3 已落), `jdMatch.recommended.*` (24 含 STRONG/GOOD/STRETCH + WHY/WHERE/SNAPSHOT/INTEL + 4 button + empty/error + 4 toast), `jdMatch.search.*` (24 含 5 step keys + searchingPanelLabel + Run/Running + dataSource × 4 + 4 filter + Saved searches + empty/error/retry/savedSearchCreate{,Error}), `jdMatch.watchlist.*` (12 含 heading + 3 tone + addedTimeLabel + empty/error + Market signals + chevron tooltip/fallback toast)；保留 plan 001 既有 `jdMatch.heroLabel/heroTitle/heroSub` + `jdMatch.tabRecommended/Search/Watchlist`；新增 `jdMatchLocaleNamespaces.test.ts`（8 tests：zh/en parity, 5 namespaces 必填覆盖, retired placeholder keys 0 命中, 动态 JD 数字 grep 0 命中）。
- 2.5 屏级 canonical test：新增 `JDMatchScreen.test.tsx`（12 tests ≥10）覆盖 Hero/chip/tabs/shell-attrs/negative testids/Profile chip 数据驱动 (default/partial-profile)/AGENT 三态/i18n zh/en 切换/topbar-nav-jd_match `aria-current=page`/D-10 不注册 setInterval/EventSource/WebSocket；删除 obsolete `JDMatchPlaceholder.test.tsx`（5 tests subsumed）。
- 2.6 BDD-Gate ready-state：method=manual bddChecklist=ready-state-only —— `E2E.P0.022` Hero/Profile/AGENT 阶段 + `E2E.P0.025` Profile/AGENT/auth 综合 ready-state 资产已就位（fixture-prep `[x]` in bdd-checklist + 2.2-2.5 components/hooks/i18n/Vitest 全 PASS）；scenario 脚本 / setup→trigger→verify→cleanup / INDEX 行延后到 Phase 6.2-6.10。
- Phase 1.5 evidence remediation：放宽 `frontend/src/api/jobMatch.contract.test.ts` `SIDE_EFFECT_FIXTURES` 类型签名为结构化 `SideEffectFixtureShape`（matches actual `fixture.scenarios.default.request.headers` usage），消除 4 个 TS2322 跨 fixture scenarios 形态错误；item 1.5 evidence comment 加入 `pnpm typecheck PASS (0 errors)` 证据。
- 验证：`pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/jd_match/ src/app/i18n/jdMatchLocaleNamespaces.test.ts src/api/jobMatch.contract.test.ts` → 9 files / 58 tests PASS；`pnpm --filter @easyinterview/frontend typecheck` PASS（0 errors）；`make validate-fixtures` OK（46 fixtures）；旧 flat profile/agent i18n keys (`jdMatch.profileInitials` / `searchingAs` / `profileSummary` / `profileSources` / `profileSourcesStats` / `agentStatus`) grep 0 命中；旧 placeholder testid grep 0 命中。

### 关联 Commit

- `feat(jd_match): wire data-driven shell with profile chip and agent badge`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-home-job-picks-and-parse/002-jd-match-recommendations。Phase 2 完成代表 `JDMatchScreen` 容器从 placeholder shell 升级为数据驱动壳：Hero / Profile chip / AGENT badge 全部由 `getJobMatchProfile` + `getAgentScanStatus` 生成数据驱动渲染；i18n 5 子命名空间就位（含 5 个 SearchTab 步骤 key 与不含具体数字的静态语义文案）；JDMatchScreen 12 屏级 + 10 dataDriven + 3 placeholderRemoved + 2 fetchBehavior + 4 useJobMatchProfile + 5 useAgentScanStatus + 8 jdMatch i18n + 14 contract tests 全 PASS。Phase 3 起进入 Recommended tab — `JobMatchCard` + `JDDetail` + Save/Mark not relevant/Confirm interview 闭环。`src/lib/conventions/ai.test.ts` (`feature_key` 21 vs 20) 与 `backend/internal/ai/registry/resolver.go` lint-events 仍属 baseline 预存（前者 shared-conventions 子题，后者 ai-provider 子题），与 plan 002 范围无关；建议在 Phase 2 checkpoint 后由用户决策是否走 /change-intake 处理。

## 07:56 工作记录

### 完成事项

- Baseline 修复 #1：`frontend/src/lib/conventions/ai.test.ts` 「keeps the full wire field set in declaration order」用例之前预期 20 字段（缺失 `feature_key`），但生成的真理源 `frontend/src/lib/conventions/ai.ts:117 FEATURE_KEY` 已经是第 21 字段（位于 `LANGUAGE` 与 `FEATURE_FLAG` 之间）。修正：在 expected 数组与 wire-name 映射两处用例中各加入 `AI_VOCABULARY_FIELDS.FEATURE_KEY` / `feature_key`，与生成源对齐。
- 验证：`pnpm --filter @easyinterview/frontend exec vitest run src/lib/conventions/ai.test.ts` → 5/5 PASS。
- 责任归属：`shared-conventions-codified` 子题（B1）。修复属于 test 与 generated 真理源对齐，没有改动 generated/* 或代码逻辑，不影响其他消费者；先单独 commit 与 plan 002 Phase 2 隔离归类。

### 关联 Commit

- `fix(shared-conventions): align ai vocabulary baseline test with feature_key field`

### 备注

Baseline 修复 - 应 plan 002 Phase 2 checkpoint 用户指示「先处理 baseline 失败后再 Phase 3」。`feature_key` 字段是被 Plan F3 prompt-rubric-registry / plan 002 共用的 AI 路由键，已在 generated 真理源中存在；本次只是同步 baseline test，不修改 wire schema。

## 07:59 工作记录

### 完成事项

- Baseline 修复 #2：`make lint-events` 报告 `backend/internal/ai/registry/resolver.go` 中 3 个 naked event/job literal (`debrief.generate` / `report.generate` / `resume.parse`)。Root cause：resolver.go::defaultModelProfile 使用 `featureKey` 字符串作为 switch case 鉴别，其中 3 个值（`resume.parse` / `report.generate` / `debrief.generate`）与 `backend/internal/shared/jobs.AsynqTask*` 字面量重叠；二者语义不同（feature_key vs Asynq 任务名），但字符串相同。
- 架构修复（按用户选项 C）：新增 `backend/internal/shared/featurekeys/featurekeys.go` 包，定义 `FeatureKey` 类型 + 10 个常量（`TargetImportParse` / `PracticeSessionFirstQuestion` / `PracticeSessionFollowUp` / `PracticeTurnLightweightObserve` / `ReportGenerate` / `ReportQuestionAssessment` / `ResumeParse` / `ResumeTailorGapReview` / `ResumeTailorBulletSuggestions` / `DebriefGenerate`） + `All()` helper + `String()` 方法；当前为手写，truth source 仍为 `config/prompts/<feature_key>/v*.yaml`，留下 codegen-from-config TODO。
- 重构 `resolver.go::defaultModelProfile` switch case，全部 10 case 改为 `case featurekeys.X:`，通过 typed `FeatureKey(featureKey)` 类型转换；不再有任何 naked feature_key 字面量。
- 更新 `scripts/lint/lint_events.py` `CONTRACT_GENERATED_DIRS`，新增 `Path("backend/internal/shared/featurekeys")` 进入 lint allowlist（与 events/jobs 一致），允许 featurekeys.go 中的 typed 常量声明字面量。
- 验证：`cd backend && go build ./...` BUILD_OK；`make lint-events` → `OK: event/job baselines match current truth sources`；`go test ./internal/ai/registry/...` PASS；`python3 scripts/lint/lint_events_test.py` 18/18 PASS。
- 责任归属：`ai-provider-and-model-routing` 子题（resolver.go consumer）+ `shared-conventions-codified` 子题（新 shared 包归类）。本次以 cross-subspec refactor 单 commit 提交，避免拆分为依赖耦合的两次提交。

### 关联 Commit

- `refactor(ai-provider): adopt typed feature_key constants in registry resolver`

### 备注

Baseline 修复 - 应 plan 002 Phase 2 checkpoint 用户指示「先处理 baseline 失败后再 Phase 3」。featurekeys.go 当前为手写常量，未来 codegen-from-config 落地后该文件可被自动生成（保持 lint-events 允许）。本次未触动 spec：`ai-provider-and-model-routing` spec / plan 不需要修改，因为新增的 `shared/featurekeys` 是契约 / 工程边界归类调整，未引入新业务行为。完成后 `pnpm test` + `make lint` 全 PASS（与 plan 002 Phase 2 提交后的状态相比，仅消除 baseline 预存的两个独立失败）。Phase 3 起进入 Recommended tab 实现。

## 08:56 工作记录

### 完成事项

- 完成 plan `frontend-home-job-picks-and-parse/002-jd-match-recommendations` Phase 3 全部 10 项 (3.1 → 3.10)。
- 3.1 三个 props-driven 组件源级复刻：`JobMatchCard.tsx` (左 3px score band 边框 + 标题 + unseen dot + saved pin + company-companyTag + location-comp + 右上 32px score + STRONG/GOOD/STRETCH label + Top reason + 底部 must X/Y · plus X/Y · posted · sourceLabel) / `JDDetail.tsx` (Header 36px score + /100 + level/location/comp tags + WHY MATCHES (✓ ok) + WHERE STRETCH (ⓘ warn) + ROLE SNAPSHOT ul + INTEL 条件渲染 networkNote || similarInterviewers > 0 + 合规措辞「公开面经 / JD / 公司资料信号」/「public interview-review, JD, and company-source signals」+ Action bar 4 button) / `RecommendedTab.tsx` (1.1fr/1.4fr 双列 grid + sticky JDDetail + selected fallback 第一张 + empty/loading/error 三态 testid)。
- 3.2 `useJobMatchRecommendations.ts` hook：generated client 调 listJobRecommendations 一次 + retry refetch + pageInfo (nextCursor/pageSize/hasMore) 透传。
- 3.3 `useToggleWatchlist.ts` hook：optimistic apply via `applyOptimistic(id, savedNext)` 闭包 + per-id sequence 处理 race + `addToWatchlist({jobMatchId})` / `removeFromWatchlist(id)` 双路径 + IK 每次唯一 + 4xx 自动 revert + window.eiToast (ok/danger tone)。
- 3.4 `useDismissRecommendation.ts` hook：default reason "not_relevant" 透传 + body 不带 freeNote (D-12 隐私) + applyOptimisticHide 返回 revert 闭包供 4xx 调用 + IK 唯一 + neutral/danger toast 双态。
- 3.5 JDMatchScreen 集成 RecommendedTab + 三 hooks + 本地 hiddenIds/savedOverrides overlay 模型 (不可变更新 upstream items) + selectedId 自动选第一张可见卡 + handleConfirmInterview 调 `navigate({name:"parse", params:{source:"jd_match", sourceJobMatchId: rec.id}})` 严格 2 字段。
- 3.6 handleOpenSource 调 `window.open(rec.sourceUrl, "_blank", "noopener,noreferrer")` + sourceUrl 缺失短路 + JDDetail.tsx Source button disabled when sourceUrl null。
- 3.7 接入 `useRequestAuth` + isUnauthenticated 守卫：4 个 handler (Confirm/Save/Unsave/Dismiss) 在 unauth 时调 `requestAuth({type:"jd_match_action", route:"jd_match", params:{tab:"recommended", selectedJobMatchId, action: enum}, label})`，Source button 旁路不进 pendingAction；pendingAction params 不携带 query/freeNote/sourceUrl/label 等隐私字段。auto-resume 自动重新触发 action 由 E2E.P0.025 scenario 阶段验证 (待 6.2)。
- 3.8 隐私反查：`RecommendedPrivacy.test.tsx` 4 tests 覆盖 toggleSave/dismiss/openSource 三动作，断言 jobMatchId/sourceUrl/freeNote 不进 console.log/error/warn + Storage.setItem + window.location URL；transport spy 形态严格等于 {url, method} 不含 body/headers。
- 3.9 新增 8 个 plan 列表测试文件全 PASS + 3 个 hook 单测 + 1 个 RecommendedPrivacy.test.tsx：JobMatchCard.test.tsx 14 / JDDetail.test.tsx 16 / RecommendedTab.test.tsx 12 / JDMatchAuthGate.test.tsx 6 / RecommendedToggleWatchlist.test.tsx 5 / RecommendedDismiss.test.tsx 5 / RecommendedConfirmInterview.test.tsx 2 / RecommendedOpenSource.test.tsx 3 / RecommendedPrivacy.test.tsx 4 / useJobMatchRecommendations.test.tsx 10 / useToggleWatchlist.test.tsx 9 / useDismissRecommendation.test.tsx 7 = ≥93 测试。
- 3.10 BDD-Gate ready-state：method=manual bddChecklist=ready-state-only —— E2E.P0.022 / E2E.P0.026 fixture variant 准备项已勾选；Phase 3 ready-state 资产 (RecommendedTab + 4 button 闭环 + auth pending action + Confirm interview 严格 2 字段 + 隐私反查) 全部就位；scenario 脚本 / setup→trigger→verify→cleanup / INDEX 行延后到 Phase 6.2-6.10。
- 新增 i18n 键 `jdMatch.recommended.actionSaved` (zh "已收藏" / en "Saved") + `jdMatch.recommended.toastSaveError` (zh "更新关注列表失败，请重试" / en "Could not update watchlist — try again") 完整覆盖 saved-state label 与 watchlist 错误提示。
- 验证：`pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/jd_match/ src/app/i18n/jdMatchLocaleNamespaces.test.ts src/api/jobMatch.contract.test.ts` → 20 files / 151 tests PASS；`pnpm --filter @easyinterview/frontend test` 全量 → 86 files / 578 tests PASS；`pnpm --filter @easyinterview/frontend typecheck` PASS (0 errors)；`make validate-fixtures` OK 46 fixtures。

### 关联 Commit

- `feat(jd_match): land recommended tab with jd-detail, watchlist loop and auth gate`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-home-job-picks-and-parse/002-jd-match-recommendations。Phase 3 完成代表 jd_match Recommended tab 主路径 + 4 button (Confirm interview / Save toggle / Source / Not relevant) + auth pending action surface + 隐私反查全部闭环就绪；JDMatchScreen 容器从 Phase 2 数据驱动壳升级为 Recommended tab 完整业务屏，Phase 4 起进入 Search tab — 自然语言搜索 + Saved searches + Result filters + 单一加载文案。`E2E.P0.022` Recommended 主路径 + `E2E.P0.026` Confirm interview → parse 出口 ready-state 资产已就位，scenario 脚本与 INDEX 行按 plan 设计延后到 Phase 6.2-6.10。auto-resume 自动重新触发 action（pendingAction params 解码与 selectedJobMatchId fallback）由 E2E.P0.025 scenario 阶段验证。

## 09:11 工作记录

### 完成事项

- 完成 plan `frontend-home-job-picks-and-parse/002-jd-match-recommendations` Phase 4 全部 9 项 (4.1 → 4.9)。
- 4.1 `SearchTab.tsx` props-driven 源级复刻：search-bar (input/run/sources) + searching=true 时 5-step AGENT panel (testid `jdmatch-search-searching-panel` + accent label `● AGENT SCANNING` / `● AGENT 扫描中` + 1px+3px accent 边框 + 5 行 step opacity 1/1/1/0.4/0.4 inline style + i18n searchingStep1..5 渲染 + 不含动态 JD 数字) + Saved searches 3 列 grid + Save current button (gated by !query.trim()) + Results 2 列 grid cap 6 + 4 chip filter (all/strong/remote/unseen + data-active) + error/empty 三态。
- 4.2 `useSearchJobs.ts` hook：per-id seq + AbortController abort on rerun/unmount + searching/results/error/hasRunOnce 四态 + IK 唯一 + run/abort/reset API；不引入 setInterval/setTimeout step-advance；slow-response 期间 5-step panel 持续渲染。
- 4.3 `useSavedSearches.ts` 单文件同时导出 `useSavedSearches(active)` + `useCreateSavedSearch({onCreated})`：list 仅在 active=true 时调 1 次 (calledRef + active gate) + 4xx error；create body match {label,query} + IK 唯一 + onCreated 回调让消费者前置插入到本地 list (无需 re-fetch) + ok/danger toast。
- 4.4 `searchFilters.ts` 暴露 `applySearchFilter` + `recommendationMatchesFilter` 纯客户端 predicate (all/strong score>=85 / remote regex /remote|远程/i / unseen !seen)；切换不调 searchJobs。
- 4.5 JDMatchScreen `handleRunSearch` / `handleSaveCurrentSearch` 在 isUnauthenticated 时调 `requestAuth({type:"jd_match_action", route:"jd_match", params:{tab:"search", action:"run_search"|"create_saved_search"}, label})`；query / saved-search label 不进 pendingAction params。
- 4.6 useEffect on tab change away from search → setQuery("") + searchJobs.abort() + searchJobs.reset() + setResultFilter("all") + setSavedSearchesOverlay([]) 全部清理；query / label / filter / sourceJobUrl 不进 console / URL / localStorage。
- 4.7 三 variant 单独测试：failed → inline error + 保留输入 + 0 伪造 results；empty → no-results 空态；slow-response → 5-step panel 持续渲染至 settle + Run.disabled=true + Enter no-op + 切 tab abort in-flight。
- 4.8 plan 列表 7 个测试文件 + 2 个 hook 单测 + 1 个 filter 单测 = 9 文件覆盖 Phase 4：SearchTab.test.tsx 14 + SearchTabRun.test.tsx 4 + SearchTabSavedSearches.test.tsx 4 + SearchTabFilter.test.tsx 8 + SearchTabFailure.test.tsx 3 + SearchTabPrivacy.test.tsx 3 + SearchTabAuthGate.test.tsx 2 + useSearchJobs.test.tsx 9 + useSavedSearches.test.tsx 7 = ≥54 测试。
- 4.9 BDD-Gate ready-state：method=manual bddChecklist=ready-state-only —— E2E.P0.023 fixture variant 已勾选 (含 searchJobs slow-response 携带 X-Mock-Delay-Ms=4500) + ready-state 资产 (SearchTab 源级复刻 + 三 hooks + filter + auth pending action + 隐私反查 + 失败/空/slow 三态 + ≥54 测试) 全部就位；scenario 脚本 / setup→trigger→verify→cleanup / INDEX 行延后到 Phase 6.2-6.10。
- JDMatchScreen 集成新增：query/resultFilter/savedSearchesOverlay 本地状态 + `handleOpenJobFromSearch` (设 selectedId + 切回 recommended tab) + `handleCreateSavedSearchRetry` (重发 create) + `mergedSavedSearches` (overlay + server list 合并) + `filteredResults` (应用 4 chip filter)。
- 验证：`pnpm --filter @easyinterview/frontend exec vitest run src/app/screens/jd_match/ src/app/i18n/jdMatchLocaleNamespaces.test.ts` → 28 files / 191 tests PASS；`pnpm --filter @easyinterview/frontend test` 全量 → 95 files / 632 tests PASS；`pnpm --filter @easyinterview/frontend typecheck` PASS (0 errors)；`make validate-fixtures` OK 46 fixtures。

### 关联 Commit

- `feat(jd_match): land search tab with saved searches, filters and auth gate`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-home-job-picks-and-parse/002-jd-match-recommendations。Phase 4 完成代表 jd_match Search tab 自然语言搜索 + Run web search + Saved searches list/create + 4 chip filter pure client-side + 5-step AGENT panel + 失败/空/slow 三态 + auth pending action surface + 隐私反查全部闭环就绪；JDMatchScreen 容器同时支持 Recommended (Phase 3) + Search (Phase 4) 两 tab 切换 + tab 切换时 search state 自动 reset。Phase 5 起进入 Watchlist tab + Market signals + chevron handoff。`E2E.P0.023` Search 主路径 ready-state 资产已就位，scenario 脚本与 INDEX 行按 plan 设计延后到 Phase 6.2-6.10。
