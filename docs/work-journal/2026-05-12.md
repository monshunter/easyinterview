# 2026-05-12

## 07:37 工作记录

### 完成事项

- 完成 `event-and-outbox-contract/002-resume-tailor-mode-drift-fix`：`ResumeTailorMode` 从旧 `inline / rewrite / mirror` 对齐为 `gap_review / bullet_suggestions`。
- 同步 `shared/events.yaml`、事件 baseline manifest、JSON Schema ref、Go/TS generated events、事件 inventory lint 真理源与 Go/TS type-narrowing 测试。
- 将 event-and-outbox-contract spec/history 升到 2.4，更新 `docs/spec/INDEX.md`，并在 OpenAPI 004 与 DB 002 checklist 中追加 B3 D-14 已落地前置信号。
- 验证：`make lint-events`、`make codegen-events && make lint-events`、Go focused events test、frontend focused events test、旧字面量精准负向 grep、`openapi_inventory.py`、`sync-doc-index --check` 均通过；`make codegen-check` 将在本提交后复跑以满足 diff gate。

### 关联 Commit

- `fix(events): align resume tailor mode contract`

### 备注

Auto-committed by `/tdd` phase-commit, plan: event-and-outbox-contract/002-resume-tailor-mode-drift-fix.

## 08:10 工作记录

### 完成事项

- 完成 `db-migrations-baseline/002-resume-versions-additive`：新增 `resume_versions` / `resume_version_suggestions` 迁移与 `resume_assets` additive 字段。
- 补齐 migration SQL contract、PG integration tests、`migrations/enum-sources.yaml`、`migrations/lint.sh` 和 migration lint parser / fixture 测试，覆盖 `CREATE TABLE IF NOT EXISTS`、nullable check 与 `ALTER TABLE ADD COLUMN` check。
- 将 B4 spec/history 升到 1.15，roadmap 升到 3.12，并向 OpenAPI 004 与 B3 002 checklist 写入 B4 D-17 已落地信号。
- 验证：`make migrate-up && make migrate-down && make migrate-up`、`make migrate-check`、`go test ./internal/migrations/...`、`python3 -m pytest scripts/lint/migrations_lint_test.py`、`migrations/lint.sh`、`sync-doc-index --check` 与相关负向 grep 均通过。

### 关联 Commit

- `feat(db-migrations): add resume version schema`

### 备注

Auto-committed by `/tdd` phase-commit, plan: db-migrations-baseline/002-resume-versions-additive.

## 08:48 工作记录

### 完成事项

- 完成 `openapi-v1-contract/004-resume-additive-coverage`：将 Resume Workshop additive contract 落到 `Resumes` tag，新建 9 个 operation、resume version schema、fixtures 与 P0 `exportResumeVersion` 501 stub。
- 同步 B1 shared conventions，新增 resume version/tailor status enum 与 `RESUME_EXPORT_NOT_AVAILABLE` 错误码，并重新生成 Go/TS shared constants 与 OpenAPI Go/TS client/server artifacts。
- 更新 OpenAPI inventory、fixture validator、mock-contract-suite、engineering-roadmap、openapi-v1-contract spec/history/INDEX，并向 workspace 001 plan 写入 `listResumes` 解锁 handoff。
- 验证：`make codegen-check`、`make validate-fixtures`、`make lint-openapi`、`make openapi-diff`、`make docs-check`、lint 脚本单测、Go focused tests 与 frontend typecheck 均通过。

### 关联 Commit

- `feat(openapi): add resume workshop contract coverage`

### 备注

Auto-committed by `/tdd` phase-commit, plan: openapi-v1-contract/004-resume-additive-coverage.

## 08:50 工作记录

### 完成事项

- 收口 Resume Workshop contract additive 三个执行 plan 生命周期：B3 `event-and-outbox-contract/002`、B4 `db-migrations-baseline/002`、B2 `openapi-v1-contract/004` 均从 `active` 切换为 `completed`。
- 同步三个 plans/INDEX 的状态与更新日期，保证 plan/checklist/header/INDEX 当前事实一致。
- 验证：`make docs-check` 与 `sync-doc-index --fix-index` 后置校验均通过。

### 关联 Commit

- `docs(plans): close resume additive lifecycle`

### 备注

Auto-committed by `/tdd` completion lifecycle sync.

## 08:53 工作记录

### 完成事项

- 按 `/retrospective --this` 对 Resume Workshop contract additive 三 plan 交付做 post-pass 复盘，创建 `docs/reports/2026-05-12-resume-workshop-contract-additives-assessment.md`。
- 在 `docs/reports/INDEX.md` 增加 assessment 索引，记录跨 plan 顺序、generator hardcode、swagger-cli 命令形态与 prototype mapping gap 的后续改进建议。
- 验证：`make docs-check` 与 markdown whitespace check 通过。

### 关联 Commit

- `docs(retrospective): assess resume additive delivery`

### 备注

Auto-committed by `/retrospective --this`.

## 08:55 工作记录

### 完成事项

- 按 `/bug-report --this` 对 B3 `ResumeTailorMode` 漂移修复补录 Bug 知识库，新增 [BUG-0042](../bugs/BUG-0042.md)。
- 在 `docs/bugs/INDEX.md` 的 Schema 分组增加 resolved 记录，关联修复提交 `fix(events): align resume tailor mode contract`。
- 验证：`make docs-check` 与 markdown whitespace check 通过。

### 关联 Commit

- `docs(bugs): record resume tailor mode drift`

### 备注

Bug 模式库未直接修改；可在后续治理任务中把 cross-layer enum retire negative-grep 抽象为通用模式。

## 10:18 工作记录

### 完成事项

- L2 remediation：修复 `RegisterResumeRequest` 和 `ResumeAsset` 中 `fileObjectId` 必填漂移，改为 optional+nullable，使 paste/guided 等 fileless 简历入水通道可用。
- 更新 codegen `render_go.go`、`render_ts.go` 正确处理 nullable 字段生成，新增 `render_types_test.go` 验证渲染契约。
- 更新 generated Go/TS types、spec.ts、openapi.yaml，同步 nullable 变化。
- 补齐 `registerResume` paste-text 和 guided-answers fixture，更新 `listResumes` fixture 中非 upload 资产 `fileObjectId: null`。
- 新增 `openapi_inventory_test.py` 的 `test_register_resume_contract_supports_fileless_sources` 和 `validate_fixtures_test.py` 的 fileless 断言。
- 移除 events 测试中已退役的 `inline` 字面量引用（Go/TS）。
- 新增 [BUG-0043](../bugs/BUG-0043.md) 记录漂移修复，新增 [L2 Remediation 复盘报告](../reports/2026-05-12-resume-workshop-contract-additives-l2-remediation-assessment.md)。

### 关联 Commit

- `fix(openapi): allow fileless resume intake contracts`

### 备注

L2 remediation，覆盖 openapi 契约 + codegen + fixtures + 测试 + 文档。

## 11:02 工作记录

### 完成事项

- L2 remediation：修复 `branchResumeVersion` 的 `202` async response typing，新增 `BranchResumeVersionAccepted` schema，并让 generated TS client 返回 `ResumeVersion | BranchResumeVersionAccepted`。
- 修复 generated TS client 对显式 P0 `501` export response 的 typed parse path；`exportResumeVersion` / `requestPrivacyExport` 现在返回 `ApiErrorResponse`，未声明 non-OK 仍 throw。
- 补齐 Resume Workshop 9 个 operation fixture 到 frontend dev mock registry，并新增 Vitest 覆盖 operationId completeness、501 fallback 与 `ai_select` branch shape。
- 修复 `resume_versions` live migration test cleanup 顺序，按 child-first 删除并让 cleanup error fail test。
- 原地修订 B2/B4 spec、plan、checklist 与 INDEX，新增 [BUG-0044](../bugs/BUG-0044.md) 和 [L2 remediation 复盘报告](../reports/2026-05-12-resume-additive-client-cleanup-l2-remediation-assessment.md)。
- 验证：`go test ./backend/cmd/codegen/openapi -count=1`、`pnpm --filter @easyinterview/frontend test src/api/devMockClient.test.ts`、`pnpm --filter @easyinterview/frontend typecheck`、focused migration test、`make lint-openapi`、`make validate-fixtures`、`make openapi-diff`、`make docs-check` 均通过；当前 `DATABASE_URL` 未设置，live migration cases 明确 skip。

### 关联 Commit

- `fix(openapi): harden resume additive client contracts`

### 备注

L2 remediation，覆盖 OpenAPI/codegen/generated client、frontend fixture-backed dev mock、migration test cleanup、Bug 知识库与复盘报告。

## 13:23 工作记录

### 完成事项

- L1 remediation：修复 `frontend-resume-workshop/001-listing-routing-and-detail-readonly` 三处 plan-review gate drift。
- 将 `listResumeVersions` 从不可执行的 per-asset fixture fanout 改为当前 mock transport 可执行的 scenario-scoped 聚合，并要求无匹配版本 asset 显示 no-versions/partial 状态。
- 对齐 UI parity gate：clean checkout 常规 PASS 使用 DOM anchor、computed style、bounding box 与 screenshot smoke；只有稳定 baseline 可取得时才启用 screenshot diff。
- 补齐 `exportResumeVersion` P0 fallback 的 `Idempotency-Key` 契约，要求 `generateIdempotencyKey()` + generated client header spy 断言。
- 验证：`validate_context.py --target frontend`、`sync-doc-index.py --check`、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `docs(frontend-resume-workshop): fix listing plan review gates`

### 备注

Manual `/work-journal` close-out for `/plan-review frontend-resume-workshop/001-listing-routing-and-detail-readonly --fix`.

## 13:35 工作记录

### 完成事项

- L1 plan-review 修订 `backend-upload/001-file-objects-and-presign-baseline`：对齐 `createUploadPresign` 返回状态码为 201，收敛 fixture 声明只消费 B2 `default`，failure/boundary 走 handler 与 BDD 直接断言；修正 privacy delete 跨域顺序引用 B4 matrix 而非自行规定全局先后。
- 同步 bump backend-upload spec、history、plan、checklist、BDD plan、BDD checklist、plans/INDEX 以及共享 `docs/spec/INDEX.md` 版本至 1.1。

### 关联 Commit

- `docs(backend-upload): fix plan-review presign status and privacy ordering`

### 备注

`/plan-review backend-upload/001-file-objects-and-presign-baseline --fix` 的分批提交第 1/2 部分。

## 13:40 工作记录

### 完成事项

- L1 plan-review 修订 `backend-resume/001-asset-register-parse-and-listing`：新增 D-9 首次创建保存边界，明确 registerResume/resume.parse 只创建解析草稿，Preview Confirm 前不创建正式 `structured_master` ResumeVersion；补齐 fixture 场景引用（paste-text/guided-answers/not-found/empty/paginated）；添加 `parsed_text_snapshot` 到 parse output；补充 checklist 3.9 验证 parse 后 `resume_versions` 不变；标记 backend-upload/001 为 active blocker。
- 修复 privacy delete 跨域顺序引用 B4 matrix 而非独立规定全局先后。
- 同步 bump backend-resume spec、history、plan、checklist、BDD plan、BDD checklist、plans/INDEX 版本至 1.1。

### 关联 Commit

- `docs(backend-resume): fix plan-review preview confirm and parse contract`

### 备注

`/plan-review backend-resume/001-asset-register-parse-and-listing --fix` 的分批提交第 2/2 部分。

## 16:06 工作记录

### 完成事项

- 完成 `backend-upload/001-file-objects-and-presign-baseline` Phase 0：A4 config contract preflight。
- 在 A4 spec/config 中登记 `objectStorage.provider`、`upload.presignTTLSeconds` 与 `upload.maxBytes.*` config-only paths，继续复用现有 `OBJECT_STORAGE_*` env 字典。
- 补齐 config validator 与单元测试，覆盖默认值、非法 provider、非正数 TTL / maxBytes fail-fast，并确认未新增未登记 `UPLOAD_*` / `OBJECT_STORE_*` env key。
- 验证：focused config tests、`make lint-config`、代码侧未登记 env key 负向 grep、`make docs-check` 均通过。

### 关联 Commit

- `feat(backend-upload): complete config contract preflight`

### 备注

Auto-committed by `/tdd` phase-commit, plan: backend-upload/001-file-objects-and-presign-baseline.

## 16:48 工作记录

### 完成事项

- 完成 `backend-upload/001-file-objects-and-presign-baseline` Phase 5：收口与 BDD。
- 新增 E2E.P0.033 场景目录 `test/scenarios/e2e/p0-033-file-presign-register-roundtrip/`，包含 README、seed / expected outcome 数据说明和 `setup/trigger/verify/cleanup` 脚本。
- 执行 E2E.P0.033 `setup → trigger → verify → cleanup` 全 PASS；在无 live `DATABASE_URL` / `OBJECT_STORAGE_*` 的当前环境下，integration-tag DB / MinIO smoke 按测试契约 skip，unit、fixture parity、状态机与隐私负向 gate 通过。
- 同步 `test/scenarios/e2e/INDEX.md`、backend-upload plan/checklist/BDD lifecycle、`docs/spec/engineering-roadmap` 3.14，以及 backend-resume/001 前置信号，明确 backend-upload/001 已可供 backend-resume 接续。
- 修复全量后端测试暴露的 config prod fixture 缺口，补齐 upload 默认配置字段。
- 验证：`make lint-config`、`cd backend && go test ./...`、`go test ./backend/internal/upload/...`、`go test ./internal/upload/handler -run TestCreateUploadPresignFixtureParity -count=1`、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `test(backend-upload): add presign register bdd scenario`

### 备注

Auto-committed by `/tdd` phase-commit, plan: backend-upload/001-file-objects-and-presign-baseline.

## 16:39 工作记录

### 完成事项

- 完成 `backend-upload/001-file-objects-and-presign-baseline` Phase 4：privacy delete 链路。
- 新增 `DeleteFileObjectsForUser` 服务路径，按 B4 隐私矩阵先删除对象存储，再 hard delete `file_objects` DB 行，最后写 audit tombstone；对象删除失败返回 retryable error 并保留 DB 行等待重试。
- 新增 `audit_events` tombstone 写入，metadata 仅包含 `fileObjectId` / `purpose` / `deletedAt`，不持久化 `objectKey`。
- 新增 `backend/internal/privacy/runner` 上传文件删除适配，作为 privacy runner 调用 backend-upload 删除链路的测试入口。
- 验证：`go test ./internal/upload/service -run 'TestDeleteFileObjectsForUser' -count=1`、`go test ./internal/upload/... ./internal/privacy/runner -count=1`、`go test ./internal/upload/store -tags=integration -run 'TestInsertAuditTombstoneIntegrationDoesNotPersistObjectKey|TestFileObjectsIntegrationDatabaseAvailable' -count=1` 均通过。

### 关联 Commit

- `feat(backend-upload): add privacy file deletion`

### 备注

Auto-committed by `/tdd` phase-commit, plan: backend-upload/001-file-objects-and-presign-baseline.

## 16:13 工作记录

### 完成事项

- 完成 `backend-upload/001-file-objects-and-presign-baseline` Phase 1：handler skeleton + IK + purpose validation。
- 新增 `backend/internal/upload/handler/presign.go`，实现 `CreateUploadPresign` handler，覆盖 auth session、`Idempotency-Key`、purpose enum、per-purpose byteSize limit 与 201 JSON response。
- 复用现有 idempotency middleware 验证 24h replay / mismatch rejection，不在 upload handler 内自建第二套 IK 机制。
- 新增 handler unit tests 与 fixture parity test，锁定 B2 `Uploads/createUploadPresign.default` 的 status 和响应字段集。
- 验证：`go test ./internal/upload/handler -count=1` 与 `go vet ./internal/upload/handler` 均通过。

### 关联 Commit

- `feat(backend-upload): add presign handler validation`

### 备注

Auto-committed by `/tdd` phase-commit, plan: backend-upload/001-file-objects-and-presign-baseline.

## 16:20 工作记录

### 完成事项

- 完成 `backend-upload/001-file-objects-and-presign-baseline` Phase 2：file_objects store + state machine。
- 新增 `backend/internal/upload/store/file_objects.go`，实现 `Create / MarkUploaded / MarkScanFailed / MarkDeleted / HardDelete / DeleteFileObjectsForUser / LockForRegister`。
- 在 store 层锁定 `pending → uploaded`、`pending|uploaded → scan_failed`、`pending|uploaded|scan_failed → deleted`，非法转换返回 `ErrInvalidStateTransition`，不引入 `registered` 状态。
- 补齐 sqlmock 单元测试与 integration-tag 入口，覆盖 pending 创建、状态迁移、cross-user/not-found row-lock 隔离和用户 scoped hard delete。
- 验证：`go test ./internal/upload/store -count=1`、`go test ./internal/upload/store -tags=integration -count=1`、`go test ./internal/upload/... -count=1`、`go vet ./internal/upload/...` 均通过。

### 关联 Commit

- `feat(backend-upload): add file object store`

### 备注

Auto-committed by `/tdd` phase-commit, plan: backend-upload/001-file-objects-and-presign-baseline.

## 16:28 工作记录

### 完成事项

- 完成 `backend-upload/001-file-objects-and-presign-baseline` Phase 3：ObjectStore interface + dev provider。
- 新增 `backend/internal/upload/objectstore`，实现 `ObjectStore` interface、filesystem fallback、MinIO SDK provider 与 config selector。
- 新增 `backend/internal/upload/service.RegisterFileObject`，覆盖 pending object exists 后标记 uploaded、uploaded 幂等通过、object missing / scan_failed / deleted 验证失败。
- 引入 `github.com/minio/minio-go/v7`，避免手写 S3 presign。
- 验证：`go test ./internal/upload/objectstore ./internal/upload/service -count=1`、`go test ./internal/upload/objectstore -tags=integration -run TestMinIO -count=1`、`go test ./internal/upload/... -count=1` 均通过；当前未配置 live MinIO env 时 integration smoke 按测试契约 skip。

### 关联 Commit

- `feat(backend-upload): add object store register service`

### 备注

Auto-committed by `/tdd` phase-commit, plan: backend-upload/001-file-objects-and-presign-baseline.
