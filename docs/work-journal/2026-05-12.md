# 2026-05-12

## 07:37 工作记录

### 完成事项

- 完成 `event-and-outbox-contract/002-resume-tailor-mode-drift-fix`：`ResumeTailorMode` 从旧 `inline / rewrite / mirror` 对齐为 `gap_review / bullet_suggestions`。
- 同步 `shared/events.yaml`、事件 baseline manifest、JSON Schema ref、Go/TS generated events、事件 inventory lint 真理源与 Go/TS type-narrowing 测试。
- 将 event-and-outbox-contract spec/history 升到 2.4，更新 `docs/spec/INDEX.md`，并在 OpenAPI 004 与 DB 002 checklist 中追加 B3 D-14 已落地前置信号。
- 验证：`make lint-events`、`make codegen-events && make lint-events`、Go focused events test、frontend focused events test、旧字面量精准负向 grep、`openapi_inventory.py`、`sync-doc-index --check` 均通过；`make codegen-check` 将在本提交后复跑以满足 diff gate。

### 关联 Commit

- `fix(events): align resume tailor mode contract`

### 备注

Auto-committed by `/tdd` phase-commit, plan: event-and-outbox-contract/002-resume-tailor-mode-drift-fix.

## 08:10 工作记录

### 完成事项

- 完成 `db-migrations-baseline/002-resume-versions-additive`：新增 `resume_versions` / `resume_version_suggestions` 迁移与 `resume_assets` additive 字段。
- 补齐 migration SQL contract、PG integration tests、`migrations/enum-sources.yaml`、`migrations/lint.sh` 和 migration lint parser / fixture 测试，覆盖 `CREATE TABLE IF NOT EXISTS`、nullable check 与 `ALTER TABLE ADD COLUMN` check。
- 将 B4 spec/history 升到 1.15，roadmap 升到 3.12，并向 OpenAPI 004 与 B3 002 checklist 写入 B4 D-17 已落地信号。
- 验证：`make migrate-up && make migrate-down && make migrate-up`、`make migrate-check`、`go test ./internal/migrations/...`、`python3 -m pytest scripts/lint/migrations_lint_test.py`、`migrations/lint.sh`、`sync-doc-index --check` 与相关负向 grep 均通过。

### 关联 Commit

- `feat(db-migrations): add resume version schema`

### 备注

Auto-committed by `/tdd` phase-commit, plan: db-migrations-baseline/002-resume-versions-additive.

## 08:48 工作记录

### 完成事项

- 完成 `openapi-v1-contract/004-resume-additive-coverage`：将 Resume Workshop additive contract 落到 `Resumes` tag，新建 9 个 operation、resume version schema、fixtures 与 P0 `exportResumeVersion` 501 stub。
- 同步 B1 shared conventions，新增 resume version/tailor status enum 与 `RESUME_EXPORT_NOT_AVAILABLE` 错误码，并重新生成 Go/TS shared constants 与 OpenAPI Go/TS client/server artifacts。
- 更新 OpenAPI inventory、fixture validator、mock-contract-suite、engineering-roadmap、openapi-v1-contract spec/history/INDEX，并向 workspace 001 plan 写入 `listResumes` 解锁 handoff。
- 验证：`make codegen-check`、`make validate-fixtures`、`make lint-openapi`、`make openapi-diff`、`make docs-check`、lint 脚本单测、Go focused tests 与 frontend typecheck 均通过。

### 关联 Commit

- `feat(openapi): add resume workshop contract coverage`

### 备注

Auto-committed by `/tdd` phase-commit, plan: openapi-v1-contract/004-resume-additive-coverage.

## 08:50 工作记录

### 完成事项

- 收口 Resume Workshop contract additive 三个执行 plan 生命周期：B3 `event-and-outbox-contract/002`、B4 `db-migrations-baseline/002`、B2 `openapi-v1-contract/004` 均从 `active` 切换为 `completed`。
- 同步三个 plans/INDEX 的状态与更新日期，保证 plan/checklist/header/INDEX 当前事实一致。
- 验证：`make docs-check` 与 `sync-doc-index --fix-index` 后置校验均通过。

### 关联 Commit

- `docs(plans): close resume additive lifecycle`

### 备注

Auto-committed by `/tdd` completion lifecycle sync.

## 08:53 工作记录

### 完成事项

- 按 `/retrospective --this` 对 Resume Workshop contract additive 三 plan 交付做 post-pass 复盘，创建 `docs/reports/2026-05-12-resume-workshop-contract-additives-assessment.md`。
- 在 `docs/reports/INDEX.md` 增加 assessment 索引，记录跨 plan 顺序、generator hardcode、swagger-cli 命令形态与 prototype mapping gap 的后续改进建议。
- 验证：`make docs-check` 与 markdown whitespace check 通过。

### 关联 Commit

- `docs(retrospective): assess resume additive delivery`

### 备注

Auto-committed by `/retrospective --this`.

## 08:55 工作记录

### 完成事项

- 按 `/bug-report --this` 对 B3 `ResumeTailorMode` 漂移修复补录 Bug 知识库，新增 [BUG-0042](../bugs/BUG-0042.md)。
- 在 `docs/bugs/INDEX.md` 的 Schema 分组增加 resolved 记录，关联修复提交 `fix(events): align resume tailor mode contract`。
- 验证：`make docs-check` 与 markdown whitespace check 通过。

### 关联 Commit

- `docs(bugs): record resume tailor mode drift`

### 备注

Bug 模式库未直接修改；可在后续治理任务中把 cross-layer enum retire negative-grep 抽象为通用模式。

## 10:18 工作记录

### 完成事项

- L2 remediation：修复 `RegisterResumeRequest` 和 `ResumeAsset` 中 `fileObjectId` 必填漂移，改为 optional+nullable，使 paste/guided 等 fileless 简历入水通道可用。
- 更新 codegen `render_go.go`、`render_ts.go` 正确处理 nullable 字段生成，新增 `render_types_test.go` 验证渲染契约。
- 更新 generated Go/TS types、spec.ts、openapi.yaml，同步 nullable 变化。
- 补齐 `registerResume` paste-text 和 guided-answers fixture，更新 `listResumes` fixture 中非 upload 资产 `fileObjectId: null`。
- 新增 `openapi_inventory_test.py` 的 `test_register_resume_contract_supports_fileless_sources` 和 `validate_fixtures_test.py` 的 fileless 断言。
- 移除 events 测试中已退役的 `inline` 字面量引用（Go/TS）。
- 新增 [BUG-0043](../bugs/BUG-0043.md) 记录漂移修复，新增 [L2 Remediation 复盘报告](../reports/2026-05-12-resume-workshop-contract-additives-l2-remediation-assessment.md)。

### 关联 Commit

- `fix(openapi): allow fileless resume intake contracts`

### 备注

L2 remediation，覆盖 openapi 契约 + codegen + fixtures + 测试 + 文档。

## 11:02 工作记录

### 完成事项

- L2 remediation：修复 `branchResumeVersion` 的 `202` async response typing，新增 `BranchResumeVersionAccepted` schema，并让 generated TS client 返回 `ResumeVersion | BranchResumeVersionAccepted`。
- 修复 generated TS client 对显式 P0 `501` export response 的 typed parse path；`exportResumeVersion` / `requestPrivacyExport` 现在返回 `ApiErrorResponse`，未声明 non-OK 仍 throw。
- 补齐 Resume Workshop 9 个 operation fixture 到 frontend dev mock registry，并新增 Vitest 覆盖 operationId completeness、501 fallback 与 `ai_select` branch shape。
- 修复 `resume_versions` live migration test cleanup 顺序，按 child-first 删除并让 cleanup error fail test。
- 原地修订 B2/B4 spec、plan、checklist 与 INDEX，新增 [BUG-0044](../bugs/BUG-0044.md) 和 [L2 remediation 复盘报告](../reports/2026-05-12-resume-additive-client-cleanup-l2-remediation-assessment.md)。
- 验证：`go test ./backend/cmd/codegen/openapi -count=1`、`pnpm --filter @easyinterview/frontend test src/api/devMockClient.test.ts`、`pnpm --filter @easyinterview/frontend typecheck`、focused migration test、`make lint-openapi`、`make validate-fixtures`、`make openapi-diff`、`make docs-check` 均通过；当前 `DATABASE_URL` 未设置，live migration cases 明确 skip。

### 关联 Commit

- `fix(openapi): harden resume additive client contracts`

### 备注

L2 remediation，覆盖 OpenAPI/codegen/generated client、frontend fixture-backed dev mock、migration test cleanup、Bug 知识库与复盘报告。

## 13:23 工作记录

### 完成事项

- L1 remediation：修复 `frontend-resume-workshop/001-listing-routing-and-detail-readonly` 三处 plan-review gate drift。
- 将 `listResumeVersions` 从不可执行的 per-asset fixture fanout 改为当前 mock transport 可执行的 scenario-scoped 聚合，并要求无匹配版本 asset 显示 no-versions/partial 状态。
- 对齐 UI parity gate：clean checkout 常规 PASS 使用 DOM anchor、computed style、bounding box 与 screenshot smoke；只有稳定 baseline 可取得时才启用 screenshot diff。
- 补齐 `exportResumeVersion` P0 fallback 的 `Idempotency-Key` 契约，要求 `generateIdempotencyKey()` + generated client header spy 断言。
- 验证：`validate_context.py --target frontend`、`sync-doc-index.py --check`、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `docs(frontend-resume-workshop): fix listing plan review gates`

### 备注

Manual `/work-journal` close-out for `/plan-review frontend-resume-workshop/001-listing-routing-and-detail-readonly --fix`.

## 13:46 工作记录

### 完成事项

- 完成 `frontend-resume-workshop/001-listing-routing-and-detail-readonly` Phase 1：路由替换 + 容器骨架，覆盖 1.1–1.5 全部小节。
- 在 `App.tsx` 路由表把 `resume_versions` 从 `PlaceholderScreen` 切到新建的 `ResumeWorkshopScreen`，同步 `AppNormalize.test.tsx` 旧 alias 期望。
- 在 `frontend/src/app/screens/resume-workshop/` 新建容器 + `params.ts` route param 解析（flow / versionId / tab / branchOriginalId），TARGETED `tab=rewrites` 不会被改写为 preview。
- 实现 `NotImplementedPlaceholder` 组件 + i18n key（`resumeWorkshop.notImplemented.*`），覆盖 flow=create / branch P0 占位。
- 实现 `adapters/resume.ts`：`mapResumeAssetToUiSource` / `mapResumeVersionToUi` / `mapBulletSuggestionToUi`，10 个单测覆盖 langTag 推导、archived、parent chain、null parsedSummary、matchScore 0..1 → 0..100、suggestion 计数、deletedAt → archived。
- 实现 `ResumeWorkshopAuthGate` + i18n key（`resumeWorkshop.auth.*`），未登录态遮罩 list / detail / not-implemented，CTA 触发 `useRequestAuth({ type: "open_resume_workshop" })`，pendingAction 仅携带 `flow` / `versionId` / `tab` / `branchOriginalId` route params，断言不出现 raw text / parsedSummary / structuredProfile / originalText / suggestion 字段。
- 验证：focused vitest（resume-workshop / App / AppNormalize / i18n）+ `pnpm --filter @easyinterview/frontend test` 全量 710/710 通过；`git status` 仅落 phase 范围内文件。

### 关联 Commit

- `feat(frontend-resume-workshop): add resume workshop route container`

### 备注

Auto-committed by `/tdd` phase-commit, plan: frontend-resume-workshop/001-listing-routing-and-detail-readonly.

## 17:54 工作记录

### 完成事项

- 修复 Resume Workshop review follow-up 的 3 个错误态缺口：`listResumeVersions` 失败时显示 retryable error，不再渲染 0 版本；`getResumeVersion` 非 404 失败时显示详情错误态；原件弹层区分 source loading / error / ready，不再把 `structuredProfile` 摘要伪装成原始来源。
- 为 `useResumeVersions` / `useResumeVersion` / `useResumeAsset` 增加 retry trigger，并保留 request sequence guard。
- 补充 en/zh i18n copy、focused regression tests、`ResumeListAggregation` pagination 依赖 mock 调整，以及 [BUG-0046](../bugs/BUG-0046.md) / Bug INDEX 记录。
- 验证：focused ResumeListView / ResumeDetailView tests、`pnpm --filter @easyinterview/frontend test src/app/screens/resume-workshop` 84/84、`pnpm --filter @easyinterview/frontend typecheck`、前端全量 Vitest 779/779、`pnpm --filter @easyinterview/frontend build`、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `fix(frontend-resume-workshop): harden resume workshop error states`

### 备注

Manual `/work-journal` close-out for Resume Workshop error-state review remediation. See [BUG-0046](../bugs/BUG-0046.md).

## 16:42 工作记录

### 完成事项

- 完成 `/plan-code-review frontend-resume-workshop/001-listing-routing-and-detail-readonly --fix` 的 L2 code review remediation。
- 修复 `ResumeDetailView` 原文弹窗数据源：打开原件时按 `version.resumeAssetId` 调用 `getResume`，展示 resume asset 的 `parsedTextSnapshot/originalText`，不再复用 `structuredProfile` 的摘要文本。
- 补齐 resume workshop 正式前端样式与 parity gate：新增 `screens.css` 源值断言、运行时 computed-style 断言和 Playwright `/resumes/{id}` mock，覆盖 list/detail/modal 的关键布局、圆角、间距、shadow 与响应式行为。
- 同步 owner plan 文档：将 `getResume` 纳入 `plan.md`、`checklist.md`、`bdd-plan.md`、`bdd-checklist.md`、`context.yaml` 和 plans INDEX 的 operation matrix / fixture / BDD gate。
- 新增 `BUG-0045` 并在既有 delivery retrospective report 追加 L2 follow-up 复盘，记录原文弹窗和 CSS parity 漏审模式。
- 验证：frontend 全量 vitest 775/775 通过，frontend build 通过，resume-workshop Playwright parity 10/10 通过，P0.036/P0.037 BDD scripts 全 PASS，`validate_context.py`、`sync-doc-index --check`、`make docs-check`、negative grep 和 `git diff --check` 均通过。

### 关联 Commit

- `fix(frontend-resume-workshop): remediate listing detail L2 gaps`

### 备注

Manual `/work-journal` close-out for `/plan-code-review frontend-resume-workshop/001-listing-routing-and-detail-readonly --fix`.

## 14:02 工作记录

### 完成事项

- 完成 `frontend-resume-workshop/001-listing-routing-and-detail-readonly` Phase 2：ResumeListView + TreeView + FlatView + StatsStrip + VersionRow 子模块。
- 新增 `hooks/useResumeAssets.ts` 与 `hooks/useResumeVersions.ts` 走 generated client；listResumeVersions 单次 scenario-scoped 抓取后客户端按 `version.resumeAssetId` 分组。
- 新增 `ResumeListView.tsx`（StatsStrip 4 项 / ViewSwitcher tree·flat / 子视图调度 / paginated 提示 / loading + error 兜底）。
- 抽出 `ResumeTreeView.tsx` 折叠 + `选为底稿` / `基于这棵树新建版本` 按钮，触发 `window.eiToast` "即将开放" 提示；抽出 `ResumeFlatView.tsx` 按 `match DESC nullsLast / updatedAt DESC` 排序；抽出 `ResumeVersionRow.tsx` 复用渲染。
- 新增 i18n keys `resumeWorkshop.eyebrow / title / subtitle / stats.* / viewSwitcher.* / tree.* / flat.* / list.* / openVersion`。
- 新增 fixture parity 测试：listResumes default/empty/paginated 与 listResumeVersions default/master-only/with-targeted-branches 均按 fixture body 派生计数；显式断言 mock transport 不按 `resumeAssetId` path 参数选 scenario。
- 新增 aggregation 边界测试：empty 不触发 listResumeVersions；paginated `hasMore=true` 显示加载更多；default 第二个 asset 没匹配版本时显示 no-versions/partial 状态而不伪造 row。
- 验证：focused vitest（resume-workshop / fixture-parity / list aggregation）+ `pnpm --filter @easyinterview/frontend test` 全量 731/731 通过。

### 关联 Commit

- `feat(frontend-resume-workshop): wire resume list and tree views`

### 备注

Auto-committed by `/tdd` phase-commit, plan: frontend-resume-workshop/001-listing-routing-and-detail-readonly.

## 14:17 工作记录

### 完成事项

- 完成 `frontend-resume-workshop/001-listing-routing-and-detail-readonly` Phase 3：ResumeDetailView Preview Tab + 原件弹层 + 三 tab + 404 fallback + export idempotency。
- 新增 `hooks/useResumeVersion.ts` 调 `getResumeVersion`，404 统一识别为 notFound。
- 实现 `components/ResumeDetailView.tsx`：Breadcrumb + 版本分支图 + 三 tab；默认 tab 按 `version.versionType` 推断（MASTER→preview / TARGETED→rewrites），URL `tab` 优先。
- 实现 `components/ResumePreviewTab.tsx`：从 `structuredProfile` 投影 headline / summary / sections / skills；`buildResumePlainText` 走 clipboard，失败时 fallback warn toast。
- 实现 `components/ComingSoonTab.tsx`（rewrites / edit 两个变体）+ `components/NotFoundEmptyState.tsx`（generic copy 不依赖 fixture `error.code`）。
- 实现 `components/OriginalResumePreviewModal.tsx`：`role=dialog` + `aria-modal=true` + `aria-labelledby/-describedby` + ESC 关闭 + 外层遮罩关闭 + X 关闭 + Tab focus trap + 关闭恢复焦点。
- 在 `ResumeDetailView` 中通过 `generateIdempotencyKey()` 生成 `Idempotency-Key` 调 `exportResumeVersion(versionId, { idempotencyKey })`，无论 generated client 返回 ApiErrorResponse 还是抛错均触发 P0 "PDF 导出能力即将开放" toast，不生成 blob、不写 localStorage。
- 在 `auth/authContractGate.test.ts` 允许列表追加 `listResumes` / `listResumeVersions` / `getResumeVersion` / `exportResumeVersion`。
- 新增 i18n keys `resumeWorkshop.detail.*`（back / crumbVersions / tab* / copyText / exportPdf / viewOriginal / exportNotAvailable / copySuccess / copyUnavailable / loading / notFound* / comingSoon* / branch* / modal*）。
- 新增 vitest 覆盖：DetailView 容器（4 case 含 tab 切换），PreviewTab 内容 + clipboard toast + buildResumePlainText 投影，ComingSoonTab 间接覆盖，OriginalResumePreviewModal 6 case，DetailFixtureParity 4 scenario（含 404），DetailExport 1 case（idempotency-key + 501 toast + localStorage 红线）。
- 验证：`pnpm --filter @easyinterview/frontend test` 全量 750/750 通过。

### 关联 Commit

- `feat(frontend-resume-workshop): add resume detail view and export fallback`

### 备注

Auto-committed by `/tdd` phase-commit, plan: frontend-resume-workshop/001-listing-routing-and-detail-readonly.

## 14:23 工作记录

### 完成事项

- 完成 `frontend-resume-workshop/001-listing-routing-and-detail-readonly` Phase 4：i18n / a11y / Accept-Language / 隐私红线。
- 在 `useResumeAssets` / `useResumeVersions` / `useResumeVersion` 与 `ResumeDetailView` 的 export 调用统一从 `useDisplayPreferencesOptional()` 读取 lang，附加 `Accept-Language: <lang>` header；effect 依赖 lang 触发重抓。
- 新增 `ResumeWorkshopI18nA11y.test.tsx` 8 个集成 case：EN / ZH 关键文案切换、`listResumes` / `getResumeVersion` Accept-Language header 透传、ViewSwitcher / TreeView toggle / DetailView tab 的 `role="tab"` + `aria-selected` + `aria-expanded`、查看原件按钮打开 modal 后焦点落在关闭按钮。
- 新增 `ResumeWorkshopPrivacy.test.ts` 4 个静态 lint case：禁止 `console.*` 含 `rawText/originalText/parsedTextSnapshot/parsed_summary/parsedSummary/structured_profile/structuredProfile`，禁止 localStorage/sessionStorage 写入 resume PII，禁止 auth gate pendingAction 包含 PII 字段，禁止运行时 import `ui-design/src/data*` 或 `ui-design/src/screen-resume-workshop`。
- 验证：`pnpm --filter @easyinterview/frontend test` 全量 762/762 通过。

### 关联 Commit

- `feat(frontend-resume-workshop): add accept-language and privacy gates`

### 备注

Auto-committed by `/tdd` phase-commit, plan: frontend-resume-workshop/001-listing-routing-and-detail-readonly.

## 14:39 工作记录

### 完成事项

- 完成 `frontend-resume-workshop/001-listing-routing-and-detail-readonly` Phase 5：UI parity gate + BDD scenarios + retired/prototype grep + INDEX/spec sync。
- 新增 `frontend/tests/pixel-parity/resume-workshop.spec.ts`：复用 workspace.spec.ts 模式，按 desktop 1440 + mobile 390x844 双 viewport 跑 5 个场景，断言 list 与 detail 关键 testid + tree toggle aria-expanded + flat row 切换 + modal role/aria-modal + bounding box 与非空 screenshot smoke；clean checkout PASS 不依赖 snapshot baseline。
- 新增 `frontend/src/app/scenarios/p0-036-resume-list-tree-flat-toggle.test.tsx`（4 case：auth gate、StatsStrip 计数派生 + ViewSwitcher + tree row + no-versions、flat 切换、retired-route negative）和 `p0-037-resume-detail-preview-readonly.test.tsx`（5 case：MASTER preview、TARGETED rewrites coming-soon、modal a11y + ESC 关闭、Export Idempotency-Key + 501 toast + localStorage 红线、404 fallback）。
- 在 `test/scenarios/e2e/` 创建 `p0-036-resume-list-tree-flat-toggle/` 与 `p0-037-resume-detail-preview-readonly/` 完整 BDD scenario：`README.md` + `data/seed-input.md` + `data/expected-outcome.md` + `scripts/{setup,trigger,verify,cleanup}.sh`，trigger 调度对应 vitest scenario test 并通过 `verify.sh` 校验 testid 通过 + retired-route negative grep；本地执行 setup→trigger→verify→cleanup 全 PASS。
- 修两处 negative grep 假阳性：`adapters/resume.test.ts` fixture 文案去 "growth"；`ResumeWorkshopPrivacy.test.ts` 拼接 forbidden 路径段使 git grep 不命中自身。
- 在 `test/scenarios/e2e/INDEX.md` 追加 P0.036 + P0.037 行，关联需求与 Ready/automated 状态。
- `engineering-roadmap` §5.2 `frontend-resume-workshop` 已为 active（前置 plan 已落地），无需再次修订；`make docs-check` 全绿。
- 配套 build 修复：去掉 6 个 test helper 的 `: ReactNode` 注解 + 未使用的 `ReactNode` import；`adapters/resume.ts::deriveLangTag` 加显式取值守护；`ResumeDetailFixtureParity.test.tsx` 不假设 not-found scenario body 必含 id。
- 验证：`pnpm --filter @easyinterview/frontend test` 全量 771/771 通过；`pnpm --filter @easyinterview/frontend build` 通过；`pnpm exec playwright test tests/pixel-parity/resume-workshop.spec.ts` 10/10 通过；BDD scenario 双套 setup→trigger→verify→cleanup 全 PASS；retired/prototype grep 0 命中；`make docs-check` 全绿。

### 关联 Commit

- `feat(frontend-resume-workshop): add pixel parity and bdd scenarios`

### 备注

Auto-committed by `/tdd` phase-commit, plan: frontend-resume-workshop/001-listing-routing-and-detail-readonly.
