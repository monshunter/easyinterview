# 2026-05-05 工作日志

## 01:31 工作记录

### 完成事项

- 重新执行 historical spec deep reimplementation：忽略既有 PASS / completed 标记，完成 Scope Inventory、13 个现存可审 target 的 artifact-level L1/L2 审查、必要修复和 Final Reconcile。
- 补强 OpenAPI fixtures / inventory、events lint、migration lint、runtime-config allowlist、AIClient model-family、dev-stack lifecycle、Makefile drift path 等语义 gate，覆盖新版 product/UI spec 后的旧口径回归风险。
- 将用户反馈写入 `Historical Spec Deep Reimplementation Ledger` 的后续执行规章，明确 diff 大小、历史 gate 和历史 checklist 都不能作为完成证据。
- 完成全局验证：`make docs-check`、`make codegen-check`、DB-backed `make migrate-check`、focused tests、`make lint`、`make test`、`make build` 均通过。

### 关联 Commit

- `fix(historical-spec): deep reconcile existing plans`

### 备注

本轮保留 `ai-gateway-and-model-routing/002-tools-streaming-and-stt` 为 draft-gated，未创建任何新的 P0 workstream child spec / plan。

## 07:24 工作记录

### 完成事项

- 根据 `Historical Spec Deep Reimplementation Ledger` 复盘结果，更新 `AGENTS.md`，新增深度重校对门禁、禁止浅层收口、主动给出下一步和主动维护执行规章要求。
- 优化 `/plan-code-review` skill，要求 L2 review 做 artifact-level semantic review、负向旧口径搜索、语义 gate 充分性检查，并在输出中记录 `Deep Evidence`。
- 完成格式验证：`git diff --check` 通过。

### 关联 Commit

- `docs(governance): codify deep reconcile workflow`

### 备注

本次治理修订直接吸收 2026-05-05 historical deep reconcile 中暴露的浅层审查风险，并把用户“不要每次都追问下一步”的反馈写入全局行为规范。

## 08:03 工作记录

### 完成事项

- 按用户确认的方案 A 全面修订 A3 AI provider 口径：重命名 subject 目录与 ADR-Q6 文件，原地修订 A3 spec / plan / checklist，并同步 A2/A4/B1/roadmap 关联文档生命周期。
- 将 runtime config 从旧连接命名硬切到 `AI_PROVIDER_BASE_URL` / `AI_PROVIDER_API_KEY`、`ProviderBaseURL` / `ProviderAPIKey` / `ErrMissingProviderConfig` 与 `ai.provider*`，不保留旧 env/API fallback。
- 将 Model Profile route schema 切到 `route` / `Route`，同步 fixtures、loader tests、OpenAI-compatible contract tests、meta builder、README、deploy assets 与 generated AI vocabulary。
- 新增 `scripts/lint/ai_provider_terminology.py` 与 contract tests，并接入 `make lint-config`，active code/config/deploy/docs/generated artifacts 不再允许 retired provider terminology。
- 建立 [BUG-0007](../bugs/BUG-0007.md)，创建交付复盘报告，并完成验证：focused Go tests、`make lint-config`、`make lint`、`make test`、`make build`、`make docs-check`、codegen drift 复核与 active-scope 负向搜索均通过。

### 关联 Commit

- `fix(ai-provider): remove gateway terminology from provider contract`

### 备注

本轮没有创建 commit；codegen drift 复核使用临时 git index 避免污染真实 staging area，同时验证生成器运行后没有额外 generated artifact 漂移。

## 11:13 工作记录

### 完成事项

- 基于 product-scope 与 UI 交互重新梳理 A3 AI 使用面，将目标架构从单一 provider endpoint 扩展为 provider registry + capability-scoped Model Profile。
- 新增 `003-provider-registry-and-capability-profiles` plan / checklist / context，覆盖 registry loader、profile schema 迁移、central fallback、A4/B1/F3 联动和验证门禁。
- 同步 ADR-Q6、A3 spec / history / plans INDEX、A4 secrets-and-config、F3 prompt-rubric-registry、F1 observability 与 engineering-roadmap 相关口径。
- 修复 L1 review findings：为 profile 增加 `status` / `unsupported_reason`，按 protocol 校验 provider secret，并把 Product/UI capability catalog 中的 wildcard profile 改为具体 profile 名。
- 完成验证：`validate_context.py`、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `docs(ai-provider): define provider registry capability profiles`

### 备注

本次为 docs-only 设计与计划收口；后续实现应通过 `/implement ai-provider-and-model-routing/003-provider-registry-and-capability-profiles backend` 进入 TDD。

## 13:01 工作记录

### 完成事项

- 删除已迁移旧技术文档包实体目录，清理当前项目内旧目录名、旧文件名、旧 shorthand 和旧外部真理源口径。
- 在 product-scope §1.5 固化当前技术契约 owner matrix，明确 B1/B2/B3/B4/F1/F3/A1/A4 等 owner spec 与 coded truth source 各自承接的字段、schema、事件、指标、日志和 gate。
- 原地修订 engineering-roadmap 001 plan/checklist/context、active specs、history、ADR、报告和工作日志中的旧引用，并同步 generated OpenAPI / shared conventions artifacts。
- 将“历史包删除零残留”规则写入 `AGENTS.md`，要求后续删除旧目录时必须实体删除、zero-reference search 和 owner 独立承接检查。
- 完成验证：`validate_context.py`、`make docs-check`、旧名称负向搜索、`lint-openapi`、`validate-fixtures`、`openapi-diff`、`conventions_yaml.py`、`conventions_drift.py`、`codegen-events-check`、codegen idempotency hash 和 `git diff --check` 均通过。

### 关联 Commit

- `docs(roadmap): remove legacy tech docs package`

### 备注

`make codegen-check` 的最终 `git diff --exit-code` 在本次预期生成物变更下返回非零；已用二次 codegen diff hash 证明生成器幂等，未发现额外漂移。

## 13:28 工作记录

### 完成事项

- 完成 `ai-provider-and-model-routing/003-provider-registry-and-capability-profiles` Phase 1：落地 `config/ai-providers.yaml` provider registry schema、loader、A4 SecretSource 解析、selected-provider fail-fast、registry 热加载快照与负向 fixture 覆盖。
- 新增 provider registry tests，覆盖重复 provider、未知 protocol、capability 拼写错误、网络 provider 缺 env ref、profile provider ref 不存在、capability mismatch、被选中真实 provider secret 缺失、fallback 超 2 跳与 `stub` 无 secret 正向路径。
- 完成 focused verification：`cd backend && go test ./internal/ai/aiclient/... -count=1` 通过。

### 关联 Commit

- `feat(ai-provider): provider registry schema and loader`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-provider-and-model-routing/003-provider-registry-and-capability-profiles

## 15:22 工作记录

### 完成事项

- 修复 `ai-provider-and-model-routing/003-provider-registry-and-capability-profiles` L2 findings：新增生产 `bootstrap.NewClient`，启动时读取 registry/profile truth sources、校验 active provider secrets，并通过 provider resolver materialize provider-ref adapters。
- 补齐 profile hot reload warning evidence，失败 reload 保持旧快照并通过 `OnWarn` 输出可观测信号。
- 补强 `make lint-ai-profile-coverage` active anti-stub gate，将 repo-tracked active default profiles 从 `unit-test-stub` 切到 `default-openai-compatible`。
- 原地更新 003 plan/checklist、A3 spec/history/INDEX，创建 [BUG-0008](../bugs/BUG-0008.md) 与交付复盘报告。
- 完成验证：focused Go tests、profile coverage pytest、`make lint-ai-profile-coverage`、`make lint-config`、context validation、`make docs-check`、`make lint`、`make test`、`make build` 与 `git diff --check` 均通过。

### 关联 Commit

- `fix(ai-provider): wire provider registry runtime bootstrap`

### 备注

See [BUG-0008](../bugs/BUG-0008.md). 本轮为 `/plan-code-review --fix` L2 remediation，同一主题跨 backend/config/scripts/docs，但属于单一 runtime bootstrap 修复提交。

## 14:32 工作记录

### 完成事项

- 按 `/retrospective` 收尾要求创建 `ai-provider-registry-capability-profiles` 交付复盘报告。
- 复盘记录本次实现中的两个证据化痛点：worker entrypoint config test 漏迁移、ADR/spec active 部署文案滞后。
- 将建议落点限定到后续 A3/A4/C14/002 plan gate：补 entrypoint config gate 与 cross-spec active wording gate。
- 完成验证：`make docs-check` 通过，reports INDEX 已同步。

### 关联 Commit

- `docs(reports): land provider registry retrospective`

### 备注

Post-pass retrospective for ai-provider-and-model-routing/003-provider-registry-and-capability-profiles.

## 14:29 工作记录

### 完成事项

- 完成 `ai-provider-and-model-routing/003-provider-registry-and-capability-profiles` Phase 5：修复 worker prod config test 的新 registry/profile env 注入断言，重新跑通完整 global gates。
- 收口 active-scope negative search：将 ADR-Q4 / ADR-Q6 / A3 spec active 文案对齐为 registry/profile/provider ref 契约，并保留旧 key 仅用于拒绝逻辑、negative fixture 或迁移说明。
- 将 plan / checklist 生命周期切到 `completed`，同步 plans INDEX、docs/spec INDEX，并补充 002 / C14 / practice / report / resume / debrief / F3 eval owner handoff。
- 完成验证：`make lint-config`、`make codegen-check`、`make docs-check`、`make lint`、`make test`、`make build`、`sync-doc-index --check`、context validation、AI provider terminology lint 与相关 pytest 均通过。

### 关联 Commit

- `feat(ai-provider): complete provider registry verification`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-provider-and-model-routing/003-provider-registry-and-capability-profiles

## 14:20 工作记录

### 完成事项

- 完成 `ai-provider-and-model-routing/003-provider-registry-and-capability-profiles` Phase 4：A4 env/config 字典切到 `AI_PROVIDER_REGISTRY_PATH` + `AI_MODEL_PROFILE_PATH`，并把 provider registry 中的 secret env refs 纳入 `make lint-config`。
- 扩展 B1 `shared/conventions.yaml` AI vocabulary：生成 AI capability、provider registry/profile 字段名、AI meta/fallback 字段与 `AI_PROVIDER_CONFIG_INVALID` / `AI_PROVIDER_SECRET_MISSING` 等 provider/profile routing 错误码，同步 Go/TS/OpenAPI artifacts。
- 将 A3 runtime `Capability` 改为 B1 生成类型 alias，新增 provider registry sentinel 到 B1 `AI_*` code 的映射。
- 将 F3/Product UI profile coverage gate 接入 `make lint-ai-profile-coverage` 与顶层 `make lint`，并同步 ADR-Q6、A3/A4/B1/F3/roadmap spec、history 与 `docs/spec/INDEX.md`。
- 完成 focused verification：backend Go tests、frontend conventions tests、lint pytest、`make lint-config`、`make lint-ai-profile-coverage`、`make docs-check`、context validation 与 `git diff --check` 均通过。

### 关联 Commit

- `feat(ai-provider): integrate provider registry contract gates`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-provider-and-model-routing/003-provider-registry-and-capability-profiles

## 13:40 工作记录

### 完成事项

- 完成 `ai-provider-and-model-routing/003-provider-registry-and-capability-profiles` Phase 2：Model Profile schema 从 `task_type` / `default.provider` 迁移到 `capability` / `provider_ref` / `status`，并拒绝旧 key fallback。
- 补齐 F3 12 个 baseline feature_key 与 Product/UI capability catalog 对应的 tracked profile fixtures，使用 `disabled` / `unsupported` + `unsupported_reason` 表达 P1/P2/002+ 不可执行能力。
- 新增 `scripts/lint/ai_profile_coverage.py` 与 pytest 覆盖，校验 F3 / Product UI 默认 profile、provider ref、capability、status 和 provider capability 对齐。
- 完成验证：`cd backend && go test ./internal/ai/aiclient/... -count=1`、`python3 -m pytest scripts/lint/ai_profile_coverage_test.py -q`、`python3 scripts/lint/ai_profile_coverage.py --repo-root .`、`git diff --check` 均通过。

### 关联 Commit

- `feat(ai-provider): capability-scoped model profiles`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-provider-and-model-routing/003-provider-registry-and-capability-profiles

## 13:59 工作记录

### 完成事项

- 完成 `ai-provider-and-model-routing/003-provider-registry-and-capability-profiles` Phase 3：AIClient 按 profile `capability` / `provider_ref` / `status` fail-closed 路由，disabled / unsupported / mismatch profile 返回 B1 `AI_UNSUPPORTED_CAPABILITY`。
- 落地 central fallback chain：primary 超时按 `when[]` 命中最多 2 跳 fallback，fallback 成功 / 条件不命中 / fallback exhausted / 超限路径均有 focused test 覆盖。
- 更新 observability fallback labels，拆分 `from_provider` / `from_model_family` / `to_provider` / `to_model_family`，并保持 privacy regression。
- 将 openai-compatible adapter 改为从 `providerregistry.ResolvedProvider` 构造，`Provider.Name()` 返回 registry provider ref，并保留 `/v1` base URL 归一化测试。
- 同步 B1 `AI_UNSUPPORTED_CAPABILITY` 与 OpenAPI / Go / TS generated artifacts。

### 关联 Commit

- `feat(ai-provider): route capability profiles with fallback`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-provider-and-model-routing/003-provider-registry-and-capability-profiles

## 16:00 工作记录

### 完成事项

- 按用户确认的方案原地修订 `ai-provider-and-model-routing/003-provider-registry-and-capability-profiles`，将 Model Profile 从“一 profile 一 YAML”收敛为单一 catalog truth source：`config/ai-profiles.yaml`。
- 更新 profile loader、bootstrap 配置、lint coverage、tracked fixtures 与相关 Go / Python 测试，明确 legacy `config/ai-profiles/` 目录路径不再作为运行时输入。
- 同步 A3/A4/F3 spec、history、plan/checklist/context、config README、`.env.example` 与 retrospective 报告，完成目录旧引用负向搜索。
- 完成验证：focused Go tests、profile coverage pytest、`make lint-ai-profile-coverage`、`make lint-config`、context validation、`make codegen-check`、`make lint`、`make test`、`make build`、`make docs-check`、`sync-doc-index --check` 与 `git diff --check` 均通过。

### 关联 Commit

- `refactor(ai-provider): consolidate model profile catalog`

### 备注

Plan remediation for ai-provider-and-model-routing/003-provider-registry-and-capability-profiles, section 2.5 / 5.6.
