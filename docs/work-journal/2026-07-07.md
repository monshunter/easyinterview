# 2026-07-07 工作日志

## 13:06 工作记录

### 完成事项

- 继续收口 `product-scope/001-core-loop-module-pruning` Phase 6：压缩 active owner specs / plans / histories / contexts，删除不再承接当前合同的 standalone report 和旧文档包引用，并同步 `docs/spec`、`docs/ui-design`、场景资产、OpenAPI/shared/generated、backend、frontend 与 `ui-design/` 的当前范围口径。
- 将 repo-wide negative gate、scenario slug、skill 指令、上下文 manifest、auth email-code、flat Resume、workspace insight、fallback shell、OpenAPI 35-operation inventory 和 event/job contract 对齐到 current / non-current 术语。
- 修复 work-journal 可追溯性细节：历史 `关联 Commit` 与 `INDEX.md` 中的 commit subject 保持与真实 `git log` 完全一致，避免术语清理破坏 commit message 索引契约。
- 验证通过：`sync-doc-index --check`、`make docs-check`、`git diff --check`、51 个 context manifest 批量校验、`product-scope/001` cross-layer context 校验、`make lint-core-loop-pruning-surface`、skill contract pytest、backend focused Go tests、frontend focused Vitest、`make lint-events`、`make lint-openapi`、`make openapi-diff`、`make validate-fixtures`、`node --test ui-design/ui-design-contract.test.mjs`。

### 关联 Commit

- `fix(core-loop): close repo pruning cleanup`

### 备注

Owner plan: `product-scope/001-core-loop-module-pruning`.

## 14:20 工作记录

### 完成事项

- 修复 `fix/repo-pruning-cleanup-0706` review remediation：`core_loop_pruning_surface` 不再用单行 negative wording 放行生产路径 residual，并补充生产路径反例测试。
- 修复 `ResumePickerModal` stale / synthetic bound resume id guard，确认按钮只接受当前加载出的 active resume id，并补充 focused Vitest。
- 修复 pnpm 11 `allowBuilds` 占位配置，执行 gofmt 漂移清理，并清理 stricter pruning gate 曝光的 active README / backend comment residual。
- 新增 [BUG-0136](../bugs/BUG-0136.md) 和交付复盘报告，记录 review 暴露的 lint、frontend guard 与 pnpm gate 漂移。

### 关联 Commit

- `fix(review): close repo pruning cleanup findings (BUG-0136)`

### 备注

Owner plan: `product-scope/001-core-loop-module-pruning`.

## 16:55 工作记录

### 完成事项

- 闭环修复 Resume Workshop 创建与详情回归：upload / paste 注册成功后直接打开只读详情，不再渲染解析动画页、预览确认页或确认保存页。
- 详情页正文改为优先展示 `parsedTextSnapshot` / `originalText` 原始内容，移除结构化投影造成的信息丢失。
- 简历名称过滤通用“粘贴的简历 / 上传的简历”类来源标签，前端从 LLM-derived `displayName`、原文、文件名或结构化字段派生可识别兜底名称，backend parse 也补强 displayName 派生规则。
- 同步 `frontend-resume-workshop` owner spec/plan/BDD、`docs/ui-design`、`ui-design` contract、P0.037/P0.081-P0.084 场景资产、[BUG-0137](../bugs/BUG-0137.md) 与交付复盘报告。

### 关联 Commit

- `fix(resume-workshop): make resume creation direct and detail readonly (BUG-0137)`

### 备注

Owner plans: `frontend-resume-workshop/001-listing-routing-and-detail-readonly`, `frontend-resume-workshop/002-create-flow`.

## 17:42 工作记录

### 完成事项

- 闭环修复 BUG-0137 二次回归：paste 创建只提交中性来源标题，列表和详情名称不再从原文第一行、上传文件名或通用来源标签兜底，解析前统一显示“名称生成中”。
- 后端 upload parse 对 PDF、DOCX、Markdown、text 输入提取可读正文，AI prompt input 与 `parsed_text_snapshot` 使用同一份正文；queued 创建不再预填 `display_name`。
- 同步 `frontend-resume-workshop`、`backend-resume` owner spec/plan/checklist/BDD、`docs/ui-design`、`ui-design` 原型、P0.035/P0.037/P0.081 场景资产、[BUG-0137](../bugs/BUG-0137.md) 与复盘报告。
- 验证通过：frontend focused Vitest、frontend resume workshop 全量 Vitest、backend resume focused Go tests、P0.035/P0.037/P0.081 场景脚本、`sync-doc-index --fix-index` 后零漂移检查。

### 关联 Commit

- `fix(resume-workshop): use parse-derived names and upload text (BUG-0137)`

### 备注

Owner plans: `frontend-resume-workshop/001-listing-routing-and-detail-readonly`, `frontend-resume-workshop/002-create-flow`, `backend-resume/001-asset-register-parse-and-listing`.

## 18:19 工作记录

### 完成事项

- 闭环修复 BUG-0137 三次回归：真实上传 PDF 不再因 256KiB 读取预算截断而空白，PDF 抽取优先使用 `pdftotext`，并对 Go parser / literal fallback 增加可读性 gate，拒绝 PDF binary / literal 乱码进入 prompt 或 `parsed_text_snapshot`。
- 后端 parse failure path 在已抽取正文后遇到 AI provider / AI output 失败时仍保存 `parsed_text_snapshot`，确保只读详情能展示原始简历内容；成功 completed event 仍只在 ready 路径发出。
- 前端 adapter 继续过滤通用来源名、raw 第一行、上传文件名和与来源 title 相同的文件名 `displayName`，文件名只作为来源信息展示。
- 同步 `backend-resume` / `frontend-resume-workshop` owner spec、plan、checklist、P0.035 场景 gate、[BUG-0137](../bugs/BUG-0137.md) 与交付复盘报告。
- 验证通过：backend resume focused / broad Go tests、frontend adapter/detail Vitest、E2E.P0.035 / P0.037 场景脚本、`sync-doc-index --check`、`make docs-check`、`git diff --check`；local UAT 对真实 PDF resume `019f3bfb-7d9d-76d2-a82d-08bcbac225ce` 重排后，`parse_status=failed` / `AI_OUTPUT_INVALID` 仍保留 3083 字符可读中文 `parsed_text_snapshot`。

### 关联 Commit

- `fix(resume-workshop): read full upload PDFs (BUG-0137)`

### 备注

Owner plans: `frontend-resume-workshop/001-listing-routing-and-detail-readonly`, `backend-resume/001-asset-register-parse-and-listing`.

## 18:55 工作记录

### 完成事项

- 闭环修复 BUG-0138：`resume.parse` prompt/schema 现在 required `displayName`，并要求模型生成候选人姓名 + headline / role / 技术定位组成的有意义名称。
- 后端 parse 成功路径优先消费并验证 AI `displayName`；AI provider / output 失败但已有可读正文时，失败事务写入正文派生 fallback `display_name`，避免详情长期停留在“名称生成中”。
- 为 `ResumeDetailView` 和 E2E.P0.037 增加 failed-with-snapshot 单次请求回归 gate，确保只有 upload `queued|processing` 且无可读正文时才轮询同一详情 URL。
- 同步 `backend-resume` / `frontend-resume-workshop` owner spec、plan、checklist、BDD、P0.035/P0.037 场景资产、prompt seed/eval export、[BUG-0138](../bugs/BUG-0138.md) 与交付复盘报告。
- 验证通过：backend touched package Go tests、frontend resume workshop + P0.037 Vitest、`make lint-prompts`、`make lint-prompts-hardcode`、AI registry test、evalkit drift-check、E2E.P0.035 / P0.037 标准场景流程、`sync-doc-index --check`、`make docs-check`、`git diff --check`。

### 关联 Commit

- `fix(resume-workshop): generate names and stop detail polling (BUG-0138)`

### 备注

Owner plans: `frontend-resume-workshop/001-listing-routing-and-detail-readonly`, `backend-resume/001-asset-register-parse-and-listing`.
