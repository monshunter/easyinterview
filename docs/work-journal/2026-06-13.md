# 2026-06-13 工作日志

## 15:56 工作记录

### 完成事项

- **D-20 简历扁平化契约基础层 owner 文档原地修订（B1/B4/B3/B2）**：按 product-scope v2.1 D-20（简历资产扁平化：删除版本树 / 主版本 / 岗位定制 / 轻量问答，面试绑定改 `resumeId`，改写仅采纳 → 预览覆盖 / 另存），经 `/change-intake` 路由分类为 design/feature change，按依赖顺序逐 owner 原地修订。数据模型决策（用户裁定，Option B）：物理重命名 `resume_assets`→`resumes`、`resume_asset_id`→`resume_id`，合并 master `structured_profile` 进表，删除 `resume_versions` / `resume_version_suggestions` / `resume_tailor_runs` 三表，契约 `resumeAssetId` / `resumeVersionId` 统一坍缩为 `resumeId`。
  - **B1 shared-conventions-codified** spec 1.20→1.21：新增 D-20 决策，退役 D-10 的 3 个 resume version 生成枚举（`ResumeVersionType` / `ResumeSeedStrategy` / `ResumeTailorSuggestionStatus`）与 `RESUME_STRUCTURED_MASTER_ALREADY_EXISTS` 错误码，保留 `RESUME_EXPORT_NOT_AVAILABLE`，术语映射重写为单一 `Resume`；修正历史计数漂移（`shared/conventions.yaml` 实际生成枚举 19、spec 旧述 17）→ 退役 3 个后 16。
  - **B4 db-migrations-baseline** spec 1.22→1.23 + history 1.23 + plan 002 新增 Phase 6（D-20 flatten migration `000015_resume_flatten`）+ checklist + context：`resume_assets`→`resumes` + `structured_profile` / `display_name` + structured_master backfill，`practice_plans.resume_asset_id`→`resume_id`，按 FK 反序 drop 三表，`enum-sources.yaml` 移除 3 项 + `source_type` 去 `guided`。**同步回填 product-scope D-17 jd_match drop 残留漂移**：§2.1 / §3.1.2 / §6 应用表 33（陈旧）→ 28（post-D-17 真实）→ 25（post-D-20）。
  - **B3 event-and-outbox-contract** spec 2.7→2.8 + history 2.8：新增 D-17，`resume.parse.completed` / `resume.tailor.completed` 的 `resumeAssetId`→`resumeId`，event resource label 收敛 `resume`；`resume_tailor` job_type 与事件保留（AI 改写延续，suggestions ephemeral，`tailorRunId` 改指 AI task run id）。
  - **B2 openapi-v1-contract** spec 1.29→1.30 + history 1.30 + plan 004 新增 Phase 7（contract collapse）+ checklist + context：新增 D-26，删除 6 个版本 / suggestion op、重命名 3（`updateResume` / `archiveResume` / `exportResume`）、新增 `duplicateResume`（另存）、`resumeAssetId` / `resumeVersionId`→`resumeId`、schema `ResumeAsset`→`Resume` 等；op 43 / tag 12。**同步回填 product-scope D-17 残留计数漂移**：§2.1 / §3.1.1 endpoint 60（陈旧）→ 48（post-D-17 真实）→ 43（post-D-20）。
- **盘点依据**：2 个 Explore 全仓盘点（跨层 `resumeVersionId` 足迹 64 frontend + 30 backend 文件、13 个 owner subspec）+ migration 真理源权威表计数（CREATE − DROP = 28 app 表）+ `openapi.yaml` `operationId` 计数（48）。
- **验证**：`sync-doc-index --check` 零漂移（INDEX 投影 8 文件已同步）；`make docs-check` 通过（无 broken link）。

### 关联 Commit

- `docs(resume): revise contract-foundation specs for d-20 flatten`

### 备注

D-20 跨约 13 个 subspec，本提交为契约基础层（B1/B4/B3/B2）doc 子集。剩余 owner doc 修订（backend C7/C5/C9、frontend D-X/D-Z/D-Y、product-scope/roadmap）与 `/plan-review --fix`、跨层实施（migration + openapi + codegen + backend + frontend）、场景清理、收口按依赖顺序后续推进。发现并固化两处 product-scope D-17 删除残留漂移（B4 应用表计数、B2 endpoint 计数）：D-17 当时只建 `000014_drop_jd_match_module` / 删 openapi JobMatch op，未回填 owner spec §2.1 / §3.1.x / §6 与 history，本次随 D-20 一并修正。

## 16:24 工作记录

### 完成事项

- **D-20 简历扁平化后端 owner 文档原地修订（C7/C5/C9）**：
  - **C7 backend-resume** spec 1.3→1.4：§1/§2 重写为单一扁平 `resumes` 表 + 9 op；新增 D-13（扁平化综合决策），退役/重塑 D-1~D-12（版本树 / master / branch / suggestion / tailor-run）；§6 验收 C-10/C-11/C-14/C-15 退役、其余重塑 `resumeId`/`resumes`、新增 C-17（updateResume 覆盖）/ C-18（duplicateResume 另存）；plan 001 Phase 7（store / handler / parse 迁移 `resumeId` + `structured_profile`）+ plan 002 Phase 10（删 7 个版本/suggestion handler + 重塑 tailor ephemeral + 新增 update/duplicate）；checklists / contexts / bdd / test 同步回 `active`。
  - **C5 backend-practice** spec 1.11→1.12 + history 1.12：新增 D-39，`createPracticePlan` / `practice_plans` 绑定 `resumeAssetId` / `resume_asset_id`→`resumeId` / `resume_id`，移除「简历主版本 / 岗位定制版本」上下文口径；plan 001 Phase 4（resumeId 绑定迁移）。
  - **C9 backend-debrief** spec 1.3→1.4 + history 1.4：新增 D-19，`suggestDebriefQuestions` `resumeVersionId?`→`resumeId?`、handler 上下文 / SQL `(user_id, resume_id)` / 复盘面试 handoff payload 同步；plan 001 Phase 8（resumeId 重命名）。
- **验证**：`sync-doc-index --check` 零漂移（含 12 个 bdd / test 同级文档随父 plan 回 `active`）；`make docs-check` 通过。

### 关联 Commit

- `docs(resume): revise backend specs for d-20 flatten`

### 备注

D-20 backend phase 的 BDD 场景 **content**（`updateResume` / `duplicateResume` / tailor-ephemeral 扁平场景、`resumeId` 重命名负向断言）尚未在各 `bdd-plan` / `bdd-checklist` 写入具体场景编号，待 `/plan-review` 与 backend 实施阶段补齐（本次仅同步 bdd / test 文档状态回 `active` 以保持 INDEX 一致）。剩余 owner doc：frontend D-X / D-Z / D-Y、product-scope / roadmap；再 `/plan-review --fix`，再跨层实施（migration + openapi + codegen + backend + frontend）。

## 16:37 工作记录

### 完成事项

- **D-20 简历扁平化前端 owner SPEC 原地修订（D-X / D-Z / D-Y，spec 层）**：
  - **D-X frontend-resume-workshop** spec 1.2→1.3：§1 / §2 组件列表重写为扁平（删除 `ResumeTreeView` / `ResumeFlatView` / `ResumeBranchFlow` / `ResumeVersionRow`，保留 `ResumeListView` 平铺 + `ResumeDetailView` preview/rewrites/edit + `RewriteSaveConfirmModal` + `ResumeCreateFlow` upload/paste）；route param `versionId`→`resumeId`、删 `branch` / `branchOriginalId`；新增 D-8（扁平化综合决策），重塑 D-2/D-3/D-6/D-7；§6 C-2 / C-3（退役）/ C-4 / C-10 / C-11 重塑、C-9 负向 grep 加版本树 terms；§3.2 guided 删除 + accept/reject 改「仅采纳 + save」。
  - **D-Z frontend-workspace-and-practice** spec 1.4→1.5：`InterviewContext` `resumeVersionId`→`resumeId`、`ResumePickerModal` 扁平（去版本展开 `listResumeVersions`）、operation matrix 删 `listResumeVersions` 行；新增 D-15。
  - **D-Y frontend-debrief** spec 1.6→1.7：Resume picker 扁平（去 asset→version 二级展开）、`resumeVersionId` / `resumeAssetId`→`resumeId`、`getResumeVersion`→`getResume`、operation matrix 删 `listResumeVersions` 行；新增 D-19。
- **验证**：`sync-doc-index --check` 零漂移；`make docs-check` 通过。

### 关联 Commit

- `docs(resume): revise frontend specs for d-20 flatten`

### 备注

**前端 owner SPEC 层（设计真理源）已闭环；前端 PLAN 的 D-20 phase 与 bdd / test 文档 active 翻转为紧接下一步**（D-X 001/002/003、D-Z 001/002、D-Y 001——specs 已 forward-reference 这些 phase，下一 commit 补齐以消除 spec(active/D-20)–plan(completed) 暂态不一致，非浅层收口）。随后 product-scope / roadmap 词汇 + `/plan-review --fix`，再跨层实施。至此全部 owner SPEC（B1/B4/B3/B2 + C7/C5/C9 + D-X/D-Z/D-Y）已对齐 D-20。

## 16:42 工作记录

### 完成事项

- **D-20 简历扁平化前端 PLAN 的 D-20 phase（消除 spec→plan forward-ref）**：为 6 个前端 plan 新增 D-20 phase + 回 `active` + bdd / test 同级文档回 `active` + context specVersion 同步：
  - `frontend-resume-workshop/001`（Phase 6：平铺列表 + `resumeId` + adapter）、`002`（Phase 7：CreateFlow upload/paste 删 guided + 删 confirmStructuredMaster）、`003`（Phase 8：删 branch/ResumeBranchFlow + rewrites 仅采纳 + `RewriteSaveConfirmModal` 覆盖/另存 + `updateResume`/`duplicateResume`）。
  - `frontend-workspace-and-practice/001`（Phase 7：`InterviewContext`/`routeUrl`/`useWorkspaceResume` `resumeId` + `ResumePickerModal` 扁平）、`002`（Phase 6：session context/handoff `resumeId` 透传）。
  - `frontend-debrief/001`（Phase 10：Resume picker 扁平 + context/nav payload/`suggestDebriefQuestions`/`createPracticePlan` `resumeId` + `getResume`；Phase 9 为既有收口，D-20 续编 Phase 10）。
- 各 plan / checklist 版本 +0.1；每个 D-20 phase 含实施 + 收口（零版本树残留 grep）双 task。
- **验证**：`sync-doc-index --check` 零漂移；`make docs-check` 通过。

### 关联 Commit

- `docs(resume): add d-20 phases to frontend plans`

### 备注

**D-20 owner 文档层（spec + plan + checklist + context + bdd/test 状态）全部对齐完成**（契约基础 + 后端 + 前端三层均已提交）。前端 plan 的 D-20 phase 与 backend 一致采用「实施 + 收口」概要 + 引用 spec D-8/D-15/D-19 详情；BDD 场景 content（与 backend 同）待 `/plan-review` 与实施阶段补具体编号。下一步：product-scope / roadmap 词汇收尾 + `/plan-review --fix` 全量审查（task #4），再进入跨层实施（migration + openapi + codegen + backend + frontend，task #5-#8）。

## 16:48 工作记录

### 完成事项

- **engineering-roadmap 对齐 D-20**（spec 3.23→3.24 + history）：Resume Workshop workstream 行 scope 从「原始简历树 / 结构化主版本 / 岗位定制版本 / 版本详情」改为「扁平简历列表 / 上传 / 粘贴创建 / 解析预览确认 / 简历详情（预览 / 改写 / 编辑）/ 改写采纳覆盖或另存」；`frontend-resume-workshop` 001/002/003 与 `backend-resume` 001/002 标记 D-20 phase active。
- **product-scope 核实**：D-20 决策为源（已锁），§5.4 / §6 discarded 表与平铺简历 scope 描述（§5.x / C-17）已正确，无需改动。
- **mock-contract-suite（E1）判定**：当前 48 op / `exportResumeVersion` / `confirmResumeStructuredMaster` 示例仍准确（openapi 未改），其 48→43 计数与 op 示例同步归 D-20 contract impl 阶段（task #5），非 doc 阶段。
- **验证**：`sync-doc-index --check` 零漂移；`make docs-check` 通过。

### 关联 Commit

- `docs(roadmap): align resume workstream scope with d-20 flatten`

### 备注

D-20 owner doc 对齐全部完成（contract 基础 + 后端 + 前端 + roadmap，5 commit）。task #4 剩 `/plan-review --fix` 验证门禁（含 BDD 场景 content 补齐），随后跨层实施。
