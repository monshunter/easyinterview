# 2026-05-24 工作日志

## 00:37 工作记录

### 完成事项

- 执行 `plan-code-review prompt-rubric-registry/002-output-schema-contract --fix` 的 L2 remediation：发现 A3 `validateOutputSchema` 只解码首个 JSON value，可能放过 schema-valid JSON 后追加 trailing prose 的模型输出。
- 在 Phase 6 追加 strict JSON trailing token remediation，新增 `TestDecorator_OutputSchemaRejectsTrailingTokens`，先红后绿修复 validator，使第二次 decode 只接受 EOF，否则返回 `AI_OUTPUT_INVALID`。
- 同步 `002-output-schema-contract` plan / checklist v1.2、plans INDEX、[BUG-0095](../bugs/BUG-0095.md)、Bug pattern 8 与交付复盘报告。

### 关联 Commit

- `fix(prompt-rubric): reject trailing AI output schema content (BUG-0095)`

### 备注

- 验证通过：focused output-schema tests、focused `go test -list` no-op guard、prompt lint pytest、`make lint-prompts`、目标 Go race regression、`make lint`。

## 09:04 工作记录

### 完成事项

- 修复 `prompt-rubric-registry/002-output-schema-contract` 的 prompt example 质量缺陷：renderer 不再输出 required-only 的 `string` / `1` 占位 JSON，而是生成包含 schema-declared required + optional 字段的完整代表性 JSON output，并明确不是 JSON Schema / OpenAPI schema。
- 重渲染 13 个 chat feature_key × 2 language coordinates 的 prompt contract block，刷新 26 个 YAML `template_hash`，同步 seed migration 中已有 prompt seed row 的 body/hash。
- 同步 `prompt-rubric-registry` spec v2.6、002 plan/checklist v1.3、context discovery、BUG-0096 与交付复盘报告。

### 关联 Commit

- `fix(prompt-rubric): render complete prompt example outputs (BUG-0096)`

### 备注

- 验证通过：prompt lint pytest、`make lint-prompts`、placeholder negative search、focused registry preflight、`validate_context.py`、`sync-doc-index --check`、`make docs-check`、`make lint`、`git diff --check`。

## 09:29 工作记录

### 完成事项

- 执行 `plan-code-review prompt-rubric-registry/002-output-schema-contract --fix` 的 seed migration L2 remediation：发现 baseline seed gate 仍写死 11 个历史 feature_key，漏掉已落地的 `jd_match.recommendation` / `jd_match.search` prompt/rubric seed rows。
- 重写 `TestSeedMigrationCoversBaselineFeatureKeys`，从 `config/prompts` active YAML 与 `config/rubrics` YAML 反推期望坐标，扫描全部 baseline prompt/rubric seed migration，拒绝缺行、额外行、重复行与 prompt hash drift。
- 新增 `000010_seed_baseline_prompt_rubric_versions_jd_match` up/down migration，补齐 JD-Match recommendation/search 的 en/multi prompt_versions 与 rubric_versions rows。
- 同步 `002-output-schema-contract` plan/checklist v1.4、context discovery、BUG-0097、Bug pattern 9 与交付复盘报告。

### 关联 Commit

- `fix(prompt-rubric): seed jd match prompt registry rows (BUG-0097)`

### 备注

- 验证通过：focused seed coverage red/green、registry package test、prompt/rubric/migration lint、dev-stack Postgres `make migrate-check`、context validation、sync-doc-index 与 whitespace gate。

## 10:30 工作记录

### 完成事项

- L2 review follow-up（BUG-0098）：将 `jd_match.recommendation` prompt schema 的 `posted` 从 required 降级为 optional，同步 seed migration template_body/hash、FEATURE_CONTRACTS、prompt block 与 YAML template_hash。
- 修复 `prompt_lint.py` 在 schema subset/contract validation 已失败时仍调用 renderer 触发 `KeyError` traceback 的缺口；新增 `test_missing_schema_description_reports_lint_error_without_traceback` 回归测试。
- 新增 `test_jd_match_recommendation_posted_is_optional_contract` 回归测试，防止 required 字段与生产 caller 输入再次漂移。
- 同步 `002-output-schema-contract` plan v1.4→v1.5、checklist Phase 10、context.yaml discovery terms；plans/INDEX 版本同步。
- 更新 `docs/bugs/PATTERNS.md` 模式 5 追加第 5 条（AI output schema required 字段必须反查生产输入与 consumer optionality）。
- 建档 [BUG-0098](../bugs/BUG-0098.md)（posted drift + lint traceback）。
- 撰写 [prompt-rubric output schema L2 review follow-up 评估报告](../reports/2026-05-24-prompt-rubric-output-schema-review-follow-up-assessment.md)。
- 验证通过：`python3 -m pytest scripts/lint/prompt_lint_test.py -q` → `13 passed`；`python3 scripts/lint/prompt_lint.py` → `26 files clean`；`rubric_lint` + `migrations_lint` PASS；`make migrate-check` PASS；`go test ./backend/internal/ai/registry -count=1` PASS；focused A3 output schema + JDMatch adapter tests PASS；`validate_context.py` PASS；`sync-doc-index --check` zero drift。

### 关联 Commit

- `fix(prompt-rubric): close output schema review findings (BUG-0098)`

### 备注

- L2 follow-up 仅涉及一次 Phase 10 收口：修复 `posted` required drift（生产不提供 freshness）与 lint traceback（缺 description 时）。不涉及产品行为变更。

## 14:10 工作记录

### 完成事项

- 修复 `frontend-home-job-picks-and-parse/001-home-jd-import-and-parse` 的 parse loading 回归：首次 `getTargetJob` 返回 `analysisStatus=ready` 时不再直接进入 preview，而是先按 `ui-design` `[600, 900, 800, 700] + 200ms` 展示 4 步 loading 演示。
- 在 `ParseScreen` 中拆分 `pendingReadyJob` 与 `loadingComplete`，ready job 暂存到 loading demo 完成后再 hydrate preview；`Re-parse` 会重置 pending ready 与 timer gate。
- 更新 `ParseFlow` ready-response regression test，并同步 `ParseEdit` / `ParseAuthGate` / `ParseFailedState` 的 preview helper，防止测试继续依赖立即 preview。
- 原地修订 owner spec/plan/checklist/BDD checklist/context/INDEX，建档 [BUG-0099](../bugs/BUG-0099.md)，并新增 [Frontend Parse Loading Demo Regression 交付复盘](../reports/2026-05-24-frontend-parse-loading-demo-regression-assessment.md)。

### 关联 Commit

- `fix(frontend-home): preserve parse loading demo (BUG-0099)`

### 备注

- 验证通过：focused ParseFlow test、parse suite、全量 frontend Vitest、frontend build、E2E.P0.015 setup/trigger/verify/cleanup、context validation、sync-doc-index、docs-check、diff whitespace gate。

## 14:18 工作记录

### 完成事项

- 完成 `prompt-rubric-registry/003-language-coordinate-simplification`：将 baseline prompt/rubric truth source 收敛为 canonical `multi`，删除 39 个重复 `v0.1.0.en.*` 文件。
- 更新 prompt/rubric README、lint 脚本与测试，新增 language override allowlist 和 `{{language}}` runtime-language instruction gate。
- 更新 registry loader/resolver/seed coverage tests：snapshot 收敛为 13；`en` 请求 fallback 到 `multi`；missing canonical multi 与 orphan override 被拒绝。
- 清理 baseline seed migration 的 `en` prompt/rubric rows，同步 spec/history/plan/checklist/context/index 与交付复盘报告。
- 验证通过：prompt/rubric lint tests、registry package tests、migration lint、context validation、sync-doc-index、docs-check、migrate-check、`make lint`、`git diff --check`。

### 关联 Commit

- `refactor(prompt-rubric): simplify language coordinate registry`

### 备注

- 多语言用户体验保留在 runtime `language` 参数、prompt `{{language}}` 指令与 resolver fallback；隐藏配置层不再维护无语义差异的 `en` 副本。

## 14:25 工作记录

### 完成事项

- 为 `E2E.P0.015` 增加 ready-response loading 的浏览器级 gate：`frontend/tests/pixel-parity/parse.spec.ts` 现在通过 Playwright mock `/api/v1/targets/{id}` ready response，打开 `/parse?targetJobId=...`，确认 loading DOM 先出现、preview 在 loading window 内缺席、tick 完成后才出现。
- browser gate 会截取 `route-parse` loading DOM screenshot 并输出 `E2E.P0.015 ready-response loading browser gate screenshotBytes=...` marker；scenario `verify.sh` 现在强制检查 Playwright spec、test title 和 screenshotBytes marker。
- `p0-015-jd-import-and-parse` 的 trigger 脚本新增 frontend build + focused Playwright run；README / expected-outcome 同步浏览器证据口径。
- 原地同步 `001-home-jd-import-and-parse` plan/checklist/BDD checklist/context 到 v1.4，固化 browser screenshot gate。

### 关联 Commit

- `test(frontend-home): add parse loading browser gate (BUG-0099)`

### 备注

- 验证通过：focused Playwright parse gate（desktop/mobile 2 passed）、`E2E.P0.015` setup→trigger→verify→cleanup PASS、context validation、sync-doc-index、docs-check、diff whitespace gate。

## 14:28 工作记录

### 完成事项

- 新增 [Frontend Parse Loading Browser Gate 交付复盘](../reports/2026-05-24-frontend-parse-loading-browser-gate-assessment.md)，记录 `E2E.P0.015` ready-response browser gate 的成功证据、脚本补强点与后续 P0.016 route/context evidence 审计建议。
- 同步 `docs/reports/INDEX.md` 与工作日志索引，保持报告和 commit 记录可追溯。

### 关联 Commit

- `docs(retrospective): assess parse loading browser gate`

### 备注

- 验证通过：`sync-doc-index --check`、`make docs-check`、`git diff --check`。

## 14:45 工作记录

### 完成事项

- 修复 `frontend-home-job-picks-and-parse/001-home-jd-import-and-parse` 的 Parse Confirm handoff：`ParseScreen` 已登录 navigate 与未登录 `requestAuth(pendingAction)` 均复用 `interviewContextFromTargetJob(targetJob)`，恢复 `targetJobId` / `jobId` / `jdId` / `planId` / `resumeVersionId` / `roundId` / `roundName` 7 字段 workspace context。
- 扩展 `ParseEdit` / `ParseAuthGate` 回归测试，新增 Playwright browser route/context gate，点击 Confirm 后验证 `/workspace` query、`workspace-missing-resume` DOM 与 screenshotBytes marker。
- 强化 `E2E.P0.016` trigger/verify/README/expected-outcome，原地同步 owner spec/plan/checklist/BDD/context/INDEX，并建档 [BUG-0100](../bugs/BUG-0100.md) 与 Bug pattern 4。

### 关联 Commit

- `fix(frontend-home): preserve parse confirm context (BUG-0100)`

### 备注

- 验证通过：focused ParseEdit/AuthGate 13 tests PASS、frontend build PASS、focused Playwright P0.016 browser gate 2 tests PASS、`E2E.P0.016` setup→trigger→verify→cleanup PASS、parse suite 27 tests PASS、context validation、sync-doc-index、docs-check、diff whitespace gate。

## 14:47 工作记录

### 完成事项

- 新增 [Frontend Parse Confirm Context Gate 交付复盘](../reports/2026-05-24-frontend-parse-confirm-context-gate-assessment.md)，记录 P0.016 route/context handoff 修复的成功证据、旧 gate 缺口、`workspace-missing-resume` 目标态校准与后续 owner plan 小范围 regression 建议。
- 同步 `docs/reports/INDEX.md` 与工作日志索引，保持报告和 commit 记录可追溯。

### 关联 Commit

- `docs(retrospective): assess parse confirm context gate`

### 备注

- 验证通过：`make docs-check`、`git diff --check`。

## 15:05 工作记录

### 完成事项

- 修复 `ParseScreen` 在 SPA 同路由参数切换（如 `?targetJobId=A` → `?targetJobId=B`）时显示旧 TargetJob preview 的 BUG-0101。
- 从 `route.params?.targetJobId` 提取稳定 `targetJobId` 供 effect 依赖；新增 `useEffect([targetJobId])` 在非 mock 模式下重置 polling timeout、editable fields、hit toggles、error/confirm error、pending ready job、loading completion 与 stage（回到 `loading`），实现 owner identity reset boundary。
- 新增 `createClientForTargetSwitch` mock client + `renderParseWithTarget` + `ParseFlow.test.tsx` rerender regression：断言旧 `Senior Frontend Engineer` 消失、`parse-loading-step-0` 新 loading DOM 出现、ready-response loading gate 后新 `Backend Platform Engineer` 渲染。
- 同步 owner spec/plan/checklist/BDD checklist/context/history/INDEX，建档 [BUG-0101](../bugs/BUG-0101.md) 与 BUG pattern 10（SPA route-param switch 复用旧 screen state）。
- 撰写 [Frontend Parse Target Switch 交付复盘报告](../reports/2026-05-24-frontend-parse-target-switch-assessment.md)。
- 验证通过：`pnpm --filter @easyinterview/frontend test src/app/screens/parse/ParseFlow.test.tsx` → 7 tests PASS；parse suite → 5 files / 28 tests PASS；frontend build PASS；`test/scenarios/e2e/p0-015-jd-import-and-parse` + `p0-016-parse-confirm-to-workspace` scenario wrapper PASS；`validate_context.py` PASS；`sync-doc-index --check` zero drift。

### 关联 Commit

- `fix(frontend-home): reset parse state on target switch (BUG-0101)`

### 备注

- BUG-0101 是 BUG-0099（ready-response loading gate）的 route-param 切换路径修复，不影响初始进入和 re-parse 流程。测试必须用 same mounted rerender 模拟 SPA 参数切换才能捕获该回归。

## 15:57 工作记录

### 完成事项

- 派生 `prompt-rubric-registry/004-real-model-profile-and-evals` 计划：新增 `plan.md` / `checklist.md` / `context.yaml`（v1.1，active），把 spec §7 后续派生项落为可实施计划（真实 LLMJudge、≥50 题离线评估集、judge.default 激活）。
- 演进 LLM Judge 契约（spec D-9，v2.8）：`Judge` 接口从返回单个 `Score` 演进为逐 rubric dimension 的 `[]Score`，`TestJudgeSignature` 断言随之更新。
- 新增 D-15 锁定评估框架选型：Promptfoo（pnpm Node）经 `RegistryClient.ResolveActive` 消费同一份 prompt 真理源（禁止第二份副本）、录制 fixture 默认 + `EVAL_LIVE` opt-in 且不进 `make test`、LLM Judge 走 `judge.default` profile、评估维度复用现有 rubric `dimensions[]`。
- 收敛 §3.2 待确认项到 D-15，§5 边界与 §6 验收口径同步：C-7/C-10 改写为接口演进 / `make eval-offline`，新增 C-14（judge.default 激活 + 非 placeholder coverage）与 C-15（eval prompt single-source）。
- 升级 spec/history 至 v2.8，并把 backend-debrief / backend-review / frontend-debrief / frontend-report-dashboard / 001-baseline plan / plans-INDEX 中的 cross-owner 引用从 `003` 重编号为 `004`/`005`。

### 关联 Commit

- `docs(prompt-rubric): derive real model profile and evals plan`

### 备注

- 纯设计文档派生，未涉及代码实施；接口演进按规约原地随 spec 版本更新，不创建 sibling spec。

## 16:10 工作记录

### 完成事项

- 对齐 004-real-model-profile-and-evals plan 的 stale 编号引用与 spec 版本基线：
  - 将 `003-grayscale` 旧简写在 `config/prompts/README.md` 中更新为 `005-grayscale-and-quality-feedback`。
  - 将 `config/prompts/README.md`、`config/rubrics/README.md`、`scripts/lint/prompt_lint.py`、`scripts/lint/rubric_lint.py` 中的 spec 版本引用从 v2.7 更新为 v2.9。
  - 在 `config/prompts/README.md` 与 `config/rubrics/README.md` 的 References 章节新增 004-real-model-profile-and-evals plan 条目。
- 细化 004 plan 的零引用 grep 门禁：扩大搜索范围至完整旧编号、短写旧编号与 stale spec version，排除范围新增 completed plan 与 history 目录。
- 将 004 计划中 eval 维度映射从 `report_calibration` 更正为 `score_outlier`（异常高分率/离群评分），与 rubric 真实维度名一致。
- Phase 5.1 聚合验证新增 `make test` + `make build`。
- context.yaml `specVersion.to` 2.8→2.9，同步 spec/history/INDEX/plans INDEX 版本。

### 关联 Commit

- `docs(prompt-rubric): align 004 plan references with spec v2.9`

### 备注

- 本 commit 不涉及代码实施，仅对已派生的 004 plan 做版本引用对齐与零引用门禁加固，确保 stale 旧编号与 spec 版本不传播到 active scope。

## 16:30 工作记录

### 完成事项

- 启动 004-real-model-profile-and-evals 实施（`/implement` → `/tdd`），创建 feature 分支 `feat/prompt-rubric-registry-004-real-model-profile-and-evals-0524`。
- Phase 0 契约预读与现状快照：
  - 确认 development.md §2 Operation matrix = N/A，替代 gate 为 lint / registry unit test / eval-offline / drift。
  - 快照实施起点：`judge.default` unsupported + `judge-placeholder`、`types.go` Judge 返回单 `Score`、`AIClient.Complete` chat-only、`bootstrap.ResolveProvider` 对 `judge_compatible` 返回 unsupported、`catalog_test.go` 断言 judge.default unsupported、13 个 chat profile active+deepseek、`ai_profile_coverage` 现状 OK。
  - 锁定 eval 维度映射只复用现有 rubric `dimensions[]`（followup_relevance / report_specificity / score_outlier / language_consistency + report_calibration）。
  - 锁定 Promptfoo single-source、fixture 默认、`EVAL_LIVE` opt-in、live 不进 make test 不变量。
- 修复 v2.9 版本传播残留：`backend/internal/ai/registry/backend_review_preflight_test.go` 的 spec 版本断言 2.7→2.9（前序 align commit 漏更新该 Go 测试，导致 registry 包在 main 上为红，属本 plan gate 5.2 范围）。

### 关联 Commit

- `test(prompt-rubric): refresh f3 spec version preflight for evals plan`

### 备注

Auto-committed by /tdd phase-commit, plan: prompt-rubric-registry/004-real-model-profile-and-evals

## 16:56 工作记录

### 完成事项

- Phase 1 Judge 接口演进为逐维度 `[]Score`（spec D-9 v2.8）：
  - 1.1 先红：重写 `types_test.go` `TestJudgeSignature` 期望 output[0] 为 slice、elem 名为 `Score`，并同步 `stubJudge` 返回 `([]Score, Reasoning, error)`，编译期红灯。
  - 1.2 绿：`types.go` `Judge` 接口返回 `([]Score, Reasoning, error)`，`Score` doc 语义改为逐 rubric dimension（Value ∈ [0,1] 可映射 score_levels label）。
  - 1.3 同步 `NotImplementedJudge.Judge` 返回 `(nil, Reasoning{}, ErrJudgeNotImplemented)`、`judge_test.go` 断言改为 scores==nil；修正 stale "plan 002" 注释为 plan 004；无其他 Go caller。
  - 1.4 `go build ./backend/...` 通过，`go test ./internal/ai/registry -count=1` 全绿（含既有 resolver/loader/cache）。

### 关联 Commit

- `feat(prompt-rubric): evolve judge interface to per-dimension scores`

### 备注

Auto-committed by /tdd phase-commit, plan: prompt-rubric-registry/004-real-model-profile-and-evals

## 17:11 工作记录

### 完成事项

- Phase 2 judge capability dispatch + 真实 LLMJudge：
  - 2.1/2.2 新增 `Client.CompleteJudge`（走 CapabilityJudge，不进 business AIClient 接口，judge 流量隔离于业务 observability）；新增 `providers/judge_compatible` adapter（OpenAI-compatible chat completions 子集 wire，protocol/capability/meta 保持 judge，secret fail-fast，Transcribe/Stream/Synthesize fail-close）；`bootstrap.ResolveProvider` materialize judge_compatible adapter，不再返回 "protocol not implemented"。
  - 提取 `aiclient/outputschema` 单一校验器，observability decorator 与 LLMJudge 复用同一 schema 子集语义（plan 2.5），observability 测试行为保留。
  - 2.3/2.4 新增 registry `LLMJudge`：窄接口 `JudgeModelClient`/`RubricProvider` 注入，经 judge.default 逐 rubric dimension 产出 []Score+Reasoning；business prompt 经 registry single-source；judge 评分指令注入（来自 config/evals，不 hardcode）。
  - 2.5 fail-close 7 个负向子测：judge 不可用 / 被评估 output schema invalid（call 前拦截）/ 不可解析 / 维度数量不匹配 / 未知维度 / value 越界 / 空 summary → ErrJudgeOutputInvalid，不静默补零。
  - 2.6 `var _ Judge = (*LLMJudge)(nil)` 编译期断言 + 保留 NotImplementedJudge 安全默认 + 构造注入。
  - 验证：`go build ./backend/...`、`go test ./internal/ai/registry ./internal/ai/aiclient/...` 全绿；`make lint-prompts-hardcode` green。

### 关联 Commit

- `feat(prompt-rubric): implement llm judge dispatch and per-dimension judge`

### 备注

Auto-committed by /tdd phase-commit, plan: prompt-rubric-registry/004-real-model-profile-and-evals

## 17:16 工作记录

### 完成事项

- Phase 3 judge.default 激活 + coverage 门禁扩展：
  - 3.1 先红：`catalog_test.go` judge.default 期望 active（config 仍 unsupported → 红）；`ai_profile_coverage_test.py` 新增 4 个期望（judge.default missing/unsupported/placeholder provider、chat placeholder model）→ 红。
  - 3.2 config 激活：`config/ai-providers.yaml` 将 judge-placeholder 替换为 judge-deepseek（protocol judge_compatible，复用 AI_PROVIDER_BASE_URL/API_KEY）；`config/ai-profiles.yaml` judge.default unsupported→active + provider_ref judge-deepseek + model deepseek-v4-pro，移除 unsupported_reason；13 个 chat 业务 profile status 未翻动（git diff 范围确认）。
  - 3.3 扩展 `ai_profile_coverage.py`：placeholder 黑名单（judge.default + 所有 chat profile 的 provider_ref/model 非 placeholder）+ judge.default 必须 active + 选中 provider protocol=judge_compatible；已经由 `make lint` 接线。
  - 3.4 `make lint-ai-profile-coverage` OK；11 个 lint 单测（含 4 个负向 placeholder/unsupported/missing）+ `go test ./internal/ai/aiclient/profile` 全绿。

### 关联 Commit

- `feat(prompt-rubric): activate judge.default and guard non-placeholder coverage`

### 备注

Auto-committed by /tdd phase-commit, plan: prompt-rubric-registry/004-real-model-profile-and-evals

## 17:37 工作记录

### 完成事项

- Phase 4 ≥50 题离线评估集 + Promptfoo runner：
  - 4.1 `config/evals/<feature_key>/cases.yaml`：13 个 §3.1.1 chat feature_key × 4 质量档 = 52 用例；golden output 抽自各 prompt `.md` 的 "Example complete JSON output"（schema-valid），judge fixture 逐 rubric dimension；含 1 个 `practice.session.follow_up` en→multi fallback 用例。新增 `config/evals/README.md` + `judge-instruction.md`。
  - 4.2 pinned `promptfoo@0.121.12` 根 devDependency + lockfile；`pnpm.onlyBuiltDependencies:[better-sqlite3]` 使原生 binding 在 pnpm v10 下可构建；`pnpm exec promptfoo --version`=0.121.12。
  - 4.3 新增 `backend/internal/eval` 包 + `backend/cmd/evalkit` CLI：经 `RegistryClient.ResolveActive` single-source 解析 + 单一 `registry.LLMJudge` grading；Promptfoo custom provider/assertion 仅 exec 桥到 evalkit（不复制 prompt、不在 JS 重实现 judge）。`resolved-prompts.json` 为 committed single-source 导出。
  - 4.4 录制 fixture 默认 + `EVAL_LIVE=1` 经 bootstrap opt-in 真实 provider/judge；no-network 断言（TestRunOfflineMakesNoNetworkCall：env 指向黑洞仍绿）。
  - 4.5 `make eval-offline`（.PHONY+help，不纳入 make test）= drift-check + count≥50 + Promptfoo runner（52 passed / 0 failed）；drift 负向 exit 1、正向 exit 0。新增 `make eval-offline-resolve` 重生成 drift baseline。
  - 4.6 `make lint-prompts-hardcode` exit 0（未复制第二份 prompt 正文）。

### 关联 Commit

- `feat(prompt-rubric): land offline eval suite and promptfoo runner`

### 备注

Auto-committed by /tdd phase-commit, plan: prompt-rubric-registry/004-real-model-profile-and-evals

## 17:47 工作记录

### 完成事项

- Phase 5 验证、生命周期与收口：
  - 5.1 聚合验证全绿：`make lint`（golangci 0 issues + 全本地 gate）、`make test`（backend ./... 全绿 + frontend 1298 passed）、`make build`、`make eval-offline`（52 passed）、`make lint-ai-profile-coverage`。
  - 5.2 active-scope zero-reference grep = 0 matches（旧编号/stale spec version 未传播到 active specs/index/README/代码/配置/脚本）。
  - 5.3 `git diff` 范围断言：仅 judge.default `status` unsupported→active + judge provider/model 替换；13 个 chat 业务 profile status 未翻动。
  - 5.4 `validate_context` OK、`make docs-check` zero drift + links OK、`git diff --check` OK；004 plan/checklist 置 completed，plans INDEX 移入 Completed。
  - 收口修复：`lint-getenv-boundary` 拦截 evalkit 直接 os.Getenv（A4 §4.1 边界）；将 evalkit live bootstrap 改走 `platform/config`+`platform/secrets`（allowlisted），`EVAL_LIVE`→`--live` flag（env→flag 桥在 Makefile/Promptfoo JS 侧），evalkit 零 os.Getenv，无需修改 A4 spec allowlist。

### 关联 Commit

- `feat(prompt-rubric): close 004 verification and route evalkit env via platform config`

### 备注

Auto-committed by /tdd phase-commit, plan: prompt-rubric-registry/004-real-model-profile-and-evals

## 17:50 工作记录

### 完成事项

- 为 `prompt-rubric-registry/004-real-model-profile-and-evals` 交付撰写复盘报告 `docs/reports/2026-05-24-prompt-rubric-real-model-profile-and-evals-assessment.md`：固化成功证据；归类 4 个阻点（spec 版本传播遗漏 Go 测试、Promptfoo 原生依赖 pnpm v10 构建、新 CLI 入口 os.Getenv 边界、Promptfoo↔Go 桥接架构决策）；给出 4 条流程资产改进建议（spec 版本 bump 传播 gate、原生依赖落地约定、契约预读补 secrets-and-config 边界、新 CLI env 范式）。

### 关联 Commit

- `docs(reports): assess real model profile and evals delivery`

### 备注

复盘建议为后续流程改进输入，不在本会话直接修改 skill/AGENTS/spec。

## 18:14 工作记录

### 完成事项

- 对 `prompt-rubric-registry/004-real-model-profile-and-evals` 执行 `/plan-code-review --fix`（L2），未采信历史 completed/PASS，实跑 registry/eval/aiclient focused tests、`make eval-offline`(52 passed)、`make lint-ai-profile-coverage`、`lint-prompts-hardcode`、`lint-getenv-boundary` 与 5.2 zero-ref grep，逐项核对 spec D-9/D-15/§6 C-7/C-10/C-14/C-15，结论实质 PASS。
- 修复 D-L2-1（Low，evidence-gate 精度）：checklist 2.1/2.2 引用的 focused gate `-run 'Test.*Judge|Test.*judge'` 不匹配 `providers/judge_compatible` adapter 测试名（`TestNewFailsFastOnMissingSecret` 等），实跑该包为 `[no tests to run]`；在 checklist 2.1/2.2 与 plan §8.1 Phase 2 evidence 补上独立包 gate `go test ./backend/internal/ai/aiclient/providers/judge_compatible -count=1` 并注明原因。
- 验证：更正后该 gate 真实执行全部 4 个 adapter 测试且 PASS；`validate_context`、`git diff --check` 均 OK。

### 关联 Commit

- `docs(prompt-rubric): tighten 004 judge adapter test gate evidence`

### 备注

纯证据命令精度修复，无代码逻辑变更，未走 /tdd Red-Green。

## 19:01 工作记录

### 完成事项

- 修复 `prompt-rubric-registry/004-real-model-profile-and-evals` L2 review 中发现的两个 eval gate 问题：
  - `backend/internal/eval` 的 offline grading 与 `GradeOutput` 不再硬编码 `v0.1.0`，改为先通过 `ResolveActive` 获取当前 prompt/rubric version，再传入 `LLMJudge`。
  - 新增 active-version 回归测试，覆盖 `RunOffline` 与 `GradeOutput` 都必须使用 registry active 坐标。
  - `make eval-offline` 的 Promptfoo config/log 状态目录改为 repo 内 `config/evals/.generated/promptfoo`，并禁用 WAL，避免默认写入用户 home 目录。
- 验证通过：`go test ./backend/internal/eval ./backend/internal/ai/registry ./backend/internal/ai/aiclient/... -count=1`、`make lint-ai-profile-coverage`、`make lint-prompts-hardcode`、`validate_context`、`make eval-offline`（52 passed / 0 failed）、`git diff --check`。

### 关联 Commit

- `fix(prompt-rubric): honor active eval versions and isolate promptfoo state`

### 备注

Code review remediation for prompt-rubric-registry/004-real-model-profile-and-evals; no new BUG record because the issue was caught pre-merge on the active feature branch.
