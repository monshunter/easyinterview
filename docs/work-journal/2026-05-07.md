# 2026-05-07 工作日志

## 00:23 工作记录

### 完成事项

- 修复 `runtime_topology.py` 二次 false-negative：将 `scripts/` tooling 面纳入扫描，并为 lint 本体保留最小例外。
- 补强 retired worker producer 匹配，覆盖 `producer: worker` 与 `"producer": "worker"` 等 YAML / JSON raw 字段形态，同时覆盖 `backend/cmd/worker` 与引号终止形式。
- 扩展 `scripts/lint/runtime_topology_test.py` Red-Green fixture，覆盖 `scripts/lint/env_dict.py` / `scripts/lint/getenv_boundary.go` 回流旧 worker 入口 / env key，以及 `shared/events.yaml` / baseline JSON 回流旧 producer 字段。
- 原地修订 `backend-runtime-topology/001-worker-consolidation` 到 v1.3，完成 Phase 4.5，并同步 spec / plan / checklist / context / INDEX。
- 创建 [BUG-0016](../bugs/BUG-0016.md) 与交付复盘报告：[Backend Runtime Topology Gate Hardening](../reports/2026-05-07-backend-runtime-topology-gate-hardening-assessment.md)。
- 完成验证：`python3 -m unittest scripts.lint.runtime_topology_test`、`make lint-runtime-topology`、`make lint`、`make test`、context validator、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `fix(runtime): harden worker topology scripts gate`

### 备注

See [BUG-0016](../bugs/BUG-0016.md). 本次提交合并 runtime topology lint、负向 fixture、owner plan v1.3、Bug 记录、复盘报告与工作日志，保持 gate remediation 证据同源。

## 01:19 工作记录

### 完成事项

- 修复 `runtime_topology.py` 跨行结构化 producer false-negative：在 `shared/` 树下对 YAML / JSON 做结构化解析，覆盖 `producer` block、`producer.values`、`producer.enum` 与 event-local producer list 等多行字段形态。
- 收窄 owner docs 例外：`docs/spec/backend-runtime-topology/plans/**/{plan,checklist}.md` 不再整包排除，只对 current handoff / build / runtime / verification 句子触发，删除、不得、回流、history、test、lint 定义等负向证据继续放行。
- 扩展 `scripts/lint/runtime_topology_test.py` Red-Green fixture：YAML producer block / list、JSON Schema producer enum、owner plan/checklist current handoff command 必须失败；负向断言、history、tests、lint 自身继续通过。
- 原地修订 `backend-runtime-topology/001-worker-consolidation` 到 v1.4，新增 Phase 4.6 / 4.7 并同步 spec / plan / checklist / context / history / INDEX。
- 创建 [BUG-0017](../bugs/BUG-0017.md) 与交付复盘报告：[Backend Runtime Topology Structured Gate](../reports/2026-05-07-backend-runtime-topology-structured-gate-assessment.md)。
- 完成验证：`python3 -m unittest scripts.lint.runtime_topology_test`、`make lint-runtime-topology`（301 active files scanned）、`make lint`、`make test`、`make codegen-check`、`make docs-check`、context validator、`git diff --check`、`cd backend && go build ./cmd/...` 均通过。

### 关联 Commit

- `fix(runtime): harden worker topology structured gate`

### 备注

See [BUG-0017](../bugs/BUG-0017.md). 本次提交合并 runtime topology lint 结构化补强、owner plan/checklist 例外收窄、负向 fixture、owner plan v1.4、Bug 记录、复盘报告与工作日志，保持 gate remediation 证据同源。

## 01:37 工作记录

### 完成事项

- 引入 frontend toolchain：React 18 + Vite 5 + Vitest jsdom + @testing-library/react，保留 node 默认 environment 让 `envelope.test.ts` 等文件 IO 测试无回归。
- 落地 `frontend/src/app/` shell：`App.tsx` 持有 route 状态，`practice` / `generating` 隐藏 chrome，其他 route 渲染 TopBar 占位与 `PlaceholderScreen`，支持 `initialRoute` 注入和 params 透传（checklist 1.1）。
- 实现 `normalizeRoute.ts`：将 `welcome` / `growth` / `plan` / `mistakes` / `drill` / `followup` / `experiences` / `star` / `resume` / `onboarding` / `voice` 11 个旧 alias 映射到当前 route，未知 name fallback `home`，App 接入 loose initialRoute 输入并先 normalize 再渲染（checklist 1.2）。
- 实现 `AppRuntimeProvider`：通过 generated client + fixture-backed mock transport 引导 `getRuntimeConfig` / `getMe`，`/me` 401 收敛为 `unauthenticated` 不阻塞 runtime，未知 scenario fail loudly；新增 `scope.test.ts` 守住 `frontend/src` 不 import `ui-design/src/data` 边界（checklist 1.3）。
- 完成验证：`pnpm --filter @easyinterview/frontend typecheck` clean；`pnpm --filter @easyinterview/frontend test` 78 passed（src/app/ 新增 26 个）。

### 关联 Commit

- `feat(frontend-shell): app bootstrap and route normalization`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/001-app-shell-auth-settings

## 09:57 工作记录

### 完成事项

- 完成 `frontend-shell/001-app-shell-auth-settings` L2 code review remediation：删除正式前端 `voice` route alias，`voice` 输入改为 unknown route fallback `home`，语音面试继续只通过 `practice?mode=voice&modality=voice` 表达。
- 修复 auth verify generated client wire：`AuthVerifyScreen` 提交的 token 现在传入 `verifyAuthEmailChallenge({ query: { token } })`，并新增 App dispatch 测试断言请求 URL 带必填 query。
- 修复 pendingAction 恢复参数泄漏：`email` / `displayName` 纳入 auth-only reserved keys，恢复到 `practice` 等业务 route 时只保留业务上下文。
- 原地修订 `frontend-shell/001-app-shell-auth-settings` plan/checklist 到 v1.2，完成 remediation checklist 1.4 / 3.5，并同步 plans/INDEX。
- 创建 [BUG-0018](../bugs/BUG-0018.md) 与交付复盘报告：[Frontend Shell L2 Remediation](../reports/2026-05-07-frontend-shell-l2-remediation-assessment.md)。
- 完成验证：frontend focused tests、`pnpm --filter @easyinterview/frontend typecheck`、`pnpm --filter @easyinterview/frontend test`（26 files / 127 tests）、E2E.P0.001 / E2E.P0.002 scenario `setup -> trigger -> verify -> cleanup`、context validator、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `fix(frontend-shell): remediate app shell L2 findings`

### 备注

See [BUG-0018](../bugs/BUG-0018.md). 本次提交合并 L2 remediation 代码、focused tests、owner plan v1.2、Bug 记录、复盘报告与工作日志，保持修复证据同源。场景脚本存在本机 `LC_ALL=C.UTF-8` warning，但脚本均 0 退出。

## 02:05 工作记录

### 完成事项

- 扩展 TopBar user-area：未登录保留登录 / 注册按钮，已登录渲染 `用户画像` / `设置与隐私` / `退出登录` 三项菜单并分别派发 `profile` / `settings` / `auth_logout` 路由（checklist 4.1）。
- 落地 `frontend/src/app/screens/ProfileScreen.tsx` 与 `SettingsScreen.tsx`：profile 镜像 ui-design §3 五大分区（身份摘要 / 分区 / 洞察卡片 / 被使用模块 / 最近证据），settings 仅承载账号 / 登录与安全 / 字体预设 / 隐私与数据 + P1 通知与订阅占位；ProfileScreen.test.tsx 7 项断言显式禁止 Growth / Experiences / Mistakes / Drill / 目标角色 / 技能标签等旧模块回流（checklist 4.2）。
- 在 [`frontend/src/app/scope.test.ts`](../../frontend/src/app/scope.test.ts) 增补两组负向 grep：1) frontend/src 不允许出现 `VoiceScreen` / `GrowthScreen` / `MistakesScreen` / `DrillScreen` / `FollowupScreen` / `ExperiencesScreen` / `WelcomeScreen` 文件名，2) `app/normalizeRoute.ts` 之外不得在活跃代码中以 `name: "voice"` 等字面量复活旧 route name；测试文件继续允许触发 alias 以验证守门（checklist 5.2）。
- 重写 [`frontend/README.md`](../../frontend/README.md)：新增 D1 工具链表、D2-D6 owner 接入点（路由表 / pendingAction 契约 / runtime + mock transport 入口 / mock 数据源边界 / owner 写入范围）以及 `DESIGN.md` 只读参考边界与开源字体约束（覆盖 checklist 5.1 + 5.3）。
- 完成验证：`pnpm --filter @easyinterview/frontend typecheck` clean；`pnpm --filter @easyinterview/frontend test` 124 passed（26 文件，新增 7 项 4.x/5.x 断言）。

### 关联 Commit

- `feat(frontend-shell): user menu, settings, and D2-D6 handoff`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/001-app-shell-auth-settings

## 02:00 工作记录

### 完成事项

- 落地 `frontend/src/app/auth/`：`AuthLoginScreen` / `AuthRegisterScreen` / `AuthVerifyScreen` / `AuthResetScreen` / `AuthLogoutScreen` 五个 route shell；密码 / OAuth / reset 仅作为 stub UI，真实 wire 仅 `startAuthEmailChallenge` / `verifyAuthEmailChallenge` / `getMe` / `logout` 四个 generated operations（checklist 3.1）。
- App.tsx 加入 ScreenRouter 派发：route 是 `auth_*` 时根据 runtime context 渲染对应 auth 屏并注入 callbacks，`auth_reset` 全程不打 API；新增 `AppAuthDispatch.test.tsx` 既验证 hallmark testid 又通过 fetch spy 断言登录提交真正命中 `POST /api/v1/auth/email/start`。
- 实现 `pendingAction` 模型：`encodePendingAction` / `decodePendingActionRoute` 把 type / label / route + interview-context params 编码到 auth 路由参数中；`useRequestAuth` 钩子结合 `NavigationProvider` 与 `AppRuntimeProvider` 提供未登录跳 `auth_login`、已登录直达目标的统一入口；`AuthLoginScreen` / `AuthRegisterScreen` 提交时透传完整 route.params 给 `auth_verify`，`AuthVerifyScreen` 优先解码 pendingAction 再回退 `returnTo`（checklist 3.2）。
- 落地 `authContractGate.test.ts` 七项负向断言：禁止 Bearer header、auth/session token 写入 localStorage / sessionStorage、password / OAuth API 路径出现、auth/ 子目录直接 import generated client；并锁 App.tsx auth dispatch 仅引用 startAuthEmailChallenge / verifyAuthEmailChallenge / logout，整个 frontend/src 客户端调用范围限定在 4 个 auth + getRuntimeConfig；`AuthResetScreen` 必须保留 UI shell 注释（checklist 3.3）。
- 实现 BDD-Gate E2E.P0.002：新增 `frontend/src/app/scenarios/p0-002-auth-pending-action-resume.test.tsx`，模拟 workspace 场景下 `requestAuth({立即面试})` → login → verify → restore practice 全流程，断言 5 个 interview-context key 完整回填；落地 `test/scenarios/e2e/p0-002-auth-pending-action-resume/` 完整 setup / trigger / verify / cleanup + data 资产；scenario INDEX 同步为 Ready，`bdd-checklist.md` 全勾选并写入 `.test-output` 证据指针（checklist 3.4）。
- 完成验证：`pnpm --filter @easyinterview/frontend typecheck` clean；`pnpm --filter @easyinterview/frontend test` 115 passed（25 文件）；E2E.P0.002 `setup → trigger → verify → cleanup` 全部 0 退出，verify.sh 负向 grep 未发现旧 testid / `ui-design/src/data` 回流。

### 关联 Commit

- `feat(frontend-shell): auth pages and pending action`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/001-app-shell-auth-settings

## 01:46 工作记录

### 完成事项

- 实现 `TopBar` 五入口 primary nav（`home` / `jd_match` / `workspace` / `resume_versions` / `debrief`），按 `activeRoute` 渲染 `aria-current=page`，点击触发 `onNavigate({ name, params: {} })`，旧入口（mistakes / growth / voice / drill / welcome）在组件中不可达（checklist 2.1）。
- 落地 `DisplayPreferencesProvider`：theme / dark / lang 默认 warm/light/zh，登录态切换后保持稳定；TopBar 渲染主题 select、暗色 toggle 与语言 select 三个全局显示控制；`useDisplayPreferencesOptional` 模式与 `useAppRuntimeOptional` 同源（checklist 2.2）。
- App.tsx 新增可选 `client` / `requestOptions` 入参：传入 generated client 时自动 mount AppRuntimeProvider，TopBar 通过 `useAppRuntimeOptional` 派生 `signedIn`，未登录时渲染登录 / 注册 user-area，已登录走 4.1 占位符。
- 实现 BDD-Gate E2E.P0.001：新增 `frontend/src/app/scenarios/p0-001-default-home-shell.test.tsx` 通过 fixture transport + `Prefer: example=unauthenticated` 渲染 `<App />`，并落地 `test/scenarios/e2e/p0-001-default-home-shell/` 完整 setup / trigger / verify / cleanup + data 资产；scenario 索引同步为 Ready，`bdd-checklist` 全勾选并写入 `.test-output` 证据指针（checklist 2.3）。
- 完成验证：`pnpm --filter @easyinterview/frontend typecheck` clean；`pnpm --filter @easyinterview/frontend test` 88 passed（19 文件）；scenario `setup → trigger → verify → cleanup` 全部 0 退出且 verify 负向 grep 未发现旧入口 `data-testid` 回流。

### 关联 Commit

- `feat(frontend-shell): topbar and display controls`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/001-app-shell-auth-settings

## 10:52 工作记录

### 完成事项

- 修复 PR review 暴露的四个 frontend shell 收口 review findings：新增 `frontend/index.html` 与 `frontend/src/main.tsx`，让 `pnpm --filter @easyinterview/frontend build`（`tsc --noEmit && vite build`）拥有真实 Vite entry，根 `make build` 不再因 `Could not resolve entry module "index.html"` 失败。
- `AppRuntimeProvider` 拆分 `AuthState`：仅 HTTP 401 收敛为 `unauthenticated`，其余 transport / fixture wiring 错误保留 `error` 状态并暴露 `error.message`；新增 focused test 使用 `Prefer: example=does-not-exist` 命中 `/me` unknown fixture scenario 时必须 fail loudly。
- `AuthLoginScreen` 注册跳转透传当前 `route.params`，避免从 pendingAction 进入 login 后切换到注册时丢失 `pendingRoute` / `pendingType` / `pendingLabel` / `planId`；补 component test 覆盖 login → register 切换。
- `AuthVerifyScreen.buildResumeRoute` 用 `URL` / `URLSearchParams` 解析 raw `returnTo`，把允许的 interview context query keys（planId / targetJobId / jdId / resumeVersionId / roundId）合并到恢复 route params；补 focused test 覆盖 raw `returnTo=/practice?...` 链路。
- 把以上 hardening 固化为 owner gate：`frontend-shell/001-app-shell-auth-settings` plan / checklist 升级至 v1.3，新增 Phase 5.5 与 checklist item 5.4，要求 package `build` 真实化时同时跑 `pnpm --filter @easyinterview/frontend build` 与根 `make build`；plans/INDEX 同步至 v1.3。
- 创建 [BUG-0019](../bugs/BUG-0019.md) 与交付复盘报告：[Frontend Shell Review Remediation Hardening 交付复盘](../reports/2026-05-07-frontend-shell-review-remediation-hardening-assessment.md)，并更新 docs/bugs/INDEX 与 docs/reports/INDEX。
- 完成验证：`pnpm --filter @easyinterview/frontend test src/app/runtime/AppRuntimeProvider.test.tsx src/app/auth/AuthScreens.test.tsx`、`pnpm --filter @easyinterview/frontend typecheck`、`pnpm --filter @easyinterview/frontend build`（输出 `dist/index.html` 与 bundle）、`pnpm --filter @easyinterview/frontend test`（26 files / 130 tests）、`make build`、`make test`、`git diff --check` 均通过。

### 关联 Commit

- `fix(frontend-shell): harden app shell review remediation`

### 备注

See [BUG-0019](../bugs/BUG-0019.md). 本次提交合并 frontend build entry、`/me` fail-loud、auth pendingAction 切换、raw `returnTo` 恢复四个修复，加上 plan v1.3 hardening gate、Bug 记录、交付复盘报告与工作日志，保持 review remediation 证据同源。

## 13:11 工作记录

### 完成事项

- 修复 frontend shell i18n 缺失：原地修订 `frontend-shell/001-app-shell-auth-settings` spec / plan / checklist / BDD 至 spec v1.4、plan/checklist v1.6、bdd v1.4，新增 C-7 / D-7 / Phase 2.4-2.8 与 E2E.P0.004，明确 UI 语言默认跟随浏览器 locale，未知时 fallback English，登录态与 runtime locale 不覆盖前端语言偏好。
- 落地独立 locale 文件：新增 `frontend/src/app/i18n/locales/{zh,en}.ts`，`messages.ts` 只做导入聚合、BCP 47 归一化与 `useI18n()` helper；`localeFiles.test.ts` 阻止 zh / en 多语言 message map 回流到同一文件。
- TopBar / auth / profile / settings / placeholder shell 改为 i18n helper 渲染静态文案，保留 RouteName / testid / params 为稳定英文 key；TopBar 语言切换固化为下拉框。
- 调整 `DisplayPreferencesProvider`：从浏览器 locale 初始化语言，未知或缺失时 fallback English，登录态切换不覆盖前端语言偏好；删除 App shell 从 runtime config 或 `/me` 回写 UI locale 的 bootstrap 逻辑。
- App 在 runtime config、`/me` 和 D1 auth operations 上合并当前 UI locale 的 `Accept-Language` display hint。
- 实现 BDD-Gate E2E.P0.004：新增 `frontend/src/app/scenarios/p0-004-app-shell-language-switch.test.tsx` 与 `test/scenarios/e2e/p0-004-app-shell-language-switch/` 完整 setup / trigger / verify / cleanup + data 资产；scenario INDEX 同步为 Ready。
- 创建 [BUG-0020](../bugs/BUG-0020.md) 与交付复盘报告：[Frontend Shell I18n Remediation](../reports/2026-05-07-frontend-shell-i18n-remediation-assessment.md)，并更新 docs/bugs/INDEX、docs/reports/INDEX、docs/spec/INDEX、frontend-shell plans/INDEX。
- 完成验证：focused i18n / locale / TopBar / auth / profile 测试；E2E.P0.004 `setup → trigger → verify → cleanup`；`pnpm --filter @easyinterview/frontend typecheck`、`pnpm --filter @easyinterview/frontend test`（30 files / 138 tests）、`pnpm --filter @easyinterview/frontend build`、`make docs-check`、context validator、`make build`、`make test`、`git diff --check` 均通过。

### 关联 Commit

- `fix(frontend-shell): restore app shell i18n`

### 备注

See [BUG-0020](../bugs/BUG-0020.md). 本次提交合并 i18n shell 修复、独立 locale 文件、Accept-Language display hint、E2E.P0.004 新场景、owner spec v1.4 / plan v1.6、Bug 记录、复盘报告与工作日志，保持 i18n remediation 证据同源。

## 14:50 工作记录

### 完成事项

- 在 `/design` skill 增加 Step 3.5 Coverage Matrix 阶段，覆盖 primary / alternate / failure-recovery / boundary / cross-layer contract / privacy-security-observability / UX / regression-legacy-negative 八类 row，并要求 plan / unit-test plan / BDD plan 的每条非文档 checklist 项都标注 verification source；同时把 BDD 场景选择写死为「主路径 + 最高风险 alternate 或 failure/recovery 流程」。
- 在 `/plan-review` skill 增加 `S-007` 与 `X-L1-Coverage` 维度，强制 L1 review 重建 coverage matrix 并对 high-risk N/A 行要求 rationale + 替代验证 gate；八类 row 的语义在 SKILL 文档中明文化。
- 在 `/plan-code-review` skill 增加 `C-series` review 维度与 `Coverage Matrix Evidence` 段约束，要求按当前 artifact 反查 coverage row、并对 retired terminology 做负向搜索，使深度 reconcile 不再只看 happy path。
- 同步扩充 `.agent-skills/design`、`.agent-skills/plan-review`、`.agent-skills/plan-code-review` 三个 skill 的 contract 测试，新增 coverage matrix / 八类 row / 新维度断言；`pytest .agent-skills/.../test_*` 25 项全部通过。

### 关联 Commit

- `feat(skills): enforce coverage matrix gate across design and review skills`

### 备注

本次只调整 governance 类 skill 与契约测试，没有改任何运行时代码或产品 spec；下一步同会话内把 D2 视觉系统接入计划落到 frontend-shell plans/。

## 14:55 工作记录

### 完成事项

- 派生 frontend-shell D2 视觉系统接入计划 `002-app-shell-visual-system/`：plan v1.1 / checklist v1.1 / context.yaml / bdd-plan v1.0 / bdd-checklist v1.0；plan 把 design token / 主题暗色 wiring / 字体 / TopBar / 五入口卡片节奏 / 认证页 / 用户菜单 / settings & profile placeholder / visual smoke 工具拆为 6 个串行 phase，并标注 TDD + BDD 双门禁。
- 在 plan / checklist 内显式写入 coverage matrix：覆盖 ui-design 真理源对齐、4 主题 × 2 模式、`customAccent`、开源字体、D1 行为 regression（E2E.P0.001 / .002 / .004）以及 `E2E.P0.005` visual smoke 五条主路径与失败路径，与刚刚收紧的 /design Step 3.5 gate 一致。
- 修订 [`docs/spec/frontend-shell/spec.md`](../spec/frontend-shell/spec.md) 至 v1.6：§4 锁紧「`docs/ui-design/` 与 `ui-design/` 是验收真理源头，主题系统必须承接 warm/forest/ocean/plum 与 `customAccent`，自定义 accent 不是降级项」；§6 新增 C-8 视觉接入对齐验收，把 4 主题 × 2 模式、`customAccent`、开源字体、`E2E.P0.005` visual smoke、D1 testid / regression 全部固化为 gate；§7 关联 plan 同步追加 002 行。
- 同步 [`docs/spec/frontend-shell/history.md`](../spec/frontend-shell/history.md)（追加 v1.5 / v1.6 历史项）、[`docs/spec/frontend-shell/plans/INDEX.md`](../spec/frontend-shell/plans/INDEX.md)（新增 002 行）、[`docs/spec/INDEX.md`](../spec/INDEX.md)（frontend-shell 升至 1.6）。Header 与 INDEX 一致，无 drift。

### 关联 Commit

- `docs(frontend-shell): derive D2 visual system plan`

### 备注

本次只是派生计划文档与修订 owner spec / history / INDEX，没有动 `frontend/` 或 `ui-design/` 实际代码。下一步推荐入口：在分支上 run `/implement frontend-shell/002-app-shell-visual-system`，按 phase 1 design token → phase 2 字体 → phase 3-5 视觉接入 → phase 6 visual smoke 与 build smoke，配套 BDD-Gate `E2E.P0.005` 与 D1 行为 regression。

## 19:47 工作记录

### 完成事项

- 删除根目录外部设计参考文件，并从 active governance / frontend / ui-design / frontend-shell spec 与 plan 文档中移除旧参考口径；`docs/ui-design/` 与 `ui-design/` 成为前端 UI 唯一真理源。
- 原地修订 `frontend-shell` spec 至 v1.7，更新 D1 handoff plan/checklist 至 v1.7，并把 D2 `002-app-shell-visual-system` plan/checklist/BDD/context 收紧为 `ui-design` 源级复刻：样式、token、className、布局和状态必须来自原型源码或文档，不允许 AI 自由设计、重新解释或重新组合视觉。
- 将 `E2E.P0.005` visual smoke gate 强化为正式 frontend 与 `ui-design` golden preview 的 parity gate，要求输出 source-to-target 映射，并覆盖 DOM 锚点、computed style、bounding box 与必要截图差异。
- 修复 Phase 6.3 review finding：负向搜索不再用会误伤治理说明的宽泛“外部品牌设计参考口径”，改为检查已删除设计参考文件名、历史设计导入标识和私有品牌字体名称，并明确排除 gate 文本自身与 work-journal 历史记录。
- 完成验证：active-scope 废弃设计引用搜索无命中、`sync-doc-index --check`、D2 context validator、`make docs-check`、`git diff --check` 均通过。

### 关联 Commit

- `docs(frontend-shell): enforce ui-design source parity`

### 备注

本次为 docs / governance / frontend README 收口，不修改正式前端运行时代码。按 Bug 记录规范判断，该 review gate 措辞修复无需新增 BUG 记录。

## 21:31 工作记录

### 完成事项

- 落地 `frontend-shell/002-app-shell-visual-system` Phase 1 全部三项：建立 `frontend/src/app/theme/` 设计 token 模块（`tokens.ts` 仅导出语义 CSS variable 名，`themes.data.ts` 透明转写 `ui-design/src/primitives.jsx::EI_THEMES` / `EI_FONT_PRESETS` / `EI_THEME_LIST`）。
- 实现 `themes.css`：`:root` 默认 radius / shadow / spacing / font-family token + 4 主题 × 2 模式（warm / forest / ocean / plum × light / dark）8 组合 21 个语义颜色变量；selector 契约 `:root[data-theme=X][data-mode=Y]`。
- 实现 `customAccent.ts` 与 `DisplayPreferencesProvider` 的根级 wiring：在 `theme` / `dark` / `customAccent` 切换时即时写入 `<html>` 的 `data-theme` / `data-mode` / `data-custom-accent`，customAccent 激活时只内联覆盖 `--ei-color-accent` 与 `--ei-color-accent-soft`，并按 `app.jsx` oklch 公式（light=58 / dark=68）计算。
- 落地 `global.css`：转写 `ei-global` 的 reset / scrollbar / fadein / pulse / focus-visible 规则，`@import "./themes.css"` 让单一 entry 同时携带 token + reset；`main.tsx` 在 `App` 加载前 `import "./app/theme/global.css"`。
- 写入三组 focused tests：`tokens.test.ts`（13 项，断言 token 语义化、CSS variables 在 8 组合上均有定义、`customAccent` 仅覆盖 accent / accent-soft、所有 hex 与字体能在 `primitives.jsx` 中找到）、`DisplayPreferencesRootWiring.test.tsx`（5 项，断言 root attr 翻转、`getComputedStyle('--ei-color-bg-canvas')` 切换主题后变化、custom accent inline override 范围限定）、`globalCss.test.ts`（5 项，断言 `main.tsx` 单次 import + 转写要点 + `tailwindcss` / `postcss-tailwind` / `styled-components` / `@emotion/*` 依赖负向）。
- 完成验证：`pnpm --filter @easyinterview/frontend test`（33 files / 161 tests，含 D1 全量 regression 不退化）、`pnpm exec tsc -p . --noEmit` 0 错。

### 关联 Commit

- `feat(frontend-shell): design tokens for ui-design parity`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/002-app-shell-visual-system.

由于当前分支 `codex/frontend-shell-ui-design-native-parity` 是 D2 v2 parity 重做分支，dev 上保留有现已被废弃的 D2 v1 实现 8 commits；本次 phase commit 仅落在 feature 分支，跳过 `git merge --ff-only dev` 步骤。后续整合策略由用户在 PR 阶段决定（force-push / merge-commit / 替换 v1）。

## 21:43 工作记录

### 完成事项

- 落地 `frontend-shell/002-app-shell-visual-system` Phase 2 全部两项：通过 fontsource 接入 7 个 EI_FONT_PRESETS 所需开源字体（Noto Serif SC、Inter、Source Serif Pro、Cormorant Garamond、IBM Plex Sans、Geist Sans、JetBrains Mono），并在 `frontend/src/app/theme/fonts.css` 集中 @import 所需 weight 与 chinese-simplified 子集；`global.css` 在 themes.css 之前 import fonts.css，在 themes.css 之后 import typography.css，构建一次性 entry。
- 落地 `frontend/src/app/theme/typography.css`：`:root` 声明 6 级 scale（display / title / subtitle / body / caption / label）的 `--ei-text-*-size` / `-line` / `-weight` / `-track` 24 个 token，每条都从 `ui-design/src/screen-home.jsx`、`screen-auth.jsx`、`screen-profile.jsx`、`primitives.jsx::SectionHeader` 与 `app.jsx::TopBar` 抽取的字号节奏；`.ei-text-*` className 全部走 `var(--ei-text-…)` 引用，`ei-text-label` 唯一携带 `text-transform: uppercase`，对齐 primitives.jsx 的 `ei-label` 规则。
- 写入两组 focused tests：`fonts.test.ts`（5 项，断言 fontsource 7 个 dep、私有 brand 字体名负向、`fonts.css` 引入完整、`global.css` 引入 `fonts.css`、`themes.css` font 链以开源字体起始且含 system fallback）、`typography.test.tsx`（17 项，断言 6 级 scale 的 token 声明 + var() wiring、family 路由、uppercase 仅落在 ei-text-label、scale 数值能在对应 ui-design 源文件 `fontSize:` 字面量中找到、`global.css` 引入顺序、`TopBar.tsx` 不内联 px font-size）。
- 完成验证：`pnpm --filter @easyinterview/frontend test`（35 files / 183 tests）、`pnpm exec tsc -p . --noEmit` 0 错；D1 全量 regression 不退化。

### 关联 Commit

- `feat(frontend-shell): open-source fonts and typography scale`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/002-app-shell-visual-system. 由于 dev 上仍是 D2 v1 实现，phase commit 仅落在 `codex/frontend-shell-ui-design-native-parity`，未触发 dev 合并。

## 21:48 工作记录

### 完成事项

- 落地 `frontend-shell/002-app-shell-visual-system` Phase 3 全部两项：用 `frontend/src/app/topbar/topbar.css` 把 TopBar 视觉节奏完全从 `ui-design/src/app.jsx` TopBar 静态原型源码迁移过来 — `ei-shell-topbar` height 58 / padding `0 var(--ei-space-8)` / gap `var(--ei-space-7)` / `position: sticky` / `top: 0` / `z-index: 30` / `border-bottom: 1px solid var(--ei-color-rule-strong)` / `background: var(--ei-color-bg-canvas)`，所有数值能在 app.jsx TopBar 节奏字面量中找到（`height: 58`、`padding: "0 32px"`、`gap: 28`、`zIndex: 30`、`borderBottom: 1px solid T.rule`、`position: "sticky"`）。
- 用 `ei-topbar-nav-button` + `ei-text-body` className 改写五入口；active 状态 `[aria-current="page"] { background: var(--ei-color-bg-soft); color: var(--ei-color-fg-primary); font-weight: 500; }`，hover 状态对齐原型 ink2 → ink，padding/radius 6px 12px / 2px。
- 给主题下拉 (`ei-topbar-theme`) / 暗色 toggle (`ei-topbar-dark`) / 语言下拉 (`ei-topbar-lang`) / 登录 (`ei-topbar-auth-login`) / 注册 (`ei-topbar-auth-register`) 控件挂上语义 className 与 `ei-topbar-control` 控件密度（高 26px、padding 0 8px、border `var(--ei-color-rule-strong)`、字号 12 / 13），D1 `topbar-theme-select` / `topbar-dark-toggle` / `topbar-lang-select` / `topbar-login` / `topbar-register` testid 不变；`aria-pressed` / `aria-current` regression 通过。
- 新增 D2 `topbar-custom-accent-button` 控件（aria-pressed toggle）+ `topbar-custom-accent-picker` popover，内含 `topbar-custom-accent-hue` / `topbar-custom-accent-chroma` slider 与 `topbar-custom-accent-clear` 按钮；按 `ui-design/src/app.jsx::AccentPicker` 转写 hue/chroma slider track 与彩虹 conic-gradient swatch；customAccent 激活后 TopBar swatch 渲染 `oklch(... )` inline style 并由 `DisplayPreferencesProvider` 把 `--ei-color-accent` / `--ei-color-accent-soft` 内联覆盖到 `<html>`。
- 写入 `TopBarVisual.test.tsx`（12 项）覆盖 root className、display controls className、custom accent 控件 toggle + popover slider + swatch oklch、active nav 状态 CSS、i18n 切换、TopBar.tsx 不内联 px。D1 `TopBar.test.tsx`（8 项）+ `E2E.P0.004` lang switch scenario regression 全通过。
- 完成验证：`pnpm --filter @easyinterview/frontend test`（36 files / 195 tests）、`pnpm exec tsc -p . --noEmit` 0 错。

### 关联 Commit

- `feat(frontend-shell): topbar visual rhythm and custom accent`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/002-app-shell-visual-system。dev merge skipped（v1 D2 已在 dev）。

## 21:58 工作记录

### 完成事项

- 落地 `frontend-shell/002-app-shell-visual-system` Phase 4：把 5 个 auth screen（login / register / verify / reset / logout）从单纯 form section 升级为对齐 `ui-design/src/screen-auth.jsx::AuthShell` 的两列卡片视觉。新增 `frontend/src/app/auth/AuthShell.tsx` 抽象 shell（左侧 eyebrow / display title / sub copy / 侧边面板，右侧 `ei-auth-card`），所有 screen 不再直接管理 `<section>` chrome；侧边面板按 pendingAction 编码自动切换为「待恢复操作」accent 卡片（保留 D1 pendingAction 隔离逻辑）。
- 新增 `frontend/src/app/auth/auth.css` 把 max-width 1160 / padding 54 48 96 / grid 0.88fr 1.12fr / gap 44 / 表单 field 高 42 / CTA 高 46 / 状态卡片背景 ok-soft / warn-soft / bg-soft 等节奏全部走 design token，逐项追溯到 `screen-auth.jsx`。
- 用 `ei-text-label` / `ei-text-display` / `ei-text-body` 取代手写 `<h1>` 与 `<p>`；`ei-auth-field-input` / `ei-auth-cta` / `ei-auth-stub` / `ei-auth-secondary-link` / `ei-auth-status` / `ei-auth-row` 等语义 className 替换原来无 className 的元素，保留所有 D1 testid（`route-auth_*`、`auth-login-email-form`、`auth-login-{password-stub,oauth-stub,link-register,link-reset}`、`auth-register-{name,email,password-stub,terms,submit}`、`auth-verify-{form,code,submit,email-hint,resend}`、`auth-reset-{form,email,send-stub,stub-hint,link-login}`、`auth-logout-{data-hint,confirm,cancel}`）。
- 在 `i18n/locales/zh.ts` / `en.ts` 各新增 `auth.principle.{eyebrow,body}` / `auth.pendingAction.{eyebrow,body}` / `auth.{login,register,verify,reset,logout}.{eyebrow,title,sub}` 共 18 个 key，所有侧边面板与标题文案均通过 typed i18n helper 渲染；保留 D1 `Accept-Language` display hint 与登录解耦的 i18n 行为。
- `frontend/src/app/theme/global.css` 追加 `@import "../auth/auth.css"`；写入 `AuthVisual.test.tsx`（18 项）覆盖 5 screen 的 `ei-auth-shell` 双列布局、`ei-auth-side-panel` 渲染、`ei-text-display` + `ei-text-label` 头部、auth.css 节奏值、`screen-auth.jsx` 字面量回查、`global.css` 引入与 D1 testid regression。
- 完成验证：`pnpm --filter @easyinterview/frontend test`（37 files / 213 tests）、`pnpm exec tsc -p . --noEmit` 0 错。`AuthScreens.test.tsx` D1 测试 + `AppPendingAction.test.tsx` + `authContractGate.test.ts` + `E2E.P0.002` scenario 全部通过。

### 关联 Commit

- `feat(frontend-shell): auth shell card visuals`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/002-app-shell-visual-system。pendingAction 检测仍走 `decodePendingActionRoute`，shell 只控制视觉表现；form 字段、testid、verify token wire、auth-only params 隔离逻辑 D1 regression 完整保留。

## 22:04 工作记录

### 完成事项

- 落地 `frontend-shell/002-app-shell-visual-system` Phase 5：把 `profile` / `settings` 与 `PlaceholderScreen` 三个 D1 占位屏幕全部接入 D2 `ei-screen-shell` + `ei-screen-card` 视觉骨架。新增 `frontend/src/app/screens/screens.css` 抽取 `ui-design/src/screen-profile.jsx` 的 max-width 1160 / padding 54 48 96 / gap 28 / card padding 28 / `repeating-linear-gradient` 骨架占位等节奏，所有规则走 design token；`global.css` 追加 `@import "../screens/screens.css"`。
- `ProfileScreen.tsx` 把 5 个分区（identity / sections / insight cards / used-by / recent evidence）逐项包裹为 `ei-screen-card`，标题 `ei-text-title`，bullet 列表 `ei-text-body`，并在 insight / recent evidence 卡片插入 `ei-skeleton-stripe` 占位让 D2-D6 owner 接入；保留所有 D1 testid（`profile-{identity-summary,sections,insight-cards,used-by,recent-evidence}`）与 negative regression（`profile-{growth,experiences,mistakes,drill,followup}` 不复活）。
- `SettingsScreen.tsx` 把 6 个分区（account / login-security / font-preset / privacy / notifications / subscription）改写为同一卡片节奏，notifications / subscription 占位卡渲染 `ei-skeleton-stripe`；保留 `settings-{account,login-security,font-preset,privacy,notifications-placeholder,subscription-placeholder}` 全部 D1 testid 与 retired-module 负向断言。
- `PlaceholderScreen.tsx` 升级为标题 + 描述 + 三段 skeleton（一个 stripe + 两条 line）的卡片骨架，`data-route-name` / `data-route-params` 与 `placeholder.{workspace,default}` i18n 文案契约保留，让后续 D2-D6 owner 在同骨架内扩展业务内容。
- 写入 `ScreensVisual.test.tsx`（11 项）覆盖三屏 root className、display + body 头部、ei-screen-card 分区、retired-module 负向、PlaceholderScreen 卡片 skeleton、screens.css 节奏值、`screen-profile.jsx` 追溯、`global.css` 引入。
- 完成验证：`pnpm --filter @easyinterview/frontend test`（38 files / 224 tests）、`pnpm exec tsc -p . --noEmit` 0 错；`ProfileScreen.test.tsx` D1 regression 5 项继续通过。

### 关联 Commit

- `feat(frontend-shell): profile, settings, and placeholder card shells`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/002-app-shell-visual-system。所有 retired modules（growth / experiences / mistakes / drill / 独立 voice）通过 negative testid + negative copy assertion 守住，避免视觉接入引入旧入口。

## 22:18 工作记录

### 完成事项

- 完成 `frontend-shell/002-app-shell-visual-system` Phase 6 全部 5 项：跑通 D1 行为 regression（`pnpm --filter @easyinterview/frontend test` 39 files / 231 tests + `E2E.P0.001 / 002 / 004` setup→trigger→verify→cleanup 全部 PASS）；通过真实 build smoke（`pnpm --filter @easyinterview/frontend build` 与根 `make build` 均 OK，bundle ~305 KB CSS / ~179 KB JS gzip 56 KB，fontsource 字体 woff/woff2 进入 dist）；通过 Active-scope 负向搜索（无 `tailwindcss` / `postcss-tailwind` / `styled-components` / `@emotion/*` 依赖、无私有 brand 字体名、`customAccent` 控件 / state / token helper 完整保留）。
- 创建 `test/scenarios/e2e/p0-005-app-shell-visual-system-smoke/` 场景包：`README.md`（含当前覆盖与 Playwright follow-up 接入步骤）、`data/{seed-input,expected-outcome}.md`、`scripts/{setup,trigger,verify,cleanup}.sh`；同步 `test/scenarios/e2e/INDEX.md` 增加 P0.005 行；`./scripts/setup.sh && ./scripts/trigger.sh && ./scripts/verify.sh && ./scripts/cleanup.sh` 端到端通过。
- 实现 `frontend/src/app/scenarios/p0-005-app-shell-visual-system-smoke.test.tsx`（7 项）：注入 themes/typography/topbar/auth/screens 五个 CSS 后断言 TopBar / auth / profile / settings / placeholder shell 全部 className wiring；warm/light → ocean/light + warm/dark `getComputedStyle(documentElement).getPropertyValue` 解析 EI_THEMES 转写值；`customAccent` 激活仅 inline 覆盖 accent / accent-soft；retired 入口（welcome / growth / mistakes / drill / 独立 voice）与文案（错题本 / 成长中心 / 经历库）均不可达；`app.jsx` / `screen-auth.jsx` / `screen-profile.jsx` 字面量尺寸（height 58 / padding 0 32 / gap 28 / max-width 1160 / padding 54 48 96 / grid 0.88fr 1.12fr / card padding 28）能在 D2 CSS 内对应位置找到。
- 同步 `bdd-checklist.md`：E2E.P0.005 + 3 个 regression 场景全部勾选并写入 verification 注释；显式记录 Playwright bounding-box / screenshot diff 为后续 follow-up，避免下次 deep reconcile 把 jsdom 范围误读成完成态。
- 扩写 `frontend/README.md` §2.7 D2 视觉骨架接入点：design tokens / 主题/暗色/customAccent wiring / 字体加载 / 视觉骨架 className 表 / visual smoke 工具与 parity gate 重跑方式 / `ui-design` 原生迁移规则 / Playwright follow-up 接入步骤，对齐 Phase 6.5 接入点契约。
- 完成验证：`pnpm --filter @easyinterview/frontend test` 39 files / 231 tests PASS；`pnpm exec tsc -p . --noEmit` 0 错；`make docs-check` zero drift（Header / INDEX / Orphans / Warnings 全部空）+ check_md_links OK。

### 关联 Commit

- `feat(frontend-shell): visual system regression and handoff`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-shell/002-app-shell-visual-system。Phase 6 收口后，本 plan 满足 spec.md C-8（视觉接入对齐 ui-design 真理源）当前可在 vitest+jsdom 范围内验证的全部门禁；真实浏览器层面的 100% pixel parity 留作 Playwright follow-up，应在用户决定整合策略后派生独立 plan 推进。
