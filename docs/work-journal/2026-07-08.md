# 2026-07-08 工作日志

## 15:48 工作记录

### 完成事项

- 闭环修复简历模块 source-format / Markdown / PDF 预览回归：详情页使用正式 Markdown renderer，PDF 原件通过 `getResumeSource` 和 page-stack renderer 展示，Markdown/PDF 共用阅读背景板且正文不再注入 header 元数据。
- 收口简历上传与解析合同：默认上传限制对齐 2MiB，`resume.maxActive` 生效，DOCX 从当前上传白名单退役，`resume.parse` success/failure path 均保持可读 Markdown 快照。
- 修复 Home / Parse 已有简历选择回归：`isSelectableInterviewResume` 统一允许非归档且已有可读正文证据的 failed / queued / processing 简历进入选择器。
- 同步 `backend-resume`、`frontend-resume-workshop`、`openapi-v1-contract`、`mock-contract-suite` owner spec/plan/checklist/BDD/context，更新 [BUG-0139](../bugs/BUG-0139.md)、[BUG-0140](../bugs/BUG-0140.md)、[BUG-0141](../bugs/BUG-0141.md) 与交付复盘报告。
- 验证通过：`sync-doc-index --check`、`make docs-check`、`git diff --check`、OpenAPI diff / fixture / inventory gate、backend resume/upload/config/cmd focused Go tests、frontend focused Vitest、frontend `typecheck` / `build`、`ui-design` contract、Resume Workshop pixel parity。

### 关联 Commit

- `fix(resume): close source preview and picker regressions (BUG-0139, BUG-0140, BUG-0141)`

### 备注

Owner plans: `backend-resume/001-asset-register-parse-and-listing`, `frontend-resume-workshop/001-listing-routing-and-detail-readonly`, `frontend-resume-workshop/002-create-flow`, `openapi-v1-contract/004-resume-additive-coverage`.

## 16:17 工作记录

### 完成事项

- 闭环修复 Parse 页 JD 解析状态读取 500：定位到 `backend/internal/targetjob/store.go` 仍引用已退役的 `target_jobs.profile_id`，当前 B4 schema 已无该列，导致 `GET /api/v1/targets/{targetJobId}` 在失败态 TargetJob 上返回 `TARGET_IMPORT_FAILED`。
- 从 TargetJob active SQL、record 和 scanner 路径删除 `profile_id`，同步 sqlmock 列集合，并新增真实 Postgres integration regression 覆盖 `analysisStatus=failed`、无 requirements、仍可读取详情的路径。
- 原地更新 `backend-targetjob/001-targetjob-import-and-parse-bootstrap` spec / plan / checklist / context，追加 C-15 与 7.11 gate，固化退役列 zero active reference、real DB-backed gate 和截图同款接口 smoke。
- 创建 [BUG-0142](../bugs/BUG-0142.md)，更新 Bug 模式库和交付复盘报告 [TargetJob Profile Schema Drift](../reports/2026-07-08-targetjob-profile-schema-drift-assessment.md)。
- 验证通过：`go test ./internal/targetjob -count=1`、TargetJob real Postgres integration gate、`cmd/api` TargetJob HTTP focused gate、`validate_context.py`、`sync-doc-index --check`、`test/scenarios/env-verify.sh`，并确认截图同款 `GET /api/v1/targets/{targetJobId}` 返回 200 + `analysisStatus=failed`。

### 关联 Commit

- `fix(targetjob): align store with pruned profile schema (BUG-0142)`

### 备注

Owner plan: `backend-targetjob/001-targetjob-import-and-parse-bootstrap`.

## 16:30 工作记录

### 完成事项

- 修复 BUG-0142 follow-up review finding：真实 Postgres integration gate 不再在缺少 `DATABASE_URL` 或 DB 不可达时 `SKIP` 假绿，而是 fail fast 暴露 schema-drift 防线未执行。
- 原地更新 `backend-targetjob/001-targetjob-import-and-parse-bootstrap` plan / checklist / context / plans index，追加并闭合 7.12 skip-proof gate。
- 更新 [BUG-0142](../bugs/BUG-0142.md)、Bug 模式库和交付复盘报告 [TargetJob Profile Schema Drift](../reports/2026-07-08-targetjob-profile-schema-drift-assessment.md)，固化强制 integration gate 必须拒绝 `--- SKIP` / `no tests to run` 的规则。
- 验证通过：未设置 `DATABASE_URL` 时 focused integration gate 非 0 且无 `--- SKIP`，设置真实 local-dev Postgres `DATABASE_URL` 后同一 gate 通过；`go test ./internal/targetjob -count=1`、`validate_context.py`、`sync-doc-index --check`、`make docs-check`、`git diff --check` 通过。

### 关联 Commit

- `fix(targetjob): require postgres for schema drift gate (BUG-0142)`

### 备注

Owner plan: `backend-targetjob/001-targetjob-import-and-parse-bootstrap`.
