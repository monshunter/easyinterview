# 2026-06-14 工作日志

## 09:38 工作记录

### 完成事项

- **UX funnel refactor drift 闭环修复（BUG-0118）**：从 `7e6ec756d91280b9046d34f362a3cde58bbf2d29` 后的变更做 deep reconcile，修复 D-17/D-18/D-20 后遗留在场景资产、contract lint、frontend route/test、backend e2e helper、fixture README 和 roadmap 文档中的旧口径。
- **D-20 flat resume 对齐**：将 OpenAPI fixture / inventory / mock contract gate、backend full-funnel helper、frontend e2e/pixel specs、scenario data/verify 全部收敛到 `resumeId`、`resume_id`、43 operations / 43 fixtures，并新增 deleted route 负向测试防止 `resume_versions` 回流。
- **D-17/D-18 UI/route 对齐**：删除 live `company_intel` route，保留 legacy alias 到 `workspace`；四入口 TopBar、workspace 嵌入、无 guided create 的 frontend README、i18n、CSS、测试与场景脚本已同步。
- **测试门禁修复**：P0.035 JSONB 断言改为语义比较；P0.098 full-funnel helper 修复 6 位 auth code 与已删除 `resume_asset_id` 字段；P0.016 verify gate 改为当前 `resumeId` evidence。
- **收尾资产**：新增 `BUG-0118` 与 2026-06-14 delivery retrospective，更新 `docs/bugs/INDEX.md`、`docs/reports/INDEX.md`、`docs/spec/INDEX.md` 与 engineering roadmap history/spec。

### 关联 Commit

- `fix(test): close ux funnel refactor drift (BUG-0118)`

### 备注

- 已验证：P0.035 / P0.004 / P0.016 / P0.098 / P0.079 / P0.083 / P0.089 / P0.006 setup-trigger-verify-cleanup；backend `go test ./...`；frontend `typecheck` / vitest / build / pixel-parity；fixture/codegen/docs/index/diff gates 全绿。
- 本次作为单一原子 bugfix commit 提交：改动跨 backend、frontend、openapi、scripts、scenario 和 docs，但都服务于同一个 refactor drift 闭环，拆分会降低 BUG-0118 的追溯完整性。

## 10:59 工作记录

### 完成事项

- **Resume flatten review regressions 闭环修复（BUG-0119）**：修复 reviewer 指出的 8 条 D-20 resume flatten / D-17 JD Match removal 后续回归，覆盖 job polling owner query、OpenAPI baseline、resume PATCH 局部更新、privacy async job 清理、resume parse job resource type、accepted rewrite 持久化和 migration 历史合法行 cleanup。
- **Contract 与 migration gate 对齐**：重新冻结 43-operation OpenAPI baseline，补充 migration SQL contract tests，并用 temporary database 验证 retired JD Match async jobs 与 guided resumes 在 constraint narrowing 前被清理或转换。
- **测试与收尾资产**：补齐 backend/frontend focused tests、全量 backend/frontend gates、`BUG-0119` 与交付复盘，更新 bug/report/work-journal indexes。

### 关联 Commit

- `fix(schema): close resume flatten review regressions (BUG-0119)`

### 备注

- 已验证：focused backend tests、ResumeDetailView focused Vitest、`make openapi-diff`、`make lint-openapi`、`make validate-fixtures`、`make codegen-check`、`go test ./backend/...`、`make migrate-check`、temporary database migration verification、frontend typecheck/full Vitest/build、`git diff --check`、`make docs-check`、sync-doc-index check。
