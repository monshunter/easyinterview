# 2026-05-09 工作日志

## 07:46 工作记录

### 完成事项

- 修复 `frontend-home-job-picks-and-parse/001-home-jd-import-and-parse` review follow-up：`parse` route 现在直接渲染 `ParseScreen`，避免回落到 `PlaceholderScreen`。
- 修复 Parse 页重新解析行为：`Re-parse` 会清理旧轮询、重置错误/步骤状态，并触发新的 `getTargetJob` polling。
- 修复 targetjob update 持久化：`TitleHint` / `CompanyNameHint` 改为覆盖写入，确保编辑后的标题和公司名能保存。
- 补充 frontend route、re-parse poll、backend title/company overwrite 的回归测试，并通过前端全量测试、typecheck、build、后端 focused package 测试和 `git diff --check`。

### 关联 Commit

- `fix(home-jd-import): address parse review findings`

### 备注

本次按用户确认使用单个 commit 收口；范围是同一轮 code review 的前后端修复。`/bug-report` 与 `/retrospective` 已评估，现有 BUG-0029 / BUG-0028 和 2026-05-08 remediation report 已覆盖根因类型，因此未新增重复记录。
## 09:45 工作记录

### 完成事项

- 新增 InterviewContext store/reducer (HYDRATE_FROM_ROUTE / MERGE_TARGET_JOB / MERGE_RESUME / MERGE_PRACTICE_PLAN / MERGE_SESSION / CLEAR) 与 useInterviewContext / useStartPracticeContext hook
- 新增 INTERVIEW_CONTEXT_ROUTES / shouldCarryInterviewContext 常量到 routes.ts
- 新增 WorkspaceScreen 静态壳 (plan eyebrow + header summary + Interview Launcher Round Rail + BindingPill 双卡 + Main Left CompanyIntelEmbed + JD 拆解 + Main Right risks/strengths + sessionHistory)，源级复刻 ui-design
- App.tsx 绑定 workspace 路由 → WorkspaceScreen，添加 InterviewContextProvider
- 扩展 i18n workspace.* 命名空间 zh/en 双向 79 keys
- 新增 useWorkspaceTargetJob hook (loading/data/error/empty 四态，inFlightRef 防 StrictMode 双触)
- TargetJob 数据接入 WorkspaceScreen (plan eyebrow/header/BindingPill JD，status tone 三态)
- Phase 1 测试覆盖 (≥20 testid、控件类型、负向断言旧 prototype testid、zh/en locale)
- App.test.tsx / AppNormalize.test.tsx / p0-002 test 适配 workspace route 变更

### 关联 Commit

- `feat(workspace): add InterviewContext store, WorkspaceScreen shell, and i18n`

### 备注

Phase 1 5/6 完成 (BDD-Gate 1.6 依赖 Phase 6 场景资产，已跳转)。Phase 2 已开始：2.1 useWorkspaceTargetJob hook + 测试，2.2 数据接入。总计 364 tests PASS、TypeScript 0 error。
## 10:35 工作记录

### 完成事项

- Phase 2 完成 JD 拆解数据驱动 (must_have/nice_to_have/hidden_signal 按 kind 分组)、risks/strengths 列表、fixture variants (with-rounds/not-found)
- Phase 3 完成 useWorkspaceResume hook (loading/data/error/empty 四态)、BindingPill Resume 数据接入 (readResumeSummary helper)
- Phase 3 完成 ResumePickerModal (disabled-list 模式，仅展示当前绑定简历 + disabled 占位卡) + PlanSwitcherModal (listTargetJobs 接入)
- Phase 3 完成 useModalA11y hook (ESC/backdrop/close button/focus trap 四种关闭路径)
- 新增 fixture variants: getResume not-found, getPracticePlan archived/not-found
- 测试: useModalA11y (5 tests)、ResumePickerModal (5 tests)、PlanSwitcherModal (5 tests)、useWorkspaceResume (4 tests)、WorkspaceHeader (6 tests)

### 关联 Commit

- `feat(workspace): add Phase 2-3 data hooks, JD breakdown, modals, and fixture variants`

### 备注

总计 389 tests PASS、60 test files、TypeScript 0 error。Phase 3 剩余 3.7（空态测试）、3.8（BDD-Gate 待 Phase 6）。Phase 4-6 待继续。
## 10:48 工作记录

### 完成事项

- Phase 3 完成 WorkspaceEmptyState / WorkspaceMissingResumeState 空态渲染与测试 (4 tests)
- Phase 4 完成 useWorkspacePracticePlan hook (ready/archived/not-found 三态)
- Phase 4 完成 useStartPractice hook（双步契约：createPracticePlan → startPracticeSession, Idempotency-Key batch, retry dedupe）
- Phase 4 CTA 接线：WorkspaceScreen 立即面试按钮接入 useStartPractice, nav practice 携带完整 InterviewContext
- 新增 buildCreatePlanRequest helper 与 newIdempotencyBatch 工具函数
- 更新 authContractGate 允许列表 (getResume/getPracticePlan/createPracticePlan/startPracticeSession)
- 修复 WorkspaceScreen 空态显隐逻辑，适配现有测试

### 关联 Commit

- `feat(workspace): add empty states, practice plan hook, and start practice dual-step contract`

### 备注

总计 397 tests PASS、62 test files、TypeScript 0 error。Phase 4 剩余 4.4-4.9（ButtonState、错误映射、fixture variants、测试）。
## 11:00 工作记录

### 完成事项

- Phase 4 CTA 错误显示：inline 错误消息 + retry 按钮 + 3 次失败 fallback `回到首页`
- Phase 4 loading 态：CTA disabled + spinner 效果 + 降低透明度
- Phase 4 fixture: startPracticeSession.json 新增 ai-timeout-502 variant
- Phase 4 测试：WorkspaceStartPractice happy path (createPracticePlan→startPracticeSession→nav practice) + retry idempotency key 复用
- 修复 HYDRATE_FROM_ROUTE reducer: 空字符串 planId/jdId 不触发 fallback 推导
- authContractGate 添加 getResume/getPracticePlan/createPracticePlan/startPracticeSession 允许操作

### 关联 Commit

- `feat(workspace): add start practice flow, error handling, and idempotency retry`

### 备注

总计 399 tests PASS、63 test files、TypeScript 0 error。Phase 4 核心双步契约已跑通。Phase 4 剩余：4.7 更完整的测试覆盖、4.8 auth pendingAction 接线测试。Phase 5-6 待继续。
## 11:06 工作记录

### 完成事项

- 派生 F3 `prompt-rubric-registry/001-baseline` impl plan：新增 `plans/001-baseline/{plan.md,checklist.md,context.yaml}`，把 spec C-1~C-11 与 D-1~D-12 落到 5-phase vertical slice（truth source + Go registry + lint gates + DB seed + targetjob retire + B1/B4/A3 provenance remediation + A3 profile coverage handoff）。
- 修订 `prompt-rubric-registry/spec.md` 至 v2.1（2026-05-09）：锁定 D-2 多 language 文件命名约定（`<version>.<language>.{yaml,md}`）、D-3 `template_hash` 自引用消除（`meta_for_hash` 去 hash 字段后规范化）、D-7 灰度按 `(feature_key, language)` 唯一 active；§4.1 SemVer 字符串 + `(feature_key, version, language)` unique；§4.3 启动预加载从 12 改为 10；§6 C-1 改为 `find -mindepth 1 -maxdepth 1` 计数避免 README 误算，C-9 升级为 typed columns（`feature_key` / `prompt_version` / `rubric_version` / `feature_flag` / `data_source_version`）；§7 关联计划改为 active 状态。
- 同步 `prompt-rubric-registry/history.md` v2.1 修订记录、`plans/INDEX.md` 把 001-baseline 列入 Active，并把 `docs/spec/INDEX.md` 主表的 prompt-rubric-registry 行升级到 2.1 / 2026-05-09。
- 用户决策固化：A3 profile coverage 仅校验 entry 存在 + status 合法（不动 `config/ai-profiles.yaml` status），DB seed 纳入本 plan 一起跑，prompt body 写真实可用文案而非 TBD，LLM Judge 落 `Judge` interface + `NotImplementedJudge` stub；本 plan 同时显式修订 B1/B4/A3 上游 prompt provenance 契约与实现，避免 cross-layer 缺口被误解为 plan 错断。

### 关联 Commit

- `docs(prompt-rubric-registry): derive baseline plan and bump spec to v2.1`

### 备注

仅文档变更（spec / plan / checklist / context / history / INDEX），无代码改动。`/plan-review` 在派生过程中已跑过两轮 L1 fix，本 commit 是固化结晶；下一步建议进入 `/implement docs/spec/prompt-rubric-registry/plans/001-baseline` 启动 Phase 0 deep reconcile（先核对 B1/B4/A3 当前缺口与上游 spec 现状），再进 Phase 1 truth source 实施。
## 11:26 工作记录

### 完成事项

- F3 `prompt-rubric-registry/001-baseline` Phase 0 上游契约复核完成（5/5 items 全过 grep / 字段对照表验证）。
- 0.1 `config/ai-profiles.yaml` 抽出 spec §3.1.1 列出的 10 个默认 `model_profile_name` 当前 status / capability / provider_ref / unsupported_reason；10 行全部 `status=active` + `provider_ref=deepseek`，写入 plan §8.1.2.1 + `/tmp/phase0-profile-snapshot.md`。
- 0.2 `migrations/000001_create_baseline.up.sql` L341-396 schema 复核：`prompt_versions` / `rubric_versions` 字段集与 spec §4.1 完全一致；`ai_task_runs` 缺少 `feature_key` / `feature_flag` / `data_source_version` typed columns 三字段，缺口写入 plan §8.1.2.2，由 Phase 4 通过 additive migration 修复。
- 0.3 `backend/internal/targetjob/prompt_registry.go` retire 标识符清单：4 个 `defaultTargetImport*` 常量 + `StaticPromptRegistry` struct + `NewStaticPromptRegistry` func + `(*StaticPromptRegistry).Resolve` method；保留 `FeatureKeyTargetImportParse` / `PromptResolution` 7 字段 / `PromptRegistryClient` interface / `ErrPromptUnsupported`，并锁定 `RegistryAdapter` 适配契约（写入 plan §8.1.2.3）。
- 0.4 `openapi/openapi.yaml` `GenerationProvenance` 6 字段 (`promptVersion / rubricVersion / modelId / language / featureFlag / dataSourceVersion`) 全部 required，对齐 backend-practice/spec.md v1.3 D-29 前置依赖；记录 `modelId` 与 registry `ModelProfileName` 的层次区别（写入 plan §8.1.2.4）。
- 0.5 B1/B4/A3 provenance contract gap 实证矩阵：B1 vocabulary 缺 `feature_key`、B4 `ai_task_runs` 缺 3 个 typed columns、A3 `CallMetadata` 缺 `FeatureFlag`、`AICallMeta` / `AITaskRunRow` / `observability/decorator.go` 全缺三字段；锁定 Phase 4 sequencing：B1 → B4 → A3（写入 plan §8.1.2.5）。

### 关联 Commit

- `docs(prompt-rubric-registry): snapshot upstream contracts for f3 baseline`

### 备注

Auto-committed by /tdd phase-commit, plan: prompt-rubric-registry/001-baseline

本次仅文档变更（plan §8 evidence log + checklist 0.1-0.5 marks），无代码改动。Phase 0 是 read-only handoff snapshot，为 Phase 1 truth source 实施提供事实基线。
## 11:50 工作记录

### 完成事项

- F3 `prompt-rubric-registry/001-baseline` Phase 1 truth source + 3 套 lint script 收口（8/8 items）。
- 1.1 新增 `config/prompts/README.md` + `config/rubrics/README.md`：固定字段顺序、canonical hash 算法（`sha256(template_body || canonical_meta_json)`，`meta_for_hash` 移除 `template_hash`，按 sort_keys + UTF-8 + LF 行尾跨工具共享）、维度命名 allowlist、multi-language 命名约定（`<v>.<lang>.{yaml,md}`）。
- 1.2 落地 10 个 feature_key × 2 language = 20 prompt yaml/md baseline，template body 写真实可用文案，无 TBD/placeholder 标记，hash 与 README §3 算法一致。
- 1.3 落地 10 个 feature_key × 2 language = 20 rubric yaml baseline，每个 rubric ≥3 dimensions，weight 总和 = 1.0 (±0.001)，dimension 名命中 spec §4.1 allowlist。
- 1.4 实现 `scripts/lint/prompt_lint.py` + 4 unit tests：字段顺序固定、SemVer regex、status 枚举、language 枚举、template_hash drift；2 个 negative fixture（hash drift / 字段顺序错乱）红→绿。
- 1.5 实现 `scripts/lint/rubric_lint.py` + 4 unit tests：dimensions 非空、weight sum 容差、score_levels schema、维度 allowlist；2 个 negative fixture（缺 weight / 违反 allowlist）红→绿。
- 1.6 实现 `scripts/lint/prompt_hardcode_lint.{go,py}` AST-based hardcoded-prompt boundary lint + 6 unit tests：AssignStmt + GenDecl 扫 `prompt :=` / `Prompt = "..."` / `systemMessage := "..."`；short version-string-style 不误报；`*_test.go` allowlisted；raw / 长 quoted string 触发。
- 1.7 在 `Makefile` 追加 `lint-prompts` / `lint-rubrics` / `lint-prompts-hardcode` 三个 target 并加入聚合 `lint`；migration_lint 经 `make migrate-check` 触发，dev 环境无 DATABASE_URL 时 cmd/migrate check 段提前退出（与既有 dev 形态一致）。
- 1.8 Regression-negative scope grep `mistakes|growth|drill|mistake.extract` 在 `config/prompts/` + `config/rubrics/` 0 行命中。

### 关联 Commit

- `feat(prompt-rubric-registry): land f3 baseline truth source and lint gates`

### 备注

Auto-committed by /tdd phase-commit, plan: prompt-rubric-registry/001-baseline

Phase 1 完成后：F3 RegistryClient（Phase 2）有可加载的 truth source；Phase 4 seed migration 写入 `prompt_versions` / `rubric_versions` 时 `template_hash` 与 yaml 一致；prompt_lint cross-file gate（4.6）在 Phase 4 seed migration 落地后即可激活。聚合 `make lint` 仍存在历史遗留 revive var-naming 警告（`backend/internal/ai/aiclient/providers/minimax_speech/`、`backend/internal/targetjob/handler.go targetJobId`），来自更早 commit (`f2f5fc9` 之前)，与本 plan 无关，不阻塞 Phase 1 收口。
## 12:01 工作记录

### 完成事项

- F3 `prompt-rubric-registry/001-baseline` Phase 2 收口：`backend/internal/ai/registry/` Go 包落地（9/9 items）。
- 2.1 `doc.go` 包注释 + 4 条边界红线（不依赖 targetjob、不调 AI client、不写业务 metric / log、不读 process env）；rephrased so red-line text does not collide with the 2.9 grep regex (no literal `AIClient`, `aiclient.`, `metric.Counter`, `os.Getenv`).
- 2.2 `types.go`：`PromptResolution`（11 字段：8 兼容 targetjob + 3 D-12 reservation）、`PromptMeta`、`RubricSchema`、`RubricDimension`、`ScoreLevel`、`Score`、`Reasoning`、`Judge` interface + 3 错误类型；`TestTypeShape` + `TestJudgeSignature` 通过反射断言固定字段顺序与 spec D-9 入参顺序。
- 2.3 `loader.go` + `loader_test.go`：扫 `config/prompts/` + `config/rubrics/`、解析 yaml、验 markdown body 存在、跨工具复用 `prompt_lint.py` 的 canonical hash 算法、强制 prompt/rubric language parity；4 tests（happy / hash drift / missing body / yaml 解析失败）。
- 2.4 `resolver.go` + `resolver_test.go`：`ResolveActive` 精确 language → multi fallback；`FallbackCount` 在 fallback 时递增；空字符串返回 `ErrPromptUnsupported` / `ErrLanguageUnsupported`；`GetPrompt` / `GetRubric` 用于 backfill / debug；6 tests。
- 2.5 `cache.go` + `cache_test.go`：`atomic.Pointer[snapshot]` 整体替换 + 30s TTL + 可注入 Now（测试用）+ Reload 显式钩子；3 tests（5× idempotent / TTL 触发 reload / 100 goroutine + reload 交错 race-free）。
- 2.6 `judge.go` + `judge_test.go`：`NotImplementedJudge` 默认实现，所有调用返回 `ErrJudgeNotImplemented`；签名 freeze 配合 types_test。
- 2.7 `registry.go` + `registry_test.go`：`NewRegistryClient(opts)` 全量加载 + 启动 fail when prompt/rubric 配对漏；`SnapshotSize` accessor 给 cache idempotency 测试用；3 tests。
- 2.8 `perf_test.go`：`TestStartupBudget` < 1s、`TestResolveP95Budget` 1000 samples P95 ≤ 5ms、`BenchmarkResolve` ~235 ns/op。
- 2.9 边界 grep red-line：`! grep -rE "AIClient|aiclient\\.|metric\\.Counter|secret\\.|Secret\\.|os\\.Getenv" backend/internal/ai/registry/` 0 行命中。

### 关联 Commit

- `feat(prompt-rubric-registry): land f3 registry go package and runtime cache`

### 备注

Auto-committed by /tdd phase-commit, plan: prompt-rubric-registry/001-baseline

Phase 2 完成后：targetjob/cmd/api 在 Phase 3 可以替换 `StaticPromptRegistry` 为 `registry.NewRegistryClient` + `RegistryAdapter`，业务执行器拿到 `PromptResolution` 7 字段映射；`Judge` interface 已锁定供 plan 002 替换 `NotImplementedJudge`。
## 12:18 工作记录

### 完成事项

- F3 `prompt-rubric-registry/001-baseline` Phase 3 targetjob retire + cross-layer 对齐收口（6/6 items）。
- 3.1 `backend/internal/targetjob/prompt_registry.go` 全文重写：删除旧 in-package bridge 与 4 个 hardcoded version 常量；新增 `RegistryAdapter` + `NewRegistryAdapter`，映射 7 字段 + 校验 `featureKey` 一致；新增 `prompt_registry_test.go` 显式断言 7 字段映射 + nil client / unknown feature_key 拒绝。
- 3.2 `backend/cmd/api/main.go` DI wire：新增 `registryDirOrDefault` 工具，构造 `registry.NewRegistryClient` + `targetjob.NewRegistryAdapter`，与未来 C5/C6/C7 handler 共享同一 `*registry.Client`；`main_test.go` 通过新增 `repoConfigPromptsRubrics` helper + `ai.promptsDir`/`ai.rubricsDir` config keys 在测试环境指向 in-repo truth source；scenario test 使用本地 `staticTestPromptRegistry` shim 保持 HTTP 行为不变。
- 3.3 扩展 `backend/internal/targetjob/active_scope_negative_test.go`：在 forbidden token 列表追加 `StaticPromptRegistry` / 4 个 `defaultTargetImport*` 常量，确保 retire 之后任何 production 文件重新引入即失败。
- 3.4 新增 `backend/internal/targetjob/parse_executor_cross_layer_test.go`：使用真实 `registry.NewRegistryClient` + `RegistryAdapter`，从 `config/prompts/target.import.parse/v0.1.0*.yaml` 端到端读到 PromptResolution，断言 7 字段值 + 6-field provenance shape；`TestParseExecutorMetadataCarriesF3Triple` 通过 `captureAIClient` 验证 ParseExecutor 把 F3 三元组 + DataSourceVersion 注入到 AI metadata。
- 3.5 `openapi/fixtures/TargetJobs/getTargetJob.json` 4 个 provenance 节点统一 F3 baseline 坐标：`promptVersion=v0.1.0`、`rubricVersion=v0.1.0|not_applicable`、`modelId=model-profile:target.import.default`、`featureFlag=none`、`dataSourceVersion=registry.v1`/`prototype-baseline.v1`；`importTargetJob.json` 路径大小写与 operation matrix 一致，response shape 不变；`make validate-fixtures` 全绿（34 fixtures）。
- 3.6 Phase 3 退出 grep red-line：production-only `! grep -rE "StaticPromptRegistry|defaultTargetImport(Prompt|Rubric|ModelProfile|DataSource)" backend/ --include='*.go' --exclude='*_test.go'` 0 行命中；`go vet ./internal/targetjob ./cmd/api ./internal/ai/registry` 全过。完整 recursive grep 仅在 `_test.go` 与 scenario shim 注释中保留 retire identifier 字面量，作为 regression guard 而非残留 production 引用（与 2.9 同口径）。

### 关联 Commit

- `feat(prompt-rubric-registry): retire targetjob static bridge and align cross-layer provenance`

### 备注

Auto-committed by /tdd phase-commit, plan: prompt-rubric-registry/001-baseline

Phase 3 完成后：F3 RegistryClient 是 targetjob ParseExecutor 的唯一 prompt/rubric 解析入口；`backend/internal/targetjob/StaticPromptRegistry` + 4 个 `defaultTargetImport*` 常量从 production 退役；TargetJobs OpenAPI fixtures 与 B2 GenerationProvenance 6 字段、F3 baseline coordinates 对齐；Phase 4 可以接管 B1/B4/A3 provenance contract remediation 与 DB seed migration。
## 12:49 工作记录

### 完成事项

- F3 `prompt-rubric-registry/001-baseline` Phase 4 上游 provenance contract remediation + DB seed 收口（8/8 items）。
- 4.1 B1 vocabulary：`shared/conventions.yaml` 与 `backend/internal/shared/ai/vocabulary.go` 在既有 `feature_flag` / `data_source_version` 旁追加 `feature_key`（FieldFeatureKey + AllFieldNames + IsFieldName allowlist 同步），`shared/fixtures/conventions-parity.json` 通过 `make codegen-conventions` 跨语言对齐。
- 4.2 B4 `ai_task_runs` schema：直接修订 `migrations/000001_create_baseline.up.sql` 增加三个 typed columns (`feature_key not null` / `feature_flag not null default 'none'` / `data_source_version not null default 'not_applicable'`)；`scripts/lint/migrations_lint.py` 把 `ai_task_runs` 加入 FEATURE_KEY allowlist 并新增 per-table required-fragment map，`scripts/lint/migrations_lint_test.py` 把 negative fixture 改为 `users`（保留 allowlist 边界守护）。
- 4.3 A3 metadata + writer：`aiclient.CallMetadata` 增加 `FeatureFlag`；`aiclient.AICallMeta` / `aiclient.AITaskRunRow` / `observability/decorator.go` 三处显式承载 `FeatureKey` / `FeatureFlag` / `DataSourceVersion`；`metabuilder.merge` 把 `FeatureFlag` 默认 `none`、`DataSourceVersion` 默认 `not_applicable`，并在 success meta 缺 `FeatureKey` 时返回错误（fail-closed）。
- 4.4 + 4.5 Seed migration：新增 `migrations/000002_seed_baseline_prompt_rubric_versions.{up,down}.sql`；up 写 20 prompt + 20 rubric rows（10 feature_keys × 2 languages，确定性 uuid5），dollar-quoted body + `ON CONFLICT DO NOTHING` 保证 idempotent；down `DELETE WHERE version='v0.1.0' AND feature_key IN (...)` 限制范围。
- 4.6 prompt_lint cross-file template_hash drift 在 Phase 1.4 已实现，本 phase 把 seed migration 喂给 lint 后命中匹配（20 hashes 全过）。
- 4.7 `backend/internal/ai/registry/db_integration_test.go`：在 repo 没有 dockertest harness 的现实下，落地为 static SQL parse + cross-file `template_hash` 对照测试；count + feature_key 集合 + hash 匹配 3 个断言全过。dockertest 升级标记为后续 P1。
- 4.8 `decorator_test.go::TestDecorator_SuccessIncrementsRunsAndLogsCompleted` 追加 6 字段 cross-layer assertion（`FeatureKey / FeatureFlag / DataSourceVersion / PromptVersion / RubricVersion / ModelProfileName`）；`samplePayload` 注入 `FeatureFlag=none` + `DataSourceVersion=registry.v1` 让 production decorator 路径全程跑通；F1 metric label 集合保持 7 个原始 family，新 typed columns 不进入 metric labels。

### 关联 Commit

- `feat(prompt-rubric-registry): land f3 provenance contract and seed migration`

### 备注

Auto-committed by /tdd phase-commit, plan: prompt-rubric-registry/001-baseline

Phase 4 完成后：F3 prompt/rubric provenance 已经从 truth source（config/prompts、config/rubrics）→ DB（`prompt_versions`、`rubric_versions`、`ai_task_runs` 三个 typed columns）→ aiclient runtime（CallMetadata、AICallMeta、AITaskRunRow）→ observability writer 全链路对齐；Phase 5 进入收口 + sync-doc-index + retrospective 候选清单。`make migrate-check` 的 cmd/migrate `check` 子步骤仍需 `DATABASE_URL`（dev 环境无该变量时提前退出，与既有 dev 形态一致）；dockertest harness 升级保留为后续 P1，由 B4 后续 plan 接管。
## 12:59 工作记录

### 完成事项

- F3 `prompt-rubric-registry/001-baseline` Phase 5 收口完成（6/6 items）。整 plan 42/42 项闭环。
- 5.1 `make lint-ai-profile-coverage` 退出码 0；spec §3.1.1 列出的 10 个默认 `model_profile_name` 全部存在于 `config/ai-profiles.yaml`，本 plan 不修改任何 profile status。
- 5.2 F3-scoped verification 全过：`make lint-prompts && make lint-rubrics && make lint-prompts-hardcode && make lint-ai-profile-coverage && python3 scripts/lint/migrations_lint.py && python3 scripts/lint/conventions_drift.py && go test (registry/targetjob/cmd-api/aiclient/shared-ai/shared-types) -race && make validate-fixtures` 全绿；顶层 `make lint` 仍有历史 revive var-naming 警告（`backend/internal/ai/aiclient/providers/minimax_speech/`、`backend/internal/targetjob/handler.go targetJobId`），来自更早 commit，不属于本 plan 引入；`make migrate-check` 的 `cmd/migrate ... check` 段需 `DATABASE_URL`，dev 环境提前退出与既有形态一致。
- 5.3 INDEX / history sync：plans INDEX.md 把 001-baseline 自动从 Active 迁到 Completed（2026-05-09 完成日期），spec.md / docs/spec/INDEX.md 保持 v2.1 / 2026-05-09；附加修订 db-migrations-baseline D-15/C-12 与 shared-conventions-codified C-8 引用 F3 阶段 4.1/4.2 落地。
- 5.4 `python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 全绿（Zero drift detected）；context validator 退出码 0。
- 5.5 retrospective 候选清单已写入 plan §8.3：StaticPromptRegistry retire 节奏 / template_hash Go ↔ Python canonical 算法对齐 / B1/B4/A3 provenance remediation 对后续 F1/backend-practice plan 影响 / 未来跨层 plan 是否要求 ai_task_runs operation matrix 等，由 `/implement` 触发 `/retrospective` 收口。
- 5.6 11 AC 自检矩阵写入 plan §8.4：C-1~C-9、C-11 在本 plan 内通过命令证据；C-5（PostHog 灰度）/ C-10（≥50 题离线评估）按 spec §2.2 显式推到 002 / 003 plan，与 §1 scope 一致；plan/checklist Header 切到 `completed`，plans INDEX 同步迁移到 Completed 段。

### 关联 Commit

- `docs(prompt-rubric-registry): close 001-baseline lifecycle and record ac self-check`

### 备注

Auto-committed by /tdd phase-commit, plan: prompt-rubric-registry/001-baseline

Phase 5 完成后整 plan 42/42 闭环：truth source（10 feature_keys × 2 languages）+ 3 套 lint script + Go registry 包 + targetjob retire + RegistryAdapter + B1 vocabulary + B4 schema + A3 provenance writer + DB seed migration + 9 类替代验证 gate 全部对齐。`backend-practice` D-29 前置依赖解锁。下一步建议由 `/implement` 触发 `/retrospective --this`，把本次交付的非显然结论沉淀到 reports 台账。
## 13:22 工作记录

### 完成事项

- 扩展 `WorkspaceStartPractice.test.tsx` 至 10 tests：覆盖双步契约 happy path（无 plan→create+start、有 plan→跳过 create）、createPracticePlan 4xx error、startPracticeSession 502+retry idempotency、3 次失败 fallback CTA、nav params 完整字段（含 practiceGoal/hintUsed/hintCount）、hintsEnabled 由 practiceMode 二值派生、debrief_replay 负向断言、body schema 断言、StrictMode 调用次数
- 新增 `WorkspaceAuthGate.test.tsx`（5 tests）：未登录→auth_login、pendingAction.params 携带 interview context + autoStartPractice、敏感字段 0 泄漏、已登录正常 startPractice、pendingLabel 匹配 i18n
- 在 `WorkspaceScreen.tsx` 接线 `useRequestAuth` auth gate：未登录时调用 `requestAuth({type:"start_practice", route:"workspace", params:{...autoStartPractice:1}})`
- 新增 `WorkspaceHandoff.test.tsx`（6 tests）：CompanyIntelEmbed nav handoff、getFeedbackReport 0 调用、EmptyHistory placeholder、点击不触发 report nav、空态 CTA→home、缺简历 CTA→resume_versions?flow=create
- 新增 `tests/pixel-parity/workspace.spec.ts`（9 tests × desktop+mobile 2 viewports）：DOM 锚点、bounding box、dark mode+customAccent 视觉 diff、screenshot 基线、旧 testid 负向
- 创建 4 个 scenario 目录（P0.018-P0.021），各含 `README.md` + `scripts/{setup,trigger,verify,cleanup}.sh` + `data/seed-input.md` + `data/expected-outcome.md`
- 更新 `test/scenarios/e2e/INDEX.md` 追加 P0.018-P0.021 行
- 负向搜索 6 类（prototype helper import、旧 testid、旧 route alias、敏感数据泄漏、AI 直调、listResumes/getCompanyIntel/getFeedbackReport 运行时调用）全部 0 命中
- Regression P0.001-005 scenario PASS；P0.006 pixel parity 87/88
- Checklist 4.7-6.9 共 12 项全部完成

### 关联 Commit

- `feat(workspace): add auth gate, comprehensive tests, pixel parity, and scenario assets`

### 备注

本轮 `/implement` → `/tdd` 会话完成 workspace plan P0 主路径闭环。全量验证：65 files / 418 tests PASS，typecheck 0 errors，build PASS。BDD-Gate P0.018-P0.021 全部 setup→trigger→verify→cleanup PASS。剩余 P0.006 pixel parity mobile 截图基线需 `--update-snapshots` 刷新。
## 14:31 工作记录

### 完成事项

- 收口 `prompt-rubric-registry/001-baseline` Phase 6 L2 remediation：`ParseExecutor` 使用 A3 `AICallMeta.ModelID` 写入 `GenerationProvenance.modelId`，并向 A3 metadata 显式传递 `FeatureFlag` 与 B4 `TaskRun` context。
- 对齐 TargetJobs fixture provenance contract：`getTargetJob.json` 改用 provider-neutral `fixture-model:target-import-parse`，`validate_fixtures.py` 与测试允许 `model-profile:*` / `fixture-model:*` 两类中立 id，同时继续拒绝 vendor/model token。
- 修订 plan/checklist 到 v1.2，追加 Phase 6 evidence；新增 BUG-0030 与 L2 remediation 复盘报告，并同步 `docs/bugs/INDEX.md`、`docs/reports/INDEX.md`、`plans/INDEX.md`。
- 提交前验证通过：`go test ./internal/targetjob -count=1 -race`、`go test ./cmd/api -count=1 -race`、`python3 -m pytest scripts/lint/validate_fixtures_test.py -q`、`make validate-fixtures`、context validator、`sync-doc-index --check`、`git diff --check`。

### 关联 Commit

- `fix(prompt-rubric-registry): remediate provenance L2 findings`

### 备注

本次变更跨 `backend/`、`openapi/`、`scripts/` 与 `docs/`，但同属 BUG-0030 / L2 remediation 单一逻辑边界，按单 commit 收口。`make migrate-check` live DB 子步骤仍需 `DATABASE_URL`，已在 plan/checklist、BUG-0030 与复盘报告中记录为 blocker，未记为绿色 DB gate。
## 14:47 工作记录

### 完成事项

- 修复 `frontend-workspace-and-practice/001-workspace-and-interview-context` L2 复核发现的运行时漂移：`App.tsx` 从 carry route params 自动 hydrate `InterviewContext`，离开 carry routes 时清空上下文。
- 正式接线 `WorkspaceScreen` 的 Plan Switcher / Resume Picker modal、`getPracticePlan` ready / archived / 404 refresh 与 replacement start flow，并补齐 `autoStartPractice=1` 登录恢复后自动开始面试。
- 加固 workspace 集成测试、auth gate、startPractice、full workspace/modal pixel parity，以及 P0.018-P0.021 场景脚本强断言。
- 收口 plan/checklist/BDD checklist 到 `v1.1 completed`，新增 BUG-0031 与 L2 remediation 复盘报告，并同步相关 INDEX。

### 关联 Commit

- `fix(frontend-workspace): remediate workspace L2 findings`

### 备注

验证证据沿用本次 L2 修复会话已记录的 gate：`pnpm --filter @easyinterview/frontend test` 66 files / 423 tests PASS，`pnpm --filter @easyinterview/frontend build` PASS，`pnpm --filter @easyinterview/frontend test:pixel-parity` 96/96 PASS，P0.018-P0.021 scenarios 均 setup→trigger→verify→cleanup PASS，`make validate-fixtures` / `make docs-check` / context validator / `git diff --check` PASS。
## 15:00 工作记录

### 完成事项

- 修订 `plan-code-review` skill：当 plan/checklist/report 引用 `go test ... -run TestX` focused gate 时，L2 review 必须确认至少执行到目标测试，不能把 `[no tests to run]` 或零匹配 `-run` 当成 PASS。
- 补充 `plan-code-review` contract test，固化 `go test -list` / source search / no-op gate failure 规则。
- 按 TDD 方式验证规则：新增 contract test 先红，更新 skill 后 `python3 -m pytest .agent-skills/plan-code-review/scripts/test_plan_code_review_skill.py -q` 通过（8 passed），`git diff --check` 通过。

### 关联 Commit

- `fix(skills): reject no-op go test run evidence`

### 备注

本次是 BUG-0030 复盘建议的流程资产修复，范围仅限 `.agent-skills/plan-code-review/`。
## 16:04 工作记录

### 完成事项

- 修复 workspace review follow-up：新增 server-bound ID 归一化，synthetic `plan-*` / `resume-unbound` / invalid UUID 不再进入 generated client path/body。
- 加固 `getTargetJob` 404/5xx recovery 与 target 切换竞态：workspace 不再用 stale completion 覆盖新选择，missing/deleted target 进入 empty recovery。
- 修复 workspace 英文 UI 派生标签本地化：JD block、round fallback、status/source/prep labels 均通过 `workspace.*` locale key 渲染。
- 收口 owner plan/checklist 到 `v1.2 completed`，新增 BUG-0032 与 follow-up review 交付复盘报告。
- 完成验证：focused Vitest 58 tests PASS、`typecheck` PASS、frontend full test 67 files / 432 tests PASS、frontend build PASS、`make docs-check` PASS、`git diff --check` PASS。

### 关联 Commit

- `fix(frontend-workspace): harden workspace review follow-up`

### 备注

本 commit 采用单一 bugfix 边界：`frontend/` 代码修复与 `docs/` 中对应 plan、Bug、report、work-journal 收口一起提交。
## 16:20 工作记录

### 完成事项

- enrichMeta 补充 FeatureKey、FeatureFlag、DataSourceVersion 默认值：当 meta 为空时从 callMeta 回填；FeatureFlag 无来源时默认 "none"，DataSourceVersion 无来源时默认 "not_applicable"
- 加强 ai_task_runs 失败行覆盖：TestDecorator_PreDispatchFailureUsesResolvedProfileLabels 和 TestDecorator_StreamErrorUsesResolvedProfileLabels 两个用例增加对 memTaskRunWriter 行中 FeatureKey / FeatureFlag / DataSourceVersion 的断言，防止失败路径丢失 payload provenance

### 关联 Commit

- `feat(observability): enrich ai call metadata with provenance defaults`

### 备注

prompt-rubric-registry 001-baseline L2 remediation 拆分提交 1/2。
## 16:25 工作记录

### 完成事项

- target.import.parse 提示词全面升级：将旧 schema 键 (role_title, required_skills, responsibilities, language_signals, parse_confidence, seniority) 替换为新 schema (coreThemes, interviewHypotheses, strengths, gaps, riskSignals, requirements with kind/label/description/evidenceLevel)
- parse_executor 中 source URL 处理：fetchURLSnapshot 返回 urlfetch.FetchResult（含 SanitizedURL），通过 {{jd_source_url}} 占位符注入提示词；buildPromptMessages 增加 sourceURL 参数
- 新增跨层测试 TestTargetImportPromptMatchesParseResponseSchema：验证提示词包含新 schema 关键字且不含旧版遗留键
- TestParseExecutor_URLFetchBodyIsPersistedAndParsed 新增断言：验证提示词包含 sanitized source URL 且不含 token 或未解析占位符
- 同步更新 config/prompts 四文件（en.md, en.yaml, md, yaml）及变更 template_hash
- 更新 seed 迁移 000002：en 和 multi 两语言提示词正文与 hash 更新

### 关联 Commit

- `feat(prompt-rubric-registry): upgrade target.import.parse prompt schema and inject source URL`

### 备注

prompt-rubric-registry 001-baseline L2 remediation 拆分提交 2/2。
## 16:59 工作记录

### 完成事项

- 修复 checkpoint review 发现的 workspace target 错误分类：`getTargetJob` 缺失/404 继续进入 JD empty recovery，5xx/network 错误进入可重试错误态，并补充 5xx retry UI 回归测试。
- 修正 TargetJobs `getTargetJob` fixture 的 `with-rounds` provenance，补齐 `5xx` scenario，并清理 P0.020 seed trailing whitespace。
- 加固 observability `AITaskRunRowFromMeta`：入库前要求 `feature_key` 非空，并为空 `feature_flag` / `data_source_version` 补 `none` / `not_applicable` 哨兵值。
- 完成验证：workspace Vitest 13 files / 85 tests PASS，frontend `typecheck` PASS，相关 Go 包 PASS，`make validate-fixtures` PASS，`git diff --check` PASS。

### 关联 Commit

- `fix(review): address workspace and provenance findings`

### 备注

本次为 checkpoint review fix 单一提交边界，覆盖 `frontend/`、`backend/`、`openapi/` 与场景 seed 的同一组 review findings。
## 19:28 工作记录

### 完成事项

- 通过 `/design` 与用户对齐 backend-practice 第一个 plan 的 Q1 + Q2 trade-off，固化新决策 D-30 到 `docs/spec/backend-practice/spec.md` v1.4：(a) Phase 0 跨 spec 修订采用 integrator 模式（plan 001 直接修订 B1 `shared/conventions.yaml` / B2 `openapi/openapi.yaml` / B3 generated event refs / B4 migrations 编码真理源，并在四个 owner spec 的 history / Header 同步追加授权记录与版本号），不再为 D-21 / D-26 / D-27 各派 sibling owner spec plan；(b) D-27 idempotency 存储载体收敛为 shared `idempotency_records` 表（含 `domain` / `operation` namespace 字段），由 plan 001 Phase 0 引入并设计为可被 backend-targetjob / backend-review / 自身 002 等未来 backend domain 复用。
- 同步微调 D-27 wording 指向 D-30，并在 §7 把第一个 plan 链接化；§4.4 把 F1 metric 字典口径改为 practice-specific business metrics + AI 调用复用 A3 已登记的 7 个 `ai_task_*` family。
- 派生 plan `001-plan-and-session-orchestration`：新增 `plan.md` / `checklist.md` / `context.yaml` / `test-plan.md` / `test-checklist.md` / `bdd-plan.md` / `bdd-checklist.md` 7 文件；范围覆盖 `createPracticePlan(goal=baseline)` / `getPracticePlan` / `startPracticeSession` reservation 三段式 / `getPracticeSession` 4 个 OpenAPI Practice operation 与对应 backend handler / service / store / Phase 0 跨 spec 修订。
- 更新 `docs/spec/backend-practice/history.md` v1.4 修订记录、`docs/spec/backend-practice/plans/INDEX.md` 把 001 列入 Active、`docs/spec/INDEX.md` 主表的 backend-practice 行升级到 1.4 / 2026-05-09。

### 关联 Commit

- `docs(backend-practice): derive orchestration plan and bump spec to v1.4`

### 备注

仅文档变更（spec / plan / checklist / test-plan / test-checklist / bdd-plan / bdd-checklist / context / history / INDEX），无代码改动。`/design` 已结晶 D-30 决策；下一步建议进入 `/implement docs/spec/backend-practice/plans/001-plan-and-session-orchestration` 启动 Phase 0 跨 spec 修订（B1/B2/B3/B4 编码真理源 + 四个 owner spec history append + Header bump）。同会话另一组 frontend-home-job-picks-and-parse plan 002 的设计结晶将作为独立 commit 拆分。
## 19:30 工作记录

### 完成事项

- 启动 frontend-home-job-picks-and-parse 第二个 plan：把 `docs/spec/frontend-home-job-picks-and-parse/spec.md` v1.1 → v1.2，§2.1 In Scope 中 jd_match 屏从「P1 placeholder shell」升级为「plan 001 placeholder + plan 002 完整三 tab 业务」；§2.2 Out of Scope 改写为剥离真实 backend handler / agent scan / 真实联网搜索 / 候选池抓取 / market signals 计算，明确 `backend-jobs-recommendations` 为后续承接 subspec。
- 重写决策 D-1 为「契约先行 + frontend fixture 消费」模式（与 D-2 一致），并新增 5 条决策：D-8（jd_match → parse 反向数据流仅锁定出口）、D-9（Watchlist + Saved Searches 服务端持久化）、D-10（Agent scan 状态来源 polling）、D-11（jd_match 隐私红线扩展）、D-12（Search loading 单一加载文案，与 ui-design 真理源对齐）。
- 在 §5 模块边界拆出 frontend 三 tab + 未来 backend 接管行；§6 新增验收标准 C-12 ~ C-16，覆盖 Recommended + Profile chip + AGENT 状态、JobMatchCard 详情 4 button 闭环、Search 自然语言搜索 + savedSearches + filter + 单一加载文案 + failure、Auth pendingAction 扩展、Watchlist + Market signals + chevron handoff。
- 派生 plan `002-jd-match-recommendations`：新增 `plan.md` / `checklist.md` / `context.yaml` / `bdd-plan.md` / `bdd-checklist.md` 5 文件；范围覆盖 OpenAPI `JobMatch` tag + 12 operationId（`getJobMatchProfile` / `getAgentScanStatus` / `listJobRecommendations` / `getJobRecommendation` / `addToWatchlist` / `removeFromWatchlist` / `markJobNotRelevant` / `searchJobs` / `listSavedSearches` / `createSavedSearch` / `listWatchlist` / `getMarketSignals`）+ fixture-backed transport + ui-design 三 tab 源级复刻 + 5 条 BDD 场景。
- 同步更新 `docs/spec/frontend-home-job-picks-and-parse/history.md` v1.2 修订记录、`plans/INDEX.md` 把 002 列入 Active 并修复 `## 3` 重复段落、`docs/spec/INDEX.md` 主表的 frontend-home-job-picks-and-parse 行升级到 1.2 / 2026-05-09。

### 关联 Commit

- `docs(frontend-home): derive jd_match plan and bump spec to v1.2`

### 备注

仅文档变更（spec / plan / checklist / bdd-plan / bdd-checklist / context / history / INDEX），无代码改动。`/design` 已结晶 D-1 重写与 D-8 ~ D-12 五条新决策；下一步建议进入 `/implement docs/spec/frontend-home-job-picks-and-parse/plans/002-jd-match-recommendations` 启动 Phase 0 OpenAPI `JobMatch` tag 扩展（B2 owner truth source 修订 + 12 operationId + fixture）。本 commit 与同会话 backend-practice plan 001 派生作为独立两次提交，避免跨主题混提。
## 20:37 工作记录

### 完成事项

- 完成 backend-practice/001 Phase 0 contract preflight：B1 `PracticeMode` 二值化、practice not-found 错误码、B2 OpenAPI / baseline / generated client-server、B3 PracticeMode event ref、B4 migration baseline 与 owner spec history/Header 全部同步。
- 新增 shared `idempotency_records` 表迁移、B4 enum-source / migration lint / Go SQL contract test，并用临时 Postgres 数据库验证 `make migrate-check` 与 `make migrate-status`。
- 新增 F3 baseline preflight gate 和 backend-practice legacy-negative gate，确认 `prompt-rubric-registry/001-baseline` 已 completed，三个 practice feature_key 可 Resolve 到完整三元组，当前 active code/spec/test 不再保留 removed legacy mode literal。
- 修订 frontend workspace 负向测试、场景 verify 脚本、OpenAPI fixture projection 与 codegen 示例，避免旧 practiceMode 字面继续进入 active surfaces。

### 关联 Commit

- `feat(backend-practice): complete phase 0 contract preflight`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-practice/001-plan-and-session-orchestration
## 21:46 工作记录

### 完成事项

- 完成 backend-practice/001 Phase 2 错误路径与 idempotency 完备性：AI timeout / invalid output / secret missing / fallback exhausted 脱敏映射，首题失败后 session failed + idempotency failed_retryable/failed_terminal，retry 后可重新进入 pending 并成功升级。
- 加固 startPracticeSession 幂等矩阵：同 key fingerprint mismatch 返回 `PRACTICE_SESSION_CONFLICT` 且不泄露首次资源，pending 单执行者冲突、跨用户同 key 隔离、same plan 多 key active session 冲突均有 repository / middleware / cmd-api 场景覆盖。
- 新增 migration `000004_practice_session_active_unique` partial unique index，保证 `(user_id, plan_id)` 在 `queued/running` 状态最多一个 active session。
- 补齐 `E2E.P0.024` 与 `E2E.P0.025` 场景资产、脚本、索引和 BDD checklist evidence。
- 完成验证：相关 Go 包 PASS、`make validate-fixtures` PASS、`make lint-events` PASS、`make docs-check` PASS、migration lint PASS、context validation PASS、两个 Phase 2 BDD 脚本链路 PASS。

### 关联 Commit

- `feat(backend-practice): complete phase 2 idempotency recovery`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-practice/001-plan-and-session-orchestration
## 21:30 工作记录

### 完成事项

- 完成 backend-practice/001 Phase 1 practice plan 与 session 主流程：新增 shared idempotency middleware、practice handler/service/store、create/get plan、start/get session、outbox `practice.session.started` payload builder，并接入 `cmd/api` runtime。
- 补齐 `E2E.P0.022` 与 `E2E.P0.023` 场景资产、`cmd/api` HTTP 场景测试、套件 INDEX 与 BDD checklist evidence，覆盖 baseline plan 创建读取、idempotency replay、cross-user 404、同步首题、session event/outbox 与 AI 事务外调用。
- 完成验证：相关 Go 包 PASS、`make validate-fixtures` PASS、`make lint-events` PASS、`make docs-check` PASS、context validation PASS、两个 BDD 脚本链路 PASS。

### 关联 Commit

- `feat(backend-practice): complete phase 1 practice orchestration`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-practice/001-plan-and-session-orchestration
