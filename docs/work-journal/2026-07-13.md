# 2026-07-13 工作日志

## 18:47 工作记录

### 完成事项

- 修复面试报告从冻结上下文到报告输出的证据链：补齐直接语义、维度/证据/问题/行动交叉约束与 fail-closed 校验，并保留完整 prompt 示例输出；示例改为成对合成输入/输出并明确禁止复用示例事实、维度、结论和措辞。
- 落地方案 A-200：`nextActions[*].label` 以 200 code points 作为畸形输出熔断，以英文 24 whitespace words / 中文 64 Unicode code points 作为用户可读边界；前后端、OpenAPI、fixture、静态原型和 390px/desktop UI 同步，长标签完整换行且不截断。
- 将报告生成重试限定为单次用户动作内 initial + 最多 3 次，退避为 10s/20s/40s；调用返回即销毁计数，新的用户动作从 attempt 1 重新开始，`async_jobs.attempts` 只表示基础设施投递尝试，不持久化产品重试计数。
- P0.099 当前运行 `e2e-p0-099-20260713T095144Z-12381` 通过中英文 ready 与真实 generating 的 desktop/mobile 六截图验收；P0.100 当前 final-prompt 运行 `e2e-p0-100-20260713T101214Z-59381` 达到机械格式 9/9、语义 judge 8/9、固定五类 4/5，严格 11/11 + blind-review 场景仍诚实记录为 FAIL。
- 建立 BUG-0164、BUG-0165、BUG-0166 与报告语义可靠性评估；通过 `make test`（Python 536 + 4517 subtests、全部 Go packages、frontend 112 files / 795 tests）、frontend build、OpenAPI 37 operations / 37 fixtures / 0 findings、Prism 7/7 byte-equal、P0.058、migration、prompt/rubric/offline eval、context、docs/index 与 diff 门禁。

### 关联 Commit

- `fix(report): ground report semantics and reliability (BUG-0164, BUG-0165, BUG-0166)`

### 备注

- 产品验收按用户确认口径区分确定性与概率性：格式、限制和跨字段契约必须 100% 正确；语义真实性与建议可靠性以固定代表类别至少 4/5 为合理置信度，不把严格 P0.100 的失败改写成 PASS。
- 严格 P0.100 保留为后续独立诊断门禁，本提交不增加运行时 judge 或文本启发式，也不扩展报告目标之外的功能。

## 21:15 工作记录

### 完成事项

- 原地重开并校对 Home/Parse、Practice、Report 及其契约 owner，固定用户消息立即展示、请求期间输入禁用与思考动画、失败消息行内重试、刷新后由服务端四态回复状态恢复的方案 A。
- 将 Home JD intake 收敛为只粘贴文本，锁定 `{rawText,targetLanguage,resumeId}`、`raw_jd_text` 唯一事实源和未上线 OpenAPI freeze correction；移除 URL、JD 文件、手工表单、来源枚举及其运行时兼容面，同时保留 Resume/隐私上传。
- 固定 Parse 内部 model/rubric/provenance/latency 调试信息与 Report 会话 UUID 的负向验收，并补齐 desktop/mobile DOM、computed style、bbox、viewport 和截图 manifest 闭环。
- 统一 Practice role-discriminated message、持久化 `replyStatus`、typed `ApiClientError.apiError`、failure fixture、operation matrix、BDD/TDD gate 与 owner context；完成 INDEX 同步和 deep L1 reconcile。
- 通过 16 个 plan context validator、`sync-doc-index --check`、`make docs-check`、`make lint-core-loop-pruning-surface` 与 `git diff --check`。

### 关联 Commit

- `docs(interview): define resilient turns and paste-only intake`

### 备注

Auto-committed by /tdd phase-commit, plan: interview-turn-ux.
