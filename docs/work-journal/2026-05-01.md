# 2026-05-01 工作日志

## 17:12 工作记录

### 完成事项

- 新增 `docs/ui-design/` 目录 13 份设计文档：架构总览、用户流程、模块地图、认证入口、岗位工作台、模拟面试与报告、报告仪表盘、简历模块、多 JD/多简历、首次简历引导、真实复盘、用户画像与设置、移除模块清单。
- 同步更新 `docs/ui-design/INDEX.md` 列出所有 active 文档及版本/更新日期。
- 在 `docs/README.md` §1 文档导航中追加 `UI 文档` 与 `UI Design 文档` 两条入口，方便从根索引进入。

### 关联 Commit

- `docs(ui-design): land ui design document set`

### 备注

UI Design 文档为 UI 原型修订的设计真理源；后续 UI 代码改动与复盘报告均引用该目录。

## 17:20 工作记录

### 完成事项

- 按 `docs/ui-design` 收敛流程修订 `ui-design` 静态原型：
  - 首页 (`screen-home.jsx`)、HTML 入口 (`index.html`)、应用骨架 (`app.jsx`) 补齐岗位推荐、最近模拟、URL/上传弹窗等入口。
  - 工作台 (`screen-workspace.jsx`)、JD 匹配 (`screen-jd-match.jsx`)、公司情报 (`screen-company-intel.jsx`) 与练习 (`screen-practice.jsx`) 对齐目标流程。
  - 报告页 (`screen-report.jsx`) 增补 session-scoped 二级详情与题目回顾视图。
  - P0/P1 屏幕集合 (`screens-p0-complete.jsx`、`screens-p1-depth.jsx`)、`primitives.jsx`、`data.jsx` 同步交互结构与数据样例。
- 新增独立屏幕：`screen-auth.jsx`（默认进首页后的登录/注册/验证/重置）、`screen-profile.jsx`（用户画像与设置）。
- 落地 17 张验证截图，覆盖首页、上传/URL 弹窗、近期模拟、JD 匹配、模拟面试设置、轮次轨道、工作台（current/react/mobile）、报告（list/detail/session-context/react）、复盘、简历创建。
- 删除已迁出 UI Design 设计文档的旧 spec 资料，避免与新设计真理源冲突。

### 关联 Commit

- `feat(ui): align prototype with ui design`

### 备注

静态原型的 hash 路由仅在挂载时解析，截图验证需配合 query 强制整页重载。

## 17:25 工作记录

### 完成事项

- 落地 `docs/reports/2026-05-01-ui-design-prototype-assessment.md` 复盘报告：覆盖范围、成功证据、阻点痛点、根因归类、流程改进建议与下一轮优先级。
- 在 `docs/reports/INDEX.md` 顶部新增 `2026-05` 月份分组并登记本次复盘条目（类型 `#assessment`）。

### 关联 Commit

- `docs(reports): land ui design prototype retrospective`

### 备注

报告引用本次 UI 原型与 ui-design 设计稿的对照证据，包括 `git diff --check`、`curl -I` 与 CDP 截图验证；流程改进建议已留作后续 spec-plan 与 skill 优化输入。

## 21:40 工作记录

### 完成事项

- 修正 `ui-design/run.sh` 默认入口文件名为 `index.html`，与仓库真实入口一致。
- 同步更新脚本顶部用法注释中的默认入口示例，避免引用旧大小写名称。

### 关联 Commit

- `fix(ui-runner): correct default entry filename casing`

### 备注

之前默认值导致 `./run.sh --no-open` 找不到入口；本次修正后默认启动可直接命中 `index.html`。

## 21:42 工作记录

### 完成事项

- 重构 `primitives.jsx` 主题系统：将 `EI_THEMES` 改为 `themeKey × {light, dark}` 的二维结构，新增 forest/ocean 等调色板，并落地字体预设 `EI_FONT_PRESETS` 与自定义 accent 种子。
- 在 `app.jsx` 顶栏与设置页接入主题/字体切换：URL 与 hash 参数同步 `theme/customAccent/fontPreset/serifFamily/sansFamily`，TopBar 增加主题菜单与自定义 accent 种子点。
- 同步 `canvas.html` 画板：`Screen` 接受 theme/customAccent/fontPreset/role 透传，并精简组件结构。
- 屏幕原型交互补全：
  - `screen-auth.jsx` 抽出 `useResendCountdown` 复用倒计时。
  - `screen-jd-match.jsx` 增加保存/隐藏/筛选状态与装饰列表。
  - `screens-p0-complete.jsx` 把 `SettingsScreen / SettingsProfile` 接入字体预设并补充导出与头像 toast 提示。
  - `screens-p1-depth.jsx`、`screens-rest.jsx`、`screens-p2.jsx`、`screens-completion.jsx`、`screen-report.jsx` 加入复制致谢信、采纳/跳过建议、报告交互等行为。
- `index.html` 更新各 babel 脚本的 `?v=` 缓存戳，对齐 `font-presets` 与 `interactions` 这一批次。

### 关联 Commit

- `feat(ui): add theme palettes, font presets and prototype interactions`

### 备注

主题与 dark/light 模式现在完全正交；字体预设以原子方式更新 `fontPreset/serifFamily/sansFamily`，避免半状态。

## 21:45 工作记录

### 完成事项

- 落地 `docs/reports/2026-05-01-ui-canvas-runner-path-fix-assessment.md` 复盘报告：覆盖修复范围、HTTP/启动验证证据、阻点与根因（路径不一致 / 文件名大小写笔误 / change-intake 误路由 / 浏览器验证局限）以及流程改进建议。
- 在 `docs/reports/INDEX.md` 2026-05 月份顶部追加该报告条目（类型 `#assessment`），保持时间倒序。

### 关联 Commit

- `docs(reports): land ui canvas runner path fix retrospective`

### 备注

复盘建议补一条 UI runner smoke 校验（README/test）与 change-intake 检索映射（spec-plan），暂未单独建 BUG 记录。
