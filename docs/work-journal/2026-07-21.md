## 16:01 工作记录

### 完成事项

- 原地重开 backend-practice 与 prompt-rubric-registry owner plans，明确面试官招聘方身份只由 TargetJob/round 提供，Resume 公司与 assistant 历史自称不得成为面试官雇主来源。
- 发布 `practice.session.chat/v0.3.0` prompt/rubric exact pair，增加 `role_identity` 维度及四类身份冲突/纠偏评测，并保留 v0.2 精确回滚坐标。
- 通过显式 owner markers、registry/cache/runtime tests 与 `000023` migration 原子激活 v0.3；disposable PostgreSQL `22 -> 23 -> 22 -> 23` 验证通过且 report v0.2 未受影响。
- 真实 DeepSeek provider 5/5 completion 未出现 Resume 公司身份代入；live judge JSON 解析失败按 unavailable 记录，未虚报通过。
- 完成根测试、构建、lint、离线评测、后端 redeploy/API 探测、Bug 建档和交付复盘。

### 关联 Commit

- `fix(practice): ground interviewer employer identity (BUG-0197)`

### 备注

Auto-committed by /tdd phase-commit, plan: backend-practice/001-plan-and-session-orchestration

## 16:38 工作记录

### 完成事项

- 原地重开 Home JD parse 与 Resume Workshop 两个 owner plans，统一规定 queued/processing 解析等待态不提供内联“返回 / Back”，失败态恢复动作保持。
- 按 TDD 仅删除 `ParseScreen` 与 `ResumeDetailView` 等待分支的 `AsyncTransitionScene.action`，shared component、轮询、ready replace、路由和 locale 不变。
- 根 `make test` 通过 backend `626` 个测试/`4628` 个 subtests 与 frontend `137` 个文件/`1126` 个测试；typecheck、build、context、docs、INDEX、pruning 与 diff 门禁通过。
- 使用 `make dev-container-up` 重建真实容器前后端，并在 Chrome desktop/390px mobile 验证 JD 与简历等待态均无 action、无横向溢出、无 console issue。
- 评估后不新增 Bug 记录；新增 Parse Waiting Back Action Removal 交付复盘，记录运行拓扑误判与浏览器文件权限边界。

### 关联 Commit

- `fix(frontend): remove parsing wait back actions`

### 备注

Auto-committed by /tdd phase-commit, plan: frontend-home-job-picks-and-parse/001-home-jd-import-and-parse + frontend-resume-workshop/001-listing-routing-and-detail-readonly
