# 2026-04-30 工作日志

## 00:00 工作记录

### 完成事项

- 收紧 `aiclient.New` 的 fail-fast 矩阵以对齐 plan 4.1 / checklist 4.2：`AppEnv=test` 必须显式 `WithStubAllowed(true)` 才能在缺 gateway 配置时构造成功；`AppEnv != test` 任意路径缺 `GatewayBaseURL` 或 `GatewayAPIKey` 一律 `ErrMissingGatewayConfig`；`WithStubAllowed(true)` 在 non-test 路径不生效，绝不静默回退到 stub。
- 落地 `config_test.go` 8 个 case 完整覆盖矩阵：test+stub-allowed→OK / test+missing→FAIL / test+gateway-set→OK / production+missing→FAIL / production+stub-allowed-but-missing→FAIL / production+gateway→OK / staging+missing-api-key→FAIL / staging+missing-base-url→FAIL。
- 同步更新现有 stub 路由测试 (`aiclient_test.go`、`observability/decorator_test.go`) 加 `aiclient.WithStubAllowed(true)`，避免新规则误判已有用例。
- 落地 `backend/internal/ai/aiclient/README.md` 包级 README：覆盖 zero-vendor-SDK 红线、AICallMeta ownership、隐私红线引用 `observability/privacy_test.go`、stub 激活矩阵表（5 行）、本地部署所需 env 变量 + smoke 验证步骤（`go test -tags smoke`，禁止嵌入真实 API key）、polling reloader 与 fsnotify fallback 升级路径、cmd/api / cmd/worker 接入示例代码。
- 验证：`go test ./internal/ai/aiclient/... -count=1` 全绿（5 packages）。

### 关联 Commit

- `feat(aiclient): fail-fast matrix 与包级 README`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-gateway-and-model-routing/001-aiclient-and-profile-bootstrap. Phase 4.3 README 中暂记录 polling reloader 为 plan §2.3 fsnotify fallback 路径，A4 secrets-and-config 后续推进 fsnotify 直依赖时可平滑切换 `profile.Loader` 实现而不触动公共 API。

## 00:08 工作记录

### 完成事项

- Phase 5.1 自检 spec C-1/C-2/C-3/C-4：`go test ./internal/ai/aiclient/... -count=1 -race` 全绿（5 packages，包含 race detector 跑 100 轮 reload + 8 readers）；`TestComplete_NormalChatCompletion` 通过 `httptest` 断言 `Authorization: Bearer test-key` + `Content-Type: application/json` + `X-Request-ID: req-1` 出站；`TestLoaderReloadConvergesUnderHotReloadSLA` 50ms cadence × 2s deadline 验证 ≤30s SLA。
- Phase 5.2 自检 spec C-5/C-6/C-7/C-9：`TestDecorator_AllSevenMetricFamiliesRegistered` + `TestDecorator_SuccessIncrementsRunsAndLogsCompleted` 验证 7 metric family 注册 + run/latency/token 增长 + ai_task_runs row + audit_events row（hash + length + profile）+ ai.task.completed 日志事件字段齐全；`TestPrivacy_NoPlaintextLeaksAnywhere` 4 sensitive token 全部不进 metric label / log / DB / audit；`TestDecorator_OutputSchemaInvalidEmitsAIOutputInvalid` 验证 validateOutput 失败返回 `AI_OUTPUT_INVALID` + `ai_output_validation_failures_total` +1 + `ai.output.validation_failed` 日志；`TestNew_FailFastMatrix` 5 个 FAIL case + 3 个 OK case 完整覆盖 fail-fast 矩阵。
- Phase 5.3 grep 红线：
  ```
  $ grep -RIn -E '"github.com/(sashabaranov/go-openai|openai/openai-go|anthropic[a-z-]*|cohere[a-z-]*|google/generative-ai-go)"' backend/
  (empty)
  $ grep -E 'openai-go|anthropic-sdk-go|cohere-go|generative-ai-go' backend/go.mod
  (empty)
  $ grep -RIn -E '(payload\.Messages|m\.Content|input\.Texts|resp\.Content|response\.Content)' backend/internal/ai/aiclient/observability/ backend/internal/ai/aiclient/providers/
  ```
  18 个匹配全部审查：`decorator.go:124` validateJSONShape（不写出）；`decorator.go:131,174,180` 通过 `buildAuditRow` 转 sha256+length；`decorator.go:318` `joinMessages` 仅作 hash 输入；`providers/openai_compatible/adapter.go:81,131,177,305` 构造 wire 请求 / token 长度统计；`providers/openai_compatible/contract_test.go:79,80` 测试断言；`providers/openai_compatible/mockserver/mockserver.go:220` mock `len(strings.Fields(m.Content))` token count；`providers/stub/stub.go:102,104,136,149,162,163` deterministic seed + token count。无任何明文落 log / metric label / DB write 路径。
- Phase 5.4 文档与 INDEX 同步：plan 与 checklist Header 切到 `状态: completed`、`更新日期: 2026-04-30`；`docs/spec/ai-gateway-and-model-routing/plans/INDEX.md` 把 001 从 §1 Active 移到 §2 Completed（完成日期 2026-04-30）；002 仍在 §3 Draft / Blocked。`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 输出 `All documents are in sync. Zero drift detected.`，根 `docs/spec/INDEX.md` 不动（spec 仍 active，plan 002 待 ADR-Q6 触发条件）。

### Handoff

A3 plan 001 已暴露以下 DI 入口供下游 owner 接入：

| Owner | DI 入口 | 用途 |
|-------|---------|------|
| F1 observability-stack | `observability.Registerer` 接口（位于 `backend/internal/ai/aiclient/observability/metrics.go`） | F1 实现适配器把 7 个 metric family 接入 `prometheus.DefaultRegisterer`；本 plan 提供 `InMemoryRegistry` 仅供单测 |
| F1 observability-stack | `observability.Logger` 接口 + `LogFields` allowlist | F1 接入结构化 logger，按 05-logging-standard.md §4.4 输出 4 类 ai.* 事件 |
| B4 db-migrations-baseline | `aiclient.AITaskRunWriter` 接口 + `AITaskRunRow` 类型 | B4 在 `ai_task_runs` 表 schema 落地后，由 C 域在自身 plan 把真实 store 注入 `observability.WithAITaskRunWriter` |
| Privacy/Audit owner | `aiclient.AuditEventWriter` 接口 + `AuditEventRow`+`AuditMetadata` 类型（白名单 5 字段） | 写 `audit_events.action='ai.call'` 行；新增字段必须先改 spec |
| F3 prompt-rubric-registry | `config/ai-profiles/*.yaml` 与 `aiclient.ModelProfile` schema | 真实 profile 内容由 F3 维护；本 plan 仅提供 2 份 fixture |
| A4 secrets-and-config | `aiclient.Config` + `New(cfg)` + `ErrMissingGatewayConfig` | A4 在 cmd/api / cmd/worker wire 时把 cfg 错误转 non-zero exit；本 plan 不创建或重写 entrypoint |
| C4-C7 / C9 / C11 / C14 业务域 | `aiclient.AIClient` 接口 + profile name 字符串 | 业务代码绝对零厂商 SDK 入侵 |

### B1 vocabulary 待切换点

- `backend/internal/ai/aiclient/meta.go` 中的 `TaskType` (chat/embed/stt) 当前是 A3 私有别名；B1 002 生成 `TaskType` 共享常量后，A3 应切换到 B1 import 并删除本地定义。
- `backend/internal/ai/aiclient/observability/logger.go` 中的 4 个事件名常量 (`ai.task.completed` 等) 当前 A3 自定义；若 B1 提供 cross-language event-name 常量，可切换。
- 7 个 metric family 名称同上：可在 F1 接入时统一拉到 cross-language 常量。

### Plan 002 状态

`002-tools-streaming-and-stt` 保持 `状态: draft` / `版本: 0.1`；`plans/INDEX.md` §3 Draft / Blocked 分组说明阻塞条件未变（需 ADR-Q6 / A3 spec 先打开 Tools / Streaming / STT 能力）。

### 关联 Commit

- `feat(aiclient): close out plan 001 verification + handoff`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-gateway-and-model-routing/001-aiclient-and-profile-bootstrap. 本提交是 plan 001 的最终收口，所有 17 个 checklist item（Phase 0 / 1 / 2 / 3 / 4 / 5）勾选完毕；spec C-1 / C-2 / C-3 / C-4 / C-5 / C-6 / C-7 / C-9 共 8 项 AC 均通过 targeted go test 验证，C-8 引用 engineering-roadmap/001-decompose-subspecs Phase 3.2 W1 review gate 不在本 plan 范畴。

## 00:14 工作记录

### 完成事项

- 落地 plan 001 交付复盘报告 `docs/reports/2026-04-30-ai-gateway-and-model-routing-001-aiclient-and-profile-bootstrap-assessment.md`，总结 5 个会话痛点、5 项根因归类（含 3 项无需仓库改动）、3 条针对 `/work-journal` / `/implement` / plan 模板的改进建议（4.1 medium / 4.2 low / 4.3 medium）。
- 在 `docs/reports/INDEX.md` §2026-04 顶端登记新报告。

### 关联 Commit

- `docs(reports): land ai-gateway-and-model-routing/001 交付复盘报告`

### 备注

`/retrospective` 不直接修改 skill / README / AGENTS.md / spec-plan，仅记录建议。

## 00:42 工作记录

### 完成事项

- 落地 secrets-and-config runtime config bootstrap：新增 `backend/internal/platform/config/` 与 `backend/internal/platform/featureflag/`，补齐 `cmd/api` / `cmd/worker` 启动入口骨架、runtime-config builder / handler、三层配置加载、redactor、validator、文件与 PostHog feature flag provider。
- 补齐仓库根配置资产与本地质量门禁：`.env.example`、`config/config.yaml` / `dev.yaml` / `staging.yaml` / `prod.yaml` / `feature-flags.yaml`、`config/README.md`、`make lint-config`、`getenv_boundary.go`、`env_dict.py`、pre-commit secret scan 与 gitleaks wrapper。
- 落地前端 `frontend/src/lib/runtime-config/` fetcher、类型与占位 hook，锁定 public runtime-config allowlist 与缓存行为。
- 验证：`go test ./internal/platform/... ./cmd/api ./cmd/worker`、`python3 -m pytest scripts/lint/env_dict_test.py`、`pnpm --dir frontend test src/lib/runtime-config/index.test.ts`、`make lint-config` 全部通过；本机未安装 gitleaks，`lint-secrets-pattern` 按脚本设计打印安装提示并跳过第二层扫描。

### 关联 Commit

- `feat(secrets-config): land runtime config bootstrap`

### 备注

按用户要求仅提交原拆分组 1；skill / governance workflow 相关改动保留未提交。

## 00:44 工作记录

### 完成事项

- 收口 `secrets-and-config/001-bootstrap` plan：`plan.md` 与 `checklist.md` Header 保持 `completed` / `v1.1` / `2026-04-30`，32/32 checklist 项完成，`plans/INDEX.md` 同步到 Completed 分组。
- 落地交付复盘报告 `docs/reports/2026-04-30-secrets-and-config-001-bootstrap-assessment.md`；`docs/reports/INDEX.md` 已存在对应 `secrets-and-config/001-bootstrap 交付复盘` 索引行。
- 验证：`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 输出 Header / INDEX zero drift；同轮复核实现组验证结果包括 backend Go tests、env_dict pytest、runtime-config vitest 与 `make lint-config`。

### 关联 Commit

- `docs(secrets-config): close bootstrap plan and assessment`

### 备注

按用户要求仅提交原拆分组 3；skill / governance workflow 相关改动保留未提交。

## 07:14 工作记录

### 完成事项

- 收口 `ai-gateway-and-model-routing/001-aiclient-and-profile-bootstrap` L2 code-review remediation：`openai_compatible` adapter 支持 root endpoint 与已含 `/v1` 的 `AI_GATEWAY_BASE_URL`，避免 `/v1/v1/...` 拼接。
- `observability` decorator 对 `OutputSchema` 增加基础 `type` / `required` / `properties` / `items` 校验，schema 不匹配时保持 `AI_OUTPUT_INVALID`、validation failure metric 与日志语义。
- `profile.Loader` 手写 schema 校验错误补 file path + line number，便于定位 profile 漂移；plan / checklist / plans INDEX 同步到 v1.2 completed。
- 验证：`go test ./internal/ai/aiclient/... -count=1` 通过；`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 通过，Header / INDEX zero drift。

### 关联 Commit

- `fix(aiclient): remediate L2 review findings`

### 备注

按用户确认的 3 commits 拆分提交，本记录对应拆分组 1。

## 07:15 工作记录

### 完成事项

- 收口 `secrets-and-config/001-bootstrap` L2 code-review remediation：新增 `config.LoadCanonical` 与 canonical env / secret bindings，让 `cmd/api` 与 `cmd/worker` 复用同一 P0 env dictionary。
- 修复 prod validation 漏校验 `AI_GATEWAY_BASE_URL`；worker prod + 完整 env 注入路径补 focused test，缺失 gateway base URL 路径补 fail-fast test。
- 修复 `scripts/lint/env_dict.py` code-side key 发现能力，把 binding map 中的 env-shaped 字面量纳入三方对齐检查，并补 pytest regression。
- 修复 runtime-config cold PostHog public flag projection：首次请求按 `FlagContext` evaluation，过滤 operator-only flags，同时保持业务 `FeatureFlagClient` 只暴露 D-4 两方法。
- 建档 `BUG-0002` 并同步 bugs INDEX；plan / checklist / plans INDEX 同步到 v1.2 completed。
- 验证：`go test ./internal/platform/... ./cmd/api ./cmd/worker -count=1`、`python3 -m pytest scripts/lint/env_dict_test.py ... -q`、`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 通过。

### 关联 Commit

- `fix(secrets-config): remediate L2 review findings`

### 备注

按用户确认的 3 commits 拆分提交，本记录对应拆分组 2。

## 07:15 工作记录

### 完成事项

- 在 `AGENTS.md` 与 `docs/spec/README.md` / `TEMPLATES.md` 中 codify TDD / BDD 质量门禁：code plan 必须声明 TDD 策略，feature behavior plan 必须维护 BDD 文件与 `BDD-Gate:`，内部计划不适用 BDD 时必须写明原因和替代验证 gate。
- 同步更新 `/create-doc`、`/design`、`/implement`、`/init-docs`、`/plan-review`、`/plan-code-review`、`/tdd` skill 指令与 plan-context contract，确保设计、创建文档、实施、审查与 TDD 入口都能识别相同质量门禁。
- 更新 init-docs spec 模板，补 conditional `test-plan.md` / `test-checklist.md` 与 BDD 文件规则，避免 context.yaml 留 dangling references。
- 补充 skill contract tests 覆盖 TDD / BDD gate 文案、quality gate classification、completed phase evidence review 与 context contract 边界。
- 验证：`python3 -m pytest .agent-skills/design/scripts/test_design_skill_contract.py .agent-skills/implement/scripts/test_execution_automation_docs_contract.py .agent-skills/implement/scripts/test_implement_skill_contract.py .agent-skills/init-docs/scripts/test_init_docs_skill_contract.py .agent-skills/plan-code-review/scripts/test_plan_code_review_skill.py .agent-skills/plan-review/scripts/test_plan_review_skill.py .agent-skills/tdd/scripts/test_tdd_skill_contract.py -q` 通过；`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 通过，Header / INDEX zero drift。

### 关联 Commit

- `docs(skills): codify TDD and BDD quality gates`

### 备注

按用户确认的 3 commits 拆分提交，本记录对应拆分组 3。

## 07:42 工作记录

### 完成事项

- 补齐 `shared-conventions-codified/002-codegen-pipeline` plan 的 TDD/BDD 质量门禁分类与 checklist 可执行验证断言，确认本内部 codegen/contract plan 不适用 BDD，并以 codegen / drift / parity / lint gate 替代。
- Phase 1 落地 AI shared vocabulary 真理源：`shared/conventions.yaml` 增加 `aiVocabulary.fields`，`scripts/lint/conventions_yaml.py` 校验必需字段、重复字段与 lower_snake_case。
- 扩展 B1 conventions generator 输出 `backend/internal/shared/ai/vocabulary.go` 与 `frontend/src/lib/conventions/ai.ts`，包含 AI field constants、all-fields 集合、validation helper 与 B1/A3/A4/B4/F1 owner boundary 注释；确认 AI meta 字段未进入 `errors/*`。
- 验证：`python3 -m pytest scripts/lint/conventions_yaml_test.py -q`、`python3 scripts/lint/conventions_yaml.py shared/conventions.yaml`、`go test ./cmd/codegen/conventions -run 'TestRunFromBytes_(GeneratesAllExpectedFiles|Idempotent)|TestRenderAIVocabulary_' -count=1`、`git diff --check` 均通过。

### 关联 Commit

- `feat(shared-conventions): ai shared vocabulary 真理源`

### 备注

Auto-committed by /tdd phase-commit, plan: shared-conventions-codified/002-codegen-pipeline

## 10:43 工作记录

### 完成事项

- Phase 3 落地 B3-owned `backend/cmd/codegen/events` 独立 generator，解析 `shared/events.yaml` / `shared/jobs.yaml` 并生成 Go、TS、JSON Schema、enum refs 与 baseline manifests。
- 生成 Go 端 `backend/internal/shared/events/` 与 `backend/internal/shared/jobs/`，TS 端 `frontend/src/lib/events/` 与 `frontend/src/lib/jobs/`，以及 18 个 `shared/events/schemas/*.v1.json`、B1/event-local enum refs、`shared/events/baseline/events.v1.json`、`shared/jobs/baseline/jobs.v1.json`。
- 根 `Makefile` 接入 `codegen-events`，`make codegen` 顺序升级为 `codegen-conventions -> codegen-events -> codegen-openapi`，并新增 baseline-only `lint-events` gate。
- 验证：`go test ./cmd/codegen/events -count=1` focused cases、`go test ./internal/shared/events ./internal/shared/jobs -count=1`、`pnpm --dir frontend test src/lib/events src/lib/jobs`、`make codegen-events`、`make lint-events` 均通过。

### 关联 Commit

- `feat(event-outbox): wire event contract generator`

### 备注

Auto-committed by /tdd phase-commit, plan: event-and-outbox-contract/001-bootstrap

## 10:29 工作记录

### 完成事项

- Phase 2 完成 `shared/jobs.yaml` 真理源：落地 10 个 canonical jobType、Asynq dotted task 映射、owner / priority / triggerEvent、7 项 API-facing subset，以及 `email_dispatch` payload allowlist + redacted blacklist。
- 扩展 `events_inventory.py` 支持 jobs 校验，覆盖 canonical 缺项、dotted name 笔误、priority 非法、triggerEvent 漂移、API-facing subset 误扩、internal-only job 误暴露与 `email_dispatch` 红线。
- 验证：`python3 -m pytest scripts/lint/events_inventory_test.py -q` 与 `python3 scripts/lint/events_inventory.py shared/events.yaml shared/jobs.yaml` 通过。

### 关联 Commit

- `feat(event-outbox): add canonical jobs truth source`

### 备注

Auto-committed by /tdd phase-commit, plan: event-and-outbox-contract/001-bootstrap

## 10:28 工作记录

### 完成事项

- Phase 1 完成 `db-migrations-baseline/001-bootstrap` migration skeleton 与工具 wrapper：新增 `backend/cmd/migrate` 入口、`backend/internal/migrations` CLI/runner、`golang-migrate v4.18.3` 接入、root `make migrate-*` targets、migration file naming lint 与 create helper。
- 落地 `migrations/000001_create_baseline.{up,down}.sql` 初版，up 幂等启用 `vector` extension，down 默认保留 extension；`MIGRATE_DROP_EXTENSIONS=1 APP_ENV=dev` 时由 wrapper 显式 drop extension。
- 落地 `schema_backfills` ledger、`migrations/backfill/manifest.yaml` 与 `backend/internal/migrations/backfills/v000001` no-op registry 示例，覆盖 dry-run/apply ledger、重复 success skip 与 prod force 拒绝。
- 补齐 `db-migrations-baseline/001-bootstrap` plan 的 TDD/BDD 质量门禁分类与 checklist 可执行验证断言，Phase 1 的 5 个 checklist item 已勾选。
- 验证：`go test ./cmd/migrate ./internal/migrations/... -count=1`、`python3 scripts/lint/migrations_lint.py --repo-root .`、`git diff --check` 通过；临时 pgvector Postgres 上验证 `make migrate-up`、默认 `make migrate-down` 保留 `vector`、`MIGRATE_DROP_EXTENSIONS=1 APP_ENV=dev make migrate-down` 后 extension count 为 0，重复 `make migrate-up` 后 `schema_backfills` 仅保留 dry_run/apply 两条 succeeded。

### 关联 Commit

- `feat(db-migrations): land migration skeleton and wrapper`

### 备注

Auto-committed by /tdd phase-commit, plan: db-migrations-baseline/001-bootstrap

## 10:41 工作记录

### 完成事项

- Phase 3 完成 enum/check 来源、backfill 幂等与 privacy dry-run：新增 `migrations/enum-sources.yaml`，逐列登记 baseline SQL 中所有 `CHECK (... IN (...))` 约束的 source / values / checksum。
- 扩展 `scripts/lint/migrations_lint.py`，在 file contract 外新增 enum-source drift 检查与明文 secret 字段红线；pytest 覆盖 registered check、unregistered check、checksum drift、`raw_token` 失败与 `*_hash` allowlist。
- 补充 backfill force 行为测试：重复 success 默认 skip，`ForceBackfill` 在 dev 可重跑并更新 ledger，`APP_ENV=prod` 被拒绝；实际 DB 重复 `make migrate-up` 后 dry_run/apply succeeded 仍各 1。
- 补充 privacy matrix 覆盖测试，并验证 `make privacy-delete-dry-run` 输出覆盖用户、auth/session、AI、async/outbox、prompt/rubric 与 migration metadata disposition。
- 验证：`go test ./backend/internal/migrations -count=1`、`python3 -m pytest scripts/lint/migrations_lint_test.py -q`、`python3 scripts/lint/migrations_lint.py --repo-root .` 均通过。

### 关联 Commit

- `feat(db-migrations): add enum lint and privacy dry-run`

### 备注

Auto-committed by /tdd phase-commit, plan: db-migrations-baseline/001-bootstrap

## 10:33 工作记录

### 完成事项

- Phase 2 完成 baseline DDL 与索引验证：确认 03-db-definition 27 张 P0 应用表、ADR-Q1 3 张 auth/session 支撑表、`schema_migrations` 与 `schema_backfills` 均在 clean Postgres 上存在，public base table count 为 32。
- 补充 `sql_contract_test.go` 静态契约测试，断言 baseline migration 定义完整表清单且不包含 `raw_token` / `session_cookie` / `api_key` / `provider_token` 等明文 secret 字段。
- 通过 SQL probes 验证 `outbox_events` retry columns 与 pending due 索引、`async_jobs.job_type` 10 项 DB canonical check 与 B2 7 项 API-facing subset、`ai_task_runs` typed columns 与 dashboard index-only query、03 §7 B-Tree / ivfflat / GIN 索引覆盖。
- 验证：`go test ./backend/internal/migrations -count=1`、`python3 scripts/lint/migrations_lint.py --repo-root .`、临时 pgvector Postgres `information_schema` table count 32 均通过。

### 关联 Commit

- `test(db-migrations): verify baseline ddl and indexes`

### 备注

Auto-committed by /tdd phase-commit, plan: db-migrations-baseline/001-bootstrap

## 10:24 工作记录

### 完成事项

- Phase 1 完成 `shared/events.yaml` 事件真理源：落地 envelope 8 字段、18 个 v1 internal eventName、required payload 字段清单、event-local enum 与 PII 边界。
- 新增 `scripts/lint/events_inventory.py` 与 pytest contract tests，覆盖 envelope 字段、UUIDv7 alias、producer enum、18 事件完整性、payload drift、event-local enum 与 dot.case/domain/past-tense 规则。
- 补齐 `event-and-outbox-contract/001-bootstrap` plan 的 TDD/BDD 质量门禁分类与 checklist 可执行验证断言，并同步 `plans/INDEX.md`。
- 验证：`python3 -m pytest scripts/lint/events_inventory_test.py -q` 与 `python3 scripts/lint/events_inventory.py shared/events.yaml shared/conventions.yaml` 通过。

### 关联 Commit

- `feat(event-outbox): land events truth source`

### 备注

Auto-committed by /tdd phase-commit, plan: event-and-outbox-contract/001-bootstrap

## 08:00 工作记录

### 完成事项

- 落地 `shared-conventions-codified/002-codegen-pipeline` 交付复盘报告 `docs/reports/2026-04-30-shared-conventions-codified-002-codegen-pipeline-assessment.md`。
- 报告总结本轮 3 个主要阻点：旧 active plan 缺质量门禁、session branch 与当前分支不一致、negative drift 仍需手工临时编辑 generated outputs。
- 在 `docs/reports/INDEX.md` 登记新报告；报告建议后续补 active plan quality-gate sweep、conventions drift negative helper，以及 `/implement` branch override 记录规则。

### 关联 Commit

- `docs(reports): land shared-conventions 002 retrospective`

### 备注

`/retrospective --this` close-out report for shared-conventions-codified/002-codegen-pipeline.

## 07:59 工作记录

### 完成事项

- Phase 6 完成最终 verification：`make codegen-conventions`、`make codegen-check`、`go test ./cmd/codegen/conventions ./internal/shared/... -count=1`、`pnpm --dir frontend test src/lib/conventions src/lib/ids`、`pnpm --dir frontend typecheck` 全部通过。
- 完成三类 AI vocabulary negative drift gate：YAML-only drift 同时指向 Go/TS AI outputs；Go-only drift 指向 `backend/internal/shared/ai/vocabulary.go`；TS-only drift 指向 `frontend/src/lib/conventions/ai.ts`；恢复后 `git diff --check` 与 `make codegen-check` 通过。
- 将 `shared-conventions-codified/002-codegen-pipeline` plan 与 checklist Header 切换为 `completed`，`plans/INDEX.md` 从 Active 移到 Completed，13/13 checklist items 全部完成。
- 验证：`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 通过，Header / INDEX zero drift；git diff 不包含 A5 workflow 或 F3 prompt registry scope。

### 关联 Commit

- `docs(shared-conventions): complete codegen pipeline plan`

### 备注

Auto-committed by /tdd phase-commit, plan: shared-conventions-codified/002-codegen-pipeline

## 07:56 工作记录

### 完成事项

- Phase 5 收口 future handoff guard：确认 `shared-conventions-codified/002-codegen-pipeline` 仅记录 F3 `feature_key + version` prompt registry bridge 与 A5 remote CI drift detection 为 future scope。
- 验证未提前实现 F3 prompt bridge：plan/spec grep 只出现 handoff 文案，`backend/`、`frontend/`、`shared/`、`scripts/` 中没有 `RegistryClient`、`GetPrompt`、prompt body 或 F3 SDK 实现。
- 验证未提前实现 remote CI：`.github/workflows` 不存在，git diff 不包含 A5 workflow / required-check / remote CI 配置。
- 验证：`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 通过，Header / INDEX zero drift。

### 关联 Commit

- `docs(shared-conventions): confirm future handoff boundaries`

### 备注

Auto-committed by /tdd phase-commit, plan: shared-conventions-codified/002-codegen-pipeline

## 07:54 工作记录

### 完成事项

- Phase 4 落地 `shared/fixtures/conventions-parity.json`，锁定 14 个生成枚举类型字面量集合、9 个错误码（含 A3 `AI_*` baseline）、13 个 AI vocabulary 字段，以及 `PageInfo` / `ApiError` canonical JSON shape。
- 新增 Go parity tests：`backend/internal/shared/types/conventions_parity_test.go` 读取共享 fixture，断言 Go enums、`sharederrors.AllCodes`、`sharedai.AllFieldNames` 与 serialization shape 一致。
- 新增 TS parity tests：`frontend/src/lib/conventions/conventions-parity.test.ts` 读取同一 fixture，断言 TS enums、`ALL_ERROR_CODES`、`ALL_AI_VOCABULARY_FIELDS` 与 `JSON.stringify` 后的 `PageInfo` / `ApiError` shape 一致。
- 验证：`go test ./internal/shared/types ./internal/shared/errors ./internal/shared/ai -count=1`、`pnpm --dir frontend test src/lib/conventions/ai.test.ts src/lib/conventions/conventions-parity.test.ts`、`pnpm --dir frontend typecheck`、`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 均通过。

### 关联 Commit

- `test(shared-conventions): add cross-language parity fixture`

### 备注

Auto-committed by /tdd phase-commit, plan: shared-conventions-codified/002-codegen-pipeline

## 07:50 工作记录

### 完成事项

- Phase 3 落地 Go / TS AI vocabulary parity tests：Go `backend/internal/shared/ai/vocabulary_test.go` 覆盖字段集合顺序、snake_case wire names 与 `IsFieldName` helper；TS `frontend/src/lib/conventions/ai.test.ts` 覆盖 barrel 导出、常量名、wire names 与 validation helper。
- 显式断言 A3 当前消费的 7 个字段：`model_profile_name`、`model_profile_version`、`model_family`、`fallback_chain`、`route`、`validation_status`、`output_schema_version`。
- 更新 `frontend/src/lib/conventions/index.ts`，让下游按既有 conventions barrel 入口导入 AI vocabulary。
- 验证：`go test ./internal/shared/ai -count=1`、`pnpm --dir frontend test src/lib/conventions/ai.test.ts`、`pnpm --dir frontend typecheck`、`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 均通过。

### 关联 Commit

- `test(shared-conventions): add ai vocabulary parity tests`

### 备注

Auto-committed by /tdd phase-commit, plan: shared-conventions-codified/002-codegen-pipeline

## 07:47 工作记录

### 完成事项

- Phase 2 落地 `scripts/lint/conventions_drift.py`，通过临时渲染 `shared/conventions.yaml` 并比较 10 个 B1 generated outputs，覆盖 enum、error code、pagination/API structure、ID convention 与 AI vocabulary 的 Go/TS drift。
- 将 conventions drift 接入 `make codegen-check`：先检查当前 generated outputs 是否相对 YAML 漂移，再重跑 codegen/openapi/lint，最后对 OpenAPI 与 B1 generated paths 执行 `git diff --exit-code`。
- 验证 AI vocabulary negative drift：临时手改 Go/TS AI generated 文件时，`make codegen-check` 失败并明确输出 `backend/internal/shared/ai/vocabulary.go` 与 `frontend/src/lib/conventions/ai.ts`；恢复后 `make codegen-check` 通过。
- 验证：`python3 -m pytest scripts/lint/conventions_drift_test.py -q`、`python3 scripts/lint/conventions_drift.py --repo-root .`、`go test ./cmd/codegen/conventions -run TestRunFromBytes_Idempotent -count=1`、`make codegen-check`、`python3 .agent-skills/sync-doc-index/scripts/sync-doc-index.py --check` 均通过。

### 关联 Commit

- `feat(shared-conventions): wire conventions drift gate`

### 备注

Auto-committed by /tdd phase-commit, plan: shared-conventions-codified/002-codegen-pipeline
