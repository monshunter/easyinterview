# 2026-04-30 工作日志

## 00:00 工作记录

### 完成事项

- 收紧 `aiclient.New` 的 fail-fast 矩阵以对齐 plan 4.1 / checklist 4.2：`AppEnv=test` 必须显式 `WithStubAllowed(true)` 才能在缺 gateway 配置时构造成功；`AppEnv != test` 任意路径缺 `GatewayBaseURL` 或 `GatewayAPIKey` 一律 `ErrMissingGatewayConfig`；`WithStubAllowed(true)` 在 non-test 路径不生效，绝不静默回退到 stub。
- 落地 `config_test.go` 8 个 case 完整覆盖矩阵：test+stub-allowed→OK / test+missing→FAIL / test+gateway-set→OK / production+missing→FAIL / production+stub-allowed-but-missing→FAIL / production+gateway→OK / staging+missing-api-key→FAIL / staging+missing-base-url→FAIL。
- 同步更新现有 stub 路由测试 (`aiclient_test.go`、`observability/decorator_test.go`) 加 `aiclient.WithStubAllowed(true)`，避免新规则误判已有用例。
- 落地 `backend/internal/ai/aiclient/README.md` 包级 README：覆盖 zero-vendor-SDK 红线、AICallMeta ownership、隐私红线引用 `observability/privacy_test.go`、stub 激活矩阵表（5 行）、本地部署所需 env 变量 + smoke 验证步骤（`go test -tags smoke`，禁止嵌入真实 API key）、polling reloader 与 fsnotify fallback 升级路径、cmd/api / cmd/worker 接入示例代码。
- 验证：`go test ./internal/ai/aiclient/... -count=1` 全绿（5 packages）。

### 关联 Commit

- `feat(aiclient): fail-fast matrix 与包级 README`

### 备注

Auto-committed by /tdd phase-commit, plan: ai-gateway-and-model-routing/001-aiclient-and-profile-bootstrap. Phase 4.3 README 中暂记录 polling reloader 为 plan §2.3 fsnotify fallback 路径，A4 secrets-and-config 后续推进 fsnotify 直依赖时可平滑切换 `profile.Loader` 实现而不触动公共 API。
