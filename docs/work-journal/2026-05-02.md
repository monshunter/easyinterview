# 2026-05-02 工作日志

## 10:53 工作记录

### 完成事项

- 根据 `ui-design/run.sh` 启动后的最新静态 UI 内容，重新同步 `docs/ui-design/` 文档，补齐顶栏主题色、暗色模式、语言切换、设置页字体预设、公司情报、面试官角色、报告 Summary Cards 和真实复盘面试官反应等当前页面能力。
- 梳理 `src/app.jsx` 的 `routeAliases`、`normalizeRoute` 与 `screens` 映射，明确 `welcome`、`growth`、`plan`、`mistakes`、`drill`、`followup`、`experiences`、`star` 等旧 route 已折返，不再代表目标模块。
- 在 `removed-modules-and-scope.md`、`module-map.md`、`ui-architecture.md`、简历模块和首次无简历引导文档中补充废弃 / 历史组件边界，覆盖旧 `WelcomeScreen`、`MistakesScreen`、`GrowthScreen`、`PlanScreen`、`ExperienceLibraryScreen`、`StarEditorScreen`、旧报告变体、旧 `ResumeScreen` 和旧 `OnboardingScreen`。
- 同步更新 `docs/ui-design/README.md` 和 `INDEX.md`，并完成 Header / INDEX 一致性校验与 `git diff --check` 检查。

### 关联 Commit

- `docs(ui-design): sync prototype docs and deprecated scope`

### 备注

本次只更新文档，不修改 `ui-design` 静态 UI 源码。`docs/ui-design` 的目标判断以 TopBar、`normalizeRoute` 后的 `activeRouteName` 和实际渲染内容为准，不以源码中仍保留的历史组件为准。

## 12:09 工作记录

### 完成事项

- 根据 `docs/ui-design/` 的保留 / 移除边界清理 `ui-design` 静态原型源码，删除旧 `screens-rest.jsx`、`screens-completion.jsx`，移除独立错题本、旧欢迎页、单题 Drill、追问树、成长中心、旧简历单页、旧 onboarding、旧报告 Editorial / Timeline 等废弃页面入口。
- 同步清理 `app.jsx` 路由注册、历史 mock data、`index.html` 脚本入口和 `canvas.html` 画板，保留旧 route 通过 `routeAliases` 折回当前目标模块。
- 将面向用户的“真实复盘”命名收敛为“复盘”，同步顶栏导航、首页入口、复盘页标题、设置 / 画像 / 认证相关文案、canvas 标签和 `docs/ui-design` 文档索引。
- 通过 `run.sh` 启动静态页，使用 `agent-browser` 验证首页、工作台、复盘页、兼容旧 route 和设计画板；同时完成 `git diff --check`、废弃符号 grep 和 `ui-design` Header / INDEX 一致性校验。

### 关联 Commit

- `refactor(ui): clean deprecated prototype screens and debrief label`

### 备注

本次将 `ui-design/` 和 `docs/ui-design/` 作为同一交付边界提交：源码清理和文档同步互为一致性要求，不拆分为独立文档提交。

## 12:56 工作记录

### 完成事项

- 修复模拟面试规划页的历史列表范围，将 `ui-design/src/screen-workspace.jsx` 中的模拟面试历史改为基于当前 `MockInterviewPlan` / `TargetJob` / 当前轮次生成，避免混入其他公司、岗位或 JD 的会话历史。
- 修复复盘页上下文交互，将 `目标岗位 / JD`、`关联模拟面试`、`绑定简历` 三个动作改为当前页选择弹窗，并移除复盘页与报告生成页残留的 `收件箱` / `Inbox` 旧文案。
- 新增 `ui-design/ui-design-contract.test.mjs`，覆盖当前规划历史取数、复盘上下文不跨页跳转、UI 源码不得暴露收件箱文案。
- 同步更新 `docs/ui-design/` 目标文档、`docs/reports/` 交付复盘报告和索引，并完成相关验证。

### 关联 Commit

- `fix(ui): scope mock history and debrief context pickers`

### 备注

已执行 `node --test ui-design/ui-design-contract.test.mjs`、`sync-doc-index --check`、`check_md_links.py docs/ui-design`、`check_md_links.py docs/reports`、`git diff --check` 和 `esbuild` JSX 解析验证。当时全局 `docs` 链接检查仍有既有顶层 UI 目录断链，未纳入该次修复范围。

## 13:41 工作记录

### 完成事项

- 将用户已迁移的 `easyinterview-ui/` 与 `docs/ui-refine/` 命名收口为 `ui-design/` 与 `docs/ui-design/`，同步修订全仓脚本、OpenAPI fixture 同步入口、报告、spec、工作日志和文档导航引用。
- 将 `docs/ui-design/` 下所有 `refined-*.md` 文件改为无 `refined-` 前缀命名，并同步更新目录规范、索引和跨文档链接。
- 将静态 UI 入口统一为 `index.html`，设计画板统一为 `canvas.html`，并修订 `ui-design/run.sh`、`scripts/local/deploy-ui.sh`、`canvas.html` iframe 入口和相关历史说明。
- 补回 `ui-design/src/data.jsx` 中供 OpenAPI prototype-baseline 同步使用的 `experiences`、`mistakes`、`growth` 数据节，避免路径迁移后 fixture 同步脚本失效。
- 完成坏链和执行验证：`check_md_links.py docs`、`check_md_links.py openapi`、`node --test ui-design/ui-design-contract.test.mjs`、`python3 -m unittest scripts.codegen.sync_fixtures_from_prototype_test`、`bash -n`、`esbuild` JSX 解析、`run.sh` 静态服务 smoke 和 `git diff --check` 均通过。

### 关联 Commit

- `chore(ui): migrate prototype and docs to ui-design`

### 备注

本次作为同一迁移边界提交：目录重命名、文档引用、脚本默认路径、fixture 同步默认数据源和验证证据需要保持一致，避免留下可执行坏链或历史旧入口。

## 15:47 工作记录

### 完成事项

- 新增静态 UI 简历工坊 IA，`screen-resume-workshop.jsx` 覆盖旧 `ResumeVersionsScreen`，支持按原始简历树分组、按版本平铺、创建原始简历、从已有树分叉岗位定制版本，以及版本详情中的预览 / 改写建议 / 手动编辑。
- 将旧 `screens-p1-depth.jsx::ResumeVersionsScreen` 收敛为 `_LegacyResumeVersionsScreen` dead code，并在 `index.html` 接入新的简历工坊 screen，避免旧单页版本工坊继续驱动运行时目标。
- 同步更新 `docs/ui-design/` 的总体架构、用户流程、模块地图、简历模块、多 JD 多简历、首次无简历、认证边界、移除范围和索引，明确新简历工坊为目标真理源。
- 完成验证：`node --test ui-design/ui-design-contract.test.mjs`、`check_md_links.py docs/ui-design`、`sync-doc-index --check`、`git diff --check` 和 `esbuild` JSX 解析均通过。

### 关联 Commit

- `feat(ui): add resume workshop ia`

### 备注

本次将 `ui-design/` 静态页面变更与 `docs/ui-design/` 文档同步作为同一交付边界提交，确保简历模块运行时 IA 与设计文档保持一致。

## 16:15 工作记录

### 完成事项

- 修订简历工坊 UX：岗位定制版本从列表打开时默认进入 `改写建议`，主版本仍进入 `预览`，让用户先处理投递前最关键的改写决策。
- 恢复并接入原始简历预览弹层，支持查看来源关系、原始文件视图和解析文本快照，补齐原件可追溯链路。
- 为导出 PDF、复制纯文本、复制为新版本、保存改动、创建版本和确认保存 v1 增加可见反馈或当前列表态变化，避免静态原型出现空动作。
- 新增简历工坊契约测试，覆盖默认标签、原件预览、导出/复制和创建动作数据变更；同步更新 `docs/ui-design/` 与交付复盘报告。
- 完成验证：`node --test ui-design/ui-design-contract.test.mjs`、`esbuild` JSX 编译、`check_md_links.py docs/ui-design`、`check_md_links.py docs/reports`、`sync-doc-index --check`、`git diff --check` 和 `bash -n ui-design/run.sh` 均通过；`bash -n` 仅有本机 locale warning。

### 关联 Commit

- `fix(ui): restore resume workshop feedback flows`

### 备注

本次变更跨 `ui-design/` 与 `docs/`，但属于同一简历工坊 UX 修订边界：静态页面、契约测试、设计文档和交付复盘需要在同一次提交中保持一致。

## 21:48 工作记录

### 完成事项

- 修复 `ui-design` 静态 UI 的 P0 上下文与报告闭环问题，补齐 `InterviewContext` 贯通、认证 pending action 恢复、报告缺失 / 失败状态、公司情报公开信号文案和当前面试规划历史范围。
- 恢复语音面试在 `PracticeScreen` 共享外层骨架内的专属 Surface，保留正在说话波形、本次回答标注波形、实时转写、表达层指标、现场提示和音频缓存说明，并将旧 `voice` route 折回 `practice?mode=voice&modality=voice`。
- 修正报告页 `复练当前轮` 和 `进入下一轮`，两条 CTA 都直接进入对应面试 session；`返回面试前确认` 作为唯一回到准备页的动作保留。
- 新增和扩展 `ui-design/ui-design-contract.test.mjs`，覆盖 P0 上下文路由、报告 session 守卫、报告 CTA 直达面试、认证恢复、语音 Surface 和旧 `voice` route 折返。
- 同步更新 `docs/ui-design/`、`docs/bugs/` 和 `docs/reports/`，新增 BUG-0004、BUG-0005 及对应交付复盘报告。

### 关联 Commit

- `fix(ui-design): align practice voice and report flows`

### 备注

本次按用户要求未提交 `EasyInterview_Spec_and_P1_Docs_v1.1/`；该目录仍保留在工作区，作为后续单独处理的外部规格同步内容。
