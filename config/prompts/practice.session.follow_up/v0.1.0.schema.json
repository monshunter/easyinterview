{
  "type": "object",
  "description": "Follow-up mock-interview question generated from the latest answer.",
  "required": [
    "questionText",
    "questionIntent"
  ],
  "properties": {
    "questionText": {
      "type": "string",
      "description": "Follow-up question text shown to the candidate."
    },
    "questionIntent": {
      "type": "string",
      "description": "Short intent label for the follow-up question."
    },
    "branchDimension": {
      "type": "string",
      "description": "Optional rubric dimension or branch reason for the follow-up."
    },
    "confidence": {
      "type": "number",
      "description": "Optional confidence score for the follow-up choice."
    }
  }
}
