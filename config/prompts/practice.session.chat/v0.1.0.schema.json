{
  "type": "object",
  "description": "One ordinary assistant message in a continuous practice conversation.",
  "required": ["messageText"],
  "properties": {
    "messageText": {
      "type": "string",
      "description": "Assistant message shown to the user."
    }
  }
}
