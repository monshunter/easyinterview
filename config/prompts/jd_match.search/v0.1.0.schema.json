{
  "type": "array",
  "description": "Ranked JD-Match search results from the internal jobs pool.",
  "items": {
    "type": "object",
    "description": "One matched internal job reference.",
    "required": [
      "jobMatchId"
    ],
    "properties": {
      "jobMatchId": {
        "type": "string",
        "description": "Stable identifier from the internal jobs pool."
      },
      "reason": {
        "type": "string",
        "description": "Optional concise relevance reason for diagnostics."
      }
    }
  }
}
