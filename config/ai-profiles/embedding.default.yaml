name: embedding.default
capability: embed
status: active
default:
  provider_ref: default-openai-compatible
  model: default-embed
fallback: []
timeout_ms: 10000
rate_limit:
  rps: 10
  tpm: 120000
route: embedding.default
version: 1.0.0
