name: report.generate.default
capability: chat
status: active
default:
  provider_ref: default-openai-compatible
  model: default-chat
  params:
    temperature: 0.2
fallback: []
timeout_ms: 30000
max_tokens: 4096
rate_limit:
  rps: 3
  tpm: 60000
route: report.generate
version: 1.0.0
