name: practice.turn_observe.default
capability: chat
status: active
default:
  provider_ref: default-openai-compatible
  model: default-chat
  params:
    temperature: 0.3
fallback: []
timeout_ms: 8000
max_tokens: 512
rate_limit:
  rps: 8
  tpm: 60000
route: practice.turn_observe
version: 1.0.0
