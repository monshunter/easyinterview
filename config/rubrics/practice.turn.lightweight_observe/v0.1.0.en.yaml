feature_key: "practice.turn.lightweight_observe"
version: "v0.1.0"
language: "en"
dimensions:
  - name: "practice_signal_strength"
    weight: 0.4
    description: "The cue or generated artifact surfaces a high-signal moment rather than commentary."
    score_levels:
      - label: "weak"
        threshold: 0.0
        description: "Falls clearly short of the dimension; the artifact would not satisfy the user goal."
      - label: "developing"
        threshold: 0.4
        description: "Partially satisfies the dimension; still has obvious gaps a reviewer would call out."
      - label: "proficient"
        threshold: 0.7
        description: "Meets the dimension at production-baseline quality with only minor refinements."
      - label: "strong"
        threshold: 0.9
        description: "Exceeds the dimension's expectation; an experienced reviewer would highlight the work."
  - name: "practice_clarity"
    weight: 0.4
    description: "Output is unambiguous and parseable without re-reading."
    score_levels:
      - label: "weak"
        threshold: 0.0
        description: "Falls clearly short of the dimension; the artifact would not satisfy the user goal."
      - label: "developing"
        threshold: 0.4
        description: "Partially satisfies the dimension; still has obvious gaps a reviewer would call out."
      - label: "proficient"
        threshold: 0.7
        description: "Meets the dimension at production-baseline quality with only minor refinements."
      - label: "strong"
        threshold: 0.9
        description: "Exceeds the dimension's expectation; an experienced reviewer would highlight the work."
  - name: "language_consistency"
    weight: 0.2
    description: "Output language matches the requested locale and uses consistent terminology."
    score_levels:
      - label: "weak"
        threshold: 0.0
        description: "Falls clearly short of the dimension; the artifact would not satisfy the user goal."
      - label: "developing"
        threshold: 0.4
        description: "Partially satisfies the dimension; still has obvious gaps a reviewer would call out."
      - label: "proficient"
        threshold: 0.7
        description: "Meets the dimension at production-baseline quality with only minor refinements."
      - label: "strong"
        threshold: 0.9
        description: "Exceeds the dimension's expectation; an experienced reviewer would highlight the work."
