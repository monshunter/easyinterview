feature_key: "debrief.suggest_questions"
version: "v0.1.0"
language: "multi"
dimensions:
  - name: "debrief_recall_completeness"
    weight: 0.4
    description: "Suggested questions cover the likely interview stages and the user's available context."
    score_levels:
      - label: "weak"
        threshold: 0.0
        description: "Falls clearly short of the dimension; the artifact would not satisfy the user goal."
      - label: "developing"
        threshold: 0.4
        description: "Partially satisfies the dimension; still has obvious gaps a reviewer would call out."
      - label: "proficient"
        threshold: 0.7
        description: "Meets the dimension at production-baseline quality with only minor refinements."
      - label: "strong"
        threshold: 0.9
        description: "Exceeds the dimension's expectation; an experienced reviewer would highlight the work."
  - name: "debrief_lesson_specificity"
    weight: 0.35
    description: "Rationales explain the signal behind each suggested question."
    score_levels:
      - label: "weak"
        threshold: 0.0
        description: "Falls clearly short of the dimension; the artifact would not satisfy the user goal."
      - label: "developing"
        threshold: 0.4
        description: "Partially satisfies the dimension; still has obvious gaps a reviewer would call out."
      - label: "proficient"
        threshold: 0.7
        description: "Meets the dimension at production-baseline quality with only minor refinements."
      - label: "strong"
        threshold: 0.9
        description: "Exceeds the dimension's expectation; an experienced reviewer would highlight the work."
  - name: "language_consistency"
    weight: 0.25
    description: "Output language matches the requested locale and uses stable source labels."
    score_levels:
      - label: "weak"
        threshold: 0.0
        description: "Falls clearly short of the dimension; the artifact would not satisfy the user goal."
      - label: "developing"
        threshold: 0.4
        description: "Partially satisfies the dimension; still has obvious gaps a reviewer would call out."
      - label: "proficient"
        threshold: 0.7
        description: "Meets the dimension at production-baseline quality with only minor refinements."
      - label: "strong"
        threshold: 0.9
        description: "Exceeds the dimension's expectation; an experienced reviewer would highlight the work."
