feature_key: "practice.session.follow_up"
version: "v0.1.0"
language: "multi"
dimensions:
  - name: "followup_relevance"
    weight: 0.4
    description: "Follow-up question targets the candidate's actual gap or signal rather than recycling the prior turn."
    score_levels:
      - label: "weak"
        threshold: 0.0
        description: "Falls clearly short of the dimension; the artifact would not satisfy the user goal."
      - label: "developing"
        threshold: 0.4
        description: "Partially satisfies the dimension; still has obvious gaps a reviewer would call out."
      - label: "proficient"
        threshold: 0.7
        description: "Meets the dimension at production-baseline quality with only minor refinements."
      - label: "strong"
        threshold: 0.9
        description: "Exceeds the dimension's expectation; an experienced reviewer would highlight the work."
  - name: "practice_depth"
    weight: 0.3
    description: "Question or follow-up probes deeply into the candidate's reasoning rather than staying on the surface."
    score_levels:
      - label: "weak"
        threshold: 0.0
        description: "Falls clearly short of the dimension; the artifact would not satisfy the user goal."
      - label: "developing"
        threshold: 0.4
        description: "Partially satisfies the dimension; still has obvious gaps a reviewer would call out."
      - label: "proficient"
        threshold: 0.7
        description: "Meets the dimension at production-baseline quality with only minor refinements."
      - label: "strong"
        threshold: 0.9
        description: "Exceeds the dimension's expectation; an experienced reviewer would highlight the work."
  - name: "language_consistency"
    weight: 0.3
    description: "Output language matches the requested locale and uses consistent terminology."
    score_levels:
      - label: "weak"
        threshold: 0.0
        description: "Falls clearly short of the dimension; the artifact would not satisfy the user goal."
      - label: "developing"
        threshold: 0.4
        description: "Partially satisfies the dimension; still has obvious gaps a reviewer would call out."
      - label: "proficient"
        threshold: 0.7
        description: "Meets the dimension at production-baseline quality with only minor refinements."
      - label: "strong"
        threshold: 0.9
        description: "Exceeds the dimension's expectation; an experienced reviewer would highlight the work."
