# test environment override (secrets-and-config spec §2.1).
# Loaded after config/config.yaml; env vars and runtime secrets still win.
# Secret fields MUST remain empty here.

ai:
  debugPrintRawOutput: true
