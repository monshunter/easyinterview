# E2E.P0.044 continuous practice conversation

Given an authenticated running session, the page renders ordered assistant/user messages in one full-width chat window. Sending text loads the same complete resume source snapshot used at start, preserves its tail marker in the AI payload, persists one user/assistant pair, and refreshes the same session.
