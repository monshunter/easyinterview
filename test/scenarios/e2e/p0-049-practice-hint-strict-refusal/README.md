# E2E.P0.049 — Practice Hint Strict Refusal

Given a strict practice session in any supported goal, when the user requests a hint, then the API returns `409 PRACTICE_SESSION_CONFLICT` and replay is stable.
