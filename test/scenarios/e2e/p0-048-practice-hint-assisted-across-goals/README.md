# E2E.P0.048 — Practice Hint Assisted Across Goals

Given an assisted practice session in any supported goal, when the user requests a hint, then the API returns `show_hint`, records `hint_generate`, and does not advance the turn lifecycle.
