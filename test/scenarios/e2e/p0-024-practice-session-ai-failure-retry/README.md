# E2E.P0.024 practice opening failure and retry

An AI timeout fails the reserved session start without emitting a success event. A retry can reserve again and persist exactly one opening assistant message.
