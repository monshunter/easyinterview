# Expected outcome

The selected structured-round duration is persisted on PracticePlan and displayed as the Top Bar budget (for example, 60 minutes becomes `60:00`). Failed plan reads display `--:--`; fixed `25:00` is absent. One full-width chat remains visible, obsolete controls are absent, and voice cannot be clicked.
