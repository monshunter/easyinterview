# Seed input

Three isolated running conversations: opening-only, one candidate message awaiting an assistant reply, and one answered candidate message. The successful case uses a canonical two-round TargetJob, a bound Resume, and a version-18 `feedback_reports.generation_context` column.
