# E2E.P0.023 practice session opening message

Starting a ready plan loads the complete resume context with `parsed_text_snapshot → original_text → structured_profile` precedence, preserves a long-input tail marker in `practice.session.chat`, persists one assistant opening message, emits lifecycle-only session-started evidence, and returns ordered messages.
