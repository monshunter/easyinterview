# E2E.P0.023 practice session opening message

Starting a ready plan resolves `practice.session.chat`, persists one assistant opening message, emits lifecycle-only session-started evidence, and returns a running session whose `messages` are ordered by `seqNo`.
