# Seed input

- Ready retry-first report with `retryFocusDimensionCodes=[]` for generic same-round practice.
- Ready retry-first report with one `needs_work` dimension, a same-code issue and the same code in `retryFocusDimensionCodes`.
- Ready next-first report with frozen `context.hasNextRound=true`, plus terminal `hasNextRound=false` and malformed-focus variants.
- Deterministic generated-client responses for a server-derived practice plan and fresh session.
- Repeated-click and signed-out pending-action states.
