# E2E.P0.050 — Practice Hint Provenance Task Runs

Given an assisted hint action, when the response is serialized, then wire provenance has only B2 fields and runtime provenance remains in `ai_task_runs`.
