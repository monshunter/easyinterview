# E2E.P0.007 voice-disabled boundary

Given the practice page and authenticated voice endpoint, the phone entry is rendered as a native disabled button and `createPracticeVoiceTurn` returns `422 AI_UNSUPPORTED_CAPABILITY`. No voice controller, provider call, or persistence path is active.
